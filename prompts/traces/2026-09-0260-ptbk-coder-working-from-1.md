# Run trace of `prompts/2026-09-0260-ptbk-coder-working-from.md#1`

-   **Prompt:** The Promptbook coder agents should resolve `FROM` correctly.
-   **Prompt section:** 1 of 2
-   **Outcome:** Succeeded
-   **Runner:** Promptbook Developer on OpenAI Codex `gpt-6-astra` thinking `max` (ChatGPT account)
-   **Attempts:** 1
-   **Steps:** Implementation ~$0.4753 31 minutes; Testing 25 minutes
-   **Verification command:** `npm run test-for-ptbk-coder`
-   **Started:** 2026-09-17T00:05:39.457Z
-   **Finished:** 2026-09-17T01:02:18.037Z
-   **Duration:** an hour

## Runtime log

````text
=== runner shell started at 2026-09-17T00:05:40.714Z ===
Script path: /c/Users/me/work/ai/promptbook/.promptbook/coder-prompts/2026-09-0260-ptbk-coder-working-from-1.sh

--- raw input ---
if [ -n "${PTBK_AGENTS_SERVER_ENV_FILE:-}" ] && [ -f "${PTBK_AGENTS_SERVER_ENV_FILE}" ]; then
set -a
source "${PTBK_AGENTS_SERVER_ENV_FILE}"
set +a
elif [ -f .env ]; then
set -a
source .env
set +a
fi

CODEX_LOGIN_STATUS="$(
    unset OPENAI_API_KEY OPENAI_BASE_URL CODEX_API_KEY
    # 'codex login status' prints the "Logged in using ChatGPT" line to stderr, so merge stderr into stdout (2>&1) to capture it
    codex login status 2>&1 || true
)"
case "$CODEX_LOGIN_STATUS" in
    *"Logged in using ChatGPT"*)
        IS_CODEX_CHATGPT_LOGIN_ACTIVE=1
        ;;
    *)
        IS_CODEX_CHATGPT_LOGIN_ACTIVE=0
        ;;
esac

CODEX_LOGIN_METHOD=chatgpt
CODEX_LOGIN_METHOD_ARGUMENTS=(-c forced_login_method=chatgpt)
unset CODEX_API_KEY
if [ "$IS_CODEX_CHATGPT_LOGIN_ACTIVE" != "1" ] &&
    [ "${PTBK_OPENAI_CODEX_USE_API_KEY:-0}" = "1" ] &&
    [ -n "${OPENAI_API_KEY:-}" ]; then
    CODEX_LOGIN_METHOD_ARGUMENTS=(-c forced_login_method=api)
    CODEX_LOGIN_METHOD=api
    CODEX_API_KEY="${OPENAI_API_KEY}"
    export CODEX_API_KEY
fi

if [ "$IS_CODEX_CHATGPT_LOGIN_ACTIVE" = "1" ] ||
    [ "${PTBK_OPENAI_CODEX_USE_API_KEY:-0}" != "1" ] ||
    [ -z "${OPENAI_API_KEY:-}" ]; then
unset OPENAI_API_KEY
unset OPENAI_BASE_URL
fi

printf '%s %s\n' 'ptbk-codex-login-method:' "${CODEX_LOGIN_METHOD}"

codex \
    "${CODEX_LOGIN_METHOD_ARGUMENTS[@]}" \
    -c model_reasoning_effort="max" \
    --ask-for-approval never \
    exec --model gpt-6-astra \
    --local-provider none \
    --sandbox danger-full-access \
    -C /c/Users/me/work/ai/promptbook \
    --skip-git-repo-check \
    <<'CODEX_PROMPT'

## Your Task

The Promptbook coder agents should resolve `FROM` correctly.

```bash
ptbk coder run --harness openai-codex --model gpt-5.6-astra --thinking-level max --agent agents/coding/pavol-workshops.book --context AGENTS.md
```

**When there are multiple books:**

```
pavol-workshops.book
pavol.book
```

**And one of them is specified in the `FROM` clause:**

```
FROM @Pavol
```

It should read all the agents and resolve the `FROM` commitments correctly.

-   Same thing applies to `IMPORT` and `TEAM` commitments
-   All of theese should work:
    -   `FROM @Pavol`, `FROM {Pavol}` - The "Pavol" Is the name of the agent from the first line of its book source
        -   This book should be auto-finded in the repository, go recursively from the folder where the primary agent lives, in this case `agents/coding/pavol-workshops.book` - so folder `agents/coding/` will be searched.
    -   `FROM {./foo/bar/agent.book}`, `FROM {../foo/bar/agent.book}` - The path to the agent book is relative to the agent
    -   `FROM {foo/agent.book}` - The path relative to the cwd (current working directory)
    -   `FROM {http://example.com/agent.book}` - The path to the agent book can also be a URL
    -   `FROM @null`, `FROM {null}`, `FROM {void}`, `FROM @void` - Agent which event dont inherit from Adam agent
-   The agents are auto-inherited from Adam agent by default, if the Adam agent not existing, auto-create it in `.core` folder, in this case in `agents/coding/.core/adam.book`
    -   Simmilar logic is in the agent server code, just reuse the same code for the same logic and patterns
    -   Also `ptbk init` should auto-create the Adam agent in the `.core` folder together with `developer.book` which is already doing
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
    -   Same logic is working on the agent server. Reuse the same code for the same logic.
-   Do a proper analysis of the current functionality of `ptbk coder` and related functionality before you start implementing.
-   Also look and update [the dev scripts in `terminals.json`](.vscode/terminals.json)
-   You are working with [`ptbk coder`](src/cli/cli-commands/coder/run.ts)
-   Update the [`ptbk coder` landing website](apps/coder-landing) if there are any changes that affect the landing page.
-   Add the changes into the [changelog](changelog/_current-preversion.md)

## Your Behavior

You are Promptbook Developer

### Rules

-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Keep in mind the SOLID principles.
-   Do a proper analysis of the current functionality before you start implementing.
-   Keep small responsibilities of functions and classes, avoid creating big functions or classes that do many things.
-   When throwing errors, throw [branded errors](src/errors) and use `spaceTrim` utility to write clear and well-formatted multiline detailed error messages.

Output Format: errors as markdown, for example `variables` should be in backticks and important notes can be in bold.
-   Constants should always be `UPPER_SNAKE_CASE`.
-   Boolean variables should always be prefixed with `is`, for example `isUserChatJobLeaseExpired` or `IS_DEBUG_MODE`.
-   Do not use abbreviations, for example use `isExpired` instead of `isExp`, `translateMessage` instead of `t`, etc.
It is fine to use well-known abbreviations, for example `id`, `url`, `html`, etc.
-   When writing multiline strings, use `spaceTrim` utility.
-   Do only the change described in the prompt. Do not add any additional features or make any additional changes that are not described there.
If you find some critical issue that is not described in the prompt, report it to `agents-messages` in the root of the project.

### Prompt suffix
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Keep in mind the SOLID principles.
-   Do a proper analysis of the current functionality before you start implementing.
-   Keep small responsibilities of functions and classes, avoid creating big functions or classes that do many things.
-   When throwing errors, throw [branded errors](src/errors) and use `spaceTrim` utility to write clear and well-formatted multiline detailed error messages.
-   Constants should always be `UPPER_SNAKE_CASE`.
-   Boolean variables should always be prefixed with `is`, for example `isUserChatJobLeaseExpired` or `IS_DEBUG_MODE`.
-   Do not use abbreviations, for example use `isExpired` instead of `isExp`, `translateMessage` instead of `t`, etc.
It is fine to use well-known abbreviations, for example `id`, `url`, `html`, etc.
-   When writing multiline strings, use `spaceTrim` utility.
-   Do only the change described in the prompt. Do not add any additional features or make any additional changes that are not described there.
If you find some critical issue that is not described in the prompt, report it to `agents-messages` in the root of the project.

## Context

### How to Contribute

-   **Add changes in [Changelog](/changelog/_current-preversion.md)**
-   **Test your changes** to ensure they work as expected, use `npm test`

### Code Style

-   Follow the existing code style and structure.
-   Use comments to explain complex logic.

### Emoji in `[brackets]`

-   `[any emoji]` Connects multiple places that are related to each other across the repository
-   `[number]` Connects multiple places that are related to each other across the file
-   `[🧠]` Marks a place where there is something to decide and think about.
-   `[🕕]` List of models _(that should be progresively updated)_
    -   Prompt: Update available models and their prices, search online
-   `[🔼]` Marks an entity (function, class, type,...) in other project (like Promptbook.studio) which should be moved to this repository
-   `[🚉]` Marks an types / interfaces / structures fully serializable as JSON, not marking `string_` and `number_` prefixed aliases
-   `[🧹]` Need to implement garbage collection
-   `[🐣]` Easter eggs
-   `[💩]` Shitty code that needs refactoring
-   `$` When entity (function, class) starts by `$`, it means it is not pure and can have side effects.
-   3x `!` Marks a place that needs to be fixed before releasing a pre-release version.
-   4x (and more) `!` Marks a place that needs to be fixed as soon as possible.
-   `@@@` Marks a place where text / documentation / ... must be written.
-   [⚫] Code in this file should never be published in any package
-   [🟢] Code in this file should never be published into packages that could be imported into browser environment
-   [🔵] Code in this file should never be published outside of `@promptbook/browser`
-   [🟡] Code in this file should never be published outside of `@promptbook/cli`
-   [💞] Ignore a discrepancy between file name and entity name

### Generated code

Across the repository, there are several places where code has been generated automatically.
Do not edit these places manually, as they will be overwritten by the code generation process. Edit the source files instead.

> ⚠️ WARNING: This code has been generated so that any manual changes will be overwritten

### Dictionary

-   **Promptbook Engine vs. Agent Server** sre two distinct parts you should know the difference:
    -   **Promptbook Engine** is the core engine that powers AI agents, it is located in `/src` folder. It is framework-agnostic and can be used in different applications. It can run as standalone javascript/typescript library, CLI environment, or be integrated into different applications. It can be also runned in Agent Server:
    -   **Agent Server** is a specific application that uses the Promptbook Engine to provide a web and API interface for creating and interacting with AI agents. It is located in `/apps/agents-server` folder.
-   **Commitments** are basic syntax elements that add specific functionalities to AI agents
    -   They are used in `agentSource`, there are commitments like `PERSONA`, `RULE`, `KNOWLEDGE`, `USE CALENDAR`, `USE PROJECT`, `META IMAGE`, etc.
    -   Commitments are in the folder `/src/commitments`
    -   Each commitment starts with a keyword, e.g., `KNOWLEDGE`, `USE PROJECT`, etc. on a begining of the line and end by new co
    -   Agent source with commitments is parsed by two functions:
        -   `parseAgentSource` which is a lightweight parser for agent source, it parses basic information and its purpose is to be quick and synchronous. The commitments there are hardcoded.
        -   `createAgentModelRequirements` which is an asynchronous function that creates model requirements it applies each commitment one by one and works asynchronously.

### Common rules

-   Always analyze the context and requirements before generating any code.
-   Write clear, maintainable, and well-documented code.
-   Write JSDoc comments for all entities - functions, classes, types, top-level constants, etc.
    -   When this entity is exported from the file and it is under `src` folder _(not for example in the `apps` folder)_, it must be marked either as `@public` or `@private` at the end of the JSDoc comment.
    -   For example: "@private internal utility of <Chat/>" / "@public exported from `@promptbook/browser`"
    -   If you don't know, prefer to mark it as private, we can always change it to public later, but changing from public to private may cause breaking changes.
-   You (the AI coding agent) are running inside a Node process, so do not kill all Node processes such as `taskkill /F /IM node.exe`. If you need to stop something you spawned, kill only that specific process, for example by PID or by port.

### Additional context

-   Look at attached images if there are present
-   Attached images are in folder `prompts` on the root of the project
    -   For example the ![alt text](screenshots/foo.png) is image file `prompts/screenshots/foo.png`

#### Agents server installed on VPS _(as additional context)_

-   There are two distinct things:
    1. Entire VPS
    2. Each server
-   On one VPS there can be multiple servers and each server has its own domain
-   Most things (like agents, projects, metadata, etc.) are living on each server and some things (like environment variables, superadmin, etc.) are bound to entire VPS
-   Purpose of this is to be able to run multiple clients on one VPS and each client can have its own server with its own domain and settings
-   The entire VPS is managed by superadmin and each server is managed by normal admin of that server _(or superadmin can also manage each server)_
-   Superadmin has always name `admin` and password hardcoded in environment variable `ADMIN_PASSWORD`
-   The VPS is installed by running [the `install.sh`](install.sh) script on a fresh VPS
-   Servers can be added and removed by superadmin on `/superadmin/servers` page

#### The Agents Server menu _(as additional context)_

The menu of the agent server looks like this:

1. The navigation hierarchy
    - Icon and Server name _(for example Promptbook Agents Server)_
    - arrow ">" and Agents or picked agent name (organized in folders)
    - arrow ">" and the view Profile / Chat / Book of the agent or nothing if no agent is picked
2. The menu items
    - Documentation
    - System
3. Control panel and user menu
    - Control panel
    - User menu with the avatar and the name of the user

#### Database migrations for Agents server _(as additional context)_

-   Migrations are located in `apps/agents-server/src/database/migrations`
-   Be aware that table names in migrations have prefix `prefix_` _(look at existing migrations for reference)_
-   Migrations should be backwards compatible:
    -   Meaning that earlier versions of the server should be able to run with the database after migration without any issues.
    -   This is important because we want to have the same database for production and preview environments.
    -   This database will be migrated to the latest version of the preview environment, but the production environment should be able to run with it without any issues.
    -   The only thing that can happen is that older versions of the server will not be able to use new features.
    -   Adding new columns, tables, etc. is fine, but do not remove or rename existing ones, and do not change the meaning of existing columns or tables.
    -   When in doubt, prefer to add new things instead of changing existing ones.

#### Metadata of Agents server _(as additional context)_

-   There is a table called `Metadata`.
-   It has `key` and `value` fields.
-   It is a similar concept to configuration, but this configuration can be changed by administrators in the Agents Server website.

#### Book Language blueprint _(as additional context)_

Book language is a domain-specific language used for defining AI agents in the Promptbook Engine and Agents server.
It has lightweight syntax and keywords (the commitments) that allow you to define the "soul" of the agent.
The book language is designed to be human-readable and easy to write, while also being powerful enough to express complex agent behaviors.

Every agent has its source defined in the book language, which is called "agent source". The agent source is parsed and processed by the Promptbook Engine to create the actual AI agent that can interact with users and perform tasks.
This agent source is internally converted to a structured format called "agent model requirements" which are the actual raw technical instructions for the AI model to run the agent.

There is a standalone book language documentation on each agent server on `/api/docs/book.md` route, for example `https://live.ptbk.io/api/docs/book-language.md?language=en`.
Use it as a reference for the syntax and semantics of the book language, and modify `apps/agents-server/src/utils/bookLanguageDocumentation/createStandaloneBookLanguageMarkdown.ts` if it is relevant to the change you are doing.

##### Commitments _(as additional context and part of Book Language)_

Commitments are basic syntax elements that add specific functionalities to AI agents written in `book` language.

-   They are used in `agentSource`, there are commitments like `PERSONA`, `RULE`, `KNOWLEDGE`, `USE CALENDAR`, `USE PROJECT`, `META IMAGE`, `CLOSED`, etc.
-   They are in the folder `src/commitments`.
-   Each commitment starts with a keyword, e.g. `PERSONA`, `KNOWLEDGE`, `USE PROJECT`, etc. on a beginning of the line and ends by a new commitment or the end of the book.
-   There is a general pattern that the commitment keyword is followed by a space and then by the content of the commitment, for example:
    -   `PERSONA You are a helpful assistant that helps with cooking recipes.`
    -   `USE MCP https://mcp.example.com/server`
-   In the commitment context, you can reference external agents, for example:
    -   `TEAM You can talk to {Criminal lawyer} and {Financial advisor}`

CODEX_PROMPT

--- raw output ---
ptbk-codex-login-method: chatgpt
Reading prompt from stdin...
OpenAI Codex v0.154.0
--------
workdir: C:\Users\me\work\ai\promptbook
model: gpt-6-astra
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: max
reasoning summaries: none
session id: 01a0acaf-1e3e-7952-b68c-7585ce323386
--------
user

## Your Task

The Promptbook coder agents should resolve `FROM` correctly.

```bash
ptbk coder run --harness openai-codex --model gpt-5.6-astra --thinking-level max --agent agents/coding/pavol-workshops.book --context AGENTS.md
```

**When there are multiple books:**

```
pavol-workshops.book
pavol.book
```

**And one of them is specified in the `FROM` clause:**

```
FROM @Pavol
```

It should read all the agents and resolve the `FROM` commitments correctly.

-   Same thing applies to `IMPORT` and `TEAM` commitments
-   All of theese should work:
    -   `FROM @Pavol`, `FROM {Pavol}` - The "Pavol" Is the name of the agent from the first line of its book source
        -   This book should be auto-finded in the repository, go recursively from the folder where the primary agent lives, in this case `agents/coding/pavol-workshops.book` - so folder `agents/coding/` will be searched.
    -   `FROM {./foo/bar/agent.book}`, `FROM {../foo/bar/agent.book}` - The path to the agent book is relative to the agent
    -   `FROM {foo/agent.book}` - The path relative to the cwd (current working directory)
    -   `FROM {http://example.com/agent.book}` - The path to the agent book can also be a URL
    -   `FROM @null`, `FROM {null}`, `FROM {void}`, `FROM @void` - Agent which event dont inherit from Adam agent
-   The agents are auto-inherited from Adam agent by default, if the Adam agent not existing, auto-create it in `.core` folder, in this case in `agents/coding/.core/adam.book`
    -   Simmilar logic is in the agent server code, just reuse the same code for the same logic and patterns
    -   Also `ptbk init` should auto-create the Adam agent in the `.core` folder together with `developer.book` which is already doing
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
    -   Same logic is working on the agent server. Reuse the same code for the same logic.
-   Do a proper analysis of the current functionality of `ptbk coder` and related functionality before you start implementing.
-   Also look and update [the dev scripts in `terminals.json`](.vscode/terminals.json)
-   You are working with [`ptbk coder`](src/cli/cli-commands/coder/run.ts)
-   Update the [`ptbk coder` landing website](apps/coder-landing) if there are any changes that affect the landing page.
-   Add the changes into the [changelog](changelog/_current-preversion.md)

## Your Behavior

You are Promptbook Developer

### Rules

-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Keep in mind the SOLID principles.
-   Do a proper analysis of the current functionality before you start implementing.
-   Keep small responsibilities of functions and classes, avoid creating big functions or classes that do many things.
-   When throwing errors, throw [branded errors](src/errors) and use `spaceTrim` utility to write clear and well-formatted multiline detailed error messages.

Output Format: errors as markdown, for example `variables` should be in backticks and important notes can be in bold.
-   Constants should always be `UPPER_SNAKE_CASE`.
-   Boolean variables should always be prefixed with `is`, for example `isUserChatJobLeaseExpired` or `IS_DEBUG_MODE`.
-   Do not use abbreviations, for example use `isExpired` instead of `isExp`, `translateMessage` instead of `t`, etc.
It is fine to use well-known abbreviations, for example `id`, `url`, `html`, etc.
-   When writing multiline strings, use `spaceTrim` utility.
-   Do only the change described in the prompt. Do not add any additional features or make any additional changes that are not described there.
If you find some critical issue that is not described in the prompt, report it to `agents-messages` in the root of the project.

### Prompt suffix
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Keep in mind the SOLID principles.
-   Do a proper analysis of the current functionality before you start implementing.
-   Keep small responsibilities of functions and classes, avoid creating big functions or classes that do many things.
-   When throwing errors, throw [branded errors](src/errors) and use `spaceTrim` utility to write clear and well-formatted multiline detailed error messages.
-   Constants should always be `UPPER_SNAKE_CASE`.
-   Boolean variables should always be prefixed with `is`, for example `isUserChatJobLeaseExpired` or `IS_DEBUG_MODE`.
-   Do not use abbreviations, for example use `isExpired` instead of `isExp`, `translateMessage` instead of `t`, etc.
It is fine to use well-known abbreviations, for example `id`, `url`, `html`, etc.
-   When writing multiline strings, use `spaceTrim` utility.
-   Do only the change described in the prompt. Do not add any additional features or make any additional changes that are not described there.
If you find some critical issue that is not described in the prompt, report it to `agents-messages` in the root of the project.

## Context

### How to Contribute

-   **Add changes in [Changelog](/changelog/_current-preversion.md)**
-   **Test your changes** to ensure they work as expected, use `npm test`

### Code Style

-   Follow the existing code style and structure.
-   Use comments to explain complex logic.

### Emoji in `[brackets]`

-   `[any emoji]` Connects multiple places that are related to each other across the repository
-   `[number]` Connects multiple places that are related to each other across the file
-   `[🧠]` Marks a place where there is something to decide and think about.
-   `[🕕]` List of models _(that should be progresively updated)_
    -   Prompt: Update available models and their prices, search online
-   `[🔼]` Marks an entity (function, class, type,...) in other project (like Promptbook.studio) which should be moved to this repository
-   `[🚉]` Marks an types / interfaces / structures fully serializable as JSON, not marking `string_` and `number_` prefixed aliases
-   `[🧹]` Need to implement garbage collection
-   `[🐣]` Easter eggs
-   `[💩]` Shitty code that needs refactoring
-   `$` When entity (function, class) starts by `$`, it means it is not pure and can have side effects.
-   3x `!` Marks a place that needs to be fixed before releasing a pre-release version.
-   4x (and more) `!` Marks a place that needs to be fixed as soon as possible.
-   `@@@` Marks a place where text / documentation / ... must be written.
-   [⚫] Code in this file should never be published in any package
-   [🟢] Code in this file should never be published into packages that could be imported into browser environment
-   [🔵] Code in this file should never be published outside of `@promptbook/browser`
-   [🟡] Code in this file should never be published outside of `@promptbook/cli`
-   [💞] Ignore a discrepancy between file name and entity name

### Generated code

Across the repository, there are several places where code has been generated automatically.
Do not edit these places manually, as they will be overwritten by the code generation process. Edit the source files instead.

> ⚠️ WARNING: This code has been generated so that any manual changes will be overwritten

### Dictionary

-   **Promptbook Engine vs. Agent Server** sre two distinct parts you should know the difference:
    -   **Promptbook Engine** is the core engine that powers AI agents, it is located in `/src` folder. It is framework-agnostic and can be used in different applications. It can run as standalone javascript/typescript library, CLI environment, or be integrated into different applications. It can be also runned in Agent Server:
    -   **Agent Server** is a specific application that uses the Promptbook Engine to provide a web and API interface for creating and interacting with AI agents. It is located in `/apps/agents-server` folder.
-   **Commitments** are basic syntax elements that add specific functionalities to AI agents
    -   They are used in `agentSource`, there are commitments like `PERSONA`, `RULE`, `KNOWLEDGE`, `USE CALENDAR`, `USE PROJECT`, `META IMAGE`, etc.
    -   Commitments are in the folder `/src/commitments`
    -   Each commitment starts with a keyword, e.g., `KNOWLEDGE`, `USE PROJECT`, etc. on a begining of the line and end by new co
    -   Agent source with commitments is parsed by two functions:
        -   `parseAgentSource` which is a lightweight parser for agent source, it parses basic information and its purpose is to be quick and synchronous. The commitments there are hardcoded.
        -   `createAgentModelRequirements` which is an asynchronous function that creates model requirements it applies each commitment one by one and works asynchronously.

### Common rules

-   Always analyze the context and requirements before generating any code.
-   Write clear, maintainable, and well-documented code.
-   Write JSDoc comments for all entities - functions, classes, types, top-level constants, etc.
    -   When this entity is exported from the file and it is under `src` folder _(not for example in the `apps` folder)_, it must be marked either as `@public` or `@private` at the end of the JSDoc comment.
    -   For example: "@private internal utility of <Chat/>" / "@public exported from `@promptbook/browser`"
    -   If you don't know, prefer to mark it as private, we can always change it to public later, but changing from public to private may cause breaking changes.
-   You (the AI coding agent) are running inside a Node process, so do not kill all Node processes such as `taskkill /F /IM node.exe`. If you need to stop something you spawned, kill only that specific process, for example by PID or by port.

### Additional context

-   Look at attached images if there are present
-   Attached images are in folder `prompts` on the root of the project
    -   For example the ![alt text](screenshots/foo.png) is image file `prompts/screenshots/foo.png`

#### Agents server installed on VPS _(as additional context)_

-   There are two distinct things:
    1. Entire VPS
    2. Each server
-   On one VPS there can be multiple servers and each server has its own domain
-   Most things (like agents, projects, metadata, etc.) are living on each server and some things (like environment variables, superadmin, etc.) are bound to entire VPS
-   Purpose of this is to be able to run multiple clients on one VPS and each client can have its own server with its own domain and settings
-   The entire VPS is managed by superadmin and each server is managed by normal admin of that server _(or superadmin can also manage each server)_
-   Superadmin has always name `admin` and password hardcoded in environment variable `ADMIN_PASSWORD`
-   The VPS is installed by running [the `install.sh`](install.sh) script on a fresh VPS
-   Servers can be added and removed by superadmin on `/superadmin/servers` page

#### The Agents Server menu _(as additional context)_

The menu of the agent server looks like this:

1. The navigation hierarchy
    - Icon and Server name _(for example Promptbook Agents Server)_
    - arrow ">" and Agents or picked agent name (organized in folders)
    - arrow ">" and the view Profile / Chat / Book of the agent or nothing if no agent is picked
2. The menu items
    - Documentation
    - System
3. Control panel and user menu
    - Control panel
    - User menu with the avatar and the name of the user

#### Database migrations for Agents server _(as additional context)_

-   Migrations are located in `apps/agents-server/src/database/migrations`
-   Be aware that table names in migrations have prefix `prefix_` _(look at existing migrations for reference)_
-   Migrations should be backwards compatible:
    -   Meaning that earlier versions of the server should be able to run with the database after migration without any issues.
    -   This is important because we want to have the same database for production and preview environments.
    -   This database will be migrated to the latest version of the preview environment, but the production environment should be able to run with it without any issues.
    -   The only thing that can happen is that older versions of the server will not be able to use new features.
    -   Adding new columns, tables, etc. is fine, but do not remove or rename existing ones, and do not change the meaning of existing columns or tables.
    -   When in doubt, prefer to add new things instead of changing existing ones.

#### Metadata of Agents server _(as additional context)_

-   There is a table called `Metadata`.
-   It has `key` and `value` fields.
-   It is a similar concept to configuration, but this configuration can be changed by administrators in the Agents Server website.

#### Book Language blueprint _(as additional context)_

Book language is a domain-specific language used for defining AI agents in the Promptbook Engine and Agents server.
It has lightweight syntax and keywords (the commitments) that allow you to define the "soul" of the agent.
The book language is designed to be human-readable and easy to write, while also being powerful enough to express complex agent behaviors.

Every agent has its source defined in the book language, which is called "agent source". The agent source is parsed and processed by the Promptbook Engine to create the actual AI agent that can interact with users and perform tasks.
This agent source is internally converted to a structured format called "agent model requirements" which are the actual raw technical instructions for the AI model to run the agent.

There is a standalone book language documentation on each agent server on `/api/docs/book.md` route, for example `https://live.ptbk.io/api/docs/book-language.md?language=en`.
Use it as a reference for the syntax and semantics of the book language, and modify `apps/agents-server/src/utils/bookLanguageDocumentation/createStandaloneBookLanguageMarkdown.ts` if it is relevant to the change you are doing.

##### Commitments _(as additional context and part of Book Language)_

Commitments are basic syntax elements that add specific functionalities to AI agents written in `book` language.

-   They are used in `agentSource`, there are commitments like `PERSONA`, `RULE`, `KNOWLEDGE`, `USE CALENDAR`, `USE PROJECT`, `META IMAGE`, `CLOSED`, etc.
-   They are in the folder `src/commitments`.
-   Each commitment starts with a keyword, e.g. `PERSONA`, `KNOWLEDGE`, `USE PROJECT`, etc. on a beginning of the line and ends by a new commitment or the end of the book.
-   There is a general pattern that the commitment keyword is followed by a space and then by the content of the commitment, for example:
    -   `PERSONA You are a helpful assistant that helps with cooking recipes.`
    -   `USE MCP https://mcp.example.com/server`
-   In the commitment context, you can reference external agents, for example:
    -   `TEAM You can talk to {Criminal lawyer} and {Financial advisor}`


codex
I’ll trace how `ptbk coder` loads books and how the Agent Server resolves inheritance, then reuse the shared logic for local references. I’ll also update initialization, dev scripts, relevant docs, and the changelog, and run the tests.

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content src/cli/cli-commands/coder/run.ts; Get-Content .vscode/terminals.json' in C:\Users\me\work\ai\promptbook
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"Adam|adam\\.book|resolve.*(Agent|Commitment)|createAgentModelRequirements|create.*Collection\" src/cli apps/agents-server/src/utils src/collection src/book-2.0 -g '*.ts'" in C:\Users\me\work\ai\promptbook
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Location; git status --short; rg --files -g AGENTS.md -g package.json -g '*jest*' -g '*vitest*' -g 'terminals.json' -g '"'!node_modules'"' -g '"'!packages'"' -g '"'!**/.next/**'"'" in C:\Users\me\work\ai\promptbook
 succeeded in 4660ms:
import {
    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
    Option,
} from 'commander';
import { spaceTrim } from 'spacetrim';
import { assertsError } from '../../../errors/assertsError';
import type { $side_effect } from '../../../utils/organization/$side_effect';
import { createPositiveIntegerOptionParser } from '../common/createPositiveIntegerOptionParser';
import { $assertSufficientFreeDiskSpace } from '../common/disk-space/$assertSufficientFreeDiskSpace';
import { handleActionErrors } from '../common/handleActionErrors';
import { $ensureHarnessInstallations } from '../common/harness/$ensureHarnessInstallations';
import {
    addQuestionsOption,
    assertUserConfirmationIsAllowed,
    normalizeQuestionsCliOptions,
    QUESTIONS_DESCRIPTION,
    type QuestionsCliOptions,
} from '../common/questionsCliOptions';
import { $ensurePromptbookCliInstallations } from '../common/promptbook-cli/$ensurePromptbookCliInstallations';
import type { PromptRunnerCliOptions } from '../common/promptRunnerCliOptions';
import {
    addPromptRunnerExecutionOptions,
    addPromptRunnerSelectionOptions,
    normalizePromptRunnerCliOptions,
    PROMPT_RUNNER_DESCRIPTION,
} from '../common/promptRunnerCliOptions';
import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
import {
    DEFAULT_CODER_TEST_COMMAND,
    TEST_BEFORE_MODE_VALUES,
    type TestBeforeMode,
} from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
import { printCoderRunFailure } from './printCoderRunFailure';

/**
 * Initializes `coder run` command for Promptbook CLI utilities
 *
 * Note: `$` is used to indicate that this function is not a pure function - it registers a command in the CLI
 *
 * @private internal function of `promptbookCli`
 */
export function $initializeCoderRunCommand(program: Program): $side_effect {
    const command = program.command('run');
    command.description(
        spaceTrim(
            (block) => `
            Execute coding prompts through selected AI agent

            ${block(PROMPT_RUNNER_DESCRIPTION)}

            ${block(QUESTIONS_DESCRIPTION)}

            Features:
            - Automatically stages and commits changes with agent identity unless --no-commit is used
            - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
            - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
            - Optional --git-changes continue resumes the single prompt an interrupted coder left in the [^] status
            - Optional post-commit git push with explicit --auto-push opt-in
            - Optional pre-prompt git pull with explicit --auto-pull opt-in
            - Optional --isolate runs every prompt in its own temporary git worktree and merges it back when verified
            - Saves the run trace of every round into prompts/traces/, named after its prompt file
            - Optional --preserve-logs keeps temp prompt/log artifacts after successful rounds
            - Optional --no-ui keeps plain streaming console output for logging and debugging
            - Refuses to start on a nearly full disk and pauses the run when the free disk space becomes critical, unless --no-questions is used
            - Checks that the selected harness is installed and up to date before the first prompt unless --no-questions is used
            - Offers to add missing project-local ignore rules for the selected harness
            - In interactive mode, checks local and global Promptbook CLI installations and offers to update them
            - Supports GPG signing of commits
            - Optional pre-coding test run that can stop or repair pre-existing failures
            - Optional post-prompt verification with test-feedback retries
            - Progress tracking and interactive P/S/X terminal controls
            - Dry-run mode to preview prompts
        `,
        ),
    );

    command.option('--dry-run', 'Print unwritten prompts without executing', false);
    addPromptRunnerSelectionOptions(command);
    addQuestionsOption(command);
    addCoderAgentOption(command);
    command.option(
        '--context <context-or-file>',
        'Append extra instructions either inline or from a file path relative to the current project',
    );
    command.option(
        '--test <test-command...>',
        'Run a verification command after each prompt; quote it when the command itself contains top-level flags',
    );
    command.addOption(
        new Option(
            '--test-before <mode>',
            `Run tests before coding: ${TEST_BEFORE_MODE_VALUES.join(
                ', ',
            )} (defaults to no; uses npm test when --test is omitted)`,
        )
            .choices([...TEST_BEFORE_MODE_VALUES])
            .default('no'),
    );
    command.option(
        '--preserve-logs',
        'Keep generated temp prompt/log artifacts after successful rounds for debugging and analytics',
        false,
    );
    addPromptRunnerExecutionOptions(command);
    command.option(
        '--isolate',
        spaceTrim(`
            Implement each prompt in its own temporary git worktree with its own isolated environment.
            A verified task is merged back into the branch the coder runs on and the worktree is deleted.
            A task that cannot be merged is marked as failed and its worktree is kept for a manual merge.
        `),
        false,
    );
    addPromptPriorityOptions(command);
    command.option(
        '--limit <run-count>',
        'Stop after processing this many prompt runs',
        createPositiveIntegerOptionParser('--limit'),
    );
    command.option(
        '--wait-after-prompt <duration>',
        spaceTrim(`
            Wait this long after each prompt has been implemented, verified and committed before starting the next prompt.
            Accepts durations like 1h, 30m, 5s. Defaults to 0 (no wait).
        `),
    );
    command.option(
        '--wait-between-prompts <duration>',
        spaceTrim(`
            Pace prompts so that each next prompt starts at least this long after the previous one began.
            If the previous prompt already took longer than this, the next prompt starts immediately.
            Accepts durations like 1h, 30m, 5s. Defaults to 0 (no pacing).
        `),
    );
    command.option(
        '--wait-after-error <duration>',
        spaceTrim(`
            Wait this long before retrying a prompt after an error occurs (up to 3 retries before giving up).
            Accepts durations like 1h, 30m, 5s. Defaults to 10m.
        `),
    );
    // Note: --no-auto disables the default auto behaviour and waits for user confirmation before each prompt
    command.option(
        '--no-auto',
        'Wait for user confirmation before each prompt instead of running automatically through the queue',
    );
    command.option(
        '--auto-migrate',
        'Run testing-server database migrations automatically after each successfully processed prompt',
    );
    command.option(
        '--allow-destructive-auto-migrate',
        'Allow auto-migrate even when heuristic SQL safety check flags destructive pending migrations',
    );

    command.action(
        handleActionErrors(async (cliOptions) => {
            const {
                dryRun,
                agent,
                context,
                test,
                testBefore,
                preserveLogs,
                isolate: isIsolated,
                priority,
                minPriority: minimumPriority,
                maxPriority: maximumPriority,
                limit,
                waitAfterPrompt: waitAfterPromptValue,
                waitBetweenPrompts: waitBetweenPromptsValue,
                waitAfterError: waitAfterErrorValue,
                auto,
                autoMigrate,
                allowDestructiveAutoMigrate,
            } = cliOptions as {
                readonly dryRun: boolean;
                readonly agent?: string;
                readonly context?: string;
                readonly test?: string | string[];
                readonly testBefore: TestBeforeMode;
                readonly preserveLogs: boolean;
                readonly isolate: boolean;
                readonly priority?: number;
                readonly minPriority?: number;
                readonly maxPriority?: number;
                readonly limit?: number;
                readonly waitAfterPrompt?: string;
                readonly waitBetweenPrompts?: string;
                readonly waitAfterError?: string;
                readonly auto: boolean;
                readonly autoMigrate: boolean;
                readonly allowDestructiveAutoMigrate: boolean;
            } & PromptRunnerCliOptions &
                CoderAgentCliOptions;

            const configuredTestCommand = normalizeCommandOptionValue(test);
            const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
            const runnerOptions = normalizePromptRunnerCliOptions(cliOptions as PromptRunnerCliOptions, {
                isAgentRequired: !dryRun,
            });
            const questionsOptions = normalizeQuestionsCliOptions(cliOptions as QuestionsCliOptions);

            // [1] Parse the wait options and --no-auto:
            //   default: run automatically through the queue (no waiting between prompts)
            //   --no-auto: wait for user confirmation before each prompt (interactive mode)
            //   --wait-after-prompt: pause after a successful round before starting the next prompt
            //   --wait-between-prompts: pace from start of one prompt to start of next
            //   --wait-after-error: wait before retrying after an error (default 10m)
            const waitForUser = !auto;

            assertUserConfirmationIsAllowed({ ...questionsOptions, isWaitingForUser: waitForUser });

            // Note: Checked before anything is installed or written, because installing a harness, updating the
            //       Promptbook CLI and coding itself all need the disk space which is missing
            await $assertSufficientFreeDiskSpace(process.cwd());

            if (await $ensurePromptbookCliInstallations(questionsOptions)) {
                return process.exit(0);
            }

            await $ensureHarnessInstallations([runnerOptions.agentName], questionsOptions);
            await $ensureCoderHarnessGitignoreRules(process.cwd(), runnerOptions.agentName, questionsOptions);

            const waitAfterPrompt = parseOptionalWaitDuration(waitAfterPromptValue, 0);
            const waitBetweenPrompts = parseOptionalWaitDuration(waitBetweenPromptsValue, 0);
            const waitAfterError = parseOptionalWaitDuration(waitAfterErrorValue, DEFAULT_WAIT_AFTER_ERROR_MS);

            // Convert commander options to RunOptions format
            const runOptions = {
                dryRun,
                waitForUser,
                waitAfterPrompt,
                waitBetweenPrompts,
                waitAfterError,
                noCommit: runnerOptions.noCommit,
                gitChanges: runnerOptions.gitChanges,
                agentName: runnerOptions.agentName,
                model: runnerOptions.model,
                agent,
                context,
                testCommand,
                testBefore,
                preserveLogs,
                isIsolated,
                noUi: runnerOptions.noUi,
                thinkingLevel: runnerOptions.thinkingLevel,
                priority: priority ?? 0,
                minimumPriority,
                maximumPriority,
                limit,
                normalizeLineEndings: runnerOptions.normalizeLineEndings,
                allowCredits: runnerOptions.allowCredits,
                autoMigrate,
                allowDestructiveAutoMigrate,
                autoPush: runnerOptions.autoPush,
                autoPull: runnerOptions.autoPull,
                isAskingQuestionsEnabled: questionsOptions.isAskingQuestionsEnabled,
            };

            // Note: Import the function dynamically to avoid loading heavy dependencies until needed
            const { runCodexPrompts } = await import('../../../../scripts/run-codex-prompts/main/runCodexPrompts');

            try {
                // Override process.argv to pass options to the legacy parseRunOptions if needed
                await runCodexPrompts(runOptions);
            } catch (error) {
                assertsError(error);
                printCoderRunFailure(error);
                return process.exit(1);
            }

            return process.exit(0);
        }),
    );
}

/**
 * Joins one Commander option that may be parsed either as a single string or a variadic token array.
 *
 * @private internal utility of `coder run` command
 */
function normalizeCommandOptionValue(value: string | string[] | undefined): string | undefined {
    if (value === undefined) {
        return undefined;
    }

    const parts = Array.isArray(value) ? value : [value];
    const normalizedValue = parts
        .map((part) => part.trim())
        .filter(Boolean)
        .join(' ')
        .trim();
    return normalizedValue === '' ? undefined : normalizedValue;
}

// Note: [🟡] Code for CLI command [run](src/cli/cli-commands/coder/run.ts) should never be published outside of `@promptbook/cli`
// Note: [💞] Ignore a discrepancy between file name and entity name
{
    "autokill": true,
    "terminals": [
        {
            "--note-0": "There are 3 types of terminals: 1) Runned automatically when coding (execute=true,onlySingle=false,focus=false) 2) Prepared automatically to be run by user but not executed by default (execute=false,onlySingle=false,focus=false), theese are marked by (💤) 3) Manually run by user (execute=true,onlySingle=true,focus=true)",
            "--note-1": "The (💸) means that script has some cost associated with it, for example, using a paid API or service.",
            "--note-2": "The (💤) means that script is not executed by default but need user to press [Enter] to run it",
            "--note-3": "The (⏳) means that script is expected to run for a longer time than usual",
            "--note-4": "The (❗) means that script should be run with caution, for example, it may cause breaking changes, data loss, etc.",
            "--note-5": "The (❗❗) means that script should be run with extreme caution, messing up with LTS versions, etc."
        },

        {
            "name": "🐙🏓 Ping Codex & Claude",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder ping --no-questions --harness claude-code --model claude-sonnet-5 --thinking-level low",
                "npx ts-node ./src/cli/test/ptbk.ts coder ping --no-questions --harness openai-codex --model gpt-5.6-luna --thinking-level low"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🐙🧪 Test ptbk coder terminal UI (💤)",
            "command": "npm run test-unit -- --runTestsByPath scripts/run-codex-prompts/ui/buildCoderRunUiFrame.test.ts scripts/run-codex-prompts/ui/buildCoderRunUiTerminalFrameUpdate.test.ts",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness openai-codex --model gpt-5.5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix --auto-pull --auto-push"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `gpt-5.6-sol` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness openai-codex --model gpt-5.6-sol --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `gpt-5.6-terra` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness openai-codex --model gpt-5.6-terra --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push --min-priority 1"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `gpt-5.6-luna` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness openai-codex --model gpt-5.6-luna --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push --max-priority 2"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `claude-fable` Coding Agent (💤)(💸)(⏳)",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness claude-code --model fable --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --min-priority 3 --test-before yes-and-fix  --auto-pull --auto-push",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `qwen3.8-max` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness qwen-code --model qwen3.8-max --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `qwen3.8-flash` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness qwen-code --model qwen3.8-flash --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `deepseek-v4-pro` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness qwen-code --model deepseek-v4-pro-0813 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `deepseek-v4-flash` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness qwen-code --model deepseek-v4-flash-0731 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `glm-5.2` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness qwen-code --model glm-5.2 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🏭 Generate \"Generic\" prompt boilerplates for Coding Agent",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 10 --template prompts/_templates/common.md --commit --auto-pull --auto-push",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🐙🏭 Generate \"Agents server\" prompt boilerplates for Coding Agent (💤)",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 5 --template prompts/_templates/agents-server.md --commit --auto-pull --auto-push",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🏭 Generate \"Utils app\" prompt boilerplates for Coding Agent",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 5 --template prompts/_templates/utils-app.md --commit --auto-pull --auto-push",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🐙🏭 Generate \"ptbk coder\" prompt boilerplates for Coding Agent (💤)",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 5 --template prompts/_templates/ptbk-coder.md --commit --auto-pull --auto-push",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🏭 Generate \"ptbk agent-folder\" prompt boilerplates for Coding Agent",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 5 --template prompts/_templates/ptbk-agent.md --commit --auto-pull --auto-push",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🐙🏭 Generate \"ptbk agents-server\" prompt boilerplates for Coding Agent",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 5 --template prompts/_templates/ptbk-agents-server.md --commit --auto-pull --auto-push",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🐙🏭 Generate test prompt boilerplates for Coding Agent",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 1 --template prompts/_templates/test.md --commit --auto-pull --auto-push",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🐙🏭 Find refactor candidates for Coding Agent",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder find-refactor-candidates",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🐙🔎 Find unwritten prompts",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder find-unwritten",
            "execute": true,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙📋 List prompts ready to run",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder list",
            "execute": true,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙✔ Verify prompts done by Coding Agent",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder verify --order from-latest --ignore Refactor --commit --auto-pull --auto-push",
            "execute": true,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🔖🟢 Tag the current commit as good (💤)",
            "command": "git fetch --tags && { base=good-$(date +%F); tag=$base; i=0; while git show-ref --verify --quiet refs/tags/$tag; do i=$((i+1)); tag=$base-$i; done; git tag -a $tag HEAD -m $tag && git push origin $tag && echo '🟢 Created tag marking good commit: ' $tag; }",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🔖🔴 Tag the current commit as bad (💤)",
            "command": "git fetch --tags && { base=bad-$(date +%F); tag=$base; i=0; while git show-ref --verify --quiet refs/tags/$tag; do i=$((i+1)); tag=$base-$i; done; git tag -a $tag HEAD -m $tag && git push origin $tag && echo '🔴 Created tag marking bad commit: ' $tag; }",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "✨👨‍💻 Run ptbk agents-server dev",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts agents-server dev --harness github-copilot --model gpt-5.4 --thinking-level max --port 4440"
            ],
            "execute": true,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "✨👨‍💻 Run ptbk agents-server start",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts agents-server dev --harness github-copilot --model gpt-5.4 --thinking-level max --port 4440"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },

        {
            "name": "(old) ✨⏩ Migrate Agents server `live` (💤)",
            "cwd": "./apps/agents-server/",
            "command": "npx tsx ./src/database/migrate.ts --only=live",
            "execute": false,
            "onlySingle": true,
            "focus": false
        },
        {
            "name": "(old)(❗) ✨⏩ Migrate Agents server `preview` (💤)",
            "cwd": "./apps/agents-server/",
            "command": "npx tsx ./src/database/migrate.ts --only=preview",
            "execute": false,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "✨🔼 Sync Agents server Vercel domains (dry-run)",
            "cwd": "./apps/agents-server/",
            "command": "npx tsx ./scripts/sync-vercel-domains.ts --dry-run",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old)(❗) ✨🔼 Sync Agents server Vercel domains (💤)",
            "cwd": "./apps/agents-server/",
            "command": "npx tsx ./scripts/sync-vercel-domains.ts",
            "execute": false,
            "onlySingle": true,
            "focus": false
        },
        {
            "name": "(old) ✨💾 Backup Supabase Postgres (💤)",
            "command": "npx ts-node ./scripts/backup-supabase/backup-supabase.ts --output-dir other/backup --schemas public --filename-pattern %timestamp%.sql.zip",
            "execute": false,
            "onlySingle": true,
            "focus": false
        },
        {
            "name": "(old)(❗) ✨🔼 Update the preview Agents servers (live / `main` -> `preview`) (💤)",
            "command": "git branch --force preview main && git push origin preview --force-with-lease && (cd ./apps/agents-server/ && npx tsx ./src/database/migrate.ts --only=preview)",
            "execute": false,
            "onlySingle": true,
            "focus": false
        },
        {
            "name": "(old)(❗❗) ✨🔼 Update the production Agents servers (`preview` -> `production`) (💤)",
            "command": "git branch --force production preview && git push origin production --force-with-lease && (cd ./apps/agents-server/ && npx tsx ./src/database/migrate.ts --only=production)",
            "execute": false,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old)(❗❗❗) ✨🔼 Update the LTS Agents servers (`production` -> `lts`) (💤)",
            "command": "git branch --force lts production && git push origin lts --force-with-lease && (cd ./apps/agents-server/ && npx tsx ./src/database/migrate.ts --only=lts)",
            "execute": false,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(❗) ✨🔼 Update Agent server (live / `main` -> `preview`) (💤)",
            "command": "git branch --force preview main && git push origin preview --force-with-lease",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "(❗) ✨🔼 Update Agent server (HEAD -> `preview`) (💤)",
            "command": "git branch --force preview HEAD && git push origin preview --force-with-lease && git checkout preview",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "(❗❗) ✨🔼 Update Agent server (`preview` -> `production`) (💤)",
            "command": "git branch --force production preview && git push origin production --force-with-lease",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "(❗❗❗) ✨🔼 Update Agent server (`production` -> `lts`) (💤)",
            "command": "git branch --force lts production && git push origin lts --force-with-lease",
            "execute": false,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 👨‍💻⚫ Run CLI example (💸)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts run ./book/examples/25-simple-knowledge.book.md --provider BRING_YOUR_OWN_KEYS"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) ✨👨‍💻🟣 Run deprecated ptbk start-agents-server",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts start-agents-server ./agents/default --port 4440 --provider BRING_YOUR_OWN_KEYS"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 👨‍💻🟣 Run ptbk start-pipelines-server",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts start-pipelines-server ./src/remote-server/testing-books --url http://localhost:4460 --provider BRING_YOUR_OWN_KEYS"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 👨‍💻⚪ Run simple-script example (💸)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./examples/usage/simple-script/simple-script.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 👨‍💻🔴 Run logging example (💸)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./examples/usage/logging/logging.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 👨‍💻🔵 Run remote example (💸)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./examples/usage/remote/server.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "👨‍💻🔵 Run CLI `ptbk about`",
            "commands": ["unset OPENAI_API_KEY && unset OPENAI_BASE_URL", "npx ts-node ./src/cli/test/ptbk.ts about"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 👨‍💻🔠 Run Book Components web app",
            "cwd": "./apps/book-components/",
            "commands": ["unset OPENAI_API_KEY && unset OPENAI_BASE_URL", "npm run dev"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🏭🍎 Build Book Editor macOS app (💤)(⏳)",
            "command": "npm run build:book-editor-macos",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🏭🆚 Build VSCode extension (💤)",
            "command": "npm run build:vscode-extension",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🧸🔠 Run Playground web app",
            "cwd": "./apps/playground/",
            "commands": ["unset OPENAI_API_KEY && unset OPENAI_BASE_URL", "npm run dev"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🧸🔠 Run Utils web app",
            "cwd": "./apps/utils/",
            "commands": ["unset OPENAI_API_KEY && unset OPENAI_BASE_URL", "npm run dev"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🧸🔠 Run ptbk coder landing page",
            "cwd": "./apps/coder-landing/",
            "commands": ["unset OPENAI_API_KEY && unset OPENAI_BASE_URL", "npm run dev"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "--note": " <- Note: [🔠] Register new apps here "
        },
        {
            "name": "🧪🏭🩹 Full checkup (LF => Prettify (+commit) => Test & Test full build) (⏳⏳)",
            "command": "./scripts/lines-to-lf.sh && npx ts-node ./scripts/prettify-all/prettify-all.ts --commit && npm run test && npx ts-node ./scripts/generate-packages/generate-packages.ts",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🧪🧪 Test all (💤)(⏳)",
            "commands": ["npm run test"],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🧪 Unit tests (💤)(⏳)",
            "command": "npm run test-unit",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🧪 Test Agents Server (💤)(⏳)",
            "command": "npm run test-app-agents-server",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🧪 Integration tests of Agents Server (💤)(⏳)",
            "cwd": "./apps/agents-server/",
            "command": "npm run test-e2e",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🧪 Integration tests of Agents Server (not headless) (⏳)",
            "cwd": "./apps/agents-server/",
            "command": "npx playwright test --headed",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🧪 Test Types (💤)",
            "command": "npm run test-types",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🧪 Test Spell check (💤)",
            "command": "npm run test-spellcheck",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🧪 Lint (💤)",
            "command": "npm run test-lint",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "(old) 🧪 Test Books",
            "command": "npm run test-books",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🧪 Test Package generation (skip build) (💤)",
            "command": "npm run test-package-generation",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🧪 Test Package generation (build) (💤)(⏳)",
            "command": "npx ts-node ./scripts/generate-packages/generate-packages.ts",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🧪 Test name discrepancies (💤)",
            "command": "npm run test-name-discrepancies",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🧪 Test 🔠 Book Components build",
            "command": "npm run test-book-components-build",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🧪 (examples) Lint ",
            "command": "npx eslint examples",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🧪 (scripts) Lint",
            "command": "npx eslint scripts",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🤪 Find fresh emoji tag",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder find-fresh-emoji-tags",
            "execute": true,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐱‍👤 Import GitHub",
            "command": "npm run import-github",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old)(❗) 🗑️ Delete OpenAI assistants",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./scripts/delete-openai-assistants/delete-openai-assistants.ts"
            ],
            "execute": false,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old)(❗) 🗑️ Delete OpenAI resources (files + vector stores)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./scripts/delete-openai-resources/delete-openai-resources.ts"
            ],
            "execute": false,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "✨🏭 Generate reserved paths for Agents server",
            "cwd": "./apps/agents-server/",
            "command": "npm run generate-reserved-paths",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🏭💉 Import markdowns",
            "command": "npm run import-markdowns",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🏭📦 Generate packages (Skip bundler)",
            "command": "npx ts-node ./scripts/generate-packages/generate-packages.ts --skip-bundler",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🏭📦 Generate packages (⏳)",
            "command": "npx ts-node ./scripts/generate-packages/generate-packages.ts",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(?old) 🏭📦📚 Generate documentation",
            "commands": ["npx ts-node ./scripts/generate-documentation/generate-documentation.ts"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(?old) 🏭📚 Generate server types from Open API",
            "commands": ["npm run generate-openapi-types"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🏭📖 Generate examples .book -> .bookc (💸) [🍠]",
            "commands": [
                "npx ts-node ./scripts/make-promptbook-collection/make-promptbook-collection.ts --verbose",
                "npx ts-node ./scripts/generate-examples-bookc/generate-examples-bookc.ts --verbose"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🏭📖 Generate reports .report.json -> .report.md",
            "commands": ["npx ts-node ./scripts/generate-example-reports/generate-example-reports.ts"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🏭🩹 Fix Lint",
            "command": "npx eslint src --fix",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🏭🩹 Fix Imports",
            "commands": ["npx ts-node ./scripts/repair-imports/repair-imports.ts --organize --organize-all"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🏭🩹 Fix end of lines to CRLF -> LF",
            "command": "./scripts/lines-to-lf.sh",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🔎🩹 Find name discrepancies",
            "commands": ["npx ts-node ./scripts/find-name-discrepancies/find-name-discrepancies.ts"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🧸 Playground (💸)",
            "commands": ["unset OPENAI_API_KEY && unset OPENAI_BASE_URL", "npx ts-node ./src/playground/playground.ts"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🧸🔴 Playground error handling",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/playground/permanent/error-handling-playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🧸 Agent with browser playground (💸)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/playground/permanent/agent-with-browser-playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "--note": "<- Note: [🎠] Add here new playgrounds"
        },
        {
            "name": "(old) 🧸 OpenAI playground (💸)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/llm-providers/openai/playground/playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🧸 Ollama playground",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/llm-providers/ollama/playground/playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🧸 Anthropic Claude playground (💸)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/llm-providers/anthropic-claude/playground/playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🧸 Vercel playground (💸)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/llm-providers/vercel/playground/playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🧸 Azure OpenAI playground (💸)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/llm-providers/azure-openai/playground/playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🧸 Agent LLM tools playground (with browser and search) (💸)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/llm-providers/agent/playground/playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🧸 Langtail playground (💸)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/llm-providers/langtail/playground/playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🧸 Multiple LLMs playground (💸)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/llm-providers/multiple/playground/playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🧸 Remote server playground",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/llm-providers/remote/playground/playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🧸📄💡 Scrape knowledge from Markdown (playground) (💸) [🍠]",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./scripts/make-promptbook-collection/make-promptbook-collection.ts --verbose",
                "npx ts-node ./src/scrapers/markdown/playground/markdown-scraper-playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🧸📄💡 Scrape knowledge by Markitdown (playground) (💸) [🍠]",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./scripts/make-promptbook-collection/make-promptbook-collection.ts --verbose",
                "npx ts-node ./src/scrapers/markitdown/playground/markitdown-scraper-playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },

        {
            "name": "(old) 🧸📃💡 Scrape knowledge from .docx (playground) (💸) [🍠]",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./scripts/make-promptbook-collection/make-promptbook-collection.ts --verbose",
                "npx ts-node ./src/scrapers/document/playground/document-scraper-playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },

        {
            "name": "(old) 🧸📃💡 Scrape knowledge from (legacy) .doc (playground) (💸) [🍠]",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./scripts/make-promptbook-collection/make-promptbook-collection.ts --verbose",
                "npx ts-node ./src/scrapers/document-legacy/playground/legacy-document-scraper-playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },

        {
            "name": "(old) 🧸📃💡 Scrape knowledge from .pdf (playground) (💸) [🍠]",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./scripts/make-promptbook-collection/make-promptbook-collection.ts --verbose",
                "npx ts-node ./src/scrapers/pdf/playground/pdf-scraper-playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },

        {
            "name": "(old) 🧸🌍💡 Scrape knowledge from website (playground) (💸)[🍠]",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./scripts/make-promptbook-collection/make-promptbook-collection.ts --verbose",
                "npx ts-node ./src/scrapers/website/playground/website-scraper-playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🧸 Transpile book -> OpenAiSdk (playground)",
            "command": "npx ts-node ./src/transpilers/openai-sdk/playground/playground.ts",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 📖 Make Promptbook collection (💸)(+commit)[🍠]",
            "command": "npx ts-node ./scripts/make-promptbook-collection/make-promptbook-collection.ts --verbose --commit",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(❗) 🔪👨🏽‍💻 Force reinstall of node modules (⏳)",
            "commands": [
                "rm -f ./package-lock.json",
                "rm -rf ./node_modules ./.next ./apps/**/node_modules ./apps/**/.next",
                "npm cache clean",
                "npm install"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🍴🔍 Git list unmerged changes",
            "commands": ["git branch --no-merged main"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(❗) 🍴🗑️ Git delete merged branches",
            "commands": [
                "./scripts/is-main-checked.sh && git branch --merged | egrep -v '(^\\*|main)' | xargs git branch -d",
                "git remote prune origin",
                "git gc"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(❗) 🔪 Kill all Node.js processes (💤)",
            "command": "echo 🔪 & taskkill /F /IM node.exe & taskkill /F /IM codex.exe & taskkill /F /IM claude.exe",
            "shellPath": "C:\\Windows\\System32\\cmd.exe",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "(❗) 🔪 Kill and release Git lock (💤)",
            "command": "echo 🔪 & taskkill /F /IM git.exe",
            "shellPath": "C:\\Windows\\System32\\cmd.exe",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🍴🔼 Make empty commit",
            "commands": ["git commit --allow-empty -m 'Empty commit'", "git push"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old)(❗) 🍴⏫ Git push all branches (⏳)",
            "command": "git diff --exit-code && ./scripts/push-all-branches.sh",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old)(❗) 🍴⏬ Git pull all branches (⏳)",
            "command": "git diff --exit-code && ./scripts/pull-all-branches.sh",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🌎 SSH to live",
            "command": "ssh root@live.ptbk.io",
            "execute": true,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🌎 SSH to preview",
            "command": "ssh root@preview.ptbk.io",
            "execute": true,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🌎 SSH to production",
            "command": "ssh root@production.ptbk.io",
            "execute": true,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🌎 SSH to LTS",
            "command": "ssh root@lts.ptbk.io",
            "execute": true,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🔼🆚 Use current version of Promptbook in all Projects / install ptbk (⏳)",
            "command": "npm run use-packages",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🏭🩹 Prettify all files (no commit)(⏳)",
            "command": "npx ts-node ./scripts/prettify-all/prettify-all.ts",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🏭🩹 Prettify all files (+commit)(⏳)",
            "command": "npx ts-node ./scripts/prettify-all/prettify-all.ts --commit",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🏭🔼 Update and install patch dependencies (+commit)(⏳)",
            "command": "npm run update-and-install-patch-dependencies-commit",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(❗) 🔼🚀 Release minor version (💸)(⏳)",
            "command": "npm version minor",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🔼🧪 Release pre-minor version (💸)(⏳)",
            "command": "npm version preminor",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🔼🧪 Release pre-release version (💤)(💸)(⏳)",
            "command": "npm version prerelease",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🔼🧪 Release pre-release version (ignore pre-checks) (💸)(⏳)",
            "command": "npm version prerelease --ignore-scripts && npm run postversion",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🔼🩹 Release patch version (💤)(💸)(⏳)",
            "command": "npm version patch",
            "onlySingle": true,
            "focus": true
        }
    ]
}

 succeeded in 6167ms:

 M prompts/2026-09-0260-ptbk-coder-working-from.md
tsconfig.jest.json
src\transpilers\openai-sdk\playground\tmp\package.json
package.json
jest.styleMock.js
jest.setup.js
jest.config.js
jest.yamlRawTransformer.js
AGENTS.md
agents-messages\2026-08-0400-jest-worker-force-exit-crashes-on-windows.message.md
examples\usage\other\vercel\package.json
apps\_boilerplate\package.json
apps\coder-landing\package.json
apps\vscode-extension\package.json
apps\book-editor-macos\package.json
apps\utils\package.json
apps\playground\package.json
apps\agents-server\package.json
book\package.json
book\jest.config.js
apps\book-components\package.json
Path
----
C:\Users\me\work\ai\promptbook


 succeeded in 7960ms:
src/collection\pipeline-collection\SimplePipelineCollection.ts:18: * @private internal function of `createPipelineCollectionFromJson`, use `createPipelineCollectionFromJson` instead
src/collection\pipeline-collection\SimplePipelineCollection.ts:29:     * Note: It is not recommended to use this constructor directly, use `createPipelineCollectionFromJson` *(or other variant)* instead
src/collection\pipeline-collection\pipelineCollectionToJson.ts:7: * Note: Functions `pipelineCollectionToJson` and `createPipelineCollectionFromJson` are complementary
src/collection\pipeline-collection\pipelineCollectionToJson.test.ts:5:import { createPipelineCollectionFromJson } from './constructors/createPipelineCollectionFromJson';
src/collection\pipeline-collection\pipelineCollectionToJson.test.ts:8:describe('createPipelineCollectionFromJson', () => {
src/collection\pipeline-collection\pipelineCollectionToJson.test.ts:39:        const collection = createPipelineCollectionFromJson(pipeline);
src/cli\cli-commands\start-pipelines-server.ts:7:import { createPipelineCollectionFromDirectory } from '../../collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory';
src/cli\cli-commands\start-pipelines-server.ts:125:            const collection = await createPipelineCollectionFromDirectory(path, tools, {
src/cli\cli-commands\make.ts:8:import { createPipelineCollectionFromDirectory } from '../../collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory';
src/cli\cli-commands\make.ts:84:    readonly collection: Awaited<ReturnType<typeof createPipelineCollectionFromDirectory>>;
src/cli\cli-commands\make.ts:188:    const makeCommandCollection = await createMakeCommandCollection(options, executionContext.tools);
src/cli\cli-commands\make.ts:192:    const artifacts = await createMakeCommandArtifacts(makeCommandCollection.collection);
src/cli\cli-commands\make.ts:280:async function createMakeCommandCollection(
src/cli\cli-commands\make.ts:285:    const collection = await createPipelineCollectionFromDirectory(options.path, tools, {
src/cli\cli-commands\make.ts:332:    collection: Awaited<ReturnType<typeof createPipelineCollectionFromDirectory>>,
src/cli\cli-commands\make.ts:459:                import { createPipelineCollectionFromJson } from '@promptbook/core';
src/cli\cli-commands\make.ts:482:                        pipelineCollection = createPipelineCollectionFromJson(
src/cli\cli-commands\make.ts:511:                import { createPipelineCollectionFromJson } from '@promptbook/core';
src/cli\cli-commands\make.ts:538:                        pipelineCollection = (createPipelineCollectionFromJson as (..._: any) => PipelineCollection)(
src/collection\pipeline-collection\constructors\createPipelineCollectionFromUrl.ts:4:import { createPipelineCollectionFromPromise } from './createPipelineCollectionFromPromise';
src/collection\pipeline-collection\constructors\createPipelineCollectionFromUrl.ts:7: * Options for `createPipelineCollectionFromDirectory` function
src/collection\pipeline-collection\constructors\createPipelineCollectionFromUrl.ts:32:export async function createPipelineCollectionFromUrl(
src/collection\pipeline-collection\constructors\createPipelineCollectionFromUrl.ts:38:    const collection = createPipelineCollectionFromPromise(async () => {
src/collection\pipeline-collection\constructors\createPipelineCollectionFromUrl.ts:55:    // TODO: Implement via createPipelineCollectionFromPromise
src/collection\pipeline-collection\constructors\createPipelineCollectionFromPromise.ts:5:import { createPipelineCollectionFromJson } from './createPipelineCollectionFromJson';
src/collection\pipeline-collection\constructors\createPipelineCollectionFromPromise.ts:15: * `createPipelineCollectionFromUrl` or `createPipelineCollectionFromDirectory`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromPromise.ts:21: * Note: Consider using  `createPipelineCollectionFromDirectory` or `createPipelineCollectionFromUrl`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromPromise.ts:29:export function createPipelineCollectionFromPromise(
src/collection\pipeline-collection\constructors\createPipelineCollectionFromPromise.ts:45:        collection = createPipelineCollectionFromJson(...promptbookSources);
src/collection\pipeline-collection\constructors\createPipelineCollectionFromPromise.test.ts:6:import { createPipelineCollectionFromPromise } from './createPipelineCollectionFromPromise';
src/collection\pipeline-collection\constructors\createPipelineCollectionFromPromise.test.ts:8:describe('createPipelineCollectionFromPromise', () => {
src/collection\pipeline-collection\constructors\createPipelineCollectionFromPromise.test.ts:36:    const collection = createPipelineCollectionFromPromise(async () => {
src/collection\pipeline-collection\constructors\createPipelineCollectionFromJson.ts:8: * Note: Functions `pipelineCollectionToJson` and `createPipelineCollectionFromJson` are complementary
src/collection\pipeline-collection\constructors\createPipelineCollectionFromJson.ts:16:export function createPipelineCollectionFromJson(...promptbooks: ReadonlyArray<PipelineJson>): PipelineCollection {
src/collection\pipeline-collection\constructors\createPipelineCollectionFromJson.test.ts:5:import { createPipelineCollectionFromJson } from './createPipelineCollectionFromJson';
src/collection\pipeline-collection\constructors\createPipelineCollectionFromJson.test.ts:7:describe('createPipelineCollectionFromJson', () => {
src/collection\pipeline-collection\constructors\createPipelineCollectionFromJson.test.ts:38:        const collection = createPipelineCollectionFromJson(pipeline);
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:26:import { createPipelineCollectionFromPromise } from './createPipelineCollectionFromPromise';
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:29: * Options for `createPipelineCollectionFromDirectory` function
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:32: * This is not same as `path` which is the first argument of `createPipelineCollectionFromDirectory` - it can be a subfolder
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:77: * Tools used by `createPipelineCollectionFromDirectory`.
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:79: * @private internal function of `createPipelineCollectionFromDirectory`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:84: * Tools with guaranteed filesystem access used by `createPipelineCollectionFromDirectory`.
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:86: * @private internal function of `createPipelineCollectionFromDirectory`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:93: * Resolved options used internally by `createPipelineCollectionFromDirectory`.
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:95: * @private internal function of `createPipelineCollectionFromDirectory`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:108: * @private internal function of `createPipelineCollectionFromDirectory`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:119: * @private internal function of `createPipelineCollectionFromDirectory`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:138:export async function createPipelineCollectionFromDirectory(
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:148:    const collection = createPipelineCollectionFromPromise(async () =>
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:162: * @private internal function of `createPipelineCollectionFromDirectory`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:182: * @private internal function of `createPipelineCollectionFromDirectory`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:207: * @private internal function of `createPipelineCollectionFromDirectory`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:243: * @private internal function of `createPipelineCollectionFromDirectory`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:259:    return createPipelineCollectionFromLoadedPipelines(rootPath, pipelinesFromFiles, options);
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:265: * @private internal function of `createPipelineCollectionFromDirectory`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:274: * @private internal function of `createPipelineCollectionFromDirectory`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:291: * @private internal function of `createPipelineCollectionFromDirectory`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:314: * @private internal function of `createPipelineCollectionFromDirectory`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:357: * @private internal function of `createPipelineCollectionFromDirectory`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:369: * @private internal function of `createPipelineCollectionFromDirectory`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:371:function createPipelineCollectionFromLoadedPipelines(
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:393: * @private internal function of `createPipelineCollectionFromDirectory`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:439: * @private internal function of `createPipelineCollectionFromDirectory`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:452: * @private internal function of `createPipelineCollectionFromDirectory`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:521: * @private internal function of `createPipelineCollectionFromDirectory`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:533: * @private internal function of `createPipelineCollectionFromDirectory`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:538:    const wrappedErrorMessage = createPipelineCollectionErrorMessage(error, fileName);
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:551: * @private internal function of `createPipelineCollectionFromDirectory`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:553:function createPipelineCollectionErrorMessage(error: Error, fileName: string_filename): string {
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:575: * @private internal function of `createPipelineCollectionFromDirectory`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:584: * @private internal function of `createPipelineCollectionFromDirectory`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts:593: * @private internal function of `createPipelineCollectionFromDirectory`
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.test.ts:5:import { createPipelineCollectionFromDirectory } from './createPipelineCollectionFromDirectory';
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.test.ts:7:describe('createPipelineCollectionFromDirectory', () => {
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.test.ts:10:        const collection = await createPipelineCollectionFromDirectory(
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.test.ts:34:        const collection = await createPipelineCollectionFromDirectory(
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.test.ts:56:        const collection = await createPipelineCollectionFromDirectory(
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.test.ts:77:                const collection = await createPipelineCollectionFromDirectory(
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.test.ts:94:                const collection = await createPipelineCollectionFromDirectory(
src/collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.test.ts:113:              const collection = await   createPipelineCollectionFromDirectory('./examples/pipelines',{}, {
src/book-2.0\book-language-documentation\englishBookLanguageManualDictionary.ts:143:                - Writing no \`FROM\` at all is the same as writing \`FROM @Adam\`, the core agent every agent inherits from.
src/book-2.0\book-language-documentation\englishBookLanguageManualDictionary.ts:145:                - An explicit parent replaces the implicit Adam parent; the selected parent's own \`FROM\` chain is then inherited normally.
src/book-2.0\book-language-documentation\englishBookLanguageManualDictionary.ts:148:                - Adam is the root agent and must explicitly use \`FROM @Null\` (\`@Void\` is equivalent). The Book editor warns about a different Adam parent and local inheritance cycles.
src/book-2.0\book-language-documentation\englishBookLanguageManualDictionary.ts:186:                - Compact references like \`@Agent Name\` and \`{Agent Name}\` are resolved by the Agents Server reference resolver.
src/book-2.0\book-language-documentation\englishBookLanguageManualDictionary.ts:232:                'Credentials are resolved from wallet records at runtime in Agents Server.',
src/book-2.0\book-language-documentation\englishBookLanguageManualDictionary.ts:244:                'Credentials are resolved from wallet-backed Google Calendar OAuth records at runtime in Agents Server.',
src/book-2.0\book-language-documentation\czechBookLanguageManualDictionary.ts:143:                - Když \`FROM\` není uvedeno vůbec, platí to samé jako \`FROM @Adam\`, tedy jádrový agent, ze kterého dědí každý agent.
src/book-2.0\book-language-documentation\czechBookLanguageManualDictionary.ts:145:                - Výslovně zvolený rodič nahradí implicitního Adama; vlastní řetězec \`FROM\` tohoto rodiče se pak zdědí běžným způsobem.
src/book-2.0\book-language-documentation\czechBookLanguageManualDictionary.ts:148:                - Adam je kořenový agent a musí výslovně použít \`FROM @Null\` (\`@Void\` je ekvivalentní). Editor knihy upozorní na jiného Adamova rodiče i na lokální cykly dědičnosti.
src/book-2.0\book-language-documentation\czechBookLanguageManualDictionary.ts:186:                - Zkrácené odkazy jako \`@Jméno agenta\` a \`{Jméno agenta}\` vyhodnocuje resolver odkazů v Agents Serveru.
apps/agents-server/src/utils\agentBook\downloadAgentBook.ts:50:async function resolveAgentBookDownloadErrorMessage(response: Response): Promise<string> {
apps/agents-server/src/utils\agentBook\downloadAgentBook.ts:78:        throw new PromptbookFetchError(await resolveAgentBookDownloadErrorMessage(response));
apps/agents-server/src/utils\agentAvatars\renderAgentAvatarVisualPng.ts:6:import { DEFAULT_AGENT_AVATAR_VISUAL_ID } from '../../../../../src/utils/agents/resolveAgentAvatarImageUrl';
apps/agents-server/src/utils\agentAccess.ts:43:export async function resolveAgentVisibility(agentIdentifier: string): Promise<AgentVisibility | null> {
apps/agents-server/src/utils\agentAccess.ts:100:export async function resolveAgentAccess(
apps/agents-server/src/utils\agentAccess.ts:104:    const [visibility, currentUser] = await Promise.all([resolveAgentVisibility(agentIdentifier), getCurrentUser()]);
src/book-2.0\agent-source\string_book.ts:53:// <- TODO: [🐱‍🚀] `GENESIS_BOOK` / `ADAM_BOOK` in `/agents/adam.book`
src/book-2.0\agent-source\pseudoAgentReferences.ts:113:export function resolvePseudoAgentKindFromReference(rawReference: string): PseudoAgentKind | null {
src/book-2.0\agent-source\pseudoAgentReferences.ts:130:export function resolvePseudoAgentKindFromUrl(agentUrl: string): PseudoAgentKind | null {
src/book-2.0\agent-source\pseudoAgentReferences.ts:153:    return resolvePseudoAgentKindFromUrl(agentUrl) !== null;
src/book-2.0\agent-source\pseudoAgentReferences.ts:199:    const pseudoAgentKind = resolvePseudoAgentKindFromReference(rawReference);
src/book-2.0\agent-source\pseudoAgentReferences.ts:204:    if (resolvePseudoAgentKindFromUrl(rawReference) === 'VOID') {
src/book-2.0\agent-source\pseudoAgentReferences.ts:221:    return resolvePseudoAgentKindFromReference(rawReference) === 'USER';
src/book-2.0\agent-source\parseAgentSource.ts:18: * - `createAgentModelRequirements` which is an asynchronous function that creates model requirements it applies each commitment one by one and works asynchronously.
src/book-2.0\agent-source\parseAgentSource.ts:24:    const resolvedAgentName = parseResult.agentName || createDefaultAgentName(agentSource);
src/book-2.0\agent-source\parseAgentSource.ts:29:    ensureMetaFullname(parsedProfile.meta, resolvedAgentName);
src/book-2.0\agent-source\parseAgentSource.ts:32:        agentName: normalizeAgentName(resolvedAgentName),
src/book-2.0\agent-source\parseAgentSource.test.ts:823:        it('ignores Adam agent in FROM commitment', () => {
src/book-2.0\agent-source\parseAgentSource.test.ts:827:                FROM Adam
src/book-2.0\agent-source\parseAgentSource\createCapabilitiesFromCommitment.ts:112:    if (reference === 'Adam' || reference === '' /* <- Note: Adam is implicit */) {
src/book-2.0\agent-source\normalizeAgentName.test.ts:7:        expect(normalizeAgentName(`Adam Smith`)).toBe(`adam-smith`);
src/book-2.0\agent-source\deduplicateSystemMessage.ts:77: * @private internal utility of `createAgentModelRequirementsWithCommitments`
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.unknown.test.ts:3:import { createAgentModelRequirementsWithCommitments } from './createAgentModelRequirementsWithCommitments';
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.unknown.test.ts:6:describe('createAgentModelRequirementsWithCommitments unknown commitments', () => {
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.unknown.test.ts:33:        const requirements = await createAgentModelRequirementsWithCommitments(agentSource);
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.ts:5:import { applyCommitmentsToAgentModelRequirements } from './createAgentModelRequirementsWithCommitments/applyCommitmentsToAgentModelRequirements';
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.ts:6:import { augmentAgentModelRequirementsFromSource } from './createAgentModelRequirementsWithCommitments/augmentAgentModelRequirementsFromSource';
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.ts:7:import { filterCommitmentsForAgentModelRequirements } from './createAgentModelRequirementsWithCommitments/filterCommitmentsForAgentModelRequirements';
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.ts:8:import { materializeInlineKnowledgeSources } from './createAgentModelRequirementsWithCommitments/materializeInlineKnowledgeSources';
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.ts:23: * @private internal utility of `createAgentModelRequirements`
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.ts:25:export async function createAgentModelRequirementsWithCommitments(
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.ts:48: * @private internal utility of `createAgentModelRequirementsWithCommitments`
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.ts:79: * @private internal utility of `createAgentModelRequirementsWithCommitments`
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.knowledge.test.ts:4:import { createAgentModelRequirementsWithCommitments } from './createAgentModelRequirementsWithCommitments';
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.knowledge.test.ts:7:describe('createAgentModelRequirementsWithCommitments with KNOWLEDGE', () => {
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.knowledge.test.ts:16:        const requirements = await createAgentModelRequirementsWithCommitments(book);
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.knowledge.test.ts:39:        const requirements = await createAgentModelRequirementsWithCommitments(book);
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.import.test.ts:3:import { createAgentModelRequirementsWithCommitments } from './createAgentModelRequirementsWithCommitments';
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.import.test.ts:6:describe('createAgentModelRequirementsWithCommitments with IMPORT', () => {
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.import.test.ts:14:        const result = await createAgentModelRequirementsWithCommitments(agentSource);
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.import.test.ts:28:        const result = await createAgentModelRequirementsWithCommitments(agentSource);
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.delete.test.ts:4:import { createAgentModelRequirementsWithCommitments } from './createAgentModelRequirementsWithCommitments';
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.delete.test.ts:17:        const modelRequirements = await createAgentModelRequirementsWithCommitments(book);
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.delete.test.ts:34:        const modelRequirements = await createAgentModelRequirementsWithCommitments(book);
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.delete.test.ts:58:        const modelRequirements = await createAgentModelRequirementsWithCommitments(book, undefined, {
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.closed.test.ts:3:import { createAgentModelRequirementsWithCommitments } from './createAgentModelRequirementsWithCommitments';
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.closed.test.ts:14:        const requirements = await createAgentModelRequirementsWithCommitments(agentSource);
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.closed.test.ts:27:        const requirements = await createAgentModelRequirementsWithCommitments(agentSource);
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.closed.test.ts:40:        const requirements = await createAgentModelRequirementsWithCommitments(agentSource);
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.closed.test.ts:54:        const requirements = await createAgentModelRequirementsWithCommitments(agentSource);
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.closed.test.ts:67:        const requirements = await createAgentModelRequirementsWithCommitments(agentSource);
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.blocks.test.ts:3:import { createAgentModelRequirementsWithCommitments } from './createAgentModelRequirementsWithCommitments';
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.blocks.test.ts:6:describe('createAgentModelRequirementsWithCommitments with code blocks in commitments', () => {
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.blocks.test.ts:23:        const requirements = await createAgentModelRequirementsWithCommitments(agentSource);
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts:4:import { createAgentModelRequirementsWithCommitments } from './createAgentModelRequirementsWithCommitments';
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts:7:describe('createAgentModelRequirementsWithCommitments agent reference resolver', () => {
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts:9:        const resolver: AgentReferenceResolver = {
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts:10:            resolveCommitmentContent: jest.fn(async () => 'https://example.com/agents/resolved'),
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts:21:        const modelRequirements = await createAgentModelRequirementsWithCommitments(book, undefined, {
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts:26:        expect(resolver.resolveCommitmentContent).toHaveBeenCalledWith('FROM', '{foo}');
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts:30:        const resolver: AgentReferenceResolver = {
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts:31:            resolveCommitmentContent: jest.fn(async () => {
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts:44:        const modelRequirements = await createAgentModelRequirementsWithCommitments(book, undefined, {
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments\ParsedAgentSourceWithCommitments.ts:6: * @private internal type of `createAgentModelRequirementsWithCommitments`
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments\materializeInlineKnowledgeSources.ts:16: * @private function of `createAgentModelRequirementsWithCommitments`
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments\filterCommitmentsForAgentModelRequirements.ts:28: * @private function of `createAgentModelRequirementsWithCommitments`
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments\augmentAgentModelRequirementsFromSource.ts:6:import { extractMcpServers } from '../createAgentModelRequirements';
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments\augmentAgentModelRequirementsFromSource.ts:39: * @private function of `createAgentModelRequirementsWithCommitments`
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments\applyCommitmentsToAgentModelRequirements.ts:25: * @private function of `createAgentModelRequirementsWithCommitments`
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments\applyCommitmentsToAgentModelRequirements.ts:37:        const commitmentContent = await resolveCommitmentContent(commitment, options?.agentReferenceResolver);
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments\applyCommitmentsToAgentModelRequirements.ts:59:async function resolveCommitmentContent(
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments\applyCommitmentsToAgentModelRequirements.ts:68:        return await agentReferenceResolver.resolveCommitmentContent(commitment.type, commitment.content);
src/book-2.0\agent-source\createAgentModelRequirementsWithCommitments\applyCommitmentsToAgentModelRequirements.ts:105: * @private internal utility of `resolveCommitmentContent`
src/book-2.0\agent-source\CreateAgentModelRequirementsOptions.ts:6: * Options for `createAgentModelRequirements` and `createAgentModelRequirementsWithCommitments`.
src/book-2.0\agent-source\createAgentModelRequirements.writing.test.ts:3:import { createAgentModelRequirements } from './createAgentModelRequirements';
src/book-2.0\agent-source\createAgentModelRequirements.writing.test.ts:6:describe('createAgentModelRequirements writing commitments', () => {
src/book-2.0\agent-source\createAgentModelRequirements.writing.test.ts:18:        const requirements = await createAgentModelRequirements(agentSource);
src/book-2.0\agent-source\createAgentModelRequirements.writing.test.ts:41:        const requirements = await createAgentModelRequirements(agentSource);
src/book-2.0\agent-source\createAgentModelRequirements.writing.test.ts:60:        const requirements = await createAgentModelRequirements(agentSource);
src/book-2.0\agent-source\createAgentModelRequirements.ts:10:import { createAgentModelRequirementsWithCommitments } from './createAgentModelRequirementsWithCommitments';
src/book-2.0\agent-source\createAgentModelRequirements.ts:20: * - `createAgentModelRequirements` which is an asynchronous function that creates model requirements it applies each commitment one by one and works asynchronous.
src/book-2.0\agent-source\createAgentModelRequirements.ts:30:export async function createAgentModelRequirements(
src/book-2.0\agent-source\createAgentModelRequirements.ts:41:        return createAgentModelRequirementsWithCommitments(agentSource, selectedModelName, options);
src/book-2.0\agent-source\createAgentModelRequirements.ts:45:    return createAgentModelRequirementsWithCommitments(agentSource, modelName, options);
src/book-2.0\agent-source\createAgentModelRequirements.ts:56: * @private function of `createAgentModelRequirements`
src/book-2.0\agent-source\createAgentModelRequirements.ts:119: * @deprecated Use createAgentModelRequirements instead
src/book-2.0\agent-source\createAgentModelRequirements.ts:124:    const modelRequirements = await createAgentModelRequirements(agentSource);
src/book-2.0\agent-source\createAgentModelRequirements.tools.test.ts:4:import { createAgentModelRequirements } from './createAgentModelRequirements';
src/book-2.0\agent-source\createAgentModelRequirements.tools.test.ts:17:        const requirements = await createAgentModelRequirements(agentSource);
src/book-2.0\agent-source\createAgentModelRequirements.tools.test.ts:35:            resolveCommitmentContent: async (_commitmentType, rawContent) =>
src/book-2.0\agent-source\createAgentModelRequirements.tools.test.ts:51:        const requirements = await createAgentModelRequirements(agentSource, undefined, undefined, undefined, {
src/book-2.0\agent-source\createAgentModelRequirements.tools.test.ts:84:        const requirements = await createAgentModelRequirements(agentSource, undefined, undefined, undefined, {
src/book-2.0\agent-source\createAgentModelRequirements.tools.test.ts:86:                resolveCommitmentContent: async (_commitmentType, rawContent) => rawContent,
src/book-2.0\agent-source\createAgentModelRequirements.tools.test.ts:122:        const requirements = await createAgentModelRequirements(agentSource, undefined, undefined, undefined, {
src/book-2.0\agent-source\createAgentModelRequirements.tools.test.ts:124:                resolveCommitmentContent: async (_commitmentType, rawContent) =>
src/book-2.0\agent-source\createAgentModelRequirements.tools.test.ts:152:        const requirements = await createAgentModelRequirements(agentSource);
src/book-2.0\agent-source\createAgentModelRequirements.tools.test.ts:183:        const requirements = await createAgentModelRequirements(agentSource);
src/book-2.0\agent-source\createAgentModelRequirements.tools.test.ts:213:        const requirements = await createAgentModelRequirements(agentSource);
src/book-2.0\agent-source\createAgentModelRequirements.tools.test.ts:226:        const requirements = await createAgentModelRequirements(agentSource);
src/book-2.0\agent-source\createAgentModelRequirements.tools.test.ts:238:        const requirements = await createAgentModelRequirements(agentSource);
src/book-2.0\agent-source\createAgentModelRequirements.deduplication.test.ts:3:import { createAgentModelRequirements } from './createAgentModelRequirements';
src/book-2.0\agent-source\createAgentModelRequirements.deduplication.test.ts:13: * @private test utility of `createAgentModelRequirements.deduplication.test.ts`
src/book-2.0\agent-source\createAgentModelRequirements.deduplication.test.ts:19:describe('createAgentModelRequirements system message deduplication', () => {
src/book-2.0\agent-source\createAgentModelRequirements.deduplication.test.ts:29:        const requirements = await createAgentModelRequirements(agentSource);
src/book-2.0\agent-source\createAgentModelRequirements.deduplication.test.ts:48:        const requirements = await createAgentModelRequirements(agentSource);
src/book-2.0\agent-source\createAgentModelRequirements.deduplication.test.ts:70:        const requirements = await createAgentModelRequirements(agentSource);
src/book-2.0\agent-source\createAgentModelRequirements.deduplication.test.ts:87:        const requirements = await createAgentModelRequirements(agentSource);
src/book-2.0\agent-source\createAgentModelRequirements.deduplication.test.ts:102:        const requirements = await createAgentModelRequirements(agentSource);
src/book-2.0\agent-source\createAgentModelRequirements.commitmentAggregation.test.ts:3:import { createAgentModelRequirements } from './createAgentModelRequirements';
src/book-2.0\agent-source\createAgentModelRequirements.commitmentAggregation.test.ts:13: * @private test utility of `createAgentModelRequirements.commitmentAggregation.test.ts`
src/book-2.0\agent-source\createAgentModelRequirements.commitmentAggregation.test.ts:19:describe('createAgentModelRequirements commitment aggregation', () => {
src/book-2.0\agent-source\createAgentModelRequirements.commitmentAggregation.test.ts:29:        const requirements = await createAgentModelRequirements(agentSource);
src/book-2.0\agent-source\createAgentModelRequirements.commitmentAggregation.test.ts:49:        const requirements = await createAgentModelRequirements(agentSource);
src/book-2.0\agent-source\communication-samples.test.ts:3:import { createAgentModelRequirementsWithCommitments } from './createAgentModelRequirementsWithCommitments';
src/book-2.0\agent-source\communication-samples.test.ts:52:        const requirements = await createAgentModelRequirementsWithCommitments(agentSource);
src/book-2.0\agent-source\communication-samples.test.ts:84:        const requirements = await createAgentModelRequirementsWithCommitments(agentSource);
src/book-2.0\agent-source\communication-samples.test.ts:105:        const requirements = await createAgentModelRequirementsWithCommitments(agentSource);
src/book-2.0\agent-source\communication-samples.test.ts:147:        const requirements = await createAgentModelRequirementsWithCommitments(agentSource);
src/book-2.0\agent-source\AgentReferenceResolver.ts:19:    resolveCommitmentContent(commitmentType: BookCommitment, content: string): Promise<string>;
apps/agents-server/src/utils\agentChatInputPlaceholder.ts:12:export function resolveAgentChatInputPlaceholder(inputPlaceholder: string | undefined): string {
apps/agents-server/src/utils\agentChatInputPlaceholder.test.ts:4:    resolveAgentChatInputPlaceholder,
apps/agents-server/src/utils\agentChatInputPlaceholder.test.ts:7:describe('resolveAgentChatInputPlaceholder', () => {
apps/agents-server/src/utils\agentChatInputPlaceholder.test.ts:9:        expect(resolveAgentChatInputPlaceholder(undefined)).toBe(DEFAULT_AGENT_CHAT_INPUT_PLACEHOLDER);
apps/agents-server/src/utils\agentChatInputPlaceholder.test.ts:13:        expect(resolveAgentChatInputPlaceholder('   ')).toBe(DEFAULT_AGENT_CHAT_INPUT_PLACEHOLDER);
apps/agents-server/src/utils\agentChatInputPlaceholder.test.ts:17:        expect(resolveAgentChatInputPlaceholder('  Ask me anything...  ')).toBe('Ask me anything...');
apps/agents-server/src/utils\agentIdentifier.ts:24:export function resolveAgentRouteIdentifier(agent: AgentRouteIdentifierSource): string {
apps/agents-server/src/utils\agentIdentifier.test.ts:1:import { buildAgentNameOrIdFilter, isSameAgentPermanentId, resolveAgentRouteIdentifier } from './agentIdentifier';
apps/agents-server/src/utils\agentIdentifier.test.ts:4:    describe('resolveAgentRouteIdentifier', () => {
apps/agents-server/src/utils\agentIdentifier.test.ts:6:            expect(resolveAgentRouteIdentifier({ agentName: 'Lawyer', permanentId: 'doQMRg82izNfJa' })).toBe(
apps/agents-server/src/utils\agentIdentifier.test.ts:12:            expect(resolveAgentRouteIdentifier({ agentName: 'Lawyer', permanentId: null })).toBe('Lawyer');
apps/agents-server/src/utils\agentGoalChat.ts:22:export { resolveEffectiveAgentGoal } from './agentGoalChat/resolveEffectiveAgentGoal';
apps/agents-server/src/utils\agentGoalChat.ts:23:export { resolveAgentGoalChatOwnerUserId } from './agentGoalChat/resolveAgentGoalChatOwnerUserId';
src/cli\cli-commands\coder\list.ts:61:            const resolvedCoderAgentBook =
src/cli\cli-commands\coder\list.ts:65:                          await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent')
src/cli\cli-commands\coder\list.ts:66:                      ).resolveCoderAgentBook(agent, process.cwd());
src/cli\cli-commands\coder\list.ts:70:                resolvedCoderAgentBook === undefined
src/cli\cli-commands\coder\list.ts:75:                          agentReferences: resolvedCoderAgentBook?.agentReferences,
src/cli\cli-commands\coder\list.test.ts:3:import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
src/cli\cli-commands\coder\list.test.ts:11:jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
src/cli\cli-commands\coder\list.test.ts:12:    resolveCoderAgentBook: jest.fn(),
src/cli\cli-commands\coder\list.test.ts:25:function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
src/cli\cli-commands\coder\list.test.ts:26:    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
src/cli\cli-commands\coder\ensureCoderDeveloperAgentFile.ts:39:    await copyFile(await resolveDefaultCoderDeveloperAgentFilePath(), absoluteFilePath);
src/cli\cli-commands\coder\ensureCoderDeveloperAgentFile.ts:46:async function resolveDefaultCoderDeveloperAgentFilePath(): Promise<string> {
apps/agents-server/src/utils\agentsTransfer\importAgentsFromFiles.ts:351:        const duplicateState = resolveAgentsImportDuplicateState(entry, knownAgentSourcesByName);
apps/agents-server/src/utils\agentsTransfer\importAgentsFromFiles.ts:360:        const sortOrder = await resolveNextImportAgentSortOrder({
apps/agents-server/src/utils\agentsTransfer\importAgentsFromFiles.ts:516:async function resolveNextImportAgentSortOrder(options: {
apps/agents-server/src/utils\agentsTransfer\importAgentsFromFiles.ts:668:function resolveAgentsImportDuplicateState(
src/cli\cli-commands\agents-server\startAgentsServer.ts:15:import { resolveAgentsServerRuntimePaths } from './startAgentsServer/AgentsServerRuntimePaths';
src/cli\cli-commands\agents-server\startAgentsServer.ts:39:    const runtimePaths = await resolveAgentsServerRuntimePaths();
apps/agents-server/src/utils\agentOrganization\loadAgentOrganizationState.ts:11:import { resolveStoredAgentStates } from '../resolveStoredAgentState';
apps/agents-server/src/utils\agentOrganization\loadAgentOrganizationState.ts:78:        resolvedAgentProfile: AgentBasicInformation;
apps/agents-server/src/utils\agentOrganization\loadAgentOrganizationState.ts:81:    const profile = row.resolvedAgentProfile;
apps/agents-server/src/utils\agentOrganization\loadAgentOrganizationState.ts:201:    const resolvedAgents = await resolveStoredAgentStates(
apps/agents-server/src/utils\agentOrganization\loadAgentOrganizationState.ts:212:    const agents = resolvedAgents.map((agent) =>
apps/agents-server/src/utils\agentOrganization\loadAgentOrganizationState.ts:215:            resolvedAgentProfile: agent.resolvedAgentProfile,
src/cli\cli-commands\agents-server\startAgentsServer\startNextServer.ts:9:import { resolveAgentsServerChildHostname } from './resolveAgentsServerChildHostname';
src/cli\cli-commands\agents-server\startAgentsServer\startNextServer.ts:37:    const hostname = resolveAgentsServerChildHostname(options.childEnvironment);
src/cli\cli-commands\agents-server\startAgentsServer\resolveAgentsServerChildHostname.ts:13:export function resolveAgentsServerChildHostname(environment: NodeJS.ProcessEnv): string | null {
src/cli\cli-commands\agents-server\startAgentsServer\AgentsServerRuntimePaths.ts:3:import { resolveAgentsServerAppPath } from '../buildAgentsServer/resolveAgentsServerAppPath';
src/cli\cli-commands\agents-server\startAgentsServer\AgentsServerRuntimePaths.ts:31:export async function resolveAgentsServerRuntimePaths(): Promise<AgentsServerRuntimePaths> {
src/cli\cli-commands\agents-server\startAgentsServer\AgentsServerRuntimePaths.ts:33:    const appPath = await resolveAgentsServerAppPath();
src/cli\cli-commands\agents-server\buildAgentsServer.ts:15:export { resolveAgentsServerAppPath } from './buildAgentsServer/resolveAgentsServerAppPath';
src/cli\cli-commands\agents-server\buildAgentsServer.ts:16:export { resolveAgentsServerBuildAppPath } from './buildAgentsServer/resolveAgentsServerBuildAppPath';
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:13:import { resolveAgentsServerBuildAppPath } from './buildAgentsServer/resolveAgentsServerBuildAppPath';
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:172:        const materializedAppPath = await resolveAgentsServerBuildAppPath({
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.ts:6:import { resolvePseudoAgentDescriptor } from '../pseudoAgents';
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.ts:69:async function resolveAgentRouteTargetUncached(
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.ts:94:    const pseudoDescriptor = resolvePseudoAgentDescriptor(normalizedReference);
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.ts:107:        const fastLocalRouteTarget = await resolveFastLocalAgentRouteTarget(normalizedReference, localServerUrl);
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.ts:113:    const resolver = await $provideAgentReferenceResolver({ forceRefresh: options?.forceRefresh });
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.ts:117:        resolvedUrlValue = await resolver.resolveCommitmentContent('TEAM', `{${normalizedReference}}`);
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.ts:127:        return resolveLocalAgentRouteTarget(normalizedReference, localServerUrl);
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.ts:130:    const resolvedAgentUrl = parseAgentUrl(resolvedUrlValue);
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.ts:131:    if (resolvedAgentUrl === null) {
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.ts:135:    const canonicalAgentId = extractAgentIdentifier(resolvedAgentUrl);
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.ts:140:    const resolvedServerUrl = normalizeServerUrl(resolvedAgentUrl.origin);
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.ts:145:            url: resolvedAgentUrl.toString(),
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.ts:167:async function resolveFastLocalAgentRouteTarget(
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.ts:191:    return resolveAgentRouteTargetUncached(rawReference);
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.ts:201:export async function resolveAgentRouteTarget(
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.ts:208:        return resolveAgentRouteTargetUncached(rawReference, options);
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.ts:308:async function resolveLocalAgentRouteTarget(
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.test.ts:1:import { resolveAgentRouteTarget } from './resolveAgentRouteTarget';
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.test.ts:34:            resolveCommitmentContent: jest.fn(async () => '{Lawyer}'),
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.test.ts:62:describe('resolveAgentRouteTarget', () => {
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.test.ts:68:        const result = await resolveAgentRouteTarget('Lawyer');
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.test.ts:82:        const result = await resolveAgentRouteTarget(embeddedIdentifier);
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.test.ts:92:        const result = await resolveAgentRouteTarget('User');
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.test.ts:103:        await resolveAgentRouteTarget('Lawyer', { forceRefresh: true });
src/cli\cli-commands\agents-server\buildAgentsServer\writeAgentsServerBuildCache.ts:8:import { resolveAgentsServerBuildOutputPath } from './resolveAgentsServerBuildOutputPath';
src/cli\cli-commands\agents-server\buildAgentsServer\writeAgentsServerBuildCache.ts:16:    const buildOutputPath = resolveAgentsServerBuildOutputPath(options);
src/cli\cli-commands\agents-server\buildAgentsServer\resolveAgentsServerBuildOutputPath.ts:14:export function resolveAgentsServerBuildOutputPath(options: AgentsServerBuildCacheOptions): string {
src/cli\cli-commands\agents-server\buildAgentsServer\resolveAgentsServerBuildAppPath.ts:11:export async function resolveAgentsServerBuildAppPath(options: {
src/cli\cli-commands\agents-server\buildAgentsServer\resolveAgentsServerAppPath.ts:11:export async function resolveAgentsServerAppPath(): Promise<string> {
src/cli\cli-commands\agents-server\buildAgentsServer\readAgentsServerBuildCache.ts:7:import { resolveAgentsServerBuildOutputPath } from './resolveAgentsServerBuildOutputPath';
src/cli\cli-commands\agents-server\buildAgentsServer\readAgentsServerBuildCache.ts:19:            join(resolveAgentsServerBuildOutputPath(options), AGENTS_SERVER_BUILD_CACHE_FILENAME),
src/cli\cli-commands\agents-server\buildAgentsServer\PreparedAgentsServerRuntime.ts:13: * Runtime paths resolved for Agents Server commands before choosing build or dev execution.
src/cli\cli-commands\agents-server\buildAgentsServer\prepareAgentsServerRuntime.ts:3:import { resolveAgentsServerAppPath } from './resolveAgentsServerAppPath';
src/cli\cli-commands\agents-server\buildAgentsServer\prepareAgentsServerRuntime.ts:4:import { resolveAgentsServerBuildAppPath } from './resolveAgentsServerBuildAppPath';
src/cli\cli-commands\agents-server\buildAgentsServer\prepareAgentsServerRuntime.ts:20:    const appPath = await resolveAgentsServerBuildAppPath({
src/cli\cli-commands\agents-server\buildAgentsServer\prepareAgentsServerRuntime.ts:21:        sourceAppPath: options.appPath ?? (await resolveAgentsServerAppPath()),
src/cli\cli-commands\agents-server\buildAgentsServer\isAgentsServerBuildCacheCurrent.ts:7:import { resolveAgentsServerBuildOutputPath } from './resolveAgentsServerBuildOutputPath';
src/cli\cli-commands\agents-server\buildAgentsServer\isAgentsServerBuildCacheCurrent.ts:21:    const buildOutputPath = resolveAgentsServerBuildOutputPath(options);
src/cli\cli-commands\agent\exec.ts:15:    resolveRequiredAgentMessage,
src/cli\cli-commands\agent\exec.ts:16:    resolveRequiredAgentPath,
src/cli\cli-commands\agent\exec.ts:49:                agentPath: resolveRequiredAgentPath(options),
src/cli\cli-commands\agent\exec.ts:51:                message: resolveRequiredAgentMessage(options),
src/cli\cli-commands\agent\chat.ts:13:import { normalizeAgentCommandRunnerOptions, resolveRequiredAgentPath } from './agentCliOptions';
src/cli\cli-commands\agent\chat.ts:44:                agentPath: resolveRequiredAgentPath(options),
src/cli\cli-commands\agent\agentCliOptions.ts:56:export function resolveRequiredAgentPath(cliOptions: AgentCommandCliOptions): string {
src/cli\cli-commands\agent\agentCliOptions.ts:71:export function resolveRequiredAgentMessage(cliOptions: AgentCommandCliOptions): string {
apps/agents-server/src/utils\agentBrowserProfile\$provideAgentBrowserProfile.ts:3:import { resolveWalletAgentPermanentId } from '../userWallet/resolveWalletAgentPermanentId';
apps/agents-server/src/utils\agentBrowserProfile\$provideAgentBrowserProfile.ts:9:import { resolveDefaultAgentBrowserProfileDirectory } from './resolveAgentBrowserProfileDirectory';
apps/agents-server/src/utils\agentBrowserProfile\$provideAgentBrowserProfile.ts:58:    const agentPermanentId = await resolveWalletAgentPermanentId(options.agentIdentifier);
apps/agents-server/src/utils\agentBrowserProfile\$provideAgentBrowserProfile.ts:65:    const directory = directoryFromWallet || resolveDefaultAgentBrowserProfileDirectory(agentPermanentId);
apps/agents-server/src/utils\agentGoalChat\ensureAgentGoalChat.ts:7:import { resolveAgentGoalChatOwnerUserId } from './resolveAgentGoalChatOwnerUserId';
apps/agents-server/src/utils\agentGoalChat\ensureAgentGoalChat.ts:29:            userId: await resolveAgentGoalChatOwnerUserId(agentPermanentId),
apps/agents-server/src/utils\agentGoalChat\recordAgentGoalChatLifecycleNote.ts:9:import { resolveEffectiveAgentGoal } from './resolveEffectiveAgentGoal';
apps/agents-server/src/utils\agentGoalChat\recordAgentGoalChatLifecycleNote.ts:26:        const currentGoal = resolveEffectiveAgentGoal(options.agentSource);
apps/agents-server/src/utils\agentGoalChat\resolveEffectiveAgentGoal.ts:13:export function resolveEffectiveAgentGoal(agentSource: string_book): string | null {
apps/agents-server/src/utils\agentGoalChat\resolveEffectiveAgentGoal.test.ts:4:import { resolveEffectiveAgentGoal } from './resolveEffectiveAgentGoal';
apps/agents-server/src/utils\agentGoalChat\resolveEffectiveAgentGoal.test.ts:6:describe('resolveEffectiveAgentGoal', () => {
apps/agents-server/src/utils\agentGoalChat\resolveEffectiveAgentGoal.test.ts:18:        expect(resolveEffectiveAgentGoal(agentSource)).toBe(
apps/agents-server/src/utils\agentGoalChat\resolveEffectiveAgentGoal.test.ts:24:        expect(resolveEffectiveAgentGoal('Helper\n\nRULE Be useful.' as string_book)).toBeNull();
apps/agents-server/src/utils\agentGoalChat\resolveAgentGoalChatOwnerUserId.ts:16:export async function resolveAgentGoalChatOwnerUserId(agentPermanentId: string): Promise<number> {
apps/agents-server/src/utils\agentGoalChat\resolveAgentGoalChatOwnerUserId.ts:32: * @private function of `resolveAgentGoalChatOwnerUserId`
apps/agents-server/src/utils\agentGoalChat\resolveAgentGoalChatOwnerUserId.ts:66: * @private function of `resolveAgentGoalChatOwnerUserId`
apps/agents-server/src/utils\agentReferenceResolver\AgentReferenceResolutionIssue.ts:46:    resolver: AgentReferenceResolver,
apps/agents-server/src/utils\agentReferenceResolver\AgentReferenceResolutionIssue.ts:47:): resolver is IssueTrackingAgentReferenceResolver {
apps/agents-server/src/utils\agentReferenceResolver\AgentReferenceResolutionIssue.ts:48:    return typeof (resolver as Partial<IssueTrackingAgentReferenceResolver>).consumeResolutionIssues === 'function';
apps/agents-server/src/utils\agentReferenceResolver\AgentReferenceResolutionIssue.ts:58:    resolver?: AgentReferenceResolver,
apps/agents-server/src/utils\agentReferenceResolver\$provideAgentReferenceResolver.ts:83:            const resolver = await createServerAgentReferenceResolver({
apps/agents-server/src/utils\agentReferenceResolver\$provideAgentReferenceResolver.test.ts:52:    resolveCommitmentContent: (_type: string, _content: string) => Promise<string>;
apps/agents-server/src/utils\agentReferenceResolver\$provideAgentReferenceResolver.test.ts:55:        resolveCommitmentContent: async () => token,
apps/agents-server/src/utils\agentReferenceResolver\$provideAgentReferenceResolver.test.ts:83:        resolveCommitmentContent: (_type: string, _content: string) => Promise<string>;
apps/agents-server/src/utils\agentReferenceResolver\$provideAgentReferenceResolver.test.ts:85:        return resolver as Awaited<ReturnType<typeof createServerAgentReferenceResolver>>;
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.ts:5:import { resolvePseudoAgentKindFromReference } from '../../../../../src/book-2.0/agent-source/pseudoAgentReferences';
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.ts:52:    readonly resolvedAgentName: string;
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.ts:53:    readonly unresolvedAgentSource: string_book;
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.ts:210:        resolveCommitmentContent: async (commitmentType, content) => {
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.ts:225:                    if (resolvePseudoAgentKindFromReference(tokenReference)) {
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.ts:248:            return fallbackResolver.resolveCommitmentContent(commitmentType, resolvedContent);
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.ts:266:function isIssueTrackingResolver(resolver?: AgentReferenceResolver): resolver is IssueTrackingResolver {
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.ts:276:export async function resolveBookScopedAgentContext(options: {
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.ts:290:        const resolvedAgentName = parsedParentProfile.agentName || normalizeAgentName(agentIdentifier);
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.ts:297:            `${normalizedServerUrl}/agents/${encodeURIComponent(resolvedAgentName)}`,
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.ts:305:            resolvedAgentName,
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.ts:306:            unresolvedAgentSource: parentAgentSource,
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.ts:331:    const resolvedAgentName = parsedEmbeddedProfile.agentName || parsedIdentifier.embeddedAgentName;
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.ts:347:        resolvedAgentName,
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.ts:348:        unresolvedAgentSource: embeddedSource,
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.test.ts:11:    resolveBookScopedAgentContext,
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.test.ts:20:function createMockCollection(sourceById: Record<string, string_book>): AgentCollection {
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.test.ts:84:        const resolver = createBookScopedAgentReferenceResolver({
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.test.ts:89:                resolveCommitmentContent: async (_commitmentType, content) =>
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.test.ts:94:        const resolved = await resolver.resolveCommitmentContent('TEAM', '{Copywriter} and {External Agent}');
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.test.ts:111:        const resolver = createBookScopedAgentReferenceResolver({
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.test.ts:116:                resolveCommitmentContent: async (_commitmentType, content) =>
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.test.ts:121:        await expect(resolver.resolveCommitmentContent('FROM', '@Null')).resolves.toBe('{Void}');
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.test.ts:134:        const collection = createMockCollection({
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.test.ts:139:        const context = await resolveBookScopedAgentContext({
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.test.ts:149:        expect(context.resolvedAgentName).toBe('copywriter');
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.test.ts:150:        expect(context.unresolvedAgentSource).toContain('Copywriter');
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.test.ts:38:function createMockAgentCollection(agentRecords: ReadonlyArray<LocalAgentRecord>): AgentCollection {
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.test.ts:98:        collection: createMockAgentCollection(agentRecords),
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.test.ts:132:        await expect(resolveDiagnostics(currentAgentSource, 'agent-a-id', persistedAgentRecords)).resolves.toEqual([
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.test.ts:179:        const diagnostics = await resolveDiagnostics(currentAgentSource, 'agent-a-id', persistedAgentRecords);
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.test.ts:221:        await expect(resolveDiagnostics(currentAgentSource, 'agent-a-id', persistedAgentRecords)).resolves.toEqual([]);
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.test.ts:224:    it('uses only the last FROM when validating Adam', async () => {
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.test.ts:226:            Adam
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.test.ts:248:        await expect(resolveDiagnostics(adamSource, 'adam-id', persistedAgentRecords)).resolves.toEqual([]);
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.test.ts:251:    it('warns when Adam has no explicit Null or Void parent', async () => {
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.test.ts:253:            Adam
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.test.ts:274:        await expect(resolveDiagnostics(adamSource, 'adam-id', persistedAgentRecords)).resolves.toEqual([
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.test.ts:278:                message: expect.stringContaining('core `Adam` agent must explicitly use `FROM @Null`'),
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.test.ts:283:    it('warns on Adam title when its source omits FROM entirely', async () => {
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.test.ts:285:            Adam
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.test.ts:297:        await expect(resolveDiagnostics(adamSource, 'adam-id', persistedAgentRecords)).resolves.toEqual([
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.test.ts:301:                message: expect.stringContaining('core `Adam` agent must explicitly use `FROM @Null`'),
apps/agents-server/src/utils\agentBrowserProfile\resolveAgentBrowserProfileDirectory.ts:21:export function resolveAgentBrowserProfileStorageDirectory(): string {
apps/agents-server/src/utils\agentBrowserProfile\resolveAgentBrowserProfileDirectory.ts:39:export function resolveDefaultAgentBrowserProfileDirectory(agentPermanentId: string): string {
apps/agents-server/src/utils\agentBrowserProfile\resolveAgentBrowserProfileDirectory.ts:52:    return `${resolveAgentBrowserProfileStorageDirectory()}/agent-${directoryName}`;
apps/agents-server/src/utils\agentBrowserProfile\resolveAgentBrowserProfileDirectory.ts:58: * @private function of `resolveDefaultAgentBrowserProfileDirectory`
apps/agents-server/src/utils\agentBrowserProfile\resolveAgentBrowserProfileDirectory.test.ts:3:    resolveAgentBrowserProfileStorageDirectory,
apps/agents-server/src/utils\agentBrowserProfile\resolveAgentBrowserProfileDirectory.test.ts:4:    resolveDefaultAgentBrowserProfileDirectory,
apps/agents-server/src/utils\agentBrowserProfile\resolveAgentBrowserProfileDirectory.test.ts:5:} from './resolveAgentBrowserProfileDirectory';
apps/agents-server/src/utils\agentBrowserProfile\resolveAgentBrowserProfileDirectory.test.ts:7:describe('resolveDefaultAgentBrowserProfileDirectory', () => {
apps/agents-server/src/utils\agentBrowserProfile\resolveAgentBrowserProfileDirectory.test.ts:25:        const directory = resolveDefaultAgentBrowserProfileDirectory('agent-permanent-id-123');
apps/agents-server/src/utils\agentBrowserProfile\resolveAgentBrowserProfileDirectory.test.ts:27:        expect(directory).toBe(`${resolveAgentBrowserProfileStorageDirectory()}/agent-agent-permanent-id-123`);
apps/agents-server/src/utils\agentBrowserProfile\resolveAgentBrowserProfileDirectory.test.ts:29:        expect(directory).toBe(resolveDefaultAgentBrowserProfileDirectory('agent-permanent-id-123'));
apps/agents-server/src/utils\agentBrowserProfile\resolveAgentBrowserProfileDirectory.test.ts:35:        const directory = resolveDefaultAgentBrowserProfileDirectory('agent/../..\\evil id');
apps/agents-server/src/utils\agentBrowserProfile\resolveAgentBrowserProfileDirectory.test.ts:44:        const directory = resolveDefaultAgentBrowserProfileDirectory('abc');
apps/agents-server/src/utils\agentBrowserProfile\resolveAgentBrowserProfileDirectory.test.ts:50:        expect(() => resolveDefaultAgentBrowserProfileDirectory('   ')).toThrow(/agentPermanentId/);
apps/agents-server/src/utils\agentReferenceResolver\createUnresolvedAgentReferenceDiagnostics.test.ts:6:import { createUnresolvedAgentReferenceDiagnostics } from './createUnresolvedAgentReferenceDiagnostics';
apps/agents-server/src/utils\agentReferenceResolver\createUnresolvedAgentReferenceDiagnostics.test.ts:15:        resolveCommitmentContent: async (commitmentType, content) => {
apps/agents-server/src/utils\agentReferenceResolver\createUnresolvedAgentReferenceDiagnostics.test.ts:41:describe('createUnresolvedAgentReferenceDiagnostics', () => {
apps/agents-server/src/utils\agentReferenceResolver\createUnresolvedAgentReferenceDiagnostics.test.ts:52:        const result = await createUnresolvedAgentReferenceDiagnostics(agentSource, resolver);
apps/agents-server/src/utils\agentReferenceResolver\createUnresolvedAgentReferenceDiagnostics.test.ts:99:        const result = await createUnresolvedAgentReferenceDiagnostics(agentSource, resolver);
apps/agents-server/src/utils\agentReferenceResolver\createUnresolvedAgentReferenceDiagnostics.test.ts:113:        const result = await createUnresolvedAgentReferenceDiagnostics(agentSource, resolver);
apps/agents-server/src/utils\agentReferenceResolver\createUnresolvedAgentReferenceDiagnostics.test.ts:128:        const result = await createUnresolvedAgentReferenceDiagnostics(agentSource, resolver);
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.ts:8:    resolvePseudoAgentKindFromReference,
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.ts:17:import { resolveAgentRouteIdentifier } from '../agentIdentifier';
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.ts:54: * Creates a resolver backed by the Agents Server collection and configured federated servers.
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.ts:64:    const resolver = new ServerAgentReferenceResolver(options);
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.ts:99:            const routeIdentifier = resolveAgentRouteIdentifier(agent);
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.ts:146:    public async resolveCommitmentContent(commitmentType: BookCommitment, content: string): Promise<string> {
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.ts:168:            const pseudoAgentReplacement = this.resolvePseudoAgentReference(commitmentType, tokenValue);
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.ts:227:    private resolvePseudoAgentReference(
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.ts:242:        const pseudoAgentKind = resolvePseudoAgentKindFromReference(tokenValue);
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.test.ts:16:function createMockAgentCollection(agentRecords: Array<{ agentName: string; permanentId?: string }>): AgentCollection {
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.test.ts:24:        const resolver = (await createServerAgentReferenceResolver({
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.test.ts:25:            agentCollection: createMockAgentCollection([{ agentName: 'Alice Cooper', permanentId: 'abc123' }]),
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.test.ts:29:        const resolved = await resolver.resolveCommitmentContent('FROM', '{Alice Cooper}');
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.test.ts:36:        const resolver = (await createServerAgentReferenceResolver({
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.test.ts:37:            agentCollection: createMockAgentCollection([]),
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.test.ts:41:        const resolved = await resolver.resolveCommitmentContent('FROM', '{Unknown Agent}');
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.test.ts:55:        const resolver = (await createServerAgentReferenceResolver({
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.test.ts:56:            agentCollection: createMockAgentCollection([]),
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.test.ts:60:        const resolved = await resolver.resolveCommitmentContent('FROM', '{VoId}');
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.test.ts:67:        const resolver = (await createServerAgentReferenceResolver({
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.test.ts:68:            agentCollection: createMockAgentCollection([]),
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.test.ts:72:        const resolved = await resolver.resolveCommitmentContent('TEAM', '{UsEr}');
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.test.ts:79:        const resolver = (await createServerAgentReferenceResolver({
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.test.ts:80:            agentCollection: createMockAgentCollection([]),
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.test.ts:84:        const resolved = await resolver.resolveCommitmentContent('FROM', '{User}');
apps/agents-server/src/utils\agentReferenceResolver\createAgentNameCollisionDiagnostics.ts:5:import type { AgentReferenceDiagnostic } from './createUnresolvedAgentReferenceDiagnostics';
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:15:    resolveLocalAgentRouteReference,
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:17:import { createBookScopedAgentReferenceResolver, resolveBookScopedAgentContext } from './bookScopedAgentReferences';
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:19:import type { AgentReferenceDiagnostic } from './createUnresolvedAgentReferenceDiagnostics';
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:96:     * Whether this source is the server's root Adam agent.
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:98:    readonly isAdamAgent: boolean;
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:102: * Creates Book editor warnings for a circular local inheritance chain and for an Adam source that is not rooted in
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:123:    const adamCanonicalAgentUrl = await resolveAdamCanonicalAgentUrl(collection, normalizedLocalServerUrl);
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:131:    const adamDiagnostics = createAdamRootDiagnostics(rootNode);
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:144: * Resolves Adam's canonical current route from its well-known name.
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:148: * @returns Canonical Adam route or `null` when the core agent is missing.
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:152:async function resolveAdamCanonicalAgentUrl(
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:195:        isAdamAgent:
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:202: * Creates the Adam-specific warning when the root agent does not explicitly inherit from `Null`/`Void`.
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:205: * @returns One warning for invalid Adam ancestry, otherwise an empty list.
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:209:function createAdamRootDiagnostics(node: LocalInheritanceNode): Array<AgentReferenceDiagnostic> {
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:210:    if (node.isAdamAgent && !isExplicitNoParentCommitment(node.effectiveFromCommitment)) {
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:215:                'The core `Adam` agent must explicitly use `FROM @Null` (or the equivalent `FROM @Void`) so it remains the root of the inheritance chain.',
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:303:        if (node.isAdamAgent) {
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:320:        resolvedParentReference = await node.agentReferenceResolver.resolveCommitmentContent(
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:328:    if (!resolvedParentReference || isVoidPseudoAgentReference(resolvedParentReference)) {
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:357:    const localRouteReference = resolveLocalAgentRouteReference(
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:367:        const resolvedAgentContext = await resolveBookScopedAgentContext({
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:374:            resolvedAgentContext.unresolvedAgentSource,
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:375:            resolvedAgentContext.resolvedAgentName,
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:379:            agentSource: resolvedAgentContext.unresolvedAgentSource,
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:380:            canonicalAgentUrl: resolvedAgentContext.canonicalAgentUrl,
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:382:            agentReferenceResolver: resolvedAgentContext.scopedAgentReferenceResolver,
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:383:            effectiveFromCommitment: getEffectiveExplicitFromCommitment(resolvedAgentContext.unresolvedAgentSource),
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:384:            isAdamAgent:
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts:385:                resolvedAgentContext.canonicalAgentUrl === options.adamCanonicalAgentUrl ||
apps/agents-server/src/utils\agentReferenceResolver\createUnresolvedAgentReferenceDiagnostics.ts:62:     * A book without any `FROM` commitment implicitly inherits from `@Adam`, so a missing Adam has to be reported
apps/agents-server/src/utils\agentReferenceResolver\createUnresolvedAgentReferenceDiagnostics.ts:121:export async function createUnresolvedAgentReferenceDiagnostics(
apps/agents-server/src/utils\agentReferenceResolver\createUnresolvedAgentReferenceDiagnostics.ts:226:        const endLineNumber = resolveCommitmentEndLine(sourceLines, startLineNumber, nextCommitmentStartLine - 1);
apps/agents-server/src/utils\agentReferenceResolver\createUnresolvedAgentReferenceDiagnostics.ts:249:function resolveCommitmentEndLine(
apps/agents-server/src/utils\agentReferenceResolver\createUnresolvedAgentReferenceDiagnostics.ts:312:    resolver: AgentReferenceResolver,
apps/agents-server/src/utils\agentReferenceResolver\createUnresolvedAgentReferenceDiagnostics.ts:317:        resolvedContent = await resolver.resolveCommitmentContent(location.commitmentType, location.token);
apps/agents-server/src/utils\agentReferenceResolver\createUnresolvedAgentReferenceDiagnostics.ts:359:function createUnresolvedReferenceMessage(location: AgentReferenceTokenLocation): string {
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:5:jest.mock('../defaultAgents/resolveDefaultAgentsStatus', () => ({
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:6:    resolveDefaultAgentsStatus: jest.fn(),
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:10:    resolveDefaultAgentsStatus,
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:13:} from '../defaultAgents/resolveDefaultAgentsStatus';
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:14:import type { MissingAgentReference } from './createUnresolvedAgentReferenceDiagnostics';
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:15:import { resolveCoreAgentAwareMissingReferences } from './resolveCoreAgentAwareMissingReferences';
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:20:const mockResolveDefaultAgentsStatus = jest.mocked(resolveDefaultAgentsStatus);
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:57: * Book that relies on the implicit `FROM @Adam` inheritance.
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:66:describe('resolveCoreAgentAwareMissingReferences', () => {
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:70:            createCoreAgentReport('adam', 'Adam', false),
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:75:    it('surfaces the implicitly inherited Adam ancestor when the core agent is missing', async () => {
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:76:        const missingAgentReferences = await resolveCoreAgentAwareMissingReferences({
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:84:            reference: 'Adam',
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:85:            token: '{Adam}',
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:90:            missingCoreAgentTitles: ['Adam', 'Teacher'],
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:96:        const missingAgentReferences = await resolveCoreAgentAwareMissingReferences({
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:105:    it('reports nothing when Adam is present on the server', async () => {
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:106:        mockCoreAgents([createCoreAgentReport('adam', 'Adam', true), createCoreAgentReport('teacher', 'Teacher', true)]);
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:108:        const missingAgentReferences = await resolveCoreAgentAwareMissingReferences({
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:126:        const missingAgentReferences = await resolveCoreAgentAwareMissingReferences({
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:140:            FROM {Adam}
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:144:        const explicitMissingAdamReference: MissingAgentReference = {
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:145:            reference: 'Adam',
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:146:            token: '{Adam}',
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:150:        const missingAgentReferences = await resolveCoreAgentAwareMissingReferences({
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:152:            missingAgentReferences: [explicitMissingAdamReference],
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:158:            reference: 'Adam',
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:159:            token: '{Adam}',
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:166:    it('keeps an ordinary missing reference creatable and still reports the implicit Adam ancestor', async () => {
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:180:        const missingAgentReferences = await resolveCoreAgentAwareMissingReferences({
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:187:        expect(missingAgentReferences[0]).toMatchObject({ reference: 'Adam', isImplicit: true });
apps/agents-server/src/utils\cachedServerAgentRuntime.ts:1:import { createAgentModelRequirements, computeAgentHash } from '@promptbook-local/core';
apps/agents-server/src/utils\cachedServerAgentRuntime.ts:9:    resolveServerAgentContext,
apps/agents-server/src/utils\cachedServerAgentRuntime.ts:12:} from './resolveServerAgentContext';
apps/agents-server/src/utils\cachedServerAgentRuntime.ts:41:    readonly unresolvedAgentReferences: ReadonlyArray<AgentReferenceResolutionIssue>;
apps/agents-server/src/utils\cachedServerAgentRuntime.ts:64:    readonly resolvedAgentContext: CachedServerAgentContext;
apps/agents-server/src/utils\cachedServerAgentRuntime.ts:113:export async function resolveCachedServerAgentContext(
apps/agents-server/src/utils\cachedServerAgentRuntime.ts:130:            const resolvedAgentContext = await resolveServerAgentContext(options);
apps/agents-server/src/utils\cachedServerAgentRuntime.ts:131:            const { scopedAgentReferenceResolver, ...cachedContext } = resolvedAgentContext;
apps/agents-server/src/utils\cachedServerAgentRuntime.ts:157:export async function resolveCachedServerAgentModelRequirements(
apps/agents-server/src/utils\cachedServerAgentRuntime.ts:175:                parentAgentSource: options.resolvedAgentContext.parentAgentSource,
apps/agents-server/src/utils\cachedServerAgentRuntime.ts:176:                parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
apps/agents-server/src/utils\cachedServerAgentRuntime.ts:180:            const modelRequirements = await createAgentModelRequirements(
apps/agents-server/src/utils\cachedServerAgentRuntime.ts:181:                options.resolvedAgentContext.resolvedAgentSource,
apps/agents-server/src/utils\cachedServerAgentRuntime.ts:190:            const unresolvedAgentReferences = consumeAgentReferenceResolutionIssues(scopedAgentReferenceResolver);
apps/agents-server/src/utils\cachedServerAgentRuntime.ts:193:                unresolvedAgentReferences,
apps/agents-server/src/utils\cachedServerAgentRuntime.ts:231:        canonicalAgentIdentifier: options.resolvedAgentContext.canonicalAgentIdentifier,
apps/agents-server/src/utils\cachedServerAgentRuntime.ts:232:        parentAgentPermanentId: options.resolvedAgentContext.parentAgentPermanentId,
apps/agents-server/src/utils\cachedServerAgentRuntime.ts:233:        resolvedAgentSourceHash: computeAgentHash(options.resolvedAgentContext.resolvedAgentSource),
apps/agents-server/src/utils\agentReferenceResolver\resolveTeamCapabilitiesFromAgentSource.ts:7:    resolvePseudoAgentKindFromUrl,
apps/agents-server/src/utils\agentReferenceResolver\resolveTeamCapabilitiesFromAgentSource.ts:38:    if (resolvePseudoAgentKindFromUrl(teammate.url) !== 'USER') {
apps/agents-server/src/utils\agentReferenceResolver\resolveTeamCapabilitiesFromAgentSource.ts:66: * @param agentReferenceResolver - Optional resolver for compact references like `{Agent}` and `@Agent`.
apps/agents-server/src/utils\agentReferenceResolver\resolveTeamCapabilitiesFromAgentSource.ts:69:export async function resolveTeamCapabilitiesFromAgentSource(
apps/agents-server/src/utils\agentReferenceResolver\resolveTeamCapabilitiesFromAgentSource.ts:74:    const resolvedCapabilities: Array<AgentCapability> = [];
apps/agents-server/src/utils\agentReferenceResolver\resolveTeamCapabilitiesFromAgentSource.ts:86:                commitmentContent = await agentReferenceResolver.resolveCommitmentContent('TEAM', commitmentContent);
apps/agents-server/src/utils\agentReferenceResolver\resolveTeamCapabilitiesFromAgentSource.test.ts:9:import { resolveTeamCapabilitiesFromAgentSource } from './resolveTeamCapabilitiesFromAgentSource';
apps/agents-server/src/utils\agentReferenceResolver\resolveTeamCapabilitiesFromAgentSource.test.ts:17:function createMockAgentCollection(agentRecords: Array<{ agentName: string; permanentId?: string }>): AgentCollection {
apps/agents-server/src/utils\agentReferenceResolver\resolveTeamCapabilitiesFromAgentSource.test.ts:23:describe('resolveTeamCapabilitiesFromAgentSource', () => {
apps/agents-server/src/utils\agentReferenceResolver\resolveTeamCapabilitiesFromAgentSource.test.ts:25:        const resolver = await createServerAgentReferenceResolver({
apps/agents-server/src/utils\agentReferenceResolver\resolveTeamCapabilitiesFromAgentSource.test.ts:26:            agentCollection: createMockAgentCollection([{ agentName: 'Known Teammate', permanentId: 'known123' }]),
apps/agents-server/src/utils\agentReferenceResolver\resolveTeamCapabilitiesFromAgentSource.test.ts:30:        const capabilities = await resolveTeamCapabilitiesFromAgentSource(
apps/agents-server/src/utils\agentReferenceResolver\resolveTeamCapabilitiesFromAgentSource.test.ts:57:        const resolver = await createServerAgentReferenceResolver({
apps/agents-server/src/utils\agentReferenceResolver\resolveTeamCapabilitiesFromAgentSource.test.ts:58:            agentCollection: createMockAgentCollection([]),
apps/agents-server/src/utils\agentReferenceResolver\resolveTeamCapabilitiesFromAgentSource.test.ts:62:        const capabilities = await resolveTeamCapabilitiesFromAgentSource(
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:4:import { resolveDefaultAgentsStatus, type CoreAgentReport } from '../defaultAgents/resolveDefaultAgentsStatus';
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:5:import { isImplicitAdamInheritance } from '../explicitFromCommitment';
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:6:import type { MissingAgentReference } from './createUnresolvedAgentReferenceDiagnostics';
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:38: * 2. A book without any explicit `FROM` implicitly inherits from `@Adam`, so a missing Adam is reported right away
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:42: * @returns Missing references carrying core-agent recovery, with the implicit `@Adam` ancestor first when missing.
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:46:export async function resolveCoreAgentAwareMissingReferences(
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:50:    const isAdamImplicitlyInherited = isImplicitAdamInheritance(agentSource);
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:52:    if (!isAdamImplicitlyInherited && missingAgentReferences.length === 0) {
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:56:    const missingCoreAgents = await resolveMissingCoreAgents();
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:72:    if (!isAdamImplicitlyInherited) {
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:76:    const missingAdamAgent = findMissingCoreAgent(missingCoreAgents, CORE_AGENTS_SERVER_WELL_KNOWN_AGENT_NAMES.ADAM);
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:78:    if (!missingAdamAgent || hasMissingAgentReferenceFor(recoverableMissingAgentReferences, missingAdamAgent)) {
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:83:        createImplicitAdamMissingReference(missingAdamAgent, coreAgentRecovery),
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:92: * disagree with it. Implicit inheritance always loads Adam from this very server, so a federated namesake elsewhere
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:97:async function resolveMissingCoreAgents(): Promise<ReadonlyArray<CoreAgentReport>> {
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:99:        const { coreAgents } = await resolveDefaultAgentsStatus();
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:111: * @param reference - Raw reference payload, for example `Adam` or `adam`.
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:139: * Creates the missing-reference entry standing for the implicit `FROM @Adam` ancestor.
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:141: * @param missingAdamAgent - Report of the missing Adam core agent.
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:145:function createImplicitAdamMissingReference(
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:146:    missingAdamAgent: CoreAgentReport,
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:150:        reference: missingAdamAgent.title,
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:151:        token: `{${missingAdamAgent.title}}`,
apps/agents-server/src/utils\cache\AssistantCacheManager.ts:3:import { createAgentModelRequirements } from '@promptbook-local/core';
apps/agents-server/src/utils\cache\AssistantCacheManager.ts:237:        const modelRequirements: AgentModelRequirements = await createAgentModelRequirements(
apps/agents-server/src/utils\cache\AssistantCacheManager.ts:247:        const unresolvedAgentReferences = consumeAgentReferenceResolutionIssues(effectiveAgentReferenceResolver);
apps/agents-server/src/utils\cache\AssistantCacheManager.ts:248:        if (unresolvedAgentReferences.length > 0) {
apps/agents-server/src/utils\cache\AssistantCacheManager.ts:249:            console.warn('[AssistantCacheManager] Unresolved agent references detected:', unresolvedAgentReferences);
apps/agents-server/src/utils\cache\AgentKitCacheManager.ts:17:import { resolveAgentKitModelRequirements } from './AgentKitCacheManager/resolveAgentKitModelRequirements';
apps/agents-server/src/utils\cache\AgentKitCacheManager.ts:175:        const resolvedModelRequirements = await resolveAgentKitModelRequirements({
apps/agents-server/src/utils\cache\AgentKitCacheManager\resolveAgentKitModelRequirements.ts:4:import { createAgentModelRequirements } from '@promptbook-local/core';
apps/agents-server/src/utils\cache\AgentKitCacheManager\resolveAgentKitModelRequirements.ts:13:export async function resolveAgentKitModelRequirements(options: {
apps/agents-server/src/utils\cache\AgentKitCacheManager\resolveAgentKitModelRequirements.ts:23:    const resolvedModelRequirements = await createAgentModelRequirements(
apps/agents-server/src/utils\cache\AgentKitCacheManager\resolveAgentKitModelRequirements.ts:33:    const unresolvedAgentReferences = consumeAgentReferenceResolutionIssues(effectiveAgentReferenceResolver);
apps/agents-server/src/utils\cache\AgentKitCacheManager\resolveAgentKitModelRequirements.ts:35:    if (unresolvedAgentReferences.length > 0) {
apps/agents-server/src/utils\cache\AgentKitCacheManager\resolveAgentKitModelRequirements.ts:36:        console.warn('[AgentKitCacheManager] Unresolved agent references detected:', unresolvedAgentReferences);
apps/agents-server/src/utils\cache\AgentKitCacheManager\AgentKitPreparedCache.ts:60:     * Builds one cache key for the fully resolved AgentKit preparation payload.
apps/agents-server/src/utils\chatMessageChips\createTouchedProjectChipResult.ts:4:import { resolveAgentProjectInfo } from '../agentProjects/resolveAgentProjectInfo';
apps/agents-server/src/utils\chatMessageChips\createTouchedProjectChipResult.ts:5:import { resolveAgentProjectRuntimeStatus } from '../agentProjects/resolveAgentProjectRuntimeStatus';
apps/agents-server/src/utils\chatMessageChips\createTouchedProjectChipResult.ts:24:    const project = await resolveAgentProjectInfo(options.agentPermanentId, options.projectName);
apps/agents-server/src/utils\chatMessageChips\createTouchedProjectChipResult.ts:30:    const runtimeStatus = await resolveAgentProjectRuntimeStatus(options.agentPermanentId, project.projectName);
apps/agents-server/src/utils\chat\createChatHistoryRecorder.ts:4:import { resolveCanonicalAgentName } from '@/src/utils/resolveCanonicalAgentName';
apps/agents-server/src/utils\chat\createChatHistoryRecorder.ts:137:    const platform = resolvePlatform(userAgent);
apps/agents-server/src/utils\chat\createChatHistoryRecorder.ts:139:    const canonicalAgentName = isEnabled ? await resolveCanonicalAgentName(agentIdentifier) : null;
apps/agents-server/src/utils\chat\createChatHistoryRecorder.ts:241:function resolvePlatform(userAgent: string | null): string | null {
apps/agents-server/src/utils\chat\createAttachmentAwareCitationLabelResolver.ts:4:import { resolveAgentsServerCitationLabel } from './resolveAgentsServerCitationLabel';
apps/agents-server/src/utils\chat\createAttachmentAwareCitationLabelResolver.ts:21: * `resolveAgentsServerCitationLabel`.
apps/agents-server/src/utils\chat\createAttachmentAwareCitationLabelResolver.ts:39:        return resolveAgentsServerCitationLabel(citation);
apps/agents-server/src/utils\chat\messageSuffix.ts:18:export function resolveMessageSuffixFromAgentSource(agentSource: string_book): string | null {
apps/agents-server/src/utils\chat\messageSuffix.test.ts:8:    resolveMessageSuffixFromAgentSource,
apps/agents-server/src/utils\chat\messageSuffix.test.ts:13:        const messageSuffix = resolveMessageSuffixFromAgentSource(
apps/agents-server/src/utils\chat\resolveAgentsServerCitationLabel.ts:31:export const resolveAgentsServerCitationLabel: AgentsServerCitationLabelResolver = async (citation) => {
apps/agents-server/src/utils\createAgentWithDefaultVisibility.test.ts:44:        const collection = { createAgent } as unknown as AgentCollection;
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDomains.ts:9:    resolveAgentProjectDomainRegistryFilePath,
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDomains.ts:10:    resolveAgentProjectDomainsFilePath,
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDomains.ts:155:        const serverDomain = resolveAgentProjectRuntimeBaseDomain(options.serverDomain);
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDomains.ts:231:        const serverDomain = resolveAgentProjectRuntimeBaseDomain(options.serverDomain);
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDomains.ts:310:    const registryFilePath = resolveAgentProjectDomainRegistryFilePath();
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDomains.ts:352:export async function resolveAgentProjectDomainRecordByHost(
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDomains.ts:371:export async function resolveAgentProjectDomainRecord(options: {
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDomains.ts:376:    const serverDomain = resolveAgentProjectRuntimeBaseDomain(options.serverDomain);
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDomains.ts:423:export function resolveAgentProjectRuntimeBaseDomain(requestedServerDomain?: string | null): string | null {
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDomains.ts:450:    const registryFilePath = resolveAgentProjectDomainRegistryFilePath();
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDomains.ts:451:    const domainsFilePath = resolveAgentProjectDomainsFilePath();
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDomains.test.ts:7:    resolveAgentProjectDomainRecordByHost,
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDomains.test.ts:8:    resolveAgentProjectRuntimeBaseDomain,
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDomains.test.ts:73:        await expect(resolveAgentProjectDomainRecordByHost('my-project.agents.example.com')).resolves.toMatchObject({
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDomains.test.ts:196:        expect(resolveAgentProjectRuntimeBaseDomain()).toBeNull();
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDesiredState.ts:6:import { resolveAgentProjectRuntimeDesiredStateFilePath } from './agentProjectRuntimePaths';
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDesiredState.ts:70:    const desiredStateFilePath = resolveAgentProjectRuntimeDesiredStateFilePath();
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDesiredState.ts:114:export function resolveAgentProjectRuntimeDesiredStateFromRecords(
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDesiredState.ts:129:export async function resolveAgentProjectRuntimeDesiredState(
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDesiredState.ts:132:    return resolveAgentProjectRuntimeDesiredStateFromRecords(await listAgentProjectRuntimeDesiredStates(), identity);
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDesiredState.ts:154:        if (resolveAgentProjectRuntimeDesiredStateFromRecords(desiredStateRecords, options) === options.desiredState) {
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDesiredState.ts:180:    const desiredStateFilePath = resolveAgentProjectRuntimeDesiredStateFilePath();
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDesiredState.test.ts:6:    resolveAgentProjectRuntimeDesiredState,
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDesiredState.test.ts:37:            resolveAgentProjectRuntimeDesiredState({ agentPermanentId: 'abc123', projectName: 'website' }),
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDesiredState.test.ts:60:            resolveAgentProjectRuntimeDesiredState({ agentPermanentId: 'abc123', projectName: 'website' }),
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDesiredState.test.ts:70:            resolveAgentProjectRuntimeDesiredState({ agentPermanentId: 'abc123', projectName: 'website' }),
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDesiredState.test.ts:89:            resolveAgentProjectRuntimeDesiredState({ agentPermanentId: 'abc123', projectName: 'website' }),
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeDesiredState.test.ts:101:            resolveAgentProjectRuntimeDesiredState({ agentPermanentId: 'abc123', projectName: 'landing' }),
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeAutostartScheduler.ts:1:import { resolveAgentProjectRuntimeEnvironmentFlag } from './resolveAgentProjectRuntimeEnvironmentFlag';
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeAutostartScheduler.ts:161:    return resolveAgentProjectRuntimeEnvironmentFlag(PTBK_AGENT_PROJECT_RUNTIME_AUTOSTART_ENABLED_ENV) ?? true;
apps/agents-server/src/utils\agentProjects\agentProjectCodeServerRegistry.ts:21:import { resolveAgentProjectInfo } from './resolveAgentProjectInfo';
apps/agents-server/src/utils\agentProjects\agentProjectCodeServerRegistry.ts:223:export async function resolveAgentProjectCodeServerThemeMode(
apps/agents-server/src/utils\agentProjects\agentProjectCodeServerRegistry.ts:259:    const project = await resolveExistingAgentProject(options.agentPermanentId, options.projectName);
apps/agents-server/src/utils\agentProjects\agentProjectCodeServerRegistry.ts:301:    const sessionDirectory = resolveAgentProjectCodeServerSessionDirectory(sessionId);
apps/agents-server/src/utils\agentProjects\agentProjectCodeServerRegistry.ts:303:    const extensionsDirectory = join(resolveAgentProjectCodeServerStateRoot(), 'extensions', sanitizePathPart(userKey));
apps/agents-server/src/utils\agentProjects\agentProjectCodeServerRegistry.ts:400:export async function resolveAgentProjectCodeServerSession(
apps/agents-server/src/utils\agentProjects\agentProjectCodeServerRegistry.ts:464:async function resolveExistingAgentProject(agentPermanentId: string, projectName: string) {
apps/agents-server/src/utils\agentProjects\agentProjectCodeServerRegistry.ts:465:    const project = await resolveAgentProjectInfo(agentPermanentId, projectName);
apps/agents-server/src/utils\agentProjects\agentProjectCodeServerRegistry.ts:702:function resolveAgentProjectCodeServerStateRoot(): string {
apps/agents-server/src/utils\agentProjects\agentProjectCodeServerRegistry.ts:721:function resolveAgentProjectCodeServerSessionDirectory(sessionId: string): string {
apps/agents-server/src/utils\agentProjects\agentProjectCodeServerRegistry.ts:722:    return join(resolveAgentProjectCodeServerStateRoot(), 'sessions', sessionId);
apps/agents-server/src/utils\agentProjects\agentProjectAccess.ts:2:    resolveAgentAccess,
apps/agents-server/src/utils\agentProjects\agentProjectAccess.ts:59:export async function resolveAgentProjectsAccess(
apps/agents-server/src/utils\agentProjects\agentProjectAccess.ts:63:    const agentAccess = await resolveAgentAccess(agentIdentifier, options);
apps/agents-server/src/utils\agentPreparation.ts:2:export { resolveAgentCollectionTablePrefix } from './agentPreparation/agentPreparationShared';
apps/agents-server/src/utils\agentProjects\agentProjectsPaths.ts:5:import { createLocalAgentDirectoryName, resolveLocalAgentRootPath } from '../localChatRunner/ensureLocalAgentFolder';
apps/agents-server/src/utils\agentProjects\agentProjectsPaths.ts:18:export function resolveAgentFolderPath(agentPermanentId: string): string {
apps/agents-server/src/utils\agentProjects\agentProjectsPaths.ts:19:    return join(resolveLocalAgentRootPath(), createLocalAgentDirectoryName(agentPermanentId));
apps/agents-server/src/utils\agentProjects\agentProjectsPaths.ts:28:export function resolveAgentProjectsRootPath(agentPermanentId: string): string {
apps/agents-server/src/utils\agentProjects\agentProjectsPaths.ts:29:    return join(resolveAgentFolderPath(agentPermanentId), AGENT_PROJECTS_DIRECTORY_PATH);
apps/agents-server/src/utils\agentProjects\agentProjectsPaths.ts:39:export function resolveSafeAgentProjectPath(options: {
apps/agents-server/src/utils\agentProjects\agentProjectsPaths.ts:45:    return join(resolveAgentProjectsRootPath(options.agentPermanentId), options.projectName);
apps/agents-server/src/utils\agentProjects\agentProjectsPaths.ts:106:export function resolveSafeAgentProjectFilePath(options: {
apps/agents-server/src/utils\agentProjects\agentProjectsPaths.ts:117:    return join(resolveSafeAgentProjectPath({ agentPermanentId, projectName }), ...filePathSegments);
apps/agents-server/src/utils\agentProjects\agentProjectsPaths.ts:127:export function resolveSafeAgentProjectDirectoryPath(options: {
apps/agents-server/src/utils\agentProjects\agentProjectsPaths.ts:138:    return join(resolveSafeAgentProjectPath({ agentPermanentId, projectName }), ...directoryPathSegments);
apps/agents-server/src/utils\agentProjects\agentProjectsPaths.test.ts:6:    resolveAgentProjectsRootPath,
apps/agents-server/src/utils\agentProjects\agentProjectsPaths.test.ts:7:    resolveSafeAgentProjectDirectoryPath,
apps/agents-server/src/utils\agentProjects\agentProjectsPaths.test.ts:8:    resolveSafeAgentProjectFilePath,
apps/agents-server/src/utils\agentProjects\agentProjectsPaths.test.ts:9:    resolveSafeAgentProjectPath,
apps/agents-server/src/utils\agentProjects\agentProjectsPaths.test.ts:42:describe('resolveSafeAgentProjectFilePath', () => {
apps/agents-server/src/utils\agentProjects\agentProjectsPaths.test.ts:44:        const projectPath = resolveSafeAgentProjectPath({
apps/agents-server/src/utils\agentProjects\agentProjectsPaths.test.ts:49:        expect(projectPath).toBe(join(resolveAgentProjectsRootPath('abc123'), 'my-website'));
apps/agents-server/src/utils\agentProjects\agentProjectsPaths.test.ts:53:        const filePath = resolveSafeAgentProjectFilePath({
apps/agents-server/src/utils\agentProjects\agentProjectsPaths.test.ts:59:        expect(filePath).toBe(join(resolveAgentProjectsRootPath('abc123'), 'my-website', 'assets', 'style.css'));
apps/agents-server/src/utils\agentProjects\agentProjectsPaths.test.ts:63:        const directoryPath = resolveSafeAgentProjectDirectoryPath({
apps/agents-server/src/utils\agentProjects\agentProjectsPaths.test.ts:69:        expect(directoryPath).toBe(join(resolveAgentProjectsRootPath('abc123'), 'my-website', 'assets', 'icons'));
apps/agents-server/src/utils\agentProjects\agentProjectsPaths.test.ts:74:            resolveSafeAgentProjectFilePath({
apps/agents-server/src/utils\agentProjects\agentProjectsPaths.test.ts:82:            resolveSafeAgentProjectFilePath({
apps/agents-server/src/utils\agentProjects\agentProjectsPaths.test.ts:90:            resolveSafeAgentProjectDirectoryPath({
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.ts:28:    resolveAgentProjectDomainRecord,
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.ts:29:    resolveAgentProjectDomainRecordByHost,
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.ts:35:    resolveAgentProjectRuntimePm2Status,
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.ts:39:import { resolveAgentProjectRuntimeRegistryFilePath } from './agentProjectRuntimePaths';
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.ts:45:import { resolveAgentProjectInfo } from './resolveAgentProjectInfo';
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.ts:142:    readonly project: NonNullable<Awaited<ReturnType<typeof resolveAgentProjectInfo>>>;
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.ts:172:    const project = await resolveExistingAgentProject(options);
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.ts:223:    const project = await resolveExistingAgentProject(options);
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.ts:304:export async function resolveAgentProjectRuntime(
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.ts:338:    const domainRecord = await resolveAgentProjectDomainRecord({
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.ts:361:export async function resolveAgentProjectRuntimeByDomain(
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.ts:364:    const domainRecord = await resolveAgentProjectDomainRecordByHost(host);
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.ts:370:    return await resolveAgentProjectRuntime(domainRecord.agentPermanentId, domainRecord.projectName);
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.ts:494:    project: NonNullable<Awaited<ReturnType<typeof resolveAgentProjectInfo>>>,
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.ts:683:async function resolveExistingAgentProject(options: AgentProjectRuntimeProjectOptions) {
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.ts:684:    const project = await resolveAgentProjectInfo(options.agentPermanentId, options.projectName);
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.ts:725:    const pm2Status = await resolveAgentProjectRuntimePm2Status(runtimeRecord.pm2ProcessName);
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.ts:887:    const registryFilePath = resolveAgentProjectRuntimeRegistryFilePath();
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.ts:912:    const registryFilePath = resolveAgentProjectRuntimeRegistryFilePath();
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.test.ts:4:import { resolveLocalAgentRootPath } from '../localChatRunner/ensureLocalAgentFolder';
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.test.ts:14:import { resolveAgentProjectRuntimeDesiredState } from './agentProjectRuntimeDesiredState';
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.test.ts:24:    resolveLocalAgentRootPath: jest.fn(),
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.test.ts:31:const resolveLocalAgentRootPathMock = resolveLocalAgentRootPath as jest.MockedFunction<
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.test.ts:32:    typeof resolveLocalAgentRootPath
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.test.ts:77:        resolveLocalAgentRootPathMock.mockReturnValue(temporaryDirectory);
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.test.ts:204:            resolveAgentProjectRuntimeDesiredState({ agentPermanentId: 'abc123', projectName: 'website' }),
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.test.ts:210:            resolveAgentProjectRuntimeDesiredState({ agentPermanentId: 'abc123', projectName: 'website' }),
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.test.ts:216:            resolveAgentProjectRuntimeDesiredState({ agentPermanentId: 'abc123', projectName: 'website' }),
apps/agents-server/src/utils\agentProjects\agentProjectRuntimeRegistry.test.ts:229:            resolveAgentProjectRuntimeDesiredState({ agentPermanentId: 'abc123', projectName: 'website' }),
apps/agents-server/src/utils\agentProjects\agentProjectRuntimePm2.ts:8:import { resolveAgentProjectRuntimeEnvironmentFlag } from './resolveAgentProjectRuntimeEnvironmentFlag';
apps/agents-server/src/utils\agentProjects\agentProjectRuntimePm2.ts:153:    const configuredFlag = resolveAgentProjectRuntimeEnvironmentFlag(PTBK_AGENT_PROJECT_RUNTIME_PM2_ENABLED_ENV);
apps/agents-server/src/utils\agentProjects\agentProjectRuntimePm2.ts:196:    const status = await resolveAgentProjectRuntimePm2Status(processName);
apps/agents-server/src/utils\agentProjects\agentProjectRuntimePm2.ts:253:export async function resolveAgentProjectRuntimePm2Status(
apps/agents-server/src/utils\agentProjects\agentProjectRuntimePaths.ts:53:export function resolveAgentProjectRuntimeStateRoot(): string {
apps/agents-server/src/utils\agentProjects\agentProjectRuntimePaths.ts:74:export function resolveAgentProjectRuntimeRegistryFilePath(): string {
apps/agents-server/src/utils\agentProjects\agentProjectRuntimePaths.ts:81:    return join(resolveAgentProjectRuntimeStateRoot(), AGENT_PROJECT_RUNTIME_REGISTRY_FILE_NAME);
apps/agents-server/src/utils\agentProjects\agentProjectRuntimePaths.ts:89:export function resolveAgentProjectDomainRegistryFilePath(): string {
apps/agents-server/src/utils\agentProjects\agentProjectRuntimePaths.ts:96:    return join(resolveAgentProjectRuntimeStateRoot(), AGENT_PROJECT_DOMAIN_REGISTRY_FILE_NAME);
apps/agents-server/src/utils\agentProjects\agentProjectRuntimePaths.ts:104:export function resolveAgentProjectRuntimeDesiredStateFilePath(): string {
apps/agents-server/src/utils\agentProjects\agentProjectRuntimePaths.ts:111:    return join(resolveAgentProjectRuntimeStateRoot(), AGENT_PROJECT_RUNTIME_DESIRED_STATE_FILE_NAME);
apps/agents-server/src/utils\agentProjects\agentProjectRuntimePaths.ts:119:export function resolveAgentProjectDomainsFilePath(): string {
apps/agents-server/src/utils\agentProjects\agentProjectRuntimePaths.ts:126:    return join(resolveAgentProjectRuntimeStateRoot(), AGENT_PROJECT_DOMAINS_FILE_NAME);
apps/agents-server/src/utils\agentProjects\createStaticAgentProjectServer.ts:9:import { resolveAgentProjectFileContentType } from './resolveAgentProjectFileContentType';
apps/agents-server/src/utils\agentProjects\createStaticAgentProjectServer.ts:154:        'Content-Type': resolveAgentProjectFileContentType(basename(filePath)),
apps/agents-server/src/utils\agentProjects\hasAgentProjects.ts:2:import { resolveAgentProjectsRootPath } from './agentProjectsPaths';
apps/agents-server/src/utils\agentProjects\hasAgentProjects.ts:12:    const projectsRootPath = resolveAgentProjectsRootPath(agentPermanentId);
apps/agents-server/src/utils\agentPreparation\agentPreparationRepository.ts:61:            const candidate = resolveSingleRow<AgentPreparationRow>(dueResult.data);
apps/agents-server/src/utils\agentPreparation\agentPreparationRepository.ts:90:            return resolveSingleRow<AgentPreparationRow>(claimResult.data);
apps/agents-server/src/utils\agentPreparation\agentPreparationRepository.ts:111:            return resolveSingleRow<AgentPreparationAgentSnapshot>(result.data);
apps/agents-server/src/utils\agentPreparation\agentPreparationRepository.ts:132:            return resolveSingleRow<AgentPreparationRow>(result.data);
apps/agents-server/src/utils\agentPreparation\agentPreparationRepository.ts:147:            return resolveSingleRow<AgentPreparationRow>(result.data);
apps/agents-server/src/utils\agentPreparation\agentPreparationRepository.ts:171:            return resolveSingleRow<AgentPreparationRow>(result.data);
apps/agents-server/src/utils\agentPreparation\agentPreparationShared.ts:65:export function resolveAgentPreparationFailureBackoffMs(retryCount: number): number {
apps/agents-server/src/utils\agentPreparation\agentPreparationShared.ts:74:export function resolveAgentCollectionTablePrefix(agentCollection: unknown): string {
apps/agents-server/src/utils\createLocalAgentSourceImporter.ts:10:    resolveBookScopedAgentContext,
apps/agents-server/src/utils\createLocalAgentSourceImporter.ts:17:    resolveLocalAgentRouteReference,
apps/agents-server/src/utils\createLocalAgentSourceImporter.ts:21:    resolveInheritedAgentSource,
apps/agents-server/src/utils\createLocalAgentSourceImporter.ts:23:} from './resolveInheritedAgentSource';
apps/agents-server/src/utils\createLocalAgentSourceImporter.ts:45:     * Adam agent URL used for implicit default inheritance.
apps/agents-server/src/utils\createLocalAgentSourceImporter.ts:64: * @returns Importer suitable for `resolveInheritedAgentSource`.
apps/agents-server/src/utils\createLocalAgentSourceImporter.ts:70:        const localRouteReference = resolveLocalAgentRouteReference(agentUrl, localServerUrls, localAgentUrlReferences);
apps/agents-server/src/utils\createLocalAgentSourceImporter.ts:79:        let resolvedAgentContext: ResolvedBookScopedAgentContext;
apps/agents-server/src/utils\createLocalAgentSourceImporter.ts:82:            resolvedAgentContext = await resolveBookScopedAgentContext({
apps/agents-server/src/utils\createLocalAgentSourceImporter.ts:96:        return resolveInheritedAgentSource(resolvedAgentContext.unresolvedAgentSource, {
apps/agents-server/src/utils\createLocalAgentSourceImporter.ts:100:            currentAgentUrl: resolvedAgentContext.canonicalAgentUrl,
apps/agents-server/src/utils\createLocalAgentSourceImporter.ts:101:            currentAgentAliases: createCurrentAgentAliases(resolvedAgentContext, localServerUrls),
apps/agents-server/src/utils\createLocalAgentSourceImporter.ts:102:            agentReferenceResolver: resolvedAgentContext.scopedAgentReferenceResolver,
apps/agents-server/src/utils\createLocalAgentSourceImporter.ts:137: * @param resolvedAgentContext - Resolved local/book-scoped agent context.
apps/agents-server/src/utils\createLocalAgentSourceImporter.ts:142:    resolvedAgentContext: ResolvedBookScopedAgentContext,
apps/agents-server/src/utils\createLocalAgentSourceImporter.ts:146:        resolvedAgentContext.canonicalAgentIdentifier,
apps/agents-server/src/utils\createLocalAgentSourceImporter.ts:147:        resolvedAgentContext.requestedAgentIdentifier,
apps/agents-server/src/utils\createLocalAgentSourceImporter.ts:148:        resolvedAgentContext.resolvedAgentName,
apps/agents-server/src/utils\createLocalAgentSourceImporter.ts:150:    const agentAliases = new Set<string>(resolvedAgentContext.currentAgentAliases);
apps/agents-server/src/utils\agentProjects\listAgentProjectChatReferences.test.ts:5:import { resolveAgentProjectPublicUrls } from './resolveAgentProjectPublicUrls';
apps/agents-server/src/utils\agentProjects\listAgentProjectChatReferences.test.ts:15:jest.mock('./resolveAgentProjectPublicUrls', () => ({
apps/agents-server/src/utils\agentProjects\listAgentProjectChatReferences.test.ts:16:    resolveAgentProjectPublicUrls: jest.fn(),
apps/agents-server/src/utils\agentProjects\listAgentProjectChatReferences.test.ts:21:const resolveMockedAgentProjectPublicUrls = jest.mocked(resolveAgentProjectPublicUrls);
apps/agents-server/src/utils\agentProjects\listAgentProjectChatReferences.test.ts:25:        resolveMockedAgentProjectPublicUrls.mockResolvedValue(new Map());
apps/agents-server/src/utils\agentProjects\listAgentProjectChatReferences.test.ts:88:        resolveMockedAgentProjectPublicUrls.mockResolvedValue(
apps/agents-server/src/utils\agentProjects\listAgentProjectChatReferences.ts:4:import { resolveAgentProjectPublicUrls } from './resolveAgentProjectPublicUrls';
apps/agents-server/src/utils\agentProjects\listAgentProjectChatReferences.ts:18:        resolveAgentProjectPublicUrls(agentPermanentId),
apps/agents-server/src/utils\agentPreparation\agentPreparationWorker.ts:6:    resolveCachedServerAgentContext,
apps/agents-server/src/utils\agentPreparation\agentPreparationWorker.ts:7:    resolveCachedServerAgentModelRequirements,
apps/agents-server/src/utils\agentPreparation\agentPreparationWorker.ts:27:    resolveAgentPreparationFailureBackoffMs,
apps/agents-server/src/utils\agentPreparation\agentPreparationWorker.ts:248:        const resolvedAgentContext = await resolveCachedServerAgentContext({
apps/agents-server/src/utils\agentPreparation\agentPreparationWorker.ts:254:        const preparedAgentModelRequirements = await resolveCachedServerAgentModelRequirements({
apps/agents-server/src/utils\agentPreparation\agentPreparationWorker.ts:255:            resolvedAgentContext,
apps/agents-server/src/utils\agentPreparation\agentPreparationWorker.ts:266:                    resolvedAgentContext.resolvedAgentSource,
apps/agents-server/src/utils\agentPreparation\agentPreparationWorker.ts:267:                    resolvedAgentContext.resolvedAgentName,
apps/agents-server/src/utils\agentPreparation\agentPreparationWorker.ts:359:    const runAfterIso = new Date(now.getTime() + resolveAgentPreparationFailureBackoffMs(nextRetryCount)).toISOString();
apps/agents-server/src/utils\agentProjects\listAgentProjectDirectoryEntries.test.ts:4:import { resolveLocalAgentRootPath } from '../localChatRunner/ensureLocalAgentFolder';
apps/agents-server/src/utils\agentProjects\listAgentProjectDirectoryEntries.test.ts:8:    resolveLocalAgentRootPath: jest.fn(),
apps/agents-server/src/utils\agentProjects\listAgentProjectDirectoryEntries.test.ts:15:const resolveLocalAgentRootPathMock = resolveLocalAgentRootPath as jest.MockedFunction<
apps/agents-server/src/utils\agentProjects\listAgentProjectDirectoryEntries.test.ts:16:    typeof resolveLocalAgentRootPath
apps/agents-server/src/utils\agentProjects\listAgentProjectDirectoryEntries.test.ts:24:        resolveLocalAgentRootPathMock.mockReturnValue(temporaryDirectory);
apps/agents-server/src/utils\agentProjects\listAgentProjectDirectoryEntries.ts:6:import { resolveSafeAgentProjectDirectoryPath } from './agentProjectsPaths';
apps/agents-server/src/utils\agentProjects\listAgentProjectDirectoryEntries.ts:76:    const directoryPath = resolveSafeAgentProjectDirectoryPath(options);
apps/agents-server/src/utils\createServerTeammateProfileResolver.ts:4:import { resolveAgentRouteIdentifier } from './agentIdentifier';
apps/agents-server/src/utils\createServerTeammateProfileResolver.ts:10:    const routeIdentifier = resolveAgentRouteIdentifier(agent);
apps/agents-server/src/utils\agentProjects\listAgentProjectNames.ts:2:import { resolveAgentProjectsRootPath } from './agentProjectsPaths';
apps/agents-server/src/utils\agentProjects\listAgentProjectNames.ts:18:    const projectsRootPath = resolveAgentProjectsRootPath(agentPermanentId);
apps/agents-server/src/utils\agentProjects\listAgentProjects.ts:2:import { resolveAgentProjectsRootPath } from './agentProjectsPaths';
apps/agents-server/src/utils\agentProjects\listAgentProjects.ts:4:import { createAgentProjectInfo } from './resolveAgentProjectInfo';
apps/agents-server/src/utils\agentProjects\listAgentProjects.ts:16:    const projectsRootPath = resolveAgentProjectsRootPath(agentPermanentId);
apps/agents-server/src/utils\customDomainRouting.test.ts:5:import { createCustomDomainMatchCandidates, createCustomDomainOrFilter, resolveCustomDomainAgent } from './customDomainRouting';
apps/agents-server/src/utils\customDomainRouting.test.ts:47:    describe('resolveCustomDomainAgent', () => {
apps/agents-server/src/utils\customDomainRouting.test.ts:73:            const resolution = await resolveCustomDomainAgent('search.ptbk.io', supabase, [server]);
apps/agents-server/src/utils\customDomainRouting.test.ts:114:            const resolution = await resolveCustomDomainAgent('search.ptbk.io', supabase, [firstServer, secondServer]);
apps/agents-server/src/utils\customDomainRouting.test.ts:172:            const resolution = await resolveCustomDomainAgent('inherited.ptbk.io', supabase, [server]);
apps/agents-server/src/utils\customDomainRouting.test.ts:238:                    resolve: (value: { data: Array<ReturnType<typeof createAgentRow>> | null; error: null }) => unknown,
apps/agents-server/src/utils\customDomainRouting.ts:28:    resolveLocalAgentRouteReference,
apps/agents-server/src/utils\customDomainRouting.ts:31:import { resolveInheritedAgentSource, type AgentSourceImporter } from './resolveInheritedAgentSource';
apps/agents-server/src/utils\customDomainRouting.ts:187:function createResolverAgentCollection(agents: ReadonlyArray<CustomDomainAgentRow>): AgentCollection {
apps/agents-server/src/utils\customDomainRouting.ts:188:    const resolveAgent = (agentNameOrPermanentId: string): CustomDomainAgentRow => {
apps/agents-server/src/utils\customDomainRouting.ts:222:            const agent = resolveAgent(agentNameOrPermanentId);
apps/agents-server/src/utils\customDomainRouting.ts:226:            return resolveAgent(agentNameOrPermanentId).agentSource as string_book;
apps/agents-server/src/utils\customDomainRouting.ts:278:        const localRouteReference = resolveLocalAgentRouteReference(agentUrl, localServerUrls, localAgentUrlReferences);
apps/agents-server/src/utils\customDomainRouting.ts:288:        let unresolvedAgentSource: string_book;
apps/agents-server/src/utils\customDomainRouting.ts:292:            unresolvedAgentSource = await options.collection.getAgentSource(agentPermanentId);
apps/agents-server/src/utils\customDomainRouting.ts:301:        const resolvedAgentName = readAgentSourceTitle(unresolvedAgentSource) || localRouteReference.agentIdentifier;
apps/agents-server/src/utils\customDomainRouting.ts:304:        return resolveInheritedAgentSource(unresolvedAgentSource, {
apps/agents-server/src/utils\customDomainRouting.ts:312:                createLocalAgentUrl(localRouteReference.localServerUrl, resolvedAgentName),
apps/agents-server/src/utils\customDomainRouting.ts:367: * @param resolvedAgentSource - Agent source after inheritance/import resolution.
apps/agents-server/src/utils\customDomainRouting.ts:370:function parseResolvedCustomDomainMetadata(resolvedAgentSource: string): ResolvedCustomDomainMetadata {
apps/agents-server/src/utils\customDomainRouting.ts:371:    const lines = resolvedAgentSource.split(/\r?\n/);
apps/agents-server/src/utils\customDomainRouting.ts:430:async function resolveCustomDomainMetadataForAgent(
apps/agents-server/src/utils\customDomainRouting.ts:440:    const resolvedAgentSource = await resolveInheritedAgentSource(agent.agentSource as string_book, {
apps/agents-server/src/utils\customDomainRouting.ts:448:    return parseResolvedCustomDomainMetadata(resolvedAgentSource);
apps/agents-server/src/utils\customDomainRouting.ts:459:export async function resolveCustomDomainAgent(
apps/agents-server/src/utils\customDomainRouting.ts:486:            const { data: resolverReferenceAgents, error: resolverReferenceError } = await supabase
apps/agents-server/src/utils\customDomainRouting.ts:491:            if (resolverReferenceError || !Array.isArray(resolverReferenceAgents)) {
apps/agents-server/src/utils\customDomainRouting.ts:499:            const agentCollection = createResolverAgentCollection(
apps/agents-server/src/utils\customDomainRouting.ts:500:                resolverReferenceAgents as Array<CustomDomainAgentRow>,
apps/agents-server/src/utils\customDomainRouting.ts:518:                const resolvedMetadata = await resolveCustomDomainMetadataForAgent(agent, {
apps/agents-server/src/utils\defaultAgents\coreAgentExplanations.ts:14:        **Adam** is the root ancestor agent. Every agent that does not use an explicit \`FROM\` commitment inherits from
apps/agents-server/src/utils\defaultAgents\coreAgentExplanations.ts:15:        Adam, so its persona, rules, and base capabilities form the shared foundation of all agents on this server.
apps/agents-server/src/utils\defaultAgents\coreAgentExplanations.ts:16:        Removing Adam breaks the default inheritance chain for new and existing agents.
apps/agents-server/src/utils\defaultAgents\coreAgentExplanations.ts:29:        used by the self-learning features and inherits from Adam.
apps/agents-server/src/utils\defaultAgents\coreAgentExplanations.ts:51:export function resolveCoreAgentExplanation(agentName: string): string {
apps/agents-server/src/utils\agentProjects\listAllAgentProjectSummaries.ts:7:import { createLocalAgentDirectoryName, resolveLocalAgentRootPath } from '../localChatRunner/ensureLocalAgentFolder';
apps/agents-server/src/utils\agentProjects\listAllAgentProjectSummaries.ts:83:        rootPath: resolveLocalAgentRootPath(),
apps/agents-server/src/utils\agentProjects\listAllAgentProjectSummaries.ts:133:    const rootPath = resolveLocalAgentRootPath();
apps/agents-server/src/utils\defaultAgents\resolveDefaultAgentsStatus.ts:5:import { resolveCoreAgentExplanation } from './coreAgentExplanations';
apps/agents-server/src/utils\defaultAgents\resolveDefaultAgentsStatus.ts:142:export async function resolveDefaultAgentsStatus(
apps/agents-server/src/utils\defaultAgents\resolveDefaultAgentsStatus.ts:154:            explanation: resolveCoreAgentExplanation(identity.agentName),
apps/agents-server/src/utils\defaultAgents\resolveDefaultAgentsStatus.ts:190:export function resolveMissingCoreAgentNames(
apps/agents-server/src/utils\defaultAgents\resolveDefaultAgentsStatus.ts:215:        title: resolveBundledAgentTitle(agentBook) ?? agentProfile.agentName,
apps/agents-server/src/utils\defaultAgents\resolveDefaultAgentsStatus.ts:228:function resolveBundledAgentTitle(agentBook: string_book): string | null {
apps/agents-server/src/utils\defaultAgents\resolveDefaultAgentsStatus.test.ts:4:import { CORE_AGENT_EXPLANATION_BY_AGENT_NAME, GENERIC_CORE_AGENT_EXPLANATION, resolveCoreAgentExplanation } from './coreAgentExplanations';
apps/agents-server/src/utils\defaultAgents\resolveDefaultAgentsStatus.test.ts:7:    resolveMissingCoreAgentNames,
apps/agents-server/src/utils\defaultAgents\resolveDefaultAgentsStatus.test.ts:9:} from './resolveDefaultAgentsStatus';
apps/agents-server/src/utils\defaultAgents\resolveDefaultAgentsStatus.test.ts:18:describe('resolveMissingCoreAgentNames', () => {
apps/agents-server/src/utils\defaultAgents\resolveDefaultAgentsStatus.test.ts:22:        expect(resolveMissingCoreAgentNames(coreIdentities, ['adam'])).toEqual(['teacher']);
apps/agents-server/src/utils\defaultAgents\resolveDefaultAgentsStatus.test.ts:23:        expect(resolveMissingCoreAgentNames(coreIdentities, ['adam', 'teacher'])).toEqual([]);
apps/agents-server/src/utils\defaultAgents\resolveDefaultAgentsStatus.test.ts:24:        expect(resolveMissingCoreAgentNames(coreIdentities, [])).toEqual(['adam', 'teacher']);
apps/agents-server/src/utils\defaultAgents\resolveDefaultAgentsStatus.test.ts:28:describe('resolveCoreAgentExplanation', () => {
apps/agents-server/src/utils\defaultAgents\resolveDefaultAgentsStatus.test.ts:30:        expect(resolveCoreAgentExplanation('adam')).toBe(CORE_AGENT_EXPLANATION_BY_AGENT_NAME.adam);
apps/agents-server/src/utils\defaultAgents\resolveDefaultAgentsStatus.test.ts:31:        expect(resolveCoreAgentExplanation('Adam')).toBe(CORE_AGENT_EXPLANATION_BY_AGENT_NAME.adam);
apps/agents-server/src/utils\defaultAgents\resolveDefaultAgentsStatus.test.ts:35:        expect(resolveCoreAgentExplanation('some-unlisted-core-agent')).toBe(GENERIC_CORE_AGENT_EXPLANATION);
apps/agents-server/src/utils\defaultAgents\resolveDefaultAgentsStatus.test.ts:60:            join(defaultAgentsDirectory, '.core', 'adam.book'),
apps/agents-server/src/utils\defaultAgents\resolveDefaultAgentsStatus.test.ts:61:            'Adam\nMETA VISIBILITY PRIVATE\n\nPERSONA You are helpful.\nCLOSED\n',
apps/agents-server/src/utils\defaultAgents\resolveDefaultAgentsStatus.test.ts:73:        expect(identities.coreAgents[0]).toMatchObject({ agentName: 'adam', title: 'Adam' });
apps/agents-server/src/utils\defaultAgents\loadDefaultAgentBooks.ts:51:    const defaultAgentDirectory = await resolveDefaultAgentDirectory(options);
apps/agents-server/src/utils\defaultAgents\loadDefaultAgentBooks.ts:66:    const defaultAgentDirectory = await resolveDefaultAgentDirectory(options);
apps/agents-server/src/utils\defaultAgents\loadDefaultAgentBooks.ts:67:    const coreAgentDirectory = resolve(defaultAgentDirectory, CORE_AGENT_DIRECTORY_NAME);
apps/agents-server/src/utils\defaultAgents\loadDefaultAgentBooks.ts:108:export async function resolveDefaultAgentDirectory(options: LoadDefaultAgentBooksOptions = {}): Promise<string> {
apps/agents-server/src/utils\defaultAgents\loadDefaultAgentBooks.ts:110:        ...(options.defaultAgentDirectory ? [resolve(options.defaultAgentDirectory)] : []),
apps/agents-server/src/utils\findAgentForCallerWriteAccess.ts:11: * 3. Administrators (resolved via {@link isUserAdmin}) fall through to {@link findAgentByIdentifier} so they can manage any agent — including legacy `ADMIN_PASSWORD` admins that have no database user id.
apps/agents-server/src/utils\email\listAgentEmailIdentities.ts:44:        const displayName = resolveAgentDisplayName(row.agentProfile, row.agentName);
apps/agents-server/src/utils\email\listAgentEmailIdentities.ts:90:function resolveAgentDisplayName(agentProfile: unknown, fallbackAgentName: string): string {
apps/agents-server/src/utils\agentProjects\startDefaultAgentProjectRuntimes.ts:4:    resolveAgentProjectRuntimeDesiredStateFromRecords,
apps/agents-server/src/utils\agentProjects\startDefaultAgentProjectRuntimes.ts:7:import { resolveAgentProjectRuntime, startAgentProjectRuntime } from './agentProjectRuntimeRegistry';
apps/agents-server/src/utils\agentProjects\startDefaultAgentProjectRuntimes.ts:66:        if (resolveAgentProjectRuntimeDesiredStateFromRecords(desiredStateRecords, projectIdentity) === 'stopped') {
apps/agents-server/src/utils\agentProjects\startDefaultAgentProjectRuntimes.ts:107:    const runtime = await resolveAgentProjectRuntime(projectIdentity.agentPermanentId, projectIdentity.projectName);
apps/agents-server/src/utils\agentProjects\startDefaultAgentProjectRuntimes.test.ts:4:import { resolveLocalAgentRootPath } from '../localChatRunner/ensureLocalAgentFolder';
apps/agents-server/src/utils\agentProjects\startDefaultAgentProjectRuntimes.test.ts:16:    resolveAgentProjectRuntime,
apps/agents-server/src/utils\agentProjects\startDefaultAgentProjectRuntimes.test.ts:22:    resolveLocalAgentRootPath: jest.fn(),
apps/agents-server/src/utils\agentProjects\startDefaultAgentProjectRuntimes.test.ts:29:const resolveLocalAgentRootPathMock = resolveLocalAgentRootPath as jest.MockedFunction<
apps/agents-server/src/utils\agentProjects\startDefaultAgentProjectRuntimes.test.ts:30:    typeof resolveLocalAgentRootPath
apps/agents-server/src/utils\agentProjects\startDefaultAgentProjectRuntimes.test.ts:61:        resolveLocalAgentRootPathMock.mockReturnValue(temporaryDirectory);
apps/agents-server/src/utils\agentProjects\startDefaultAgentProjectRuntimes.test.ts:147:        const startedRuntime = await resolveAgentProjectRuntime('abc123', 'website');
apps/agents-server/src/utils\agentProjects\startDefaultAgentProjectRuntimes.test.ts:149:        const unchangedRuntime = await resolveAgentProjectRuntime('abc123', 'website');
apps/agents-server/src/utils\agentProjects\resolveCurrentAgentProjectServerDomain.ts:8:export async function resolveCurrentAgentProjectServerDomain(): Promise<string | null> {
apps/agents-server/src/utils\agentProjects\resolveAgentProjectRuntimeStatus.ts:2:import { resolveAgentProjectRuntime } from './agentProjectRuntimeRegistry';
apps/agents-server/src/utils\agentProjects\resolveAgentProjectRuntimeStatus.ts:3:import { resolveAgentProjectPublicUrls } from './resolveAgentProjectPublicUrls';
apps/agents-server/src/utils\agentProjects\resolveAgentProjectRuntimeStatus.ts:38:export async function resolveAgentProjectRuntimeStatus(
apps/agents-server/src/utils\agentProjects\resolveAgentProjectRuntimeStatus.ts:42:    const runtime = await resolveAgentProjectRuntime(agentPermanentId, projectName);
apps/agents-server/src/utils\agentProjects\resolveAgentProjectRuntimeStatus.ts:47:        projectUrl: runtime?.publicUrl ?? (await resolveAssignedAgentProjectPublicUrl(agentPermanentId, projectName)),
apps/agents-server/src/utils\agentProjects\resolveAgentProjectRuntimeStatus.ts:62: * @private helper of `resolveAgentProjectRuntimeStatus`
apps/agents-server/src/utils\agentProjects\resolveAgentProjectRuntimeStatus.ts:64:async function resolveAssignedAgentProjectPublicUrl(
apps/agents-server/src/utils\agentProjects\resolveAgentProjectRuntimeStatus.ts:69:        return (await resolveAgentProjectPublicUrls(agentPermanentId)).get(projectName.toLowerCase()) ?? null;
apps/agents-server/src/utils\agentProjects\resolveAgentProjectRuntimeEnvironmentFlag.ts:15:export function resolveAgentProjectRuntimeEnvironmentFlag(environmentVariableName: string): boolean | null {
apps/agents-server/src/utils\agentProjects\resolveAgentProjectPublicUrls.ts:1:import { listAgentProjectDomainRecords, resolveAgentProjectRuntimeBaseDomain } from './agentProjectRuntimeDomains';
apps/agents-server/src/utils\agentProjects\resolveAgentProjectPublicUrls.ts:2:import { resolveCurrentAgentProjectServerDomain } from './resolveCurrentAgentProjectServerDomain';
apps/agents-server/src/utils\agentProjects\resolveAgentProjectPublicUrls.ts:13:export async function resolveAgentProjectPublicUrls(agentPermanentId: string): Promise<ReadonlyMap<string, string>> {
apps/agents-server/src/utils\agentProjects\resolveAgentProjectPublicUrls.ts:16:        resolveCurrentAgentProjectServerDomain(),
apps/agents-server/src/utils\agentProjects\resolveAgentProjectPublicUrls.ts:18:    const serverDomain = resolveAgentProjectRuntimeBaseDomain(currentServerDomain);
apps/agents-server/src/utils\agentProjects\resolveAgentProjectPublicUrls.ts:26:        if (serverDomain && resolveAgentProjectRuntimeBaseDomain(projectDomainRecord.serverDomain) !== serverDomain) {
apps/agents-server/src/utils\agentProjects\resolveAgentProjectProfile.ts:58:export function resolveAgentProjectProfile(options: {
apps/agents-server/src/utils\agentProjects\resolveAgentProjectProfile.test.ts:2:import { resolveAgentProjectProfile } from './resolveAgentProjectProfile';
apps/agents-server/src/utils\agentProjects\resolveAgentProjectProfile.test.ts:4:describe('resolveAgentProjectProfile', () => {
apps/agents-server/src/utils\agentProjects\resolveAgentProjectProfile.test.ts:7:            resolveAgentProjectProfile({
apps/agents-server/src/utils\agentProjects\resolveAgentProjectProfile.test.ts:21:            resolveAgentProjectProfile({
apps/agents-server/src/utils\agentProjects\resolveAgentProjectProfile.test.ts:47:            resolveAgentProjectProfile({
apps/agents-server/src/utils\agentProjects\resolveAgentProjectProfile.test.ts:68:            resolveAgentProjectProfile({
apps/agents-server/src/utils\agentProjects\resolveAgentProjectProfile.test.ts:82:            resolveAgentProjectProfile({
apps/agents-server/src/utils\agentProjects\resolveAgentProjectProfile.test.ts:106:            resolveAgentProjectProfile({
apps/agents-server/src/utils\agentProjects\resolveAgentProjectInfo.ts:5:import { resolveAgentProjectsRootPath, resolveSafeAgentProjectPath } from './agentProjectsPaths';
apps/agents-server/src/utils\agentProjects\resolveAgentProjectInfo.ts:10:import { resolveAgentProjectFaviconRelativePath } from './resolveAgentProjectFaviconRelativePath';
apps/agents-server/src/utils\agentProjects\resolveAgentProjectInfo.ts:11:import { resolveAgentProjectProfile } from './resolveAgentProjectProfile';
apps/agents-server/src/utils\agentProjects\resolveAgentProjectInfo.ts:31:    const profile = resolveAgentProjectProfile({
apps/agents-server/src/utils\agentProjects\resolveAgentProjectInfo.ts:36:    const faviconRelativePath = await resolveAgentProjectFaviconRelativePath({
apps/agents-server/src/utils\agentProjects\resolveAgentProjectInfo.ts:64:export async function resolveAgentProjectInfo(
apps/agents-server/src/utils\agentProjects\resolveAgentProjectInfo.ts:68:    const projectPath = resolveSafeAgentProjectPath({ agentPermanentId, projectName });
apps/agents-server/src/utils\agentProjects\resolveAgentProjectInfo.ts:83:    return createAgentProjectInfo(resolveAgentProjectsRootPath(agentPermanentId), projectName);
apps/agents-server/src/utils\agentProjects\resolveAgentProjectInfo.test.ts:5:import { createAgentProjectInfo } from './resolveAgentProjectInfo';
apps/agents-server/src/utils\agentProjects\resolveAgentProjectFileContentType.ts:43:export function resolveAgentProjectFileContentType(fileName: string): string {
apps/agents-server/src/utils\agentProjects\resolveAgentProjectFaviconRelativePath.ts:6:import { resolveAgentProjectFileContentType } from './resolveAgentProjectFileContentType';
apps/agents-server/src/utils\agentProjects\resolveAgentProjectFaviconRelativePath.ts:29:export async function resolveAgentProjectFaviconRelativePath(options: {
apps/agents-server/src/utils\agentProjects\resolveAgentProjectFaviconRelativePath.ts:94:    if (!resolveAgentProjectFileContentType(iconFileName).startsWith(ICON_CONTENT_TYPE_PREFIX)) {
apps/agents-server/src/utils\agentProjects\resolveAgentProjectFaviconRelativePath.test.ts:4:import { resolveAgentProjectFaviconRelativePath } from './resolveAgentProjectFaviconRelativePath';
apps/agents-server/src/utils\agentProjects\resolveAgentProjectFaviconRelativePath.test.ts:6:describe('resolveAgentProjectFaviconRelativePath', () => {
apps/agents-server/src/utils\agentProjects\resolveAgentProjectFaviconRelativePath.test.ts:26:            resolveAgentProjectFaviconRelativePath({
apps/agents-server/src/utils\agentProjects\resolveAgentProjectFaviconRelativePath.test.ts:38:            resolveAgentProjectFaviconRelativePath({
apps/agents-server/src/utils\agentProjects\resolveAgentProjectFaviconRelativePath.test.ts:49:            resolveAgentProjectFaviconRelativePath({
apps/agents-server/src/utils\agentProjects\resolveAgentProjectFaviconRelativePath.test.ts:58:            resolveAgentProjectFaviconRelativePath({
apps/agents-server/src/utils\agentProjects\resolveAgentProjectFaviconRelativePath.test.ts:69:            resolveAgentProjectFaviconRelativePath({
apps/agents-server/src/utils\agentProjects\resolveAgentProjectFaviconRelativePath.test.ts:78:            resolveAgentProjectFaviconRelativePath({
apps/agents-server/src/utils\agentProjects\resolveAgentChatProjectReferences.ts:1:import { resolveAgentProjectsAccess, type AgentProjectsAccessResolution } from './agentProjectAccess';
apps/agents-server/src/utils\agentProjects\resolveAgentChatProjectReferences.ts:14:export async function resolveAgentChatProjectReferences(options: {
apps/agents-server/src/utils\agentProjects\resolveAgentChatProjectReferences.ts:18:    const projectsAccess = options.projectsAccess || (await resolveAgentProjectsAccess(options.agentPermanentId));
apps/agents-server/src/utils\agentProjects\readAgentProjectFile.ts:5:import { resolveSafeAgentProjectFilePath } from './agentProjectsPaths';
apps/agents-server/src/utils\agentProjects\readAgentProjectFile.ts:7:import { resolveAgentProjectFileContentType } from './resolveAgentProjectFileContentType';
apps/agents-server/src/utils\agentProjects\readAgentProjectFile.ts:42:    const filePath = resolveSafeAgentProjectFilePath(options);
apps/agents-server/src/utils\agentProjects\readAgentProjectFile.ts:54:            contentType: resolveAgentProjectFileContentType(fileName),
apps/agents-server/src/utils\agentProjects\listAllLocalAgentProjectIdentities.ts:2:import { resolveLocalAgentRootPath } from '../localChatRunner/ensureLocalAgentFolder';
apps/agents-server/src/utils\agentProjects\listAllLocalAgentProjectIdentities.ts:18:    const localAgentRootPath = resolveLocalAgentRootPath();
apps/agents-server/src/utils\explicitFromCommitment.ts:118: * Returns true when one book declares no parent at all and therefore implicitly inherits from `@Adam`.
apps/agents-server/src/utils\explicitFromCommitment.ts:120: * Writing no `FROM` commitment is equivalent to writing `FROM @Adam`, so only an explicit
apps/agents-server/src/utils\explicitFromCommitment.ts:124: * @returns True when the implicit `@Adam` ancestor applies to this book.
apps/agents-server/src/utils\explicitFromCommitment.ts:128:export function isImplicitAdamInheritance(agentSource: string_book): boolean {
apps/agents-server/src/utils\getWellKnownAgentUrl.ts:7: * Resolves the URL of one well-known agent (Adam, Teacher, ...) hosted locally in the `.core` folder of the current Agents Server.
apps/agents-server/src/utils\externalChatRunner\processExternalUserChatJob.ts:176:        resolveInitialAgentMessage: () => parseAgentSource(agentSourceSnapshot.agentSource).initialMessage,
apps/agents-server/src/utils\handleChatCompletion\createHandleChatCompletionPromptContext.ts:7:import { resolveTeamInternalAgentAccessToken } from '../../../../../src/commitments/_common/teamInternalAgentAccess';
apps/agents-server/src/utils\handleChatCompletion\createHandleChatCompletionPromptContext.ts:63:        agentName: runtime.resolvedAgentContext.resolvedAgentName,
apps/agents-server/src/utils\handleChatCompletion\createHandleChatCompletionPromptContext.ts:71:        teamInternalAccessToken: resolveTeamInternalAgentAccessToken(),
apps/agents-server/src/utils\handleChatCompletion\finalizeHandleChatCompletionResult.ts:2:import { resolveAppendOnlySelfLearningAgentSource } from '@/src/utils/resolveAppendOnlySelfLearningAgentSource';
apps/agents-server/src/utils\handleChatCompletion\finalizeHandleChatCompletionResult.ts:65:    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
apps/agents-server/src/utils\handleChatCompletion\finalizeHandleChatCompletionResult.ts:69:    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
apps/agents-server/src/utils\handleChatCompletion\finalizeHandleChatCompletionResult.ts:70:        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
apps/agents-server/src/utils\handleChatCompletion\finalizeHandleChatCompletionResult.ts:71:        resolvedAgentSourceBeforeLearning: runtime.agentSource,
apps/agents-server/src/utils\handleChatCompletion\finalizeHandleChatCompletionResult.ts:72:        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
apps/agents-server/src/utils\handleChatCompletion\resolveHandleChatCompletionRuntime.ts:3:import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
apps/agents-server/src/utils\handleChatCompletion\resolveHandleChatCompletionRuntime.ts:17:import { resolveMessageSuffixFromAgentSource } from '@/src/utils/chat/messageSuffix';
apps/agents-server/src/utils\handleChatCompletion\resolveHandleChatCompletionRuntime.ts:22:    resolveAgentCollectionTablePrefix,
apps/agents-server/src/utils\handleChatCompletion\resolveHandleChatCompletionRuntime.ts:25:import { resolveCachedServerAgentContext, resolveCachedServerAgentModelRequirements } from '../cachedServerAgentRuntime';
apps/agents-server/src/utils\handleChatCompletion\resolveHandleChatCompletionRuntime.ts:36:    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
apps/agents-server/src/utils\handleChatCompletion\resolveHandleChatCompletionRuntime.ts:39:    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
apps/agents-server/src/utils\handleChatCompletion\resolveHandleChatCompletionRuntime.ts:81:    const resolvedAgentContext = await resolveCachedServerAgentContext({
apps/agents-server/src/utils\handleChatCompletion\resolveHandleChatCompletionRuntime.ts:88:    if (!resolvedAgentContext?.resolvedAgentSource) {
apps/agents-server/src/utils\handleChatCompletion\resolveHandleChatCompletionRuntime.ts:92:    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
apps/agents-server/src/utils\handleChatCompletion\resolveHandleChatCompletionRuntime.ts:93:    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
apps/agents-server/src/utils\handleChatCompletion\resolveHandleChatCompletionRuntime.ts:96:    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
apps/agents-server/src/utils\handleChatCompletion\resolveHandleChatCompletionRuntime.ts:101:        ? await resolveCachedServerAgentModelRequirements({
apps/agents-server/src/utils\handleChatCompletion\resolveHandleChatCompletionRuntime.ts:102:              resolvedAgentContext,
apps/agents-server/src/utils\handleChatCompletion\resolveHandleChatCompletionRuntime.ts:107:    const agentId = resolvedAgentContext.parentAgentPermanentId;
apps/agents-server/src/utils\handleChatCompletion\resolveHandleChatCompletionRuntime.ts:126:        resolvedAgentContext,
apps/agents-server/src/utils\handleChatCompletion\resolveHandleChatCompletionRuntime.ts:135:        resolvedAgentContext,
apps/agents-server/src/utils\handleChatCompletion\resolveHandleChatCompletionRuntime.ts:138:        unresolvedAgentSource,
apps/agents-server/src/utils\handleChatCompletion\resolveHandleChatCompletionRuntime.ts:186:    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
apps/agents-server/src/utils\handleChatCompletion\resolveHandleChatCompletionRuntime.ts:248:    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
apps/agents-server/src/utils\handleChatCompletion\resolveHandleChatCompletionRuntime.ts:252:    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
apps/agents-server/src/utils\handleChatCompletion\resolveHandleChatCompletionRuntime.ts:258:              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
apps/agents-server/src/utils\handleChatCompletion\resolveHandleChatCompletionRuntime.ts:259:              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
apps/agents-server/src/utils\handleChatCompletion\resolveHandleChatCompletionRuntime.ts:266:        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
apps/agents-server/src/utils\handleChatCompletion\resolveHandleChatCompletionRuntime.ts:273:        options.resolvedAgentContext.resolvedAgentName,
apps/agents-server/src/utils\localAgentRouteReferences.ts:90:export function resolveLocalAgentRouteReference(
apps/agents-server/src/utils\localChatRunner.ts:4:    resolveLocalAgentRootPath,
apps/agents-server/src/utils\managementApi\managementApiAgents.ts:13:import { resolveStoredAgentState } from '../resolveStoredAgentState';
apps/agents-server/src/utils\managementApi\managementApiAgents.ts:46:export async function resolveOwnedAgentDerivedState(row: OwnedAgentRow) {
apps/agents-server/src/utils\managementApi\managementApiAgents.ts:58:    return resolveStoredAgentState(row, {
apps/agents-server/src/utils\managementApi\managementApiAgents.ts:202:        const profile = agent.resolvedAgentProfile as Partial<AgentBasicInformation>;
apps/agents-server/src/utils\managementApi\managementApiAgents.ts:209:            agent.resolvedAgentSource || '',
apps/agents-server/src/utils\managementApi\managementApiAgents.ts:220:                      text: agent.resolvedAgentSource || '',
apps/agents-server/src/utils\managementApi\managementApiAgents.ts:221:                      snippetText: agent.resolvedAgentSource || '',
apps/agents-server/src/utils\localChatRunner\ensureLocalAgentFolder.ts:59:    const directoryPath = join(resolveLocalAgentRootPath(), directoryName);
apps/agents-server/src/utils\localChatRunner\ensureLocalAgentFolder.ts:77:export function resolveLocalAgentRootPath(): string {
apps/agents-server/src/utils\localChatRunner\applyLocalAgentPlannedMessageCommands.ts:55:    const sidecarPath = resolveLocalAgentPlannedMessagesSidecarPath(options.agentDirectoryPath, options.metadata);
apps/agents-server/src/utils\localChatRunner\applyLocalAgentPlannedMessageCommands.ts:103:    await rm(resolveLocalAgentPlannedMessagesSidecarPath(agentDirectoryPath, metadata), { force: true }).catch(
apps/agents-server/src/utils\localChatRunner\applyLocalAgentPlannedMessageCommands.ts:219:function resolveLocalAgentPlannedMessagesSidecarPath(
apps/agents-server/src/utils\metaDisclaimer.ts:65:export function resolveMetaDisclaimerMarkdown(meta: AgentBasicInformation['meta']): string | null {
apps/agents-server/src/utils\metaDisclaimer.ts:78:export function resolveMetaDisclaimerMarkdownFromAgentSource(agentSource: string_book): string | null {
apps/agents-server/src/utils\metaDisclaimer.ts:143:    const disclaimerMarkdown = resolveMetaDisclaimerMarkdownFromAgentSource(agentSource);
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:6:import { createAgentModelRequirements } from '../../../../src/_packages/core.index'; // <- [🚾]
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:16:import { resolveAgentStateFromSource } from './resolveAgentStateFromSource';
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:29: * Well-known Adam URL, built exactly the way `getWellKnownAgentUrl('ADAM')` builds it.
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:34: * Bundled `.core` Adam book, trimmed to the commitments these tests assert on.
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:37:    Adam
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:45:    RULE Start from Adam.
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:69:describe('how `resolveAgentStateFromSource` resolves stored agents', () => {
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:93:     * Deletes the first Adam and reinstates it, exactly like the Core Agents admin page does.
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:95:     * @returns Permanent ids of the deleted leftover and of the Adam that is live afterwards.
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:97:    async function reinstateAdamCoreAgent(): Promise<{
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:98:        readonly deletedAdamPermanentId: string;
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:99:        readonly liveAdamPermanentId: string;
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:101:        const deletedAdam = await collection.createAgent(ADAM_BOOK);
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:102:        await collection.deleteAgent(deletedAdam.permanentId);
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:103:        const liveAdam = await collection.createAgent(ADAM_BOOK);
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:106:            deletedAdamPermanentId: deletedAdam.permanentId,
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:107:            liveAdamPermanentId: liveAdam.permanentId,
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:117:    async function resolveChildAgentSource(childAgentSource: string_book): Promise<string_book> {
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:129:        const resolvedAgentState = await resolveAgentStateFromSource(childAgentSource, {
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:136:        return resolvedAgentState.resolvedAgentSource;
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:145:    async function resolveChildSystemMessage(childAgentSource: string_book): Promise<string> {
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:146:        const resolvedAgentSource = await resolveChildAgentSource(childAgentSource);
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:147:        return (await createAgentModelRequirements(resolvedAgentSource)).systemMessage;
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:151:        const { deletedAdamPermanentId, liveAdamPermanentId } = await reinstateAdamCoreAgent();
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:153:        await expect(collection.getAgentPermanentId('adam')).resolves.toBe(liveAdamPermanentId);
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:154:        expect(liveAdamPermanentId).not.toBe(deletedAdamPermanentId);
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:161:        await expect(collection.getAgentPermanentId('generic-chatter')).resolves.toBe(deletedAgent.permanentId);
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:164:    it('should implicitly inherit from a reinstated Adam exactly like an explicit `FROM @Adam`', async () => {
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:165:        await reinstateAdamCoreAgent();
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:168:        const systemMessageWithAtAdam = await resolveChildSystemMessage(createChildBook('FROM @Adam'));
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:169:        const systemMessageWithBracedAdam = await resolveChildSystemMessage(createChildBook('FROM {Adam}'));
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:171:            createChildBook('FROM @aaa\nFROM {bbb ccc}\nFROM {ddd}\nFROM @Adam'),
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:175:        expect(systemMessageWithoutFrom).toContain('Start from Adam.');
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:177:        expect(systemMessageWithAtAdam).toBe(systemMessageWithoutFrom);
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:178:        expect(systemMessageWithBracedAdam).toBe(systemMessageWithoutFrom);
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:182:    it('should keep an explicit `FROM @Null` free of the implicit Adam ancestor', async () => {
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:183:        await reinstateAdamCoreAgent();
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:185:        const resolvedWithNullParent = await resolveChildAgentSource(createChildBook('FROM @Null'));
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts:188:        expect(resolvedWithNullParent).not.toContain('Start from Adam.');
apps/agents-server/src/utils\resolveAgentProfileWithInheritance.ts:4:import { resolveAgentStateFromSource } from './resolveAgentStateFromSource';
apps/agents-server/src/utils\resolveAgentProfileWithInheritance.ts:11:     * Adam agent URL used when inheritance defaults to Adam.
apps/agents-server/src/utils\resolveAgentProfileWithInheritance.ts:31:export async function resolveAgentProfileWithInheritance(
apps/agents-server/src/utils\resolveAgentProfileWithInheritance.ts:35:    const resolvedAgentState = await resolveAgentStateFromSource(agentSource, options);
apps/agents-server/src/utils\resolveAgentProfileWithInheritance.ts:36:    return resolvedAgentState.resolvedAgentProfile;
apps/agents-server/src/utils\resolveAgentHash.ts:20:export async function resolveAgentHash(agentIdentifier: string): Promise<string | null> {
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.test.ts:7:import { resolveInheritedAgentSource } from './resolveInheritedAgentSource';
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.test.ts:8:import { resolveAppendOnlySelfLearningAgentSource } from './resolveAppendOnlySelfLearningAgentSource';
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.test.ts:84:describe('resolveAppendOnlySelfLearningAgentSource', () => {
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.test.ts:85:    it('preserves child-only storage for implicit default Adam inheritance', async () => {
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.test.ts:95:                    Adam
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.test.ts:98:                    RULE Parent rule from Adam.
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.test.ts:103:        const resolvedBeforeLearning = await resolveInheritedAgentSource(unresolvedChildSource, {
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.test.ts:114:        const nextStoredSource = resolveAppendOnlySelfLearningAgentSource({
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.test.ts:115:            unresolvedAgentSourceBeforeLearning: unresolvedChildSource,
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.test.ts:116:            resolvedAgentSourceBeforeLearning: resolvedBeforeLearning,
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.test.ts:117:            resolvedAgentSourceAfterLearning: resolvedAfterLearning,
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.test.ts:123:        expect(nextStoredSource).not.toContain('NOTE Inherited Adam FROM https://core-test.ptbk.io/agents/adam');
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.test.ts:124:        expect(nextStoredSource).not.toContain('RULE Parent rule from Adam.');
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.test.ts:146:        const resolvedBeforeLearning = await resolveInheritedAgentSource(unresolvedChildSource, {
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.test.ts:157:        const nextStoredSource = resolveAppendOnlySelfLearningAgentSource({
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.test.ts:158:            unresolvedAgentSourceBeforeLearning: unresolvedChildSource,
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.test.ts:159:            resolvedAgentSourceBeforeLearning: resolvedBeforeLearning,
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.test.ts:160:            resolvedAgentSourceAfterLearning: resolvedAfterLearning,
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.test.ts:186:        const nextStoredSource = resolveAppendOnlySelfLearningAgentSource({
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.test.ts:187:            unresolvedAgentSourceBeforeLearning: unresolvedChildSource,
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.test.ts:188:            resolvedAgentSourceBeforeLearning: resolvedBeforeLearning,
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.test.ts:189:            resolvedAgentSourceAfterLearning: resolvedAfterLearning,
apps/agents-server/src/utils\resolveAgentStateFromSource.ts:5:import { resolveTeamCapabilitiesFromAgentSource } from './agentReferenceResolver/resolveTeamCapabilitiesFromAgentSource';
apps/agents-server/src/utils\resolveAgentStateFromSource.ts:6:import { resolveInheritedAgentSource, type AgentSourceImporter } from './resolveInheritedAgentSource';
apps/agents-server/src/utils\resolveAgentStateFromSource.ts:13:     * Adam agent URL used when inheritance defaults to Adam.
apps/agents-server/src/utils\resolveAgentStateFromSource.ts:50:    readonly unresolvedAgentSource: string_book;
apps/agents-server/src/utils\resolveAgentStateFromSource.ts:55:    readonly resolvedAgentSource: string_book;
apps/agents-server/src/utils\resolveAgentStateFromSource.ts:58:     * Parsed presentation/runtime profile derived only from `resolvedAgentSource`.
apps/agents-server/src/utils\resolveAgentStateFromSource.ts:60:    readonly resolvedAgentProfile: AgentBasicInformation;
apps/agents-server/src/utils\resolveAgentStateFromSource.ts:72:    resolvedTeamCapabilities: ReadonlyArray<AgentBasicInformation['capabilities'][number]>,
apps/agents-server/src/utils\resolveAgentStateFromSource.ts:103: * Everything outside the book editor should consume the returned `resolvedAgentSource`
apps/agents-server/src/utils\resolveAgentStateFromSource.ts:104: * and `resolvedAgentProfile` instead of reparsing the unresolved source.
apps/agents-server/src/utils\resolveAgentStateFromSource.ts:106: * @param unresolvedAgentSource - Editable source stored for the agent.
apps/agents-server/src/utils\resolveAgentStateFromSource.ts:110:export async function resolveAgentStateFromSource(
apps/agents-server/src/utils\resolveAgentStateFromSource.ts:111:    unresolvedAgentSource: string_book,
apps/agents-server/src/utils\resolveAgentStateFromSource.ts:114:    const resolvedAgentSource = await resolveInheritedAgentSource(unresolvedAgentSource, {
apps/agents-server/src/utils\resolveAgentStateFromSource.ts:122:    const resolvedAgentProfile = parseAgentSource(resolvedAgentSource);
apps/agents-server/src/utils\resolveAgentStateFromSource.ts:123:    const resolvedTeamCapabilities = await resolveTeamCapabilitiesFromAgentSource(
apps/agents-server/src/utils\resolveAgentStateFromSource.ts:124:        resolvedAgentSource,
apps/agents-server/src/utils\resolveAgentStateFromSource.ts:129:        unresolvedAgentSource,
apps/agents-server/src/utils\resolveAgentStateFromSource.ts:130:        resolvedAgentSource,
apps/agents-server/src/utils\resolveAgentStateFromSource.ts:131:        resolvedAgentProfile: {
apps/agents-server/src/utils\resolveAgentStateFromSource.ts:132:            ...resolvedAgentProfile,
apps/agents-server/src/utils\resolveAgentStateFromSource.ts:133:            capabilities: mergeResolvedTeamCapabilities(resolvedAgentProfile.capabilities, resolvedTeamCapabilities),
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:3:import { book, createAgentModelRequirements } from '../../../../src/_packages/core.index'; // <- [🚾]
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:9:import { resolveAgentStateFromSource } from './resolveAgentStateFromSource';
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:46:function createMockAgentCollection(
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:98: * @param adamAgentUrl - Adam URL for implicit inheritance.
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:101:async function createLocalInheritanceTestDependencies(collection: AgentCollection, adamAgentUrl: string) {
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:121:describe('resolveAgentStateFromSource', () => {
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:124:        const collection = createMockAgentCollection([
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:126:                agentName: 'Adam',
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:129:                    Adam
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:132:                    RULE Start from Adam.
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:154:        const resolvedAgentState = await resolveAgentStateFromSource(
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:169:        const modelRequirements = await createAgentModelRequirements(resolvedAgentState.resolvedAgentSource);
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:172:        expect(resolvedAgentState.resolvedAgentSource).toContain(
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:175:        expect(resolvedAgentState.resolvedAgentSource).not.toContain('RULE Start from Adam.');
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:178:        expect(modelRequirements.systemMessage).not.toContain('Start from Adam.');
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:181:    it('inherits from local Adam by default without HTTP import', async () => {
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:183:        const collection = createMockAgentCollection([
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:185:                agentName: 'Adam',
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:188:                    Adam
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:191:                    RULE Start from Adam.
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:200:            throw new Error('HTTP import should not be used for local Adam inheritance.');
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:203:        const resolvedAgentState = await resolveAgentStateFromSource(
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:218:        expect(resolvedAgentState.resolvedAgentSource).toContain(
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:219:            'NOTE Inherited Adam FROM https://local.example/agents/adam',
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:221:        expect(resolvedAgentState.resolvedAgentSource).toContain('RULE Start from Adam.');
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:226:        const collection = createMockAgentCollection([
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:228:                agentName: 'Adam',
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:231:                    Adam
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:234:                    RULE Start from Adam.
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:246:        const resolvedAgentState = await resolveAgentStateFromSource(
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:262:        expect(resolvedAgentState.resolvedAgentSource).toContain('FROM @Void');
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:263:        expect(resolvedAgentState.resolvedAgentSource).not.toContain('RULE Start from Adam.');
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:266:    it('inherits each explicitly selected local ancestor without adding Adam after an explicit Null root', async () => {
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:268:        const collection = createMockAgentCollection([
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:270:                agentName: 'Adam',
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:273:                    Adam
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:276:                    RULE Start from Adam.
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:308:        const resolvedAgentState = await resolveAgentStateFromSource(
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:322:        const modelRequirements = await createAgentModelRequirements(resolvedAgentState.resolvedAgentSource);
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:328:        expect(modelRequirements.systemMessage).not.toContain('Start from Adam.');
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:339:        const collection = createMockAgentCollection([
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:364:        const resolvedAgentState = await resolveAgentStateFromSource(agentASource, {
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:370:        const modelRequirements = await createAgentModelRequirements(resolvedAgentState.resolvedAgentSource);
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:373:        expect(resolvedAgentState.resolvedAgentSource).toContain(
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:376:        expect(resolvedAgentState.resolvedAgentSource).not.toContain(
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:379:        expect(resolvedAgentState.resolvedAgentSource).toContain('FROM @Null');
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:380:        expect(resolvedAgentState.resolvedAgentSource).not.toContain('FROM {Agent A}');
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:385:    it('includes Adam through an ancestor that does not declare its own FROM', async () => {
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:387:        const collection = createMockAgentCollection([
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:389:                agentName: 'Adam',
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:392:                    Adam
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:395:                    RULE Start from Adam.
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:413:        const resolvedAgentState = await resolveAgentStateFromSource(
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:427:        const modelRequirements = await createAgentModelRequirements(resolvedAgentState.resolvedAgentSource);
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:429:        expect(modelRequirements.systemMessage).toContain('Start from Adam.');
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:449:        const resolvedAgentState = await resolveAgentStateFromSource(
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:462:        expect(resolvedAgentState.resolvedAgentProfile.meta.image).toBe('https://cdn.example.com/parent.png');
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:463:        expect(resolvedAgentState.resolvedAgentProfile.meta.description).toBe('Parent description');
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:464:        expect(resolvedAgentState.resolvedAgentProfile.meta.color).toBe('#123456');
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:465:        expect(resolvedAgentState.resolvedAgentProfile.meta.font).toBe('Arial, sans-serif');
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:466:        expect(resolvedAgentState.resolvedAgentProfile.initialMessage).toBe('Hello from parent.');
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:484:        const resolvedAgentState = await resolveAgentStateFromSource(
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:499:        const modelRequirements = await createAgentModelRequirements(resolvedAgentState.resolvedAgentSource);
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:501:        expect(resolvedAgentState.resolvedAgentProfile.meta.color).toBe('#654321');
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:502:        expect(resolvedAgentState.resolvedAgentProfile.meta.font).toBe('Arial, sans-serif');
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:503:        expect(resolvedAgentState.resolvedAgentProfile.initialMessage).toBe('Hello from child.');
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:505:            resolvedAgentState.resolvedAgentProfile.capabilities.some((capability) => capability.type === 'privacy'),
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:508:            resolvedAgentState.resolvedAgentProfile.capabilities.some((capability) => capability.type === 'calendar'),
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:526:        const resolvedAgentState = await resolveAgentStateFromSource(
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:540:        const modelRequirements = await createAgentModelRequirements(resolvedAgentState.resolvedAgentSource);
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts:542:        expect(resolvedAgentState.resolvedAgentProfile.personaDescription).toBe('Child goal.');
apps/agents-server/src/utils\resolveCanonicalAgentName.ts:17:export async function resolveCanonicalAgentName(agentIdentifier: string): Promise<string | null> {
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.ts:13:    readonly unresolvedAgentSourceBeforeLearning: string_book;
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.ts:17:    readonly resolvedAgentSourceBeforeLearning: string_book;
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.ts:21:    readonly resolvedAgentSourceAfterLearning: string_book;
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.ts:75:export function resolveAppendOnlySelfLearningAgentSource(
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.ts:79:        options.resolvedAgentSourceBeforeLearning,
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.ts:80:        options.resolvedAgentSourceAfterLearning,
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.ts:87:    const normalizedUnresolvedSource = normalizeBookForComparison(options.unresolvedAgentSourceBeforeLearning);
apps/agents-server/src/utils\localChatRunner\processLocalUserChatJob.ts:50:    resolveLocalAgentRootPath,
apps/agents-server/src/utils\localChatRunner\processLocalUserChatJob.ts:185:        resolveInitialAgentMessage: () => parseAgentSource(agentSourceSnapshot.agentSource).initialMessage,
apps/agents-server/src/utils\localChatRunner\processLocalUserChatJob.ts:267:    const agentDirectoryPath = join(resolveLocalAgentRootPath(), metadata.agentDirectoryName);
apps/agents-server/src/utils\localChatRunner\processLocalUserChatJob.ts:506:    const agentDirectoryPath = join(resolveLocalAgentRootPath(), metadata.agentDirectoryName);
apps/agents-server/src/utils\localChatRunner\prepareLocalTeamConversationWorkspace.ts:14:    resolveCachedServerAgentContext,
apps/agents-server/src/utils\localChatRunner\prepareLocalTeamConversationWorkspace.ts:15:    resolveCachedServerAgentModelRequirements,
apps/agents-server/src/utils\localChatRunner\prepareLocalTeamConversationWorkspace.ts:18:import { normalizeLocalServerUrls, resolveLocalAgentRouteReference } from '../localAgentRouteReferences';
apps/agents-server/src/utils\localChatRunner\prepareLocalTeamConversationWorkspace.ts:49:    const primaryAgentContext = await resolveCachedServerAgentContext({
apps/agents-server/src/utils\localChatRunner\prepareLocalTeamConversationWorkspace.ts:55:    const preparedRequirements = await resolveCachedServerAgentModelRequirements({
apps/agents-server/src/utils\localChatRunner\prepareLocalTeamConversationWorkspace.ts:56:        resolvedAgentContext: primaryAgentContext,
apps/agents-server/src/utils\localChatRunner\prepareLocalTeamConversationWorkspace.ts:147:        const routeReference = resolveLocalAgentRouteReference(
apps/agents-server/src/utils\localChatRunner\prepareLocalTeamConversationWorkspace.ts:166:        const teammateContext = await resolveCachedServerAgentContext({
apps/agents-server/src/utils\localChatRunner\prepareLocalTeamConversationWorkspace.ts:172:        const teammateName = preparedTeammate.label?.trim() || teammateContext.resolvedAgentName;
apps/agents-server/src/utils\localChatRunner\prepareLocalTeamConversationWorkspace.ts:186:            source: teammateContext.resolvedAgentSource,
apps/agents-server/src/utils\localChatRunner\persistLocalUserChatJobProgressFromRuntimeLog.ts:5:import { resolveAgentMessageRuntimeActivity } from '../../../../../src/utils/agent-message-runtime/resolveAgentMessageRuntimeActivity';
apps/agents-server/src/utils\localChatRunner\persistLocalUserChatJobProgressFromRuntimeLog.ts:8:import { resolveLocalAgentRootPath } from './ensureLocalAgentFolder';
apps/agents-server/src/utils\localChatRunner\persistLocalUserChatJobProgressFromRuntimeLog.ts:66:            join(resolveLocalAgentRootPath(), metadata.agentDirectoryName),
apps/agents-server/src/utils\localChatRunner\persistLocalUserChatJobProgressFromRuntimeLog.ts:74:        const activity = resolveAgentMessageRuntimeActivity(logText);
apps/agents-server/src/utils\localChatRunner\parseLocalTeamConversations.ts:169:        const sender = resolveTranscriptSender(String(message.sender), primaryAgentName, teammateName);
apps/agents-server/src/utils\pseudoAgents.ts:3:    resolvePseudoAgentKindFromReference,
apps/agents-server/src/utils\pseudoAgents.ts:166:export function resolvePseudoAgentDescriptor(
apps/agents-server/src/utils\pseudoAgents.ts:169:    const kind = resolvePseudoAgentKindFromReference(reference);
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:11:import { resolveInheritedAgentSource } from './resolveInheritedAgentSource';
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:34:function createMockAgentCollection(agentRecords: Array<{ agentName: string; permanentId?: string }>): AgentCollection {
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:86:describe('how `resolveInheritedAgentSource` works', () => {
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:100:            resolveInheritedAgentSource(
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:126:    it('should resolve implicit default inheritance from Adam', async () => {
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:130:                    Adam
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:133:                    RULE Start from Adam.
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:138:        const resolvedAgentSource = await resolveInheritedAgentSource(
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:150:        expect(resolvedAgentSource).toContain('NOTE Inherited Adam FROM https://core-test.ptbk.io/agents/adam');
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:151:        expect(resolvedAgentSource).toContain('RULE Start from Adam.');
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:152:        expect(resolvedAgentSource).toContain('LANGUAGE Italian');
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:155:    it('should treat a book without `FROM` exactly like an explicit `FROM @Adam` / `FROM {Adam}`', async () => {
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:156:        const localAdamAgentUrl = 'https://local.example/agents/adam-1';
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:159:         * Resolves one of the three equivalent books against the same local Adam agent.
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:164:        async function resolveAgainstLocalAdam(agentSource: string_book): Promise<string> {
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:166:                agentCollection: createMockAgentCollection([{ agentName: 'Adam', permanentId: 'adam-1' }]),
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:171:                [`${localAdamAgentUrl}/api/book?recursionLevel=1`]: {
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:173:                        Adam
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:176:                        RULE Start from Adam.
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:181:            const resolvedAgentSource = await resolveInheritedAgentSource(agentSource, {
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:183:                adamAgentUrl: localAdamAgentUrl,
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:188:            return resolvedAgentSource.split('NOTE Inherited Adam FROM ').join('NOTE Inherited FROM ');
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:191:        const resolvedWithoutFrom = await resolveAgainstLocalAdam(book`
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:197:        const resolvedWithAtAdam = await resolveAgainstLocalAdam(book`
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:200:            FROM @Adam
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:204:        const resolvedWithBracedAdam = await resolveAgainstLocalAdam(book`
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:207:            FROM {Adam}
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:212:        expect(resolvedWithoutFrom).toContain('RULE Start from Adam.');
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:214:        expect(resolvedWithAtAdam).toBe(resolvedWithoutFrom);
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:215:        expect(resolvedWithBracedAdam).toBe(resolvedWithoutFrom);
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:221:                agentCollection: createMockAgentCollection([{ agentName: 'Adam', permanentId: 'adam-1' }]),
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:225:            const resolvedAgentSource = await resolveInheritedAgentSource(
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:242:            expect(resolvedAgentSource).not.toContain('Inherited');
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:243:            expect(resolvedAgentSource).toContain(`FROM ${voidReference}`);
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:244:            expect(resolvedAgentSource).toContain('GOAL Keep your projects up to date');
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:264:        const resolvedWithOverriddenFrom = await resolveInheritedAgentSource(
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:278:        const resolvedWithSingleFrom = await resolveInheritedAgentSource(
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:292:        expect(resolvedWithOverriddenFrom).toContain(`NOTE Inherited FROM ${effectiveParentAgentUrl}`);
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:295:        expect(resolvedWithOverriddenFrom).not.toContain(overriddenParentAgentUrl);
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:301:            resolveInheritedAgentSource(book`
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:319:            agentCollection: createMockAgentCollection([{ agentName: 'Local Parent', permanentId: 'parent-1' }]),
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:334:        const resolvedAgentSource = await resolveInheritedAgentSource(
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:348:        expect(resolvedAgentSource).toContain('NOTE Inherited FROM https://local.example/agents/parent-1');
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:349:        expect(resolvedAgentSource).toContain('RULE Parent from local server.');
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:350:        expect(resolvedAgentSource).toContain('RULE Child rule.');
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:355:            agentCollection: createMockAgentCollection([]),
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:377:        const resolvedAgentSource = await resolveInheritedAgentSource(
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:391:        expect(resolvedAgentSource).toContain('NOTE Inherited FROM https://federated.example/agents/remote-parent');
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:392:        expect(resolvedAgentSource).toContain('RULE Parent from federated server.');
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:393:        expect(resolvedAgentSource).toContain('RULE Child rule.');
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:396:    it('should embed fallback source after failed default Adam retries', async () => {
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:409:        const resolvedAgentSource = await resolveInheritedAgentSource(
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:426:        expect(resolvedAgentSource).toContain('NOTE Inherited Adam FROM https://core-test.ptbk.io/agents/adam');
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:427:        expect(resolvedAgentSource).toContain(
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:430:        expect(resolvedAgentSource).toContain('LANGUAGE Italian');
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:435:            agentCollection: createMockAgentCollection([]),
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:439:        const resolvedAgentSource = await resolveInheritedAgentSource(
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:453:        expect(resolvedAgentSource).toContain(
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:456:        expect(resolvedAgentSource).toContain('LANGUAGE Italian');
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:461:            agentCollection: createMockAgentCollection([]),
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:465:        const resolvedAgentSource = await resolveInheritedAgentSource(
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:480:        expect(resolvedAgentSource).toContain(
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:483:        expect(resolvedAgentSource).toContain('LANGUAGE Italian');
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:499:        const resolvedAgentSource = await resolveInheritedAgentSource(
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:517:        expect(resolvedAgentSource).toContain('NOTE Imported from https://federated.example/agents/missing-source');
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:518:        expect(resolvedAgentSource).toContain(
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:521:        expect(resolvedAgentSource).toContain('LANGUAGE Italian');
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:534:            resolveInheritedAgentSource(agentSource, {
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:558:            resolveInheritedAgentSource(agentSource, {
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:573:    it('should not make Adam implicitly inherit from itself through an agent URL alias', async () => {
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:577:        const resolvedAgentSource = await resolveInheritedAgentSource(
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:579:                Adam
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:581:                RULE Adam rule.
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:592:        expect(resolvedAgentSource).toContain('RULE Adam rule.');
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts:593:        expect(resolvedAgentSource).not.toContain('Inherited Adam FROM');
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:113:async function resolveParentAgentUrlFromCommitments(
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:127:            await agentReferenceResolver.resolveCommitmentContent('FROM', resolvedParentReference)
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:131:    if (!resolvedParentReference || isVoidPseudoAgentReference(resolvedParentReference)) {
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:158:     * The URL of the Adam agent to use as the default ancestor
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:224:     * Effective parent URL after resolving explicit `FROM`, `FROM VOID`, or implicit Adam fallback.
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:249:     * Adam agent URL used as the implicit default ancestor.
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:393: * @param options - Resolution options passed to `resolveInheritedAgentSource`.
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:412: * Computes the effective parent URL after applying explicit `FROM` and Adam fallback rules.
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:414: * @param resolvedParentAgentUrl - Parent URL derived from lightweight commitment parsing.
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:416: * @param adamAgentUrl - Default Adam ancestor URL.
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:423:    resolvedParentAgentUrl: string_agent_url | null | undefined,
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:430:    if (isValidAgentUrl(resolvedParentAgentUrl)) {
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:431:        return resolvedParentAgentUrl as string_agent_url;
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:434:    if (resolvedParentAgentUrl === null && hasExplicitFromCommitment) {
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:438:    if (resolvedParentAgentUrl === undefined || resolvedParentAgentUrl === null) {
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:442:        const isCurrentAgentAdam = currentAgentUrls.some(
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:446:        return isCurrentAgentAdam ? null : adamAgentUrl;
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:452:                Invalid parent agent URL in FROM "${resolvedParentAgentUrl}" commitment:
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:502:async function resolveParentAgentContext(
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:508:    const resolvedParentAgentUrl = await resolveParentAgentUrlFromCommitments(
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:516:        resolvedParentAgentUrl,
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:564:async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:571:            importedUrlOrPath = await context.agentReferenceResolver.resolveCommitmentContent(
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:614:function resolveFromCommitmentLine(
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:678:async function resolveAgentSourceBuild(
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:684:    const resolvedAgentSourceChunks: Array<string> = [];
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:701:            resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:706:            const resolvedFromCommitment = resolveFromCommitmentLine(line, {
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:711:            resolvedAgentSourceChunks.push(...resolvedFromCommitment.agentSourceChunks);
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:712:            fromResolutionIssues = resolvedFromCommitment.fromResolutionIssues;
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:717:        resolvedAgentSourceChunks.push(line);
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:722:        agentSourceChunks: resolvedAgentSourceChunks,
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:729: * Materializes the implicit Adam inheritance block when no explicit `FROM ...` was present.
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:733: * @param adamAgentUrl - Default Adam ancestor URL.
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:734: * @returns Updated source build with implicit Adam inheritance applied when needed.
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:736:function applyImplicitAdamInheritance(
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:752:                `NOTE Inherited Adam FROM ${parentContext.parentAgentUrl}`,
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:778:    const resolvedAgentSource = padBook(validateBook(build.agentSourceChunks.join('\n')));
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:781:        return resolvedAgentSource;
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:785:    return insertNotesAfterTitle(resolvedAgentSource, unresolvedFromNotes);
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:833:export async function resolveInheritedAgentSource(
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:838:    const parentContext = await resolveParentAgentContext(agentSource, context);
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:839:    const resolvedBuild = await resolveAgentSourceBuild(agentSource, context, parentContext);
apps/agents-server/src/utils\resolveInheritedAgentSource.ts:840:    const finalizedBuild = applyImplicitAdamInheritance(resolvedBuild, parentContext, context.adamAgentUrl);
apps/agents-server/src/utils\plannedMessageManager\collectPlannedMessageAgentOptions.ts:40:        resolvePlannedMessageAgentOptionLabel(leftOption).localeCompare(
apps/agents-server/src/utils\plannedMessageManager\collectPlannedMessageAgentOptions.ts:41:            resolvePlannedMessageAgentOptionLabel(rightOption),
apps/agents-server/src/utils\plannedMessageManager\collectPlannedMessageAgentOptions.ts:54:export function resolvePlannedMessageAgentOptionLabel(option: PlannedMessageAgentOption): string {
apps/agents-server/src/utils\resolveServerAgentContext.ts:3:import { resolveBookScopedAgentContext, type ResolvedBookScopedAgentContext } from './agentReferenceResolver/bookScopedAgentReferences';
apps/agents-server/src/utils\resolveServerAgentContext.ts:7:import { resolveAgentStateFromSource, type ResolvedAgentState } from './resolveAgentStateFromSource';
apps/agents-server/src/utils\resolveServerAgentContext.ts:45:export async function resolveServerAgentContext(
apps/agents-server/src/utils\resolveServerAgentContext.ts:48:    const bookScopedAgentContext = await resolveBookScopedAgentContext(options);
apps/agents-server/src/utils\resolveServerAgentContext.ts:59:    const resolvedAgentState = await resolveAgentStateFromSource(bookScopedAgentContext.unresolvedAgentSource, {
apps/agents-server/src/utils\resolveServerAgentContext.ts:70:        ...resolvedAgentState,
apps/agents-server/src/utils\resolveStoredAgentState.ts:4:    resolveAgentStateFromSource,
apps/agents-server/src/utils\resolveStoredAgentState.ts:7:} from './resolveAgentStateFromSource';
apps/agents-server/src/utils\resolveStoredAgentState.ts:80:export async function resolveStoredAgentState<TAgent extends StoredAgentSourceRecord>(
apps/agents-server/src/utils\resolveStoredAgentState.ts:88:    const resolvedAgentState = await resolveAgentStateFromSource(agent.agentSource as string_book, {
apps/agents-server/src/utils\resolveStoredAgentState.ts:102:        ...resolvedAgentState,
apps/agents-server/src/utils\resolveStoredAgentState.ts:113:export async function resolveStoredAgentStates<TAgent extends StoredAgentSourceRecord>(
apps/agents-server/src/utils\resolveStoredAgentState.ts:117:    return Promise.all(agents.map((agent) => resolveStoredAgentState(agent, options)));
apps/agents-server/src/utils\resourceMonitor\readAgentProjectsResourceUsage.ts:3:import { resolveLocalAgentRootPath } from '../localChatRunner/ensureLocalAgentFolder';
apps/agents-server/src/utils\resourceMonitor\readAgentProjectsResourceUsage.ts:41:            rootPath: resolveLocalAgentRootPath(),
apps/agents-server/src/utils\sendUserChatPushNotification.ts:206:        { resolveAgentAvatarImageUrl },
apps/agents-server/src/utils\sendUserChatPushNotification.ts:210:        { resolveServerAgentContext },
apps/agents-server/src/utils\sendUserChatPushNotification.ts:214:        import('../../../../src/utils/agents/resolveAgentAvatarImageUrl'),
apps/agents-server/src/utils\sendUserChatPushNotification.ts:218:        import('./resolveServerAgentContext'),
apps/agents-server/src/utils\sendUserChatPushNotification.ts:223:    const resolvedAgentContext = await resolveServerAgentContext({
apps/agents-server/src/utils\sendUserChatPushNotification.ts:229:    const agentProfile = resolvedAgentContext.resolvedAgentProfile;
apps/agents-server/src/utils\sendUserChatPushNotification.ts:230:    const agentIdentifier = agentProfile.permanentId || resolvedAgentContext.parentAgentPermanentId;
apps/agents-server/src/utils\sendUserChatPushNotification.ts:233:        resolveAgentAvatarImageUrl({ agent: agentProfile, baseUrl: serverOrigin }) ||
apps/agents-server/src/utils\taskTerminal\resolveAdminTaskTerminalSession.ts:9:import { resolveLocalAgentRootPath } from '../localChatRunner/ensureLocalAgentFolder';
apps/agents-server/src/utils\taskTerminal\resolveAdminTaskTerminalSession.ts:234:        join(resolveLocalAgentRootPath(), options.agentDirectoryName),
apps/agents-server/src/utils\transpilers\createTranspiledAgentExportArtifacts.ts:283:    const runtimeScaffold = await resolveTranspiledAgentRuntimeScaffold({
apps/agents-server/src/utils\transpilers\createTranspiledAgentExportArtifacts.ts:327:async function resolveTranspiledAgentRuntimeScaffold(options: {
apps/agents-server/src/utils\transpilers\resolveTranspiledAgentCodeExport.test.ts:2:import { findBookTranspilerForExport, listBookTranspilersForExport } from './resolveTranspiledAgentCodeExport';
apps/agents-server/src/utils\transpilers\resolveTranspiledAgentCodeExport.test.ts:4:describe('resolveTranspiledAgentCodeExport', () => {
apps/agents-server/src/utils\transpilers\resolveTranspiledAgentCodeExport.ts:4:import { resolveServerAgentContext } from '@/src/utils/resolveServerAgentContext';
apps/agents-server/src/utils\transpilers\resolveTranspiledAgentCodeExport.ts:84:export async function resolveTranspiledAgentCodeExport(options: {
apps/agents-server/src/utils\transpilers\resolveTranspiledAgentCodeExport.ts:98:    const resolvedAgentContext = await resolveServerAgentContext({
apps/agents-server/src/utils\transpilers\resolveTranspiledAgentCodeExport.ts:104:    const resolvedAgentSource = resolvedAgentContext.resolvedAgentSource;
apps/agents-server/src/utils\transpilers\resolveTranspiledAgentCodeExport.ts:106:    if (!resolvedAgentSource) {
apps/agents-server/src/utils\transpilers\resolveTranspiledAgentCodeExport.ts:111:        rootAgentUrl: resolvedAgentContext.canonicalAgentUrl,
apps/agents-server/src/utils\transpilers\resolveTranspiledAgentCodeExport.ts:112:        rootAgentSource: resolvedAgentSource,
apps/agents-server/src/utils\transpilers\resolveTranspiledAgentCodeExport.ts:113:        agentReferenceResolver: resolvedAgentContext.scopedAgentReferenceResolver,
apps/agents-server/src/utils\transpilers\resolveTranspiledAgentCodeExport.ts:117:    const transpiledCode = await transpiler.transpileBook(resolvedAgentSource, tools, {
apps/agents-server/src/utils\transpilers\resolveTranspiledAgentCodeExport.ts:118:        agentReferenceResolver: resolvedAgentContext.scopedAgentReferenceResolver,
apps/agents-server/src/utils\transpilers\resolveTranspiledTeamExport.ts:1:import { createAgentModelRequirements, parseAgentSource } from '@promptbook-local/core';
apps/agents-server/src/utils\transpilers\resolveTranspiledTeamExport.ts:4:import { resolvePseudoAgentKindFromUrl } from '../../../../../src/book-2.0/agent-source/pseudoAgentReferences';
apps/agents-server/src/utils\transpilers\resolveTranspiledTeamExport.ts:83:        const teammateAgent = await resolveTranspiledTeamMemberAgent({
apps/agents-server/src/utils\transpilers\resolveTranspiledTeamExport.ts:105:async function resolveTranspiledTeamMemberAgent(options: {
apps/agents-server/src/utils\transpilers\resolveTranspiledTeamExport.ts:110:    const pseudoAgentKind = resolvePseudoAgentKindFromUrl(options.teammate.url);
apps/agents-server/src/utils\transpilers\resolveTranspiledTeamExport.ts:144:    const modelRequirements = await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
apps/agents-server/src/utils\transpilers\resolveTranspiledTeamExport.ts:167:    const pseudoAgentKind = resolvePseudoAgentKindFromUrl(teammate.url);
apps/agents-server/src/utils\userChat\createRunUserChatJobExecutionContext.ts:6:    resolveAgentCollectionTablePrefix,
apps/agents-server/src/utils\userChat\createRunUserChatJobExecutionContext.ts:12:    resolveCachedServerAgentContext,
apps/agents-server/src/utils\userChat\createRunUserChatJobExecutionContext.ts:13:    resolveCachedServerAgentModelRequirements,
apps/agents-server/src/utils\userChat\createRunUserChatJobExecutionContext.ts:25:import { resolveTeamInternalAgentAccessToken } from '../../../../../src/commitments/_common/teamInternalAgentAccess';
apps/agents-server/src/utils\userChat\createRunUserChatJobExecutionContext.ts:96:    const resolvedAgentContext = await resolveCachedServerAgentContext({
apps/agents-server/src/utils\userChat\createRunUserChatJobExecutionContext.ts:102:    const preparedAgentModelRequirements = await resolveCachedServerAgentModelRequirements({
apps/agents-server/src/utils\userChat\createRunUserChatJobExecutionContext.ts:103:        resolvedAgentContext,
apps/agents-server/src/utils\userChat\createRunUserChatJobExecutionContext.ts:107:    const agentSource = resolvedAgentContext.resolvedAgentSource;
apps/agents-server/src/utils\userChat\createRunUserChatJobExecutionContext.ts:108:    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
apps/agents-server/src/utils\userChat\createRunUserChatJobExecutionContext.ts:109:    const agentPermanentId = resolvedAgentContext.parentAgentPermanentId;
apps/agents-server/src/utils\userChat\createRunUserChatJobExecutionContext.ts:110:    const resolvedAgentName = resolvedAgentContext.resolvedAgentName;
apps/agents-server/src/utils\userChat\createRunUserChatJobExecutionContext.ts:118:        agentName: resolvedAgentName,
apps/agents-server/src/utils\userChat\createRunUserChatJobExecutionContext.ts:154:        agentName: resolvedAgentName,
apps/agents-server/src/utils\userChat\createRunUserChatJobExecutionContext.ts:165:        teamInternalAccessToken: resolveTeamInternalAgentAccessToken(),
apps/agents-server/src/utils\userChat\createRunUserChatJobExecutionContext.ts:168:        resolvedAgentName,
apps/agents-server/src/utils\userChat\createRunUserChatJobExecutionContext.ts:182:    const tablePrefix = resolveAgentCollectionTablePrefix(collection);
apps/agents-server/src/utils\userChat\createRunUserChatJobExecutionContext.ts:201:        resolvedAgentName,
apps/agents-server/src/utils\userChat\createRunUserChatJobExecutionContext.ts:231:        unresolvedAgentSource,
apps/agents-server/src/utils\userChat\createRunUserChatJobExecutionContext.ts:233:        resolvedAgentName,
apps/agents-server/src/utils\userChat\createRunUserChatJobExecutionContext.ts:238:        isBookScopedAgent: resolvedAgentContext.isBookScopedAgent,
apps/agents-server/src/utils\userChat\createRunUserChatJobExecutionContext.ts:282:    resolvedAgentName: string;
apps/agents-server/src/utils\userChat\createRunUserChatJobExecutionContext.ts:293:        title: `Chat with agent ${options.resolvedAgentName}`,
apps/agents-server/src/utils\usageAnalytics\UsageAnalyticsSource.ts:29:        const allowedAgentNames = resolveAllowedAgentNames({
apps/agents-server/src/utils\usageAnalytics\UsageAnalyticsSource.ts:132:function resolveAllowedAgentNames(options: {
apps/agents-server/src/utils\userWallet\createUserWalletRecord.ts:7:import { resolveWalletAgentPermanentId } from './resolveWalletAgentPermanentId';
apps/agents-server/src/utils\userWallet\createUserWalletRecord.ts:14:    const agentPermanentId = options.isGlobal ? null : await resolveWalletAgentPermanentId(options.agentPermanentId);
apps/agents-server/src/utils\userWallet\createUserWalletRecord.test.ts:6:import { resolveWalletAgentPermanentId } from './resolveWalletAgentPermanentId';
apps/agents-server/src/utils\userWallet\createUserWalletRecord.test.ts:20:jest.mock('./resolveWalletAgentPermanentId', () => ({
apps/agents-server/src/utils\userWallet\createUserWalletRecord.test.ts:21:    resolveWalletAgentPermanentId: jest.fn(),
apps/agents-server/src/utils\userWallet\createUserWalletRecord.test.ts:42:const resolveWalletAgentPermanentIdMock = resolveWalletAgentPermanentId as jest.MockedFunction<
apps/agents-server/src/utils\userWallet\createUserWalletRecord.test.ts:43:    typeof resolveWalletAgentPermanentId
apps/agents-server/src/utils\userWallet\createUserWalletRecord.test.ts:76:        resolveWalletAgentPermanentIdMock.mockResolvedValue('lawyer-123');
apps/agents-server/src/utils\userWallet\createUserWalletRecord.test.ts:109:        expect(resolveWalletAgentPermanentIdMock).toHaveBeenCalledWith('Lawyer');
apps/agents-server/src/utils\userWallet\listUserWalletRecords.ts:5:import { resolveWalletAgentPermanentId } from './resolveWalletAgentPermanentId';
apps/agents-server/src/utils\userWallet\listUserWalletRecords.ts:52:    const resolvedAgentPermanentId = await resolveListUserWalletRecordsAgentPermanentId(options.agentPermanentId);
apps/agents-server/src/utils\userWallet\listUserWalletRecords.ts:54:    if (options.agentPermanentId && !resolvedAgentPermanentId) {
apps/agents-server/src/utils\userWallet\listUserWalletRecords.ts:58:    const rows = await listUserWalletRows(userWalletTable, options, resolvedAgentPermanentId);
apps/agents-server/src/utils\userWallet\listUserWalletRecords.ts:66:async function resolveListUserWalletRecordsAgentPermanentId(agentPermanentId?: string): Promise<string | null> {
apps/agents-server/src/utils\userWallet\listUserWalletRecords.ts:71:    return (await resolveWalletAgentPermanentId(agentPermanentId)) || null;
apps/agents-server/src/utils\userWallet\listUserWalletRecords.ts:80:    resolvedAgentPermanentId: string | null,
apps/agents-server/src/utils\userWallet\listUserWalletRecords.ts:82:    if (!resolvedAgentPermanentId) {
apps/agents-server/src/utils\userWallet\listUserWalletRecords.ts:92:        resolvedAgentPermanentId,
apps/agents-server/src/utils\vpsSelfUpdate\readAgentsServerFooterVersion.ts:172:export function resolveAgentsServerFooterVersionUrl(options: {
apps/agents-server/src/utils\vpsSelfUpdate\readAgentsServerFooterVersion.ts:196:    const repositoryDirectory = await resolveAgentsServerFooterRepositoryDirectory();
apps/agents-server/src/utils\vpsSelfUpdate\readAgentsServerFooterVersion.ts:222:        versionUrl: resolveAgentsServerFooterVersionUrl({
apps/agents-server/src/utils\vpsSelfUpdate\readAgentsServerFooterVersion.ts:253:async function resolveAgentsServerFooterRepositoryDirectory(): Promise<string | null> {
apps/agents-server/src/utils\vpsSelfUpdate\readAgentsServerFooterVersion.ts:324:        versionUrl: resolveAgentsServerFooterVersionUrl({
apps/agents-server/src/utils\vpsSelfUpdate\readAgentsServerFooterVersion.test.ts:6:    resolveAgentsServerFooterVersionUrl,
apps/agents-server/src/utils\vpsSelfUpdate\readAgentsServerFooterVersion.test.ts:42:            resolveAgentsServerFooterVersionUrl({
apps/agents-server/src/utils\vpsSelfUpdate\readAgentsServerFooterVersion.test.ts:50:            resolveAgentsServerFooterVersionUrl({
apps/agents-server/src/utils\userWallet\resolveWalletAccessTokenFromScopes.ts:3:import { resolveWalletAgentPermanentId } from './resolveWalletAgentPermanentId';
apps/agents-server/src/utils\userWallet\resolveWalletAccessTokenFromScopes.ts:50:    const agentPermanentId = await resolveWalletAgentPermanentId(options.agentPermanentId);
apps/agents-server/src/utils\userWallet\resolveWalletAgentPermanentId.test.ts:4:import { resolveWalletAgentPermanentId } from './resolveWalletAgentPermanentId';
apps/agents-server/src/utils\userWallet\resolveWalletAgentPermanentId.test.ts:27: * Creates one minimal chained Supabase mock for `resolveWalletAgentPermanentId`.
apps/agents-server/src/utils\userWallet\resolveWalletAgentPermanentId.test.ts:49:describe('resolveWalletAgentPermanentId', () => {
apps/agents-server/src/utils\userWallet\resolveWalletAgentPermanentId.test.ts:65:        await expect(resolveWalletAgentPermanentId('Lawyer')).resolves.toBe('lawyer-123');
apps/agents-server/src/utils\userWallet\resolveWalletAgentPermanentId.test.ts:71:        await expect(resolveWalletAgentPermanentId('   ')).resolves.toBeNull();
apps/agents-server/src/utils\userWallet\resolveWalletAgentPermanentId.test.ts:86:        await expect(resolveWalletAgentPermanentId('Legacy Agent')).resolves.toBeNull();
apps/agents-server/src/utils\userWallet\resolveWalletAgentPermanentId.ts:14:export async function resolveWalletAgentPermanentId(
apps/agents-server/src/utils\userWallet\resolveWalletAgentPermanentId.ts:45: * @private function of `resolveWalletAgentPermanentId`
apps/agents-server/src/utils\userWallet\updateUserWalletRecord.ts:6:import { resolveWalletAgentPermanentId } from './resolveWalletAgentPermanentId';
apps/agents-server/src/utils\userWallet\updateUserWalletRecord.ts:12:    const agentPermanentId = options.isGlobal ? null : await resolveWalletAgentPermanentId(options.agentPermanentId);
apps/agents-server/src/utils\userChat\recordUserChatMessageInChatHistory.ts:7:import { resolveAgentHash } from '@/src/utils/resolveAgentHash';
apps/agents-server/src/utils\userChat\recordUserChatMessageInChatHistory.ts:83:        const agentHash = await resolveAgentHash(agentPermanentId);
apps/agents-server/src/utils\userChat\runImmediateUserChatAnswer.ts:4:import { resolveCachedServerAgentContext } from '@/src/utils/cachedServerAgentRuntime';
apps/agents-server/src/utils\userChat\runImmediateUserChatAnswer.ts:6:import { appendMessageSuffix, resolveMessageSuffixFromAgentSource } from '@/src/utils/chat/messageSuffix';
apps/agents-server/src/utils\userChat\runImmediateUserChatAnswer.ts:34:    messageSuffix: ReturnType<typeof resolveMessageSuffixFromAgentSource>;
apps/agents-server/src/utils\userChat\runImmediateUserChatAnswer.ts:35:    resolvedAgentName: string;
apps/agents-server/src/utils\userChat\runImmediateUserChatAnswer.ts:43:    resolvedAgentName?: string;
apps/agents-server/src/utils\userChat\runImmediateUserChatAnswer.ts:73:            context: `Immediate agent chat ${context.resolvedAgentName}`,
apps/agents-server/src/utils\userChat\runImmediateUserChatAnswer.ts:123:    const resolvedAgentContext =
apps/agents-server/src/utils\userChat\runImmediateUserChatAnswer.ts:124:        options.agentSource && options.resolvedAgentName
apps/agents-server/src/utils\userChat\runImmediateUserChatAnswer.ts:126:                  resolvedAgentSource: options.agentSource,
apps/agents-server/src/utils\userChat\runImmediateUserChatAnswer.ts:127:                  resolvedAgentName: options.resolvedAgentName,
apps/agents-server/src/utils\userChat\runImmediateUserChatAnswer.ts:129:            : await resolveImmediateUserChatAgentContext(job.agentPermanentId);
apps/agents-server/src/utils\userChat\runImmediateUserChatAnswer.ts:130:    const agentSource = resolvedAgentContext.resolvedAgentSource;
apps/agents-server/src/utils\userChat\runImmediateUserChatAnswer.ts:134:            title: `Immediate chat with agent ${resolvedAgentContext.resolvedAgentName}`,
apps/agents-server/src/utils\userChat\runImmediateUserChatAnswer.ts:141:        messageSuffix: resolveMessageSuffixFromAgentSource(agentSource),
apps/agents-server/src/utils\userChat\runImmediateUserChatAnswer.ts:142:        resolvedAgentName: resolvedAgentContext.resolvedAgentName,
apps/agents-server/src/utils\userChat\runImmediateUserChatAnswer.ts:149:async function resolveImmediateUserChatAgentContext(agentPermanentId: string) {
apps/agents-server/src/utils\userChat\runImmediateUserChatAnswer.ts:156:    return resolveCachedServerAgentContext({
apps/agents-server/src/utils\userChat\runUserChatJob.ts:3:import { appendMessageSuffix, resolveMessageSuffixFromAgentSource } from '@/src/utils/chat/messageSuffix';
apps/agents-server/src/utils\userChat\runUserChatJob.ts:5:import { resolveAppendOnlySelfLearningAgentSource } from '@/src/utils/resolveAppendOnlySelfLearningAgentSource';
apps/agents-server/src/utils\userChat\runUserChatJob.ts:331:        context: `Durable agent chat ${options.executionContext.resolvedAgentName}`,
apps/agents-server/src/utils\userChat\runUserChatJob.ts:335:        resolveMessageSuffixFromAgentSource(options.executionContext.agentSource),
apps/agents-server/src/utils\userChat\runUserChatJob.ts:512:    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
apps/agents-server/src/utils\userChat\runUserChatJob.ts:513:        unresolvedAgentSourceBeforeLearning: executionContext.unresolvedAgentSource,
apps/agents-server/src/utils\userChat\runUserChatJob.ts:514:        resolvedAgentSourceBeforeLearning: executionContext.agentSource,
apps/agents-server/src/utils\userChat\runUserChatJob.ts:515:        resolvedAgentSourceAfterLearning: executionContext.agent.agentSource.value,
apps/agents-server/src/utils\userChat\userChatJobRunReport.ts:54:export function resolveUserChatJobExecutionTiming(report: AgentMessageRunReport | null): {
apps/agents-server/src/utils\userChat\userChatMessageLifecycle.test.ts:131:        const resolveInitialAgentMessage = jest.fn(() => 'Hello, I am ready to help.');
apps/agents-server/src/utils\userChat\userChatMessageLifecycle.test.ts:153:                resolveInitialAgentMessage,
apps/agents-server/src/utils\userChat\userChatMessageLifecycle.test.ts:165:        expect(resolveInitialAgentMessage).toHaveBeenCalledTimes(1);
apps/agents-server/src/utils\userChat\userChatMessageLifecycle.test.ts:169:        const resolveInitialAgentMessage = jest.fn(() => 'Hello, I am ready to help.');
apps/agents-server/src/utils\userChat\userChatMessageLifecycle.test.ts:198:                resolveInitialAgentMessage,
apps/agents-server/src/utils\userChat\userChatMessageLifecycle.test.ts:214:        expect(resolveInitialAgentMessage).not.toHaveBeenCalled();
apps/agents-server/src/utils\userChat\userChatMessageLifecycle.test.ts:245:            resolveInitialAgentMessage: () => null,
apps/agents-server/src/utils\userChat\userChatMessageLifecycle.ts:112:    resolveInitialAgentMessage: () => string | null | undefined;
apps/agents-server/src/utils\userChat\userChatMessageLifecycle.ts:124:            resolveInitialAgentMessage: options.resolveInitialAgentMessage,
apps/agents-server/src/utils\userChat\userChatMessageLifecycle.ts:157:    resolveInitialAgentMessage: () => string | null | undefined;
apps/agents-server/src/utils\userChat\userChatMessageLifecycle.ts:163:    const initialAgentMessage = options.resolveInitialAgentMessage();

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content AGENTS.md; Get-Content package.json; Get-Content jest.config.js; Get-Content tsconfig.json' in C:\Users\me\work\ai\promptbook
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n 'agentSource|agentReference|AgentCollection|agentFile|agentPath|Adam' scripts/run-codex-prompts src/cli/cli-commands/init* src/cli/cli-commands/coder/agentCliOptions.ts; rg --files src apps/agents-server/src/utils | rg '(Adam|[Aa]dam|[Ii]nherit|[Rr]esolve[Aa]gent|[Rr]eference[Rr]esolver|[Ff]ile[Aa]gent|[Dd]irectory[Aa]gent|[Bb]ookScoped)'" in C:\Users\me\work\ai\promptbook
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg --files apps/coder-landing agents/coding src/cli/cli-commands | rg '(init|coder-landing|agents/coding|agents\\\\coding)'; Get-Content changelog/_current-preversion.md -TotalCount 100; rg -n 'coder|agent|init' .vscode/terminals.json" in C:\Users\me\work\ai\promptbook
 succeeded in 1781ms:
src/cli/cli-commands\common\harness\HarnessDefinition.ts
src/cli/cli-commands\common\harness\HarnessDefinition.test.ts
src/cli/cli-commands\coder\initializeCoderProjectConfiguration.ts
src/cli/cli-commands\coder\init.ts
src/cli/cli-commands\coder\init.test.ts
src/cli/cli-commands\agents-server\initializeAgentsServerProjectConfiguration.ts
src/cli/cli-commands\agents-server\init.ts
src/cli/cli-commands\agents-server\init.test.ts
src/cli/cli-commands\agent-folder\initializeAgentRunnerCommand.ts
src/cli/cli-commands\agent-folder\initializeAgentProjectConfiguration.ts
src/cli/cli-commands\agent-folder\init.ts
agents/coding\unslopper.book
agents/coding\developer.book
apps/coder-landing\tsconfig.json
apps/coder-landing\tailwind.config.ts
apps/coder-landing\src\data\structuredData.ts
apps/coder-landing\src\data\siteMetadata.ts
apps/coder-landing\src\data\liveDemoScript.ts
apps/coder-landing\src\data\links.ts
apps/coder-landing\src\data\heroClaims.ts
apps/coder-landing\src\data\harnessCatalog.ts
apps/coder-landing\src\data\github.ts
apps/coder-landing\src\data\developerAgentBook.ts
apps/coder-landing\src\data\coreBenefits.ts
apps/coder-landing\src\data\comparison.ts
apps/coder-landing\src\data\commands.ts
apps/coder-landing\src\data\advancedFeatures.ts
apps/coder-landing\src\components\TerminalBlock\TerminalBlock.tsx
apps/coder-landing\src\components\TerminalBlock\highlightShellCommand.tsx
apps/coder-landing\src\components\StructuredData\StructuredData.tsx
apps/coder-landing\src\components\SocialShareImage\renderSocialShareImage.tsx
apps/coder-landing\src\components\sections\Quickstart.tsx
apps/coder-landing\src\components\sections\HowItWorks.tsx
apps/coder-landing\src\components\sections\Hero.tsx
apps/coder-landing\src\components\sections\Header.tsx
apps/coder-landing\src\components\sections\HarnessesSection.tsx
apps/coder-landing\src\components\sections\Footer.tsx
apps/coder-landing\src\components\sections\FeaturesSection.tsx
apps/coder-landing\src\components\sections\ComparisonSection.tsx
apps/coder-landing\src\components\sections\AgentsSection.tsx
apps/coder-landing\src\components\LiveTerminalDemo\useFittedTerminalMetrics.ts
apps/coder-landing\src\components\LiveTerminalDemo\TerminalAnsiTextLine.tsx
apps/coder-landing\src\components\LiveTerminalDemo\SharedAgentTerminalVisual.tsx
apps/coder-landing\src\components\LiveTerminalDemo\LiveTerminalDemo.tsx
apps/coder-landing\src\components\HeroClaimRotator\HeroClaimRotator.tsx
apps/coder-landing\src\components\HarnessLogo\HarnessLogo.tsx
apps/coder-landing\src\components\GitHubStarCallToAction\GitHubStarCallToAction.tsx
apps/coder-landing\src\components\DeveloperAgentBookPreview\DeveloperAgentBookPreview.tsx
apps/coder-landing\src\components\CoreBenefitGraphic\CoreBenefitGraphic.tsx
apps/coder-landing\src\components\ComparisonTable\resolveComparisonRowCellGroups.ts
apps/coder-landing\src\components\ComparisonTable\ComparisonTable.tsx
apps/coder-landing\src\components\ComparisonTable\ComparisonSupportMark.tsx
apps/coder-landing\src\components\ComparisonTable\ComparisonSolutionLogo.tsx
apps/coder-landing\src\app\twitter-image.tsx
apps/coder-landing\src\app\sitemap.ts
apps/coder-landing\src\app\robots.ts
apps/coder-landing\src\app\page.tsx
apps/coder-landing\src\app\opengraph-image.tsx
apps/coder-landing\src\app\layout.tsx
apps/coder-landing\src\app\globals.css
apps/coder-landing\specs\sections\what-is-ptbk-coder.md
apps/coder-landing\specs\sections\quickstart.md
apps/coder-landing\specs\sections\hero.md
apps/coder-landing\specs\sections\header.md
apps/coder-landing\specs\sections\harnesses.md
apps/coder-landing\specs\sections\footer.md
apps/coder-landing\specs\sections\comparison.md
apps/coder-landing\specs\sections\agent-book.md
apps/coder-landing\specs\sections\advanced-features.md
apps/coder-landing\specs\README.md
apps/coder-landing\specs\product.md
apps/coder-landing\specs\page-structure.md
apps/coder-landing\specs\non-functional.md
apps/coder-landing\specs\metadata.md
apps/coder-landing\specs\design.md
apps/coder-landing\specs\content\harness-catalog.md
apps/coder-landing\specs\content\developer-agent.md
apps/coder-landing\specs\content\comparison-matrix.md
apps/coder-landing\specs\content\commands.md
apps/coder-landing\specs\components\terminal-block.md
apps/coder-landing\specs\components\live-terminal.md
apps/coder-landing\specs\components\github-star-call-to-action.md
apps/coder-landing\specs\components\book-editor-embed.md
apps/coder-landing\README.md
apps/coder-landing\public\logo-white-transparent-1024.png
apps/coder-landing\public\logo-blue-white-256.png
apps/coder-landing\public\favicon.ico
apps/coder-landing\postcss.config.mjs
apps/coder-landing\package.json
apps/coder-landing\next.config.ts
-   Fixed the React 19 peer dependency warnings during `npm install` by replacing `swagger-ui-react` with the
    standalone `swagger-ui-dist` bundle, shared with the Express API docs. The Swagger page still loads on demand
    and keeps its API-key panel, saved authorization and display settings.
    Test builds now skip Sentry source-map uploads to avoid temporary upload bundles and Windows cleanup failures.
    CLI help tests use the same command registration as the executable without starting a new process for every
    subcommand. CLI startup and version checks still run in child processes, with a timeout that stops stuck children.

-   A generated system message no longer repeats the same instructions. Commitments are applied one by one, so a
    commitment used several times in one book emitted its whole section every time, including the shared guidance
    paragraph which introduces it. Two `WRITING RULES` blocks produced two `## Writing rules` headings each preceded by
    the same four-line explanation of what a writing rule is, two `USE PRIVACY` blocks produced the `## Privacy`
    instructions twice, and the same held for `WRITING SAMPLE`, `SAMPLE`, `LANGUAGE`, `USE POPUP`,
    `USE IMAGE GENERATOR`, `USE USER LOCATION` and `TEAM`. The final system message is now deduplicated once for all
    commitments instead of being fixed in each of them separately:

    -   All sections sharing one `## Title` are merged into the position of their first occurrence, so each heading
        appears exactly once while the content of every commitment keeps its original order inside that section.
    -   Every identical block is kept only once, so the shared guidance paragraph of a repeated commitment is stated
        once and a repeated instruction without its own heading, such as `STYLE Formal.` written twice, is no longer
        duplicated.
    -   Two lists joined by a merge become one compact list, while everything which was not merged keeps its original
        spacing, and fenced code blocks - imported files and inline `KNOWLEDGE` content among them - are never split
        or deduplicated.
    -   A book which uses each of its commitments once produces the same system message as before.

-   `ptbk coder` now tracks the agent of a run next to its harness and model. Two runs of the same harness, model and
    thinking level are not the same run when one of them carries an `--agent`, so the run report of every prompt now
    names the agent which implemented it:

    -   A prompt status line signs its work with the agent in front of the harness, as
        `` [x] by Developer on OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.13 23 minutes ``.
        The `[^]`, `[x]` and `[!]` status lines and the run trace of the round all report it, and a prompt continued
        through `--git-changes continue` keeps the agent in each of its chronological `interrupted, continued by …`
        reports.
    -   The reported name is the display name of the Book — its `META FULLNAME`, or the plain title on its first line
        — so `agents/coding/developer.book` signs as its Book title and not as its normalized identifier.
    -   A run started without `--agent` reports exactly what it reported before.

-   Fixed `MockedChat` typing animations blinking for messages without ids by assigning stable playback-only ids before rendering each simulated transcript.

-   Fixed the rich `ptbk coder run` dashboard so an animated agent avatar can no longer overwrite its own UI in an
    exact-width terminal pane:

    -   The shared frame layout now leaves one physical terminal column unused, preventing boxed rows from
        auto-wrapping and keeping incremental cursor-based redraws aligned with their logical frame rows.
    -   Added a focused VS Code terminal preset and regression coverage for the terminal-width invariant.

-   Added agent-specific prompt routing to `ptbk coder`:

    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
        filename without `.book`, or the Book title from its first line. For example, a run using
        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
        `developer`, and `Promptbook Developer` routing tokens.
    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
        <path.book>` previews the same agent-specific queue without starting a harness.
    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
        documented agent-specific routing on the `ptbk coder` landing page.

-   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
    `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
    pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
    own `Deprecated:` paragraph, so the listing which is supposed to introduce the CLI was mostly made of commands
    nobody should start using. `ptbk --help`, and the same listing which plain `ptbk` prints when it asks for a
    subcommand, now offers only the commands which are meant to be used - `coder`, `agent`, `agent-folder`,
    `agents-server`, `about` and `hello`. Nothing was removed: every deprecated command still runs, still warns that it
    is deprecated when it is used, still explains the deprecation in its own help - `ptbk run --help` - and is still
    reachable through `ptbk help run`. Only the "did you mean" hint for a mistyped command stops pointing at them.
    Being left out of the help is now simply a part of what deprecating a command means, written once in
    `$deprecateCliCommand`, so any command deprecated later disappears from the help on its own.

-   `ptbk` without a subcommand no longer falls back to the deprecated `ptbk run`. Until now `run` was registered as
    the default command of the CLI, so everything which did not name a command - `ptbk ./write-cv.book`, a mistyped
    `ptbk codr` or a stray option - was quietly handed over to the pipeline runner of the old system, which was the
    last thing a deprecated command should be doing. Plain `ptbk` now asks to specify a subcommand and prints the
    top-level help, exactly like `ptbk coder`, `ptbk agent`, `ptbk agent-folder` and `ptbk agents-server` already did
    when they were called without one of their own subcommands, and that one shared behavior is now written once
    instead of five times. Anything which is not a command is refused with `unknown command` and a "did you mean"
    hint instead of being executed as a pipeline, so a script which still passes a book path to `ptbk` fails loudly
    rather than appearing to succeed. `ptbk run` itself is untouched and keeps working for everyone who asks for it
    explicitly. The command list in the help now starts with `coder`, followed by `agent`, `agent-folder` and
    `agents-server`, and the deprecated commands of the old pipeline system are listed after them.

-   `ptbk coder` now saves the run trace of every prompt round, so a finished run can still be analyzed afterwards.
    Until now the only durable record of a round was the one status line written into the prompt file: the live
    runtime log holding everything the harness and the verification command had written was a temporary artifact,
    deleted as soon as a successful round was over unless `--preserve-logs` was passed, and lost together with the
    reasoning, the tool calls and the diffs of the run. Each round now writes `prompts/traces/<prompt-file-name>.md`
    - a trace named exactly like the prompt file it belongs to, with the same `-2` section suffix the temporary
    scripts of a multi-section prompt file already use. The trace pairs the metadata of the round - which prompt and
    section it implemented, whether it succeeded or failed, the harness, model, thinking level and login method which
    ran it, how many coding attempts it took, what each step cost and how long each one ran, the verification
    command, the start, end and duration, and the error which ended a failed round - with the untouched runtime log
    of the harness. Because the trace is written before the round is committed, it lands in the very same commit as
    the prompt it describes, and a task whose isolated worktree cannot be merged back carries its trace into the
    failure commit instead. A failed round is traced too, next to the `.error.log` it already wrote. Re-running the
    same prompt overwrites its trace and leaves the earlier ones in the git history.

-   `ptbk coder run` and `ptbk coder server` no longer walk into a full disk. A coding round writes prompt and log
    artifacts, lets the harness rewrite the project, may install npm packages, may create a git worktree for
    `--isolate` and usually runs a build, so a run started without enough space used to die somewhere in the middle,
18:                "npx ts-node ./src/cli/test/ptbk.ts coder ping --no-questions --harness claude-code --model claude-sonnet-5 --thinking-level low",
19:                "npx ts-node ./src/cli/test/ptbk.ts coder ping --no-questions --harness openai-codex --model gpt-5.6-luna --thinking-level low"
26:            "name": "🐙🧪 Test ptbk coder terminal UI (💤)",
33:            "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
37:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness openai-codex --model gpt-5.5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix --auto-pull --auto-push"
44:            "name": "🐙🚀 Run ptbk coder `gpt-5.6-sol` Coding Agent (💤)(💸)(⏳)",
48:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness openai-codex --model gpt-5.6-sol --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
55:            "name": "🐙🚀 Run ptbk coder `gpt-5.6-terra` Coding Agent (💤)(💸)(⏳)",
59:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness openai-codex --model gpt-5.6-terra --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push --min-priority 1"
66:            "name": "🐙🚀 Run ptbk coder `gpt-5.6-luna` Coding Agent (💤)(💸)(⏳)",
70:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness openai-codex --model gpt-5.6-luna --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push --max-priority 2"
77:            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Coding Agent (💤)(💸)(⏳)",
81:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
88:            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
92:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
99:            "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
100:            "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
106:            "name": "🐙🚀 Run ptbk coder `claude-fable` Coding Agent (💤)(💸)(⏳)",
107:            "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness claude-code --model fable --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --min-priority 3 --test-before yes-and-fix  --auto-pull --auto-push",
113:            "name": "🐙🚀 Run ptbk coder `qwen3.8-max` Coding Agent (💤)(💸)(⏳)",
117:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness qwen-code --model qwen3.8-max --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
124:            "name": "🐙🚀 Run ptbk coder `qwen3.8-flash` Coding Agent (💤)(💸)(⏳)",
128:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness qwen-code --model qwen3.8-flash --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
135:            "name": "🐙🚀 Run ptbk coder `deepseek-v4-pro` Coding Agent (💤)(💸)(⏳)",
139:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness qwen-code --model deepseek-v4-pro-0813 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
146:            "name": "🐙🚀 Run ptbk coder `deepseek-v4-flash` Coding Agent (💤)(💸)(⏳)",
150:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness qwen-code --model deepseek-v4-flash-0731 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
157:            "name": "🐙🚀 Run ptbk coder `glm-5.2` Coding Agent (💤)(💸)(⏳)",
161:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-questions --harness qwen-code --model glm-5.2 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
169:            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 10 --template prompts/_templates/common.md --commit --auto-pull --auto-push",
176:            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 5 --template prompts/_templates/agents-server.md --commit --auto-pull --auto-push",
183:            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 5 --template prompts/_templates/utils-app.md --commit --auto-pull --auto-push",
189:            "name": "🐙🏭 Generate \"ptbk coder\" prompt boilerplates for Coding Agent (💤)",
190:            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 5 --template prompts/_templates/ptbk-coder.md --commit --auto-pull --auto-push",
196:            "name": "🐙🏭 Generate \"ptbk agent-folder\" prompt boilerplates for Coding Agent",
197:            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 5 --template prompts/_templates/ptbk-agent.md --commit --auto-pull --auto-push",
203:            "name": "🐙🏭 Generate \"ptbk agents-server\" prompt boilerplates for Coding Agent",
204:            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 5 --template prompts/_templates/ptbk-agents-server.md --commit --auto-pull --auto-push",
211:            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 1 --template prompts/_templates/test.md --commit --auto-pull --auto-push",
218:            "command": "npx ts-node ./src/cli/test/ptbk.ts coder find-refactor-candidates",
225:            "command": "npx ts-node ./src/cli/test/ptbk.ts coder find-unwritten",
232:            "command": "npx ts-node ./src/cli/test/ptbk.ts coder list",
239:            "command": "npx ts-node ./src/cli/test/ptbk.ts coder verify --order from-latest --ignore Refactor --commit --auto-pull --auto-push",
259:            "name": "✨👨‍💻 Run ptbk agents-server dev",
262:                "npx ts-node ./src/cli/test/ptbk.ts agents-server dev --harness github-copilot --model gpt-5.4 --thinking-level max --port 4440"
269:            "name": "✨👨‍💻 Run ptbk agents-server start",
272:                "npx ts-node ./src/cli/test/ptbk.ts agents-server dev --harness github-copilot --model gpt-5.4 --thinking-level max --port 4440"
281:            "cwd": "./apps/agents-server/",
289:            "cwd": "./apps/agents-server/",
297:            "cwd": "./apps/agents-server/",
305:            "cwd": "./apps/agents-server/",
320:            "command": "git branch --force preview main && git push origin preview --force-with-lease && (cd ./apps/agents-server/ && npx tsx ./src/database/migrate.ts --only=preview)",
327:            "command": "git branch --force production preview && git push origin production --force-with-lease && (cd ./apps/agents-server/ && npx tsx ./src/database/migrate.ts --only=production)",
334:            "command": "git branch --force lts production && git push origin lts --force-with-lease && (cd ./apps/agents-server/ && npx tsx ./src/database/migrate.ts --only=lts)",
378:            "name": "(old) ✨👨‍💻🟣 Run deprecated ptbk start-agents-server",
381:                "npx ts-node ./src/cli/test/ptbk.ts start-agents-server ./agents/default --port 4440 --provider BRING_YOUR_OWN_KEYS"
473:            "name": "🧸🔠 Run ptbk coder landing page",
474:            "cwd": "./apps/coder-landing/",
506:            "command": "npm run test-app-agents-server",
513:            "cwd": "./apps/agents-server/",
521:            "cwd": "./apps/agents-server/",
599:            "command": "npx ts-node ./src/cli/test/ptbk.ts coder find-fresh-emoji-tags",
633:            "cwd": "./apps/agents-server/",
740:                "npx ts-node ./src/playground/permanent/agent-with-browser-playground.ts"
803:                "npx ts-node ./src/llm-providers/agent/playground/playground.ts"

 succeeded in 1867ms:
rg: src/cli/cli-commands/init*: The filename, directory name, or volume label syntax is incorrect. (os error 123)
scripts/run-codex-prompts\main\runCodexPrompts.ts:128:            agentReferences: resolvedCoderAgent?.agentReferences,
scripts/run-codex-prompts\main\runCodexPrompts.ts:133:        await initializeRunUiAgentVisual(uiHandle, resolvedCoderAgent?.agentSource);
scripts/run-codex-prompts\main\runCodexPrompts.ts:767:    agentSource: string_book | undefined,
scripts/run-codex-prompts\main\runCodexPrompts.ts:769:    if (!uiHandle || !agentSource) {
scripts/run-codex-prompts\main\runCodexPrompts.ts:773:    const agentVisual = await buildCoderRunAgentVisual(agentSource);
scripts/run-codex-prompts\ui\buildCoderRunAgentVisual.ts:52: * @param agentSource Source of the `--agent` book file.
scripts/run-codex-prompts\ui\buildCoderRunAgentVisual.ts:55:export async function buildCoderRunAgentVisual(agentSource: string_book): Promise<CoderRunAgentVisual | null> {
scripts/run-codex-prompts\ui\buildCoderRunAgentVisual.ts:58:            agentSource,
scripts/run-codex-prompts\common\resolveCoderAgent.ts:16:    readonly agentSource: string_book;
scripts/run-codex-prompts\common\resolveCoderAgent.ts:32:    readonly agentReferences: ReadonlyArray<string>;
scripts/run-codex-prompts\common\resolveCoderAgent.ts:61:    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
scripts/run-codex-prompts\common\resolveCoderAgent.ts:75:    const parsedAgentSource = parseAgentSource(agentSource);
scripts/run-codex-prompts\common\resolveCoderAgent.ts:78:        agentSource,
scripts/run-codex-prompts\common\resolveCoderAgent.ts:80:        agentReferences: createCoderAgentReferences({
scripts/run-codex-prompts\common\resolveCoderAgent.ts:106:        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
scripts/run-codex-prompts\common\resolveCoderAgent.ts:136:            ].filter((agentReference) => agentReference.trim() !== ''),
scripts/run-codex-prompts\common\resolveCoderAgent.test.ts:32:        expect(resolvedAgentBook?.agentReferences).toEqual(
scripts/run-codex-prompts\prompts\isPromptCompatibleWithRunner.ts:20:    readonly agentReferences?: ReadonlyArray<string>;
scripts/run-codex-prompts\prompts\isPromptCompatibleWithRunner.ts:50:        ...(promptRunnerIdentity.agentReferences ?? []),
scripts/run-codex-prompts\prompts\promptRunnerFiltering.test.ts:116:                agentReferences: [
apps/agents-server/src/utils\resolveInheritedAgentSource.ts
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts
apps/agents-server/src/utils\resolveAgentStateFromSource.ts
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts
apps/agents-server/src/utils\resolveAgentProfileWithInheritance.ts
apps/agents-server/src/utils\resolveAgentHash.ts
apps/agents-server/src/utils\chat\resolveAgentsServerCitationLabel.ts
apps/agents-server/src/utils\cache\AgentKitCacheManager\resolveAgentKitModelRequirements.ts
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.ts
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.test.ts
apps/agents-server/src/utils\agentReferenceResolver\resolveTeamCapabilitiesFromAgentSource.ts
apps/agents-server/src/utils\agentReferenceResolver\resolveTeamCapabilitiesFromAgentSource.test.ts
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts
apps/agents-server/src/utils\agentReferenceResolver\MissingCoreAgentRecovery.ts
apps/agents-server/src/utils\agentReferenceResolver\extractAgentReferenceTokens.ts
apps/agents-server/src/utils\agentReferenceResolver\createUnresolvedAgentReferenceDiagnostics.ts
apps/agents-server/src/utils\agentReferenceResolver\createUnresolvedAgentReferenceDiagnostics.test.ts
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.ts
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.test.ts
apps/agents-server/src/utils\agentReferenceResolver\createAgentNameCollisionDiagnostics.ts
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.test.ts
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.ts
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.test.ts
apps/agents-server/src/utils\agentReferenceResolver\AgentReferenceResolutionIssue.ts
apps/agents-server/src/utils\agentReferenceResolver\$provideAgentReferenceResolver.ts
apps/agents-server/src/utils\agentReferenceResolver\$provideAgentReferenceResolver.test.ts
apps/agents-server/src/utils\agentProjects\resolveAgentProjectRuntimeStatus.ts
apps/agents-server/src/utils\agentProjects\resolveAgentProjectRuntimeEnvironmentFlag.ts
apps/agents-server/src/utils\agentProjects\resolveAgentProjectPublicUrls.ts
apps/agents-server/src/utils\agentProjects\resolveAgentProjectProfile.ts
apps/agents-server/src/utils\agentProjects\resolveAgentProjectProfile.test.ts
apps/agents-server/src/utils\agentProjects\resolveAgentProjectInfo.ts
apps/agents-server/src/utils\agentProjects\resolveAgentProjectInfo.test.ts
apps/agents-server/src/utils\agentProjects\resolveAgentProjectFileContentType.ts
apps/agents-server/src/utils\agentProjects\resolveAgentProjectFaviconRelativePath.ts
apps/agents-server/src/utils\agentProjects\resolveAgentProjectFaviconRelativePath.test.ts
apps/agents-server/src/utils\agentProjects\resolveAgentChatProjectReferences.ts
apps/agents-server/src/utils\agentGoalChat\resolveAgentGoalChatOwnerUserId.ts
apps/agents-server/src/utils\agentBrowserProfile\resolveAgentBrowserProfileDirectory.ts
apps/agents-server/src/utils\agentBrowserProfile\resolveAgentBrowserProfileDirectory.test.ts
src\utils\agents\resolveAgentAvatarImageUrl.ts
src\utils\agents\resolveAgentAvatarImageUrl.test.ts
src\utils\agent-message-runtime\resolveAgentMessageTouchedProjectNames.ts
src\utils\agent-message-runtime\resolveAgentMessageTouchedProjectNames.test.ts
src\utils\agent-message-runtime\resolveAgentMessageTouchedExternalSources.ts
src\utils\agent-message-runtime\resolveAgentMessageTouchedExternalSources.test.ts
src\utils\agent-message-runtime\resolveAgentMessageRuntimeActivity.ts
src\utils\agent-message-runtime\resolveAgentMessageRuntimeActivity.test.ts
src\cli\cli-commands\agents-server\startAgentsServer\resolveAgentsServerChildHostname.ts
src\cli\cli-commands\agents-server\buildAgentsServer\resolveAgentsServerBuildOutputPath.ts
src\cli\cli-commands\agents-server\buildAgentsServer\resolveAgentsServerBuildAppPath.ts
src\cli\cli-commands\agents-server\buildAgentsServer\resolveAgentsServerAppPath.ts
src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts
src\book-2.0\agent-source\AgentReferenceResolver.ts

 succeeded in 2103ms:
## How to Contribute

-   **Add changes in [Changelog](/changelog/_current-preversion.md)**
-   **Test your changes** to ensure they work as expected, use `npm test`

## Code Style

-   Follow the existing code style and structure.
-   Use comments to explain complex logic.

## Emoji in `[brackets]`

-   `[any emoji]` Connects multiple places that are related to each other across the repository
-   `[number]` Connects multiple places that are related to each other across the file
-   `[🧠]` Marks a place where there is something to decide and think about.
-   `[🕕]` List of models _(that should be progresively updated)_
    -   Prompt: Update available models and their prices, search online
-   `[🔼]` Marks an entity (function, class, type,...) in other project (like Promptbook.studio) which should be moved to this repository
-   `[🚉]` Marks an types / interfaces / structures fully serializable as JSON, not marking `string_` and `number_` prefixed aliases
-   `[🧹]` Need to implement garbage collection
-   `[🐣]` Easter eggs
-   `[💩]` Shitty code that needs refactoring
-   `$` When entity (function, class) starts by `$`, it means it is not pure and can have side effects.
-   3x `!` Marks a place that needs to be fixed before releasing a pre-release version.
-   4x (and more) `!` Marks a place that needs to be fixed as soon as possible.
-   `@@@` Marks a place where text / documentation / ... must be written.
-   [⚫] Code in this file should never be published in any package
-   [🟢] Code in this file should never be published into packages that could be imported into browser environment
-   [🔵] Code in this file should never be published outside of `@promptbook/browser`
-   [🟡] Code in this file should never be published outside of `@promptbook/cli`
-   [💞] Ignore a discrepancy between file name and entity name

## Generated code

Across the repository, there are several places where code has been generated automatically.
Do not edit these places manually, as they will be overwritten by the code generation process. Edit the source files instead.

> ⚠️ WARNING: This code has been generated so that any manual changes will be overwritten

## Dictionary

-   **Promptbook Engine vs. Agent Server** sre two distinct parts you should know the difference:
    -   **Promptbook Engine** is the core engine that powers AI agents, it is located in `/src` folder. It is framework-agnostic and can be used in different applications. It can run as standalone javascript/typescript library, CLI environment, or be integrated into different applications. It can be also runned in Agent Server:
    -   **Agent Server** is a specific application that uses the Promptbook Engine to provide a web and API interface for creating and interacting with AI agents. It is located in `/apps/agents-server` folder.
-   **Commitments** are basic syntax elements that add specific functionalities to AI agents
    -   They are used in `agentSource`, there are commitments like `PERSONA`, `RULE`, `KNOWLEDGE`, `USE CALENDAR`, `USE PROJECT`, `META IMAGE`, etc.
    -   Commitments are in the folder `/src/commitments`
    -   Each commitment starts with a keyword, e.g., `KNOWLEDGE`, `USE PROJECT`, etc. on a begining of the line and end by new co
    -   Agent source with commitments is parsed by two functions:
        -   `parseAgentSource` which is a lightweight parser for agent source, it parses basic information and its purpose is to be quick and synchronous. The commitments there are hardcoded.
        -   `createAgentModelRequirements` which is an asynchronous function that creates model requirements it applies each commitment one by one and works asynchronously.

## Common rules

-   Always analyze the context and requirements before generating any code.
-   Write clear, maintainable, and well-documented code.
-   Write JSDoc comments for all entities - functions, classes, types, top-level constants, etc.
    -   When this entity is exported from the file and it is under `src` folder _(not for example in the `apps` folder)_, it must be marked either as `@public` or `@private` at the end of the JSDoc comment.
    -   For example: "@private internal utility of <Chat/>" / "@public exported from `@promptbook/browser`"
    -   If you don't know, prefer to mark it as private, we can always change it to public later, but changing from public to private may cause breaking changes.
-   You (the AI coding agent) are running inside a Node process, so do not kill all Node processes such as `taskkill /F /IM node.exe`. If you need to stop something you spawned, kill only that specific process, for example by PID or by port.

## Additional context

-   Look at attached images if there are present
-   Attached images are in folder `prompts` on the root of the project
    -   For example the ![alt text](screenshots/foo.png) is image file `prompts/screenshots/foo.png`

### Agents server installed on VPS _(as additional context)_

-   There are two distinct things:
    1. Entire VPS
    2. Each server
-   On one VPS there can be multiple servers and each server has its own domain
-   Most things (like agents, projects, metadata, etc.) are living on each server and some things (like environment variables, superadmin, etc.) are bound to entire VPS
-   Purpose of this is to be able to run multiple clients on one VPS and each client can have its own server with its own domain and settings
-   The entire VPS is managed by superadmin and each server is managed by normal admin of that server _(or superadmin can also manage each server)_
-   Superadmin has always name `admin` and password hardcoded in environment variable `ADMIN_PASSWORD`
-   The VPS is installed by running [the `install.sh`](install.sh) script on a fresh VPS
-   Servers can be added and removed by superadmin on `/superadmin/servers` page

### The Agents Server menu _(as additional context)_

The menu of the agent server looks like this:

1. The navigation hierarchy
    - Icon and Server name _(for example Promptbook Agents Server)_
    - arrow ">" and Agents or picked agent name (organized in folders)
    - arrow ">" and the view Profile / Chat / Book of the agent or nothing if no agent is picked
2. The menu items
    - Documentation
    - System
3. Control panel and user menu
    - Control panel
    - User menu with the avatar and the name of the user

### Database migrations for Agents server _(as additional context)_

-   Migrations are located in `apps/agents-server/src/database/migrations`
-   Be aware that table names in migrations have prefix `prefix_` _(look at existing migrations for reference)_
-   Migrations should be backwards compatible:
    -   Meaning that earlier versions of the server should be able to run with the database after migration without any issues.
    -   This is important because we want to have the same database for production and preview environments.
    -   This database will be migrated to the latest version of the preview environment, but the production environment should be able to run with it without any issues.
    -   The only thing that can happen is that older versions of the server will not be able to use new features.
    -   Adding new columns, tables, etc. is fine, but do not remove or rename existing ones, and do not change the meaning of existing columns or tables.
    -   When in doubt, prefer to add new things instead of changing existing ones.

### Metadata of Agents server _(as additional context)_

-   There is a table called `Metadata`.
-   It has `key` and `value` fields.
-   It is a similar concept to configuration, but this configuration can be changed by administrators in the Agents Server website.

### Book Language blueprint _(as additional context)_

Book language is a domain-specific language used for defining AI agents in the Promptbook Engine and Agents server.
It has lightweight syntax and keywords (the commitments) that allow you to define the "soul" of the agent.
The book language is designed to be human-readable and easy to write, while also being powerful enough to express complex agent behaviors.

Every agent has its source defined in the book language, which is called "agent source". The agent source is parsed and processed by the Promptbook Engine to create the actual AI agent that can interact with users and perform tasks.
This agent source is internally converted to a structured format called "agent model requirements" which are the actual raw technical instructions for the AI model to run the agent.

There is a standalone book language documentation on each agent server on `/api/docs/book.md` route, for example `https://live.ptbk.io/api/docs/book-language.md?language=en`.
Use it as a reference for the syntax and semantics of the book language, and modify `apps/agents-server/src/utils/bookLanguageDocumentation/createStandaloneBookLanguageMarkdown.ts` if it is relevant to the change you are doing.

#### Commitments _(as additional context and part of Book Language)_

Commitments are basic syntax elements that add specific functionalities to AI agents written in `book` language.

-   They are used in `agentSource`, there are commitments like `PERSONA`, `RULE`, `KNOWLEDGE`, `USE CALENDAR`, `USE PROJECT`, `META IMAGE`, `CLOSED`, etc.
-   They are in the folder `src/commitments`.
-   Each commitment starts with a keyword, e.g. `PERSONA`, `KNOWLEDGE`, `USE PROJECT`, etc. on a beginning of the line and ends by a new commitment or the end of the book.
-   There is a general pattern that the commitment keyword is followed by a space and then by the content of the commitment, for example:
    -   `PERSONA You are a helpful assistant that helps with cooking recipes.`
    -   `USE MCP https://mcp.example.com/server`
-   In the commitment context, you can reference external agents, for example:
    -   `TEAM You can talk to {Criminal lawyer} and {Financial advisor}`
{
    "name": "promptbook",
    "version": "0.114.0-38",
    "description": "Promptbook: Create persistent AI agents that turn your company's scattered knowledge into action",
    "--note-0": "    <- [🐊]",
    "private": false,
    "sideEffects": false,
    "repository": {
        "type": "git",
        "url": "git+https://github.com/webgptorg/promptbook.git"
    },
    "author": "Pavol Hejný <pavol@ptbk.io> (https://www.pavolhejny.com/)",
    "contributors": [
        "Pavol Hejný <pavol@ptbk.io> (https://www.pavolhejny.com/)",
        "Jiří Jahn <jiri@ptbk.io> (https://www.ptbk.io/)"
    ],
    "--note-1": "[❇️]",
    "keywords": [
        "ai",
        "llm",
        "prompt",
        "template",
        "language-model",
        "chatgpt",
        "autogpt",
        "machine-learning",
        "natural-language-processing",
        "nlp",
        "openai",
        "o3",
        "o3-mini",
        "deepseek",
        "gpt-3",
        "gpt-4",
        "gpt-4o",
        "gpt-4o-mini",
        "o1",
        "o1-mini",
        "o1-preview",
        "anthropic",
        "claude",
        "claude-3",
        "claude-3-opus",
        "claude-3-sonnet",
        "claude-3-haiku",
        "gemini",
        "gemini-pro",
        "gemini-flash",
        "mixtral",
        "mistral",
        "ollama",
        "ai-orchestration",
        "prompt-engineering",
        "llmops",
        "multimodal",
        "reasoning",
        "rag",
        "embeddings",
        "function-calling",
        "large-language-models",
        "ai-application-framework",
        "text-generation",
        "ai-agents",
        "LLMOps",
        "book-language",
        "markdown-dsl",
        "ai-workflow",
        "ai-automation",
        "pipeline",
        "workflow",
        "orchestration",
        "ai-pipeline",
        "prompt-template",
        "prompt-chaining",
        "ai-scripting",
        "conversational-ai",
        "chatbot",
        "ai-assistant",
        "knowledge-base",
        "document-processing",
        "web-scraping",
        "pdf-processing",
        "csv-processing",
        "json-processing",
        "xml-processing",
        "markdown-processing",
        "wikipedia-scraping",
        "website-crawler",
        "azure-openai",
        "vercel-ai",
        "docker",
        "cli-tool",
        "typescript",
        "javascript",
        "nodejs",
        "browser",
        "cross-platform",
        "api-integration",
        "remote-execution",
        "caching",
        "rate-limiting",
        "error-handling",
        "validation",
        "testing",
        "mocking",
        "personas",
        "expectations",
        "postprocessing",
        "preprocessing",
        "templating",
        "parameterization",
        "configuration",
        "logging",
        "monitoring",
        "debugging",
        "vscode-extension",
        "syntax-highlighting",
        "intellisense",
        "developer-tools",
        "ai-development",
        "prompt-management",
        "model-agnostic",
        "multi-model",
        "ai-sdk",
        "ai-framework",
        "ai-platform",
        "generative-ai",
        "content-generation",
        "text-processing",
        "natural-language",
        "human-readable",
        "plain-english",
        "no-code",
        "low-code",
        "automation-framework",
        "workflow-engine",
        "task-automation",
        "ai-ops",
        "mlops",
        "devops",
        "ci-cd",
        "version-control",
        "git-friendly",
        "collaboration",
        "team-development",
        "enterprise",
        "scalable",
        "production-ready",
        "open-source",
        "community",
        "extensible",
        "plugin-system",
        "integration",
        "api-wrapper",
        "sdk-wrapper",
        "abstraction-layer",
        "unified-interface",
        "cross-provider",
        "vendor-agnostic",
        "future-proof",
        "paradigm-shift",
        "next-generation",
        "revolutionary",
        "innovative",
        "cutting-edge",
        "state-of-the-art"
    ],
    "license": "BUSL-1.1",
    "bugs": {
        "url": "https://github.com/webgptorg/promptbook/issues"
    },
    "homepage": "https://ptbk.io/",
    "funding": [
        {
            "type": "individual",
            "url": "https://buymeacoffee.com/hejny"
        },
        {
            "type": "github",
            "url": "https://github.com/webgptorg/promptbook/blob/main/README.md#%EF%B8%8F-contributing"
        }
    ],
    "scripts": {
        "start": "npx ts-node ./src/cli/test/ptbk.ts agents-server start --harness github-copilot --model gpt-5.4 --thinking-level high --port 4440",
        "test": "echo \"Note: Running from most lightweight to heaviest tests\" && npm run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm run test-books && npm run test-book-components-build && npm run test-package-generation && npm run test-unit && npm run test-app-agents-server && echo \"🎉 All tests passed!\"",
        "test-without-package-generation": "echo \"Note: Running from most lightweight to heaviest tests\" && npm run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm run test-books && npm run test-book-components-build && npm run test-unit",
        "test-without-package-generation-and-unit": "echo \"Note: Running from most lightweight to heaviest tests\" && npm run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm run test-books && npm run test-book-components-build",
        "test-for-ptbk-coder": "node ./scripts/test-for-ptbk-coder.js",
        "test-unit": "node -r ./scripts/ignore-kill-eperm.js ./node_modules/jest/bin/jest.js --forceExit",
        "_TODO-0": "           <- TODO: Remove `--forceExit` flag, now its present to ensure return code 0 when all tests ",
        "test-types": "tsc",
        "test-lint": "eslint src",
        "test-spellcheck": "bash ./scripts/spellcheck.sh",
        "test-books": "echo \"Note: [⚡️] Skipping the book test because it tests just the old pipeline books which are deprecated\"",
        "_test-books": "ts-node ./src/cli/test/ptbk.ts test --provider BRING_YOUR_OWN_KEYS --no-prepare \"./**/*.book\" --ignore \"./book/**/*,./examples/pipelines/errors/**/*\" --verbose",
        "test-package-generation": "ts-node ./scripts/generate-packages/generate-packages.ts --skip-bundler",
        "test-name-discrepancies": "ts-node ./scripts/find-name-discrepancies/find-name-discrepancies.ts",
        "test-book-components-build": "echo \"Note: Skipping book components build tests because of constants problems with this test\"",
        "_TODO-test-book-components-build": "(cd ./apps/book-components/ && npm run test-build)",
        "test-app-agents-server": "(cd ./apps/agents-server/ && npm run test)",
        "preversion": "git pull && git pull --tags && npm run test-without-package-generation && echo \"<- Note: In next command the packages are generated\" && npm run generate-packages-commit",
        "postversion": "git push && git push --tags && npm run update-version-in-config-commit && npm run generate-packages-commit && npm run use-packages",
        "make": "npm run make-promptbook-collection && npm run update-version-in-config && npm run generate-packages && npm run generate-examples-bookc && npm run generate-documentation && npm run import-markdowns && npm run generate-openapi-types",
        "generate-packages": "ts-node ./scripts/generate-packages/generate-packages.ts",
        "generate-packages-commit": "ts-node ./scripts/generate-packages/generate-packages.ts --skip-bundler --commit",
        "import-github": "ts-node ./scripts/import-github/import-github.ts",
        "import-github-commit": "ts-node ./scripts/import-github/import-github.ts --commit",
        "_NOTE-0": " <- Note: This is here to ensure that publish.yml is up to date before tagging the new version",
        "generate-documentation": "ts-node ./scripts/generate-documentation/generate-documentation.ts",
        "generate-documentation-commit": "ts-node ./scripts/generate-documentation/generate-documentation.ts --commit",
        "generate-examples-bookc": "ts-node ./scripts/generate-examples-bookc/generate-examples-bookc.ts ",
        "generate-examples-bookc-commit": "ts-node ./scripts/generate-examples-bookc/generate-examples-bookc.ts --commit",
        "generate-openapi-types": "npx openapi-typescript src/remote-server/openapi.yml -o src/remote-server/openapi-types.ts --immutable --export-type",
        "generate-openapi-types-commit": "npx openapi-typescript src/remote-server/openapi.yml -o src/remote-server/openapi-types.ts --immutable --export-type && git add . && git commit --allow-empty --message \"🏭 Generate OpenAPI types\" && git push",
        "generate-vscode-grammar": "ts-node ./scripts/generate-vscode-grammar/generate-vscode-grammar.ts",
        "generate-vscode-grammar-commit": "ts-node ./scripts/generate-vscode-grammar/generate-vscode-grammar.ts && git add . && git commit --allow-empty --message \"🏭 Generate VSCode grammar\" && git push",
        "import-markdowns": "npx deno --allow-env --allow-read --allow-write --allow-sys --unstable-sloppy-imports ./book/scripts/import-markdown/import-markdown.ts",
        "import-markdowns-commit": "npx deno --allow-env --allow-read --allow-write --allow-sys --unstable-sloppy-imports ./book/scripts/import-markdown/import-markdown.ts && cd ./book && git add . && git commit --allow-empty --message \"📄 Import markdowns\" && git push && cd .. && git add . && git commit --allow-empty --message \"📄 Import markdowns\"",
        "make-promptbook-collection": "ts-node ./scripts/make-promptbook-collection/make-promptbook-collection.ts",
        "make-promptbook-collection-commit": "ts-node ./scripts/make-promptbook-collection/make-promptbook-collection.ts --commit",
        "repair-imports-commit": "ts-node ./scripts/repair-imports/repair-imports.ts --commit",
        "update-version-in-config": "ts-node ./scripts/update-version-in-config/update-version-in-config.ts",
        "update-version-in-config-commit": "ts-node ./scripts/update-version-in-config/update-version-in-config.ts --commit",
        "update-and-install-patch-dependencies": "npx npm-check-updates --upgrade --target patch && npm install",
        "update-and-install-patch-dependencies-commit": "npm run update-and-install-patch-dependencies && git add package.json package-lock.json && git commit --allow-empty --message \"🔼 Update patch dependencies\" && git push",
        "_TODO-3": "<- TODO: Detect if the worktree is clean before running patch-dependencies-commit",
        "use-packages": "ts-node ./scripts/use-packages/use-packages.ts",
        "build:book-editor-macos": "node ./scripts/book-editor-macos/build-book-editor-macos.js",
        "build:vscode-extension": "node ./scripts/vscode-extension/build-vscode-extension.js",
        "components-playground": "cd ./apps/book-components/ && npm run dev",
        "_TODO-1": "<- TODO: Remove all unused scripts",
        "_TODO-2": "<- TODO: [⛎] Automatically sync commands to generate-packages.ts",
        "coder:generate-boilerplates": "npx ptbk coder generate-boilerplates --count 5*1 --template ./prompts/templates/common.md",
        "coder:run": "npx ptbk coder run --harness openai-codex --model gpt-5.6-terra --thinking-level max --agent agents/developer.book --context AGENTS.md --test-before yes-and-fix",
        "coder:find-refactor-candidates": "npx ptbk coder find-refactor-candidates",
        "coder:verify": "npx ptbk coder verify",
        "coder:add": "npx ptbk coder add --template ./prompts/templates/common.md"
    },
    "engines": {
        "node": ">=18.18.0",
        "npm": ">=8.0.0"
    },
    "devDependencies": {
        "@eslint/eslintrc": "3",
        "@next/bundle-analyzer": "16.0.11",
        "@playwright/test": "^1.62.1",
        "@rollup/plugin-json": "6.1.0",
        "@rollup/plugin-typescript": "8.3.4",
        "@rollup/plugin-url": "8.0.2",
        "@tailwindcss/typography": "0.5.19",
        "@testing-library/react": "16.3.2",
        "@types/better-sqlite3": "^7.6.13",
        "@types/crypto-js": "4.2.2",
        "@types/express": "5.0.6",
        "@types/geojson": "7946.0.16",
        "@types/jest": "29.5.14",
        "@types/js-yaml": "4.0.9",
        "@types/jsdom": "21.1.7",
        "@types/katex": "0.16.8",
        "@types/leaflet": "1.9.21",
        "@types/localtunnel": "^2.0.4",
        "@types/mime-types": "2.1.4",
        "@types/node": "20",
        "@types/node-fetch": "2.6.13",
        "@types/node-gzip": "1.1.3",
        "@types/nodemailer": "^7.0.11",
        "@types/papaparse": "5.3.16",
        "@types/pg": "8.15.6",
        "@types/prettier": "2.7.3",
        "@types/prompts": "2.4.9",
        "@types/qrcode": "1.5.6",
        "@types/react": "19",
        "@types/react-dom": "19",
        "@types/readline-sync": "1.4.8",
        "@types/showdown": "2.0.6",
        "@types/swagger-ui-dist": "3.30.6",
        "@types/swagger-ui-express": "4.1.8",
        "@types/web-push": "^3.6.4",
        "@typescript-eslint/eslint-plugin": "6.21.0",
        "@typescript-eslint/parser": "6.21.0",
        "autoprefixer": "10.4.27",
        "cspell": "9.0.2",
        "deno": "2.2.15",
        "eslint": "8.57.1",
        "eslint-config-next": "15.4.11",
        "jest": "29.7.0",
        "jest-environment-jsdom": "30.2.0",
        "js-yaml": "4.1.1",
        "llamaindex": "0.12.1",
        "lucide-react": "0.536.0",
        "mermaid": "11.9.0",
        "next": "15.4.11",
        "openapi-typescript": "7.6.1",
        "organize-imports-cli": "0.10.0",
        "playwright": "1.62.1",
        "postcss": "8.5.12",
        "prettier": "2.8.8",
        "react": "19.1.2",
        "react-dom": "19.1.2",
        "readline-sync": "1.4.10",
        "rollup": "2.79.2",
        "rollup-plugin-polyfill-node": "0.13.0",
        "rollup-plugin-postcss": "4.0.2",
        "rollup-plugin-visualizer": "5.13.1",
        "tailwindcss": "3.4.19",
        "ts-jest": "29.1.5",
        "tsconfig-paths-webpack-plugin": "4.2.0",
        "tslib": "2.3.1",
        "type-fest": "4.41.0",
        "typescript": "5.2.2",
        "use-state-with-deps": "1.1.4",
        "yaml": "2.3.4"
    },
    "dependencies": {
        "@ai-sdk/deepseek": "1.0.56",
        "@ai-sdk/google": "2.0.97",
        "@ai-sdk/openai": "2.0.127",
        "@anthropic-ai/sdk": "0.68.0",
        "@aws-sdk/client-s3": "3.936.0",
        "@azure/openai": "1.0.0-beta.13",
        "@dnd-kit/core": "6.3.1",
        "@dnd-kit/sortable": "10.0.0",
        "@dnd-kit/utilities": "3.2.2",
        "@elevenlabs/react": "0.12.3",
        "@modelcontextprotocol/sdk": "1.24.3",
        "@monaco-editor/react": "4.7.0",
        "@mozilla/readability": "0.6.0",
        "@node-saml/node-saml": "^5.1.0",
        "@openai/agents": "0.4.15",
        "@prisma/studio-core": "0.27.3",
        "@sendgrid/mail": "8.1.6",
        "@sentry/nextjs": "^10.57.0",
        "@supabase/supabase-js": "2.81.1",
        "@types/formidable": "3.4.7",
        "@types/mailparser": "3.4.6",
        "@types/turndown": "5.0.6",
        "@vercel/blob": "1.1.1",
        "@xmldom/xmldom": "^0.9.10",
        "@xterm/addon-fit": "0.11.0",
        "@xterm/xterm": "6.0.0",
        "better-sqlite3": "^12.11.1",
        "bottleneck": "2.19.5",
        "colors": "1.4.0",
        "commander": "12.0.0",
        "configchecker": "1.5.132",
        "cross-fetch": "4.0.0",
        "crypto": "1.0.1",
        "crypto-js": "4.2.0",
        "destroyable": "0.12.145",
        "dompurify": "3.4.6",
        "dotenv": "16.3.2",
        "express": "4.21.2",
        "express-openapi-validator": "5.4.9",
        "formidable": "3.5.4",
        "glob-promise": "6.0.7",
        "highlight.js": "11.11.1",
        "html-to-image": "1.11.13",
        "ignore": "^5.3.2",
        "jsdom": "25.0.1",
        "jspdf": "4.1.0",
        "jszip": "3.10.1",
        "katex": "0.16.45",
        "leaflet": "1.9.4",
        "localtunnel": "^2.0.2",
        "lorem-ipsum": "2.0.8",
        "mailparser": "3.9.8",
        "markitdown-ts": "0.0.10",
        "moment": "2.30.1",
        "monaco-editor": "0.54.0",
        "node-gzip": "1.1.2",
        "nodemailer": "^7.0.13",
        "openai": "6.18.0",
        "papaparse": "5.4.1",
        "pg": "8.16.3",
        "prompts": "2.4.2",
        "qrcode": "1.5.4",
        "raw-loader": "^4.0.2",
        "react": "18.2.0",
        "react-dom": "18.2.0",
        "rxjs": "7.8.2",
        "showdown": "2.1.0",
        "socket.io": "4.8.3",
        "socket.io-client": "4.7.5",
        "spacetrim": "0.11.60",
        "swagger-ui-dist": "5.32.15",
        "swagger-ui-express": "5.0.1",
        "ts-node": "10.9.2",
        "turndown": "7.2.4",
        "type-fest": "4.5.0",
        "userhome": "1.0.1",
        "waitasecond": "1.11.164",
        "web-push": "^3.6.7",
        "y-monaco": "0.1.6",
        "y-protocols": "1.0.7",
        "y-websocket": "3.0.0",
        "y-websocket-server": "1.0.2",
        "yjs": "13.6.30",
        "zeptomail": "7.0.2",
        "zod": "4.3.6"
    },
    "overrides": {
        "zod": "$zod"
    }
}
/**
 * Directories containing the repository's unit tests.
 */
const TEST_ROOTS = [
    '<rootDir>/src',
    '<rootDir>/apps/agents-server/src',
    '<rootDir>/apps/agents-server/scripts',
    '<rootDir>/apps/utils/src',
    '<rootDir>/scripts',
    '<rootDir>/book',
];

module.exports = {
    // Keep Jest's filesystem crawl limited to directories that contain unit tests.
    roots: TEST_ROOTS,
    transform: {
        // Type checking is already performed once by `npm run test-types`; unit tests only need transpilation.
        '(jsx?|tsx?)$': [
            'ts-jest',
            {
                tsconfig: 'tsconfig.jest.json',
                isolatedModules: true,
                diagnostics: false,
            },
        ],
        '\\.ya?ml$': '<rootDir>/jest.yamlRawTransformer.js',
    },
    testRegex: '(test)\\.(jsx?|tsx?)$',
    testPathIgnorePatterns: ['/node_modules/', '<rootDir>/\\.promptbook/', '<rootDir>/\\.tmp/'],
    setupFilesAfterEnv: ['./jest.setup.js'],
    moduleFileExtensions: ['ts', 'tsx', 'js', 'jsx', 'json', 'yaml', 'yml', 'node'],
    moduleNameMapper: {
        '^@/(.*)$': '<rootDir>/apps/agents-server/$1',
        '^@common/(.*)$': '<rootDir>/apps/_common/$1',
        '^@promptbook-local/(.*)$': '<rootDir>/src/_packages/$1.index',
        '\\.(css|less|sass|scss)$': '<rootDir>/jest.styleMock.js',
        '^(.*\\.ya?ml)\\?raw$': '$1',
    },
    coverageDirectory: './coverage/',
    collectCoverage: false,
    testTimeout: 5 /* minutes */ * 60 * 1000,
    // Keep a small amount of parallelism without overloading network-heavy tests.
    maxWorkers: 2,
    // Prevent test interference
    maxConcurrency: 1,
};
{
    "compilerOptions": {
        "target": "ES2018",
        "lib": ["dom", "dom.iterable", "esnext"],
        "allowJs": true,
        "skipLibCheck": true,
        "esModuleInterop": true,
        "allowSyntheticDefaultImports": true,
        "strict": true,
        "forceConsistentCasingInFileNames": true,
        "noFallthroughCasesInSwitch": true,
        "noUncheckedIndexedAccess": true,
        "module": "esnext",
        "moduleResolution": "node",
        "baseUrl": ".",
        "paths": {
            "@/*": ["apps/agents-server/*"],
            "@common/*": ["apps/_common/*"],
            "@promptbook-local/*": ["src/_packages/*.index"]
        },
        "isolatedModules": false,
        "resolveJsonModule": true,
        "downlevelIteration": true,
        "noEmit": true,
        "sourceMap": true,
        "declaration": true,
        "outDir": "typings",
        "jsx": "react-jsx"
    },
    "include": ["src", "books", "src/cli/test/ptbk.test.ts", "src/remote-server/other/index.umd.js"],
    "exclude": ["src/scrapers/pdf/lib/pdf2md"],
    "ts-node": {
        "transpileOnly": true
    }
}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n 'agentSource|agentReference|AgentCollection|agentFile|agentPath|Adam' scripts/run-codex-prompts -g '*.ts' -g '"'!*.test.ts'"'; rg --files src/cli/cli-commands | rg init; rg --files src apps/agents-server/src/utils | rg '(Adam|[Aa]dam|[Ii]nherit|ReferenceResolver|[Ff]ile[Aa]gent|[Dd]irectory[Aa]gent|[Bb]ookScoped)'" in C:\Users\me\work\ai\promptbook
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n 'ADAM|Adam|adamSource|resolveAgentSource' apps/agents-server/src/utils/resolveServerAgentContext.ts apps/agents-server/src/collection* src/collection src/book-2.0 -g '*.ts' -g '"'!*.test.ts'"'; Get-Content jest.config.js; Get-Content tsconfig.json; rg --files -g '*init*.ts' src/cli; rg --files apps/coder-landing/src agents/coding" in C:\Users\me\work\ai\promptbook
 succeeded in 3978ms:
scripts/run-codex-prompts\main\runCodexPrompts.ts:128:            agentReferences: resolvedCoderAgent?.agentReferences,
scripts/run-codex-prompts\main\runCodexPrompts.ts:133:        await initializeRunUiAgentVisual(uiHandle, resolvedCoderAgent?.agentSource);
scripts/run-codex-prompts\main\runCodexPrompts.ts:767:    agentSource: string_book | undefined,
scripts/run-codex-prompts\main\runCodexPrompts.ts:769:    if (!uiHandle || !agentSource) {
scripts/run-codex-prompts\main\runCodexPrompts.ts:773:    const agentVisual = await buildCoderRunAgentVisual(agentSource);
scripts/run-codex-prompts\ui\buildCoderRunAgentVisual.ts:52: * @param agentSource Source of the `--agent` book file.
scripts/run-codex-prompts\ui\buildCoderRunAgentVisual.ts:55:export async function buildCoderRunAgentVisual(agentSource: string_book): Promise<CoderRunAgentVisual | null> {
scripts/run-codex-prompts\ui\buildCoderRunAgentVisual.ts:58:            agentSource,
scripts/run-codex-prompts\prompts\isPromptCompatibleWithRunner.ts:20:    readonly agentReferences?: ReadonlyArray<string>;
scripts/run-codex-prompts\prompts\isPromptCompatibleWithRunner.ts:50:        ...(promptRunnerIdentity.agentReferences ?? []),
scripts/run-codex-prompts\common\resolveCoderAgent.ts:16:    readonly agentSource: string_book;
scripts/run-codex-prompts\common\resolveCoderAgent.ts:32:    readonly agentReferences: ReadonlyArray<string>;
scripts/run-codex-prompts\common\resolveCoderAgent.ts:61:    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
scripts/run-codex-prompts\common\resolveCoderAgent.ts:75:    const parsedAgentSource = parseAgentSource(agentSource);
scripts/run-codex-prompts\common\resolveCoderAgent.ts:78:        agentSource,
scripts/run-codex-prompts\common\resolveCoderAgent.ts:80:        agentReferences: createCoderAgentReferences({
scripts/run-codex-prompts\common\resolveCoderAgent.ts:106:        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
scripts/run-codex-prompts\common\resolveCoderAgent.ts:136:            ].filter((agentReference) => agentReference.trim() !== ''),
src/cli/cli-commands\agent-folder\initializeAgentRunnerCommand.ts
src/cli/cli-commands\agent-folder\initializeAgentProjectConfiguration.ts
src/cli/cli-commands\agent-folder\init.ts
src/cli/cli-commands\common\harness\HarnessDefinition.ts
src/cli/cli-commands\common\harness\HarnessDefinition.test.ts
src/cli/cli-commands\coder\initializeCoderProjectConfiguration.ts
src/cli/cli-commands\coder\init.ts
src/cli/cli-commands\coder\init.test.ts
src/cli/cli-commands\agents-server\initializeAgentsServerProjectConfiguration.ts
src/cli/cli-commands\agents-server\init.ts
src/cli/cli-commands\agents-server\init.test.ts
apps/agents-server/src/utils\agentReferenceResolver\resolveTeamCapabilitiesFromAgentSource.ts
apps/agents-server/src/utils\agentReferenceResolver\resolveTeamCapabilitiesFromAgentSource.test.ts
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts
apps/agents-server/src/utils\agentReferenceResolver\MissingCoreAgentRecovery.ts
apps/agents-server/src/utils\agentReferenceResolver\extractAgentReferenceTokens.ts
apps/agents-server/src/utils\agentReferenceResolver\createUnresolvedAgentReferenceDiagnostics.ts
apps/agents-server/src/utils\agentReferenceResolver\createUnresolvedAgentReferenceDiagnostics.test.ts
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.ts
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.test.ts
apps/agents-server/src/utils\agentReferenceResolver\createAgentNameCollisionDiagnostics.ts
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.ts
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.test.ts
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.ts
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.test.ts
apps/agents-server/src/utils\agentReferenceResolver\AgentReferenceResolutionIssue.ts
apps/agents-server/src/utils\agentReferenceResolver\$provideAgentReferenceResolver.ts
apps/agents-server/src/utils\agentReferenceResolver\$provideAgentReferenceResolver.test.ts
apps/agents-server/src/utils\resolveInheritedAgentSource.ts
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts
apps/agents-server/src/utils\resolveAgentProfileWithInheritance.ts
src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts
src\book-2.0\agent-source\AgentReferenceResolver.ts

 succeeded in 3899ms:
rg: apps/agents-server/src/utils/resolveServerAgentContext.ts:50:    const adamAgentUrl = await getWellKnownAgentUrl('ADAM');
src/book-2.0\book-language-documentation\englishBookLanguageManualDictionary.ts:143:                - Writing no \`FROM\` at all is the same as writing \`FROM @Adam\`, the core agent every agent inherits from.
src/book-2.0\book-language-documentation\englishBookLanguageManualDictionary.ts:145:                - An explicit parent replaces the implicit Adam parent; the selected parent's own \`FROM\` chain is then inherited normally.
src/book-2.0\book-language-documentation\englishBookLanguageManualDictionary.ts:148:                - Adam is the root agent and must explicitly use \`FROM @Null\` (\`@Void\` is equivalent). The Book editor warns about a different Adam parent and local inheritance cycles.
src/book-2.0\book-language-documentation\czechBookLanguageManualDictionary.ts:143:                - Když \`FROM\` není uvedeno vůbec, platí to samé jako \`FROM @Adam\`, tedy jádrový agent, ze kterého dědí každý agent.
src/book-2.0\book-language-documentation\czechBookLanguageManualDictionary.ts:145:                - Výslovně zvolený rodič nahradí implicitního Adama; vlastní řetězec \`FROM\` tohoto rodiče se pak zdědí běžným způsobem.
src/book-2.0\book-language-documentation\czechBookLanguageManualDictionary.ts:148:                - Adam je kořenový agent a musí výslovně použít \`FROM @Null\` (\`@Void\` je ekvivalentní). Editor knihy upozorní na jiného Adamova rodiče i na lokální cykly dědičnosti.
src/book-2.0\agent-source\string_book.ts:53:// <- TODO: [🐱‍🚀] `GENESIS_BOOK` / `ADAM_BOOK` in `/agents/adam.book`
src/book-2.0\agent-source\parseAgentSource\createCapabilitiesFromCommitment.ts:112:    if (reference === 'Adam' || reference === '' /* <- Note: Adam is implicit */) {
apps/agents-server/src/collection*: The filename, directory name, or volume label syntax is incorrect. (os error 123)
/**
 * Directories containing the repository's unit tests.
 */
const TEST_ROOTS = [
    '<rootDir>/src',
    '<rootDir>/apps/agents-server/src',
    '<rootDir>/apps/agents-server/scripts',
    '<rootDir>/apps/utils/src',
    '<rootDir>/scripts',
    '<rootDir>/book',
];

module.exports = {
    // Keep Jest's filesystem crawl limited to directories that contain unit tests.
    roots: TEST_ROOTS,
    transform: {
        // Type checking is already performed once by `npm run test-types`; unit tests only need transpilation.
        '(jsx?|tsx?)$': [
            'ts-jest',
            {
                tsconfig: 'tsconfig.jest.json',
                isolatedModules: true,
                diagnostics: false,
            },
        ],
        '\\.ya?ml$': '<rootDir>/jest.yamlRawTransformer.js',
    },
    testRegex: '(test)\\.(jsx?|tsx?)$',
    testPathIgnorePatterns: ['/node_modules/', '<rootDir>/\\.promptbook/', '<rootDir>/\\.tmp/'],
    setupFilesAfterEnv: ['./jest.setup.js'],
    moduleFileExtensions: ['ts', 'tsx', 'js', 'jsx', 'json', 'yaml', 'yml', 'node'],
    moduleNameMapper: {
        '^@/(.*)$': '<rootDir>/apps/agents-server/$1',
        '^@common/(.*)$': '<rootDir>/apps/_common/$1',
        '^@promptbook-local/(.*)$': '<rootDir>/src/_packages/$1.index',
        '\\.(css|less|sass|scss)$': '<rootDir>/jest.styleMock.js',
        '^(.*\\.ya?ml)\\?raw$': '$1',
    },
    coverageDirectory: './coverage/',
    collectCoverage: false,
    testTimeout: 5 /* minutes */ * 60 * 1000,
    // Keep a small amount of parallelism without overloading network-heavy tests.
    maxWorkers: 2,
    // Prevent test interference
    maxConcurrency: 1,
};
{
    "compilerOptions": {
        "target": "ES2018",
        "lib": ["dom", "dom.iterable", "esnext"],
        "allowJs": true,
        "skipLibCheck": true,
        "esModuleInterop": true,
        "allowSyntheticDefaultImports": true,
        "strict": true,
        "forceConsistentCasingInFileNames": true,
        "noFallthroughCasesInSwitch": true,
        "noUncheckedIndexedAccess": true,
        "module": "esnext",
        "moduleResolution": "node",
        "baseUrl": ".",
        "paths": {
            "@/*": ["apps/agents-server/*"],
            "@common/*": ["apps/_common/*"],
            "@promptbook-local/*": ["src/_packages/*.index"]
        },
        "isolatedModules": false,
        "resolveJsonModule": true,
        "downlevelIteration": true,
        "noEmit": true,
        "sourceMap": true,
        "declaration": true,
        "outDir": "typings",
        "jsx": "react-jsx"
    },
    "include": ["src", "books", "src/cli/test/ptbk.test.ts", "src/remote-server/other/index.umd.js"],
    "exclude": ["src/scrapers/pdf/lib/pdf2md"],
    "ts-node": {
        "transpileOnly": true
    }
}
src/cli\$initializePromptbookCliProgram.ts
src/cli\cli-commands\common\harness\HarnessDefinition.ts
src/cli\cli-commands\common\harness\HarnessDefinition.test.ts
src/cli\cli-commands\coder\initializeCoderProjectConfiguration.ts
src/cli\cli-commands\coder\init.ts
src/cli\cli-commands\coder\init.test.ts
src/cli\cli-commands\agents-server\init.test.ts
src/cli\cli-commands\agents-server\initializeAgentsServerProjectConfiguration.ts
src/cli\cli-commands\agents-server\init.ts
src/cli\cli-commands\agent-folder\init.ts
src/cli\cli-commands\agent-folder\initializeAgentProjectConfiguration.ts
src/cli\cli-commands\agent-folder\initializeAgentRunnerCommand.ts
agents/coding\unslopper.book
agents/coding\developer.book
apps/coder-landing/src\data\structuredData.ts
apps/coder-landing/src\data\siteMetadata.ts
apps/coder-landing/src\data\liveDemoScript.ts
apps/coder-landing/src\data\links.ts
apps/coder-landing/src\data\heroClaims.ts
apps/coder-landing/src\data\harnessCatalog.ts
apps/coder-landing/src\data\github.ts
apps/coder-landing/src\data\developerAgentBook.ts
apps/coder-landing/src\data\coreBenefits.ts
apps/coder-landing/src\data\comparison.ts
apps/coder-landing/src\data\commands.ts
apps/coder-landing/src\data\advancedFeatures.ts
apps/coder-landing/src\components\TerminalBlock\TerminalBlock.tsx
apps/coder-landing/src\components\TerminalBlock\highlightShellCommand.tsx
apps/coder-landing/src\components\StructuredData\StructuredData.tsx
apps/coder-landing/src\components\SocialShareImage\renderSocialShareImage.tsx
apps/coder-landing/src\components\sections\Quickstart.tsx
apps/coder-landing/src\components\sections\HowItWorks.tsx
apps/coder-landing/src\components\sections\Hero.tsx
apps/coder-landing/src\components\sections\Header.tsx
apps/coder-landing/src\components\sections\HarnessesSection.tsx
apps/coder-landing/src\components\sections\Footer.tsx
apps/coder-landing/src\components\sections\FeaturesSection.tsx
apps/coder-landing/src\components\sections\ComparisonSection.tsx
apps/coder-landing/src\components\sections\AgentsSection.tsx
apps/coder-landing/src\components\LiveTerminalDemo\useFittedTerminalMetrics.ts
apps/coder-landing/src\components\LiveTerminalDemo\TerminalAnsiTextLine.tsx
apps/coder-landing/src\components\LiveTerminalDemo\SharedAgentTerminalVisual.tsx
apps/coder-landing/src\components\LiveTerminalDemo\LiveTerminalDemo.tsx
apps/coder-landing/src\components\HeroClaimRotator\HeroClaimRotator.tsx
apps/coder-landing/src\components\HarnessLogo\HarnessLogo.tsx
apps/coder-landing/src\components\GitHubStarCallToAction\GitHubStarCallToAction.tsx
apps/coder-landing/src\components\DeveloperAgentBookPreview\DeveloperAgentBookPreview.tsx
apps/coder-landing/src\components\CoreBenefitGraphic\CoreBenefitGraphic.tsx
apps/coder-landing/src\components\ComparisonTable\resolveComparisonRowCellGroups.ts
apps/coder-landing/src\components\ComparisonTable\ComparisonTable.tsx
apps/coder-landing/src\components\ComparisonTable\ComparisonSupportMark.tsx
apps/coder-landing/src\components\ComparisonTable\ComparisonSolutionLogo.tsx
apps/coder-landing/src\app\twitter-image.tsx
apps/coder-landing/src\app\sitemap.ts
apps/coder-landing/src\app\robots.ts
apps/coder-landing/src\app\page.tsx
apps/coder-landing/src\app\opengraph-image.tsx
apps/coder-landing/src\app\layout.tsx
apps/coder-landing/src\app\globals.css

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content apps/agents-server/src/utils/agentReferenceResolver/createServerAgentReferenceResolver.ts -TotalCount 180; Get-Content src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts; Get-Content src/cli/cli-commands/coder/init.ts; rg -n 'adam|ADAM|Adam' src/collection apps/agents-server/src/utils/core* apps/agents-server/src/utils -g '*Core*' -g '*core*'" in C:\Users\me\work\ai\promptbook
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content scripts/run-codex-prompts/common/resolveCoderAgent.ts; rg -n 'createAgentRunnerSystemMessage' src scripts; Get-Content src/book-2.0/agent-source/AgentReferenceResolver.ts; Get-Content apps/agents-server/src/utils/resolveInheritedAgentSource.ts" in C:\Users\me\work\ai\promptbook
 exited 1 in 2591ms:
import type { AgentReferenceResolver } from '../../../../../src/book-2.0/agent-source/AgentReferenceResolver';
import { normalizeAgentName } from '../../../../../src/book-2.0/agent-source/normalizeAgentName';
import type { TeammateProfile } from '../../../../../src/book-2.0/agent-source/TeammateProfileResolver';
import {
    VOID_PSEUDO_AGENT_REFERENCE,
    createPseudoAgentUrl,
    isPseudoAgentAllowedInCommitment,
    resolvePseudoAgentKindFromReference,
} from '../../../../../src/book-2.0/agent-source/pseudoAgentReferences';
import type { AgentCollection } from '../../../../../src/collection/agent-collection/AgentCollection';
import type { BookCommitment } from '../../../../../src/commitments/_base/BookCommitment';
import {
    type AgentReferenceResolutionIssue,
    type IssueTrackingAgentReferenceResolver,
} from './AgentReferenceResolutionIssue';
import { extractAgentReferenceTokens } from './extractAgentReferenceTokens';
import { resolveAgentRouteIdentifier } from '../agentIdentifier';

/**
 * Dependencies required to resolve local/federated compact references.
 */
type ServerResolverOptions = {
    readonly agentCollection: AgentCollection;
    readonly localServerUrl: string;
    readonly federatedServers?: readonly string[];
};

/**
 * Cached lookup maps fetched from a federated server.
 */
type RemoteAgentLookup = {
    readonly byName: Map<string, string>;
    readonly byId: Map<string, string>;
};

/**
 * Lightweight heuristic for identifying base58-like permanent IDs.
 */
const BASE58_PATTERN = /^[1-9A-HJ-NP-Za-km-z]+$/;

/**
 * Upper bound for in-memory unresolved-reference issue tracking.
 */
const MAX_TRACKED_RESOLUTION_ISSUES = 200;

/**
 * Upper bound for one federated-agent lookup request before falling back to an empty cache.
 *
 * @private
 */
const FEDERATED_AGENT_LOOKUP_TIMEOUT_MS = 1_500;

/**
 * Creates a resolver backed by the Agents Server collection and configured federated servers.
 *
 * @param options - Resolver dependencies
 * @returns Agent reference resolver for the current agents server instance
 *
 * @private
 */
export async function createServerAgentReferenceResolver(
    options: ServerResolverOptions,
): Promise<AgentReferenceResolver> {
    const resolver = new ServerAgentReferenceResolver(options);
    await resolver.initialize();
    return resolver;
}

/**
 * Agents Server resolver that expands compact references and tracks unresolved tokens.
 */
class ServerAgentReferenceResolver implements IssueTrackingAgentReferenceResolver {
    private readonly agentCollection: AgentCollection;
    private readonly localServerUrl: string;
    private readonly federatedServers: string[];
    private readonly localNameToUrl = new Map<string, string>();
    private readonly localIdToUrl = new Map<string, string>();
    private readonly localUrlToProfile = new Map<string, TeammateProfile>();
    private readonly remoteCaches = new Map<string, RemoteAgentLookup>();
    private readonly remoteRequests = new Map<string, Promise<RemoteAgentLookup>>();
    private readonly resolutionIssues: Array<AgentReferenceResolutionIssue> = [];

    public constructor(options: ServerResolverOptions) {
        this.agentCollection = options.agentCollection;
        this.localServerUrl = ServerAgentReferenceResolver.normalizeServerUrl(options.localServerUrl);
        const servers = options.federatedServers || [];
        this.federatedServers = [
            ...new Set(
                servers
                    .map(ServerAgentReferenceResolver.normalizeServerUrl)
                    .filter((server) => server.length > 0 && server !== this.localServerUrl),
            ),
        ];
    }

    public async initialize(): Promise<void> {
        const agents = await this.agentCollection.listAgents();
        for (const agent of agents) {
            const routeIdentifier = resolveAgentRouteIdentifier(agent);
            const url = this.buildLocalAgentUrl(routeIdentifier);
            this.localNameToUrl.set(normalizeAgentName(agent.agentName), url);
            this.localNameToUrl.set(agent.agentName, url);
            if (agent.permanentId) {
                this.localIdToUrl.set(agent.permanentId, url);
            }

            const profile: TeammateProfile = {
                agentName: agent.agentName,
                personaDescription: agent.personaDescription,
            };
            this.localUrlToProfile.set(url, profile);

            if (routeIdentifier !== agent.agentName) {
                this.localUrlToProfile.set(this.buildLocalAgentUrl(agent.agentName), profile);
            }
        }
    }

    /**
     * Drains and returns unresolved-reference issues captured in previous resolutions.
     */
    public consumeResolutionIssues(): Array<AgentReferenceResolutionIssue> {
        const issues = [...this.resolutionIssues];
        this.resolutionIssues.length = 0;
        return issues;
    }

    /**
     * Returns the human-readable agent profile for a teammate URL if it belongs to this server.
     *
     * Used during TEAM commitment processing to enrich tool definitions with real agent names
     * and profile descriptions instead of technical IDs.
     *
     * @param url - Canonical teammate URL from the resolved TEAM commitment content.
     * @returns Agent profile or `null` when the URL is not a local agent.
     */
    public async resolveTeammateProfile(url: string): Promise<TeammateProfile | null> {
        return this.localUrlToProfile.get(url) ?? null;
    }

    /**
     * Rewrites compact references in commitment content into canonical URLs.
     *
     * Missing references are handled with commitment-specific safe fallbacks instead of throwing.
     */
    public async resolveCommitmentContent(commitmentType: BookCommitment, content: string): Promise<string> {
        if (!content) {
            return content;
        }

        const parts: string[] = [];
        let lastIndex = 0;
        let hasMissingReference = false;

        for (const tokenMatch of extractAgentReferenceTokens(content)) {
            const token = tokenMatch.token;
            const tokenValue = tokenMatch.reference;
            const tokenIndex = tokenMatch.index;

            parts.push(content.slice(lastIndex, tokenIndex));
            lastIndex = tokenIndex + token.length;

            if (!tokenValue) {
                parts.push(token);
                continue;
            }

            const pseudoAgentReplacement = this.resolvePseudoAgentReference(commitmentType, tokenValue);
            if (pseudoAgentReplacement.status === 'resolved') {
                parts.push(pseudoAgentReplacement.content);
                continue;
            }

            if (pseudoAgentReplacement.status === 'disallowed') {
                hasMissingReference = true;
                this.trackResolutionIssue({
                    commitmentType,
                    token,
                    reference: tokenValue,
                    message: pseudoAgentReplacement.message,
import type { InitializationStatus } from './boilerplateTemplates';
import {
    PROMPTS_DIRECTORY_PATH,
    PROMPTS_DONE_DIRECTORY_PATH,
    PROMPTS_TEMPLATES_DIRECTORY_PATH,
} from './boilerplateTemplates';
import type { EnsuredCoderReferencedArtifact } from './coderReferencedArtifacts';
import { ensureCoderReferencedArtifacts } from './coderReferencedArtifacts';
import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
import { ensureCoderEnvFile } from './ensureCoderEnvFile';
import { ensureCoderGitignoreFile } from './ensureCoderGitignoreFile';
import { ensureCoderPackageJsonFile } from './ensureCoderPackageJsonFile';
import { ensureCoderVscodeSettingsFile } from './ensureCoderVscodeSettingsFile';
import { ensureDirectory } from './ensureDirectory';
import { resolveCoderPackageJsonScriptReferencedArtifactPaths } from './getDefaultCoderPackageJsonScripts';

/**
 * Result summary returned after coder configuration initialization.
 *
 * @private internal utility of `coder init` command
 */
export type CoderInitializationSummary = {
    readonly promptsDirectoryStatus: InitializationStatus;
    readonly promptsDoneDirectoryStatus: InitializationStatus;
    readonly promptsTemplatesDirectoryStatus: InitializationStatus;
    readonly agentsDirectoryStatus: InitializationStatus;
    readonly envFileStatus: InitializationStatus;
    readonly gitignoreFileStatus: InitializationStatus;
    readonly packageJsonFileStatus: InitializationStatus;
    readonly vscodeSettingsFileStatus: InitializationStatus;
    readonly addedPackageJsonScriptNames: ReadonlyArray<string>;
    readonly referencedArtifactStatuses: ReadonlyArray<EnsuredCoderReferencedArtifact>;
    readonly initializedEnvVariableNames: ReadonlyArray<string>;
};

/**
 * Creates or updates all coder configuration artifacts required in the current project.
 *
 * Nothing the project already owns is ever overwritten - existing scripts, settings and files are kept as they are,
 * and the artifacts they would reference are created only together with the scripts which actually reference them.
 *
 * @private internal utility of `coder init` command
 */
export async function initializeCoderProjectConfiguration(projectPath: string): Promise<CoderInitializationSummary> {
    const promptsDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DIRECTORY_PATH);
    const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
    const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
    const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
    const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
    const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
    const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
        await ensureCoderPackageJsonFile(projectPath);
    const vscodeSettingsFileStatus = await ensureCoderVscodeSettingsFile(projectPath);
    const referencedArtifactStatuses = await ensureCoderReferencedArtifacts(
        projectPath,
        resolveCoderPackageJsonScriptReferencedArtifactPaths(addedPackageJsonScriptNames),
    );

    return {
        promptsDirectoryStatus,
        promptsDoneDirectoryStatus,
        promptsTemplatesDirectoryStatus,
        agentsDirectoryStatus,
        envFileStatus,
        gitignoreFileStatus,
        packageJsonFileStatus,
        vscodeSettingsFileStatus,
        addedPackageJsonScriptNames,
        referencedArtifactStatuses,
        initializedEnvVariableNames,
    };
}

// Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
import type {
    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
} from 'commander';
import { spaceTrim } from 'spacetrim';
import type { $side_effect } from '../../../utils/organization/$side_effect';
import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
import {
    addCoderGitSyncOptions,
    CODER_GIT_SYNC_DESCRIPTION,
    normalizeCoderGitSyncCliOptions,
} from '../common/coderGitSyncCliOptions';
import { handleActionErrors } from '../common/handleActionErrors';
import { $ensureHarnessInstallations } from '../common/harness/$ensureHarnessInstallations';
import { getHarnessDefinition } from '../common/harness/HarnessDefinition';
import {
    addQuestionsOption,
    normalizeQuestionsCliOptions,
    QUESTIONS_DESCRIPTION,
    type QuestionsCliOptions,
} from '../common/questionsCliOptions';
import type { PromptRunnerHarnessName } from '../common/promptRunnerCliOptions';
import { AGENTS_FILE_PATH } from './agentsFile';
import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
import { isDirectoryEmpty } from './ensureDirectory';
import { formatDisplayPath } from './formatDisplayPath';
import { generatePromptBoilerplate } from './generate-boilerplates';
import { initializeCoderProjectConfiguration } from './initializeCoderProjectConfiguration';
import { printInitializationSummary } from './printInitializationSummary';

export { getDefaultCoderPackageJsonScripts } from './getDefaultCoderPackageJsonScripts';
export { getDefaultCoderVscodeSettings } from './getDefaultCoderVscodeSettings';
export { initializeCoderProjectConfiguration } from './initializeCoderProjectConfiguration';

/**
 * Harnesses whose global installation is checked by `ptbk coder init` so that the initialized
 * project is ready to run coding prompts right away.
 *
 * @private internal constant of `coder init` command
 */
const CODER_INIT_CHECKED_HARNESS_NAMES: ReadonlyArray<PromptRunnerHarnessName> = ['openai-codex', 'claude-code'];

/**
 * Initializes `coder init` command for Promptbook CLI utilities.
 *
 * Note: `$` is used to indicate that this function is not a pure function - it registers a command in the CLI.
 *
 * @private internal function of `promptbookCli`
 */
export function $initializeCoderInitCommand(program: Program): $side_effect {
    const command = program.command('init');
    command.alias('initialize');
    command.description(
        spaceTrim(
            (block) => `
                Initialize Promptbook coder configuration for current project

                Creates or updates:
                - prompts/
                - prompts/done/
                ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                - ${CODER_DEVELOPER_AGENT_FILE_PATH}
                - ${AGENTS_FILE_PATH}
                - .gitignore with local artifacts from every supported harness
                - package.json
                - .vscode/settings.json

                Never overwrites what the project already owns:
                - Existing package.json scripts and .vscode/settings.json settings are kept, only missing ones are added
                - Files referenced by a script, like the agent and context of coder:run, are created only together
                  with the script which references them

                Ensures required coding-agent environment variables in .env:
                - CODING_AGENT_GIT_NAME
                - CODING_AGENT_GIT_EMAIL
                - CODING_AGENT_GIT_SIGNING_KEY

                Checks that the coding harnesses are installed and up to date unless \`--no-questions\` is used:
                ${block(listCheckedHarnessLabels())}

                ${block(CODER_GIT_SYNC_DESCRIPTION)}

                ${block(QUESTIONS_DESCRIPTION)}
            `,
        ),
    );

    addCoderGitSyncOptions(command);
    addQuestionsOption(command);

    command.action(
        handleActionErrors(async (cliOptions) => {
            const gitSync = normalizeCoderGitSyncCliOptions(cliOptions as CoderGitSyncCliOptions);
            const questionsOptions = normalizeQuestionsCliOptions(cliOptions as QuestionsCliOptions);
            const projectPath = process.cwd();

            // Note: Import the git synchronization dynamically to keep the CLI fast for runs without `--commit`
            const { $commitCoderChanges, $startCoderGitSync } = await import(
                '../../../../scripts/run-codex-prompts/git/coderGitSync'
            );

            const commitScope = await $startCoderGitSync({ gitSync, projectPath });
            const isPromptsDirectoryEmpty = await isDirectoryEmpty(projectPath, PROMPTS_DIRECTORY_PATH);

            const summary = await initializeCoderProjectConfiguration(projectPath);
            printInitializationSummary(summary);

            if (isPromptsDirectoryEmpty) {
                await generatePromptBoilerplate({ projectPath, boilerplateCount: DEFAULT_BOILERPLATE_COUNT });
            }

            await $commitCoderChanges({
                gitSync,
                commitScope,
                commitMessage: 'Initialize Promptbook Coder',
            });

            await $ensureHarnessInstallations(CODER_INIT_CHECKED_HARNESS_NAMES, questionsOptions);
        }),
    );
}

/**
 * Lists the harnesses whose global installation is checked by `ptbk coder init`.
 */
function listCheckedHarnessLabels(): string {
    return CODER_INIT_CHECKED_HARNESS_NAMES.map((harnessName) => `- ${getHarnessDefinition(harnessName).label}`).join(
        '\n',
    );
}

/**
 * Lists the project-owned template file paths created by `ptbk coder init`.
 */
function listDefaultCoderProjectPromptTemplateDisplayPaths(): string {
    return getDefaultCoderProjectPromptTemplateDefinitions()
        .map(({ relativeFilePath }) => `- ${formatDisplayPath(relativeFilePath)}`)
        .join('\n');
}

// Note: [🟡] Code for CLI command [init](src/cli/cli-commands/coder/init.ts) should never be published outside of `@promptbook/cli`
// Note: [💞] Ignore a discrepancy between file name and entity name
rg: apps/agents-server/src/utils/core*: The filename, directory name, or volume label syntax is incorrect. (os error 123)
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:5:import { isImplicitAdamInheritance } from '../explicitFromCommitment';
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:38: * 2. A book without any explicit `FROM` implicitly inherits from `@Adam`, so a missing Adam is reported right away
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:42: * @returns Missing references carrying core-agent recovery, with the implicit `@Adam` ancestor first when missing.
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:50:    const isAdamImplicitlyInherited = isImplicitAdamInheritance(agentSource);
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:52:    if (!isAdamImplicitlyInherited && missingAgentReferences.length === 0) {
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:72:    if (!isAdamImplicitlyInherited) {
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:76:    const missingAdamAgent = findMissingCoreAgent(missingCoreAgents, CORE_AGENTS_SERVER_WELL_KNOWN_AGENT_NAMES.ADAM);
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:78:    if (!missingAdamAgent || hasMissingAgentReferenceFor(recoverableMissingAgentReferences, missingAdamAgent)) {
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:83:        createImplicitAdamMissingReference(missingAdamAgent, coreAgentRecovery),
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:92: * disagree with it. Implicit inheritance always loads Adam from this very server, so a federated namesake elsewhere
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:111: * @param reference - Raw reference payload, for example `Adam` or `adam`.
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:139: * Creates the missing-reference entry standing for the implicit `FROM @Adam` ancestor.
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:141: * @param missingAdamAgent - Report of the missing Adam core agent.
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:145:function createImplicitAdamMissingReference(
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:146:    missingAdamAgent: CoreAgentReport,
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:150:        reference: missingAdamAgent.title,
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:151:        token: `{${missingAdamAgent.title}}`,
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:57: * Book that relies on the implicit `FROM @Adam` inheritance.
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:70:            createCoreAgentReport('adam', 'Adam', false),
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:75:    it('surfaces the implicitly inherited Adam ancestor when the core agent is missing', async () => {
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:84:            reference: 'Adam',
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:85:            token: '{Adam}',
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:90:            missingCoreAgentTitles: ['Adam', 'Teacher'],
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:105:    it('reports nothing when Adam is present on the server', async () => {
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:106:        mockCoreAgents([createCoreAgentReport('adam', 'Adam', true), createCoreAgentReport('teacher', 'Teacher', true)]);
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:140:            FROM {Adam}
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:144:        const explicitMissingAdamReference: MissingAgentReference = {
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:145:            reference: 'Adam',
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:146:            token: '{Adam}',
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:152:            missingAgentReferences: [explicitMissingAdamReference],
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:158:            reference: 'Adam',
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:159:            token: '{Adam}',
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:166:    it('keeps an ordinary missing reference creatable and still reports the implicit Adam ancestor', async () => {
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts:187:        expect(missingAgentReferences[0]).toMatchObject({ reference: 'Adam', isImplicit: true });
apps/agents-server/src/utils\defaultAgents\coreAgentExplanations.ts:13:    adam: spaceTrim(`
apps/agents-server/src/utils\defaultAgents\coreAgentExplanations.ts:14:        **Adam** is the root ancestor agent. Every agent that does not use an explicit \`FROM\` commitment inherits from
apps/agents-server/src/utils\defaultAgents\coreAgentExplanations.ts:15:        Adam, so its persona, rules, and base capabilities form the shared foundation of all agents on this server.
apps/agents-server/src/utils\defaultAgents\coreAgentExplanations.ts:16:        Removing Adam breaks the default inheritance chain for new and existing agents.
apps/agents-server/src/utils\defaultAgents\coreAgentExplanations.ts:29:        used by the self-learning features and inherits from Adam.
apps/agents-server/src/utils\defaultAgents\coreAgentExplanations.ts:46: * @param agentName - Normalized agent name (for example \`adam\`).

codex
The coder currently reads only the selected book and compiles it without a reference resolver. I’m checking the existing inheritance and core-agent helpers so the fix uses the same Book behavior as the server, including Adam defaults and null/void opt-outs.

 succeeded in 15146ms:
import { readFile } from 'fs/promises';
import { basename, extname, relative, resolve } from 'path';
import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
import { NotFoundError } from '../../../src/errors/NotFoundError';
import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';

/**
 * Source and prompt-routing references resolved from an optional `--agent` book file.
 */
export type ResolvedCoderAgentBook = {
    /**
     * Raw source of the agent `.book` file.
     */
    readonly agentSource: string_book;

    /**
     * Human-readable name of the agent, as it is reported in prompt status lines and run traces.
     *
     * Comes from the `META FULLNAME` of the Book, or from the plain title on its first line, so it is the
     * display name of the agent and not its normalized identifier.
     */
    readonly agentName: string;

    /**
     * Equivalent references by which a prompt may target this agent.
     *
     * Includes the configured path, relative path, filename, filename without `.book`, and the
     * normalized Book title from its first line.
     */
    readonly agentReferences: ReadonlyArray<string>;
};

/**
 * Agent data resolved from the optional `--agent` book file.
 */
export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
    /**
     * Compiled system message injected into each coding prompt.
     */
    readonly systemMessage: string;
};

/**
 * Reads an optional agent `.book` file and prepares the references that route prompts to it.
 *
 * Returns `undefined` when no agent path is provided.
 */
export async function resolveCoderAgentBook(
    agentBookReference: string | undefined,
    currentWorkingDirectory: string,
): Promise<ResolvedCoderAgentBook | undefined> {
    const normalizedAgentBookReference = agentBookReference?.trim();

    if (!normalizedAgentBookReference) {
        return undefined;
    }

    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
        if (error.code === 'ENOENT' || error.code === 'EISDIR') {
            throw new NotFoundError(
                spaceTrim(`
                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.

                    Pass a path to a \`.book\` file in \`--agent\`.
                `),
            );
        }
        throw error;
    })) as string_book;

    // Note: Parsed once here, because both the display name of the agent and its routing references need it
    const parsedAgentSource = parseAgentSource(agentSource);

    return {
        agentSource,
        agentName: parsedAgentSource.meta.fullname || parsedAgentSource.agentName,
        agentReferences: createCoderAgentReferences({
            agentBookReference: normalizedAgentBookReference,
            resolvedAgentBookPath,
            currentWorkingDirectory,
            normalizedAgentNameFromBook: parsedAgentSource.agentName,
        }),
    };
}

/**
 * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
 *
 * Returns `undefined` when no agent path is provided.
 */
export async function resolveCoderAgent(
    agentBookReference: string | undefined,
    currentWorkingDirectory: string,
): Promise<ResolvedCoderAgent | undefined> {
    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);

    if (resolvedAgentBook === undefined) {
        return undefined;
    }

    return {
        ...resolvedAgentBook,
        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
    };
}

/**
 * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
 *
 * @private internal utility of `resolveCoderAgentBook`
 */
function createCoderAgentReferences(options: {
    readonly agentBookReference: string;
    readonly resolvedAgentBookPath: string;
    readonly currentWorkingDirectory: string;
    readonly normalizedAgentNameFromBook: string;
}): string[] {
    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
        '\\',
        '/',
    );
    const agentBookFileName = basename(options.resolvedAgentBookPath);
    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));

    return Array.from(
        new Set(
            [
                options.agentBookReference,
                relativeAgentBookPath,
                agentBookFileName,
                agentBookName,
                options.normalizedAgentNameFromBook,
            ].filter((agentReference) => agentReference.trim() !== ''),
        ),
    );
}
scripts\run-codex-prompts\common\resolveCoderAgent.ts:7:import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
scripts\run-codex-prompts\common\resolveCoderAgent.ts:106:        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
scripts\run-agent-messages\messages\createAgentRunnerSystemMessage.ts:9:export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
scripts\run-agent-messages\main\tickAgentMessages.ts:35:import { createAgentRunnerSystemMessage } from '../messages/createAgentRunnerSystemMessage';
scripts\run-agent-messages\main\tickAgentMessages.ts:397:    return createAgentRunnerSystemMessage(agentSource as string_book);
scripts\run-agent-chat\executeAgentChatTurn.ts:20:import { createAgentRunnerSystemMessage } from '../run-agent-messages/messages/createAgentRunnerSystemMessage';
scripts\run-agent-chat\executeAgentChatTurn.ts:79:    const agentSystemMessage = await createAgentRunnerSystemMessage(agentSourceFile.agentSource);
import type { BookCommitment } from '../../commitments/_base/BookCommitment';
import type { TeammateProfile } from './TeammateProfileResolver';

/**
 * Resolves compact agent references that appear inside FROM, IMPORT, and TEAM commitments.
 *
 * Implementations are expected to rewrite reference tokens such as `{Activation code agent}`
 * or `@Superagent` into concrete agent URLs before the commitment is applied.
 *
 * @private @@@
 */
export type AgentReferenceResolver = {
    /**
     * Normalizes the content of a commitment before it is applied to the model requirements.
     *
     * @param commitmentType - The commitment keyword (e.g. FROM, IMPORT, TEAM)
     * @param content - Original payload of the commitment
     */
    resolveCommitmentContent(commitmentType: BookCommitment, content: string): Promise<string>;

    /**
     * Optional: returns the actual human-readable name and description for a teammate agent URL.
     *
     * When implemented, this enriches TEAM tool definitions with the agent's real name and
     * persona description instead of technical IDs derived from the URL path.
     *
     * @param url - Canonical teammate URL from the resolved TEAM commitment content.
     * @returns Agent profile or `null` when the URL is not resolvable locally.
     */
    resolveTeammateProfile?: (url: string) => Promise<TeammateProfile | null>;
};
import type { string_agent_url, string_book } from '../../../../src/_packages/types.index'; // <- [🚾]
import type { AgentReferenceResolver } from '../../../../src/book-2.0/agent-source/AgentReferenceResolver';
import { padBook } from '../../../../src/book-2.0/agent-source/padBook';
import { isVoidPseudoAgentReference } from '../../../../src/book-2.0/agent-source/pseudoAgentReferences';
import { validateBook } from '../../../../src/book-2.0/agent-source/string_book';
import { ParseError } from '../../../../src/errors/ParseError';
import { spaceTrim } from '../../../../src/utils/organization/spaceTrim';
import { isValidAgentUrl } from '../../../../src/utils/validators/url/isValidAgentUrl';
import {
    DEFAULT_FEDERATED_AGENT_IMPORT_CONFIGURATION,
    type FederatedAgentImportConfiguration,
} from '../constants/federatedAgentImport';
import {
    type AgentReferenceResolutionIssue,
    consumeAgentReferenceResolutionIssues,
} from './agentReferenceResolver/AgentReferenceResolutionIssue';
import { collectExplicitFromCommitments, getExplicitFromCommitmentContent } from './explicitFromCommitment';
import { type ImportAgentOptions } from './importAgent';
import { importAgentWithFallback } from './importAgentWithFallback';

/**
 * Gets the corpus of an agent source (removes title and trailing status)
 *
 * @param agentSource The agent source
 * @returns The agent source corpus
 */
function getAgentSourceCorpus(agentSource: string_book): string {
    // Remove trailing OPEN or CLOSED if present
    const agentSourceWithoutStatus = agentSource.replace(/\n?(OPEN|CLOSED)\s*$/i, '') as string_book;
    // <- TODO: [🈲] Simple and encapsulated way to get book corpus

    // Remove the first line (title) from agent source
    const agentSourceCorpus = spaceTrim(agentSourceWithoutStatus.replace(/^.*$/m, ''));
    // <- TODO: [🈲] Simple and encapsulated way to get book corpus

    return agentSourceCorpus;
}

/**
 * Formats a resolver issue into a visible NOTE line in resolved agent source.
 *
 * @param issue - Tracked missing-reference issue.
 * @returns Single-line NOTE statement.
 */
function formatResolutionIssueAsNote(issue: AgentReferenceResolutionIssue): string {
    const commitmentType = issue.commitmentType === 'IMPORTS' ? 'IMPORT' : issue.commitmentType;

    if (commitmentType === 'FROM') {
        return `NOTE Referenced agent "${issue.reference}" in FROM commitment was not found. Inheritance skipped.`;
    }

    if (commitmentType === 'IMPORT') {
        return `NOTE Referenced agent "${issue.reference}" in IMPORT commitment was not found. Import skipped.`;
    }

    if (commitmentType === 'TEAM') {
        return `NOTE Referenced agent "${issue.reference}" in TEAM commitment was not found. Teammate disabled.`;
    }

    return `NOTE Referenced agent "${issue.reference}" in ${commitmentType} commitment was not found.`;
}

/**
 * Appends NOTE lines for unresolved references while avoiding duplicates.
 *
 * @param targetChunks - Output chunks being assembled for the resolved book.
 * @param issues - Missing-reference issues to materialize into NOTE lines.
 */
function appendResolutionIssueNotes(
    targetChunks: Array<string>,
    issues: ReadonlyArray<AgentReferenceResolutionIssue>,
): void {
    const seenIssueKeys = new Set<string>();

    for (const issue of issues) {
        const key = `${issue.commitmentType}:${issue.reference}`.toLowerCase();
        if (seenIssueKeys.has(key)) {
            continue;
        }

        seenIssueKeys.add(key);
        targetChunks.push(formatResolutionIssueAsNote(issue), '');
    }
}

/**
 * Inserts NOTE lines right after the title line in a book.
 *
 * @param agentSource - Original agent source.
 * @param notes - NOTE lines to insert.
 * @returns Updated book with notes placed after the title.
 */
function insertNotesAfterTitle(agentSource: string_book, notes: ReadonlyArray<string>): string_book {
    if (notes.length === 0) {
        return agentSource;
    }

    const sourceLines = spaceTrim(agentSource).split(/\r?\n/);
    const titleLine = sourceLines[0] || '';

    /**
     * Parent source body without title/status, ready to be embedded into the child source.
     */
    readonly parentAgentSourceCorpus: string | null;

    /**
     * Unresolved compact-reference issues captured while resolving `FROM`.
     */
    readonly fromResolutionIssues: Array<AgentReferenceResolutionIssue>;

    /**
     * Whether the effective parent closed the current lineage and must be materialized as `FROM @Null`.
     */
    readonly isCyclicFrom: boolean;
};

/**
 * Shared import settings reused for parent and imported-agent loading.
 */
type AgentImportContext = {
    /**
     * Adam agent URL used as the implicit default ancestor.
     */
    readonly adamAgentUrl: string_agent_url;

    /**
     * Resolver used to expand compact references in commitments.
     */
    readonly agentReferenceResolver?: AgentReferenceResolver;

    /**
     * Retry policy used for federated imports.
     */
    readonly federatedAgentImportConfiguration: FederatedAgentImportConfiguration;

    /**
     * Import options propagated to downstream agent loading.
     */
    readonly importAgentOptions: ImportAgentOptions;

    /**
     * Optional importer for same-instance agent sources.
     */
    readonly agentSourceImporter?: AgentSourceImporter;

    /**
     * Original resolution options used for cycle detection.
     */
    readonly resolutionOptions?: ResolveInheritedAgentSourceOptions;
};

/**
 * Result of rewriting the source line by line before final validation.
 */
type ResolvedAgentSourceBuild = {
    /**
     * Rewritten source lines.
     */
    readonly agentSourceChunks: Array<string>;

    /**
     * Whether one explicit `FROM ...` line was materialized during rewriting.
     */
    readonly isFromResolved: boolean;

    /**
     * Remaining unresolved `FROM` issues that still need to be turned into NOTE lines.
     */
    readonly fromResolutionIssues: Array<AgentReferenceResolutionIssue>;
};

/**
 * Result of handling one explicit `FROM ...` line.
 */
type ResolvedFromCommitmentLine = {
    /**
     * Output lines produced for the `FROM` commitment.
     */
    readonly agentSourceChunks: Array<string>;

    /**
     * Remaining `FROM` issues after the line was handled.
     */
    readonly fromResolutionIssues: Array<AgentReferenceResolutionIssue>;
};

/**
 * One cycle found while following a source-resolution lineage.
 *
 * @private internal helper for Agents Server inherited/imported agent resolution
 */
type ResolutionCycle = {
    /**
     * Normalized URL that closes the cycle.
     */
    readonly normalizedReferenceUrl: string_agent_url;

    /**
     * URLs visited before the repeated reference was encountered.
     */
    readonly lineage: ReadonlyArray<string_agent_url>;

    /**
     * Position in `lineage` where the cycle begins.
     */
    readonly cycleStartIndex: number;
};

/**
 * Normalizes agent URLs used for cycle detection and lineage reporting.
 *
 * @param agentUrl - Raw agent URL.
 * @returns URL without trailing slashes.
 */
function normalizeAgentUrl(agentUrl: string_agent_url): string_agent_url {
    return agentUrl.replace(/\/+$/g, '') as string_agent_url;
}

/**
 * Builds the current resolution lineage ending with the agent being resolved now.
 *
 * @param options - Resolution options with current agent metadata.
 * @returns Ordered lineage without empty values.
 */
function createResolutionLineage(options?: ResolveInheritedAgentSourceOptions): Array<string_agent_url> {
    const lineage = [...(options?.inheritancePath || [])];

    if (options?.currentAgentUrl) {
        lineage.push(options.currentAgentUrl);
    }

    for (const alias of options?.currentAgentAliases || []) {
        lineage.push(alias);
    }

    return [...new Set(lineage.map(normalizeAgentUrl))];
}

/**
 * Finds a cycle that would be closed by resolving one more agent reference.
 *
 * @param referenceUrl - Next agent reference to resolve.
 * @param options - Current resolution options.
 * @returns Cycle details, or `null` when the reference is not in the current lineage.
 *
 * @private internal helper for Agents Server inherited/imported agent resolution
 */
function findResolutionCycle(
    referenceUrl: string_agent_url,
    options?: ResolveInheritedAgentSourceOptions,
): ResolutionCycle | null {
    const normalizedReferenceUrl = normalizeAgentUrl(referenceUrl);
    const lineage = createResolutionLineage(options);
    const cycleStartIndex = lineage.findIndex((visitedUrl) => visitedUrl === normalizedReferenceUrl);

    if (cycleStartIndex === -1) {
        return null;
    }

    return { normalizedReferenceUrl, lineage, cycleStartIndex };
}

/**
 * Creates the shared import context used throughout one resolution pass.
 *
 * @param options - Resolution options passed to `resolveInheritedAgentSource`.
 * @returns Normalized shared import context.
 */
function createAgentImportContext(options?: ResolveInheritedAgentSourceOptions): AgentImportContext {
    return {
        adamAgentUrl: options?.adamAgentUrl || 'https://core.ptbk.io/agents/adam',
        agentReferenceResolver: options?.agentReferenceResolver,
        federatedAgentImportConfiguration:
            options?.federatedAgentImportConfiguration || DEFAULT_FEDERATED_AGENT_IMPORT_CONFIGURATION,
        importAgentOptions: {
            recursionLevel: options?.recursionLevel || 0,
            inheritancePath: createResolutionLineage(options),
        },
        agentSourceImporter: options?.agentSourceImporter,
        resolutionOptions: options,
    };
}

/**
 * Computes the effective parent URL after applying explicit `FROM` and Adam fallback rules.
 *
 * @param resolvedParentAgentUrl - Parent URL derived from lightweight commitment parsing.
 * @param hasExplicitFromCommitment - Whether the source explicitly declared `FROM`.
 * @param adamAgentUrl - Default Adam ancestor URL.
 * @param currentAgentUrl - Canonical URL of the source being resolved.
 * @param currentAgentAliases - Additional URLs that identify the source being resolved.
 * @param agentSource - Raw source used for unexpected diagnostics.
 * @returns Effective parent URL or `null` when inheritance is disabled.
 */
function determineParentAgentUrl(
    resolvedParentAgentUrl: string_agent_url | null | undefined,
    hasExplicitFromCommitment: boolean,
    adamAgentUrl: string_agent_url,
    currentAgentUrl?: string_agent_url,
    currentAgentAliases?: ReadonlyArray<string_agent_url>,
    agentSource?: string_book,
): string_agent_url | null {
    if (isValidAgentUrl(resolvedParentAgentUrl)) {
        return resolvedParentAgentUrl as string_agent_url;
    }

    if (resolvedParentAgentUrl === null && hasExplicitFromCommitment) {
        return null;
    }

    if (resolvedParentAgentUrl === undefined || resolvedParentAgentUrl === null) {
        const currentAgentUrls = [currentAgentUrl, ...(currentAgentAliases || [])].filter(
            (agentUrl): agentUrl is string_agent_url => Boolean(agentUrl),
        );
        const isCurrentAgentAdam = currentAgentUrls.some(
            (agentUrl) => normalizeAgentUrl(agentUrl) === normalizeAgentUrl(adamAgentUrl),
        );

        return isCurrentAgentAdam ? null : adamAgentUrl;
    }

    throw new ParseError(
        spaceTrim(
            (block) => `
                Invalid parent agent URL in FROM "${resolvedParentAgentUrl}" commitment:

                \`\`\`book
                ${block(agentSource || '')}
                \`\`\`
            `,
        ),
    );
}

/**
 * Imports one referenced agent and returns only its body content ready to be embedded as a NOTE block.
 *
 * @param agentUrl - Canonical referenced agent URL.
 * @param commitmentType - Commitment that introduced the reference.
 * @param context - Shared import context for the current resolution pass.
 * @returns Imported source body without title/status.
 */
async function importAgentCorpus(
    agentUrl: string_agent_url,
    commitmentType: 'FROM' | 'IMPORT',
    context: AgentImportContext,
): Promise<string> {
    assertNoResolutionCycle(agentUrl, commitmentType, context.resolutionOptions);

    const locallyImportedAgentSource = context.agentSourceImporter
        ? await context.agentSourceImporter(agentUrl, {
              commitmentType,
              importAgentOptions: context.importAgentOptions,
              federatedAgentImportConfiguration: context.federatedAgentImportConfiguration,
          })
        : null;
    const importedAgentSource =
        locallyImportedAgentSource ??
        (await importAgentWithFallback(
            agentUrl,
            context.importAgentOptions,
            context.federatedAgentImportConfiguration,
        ));

    return getAgentSourceCorpus(importedAgentSource as string_book);
}

/**
 * Resolves the effective parent state before the source body is rewritten.
 *
 * @param agentSource - Raw child agent source.
 * @param context - Shared import context for the current resolution pass.
 * @returns Parent URL, imported parent body, and unresolved `FROM` issues.
 */
async function resolveParentAgentContext(
    agentSource: string_book,
    context: AgentImportContext,
): Promise<ResolvedParentAgentContext> {
    const explicitFromContent = getExplicitFromCommitmentContent(agentSource);
    const hasExplicitFromCommitment = explicitFromContent !== undefined;
    const resolvedParentAgentUrl = await resolveParentAgentUrlFromCommitments(
        agentSource,
        context.agentReferenceResolver,
    );
    const fromResolutionIssues = consumeAgentReferenceResolutionIssues(context.agentReferenceResolver).filter(
        (issue) => issue.commitmentType === 'FROM',
    );
    const parentAgentUrl = determineParentAgentUrl(
        resolvedParentAgentUrl,
        hasExplicitFromCommitment,
        context.adamAgentUrl,
        context.resolutionOptions?.currentAgentUrl,
        context.resolutionOptions?.currentAgentAliases,
        agentSource,
    );
    const isCyclicFrom = Boolean(parentAgentUrl && findResolutionCycle(parentAgentUrl, context.resolutionOptions));
    const effectiveParentAgentUrl = isCyclicFrom ? null : parentAgentUrl;
    const parentAgentSourceCorpus = effectiveParentAgentUrl
        ? await importAgentCorpus(effectiveParentAgentUrl, 'FROM', context)
        : null;

    return {
        // A parent that closes the current lineage has the same runtime effect as `FROM @Null`.
        parentAgentUrl: effectiveParentAgentUrl,
        parentAgentSourceCorpus,
        fromResolutionIssues,
        isCyclicFrom,
    };
}

/**
 * Formats one embedded agent body into the NOTE block used by inheritance/import resolution.
 *
 * @param noteLine - First NOTE line describing the embedded source.
 * @param sourceCorpus - Imported source body to embed.
 * @returns Multi-line NOTE block.
 */
function createEmbeddedAgentSourceNote(noteLine: string, sourceCorpus: string): string {
    return spaceTrim(
        (block) => `

            ${noteLine}
            ${block(sourceCorpus)}

            NOTE ===========
    `,
    );
}

/**
 * Resolves one `IMPORT ...` line into embedded source or leaves non-agent imports untouched.
 *
 * @param line - Current source line.
 * @param context - Shared import context for the current resolution pass.
 * @returns Output lines replacing the original line.
 */
async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
    let importedUrlOrPath = rawImportedUrlOrPath;
    let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];

    if (context.agentReferenceResolver && rawImportedUrlOrPath) {
        try {
            importedUrlOrPath = await context.agentReferenceResolver.resolveCommitmentContent(
                'IMPORT',
                rawImportedUrlOrPath,
            );
        } catch (error) {
            console.warn('[AgentReferenceResolver] Failed to resolve IMPORT commitment references:', error);
        } finally {
            importResolutionIssues = consumeAgentReferenceResolutionIssues(context.agentReferenceResolver).filter(
                (issue) => issue.commitmentType === 'IMPORT' || issue.commitmentType === 'IMPORTS',
            );
        }
    }

    const resolvedChunks: Array<string> = [];
    appendResolutionIssueNotes(resolvedChunks, importResolutionIssues);

    if (!importedUrlOrPath) {
        return resolvedChunks;
    }

    if (!isValidAgentUrl(importedUrlOrPath)) {
        resolvedChunks.push(line);
        return resolvedChunks;
    }

    const importedAgentUrl = importedUrlOrPath as string_agent_url;
    const importedAgentSourceCorpus = await importAgentCorpus(importedAgentUrl, 'IMPORT', context);

    resolvedChunks.push(
        createEmbeddedAgentSourceNote(`NOTE Imported from ${importedAgentUrl}`, importedAgentSourceCorpus),
        '', // <- Note: Add an extra newline for separation
    );

    return resolvedChunks;
}

/**
 * Resolves the effective `FROM ...` line into inherited content or fallback NOTE lines.
 *
 * @param line - Current source line.
 * @param parentContext - Effective parent state computed earlier in the resolution.
 * @returns Output lines and remaining `FROM` issues.
 */
function resolveFromCommitmentLine(
    line: string,
    parentContext: ResolvedParentAgentContext,
): ResolvedFromCommitmentLine {
    if (parentContext.parentAgentUrl === null) {
        const agentSourceChunks = [parentContext.isCyclicFrom ? 'FROM @Null' : line];

        if (parentContext.fromResolutionIssues.length > 0) {
            appendResolutionIssueNotes(agentSourceChunks, parentContext.fromResolutionIssues);
        }

        return {
            agentSourceChunks,
            fromResolutionIssues: [],
        };
    }

    if (parentContext.parentAgentSourceCorpus) {
        return {
            agentSourceChunks: [
                createEmbeddedAgentSourceNote(
                    `NOTE Inherited FROM ${parentContext.parentAgentUrl}`,
                    parentContext.parentAgentSourceCorpus,
                ),
                '', // <- Note: Add an extra newline for separation
            ],
            fromResolutionIssues: parentContext.fromResolutionIssues,
        };
    }

    return {
        agentSourceChunks: [
            `NOTE Parent agent "${parentContext.parentAgentUrl}" was not found or could not be loaded. Inheritance skipped.`,
            '',
        ],
        fromResolutionIssues: parentContext.fromResolutionIssues,
    };
}

/**
 * Finds the `FROM` lines that a later `FROM` takes precedence over.
 *
 * A book is allowed to repeat `FROM`; the last one wins, so every earlier one is dropped instead of being resolved.
 * The Book editor warns about this while the author is still writing the book.
 *
 * @param explicitFromCommitments - Explicit parent declarations found in source order.
 * @returns Zero-based indexes of every overridden `FROM` line, empty when at most one `FROM` is present.
 */
function collectOverriddenFromLineIndexes(
    explicitFromCommitments: ReadonlyArray<{ readonly lineIndex: number }>,
): ReadonlySet<number> {
    return new Set(
        explicitFromCommitments.slice(0, -1).map((explicitFromCommitment) => explicitFromCommitment.lineIndex),
    );
}

/**
 * Rewrites the source body line by line while delegating each branching step to a focused helper.
 *
 * @param agentSource - Raw child agent source.
 * @param context - Shared import context for the current resolution pass.
 * @param parentContext - Effective parent state computed earlier in the resolution.
 * @returns Rewritten chunks plus final `FROM` bookkeeping.
 */
async function resolveAgentSourceBuild(
    agentSource: string_book,
    context: AgentImportContext,
    parentContext: ResolvedParentAgentContext,
): Promise<ResolvedAgentSourceBuild> {
    const agentSourceChunks = spaceTrim(agentSource).split(/\r?\n/);
    const resolvedAgentSourceChunks: Array<string> = [];
    const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
    const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
    const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
    let isFromResolved = false;
    let fromResolutionIssues = parentContext.fromResolutionIssues;
    // <- TODO: [🈲] Simple and encapsulated way to split book into commitments

    for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
        const line = agentSourceChunks[lineIndex] || '';

        // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
        if (overriddenFromLineIndexes.has(lineIndex)) {
            continue;
        }

        if (line.trim().startsWith('IMPORT ')) {
            resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
            continue;
        }

        if (effectiveFromCommitment?.lineIndex === lineIndex) {
            const resolvedFromCommitment = resolveFromCommitmentLine(line, {
                ...parentContext,
                fromResolutionIssues,
            });

            resolvedAgentSourceChunks.push(...resolvedFromCommitment.agentSourceChunks);
            fromResolutionIssues = resolvedFromCommitment.fromResolutionIssues;
            isFromResolved = true;
            continue;
        }

        resolvedAgentSourceChunks.push(line);
    }
    // <- TODO: [🈲] Simple and encapsulated way to split book into commitments

    return {
        agentSourceChunks: resolvedAgentSourceChunks,
        isFromResolved,
        fromResolutionIssues,
    };
}

/**
 * Materializes the implicit Adam inheritance block when no explicit `FROM ...` was present.
 *
 * @param build - Current rewritten source build.
 * @param parentContext - Effective parent state computed earlier in the resolution.
 * @param adamAgentUrl - Default Adam ancestor URL.
 * @returns Updated source build with implicit Adam inheritance applied when needed.
 */
function applyImplicitAdamInheritance(
    build: ResolvedAgentSourceBuild,
    parentContext: ResolvedParentAgentContext,
    adamAgentUrl: string_agent_url,
): ResolvedAgentSourceBuild {
    if (build.isFromResolved || parentContext.parentAgentUrl !== adamAgentUrl) {
        return build;
    }

    const titleLine = build.agentSourceChunks[0] || '';
    const restLines = build.agentSourceChunks.slice(1);
    const agentSourceChunks = [titleLine, ''];

    if (parentContext.parentAgentSourceCorpus) {
        agentSourceChunks.push(
            createEmbeddedAgentSourceNote(
                `NOTE Inherited Adam FROM ${parentContext.parentAgentUrl}`,
                parentContext.parentAgentSourceCorpus,
            ),
            ...restLines,
        );
    } else {
        agentSourceChunks.push(
            `NOTE Default parent agent "${parentContext.parentAgentUrl}" was not found or could not be loaded. Inheritance skipped.`,
            '',
            ...restLines,
        );
    }

    return {
        ...build,
        agentSourceChunks,
    };
}

/**
 * Validates and pads the resolved source, then inserts any deferred unresolved-`FROM` notes.
 *
 * @param build - Final rewritten source build.
 * @returns Valid resolved book.
 */
function finalizeResolvedAgentSource(build: ResolvedAgentSourceBuild): string_book {
    const resolvedAgentSource = padBook(validateBook(build.agentSourceChunks.join('\n')));

    if (build.fromResolutionIssues.length === 0) {
        return resolvedAgentSource;
    }

    const unresolvedFromNotes = build.fromResolutionIssues.map(formatResolutionIssueAsNote);
    return insertNotesAfterTitle(resolvedAgentSource, unresolvedFromNotes);
}

/**
 * Throws when a `FROM`/`IMPORT` edge would create an inheritance cycle.
 *
 * @param referenceUrl - Next referenced agent URL.
 * @param commitmentType - Commitment that introduced the reference.
 * @param options - Current resolution options.
 */
function assertNoResolutionCycle(
    referenceUrl: string_agent_url,
    commitmentType: 'FROM' | 'IMPORT',
    options?: ResolveInheritedAgentSourceOptions,
): void {
    const resolutionCycle = findResolutionCycle(referenceUrl, options);

    if (!resolutionCycle) {
        return;
    }

    const cycleChain = [
        ...resolutionCycle.lineage.slice(resolutionCycle.cycleStartIndex),
        resolutionCycle.normalizedReferenceUrl,
    ]
        .map((visitedUrl) => `- \`${visitedUrl}\``)
        .join('\n');

    throw new ParseError(
        spaceTrim(
            (block) => `
                Cyclic \`${commitmentType}\` reference detected while resolving agent source.

                Resolution chain:
                ${block(cycleChain)}
            `,
        ),
    );
}

/**
 * Resolves agent source with inheritance (FROM commitment)
 *
 * It recursively fetches the parent agent source and merges it with the current source.
 *
 * @param agentSource The initial agent source
 * @returns The resolved agent source with inheritance applied
 */
export async function resolveInheritedAgentSource(
    agentSource: string_book,
    options?: ResolveInheritedAgentSourceOptions,
): Promise<string_book> {
    const context = createAgentImportContext(options);
    const parentContext = await resolveParentAgentContext(agentSource, context);
    const resolvedBuild = await resolveAgentSourceBuild(agentSource, context, parentContext);
    const finalizedBuild = applyImplicitAdamInheritance(resolvedBuild, parentContext, context.adamAgentUrl);

    return finalizeResolvedAgentSource(finalizedBuild);
}

// TODO: [🈲] Create a function that can manipulate books by modifying commitments, splitting the book up into commitments or syntactic tokens, and editing or deleting these via object methods.
// TODO: [🐱‍🚀][⏩] This function should be in `/src` and exported from `@promptbook/core`

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content src/book-2.0/agent-source/AgentReferenceResolver.ts; Get-Content apps/agents-server/src/utils/resolveInheritedAgentSource.ts; Get-Content scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts; Get-Content apps/agents-server/src/utils/resolveServerAgentContext.ts -TotalCount 130' in C:\Users\me\work\ai\promptbook
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts; Get-Content src/cli/cli-commands/coder/init.ts; rg --files src apps/agents-server/src/utils/defaultAgents | rg '(resolveInherited|CoreAgent|coreAgent|defaultAgents|AgentCollection|[Ff]rom|[Ii]mport)'" in C:\Users\me\work\ai\promptbook
 succeeded in 1066ms:
import type { InitializationStatus } from './boilerplateTemplates';
import {
    PROMPTS_DIRECTORY_PATH,
    PROMPTS_DONE_DIRECTORY_PATH,
    PROMPTS_TEMPLATES_DIRECTORY_PATH,
} from './boilerplateTemplates';
import type { EnsuredCoderReferencedArtifact } from './coderReferencedArtifacts';
import { ensureCoderReferencedArtifacts } from './coderReferencedArtifacts';
import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
import { ensureCoderEnvFile } from './ensureCoderEnvFile';
import { ensureCoderGitignoreFile } from './ensureCoderGitignoreFile';
import { ensureCoderPackageJsonFile } from './ensureCoderPackageJsonFile';
import { ensureCoderVscodeSettingsFile } from './ensureCoderVscodeSettingsFile';
import { ensureDirectory } from './ensureDirectory';
import { resolveCoderPackageJsonScriptReferencedArtifactPaths } from './getDefaultCoderPackageJsonScripts';

/**
 * Result summary returned after coder configuration initialization.
 *
 * @private internal utility of `coder init` command
 */
export type CoderInitializationSummary = {
    readonly promptsDirectoryStatus: InitializationStatus;
    readonly promptsDoneDirectoryStatus: InitializationStatus;
    readonly promptsTemplatesDirectoryStatus: InitializationStatus;
    readonly agentsDirectoryStatus: InitializationStatus;
    readonly envFileStatus: InitializationStatus;
    readonly gitignoreFileStatus: InitializationStatus;
    readonly packageJsonFileStatus: InitializationStatus;
    readonly vscodeSettingsFileStatus: InitializationStatus;
    readonly addedPackageJsonScriptNames: ReadonlyArray<string>;
    readonly referencedArtifactStatuses: ReadonlyArray<EnsuredCoderReferencedArtifact>;
    readonly initializedEnvVariableNames: ReadonlyArray<string>;
};

/**
 * Creates or updates all coder configuration artifacts required in the current project.
 *
 * Nothing the project already owns is ever overwritten - existing scripts, settings and files are kept as they are,
 * and the artifacts they would reference are created only together with the scripts which actually reference them.
 *
 * @private internal utility of `coder init` command
 */
export async function initializeCoderProjectConfiguration(projectPath: string): Promise<CoderInitializationSummary> {
    const promptsDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DIRECTORY_PATH);
    const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
    const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
    const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
    const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
    const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
    const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
        await ensureCoderPackageJsonFile(projectPath);
    const vscodeSettingsFileStatus = await ensureCoderVscodeSettingsFile(projectPath);
    const referencedArtifactStatuses = await ensureCoderReferencedArtifacts(
        projectPath,
        resolveCoderPackageJsonScriptReferencedArtifactPaths(addedPackageJsonScriptNames),
    );

    return {
        promptsDirectoryStatus,
        promptsDoneDirectoryStatus,
        promptsTemplatesDirectoryStatus,
        agentsDirectoryStatus,
        envFileStatus,
        gitignoreFileStatus,
        packageJsonFileStatus,
        vscodeSettingsFileStatus,
        addedPackageJsonScriptNames,
        referencedArtifactStatuses,
        initializedEnvVariableNames,
    };
}

// Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
import type {
    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
} from 'commander';
import { spaceTrim } from 'spacetrim';
import type { $side_effect } from '../../../utils/organization/$side_effect';
import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
import {
    addCoderGitSyncOptions,
    CODER_GIT_SYNC_DESCRIPTION,
    normalizeCoderGitSyncCliOptions,
} from '../common/coderGitSyncCliOptions';
import { handleActionErrors } from '../common/handleActionErrors';
import { $ensureHarnessInstallations } from '../common/harness/$ensureHarnessInstallations';
import { getHarnessDefinition } from '../common/harness/HarnessDefinition';
import {
    addQuestionsOption,
    normalizeQuestionsCliOptions,
    QUESTIONS_DESCRIPTION,
    type QuestionsCliOptions,
} from '../common/questionsCliOptions';
import type { PromptRunnerHarnessName } from '../common/promptRunnerCliOptions';
import { AGENTS_FILE_PATH } from './agentsFile';
import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
import { isDirectoryEmpty } from './ensureDirectory';
import { formatDisplayPath } from './formatDisplayPath';
import { generatePromptBoilerplate } from './generate-boilerplates';
import { initializeCoderProjectConfiguration } from './initializeCoderProjectConfiguration';
import { printInitializationSummary } from './printInitializationSummary';

export { getDefaultCoderPackageJsonScripts } from './getDefaultCoderPackageJsonScripts';
export { getDefaultCoderVscodeSettings } from './getDefaultCoderVscodeSettings';
export { initializeCoderProjectConfiguration } from './initializeCoderProjectConfiguration';

/**
 * Harnesses whose global installation is checked by `ptbk coder init` so that the initialized
 * project is ready to run coding prompts right away.
 *
 * @private internal constant of `coder init` command
 */
const CODER_INIT_CHECKED_HARNESS_NAMES: ReadonlyArray<PromptRunnerHarnessName> = ['openai-codex', 'claude-code'];

/**
 * Initializes `coder init` command for Promptbook CLI utilities.
 *
 * Note: `$` is used to indicate that this function is not a pure function - it registers a command in the CLI.
 *
 * @private internal function of `promptbookCli`
 */
export function $initializeCoderInitCommand(program: Program): $side_effect {
    const command = program.command('init');
    command.alias('initialize');
    command.description(
        spaceTrim(
            (block) => `
                Initialize Promptbook coder configuration for current project

                Creates or updates:
                - prompts/
                - prompts/done/
                ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                - ${CODER_DEVELOPER_AGENT_FILE_PATH}
                - ${AGENTS_FILE_PATH}
                - .gitignore with local artifacts from every supported harness
                - package.json
                - .vscode/settings.json

                Never overwrites what the project already owns:
                - Existing package.json scripts and .vscode/settings.json settings are kept, only missing ones are added
                - Files referenced by a script, like the agent and context of coder:run, are created only together
                  with the script which references them

                Ensures required coding-agent environment variables in .env:
                - CODING_AGENT_GIT_NAME
                - CODING_AGENT_GIT_EMAIL
                - CODING_AGENT_GIT_SIGNING_KEY

                Checks that the coding harnesses are installed and up to date unless \`--no-questions\` is used:
                ${block(listCheckedHarnessLabels())}

                ${block(CODER_GIT_SYNC_DESCRIPTION)}

                ${block(QUESTIONS_DESCRIPTION)}
            `,
        ),
    );

    addCoderGitSyncOptions(command);
    addQuestionsOption(command);

    command.action(
        handleActionErrors(async (cliOptions) => {
            const gitSync = normalizeCoderGitSyncCliOptions(cliOptions as CoderGitSyncCliOptions);
            const questionsOptions = normalizeQuestionsCliOptions(cliOptions as QuestionsCliOptions);
            const projectPath = process.cwd();

            // Note: Import the git synchronization dynamically to keep the CLI fast for runs without `--commit`
            const { $commitCoderChanges, $startCoderGitSync } = await import(
                '../../../../scripts/run-codex-prompts/git/coderGitSync'
            );

            const commitScope = await $startCoderGitSync({ gitSync, projectPath });
            const isPromptsDirectoryEmpty = await isDirectoryEmpty(projectPath, PROMPTS_DIRECTORY_PATH);

            const summary = await initializeCoderProjectConfiguration(projectPath);
            printInitializationSummary(summary);

            if (isPromptsDirectoryEmpty) {
                await generatePromptBoilerplate({ projectPath, boilerplateCount: DEFAULT_BOILERPLATE_COUNT });
            }

            await $commitCoderChanges({
                gitSync,
                commitScope,
                commitMessage: 'Initialize Promptbook Coder',
            });

            await $ensureHarnessInstallations(CODER_INIT_CHECKED_HARNESS_NAMES, questionsOptions);
        }),
    );
}

/**
 * Lists the harnesses whose global installation is checked by `ptbk coder init`.
 */
function listCheckedHarnessLabels(): string {
    return CODER_INIT_CHECKED_HARNESS_NAMES.map((harnessName) => `- ${getHarnessDefinition(harnessName).label}`).join(
        '\n',
    );
}

/**
 * Lists the project-owned template file paths created by `ptbk coder init`.
 */
function listDefaultCoderProjectPromptTemplateDisplayPaths(): string {
    return getDefaultCoderProjectPromptTemplateDefinitions()
        .map(({ relativeFilePath }) => `- ${formatDisplayPath(relativeFilePath)}`)
        .join('\n');
}

// Note: [🟡] Code for CLI command [init](src/cli/cli-commands/coder/init.ts) should never be published outside of `@promptbook/cli`
// Note: [💞] Ignore a discrepancy between file name and entity name
apps/agents-server/src/utils/defaultAgents\resolveDefaultAgentsStatus.ts
apps/agents-server/src/utils/defaultAgents\resolveDefaultAgentsStatus.test.ts
apps/agents-server/src/utils/defaultAgents\loadDefaultAgentBooks.ts
apps/agents-server/src/utils/defaultAgents\coreAgentExplanations.ts
src\book-2.0\agent-source\removeCommentsFromSystemMessage.ts
src\book-2.0\agent-source\parseAgentSource.import.test.ts
src\book-2.0\agent-source\parseAgentSource\createCapabilitiesFromCommitment.ts
src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.import.test.ts
src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments\augmentAgentModelRequirementsFromSource.ts
src\conversion\validation\_importPipeline.test.ts
src\conversion\utils\extractParameterNamesFromTask.ts
src\conversion\utils\extractParameterNamesFromTask.test.ts
src\utils\validators\url\extractUrlsFromText.ts
src\utils\validators\url\extractUrlsFromText.test.ts
src\utils\chat\decodeChatStreamWhitespaceFromTransport.ts
src\utils\organization\keepTypeImported.ts
src\utils\organization\keepImported.ts
src\utils\normalization\parseKeywordsFromString.ts
src\utils\normalization\parseKeywordsFromString.test.ts
src\utils\misc\FromtoItems.ts
src\utils\markdown\removeAllListItemsFromMarkdown.ts.todo
src\utils\markdown\removeAllBlocksFromMarkdown.ts.todo
src\utils\markdown\extractOneBlockFromMarkdown.ts
src\utils\markdown\extractOneBlockFromMarkdown.test.ts
src\utils\markdown\extractAllListItemsFromMarkdown.ts
src\utils\markdown\extractAllListItemsFromMarkdown.test.ts
src\utils\markdown\extractAllBlocksFromMarkdown.ts
src\utils\markdown\extractAllBlocksFromMarkdown.test.ts
src\utils\markdown\extractAllBlocksFromMarkdown-real.test.ts
src\storage\local-storage\utils\makePromptbookStorageFromWebStorage.ts
src\storage\local-storage\utils\makePromptbookStorageFromIndexedDb.ts
src\collection\pipeline-collection\constructors\createPipelineCollectionFromUrl.ts
src\collection\pipeline-collection\constructors\createPipelineCollectionFromPromise.ts
src\collection\pipeline-collection\constructors\createPipelineCollectionFromPromise.test.ts
src\collection\pipeline-collection\constructors\createPipelineCollectionFromJson.ts
src\collection\pipeline-collection\constructors\createPipelineCollectionFromJson.test.ts
src\collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.ts
src\collection\pipeline-collection\constructors\createPipelineCollectionFromDirectory.test.ts
src\remote-server\startRemoteServer\getExecutionToolsFromIdentification.ts
src\collection\agent-collection\constructors\agent-collection-in-supabase\AgentCollectionInSupabaseOptions.ts
src\collection\agent-collection\constructors\agent-collection-in-supabase\AgentCollectionInSupabase.ts
src\collection\agent-collection\constructors\agent-collection-in-directory\AgentCollectionInDirectory.ts.todo
src\collection\agent-collection\constructors\agent-collection-in-directory\AgentCollectionInDirectory.test.ts.todo
src\collection\agent-collection\AgentCollection.ts
src\cli\common\$hideCliCommandFromHelp.ts
src\cli\common\$hideCliCommandFromHelp.test.ts
src\scripting\javascript\utils\extractVariablesFromScript.test.ts
src\scripting\javascript\utils\extractVariablesFromJavascript.ts
src\scripting\javascript\utils\extractVariablesFromJavascript.test.ts
src\cli\cli-commands\common\npm\extractNpmPackageVersionFromOutput.ts
src\cli\cli-commands\common\npm\extractNpmPackageVersionFromOutput.test.ts
src\cli\cli-commands\common\harness\resolveHarnessInstallationMethodFromPaths.ts
src\cli\cli-commands\common\harness\resolveHarnessInstallationMethodFromPaths.test.ts
src\book-components\Chat\utils\resolveToolCallFromChatMessages.ts
src\book-components\Chat\utils\parseCitationsFromContent.ts
src\book-components\Chat\utils\parseCitationsFromContent.test.ts
src\book-components\Chat\utils\createTeamToolNameFromUrl.ts
src\llm-providers\_common\register\createLlmToolsFromConfiguration.ts
src\llm-providers\_common\register\createLlmToolsFromConfiguration.test.ts
src\book-components\BookEditor\createDuplicateFromCommitmentDiagnostics.ts
src\book-components\BookEditor\createDuplicateFromCommitmentDiagnostics.test.ts
src\book-components\AvatarProfile\AvatarProfile\AvatarProfileFromSource.tsx
src\book-components\AvatarProfile\AvatarChip\AvatarChipFromSource.tsx
src\llm-providers\_common\register\$provideLlmToolsConfigurationFromEnv.ts
src\llm-providers\_common\register\$provideLlmToolsFromEnv.ts
src\llm-providers\vercel\createExecutionToolsFromVercelProvider.ts
src\import-plugins\AgentFileImportPlugin.ts
src\import-plugins\$fileImportPlugins.ts
src\import-plugins\FileImportPlugin.ts
src\import-plugins\JsonFileImportPlugin.ts
src\import-plugins\TextFileImportPlugin.ts

 succeeded in 5848ms:
import type { BookCommitment } from '../../commitments/_base/BookCommitment';
import type { TeammateProfile } from './TeammateProfileResolver';

/**
 * Resolves compact agent references that appear inside FROM, IMPORT, and TEAM commitments.
 *
 * Implementations are expected to rewrite reference tokens such as `{Activation code agent}`
 * or `@Superagent` into concrete agent URLs before the commitment is applied.
 *
 * @private @@@
 */
export type AgentReferenceResolver = {
    /**
     * Normalizes the content of a commitment before it is applied to the model requirements.
     *
     * @param commitmentType - The commitment keyword (e.g. FROM, IMPORT, TEAM)
     * @param content - Original payload of the commitment
     */
    resolveCommitmentContent(commitmentType: BookCommitment, content: string): Promise<string>;

    /**
     * Optional: returns the actual human-readable name and description for a teammate agent URL.
     *
     * When implemented, this enriches TEAM tool definitions with the agent's real name and
     * persona description instead of technical IDs derived from the URL path.
     *
     * @param url - Canonical teammate URL from the resolved TEAM commitment content.
     * @returns Agent profile or `null` when the URL is not resolvable locally.
     */
    resolveTeammateProfile?: (url: string) => Promise<TeammateProfile | null>;
};
import type { string_agent_url, string_book } from '../../../../src/_packages/types.index'; // <- [🚾]
import type { AgentReferenceResolver } from '../../../../src/book-2.0/agent-source/AgentReferenceResolver';
import { padBook } from '../../../../src/book-2.0/agent-source/padBook';
import { isVoidPseudoAgentReference } from '../../../../src/book-2.0/agent-source/pseudoAgentReferences';
import { validateBook } from '../../../../src/book-2.0/agent-source/string_book';
import { ParseError } from '../../../../src/errors/ParseError';
import { spaceTrim } from '../../../../src/utils/organization/spaceTrim';
import { isValidAgentUrl } from '../../../../src/utils/validators/url/isValidAgentUrl';
import {
    DEFAULT_FEDERATED_AGENT_IMPORT_CONFIGURATION,
    type FederatedAgentImportConfiguration,
} from '../constants/federatedAgentImport';
import {
    type AgentReferenceResolutionIssue,
    consumeAgentReferenceResolutionIssues,
} from './agentReferenceResolver/AgentReferenceResolutionIssue';
import { collectExplicitFromCommitments, getExplicitFromCommitmentContent } from './explicitFromCommitment';
import { type ImportAgentOptions } from './importAgent';
import { importAgentWithFallback } from './importAgentWithFallback';

/**
 * Gets the corpus of an agent source (removes title and trailing status)
 *
 * @param agentSource The agent source
 * @returns The agent source corpus
 */
function getAgentSourceCorpus(agentSource: string_book): string {
    // Remove trailing OPEN or CLOSED if present
    const agentSourceWithoutStatus = agentSource.replace(/\n?(OPEN|CLOSED)\s*$/i, '') as string_book;
    // <- TODO: [🈲] Simple and encapsulated way to get book corpus

    // Remove the first line (title) from agent source
    const agentSourceCorpus = spaceTrim(agentSourceWithoutStatus.replace(/^.*$/m, ''));
    // <- TODO: [🈲] Simple and encapsulated way to get book corpus

    return agentSourceCorpus;
}

/**
 * Formats a resolver issue into a visible NOTE line in resolved agent source.
 *
 * @param issue - Tracked missing-reference issue.
 * @returns Single-line NOTE statement.
 */
function formatResolutionIssueAsNote(issue: AgentReferenceResolutionIssue): string {
    const commitmentType = issue.commitmentType === 'IMPORTS' ? 'IMPORT' : issue.commitmentType;

    if (commitmentType === 'FROM') {
        return `NOTE Referenced agent "${issue.reference}" in FROM commitment was not found. Inheritance skipped.`;
    }

    if (commitmentType === 'IMPORT') {
        return `NOTE Referenced agent "${issue.reference}" in IMPORT commitment was not found. Import skipped.`;
    }

    if (commitmentType === 'TEAM') {
        return `NOTE Referenced agent "${issue.reference}" in TEAM commitment was not found. Teammate disabled.`;
    }

    return `NOTE Referenced agent "${issue.reference}" in ${commitmentType} commitment was not found.`;
}

/**
 * Appends NOTE lines for unresolved references while avoiding duplicates.
 *
 * @param targetChunks - Output chunks being assembled for the resolved book.
 * @param issues - Missing-reference issues to materialize into NOTE lines.
 */
function appendResolutionIssueNotes(
    targetChunks: Array<string>,
    issues: ReadonlyArray<AgentReferenceResolutionIssue>,
): void {
    const seenIssueKeys = new Set<string>();

    for (const issue of issues) {
        const key = `${issue.commitmentType}:${issue.reference}`.toLowerCase();
        if (seenIssueKeys.has(key)) {
            continue;
        }

        seenIssueKeys.add(key);
        targetChunks.push(formatResolutionIssueAsNote(issue), '');
    }
}

/**
 * Inserts NOTE lines right after the title line in a book.
 *
 * @param agentSource - Original agent source.
 * @param notes - NOTE lines to insert.
 * @returns Updated book with notes placed after the title.
 */
function insertNotesAfterTitle(agentSource: string_book, notes: ReadonlyArray<string>): string_book {
    if (notes.length === 0) {
        return agentSource;
    }

    const sourceLines = spaceTrim(agentSource).split(/\r?\n/);
    const titleLine = sourceLines[0] || '';
    const restLines = sourceLines.slice(1);
    const nextLines = [titleLine, '', ...notes, '', ...restLines];

    return padBook(validateBook(nextLines.join('\n')));
}

/**
 * Resolves the effective `FROM` parent URL using only lightweight commitment parsing.
 *
 * @param rawAgentSource - Original source used for diagnostics.
 * @param agentReferenceResolver - Optional compact-reference resolver.
 * @returns Valid parent URL, explicit `null` for `FROM VOID`/blank `FROM`, or `undefined` when `FROM` is absent.
 */
async function resolveParentAgentUrlFromCommitments(
    rawAgentSource: string_book,
    agentReferenceResolver?: AgentReferenceResolver,
): Promise<string_agent_url | null | undefined> {
    const explicitFromContent = getExplicitFromCommitmentContent(rawAgentSource);

    if (explicitFromContent === undefined) {
        return undefined;
    }

    let resolvedParentReference = explicitFromContent.trim();

    if (agentReferenceResolver && resolvedParentReference) {
        resolvedParentReference = (
            await agentReferenceResolver.resolveCommitmentContent('FROM', resolvedParentReference)
        ).trim();
    }

    if (!resolvedParentReference || isVoidPseudoAgentReference(resolvedParentReference)) {
        return null;
    }

    if (!isValidAgentUrl(resolvedParentReference)) {
        throw new ParseError(
            spaceTrim(
                (block) => `
                    Invalid parent agent URL in FROM "${resolvedParentReference}" commitment:

                    \`\`\`book
                    ${block(rawAgentSource)}
                    \`\`\`
            
                `,
            ),
        );
    }

    return resolvedParentReference as string_agent_url;
}

/**
 * Shared options for resolving one agent source with inheritance and imports applied.
 */
type ResolveInheritedAgentSourceOptions = ImportAgentOptions & {
    /**
     * The URL of the Adam agent to use as the default ancestor
     *
     * @default 'https://core.ptbk.io/agents/adam'
     */
    readonly adamAgentUrl?: string_agent_url;
    /**
     * Custom resolver used to expand compact agent references.
     */
    readonly agentReferenceResolver?: AgentReferenceResolver;
    /**
     * Canonical URL of the currently resolved agent.
     */
    readonly currentAgentUrl?: string_agent_url;
    /**
     * Additional equivalent URLs that should be treated as the current agent while detecting cycles.
     */
    readonly currentAgentAliases?: ReadonlyArray<string_agent_url>;
    /**
     * Already visited agent URLs in the current resolution stack.
     */
    readonly inheritancePath?: ReadonlyArray<string_agent_url>;
    /**
     * Retry configuration used for federated imported-agent loading.
     */
    readonly federatedAgentImportConfiguration?: FederatedAgentImportConfiguration;
    /**
     * Optional same-instance source importer used before falling back to HTTP import.
     */
    readonly agentSourceImporter?: AgentSourceImporter;
};

/**
 * Context passed to a custom agent source importer.
 */
export type AgentSourceImporterContext = {
    /**
     * Commitment that requested the imported source.
     */
    readonly commitmentType: 'FROM' | 'IMPORT';

    /**
     * Import options propagated from the current resolution pass.
     */
    readonly importAgentOptions: ImportAgentOptions;

    /**
     * Retry configuration used by the default HTTP importer.
     */
    readonly federatedAgentImportConfiguration: FederatedAgentImportConfiguration;
};

/**
 * Imports one agent source before the resolver falls back to the default HTTP importer.
 *
 * Return `null` when the importer does not own the given URL.
 */
export type AgentSourceImporter = (
    agentUrl: string_agent_url,
    context: AgentSourceImporterContext,
) => Promise<string_book | null>;

/**
 * Parent inheritance state derived before rewriting the source body.
 */
type ResolvedParentAgentContext = {
    /**
     * Effective parent URL after resolving explicit `FROM`, `FROM VOID`, or implicit Adam fallback.
     */
    readonly parentAgentUrl: string_agent_url | null;

    /**
     * Parent source body without title/status, ready to be embedded into the child source.
     */
    readonly parentAgentSourceCorpus: string | null;

    /**
     * Unresolved compact-reference issues captured while resolving `FROM`.
     */
    readonly fromResolutionIssues: Array<AgentReferenceResolutionIssue>;

    /**
     * Whether the effective parent closed the current lineage and must be materialized as `FROM @Null`.
     */
    readonly isCyclicFrom: boolean;
};

/**
 * Shared import settings reused for parent and imported-agent loading.
 */
type AgentImportContext = {
    /**
     * Adam agent URL used as the implicit default ancestor.
     */
    readonly adamAgentUrl: string_agent_url;

    /**
     * Resolver used to expand compact references in commitments.
     */
    readonly agentReferenceResolver?: AgentReferenceResolver;

    /**
     * Retry policy used for federated imports.
     */
    readonly federatedAgentImportConfiguration: FederatedAgentImportConfiguration;

    /**
     * Import options propagated to downstream agent loading.
     */
    readonly importAgentOptions: ImportAgentOptions;

    /**
     * Optional importer for same-instance agent sources.
     */
    readonly agentSourceImporter?: AgentSourceImporter;

    /**
     * Original resolution options used for cycle detection.
     */
    readonly resolutionOptions?: ResolveInheritedAgentSourceOptions;
};

/**
 * Result of rewriting the source line by line before final validation.
 */
type ResolvedAgentSourceBuild = {
    /**
     * Rewritten source lines.
     */
    readonly agentSourceChunks: Array<string>;

    /**
     * Whether one explicit `FROM ...` line was materialized during rewriting.
     */
    readonly isFromResolved: boolean;

    /**
     * Remaining unresolved `FROM` issues that still need to be turned into NOTE lines.
     */
    readonly fromResolutionIssues: Array<AgentReferenceResolutionIssue>;
};

/**
 * Result of handling one explicit `FROM ...` line.
 */
type ResolvedFromCommitmentLine = {
    /**
     * Output lines produced for the `FROM` commitment.
     */
    readonly agentSourceChunks: Array<string>;

    /**
     * Remaining `FROM` issues after the line was handled.
     */
    readonly fromResolutionIssues: Array<AgentReferenceResolutionIssue>;
};

/**
 * One cycle found while following a source-resolution lineage.
 *
 * @private internal helper for Agents Server inherited/imported agent resolution
 */
type ResolutionCycle = {
    /**
     * Normalized URL that closes the cycle.
     */
    readonly normalizedReferenceUrl: string_agent_url;

    /**
     * URLs visited before the repeated reference was encountered.
     */
    readonly lineage: ReadonlyArray<string_agent_url>;

    /**
     * Position in `lineage` where the cycle begins.
     */
    readonly cycleStartIndex: number;
};

/**
 * Normalizes agent URLs used for cycle detection and lineage reporting.
 *
 * @param agentUrl - Raw agent URL.
 * @returns URL without trailing slashes.
 */
function normalizeAgentUrl(agentUrl: string_agent_url): string_agent_url {
    return agentUrl.replace(/\/+$/g, '') as string_agent_url;
}

/**
 * Builds the current resolution lineage ending with the agent being resolved now.
 *
 * @param options - Resolution options with current agent metadata.
 * @returns Ordered lineage without empty values.
 */
function createResolutionLineage(options?: ResolveInheritedAgentSourceOptions): Array<string_agent_url> {
    const lineage = [...(options?.inheritancePath || [])];

    if (options?.currentAgentUrl) {
        lineage.push(options.currentAgentUrl);
    }

    for (const alias of options?.currentAgentAliases || []) {
        lineage.push(alias);
    }

    return [...new Set(lineage.map(normalizeAgentUrl))];
}

/**
 * Finds a cycle that would be closed by resolving one more agent reference.
 *
 * @param referenceUrl - Next agent reference to resolve.
 * @param options - Current resolution options.
 * @returns Cycle details, or `null` when the reference is not in the current lineage.
 *
 * @private internal helper for Agents Server inherited/imported agent resolution
 */
function findResolutionCycle(
    referenceUrl: string_agent_url,
    options?: ResolveInheritedAgentSourceOptions,
): ResolutionCycle | null {
    const normalizedReferenceUrl = normalizeAgentUrl(referenceUrl);
    const lineage = createResolutionLineage(options);
    const cycleStartIndex = lineage.findIndex((visitedUrl) => visitedUrl === normalizedReferenceUrl);

    if (cycleStartIndex === -1) {
        return null;
    }

    return { normalizedReferenceUrl, lineage, cycleStartIndex };
}

/**
 * Creates the shared import context used throughout one resolution pass.
 *
 * @param options - Resolution options passed to `resolveInheritedAgentSource`.
 * @returns Normalized shared import context.
 */
function createAgentImportContext(options?: ResolveInheritedAgentSourceOptions): AgentImportContext {
    return {
        adamAgentUrl: options?.adamAgentUrl || 'https://core.ptbk.io/agents/adam',
        agentReferenceResolver: options?.agentReferenceResolver,
        federatedAgentImportConfiguration:
            options?.federatedAgentImportConfiguration || DEFAULT_FEDERATED_AGENT_IMPORT_CONFIGURATION,
        importAgentOptions: {
            recursionLevel: options?.recursionLevel || 0,
            inheritancePath: createResolutionLineage(options),
        },
        agentSourceImporter: options?.agentSourceImporter,
        resolutionOptions: options,
    };
}

/**
 * Computes the effective parent URL after applying explicit `FROM` and Adam fallback rules.
 *
 * @param resolvedParentAgentUrl - Parent URL derived from lightweight commitment parsing.
 * @param hasExplicitFromCommitment - Whether the source explicitly declared `FROM`.
 * @param adamAgentUrl - Default Adam ancestor URL.
 * @param currentAgentUrl - Canonical URL of the source being resolved.
 * @param currentAgentAliases - Additional URLs that identify the source being resolved.
 * @param agentSource - Raw source used for unexpected diagnostics.
 * @returns Effective parent URL or `null` when inheritance is disabled.
 */
function determineParentAgentUrl(
    resolvedParentAgentUrl: string_agent_url | null | undefined,
    hasExplicitFromCommitment: boolean,
    adamAgentUrl: string_agent_url,
    currentAgentUrl?: string_agent_url,
    currentAgentAliases?: ReadonlyArray<string_agent_url>,
    agentSource?: string_book,
): string_agent_url | null {
    if (isValidAgentUrl(resolvedParentAgentUrl)) {
        return resolvedParentAgentUrl as string_agent_url;
    }

    if (resolvedParentAgentUrl === null && hasExplicitFromCommitment) {
        return null;
    }

    if (resolvedParentAgentUrl === undefined || resolvedParentAgentUrl === null) {
        const currentAgentUrls = [currentAgentUrl, ...(currentAgentAliases || [])].filter(
            (agentUrl): agentUrl is string_agent_url => Boolean(agentUrl),
        );
        const isCurrentAgentAdam = currentAgentUrls.some(
            (agentUrl) => normalizeAgentUrl(agentUrl) === normalizeAgentUrl(adamAgentUrl),
        );

        return isCurrentAgentAdam ? null : adamAgentUrl;
    }

    throw new ParseError(
        spaceTrim(
            (block) => `
                Invalid parent agent URL in FROM "${resolvedParentAgentUrl}" commitment:

                \`\`\`book
                ${block(agentSource || '')}
                \`\`\`
            `,
        ),
    );
}

/**
 * Imports one referenced agent and returns only its body content ready to be embedded as a NOTE block.
 *
 * @param agentUrl - Canonical referenced agent URL.
 * @param commitmentType - Commitment that introduced the reference.
 * @param context - Shared import context for the current resolution pass.
 * @returns Imported source body without title/status.
 */
async function importAgentCorpus(
    agentUrl: string_agent_url,
    commitmentType: 'FROM' | 'IMPORT',
    context: AgentImportContext,
): Promise<string> {
    assertNoResolutionCycle(agentUrl, commitmentType, context.resolutionOptions);

    const locallyImportedAgentSource = context.agentSourceImporter
        ? await context.agentSourceImporter(agentUrl, {
              commitmentType,
              importAgentOptions: context.importAgentOptions,
              federatedAgentImportConfiguration: context.federatedAgentImportConfiguration,
          })
        : null;
    const importedAgentSource =
        locallyImportedAgentSource ??
        (await importAgentWithFallback(
            agentUrl,
            context.importAgentOptions,
            context.federatedAgentImportConfiguration,
        ));

    return getAgentSourceCorpus(importedAgentSource as string_book);
}

/**
 * Resolves the effective parent state before the source body is rewritten.
 *
 * @param agentSource - Raw child agent source.
 * @param context - Shared import context for the current resolution pass.
 * @returns Parent URL, imported parent body, and unresolved `FROM` issues.
 */
async function resolveParentAgentContext(
    agentSource: string_book,
    context: AgentImportContext,
): Promise<ResolvedParentAgentContext> {
    const explicitFromContent = getExplicitFromCommitmentContent(agentSource);
    const hasExplicitFromCommitment = explicitFromContent !== undefined;
    const resolvedParentAgentUrl = await resolveParentAgentUrlFromCommitments(
        agentSource,
        context.agentReferenceResolver,
    );
    const fromResolutionIssues = consumeAgentReferenceResolutionIssues(context.agentReferenceResolver).filter(
        (issue) => issue.commitmentType === 'FROM',
    );
    const parentAgentUrl = determineParentAgentUrl(
        resolvedParentAgentUrl,
        hasExplicitFromCommitment,
        context.adamAgentUrl,
        context.resolutionOptions?.currentAgentUrl,
        context.resolutionOptions?.currentAgentAliases,
        agentSource,
    );
    const isCyclicFrom = Boolean(parentAgentUrl && findResolutionCycle(parentAgentUrl, context.resolutionOptions));
    const effectiveParentAgentUrl = isCyclicFrom ? null : parentAgentUrl;
    const parentAgentSourceCorpus = effectiveParentAgentUrl
        ? await importAgentCorpus(effectiveParentAgentUrl, 'FROM', context)
        : null;

    return {
        // A parent that closes the current lineage has the same runtime effect as `FROM @Null`.
        parentAgentUrl: effectiveParentAgentUrl,
        parentAgentSourceCorpus,
        fromResolutionIssues,
        isCyclicFrom,
    };
}

/**
 * Formats one embedded agent body into the NOTE block used by inheritance/import resolution.
 *
 * @param noteLine - First NOTE line describing the embedded source.
 * @param sourceCorpus - Imported source body to embed.
 * @returns Multi-line NOTE block.
 */
function createEmbeddedAgentSourceNote(noteLine: string, sourceCorpus: string): string {
    return spaceTrim(
        (block) => `

            ${noteLine}
            ${block(sourceCorpus)}

            NOTE ===========
    `,
    );
}

/**
 * Resolves one `IMPORT ...` line into embedded source or leaves non-agent imports untouched.
 *
 * @param line - Current source line.
 * @param context - Shared import context for the current resolution pass.
 * @returns Output lines replacing the original line.
 */
async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
    let importedUrlOrPath = rawImportedUrlOrPath;
    let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];

    if (context.agentReferenceResolver && rawImportedUrlOrPath) {
        try {
            importedUrlOrPath = await context.agentReferenceResolver.resolveCommitmentContent(
                'IMPORT',
                rawImportedUrlOrPath,
            );
        } catch (error) {
            console.warn('[AgentReferenceResolver] Failed to resolve IMPORT commitment references:', error);
        } finally {
            importResolutionIssues = consumeAgentReferenceResolutionIssues(context.agentReferenceResolver).filter(
                (issue) => issue.commitmentType === 'IMPORT' || issue.commitmentType === 'IMPORTS',
            );
        }
    }

    const resolvedChunks: Array<string> = [];
    appendResolutionIssueNotes(resolvedChunks, importResolutionIssues);

    if (!importedUrlOrPath) {
        return resolvedChunks;
    }

    if (!isValidAgentUrl(importedUrlOrPath)) {
        resolvedChunks.push(line);
        return resolvedChunks;
    }

    const importedAgentUrl = importedUrlOrPath as string_agent_url;
    const importedAgentSourceCorpus = await importAgentCorpus(importedAgentUrl, 'IMPORT', context);

    resolvedChunks.push(
        createEmbeddedAgentSourceNote(`NOTE Imported from ${importedAgentUrl}`, importedAgentSourceCorpus),
        '', // <- Note: Add an extra newline for separation
    );

    return resolvedChunks;
}

/**
 * Resolves the effective `FROM ...` line into inherited content or fallback NOTE lines.
 *
 * @param line - Current source line.
 * @param parentContext - Effective parent state computed earlier in the resolution.
 * @returns Output lines and remaining `FROM` issues.
 */
function resolveFromCommitmentLine(
    line: string,
    parentContext: ResolvedParentAgentContext,
): ResolvedFromCommitmentLine {
    if (parentContext.parentAgentUrl === null) {
        const agentSourceChunks = [parentContext.isCyclicFrom ? 'FROM @Null' : line];

        if (parentContext.fromResolutionIssues.length > 0) {
            appendResolutionIssueNotes(agentSourceChunks, parentContext.fromResolutionIssues);
        }

        return {
            agentSourceChunks,
            fromResolutionIssues: [],
        };
    }

    if (parentContext.parentAgentSourceCorpus) {
        return {
            agentSourceChunks: [
                createEmbeddedAgentSourceNote(
                    `NOTE Inherited FROM ${parentContext.parentAgentUrl}`,
                    parentContext.parentAgentSourceCorpus,
                ),
                '', // <- Note: Add an extra newline for separation
            ],
            fromResolutionIssues: parentContext.fromResolutionIssues,
        };
    }

    return {
        agentSourceChunks: [
            `NOTE Parent agent "${parentContext.parentAgentUrl}" was not found or could not be loaded. Inheritance skipped.`,
            '',
        ],
        fromResolutionIssues: parentContext.fromResolutionIssues,
    };
}

/**
 * Finds the `FROM` lines that a later `FROM` takes precedence over.
 *
 * A book is allowed to repeat `FROM`; the last one wins, so every earlier one is dropped instead of being resolved.
 * The Book editor warns about this while the author is still writing the book.
 *
 * @param explicitFromCommitments - Explicit parent declarations found in source order.
 * @returns Zero-based indexes of every overridden `FROM` line, empty when at most one `FROM` is present.
 */
function collectOverriddenFromLineIndexes(
    explicitFromCommitments: ReadonlyArray<{ readonly lineIndex: number }>,
): ReadonlySet<number> {
    return new Set(
        explicitFromCommitments.slice(0, -1).map((explicitFromCommitment) => explicitFromCommitment.lineIndex),
    );
}

/**
 * Rewrites the source body line by line while delegating each branching step to a focused helper.
 *
 * @param agentSource - Raw child agent source.
 * @param context - Shared import context for the current resolution pass.
 * @param parentContext - Effective parent state computed earlier in the resolution.
 * @returns Rewritten chunks plus final `FROM` bookkeeping.
 */
async function resolveAgentSourceBuild(
    agentSource: string_book,
    context: AgentImportContext,
    parentContext: ResolvedParentAgentContext,
): Promise<ResolvedAgentSourceBuild> {
    const agentSourceChunks = spaceTrim(agentSource).split(/\r?\n/);
    const resolvedAgentSourceChunks: Array<string> = [];
    const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
    const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
    const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
    let isFromResolved = false;
    let fromResolutionIssues = parentContext.fromResolutionIssues;
    // <- TODO: [🈲] Simple and encapsulated way to split book into commitments

    for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
        const line = agentSourceChunks[lineIndex] || '';

        // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
        if (overriddenFromLineIndexes.has(lineIndex)) {
            continue;
        }

        if (line.trim().startsWith('IMPORT ')) {
            resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
            continue;
        }

        if (effectiveFromCommitment?.lineIndex === lineIndex) {
            const resolvedFromCommitment = resolveFromCommitmentLine(line, {
                ...parentContext,
                fromResolutionIssues,
            });

            resolvedAgentSourceChunks.push(...resolvedFromCommitment.agentSourceChunks);
            fromResolutionIssues = resolvedFromCommitment.fromResolutionIssues;
            isFromResolved = true;
            continue;
        }

        resolvedAgentSourceChunks.push(line);
    }
    // <- TODO: [🈲] Simple and encapsulated way to split book into commitments

    return {
        agentSourceChunks: resolvedAgentSourceChunks,
        isFromResolved,
        fromResolutionIssues,
    };
}

/**
 * Materializes the implicit Adam inheritance block when no explicit `FROM ...` was present.
 *
 * @param build - Current rewritten source build.
 * @param parentContext - Effective parent state computed earlier in the resolution.
 * @param adamAgentUrl - Default Adam ancestor URL.
 * @returns Updated source build with implicit Adam inheritance applied when needed.
 */
function applyImplicitAdamInheritance(
    build: ResolvedAgentSourceBuild,
    parentContext: ResolvedParentAgentContext,
    adamAgentUrl: string_agent_url,
): ResolvedAgentSourceBuild {
    if (build.isFromResolved || parentContext.parentAgentUrl !== adamAgentUrl) {
        return build;
    }

    const titleLine = build.agentSourceChunks[0] || '';
    const restLines = build.agentSourceChunks.slice(1);
    const agentSourceChunks = [titleLine, ''];

    if (parentContext.parentAgentSourceCorpus) {
        agentSourceChunks.push(
            createEmbeddedAgentSourceNote(
                `NOTE Inherited Adam FROM ${parentContext.parentAgentUrl}`,
                parentContext.parentAgentSourceCorpus,
            ),
            ...restLines,
        );
    } else {
        agentSourceChunks.push(
            `NOTE Default parent agent "${parentContext.parentAgentUrl}" was not found or could not be loaded. Inheritance skipped.`,
            '',
            ...restLines,
        );
    }

    return {
        ...build,
        agentSourceChunks,
    };
}

/**
 * Validates and pads the resolved source, then inserts any deferred unresolved-`FROM` notes.
 *
 * @param build - Final rewritten source build.
 * @returns Valid resolved book.
 */
function finalizeResolvedAgentSource(build: ResolvedAgentSourceBuild): string_book {
    const resolvedAgentSource = padBook(validateBook(build.agentSourceChunks.join('\n')));

    if (build.fromResolutionIssues.length === 0) {
        return resolvedAgentSource;
    }

    const unresolvedFromNotes = build.fromResolutionIssues.map(formatResolutionIssueAsNote);
    return insertNotesAfterTitle(resolvedAgentSource, unresolvedFromNotes);
}

/**
 * Throws when a `FROM`/`IMPORT` edge would create an inheritance cycle.
 *
 * @param referenceUrl - Next referenced agent URL.
 * @param commitmentType - Commitment that introduced the reference.
 * @param options - Current resolution options.
 */
function assertNoResolutionCycle(
    referenceUrl: string_agent_url,
    commitmentType: 'FROM' | 'IMPORT',
    options?: ResolveInheritedAgentSourceOptions,
): void {
    const resolutionCycle = findResolutionCycle(referenceUrl, options);

    if (!resolutionCycle) {
        return;
    }

    const cycleChain = [
        ...resolutionCycle.lineage.slice(resolutionCycle.cycleStartIndex),
        resolutionCycle.normalizedReferenceUrl,
    ]
        .map((visitedUrl) => `- \`${visitedUrl}\``)
        .join('\n');

    throw new ParseError(
        spaceTrim(
            (block) => `
                Cyclic \`${commitmentType}\` reference detected while resolving agent source.

                Resolution chain:
                ${block(cycleChain)}
            `,
        ),
    );
}

/**
 * Resolves agent source with inheritance (FROM commitment)
 *
 * It recursively fetches the parent agent source and merges it with the current source.
 *
 * @param agentSource The initial agent source
 * @returns The resolved agent source with inheritance applied
 */
export async function resolveInheritedAgentSource(
    agentSource: string_book,
    options?: ResolveInheritedAgentSourceOptions,
): Promise<string_book> {
    const context = createAgentImportContext(options);
    const parentContext = await resolveParentAgentContext(agentSource, context);
    const resolvedBuild = await resolveAgentSourceBuild(agentSource, context, parentContext);
    const finalizedBuild = applyImplicitAdamInheritance(resolvedBuild, parentContext, context.adamAgentUrl);

    return finalizeResolvedAgentSource(finalizedBuild);
}

// TODO: [🈲] Create a function that can manipulate books by modifying commitments, splitting the book up into commitments or syntactic tokens, and editing or deleting these via object methods.
// TODO: [🐱‍🚀][⏩] This function should be in `/src` and exported from `@promptbook/core`
import { spaceTrim } from 'spacetrim';
import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
import type { string_book } from '../../../src/book-2.0/agent-source/string_book';

/**
 * Compiles agent source into the behavior text passed to CLI harness runners.
 */
export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
}

/**
 * Formats prepared model requirements for a text-only CLI harness prompt.
 */
export function formatAgentModelRequirementsForRunner(modelRequirements: AgentModelRequirements): string {
    const promptSuffix = modelRequirements.promptSuffix.trim();

    return [
        modelRequirements.systemMessage.trim(),
        promptSuffix
            ? spaceTrim(
                  (block) => `
                      ## Prompt suffix
                      ${block(promptSuffix)}
                  `,
              )
            : '',
    ]
        .filter(Boolean)
        .join('\n\n');
}
import type { AgentReferenceResolver } from '../../../../src/book-2.0/agent-source/AgentReferenceResolver';
import type { AgentCollection } from '../../../../src/collection/agent-collection/AgentCollection';
import { resolveBookScopedAgentContext, type ResolvedBookScopedAgentContext } from './agentReferenceResolver/bookScopedAgentReferences';
import { createLocalAgentSourceImporter } from './createLocalAgentSourceImporter';
import { loadFederatedAgentImportConfiguration } from './federatedAgentImportConfiguration';
import { getWellKnownAgentUrl } from './getWellKnownAgentUrl';
import { resolveAgentStateFromSource, type ResolvedAgentState } from './resolveAgentStateFromSource';

/**
 * Fully resolved server-side agent context used by runtime, metadata, and API consumers.
 */
export type ResolvedServerAgentContext = ResolvedBookScopedAgentContext & ResolvedAgentState;

/**
 * Options for resolving one route/runtime agent identifier to canonical state.
 */
export type ResolveServerAgentContextOptions = {
    /**
     * Source collection used to load local agent sources.
     */
    readonly collection: AgentCollection;

    /**
     * Requested agent identifier (agent name, permanent id, or book-scoped id).
     */
    readonly agentIdentifier: string;

    /**
     * Public/current server origin used for local agent URLs.
     */
    readonly localServerUrl: string;

    /**
     * Optional fallback resolver for compact local/federated references.
     */
    readonly fallbackResolver?: AgentReferenceResolver;
};

/**
 * Resolves local/book-scoped context first, then derives the canonical resolved source/profile.
 *
 * @param options - Context dependencies for the current server/runtime request.
 * @returns Fully resolved agent context.
 */
export async function resolveServerAgentContext(
    options: ResolveServerAgentContextOptions,
): Promise<ResolvedServerAgentContext> {
    const bookScopedAgentContext = await resolveBookScopedAgentContext(options);
    const federatedAgentImportConfiguration = await loadFederatedAgentImportConfiguration();
    const adamAgentUrl = await getWellKnownAgentUrl('ADAM');
    const agentSourceImporter = createLocalAgentSourceImporter({
        collection: options.collection,
        localServerUrls: [options.localServerUrl],
        localAgentUrls: [adamAgentUrl],
        adamAgentUrl,
        fallbackResolver: options.fallbackResolver,
        federatedAgentImportConfiguration,
    });
    const resolvedAgentState = await resolveAgentStateFromSource(bookScopedAgentContext.unresolvedAgentSource, {
        adamAgentUrl,
        canonicalAgentUrl: bookScopedAgentContext.canonicalAgentUrl,
        currentAgentAliases: bookScopedAgentContext.currentAgentAliases,
        agentReferenceResolver: bookScopedAgentContext.scopedAgentReferenceResolver,
        federatedAgentImportConfiguration,
        agentSourceImporter,
    });

    return {
        ...bookScopedAgentContext,
        ...resolvedAgentState,
    };
}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content apps/agents-server/src/utils/resolveInheritedAgentSource.ts | Select-Object -Skip 185 -First 215; Get-Content apps/agents-server/src/utils/importAgent.ts; Get-Content apps/agents-server/src/utils/importAgentWithFallback.ts -TotalCount 120' in C:\Users\me\work\ai\promptbook
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content apps/agents-server/src/utils/resolveInheritedAgentSource.ts | Select-Object -Skip 400 -First 225; Get-Content apps/agents-server/src/utils/explicitFromCommitment.ts; Get-Content apps/agents-server/src/utils/defaultAgents/loadDefaultAgentBooks.ts -TotalCount 115; Get-Content src/cli/cli-commands/coder/ensureCoderDeveloperAgentFile.ts; Get-Content src/cli/cli-commands/coder/coderReferencedArtifacts.ts -TotalCount 130' in C:\Users\me\work\ai\promptbook
 succeeded in 5405ms:
    readonly agentSourceImporter?: AgentSourceImporter;
};

/**
 * Context passed to a custom agent source importer.
 */
export type AgentSourceImporterContext = {
    /**
     * Commitment that requested the imported source.
     */
    readonly commitmentType: 'FROM' | 'IMPORT';

    /**
     * Import options propagated from the current resolution pass.
     */
    readonly importAgentOptions: ImportAgentOptions;

    /**
     * Retry configuration used by the default HTTP importer.
     */
    readonly federatedAgentImportConfiguration: FederatedAgentImportConfiguration;
};

/**
 * Imports one agent source before the resolver falls back to the default HTTP importer.
 *
 * Return `null` when the importer does not own the given URL.
 */
export type AgentSourceImporter = (
    agentUrl: string_agent_url,
    context: AgentSourceImporterContext,
) => Promise<string_book | null>;

/**
 * Parent inheritance state derived before rewriting the source body.
 */
type ResolvedParentAgentContext = {
    /**
     * Effective parent URL after resolving explicit `FROM`, `FROM VOID`, or implicit Adam fallback.
     */
    readonly parentAgentUrl: string_agent_url | null;

    /**
     * Parent source body without title/status, ready to be embedded into the child source.
     */
    readonly parentAgentSourceCorpus: string | null;

    /**
     * Unresolved compact-reference issues captured while resolving `FROM`.
     */
    readonly fromResolutionIssues: Array<AgentReferenceResolutionIssue>;

    /**
     * Whether the effective parent closed the current lineage and must be materialized as `FROM @Null`.
     */
    readonly isCyclicFrom: boolean;
};

/**
 * Shared import settings reused for parent and imported-agent loading.
 */
type AgentImportContext = {
    /**
     * Adam agent URL used as the implicit default ancestor.
     */
    readonly adamAgentUrl: string_agent_url;

    /**
     * Resolver used to expand compact references in commitments.
     */
    readonly agentReferenceResolver?: AgentReferenceResolver;

    /**
     * Retry policy used for federated imports.
     */
    readonly federatedAgentImportConfiguration: FederatedAgentImportConfiguration;

    /**
     * Import options propagated to downstream agent loading.
     */
    readonly importAgentOptions: ImportAgentOptions;

    /**
     * Optional importer for same-instance agent sources.
     */
    readonly agentSourceImporter?: AgentSourceImporter;

    /**
     * Original resolution options used for cycle detection.
     */
    readonly resolutionOptions?: ResolveInheritedAgentSourceOptions;
};

/**
 * Result of rewriting the source line by line before final validation.
 */
type ResolvedAgentSourceBuild = {
    /**
     * Rewritten source lines.
     */
    readonly agentSourceChunks: Array<string>;

    /**
     * Whether one explicit `FROM ...` line was materialized during rewriting.
     */
    readonly isFromResolved: boolean;

    /**
     * Remaining unresolved `FROM` issues that still need to be turned into NOTE lines.
     */
    readonly fromResolutionIssues: Array<AgentReferenceResolutionIssue>;
};

/**
 * Result of handling one explicit `FROM ...` line.
 */
type ResolvedFromCommitmentLine = {
    /**
     * Output lines produced for the `FROM` commitment.
     */
    readonly agentSourceChunks: Array<string>;

    /**
     * Remaining `FROM` issues after the line was handled.
     */
    readonly fromResolutionIssues: Array<AgentReferenceResolutionIssue>;
};

/**
 * One cycle found while following a source-resolution lineage.
 *
 * @private internal helper for Agents Server inherited/imported agent resolution
 */
type ResolutionCycle = {
    /**
     * Normalized URL that closes the cycle.
     */
    readonly normalizedReferenceUrl: string_agent_url;

    /**
     * URLs visited before the repeated reference was encountered.
     */
    readonly lineage: ReadonlyArray<string_agent_url>;

    /**
     * Position in `lineage` where the cycle begins.
     */
    readonly cycleStartIndex: number;
};

/**
 * Normalizes agent URLs used for cycle detection and lineage reporting.
 *
 * @param agentUrl - Raw agent URL.
 * @returns URL without trailing slashes.
 */
function normalizeAgentUrl(agentUrl: string_agent_url): string_agent_url {
    return agentUrl.replace(/\/+$/g, '') as string_agent_url;
}

/**
 * Builds the current resolution lineage ending with the agent being resolved now.
 *
 * @param options - Resolution options with current agent metadata.
 * @returns Ordered lineage without empty values.
 */
function createResolutionLineage(options?: ResolveInheritedAgentSourceOptions): Array<string_agent_url> {
    const lineage = [...(options?.inheritancePath || [])];

    if (options?.currentAgentUrl) {
        lineage.push(options.currentAgentUrl);
    }

    for (const alias of options?.currentAgentAliases || []) {
        lineage.push(alias);
    }

    return [...new Set(lineage.map(normalizeAgentUrl))];
}

/**
 * Finds a cycle that would be closed by resolving one more agent reference.
 *
 * @param referenceUrl - Next agent reference to resolve.
 * @param options - Current resolution options.
 * @returns Cycle details, or `null` when the reference is not in the current lineage.
 *
 * @private internal helper for Agents Server inherited/imported agent resolution
 */
function findResolutionCycle(
    referenceUrl: string_agent_url,
    options?: ResolveInheritedAgentSourceOptions,
): ResolutionCycle | null {
    const normalizedReferenceUrl = normalizeAgentUrl(referenceUrl);
    const lineage = createResolutionLineage(options);
    const cycleStartIndex = lineage.findIndex((visitedUrl) => visitedUrl === normalizedReferenceUrl);

    if (cycleStartIndex === -1) {
        return null;
    }

    return { normalizedReferenceUrl, lineage, cycleStartIndex };
}

/**
 * Creates the shared import context used throughout one resolution pass.
 *
 * @param options - Resolution options passed to `resolveInheritedAgentSource`.
 * @returns Normalized shared import context.
 */
function createAgentImportContext(options?: ResolveInheritedAgentSourceOptions): AgentImportContext {
    return {
        adamAgentUrl: options?.adamAgentUrl || 'https://core.ptbk.io/agents/adam',
        agentReferenceResolver: options?.agentReferenceResolver,
        federatedAgentImportConfiguration:
import {
    string_agent_name,
    string_agent_permanent_id,
    string_agent_url,
    string_book,
    TODO_any,
} from '../../../../src/_packages/types.index'; // <- [🚾]
import { spaceTrim } from 'spacetrim';
import { assertsError } from '../../../../src/errors/assertsError';
import { NotYetImplementedError } from '../../../../src/errors/NotYetImplementedError';
import { deserializeError } from '../../../../src/errors/utils/deserializeError';
import { keepUnused } from '../../../../src/utils/organization/keepUnused';
import { isValidUrl } from '../../../../src/utils/validators/url/isValidUrl';

/**
 * @@@
 */
export type ImportAgentOptions = {
    /**
     * The current recursion level
     *
     * Used to prevent infinite loops when resolving inherited agent sources
     *
     * @default 0
     */
    recursionLevel?: number;
    /**
     * Already visited canonical agent URLs used for remote cycle detection.
     */
    readonly inheritancePath?: ReadonlyArray<string_agent_url>;
};

/**
 * Cached successful remote agent import payload.
 */
type ImportedAgentCacheRecord = {
    /**
     * Last successfully fetched agent source.
     */
    readonly source: string_book;

    /**
     * Last observed ETag returned by the remote `/api/book` endpoint.
     */
    readonly etag: string | null;

    /**
     * Last observed Last-Modified returned by the remote endpoint.
     */
    readonly lastModified: string | null;
};

/**
 * Successful remote imports cached by canonical agent identifier.
 */
const IMPORTED_AGENT_CACHE = new Map<string, ImportedAgentCacheRecord>();

/**
 * In-flight remote imports deduplicated by canonical agent identifier.
 */
const PENDING_IMPORTED_AGENT_REQUESTS = new Map<string, Promise<string_book>>();

/**
 * Builds the remote URL used to fetch a canonical agent book.
 *
 * @param agentIdentification - Agent page URL or direct book URL.
 * @param options - Import options controlling recursion metadata.
 * @returns Fetch URL with recursion/cycle-detection query parameters.
 */
function createAgentBookUrl(
    agentIdentification: string_agent_name | string_agent_permanent_id | string_agent_url,
    options: ImportAgentOptions,
): string {
    if (agentIdentification.endsWith('.book') || agentIdentification.endsWith('.md')) {
        return agentIdentification;
    }

    const recursionLevel = options.recursionLevel || 0;
    const baseUrl = agentIdentification.includes('/api/book')
        ? new URL(agentIdentification)
        : new URL(`${agentIdentification.replace(/\/$/, '')}/api/book`);

    baseUrl.searchParams.set('recursionLevel', String(recursionLevel + 1));

    for (const visitedAgentUrl of options.inheritancePath || []) {
        baseUrl.searchParams.append('resolutionPath', visitedAgentUrl);
    }

    return baseUrl.href;
}

/**
 * Returns a stable cache key for one imported agent.
 *
 * @param agentIdentification - Agent identifier provided to `importAgent`.
 * @returns Stable cache key reused across conditional fetches.
 */
function createImportCacheKey(
    agentIdentification: string_agent_name | string_agent_permanent_id | string_agent_url,
): string {
    return agentIdentification.replace(/\/+$/g, '');
}

/**
 * Extracts one text/book payload from a successful HTTP response.
 *
 * @param agentIdentification - Original agent identifier for diagnostics.
 * @param response - Successful HTTP response.
 * @returns Imported agent source text.
 */
async function readImportedAgentSource(
    agentIdentification: string_agent_name | string_agent_permanent_id | string_agent_url,
    response: Response,
): Promise<string_book> {
    const contentType: string | null = response.headers.get('content-type');
    if (contentType && contentType.includes('application/json')) {
        const data: TODO_any = await response.json();
        if (typeof data === 'string') {
            return data as string_book;
        } else if (data.source) {
            return data.source as string_book;
        } else {
            console.warn(
                `Received JSON from ${agentIdentification} but couldn't determine source property. Using text.`,
            );
            throw new Error(`Received JSON from ${agentIdentification} but structure is unknown.`);
        }
    }

    return (await response.text()) as string_book;
}

/**
 * Imports an agent by its URL or name
 *
 * @param agentUrlOrName The identifier  of the agent to import
 * @returns The resolved agent source with inheritance applied
 */
export async function importAgent(
    agentIdentification: string_agent_name | string_agent_permanent_id | string_agent_url,
    options?: ImportAgentOptions,
): Promise<string_book> {
    console.log(`importAgent "${agentIdentification}"`);

    const { recursionLevel = 0 }: ImportAgentOptions = options || {};

    if (!isValidUrl(agentIdentification)) {
        throw new NotYetImplementedError(`[🏠] Importing local agents be name or permanent id is not implemented yet`);
    }

    const importOptions = {
        recursionLevel,
        inheritancePath: options?.inheritancePath,
    } satisfies ImportAgentOptions;
    const cacheKey = createImportCacheKey(agentIdentification);
    const cachedImport = IMPORTED_AGENT_CACHE.get(cacheKey);
    const existingRequest = PENDING_IMPORTED_AGENT_REQUESTS.get(cacheKey);
    if (existingRequest) {
        return existingRequest;
    }

    const pendingRequest = (async (): Promise<string_book> => {
        try {
            const agentBookUrl = createAgentBookUrl(agentIdentification, importOptions);
            const headers = new Headers();

            if (cachedImport?.etag) {
                headers.set('If-None-Match', cachedImport.etag);
            }

            if (!cachedImport?.etag && cachedImport?.lastModified) {
                headers.set('If-Modified-Since', cachedImport.lastModified);
            }

            const response: Response = await fetch(agentBookUrl, {
                cache: 'no-store',
                headers,
            });

            if (response.status === 304 && cachedImport) {
                return cachedImport.source;
            }

            if (!response.ok) {
                let error: Error | null = null;
                try {
                    const body: TODO_any = await response.json();
                    error = deserializeError(body, false);
                } catch (error: TODO_any) {
                    keepUnused(error);
                } finally {
                    if (error === null) {
                        error = new Error(
                            `Failed to fetch parent agent from ${agentBookUrl}: ${response.status} ${response.statusText}`,
                        );
                    }
                }

                throw error;
            }

            const source = await readImportedAgentSource(agentIdentification, response);
            IMPORTED_AGENT_CACHE.set(cacheKey, {
                source,
                etag: response.headers.get('etag'),
                lastModified: response.headers.get('last-modified'),
            });

            return source;
        } catch (error) {
            assertsError(error);
            const originalMessage = error.message;

            error.message = spaceTrim(
                (block) => `
                    Failed to import agent from "${agentIdentification}"

                    ${block(originalMessage)}
                `,
            );
            throw error;
        } finally {
            PENDING_IMPORTED_AGENT_REQUESTS.delete(cacheKey);
        }
    })();

    PENDING_IMPORTED_AGENT_REQUESTS.set(cacheKey, pendingRequest);

    try {
        return await pendingRequest;
    } catch (error) {
        assertsError(error);

        throw error;

        /*

        throw new NotFoundError(
            spaceTrim(
                (block) => `
                    Failed to import agent from "${agentIdentification}"
                    
                    Raw error message:
                    ${block((error as Error).message)}
                `,
            ),
        );
        */
    }
}

// TODO: [🐱‍🚀][⏩] This function should be in `/src` and exported from `@promptbook/core`
// TODO: [🐱‍🚀][🏠] Implement local requesting agents by name and permanent ID
import type { string_agent_url } from '../../../../src/types/typeAliases';
import type { string_book } from '../../../../src/book-2.0/agent-source/string_book';
import {
    DEFAULT_FEDERATED_AGENT_IMPORT_CONFIGURATION,
    type FederatedAgentImportConfiguration,
} from '../constants/federatedAgentImport';
import { createMissingImportedAgentFallback } from './createMissingImportedAgentFallback';
import { retryWithBackoff } from './retryWithBackoff';
import { importAgent, type ImportAgentOptions } from './importAgent';

/**
 * Cached failed-import fallback payload reused for a short time to avoid
 * retrying the same unavailable remote agent on every navigation.
 */
type FailedImportedAgentFallbackCacheRecord = {
    /**
     * Generated fallback book returned for the failed import.
     */
    readonly fallbackSource: string_book;
    /**
     * Cache expiration timestamp in epoch milliseconds.
     */
    readonly expiresAt: number;
};

/**
 * Negative-cache lifetime for failed imported-agent fetches.
 */
const FAILED_IMPORTED_AGENT_FALLBACK_CACHE_TTL_MS = 60_000;

/**
 * Failed imported-agent fallback books keyed by canonical agent URL.
 */
const cachedFailedImportedAgentFallbackByKey = new Map<string, FailedImportedAgentFallbackCacheRecord>();

/**
 * In-flight import-with-fallback resolutions keyed by canonical agent URL.
 */
const pendingImportedAgentFallbackByKey = new Map<string, Promise<string_book>>();

/**
 * Builds the stable cache key used for import-with-fallback memoization.
 *
 * @param agentUrl - Canonical imported agent URL.
 * @returns Stable cache key without trailing slashes.
 */
function createImportedAgentFallbackCacheKey(agentUrl: string_agent_url): string {
    return agentUrl.replace(/\/+$/g, '');
}

/**
 * Reads one cached failed-import fallback while it is still fresh.
 *
 * @param cacheKey - Canonical failed-import cache key.
 * @returns Cached fallback source or `null` when unavailable.
 */
function readCachedFailedImportedAgentFallback(cacheKey: string): string_book | null {
    const cachedFallback = cachedFailedImportedAgentFallbackByKey.get(cacheKey);
    if (!cachedFallback) {
        return null;
    }

    if (cachedFallback.expiresAt <= Date.now()) {
        cachedFailedImportedAgentFallbackByKey.delete(cacheKey);
        return null;
    }

    return cachedFallback.fallbackSource;
}

/**
 * Stores one generated fallback source for short-lived reuse after repeated import failures.
 *
 * @param cacheKey - Canonical failed-import cache key.
 * @param fallbackSource - Generated fallback source.
 */
function writeCachedFailedImportedAgentFallback(cacheKey: string, fallbackSource: string_book): void {
    cachedFailedImportedAgentFallbackByKey.set(cacheKey, {
        fallbackSource,
        expiresAt: Date.now() + FAILED_IMPORTED_AGENT_FALLBACK_CACHE_TTL_MS,
    });
}

/**
 * Loads one imported agent with bounded retries and falls back to a valid book when loading fails.
 *
 * @param agentUrl - Canonical imported agent URL.
 * @param options - Import options forwarded to the strict importer.
 * @param configuration - Retry configuration for federated agent imports.
 * @returns Imported source or the ad-hoc fallback book.
 *
 * @private internal helper for Agents Server inherited/imported agent resolution
 */
export async function importAgentWithFallback(
    agentUrl: string_agent_url,
    options: ImportAgentOptions,
    configuration: FederatedAgentImportConfiguration = DEFAULT_FEDERATED_AGENT_IMPORT_CONFIGURATION,
): Promise<string_book> {
    const cacheKey = createImportedAgentFallbackCacheKey(agentUrl);
    const cachedFailedFallback = readCachedFailedImportedAgentFallback(cacheKey);
    if (cachedFailedFallback) {
        return cachedFailedFallback;
    }

    const pendingImport = pendingImportedAgentFallbackByKey.get(cacheKey);
    if (pendingImport) {
        return pendingImport;
    }

    const nextPendingImport = (async (): Promise<string_book> => {
        try {
            const result = await retryWithBackoff(
                () => importAgent(agentUrl, options),
                {
                    retries: Math.max(0, configuration.maxAttempts - 1),
                    initialDelayMs: configuration.retryDelayMs,
                    maxDelayMs: configuration.retryDelayMs,
                    backoffFactor: 1,
                    jitterRatio: 0,
                },

 succeeded in 5525ms:
            options?.federatedAgentImportConfiguration || DEFAULT_FEDERATED_AGENT_IMPORT_CONFIGURATION,
        importAgentOptions: {
            recursionLevel: options?.recursionLevel || 0,
            inheritancePath: createResolutionLineage(options),
        },
        agentSourceImporter: options?.agentSourceImporter,
        resolutionOptions: options,
    };
}

/**
 * Computes the effective parent URL after applying explicit `FROM` and Adam fallback rules.
 *
 * @param resolvedParentAgentUrl - Parent URL derived from lightweight commitment parsing.
 * @param hasExplicitFromCommitment - Whether the source explicitly declared `FROM`.
 * @param adamAgentUrl - Default Adam ancestor URL.
 * @param currentAgentUrl - Canonical URL of the source being resolved.
 * @param currentAgentAliases - Additional URLs that identify the source being resolved.
 * @param agentSource - Raw source used for unexpected diagnostics.
 * @returns Effective parent URL or `null` when inheritance is disabled.
 */
function determineParentAgentUrl(
    resolvedParentAgentUrl: string_agent_url | null | undefined,
    hasExplicitFromCommitment: boolean,
    adamAgentUrl: string_agent_url,
    currentAgentUrl?: string_agent_url,
    currentAgentAliases?: ReadonlyArray<string_agent_url>,
    agentSource?: string_book,
): string_agent_url | null {
    if (isValidAgentUrl(resolvedParentAgentUrl)) {
        return resolvedParentAgentUrl as string_agent_url;
    }

    if (resolvedParentAgentUrl === null && hasExplicitFromCommitment) {
        return null;
    }

    if (resolvedParentAgentUrl === undefined || resolvedParentAgentUrl === null) {
        const currentAgentUrls = [currentAgentUrl, ...(currentAgentAliases || [])].filter(
            (agentUrl): agentUrl is string_agent_url => Boolean(agentUrl),
        );
        const isCurrentAgentAdam = currentAgentUrls.some(
            (agentUrl) => normalizeAgentUrl(agentUrl) === normalizeAgentUrl(adamAgentUrl),
        );

        return isCurrentAgentAdam ? null : adamAgentUrl;
    }

    throw new ParseError(
        spaceTrim(
            (block) => `
                Invalid parent agent URL in FROM "${resolvedParentAgentUrl}" commitment:

                \`\`\`book
                ${block(agentSource || '')}
                \`\`\`
            `,
        ),
    );
}

/**
 * Imports one referenced agent and returns only its body content ready to be embedded as a NOTE block.
 *
 * @param agentUrl - Canonical referenced agent URL.
 * @param commitmentType - Commitment that introduced the reference.
 * @param context - Shared import context for the current resolution pass.
 * @returns Imported source body without title/status.
 */
async function importAgentCorpus(
    agentUrl: string_agent_url,
    commitmentType: 'FROM' | 'IMPORT',
    context: AgentImportContext,
): Promise<string> {
    assertNoResolutionCycle(agentUrl, commitmentType, context.resolutionOptions);

    const locallyImportedAgentSource = context.agentSourceImporter
        ? await context.agentSourceImporter(agentUrl, {
              commitmentType,
              importAgentOptions: context.importAgentOptions,
              federatedAgentImportConfiguration: context.federatedAgentImportConfiguration,
          })
        : null;
    const importedAgentSource =
        locallyImportedAgentSource ??
        (await importAgentWithFallback(
            agentUrl,
            context.importAgentOptions,
            context.federatedAgentImportConfiguration,
        ));

    return getAgentSourceCorpus(importedAgentSource as string_book);
}

/**
 * Resolves the effective parent state before the source body is rewritten.
 *
 * @param agentSource - Raw child agent source.
 * @param context - Shared import context for the current resolution pass.
 * @returns Parent URL, imported parent body, and unresolved `FROM` issues.
 */
async function resolveParentAgentContext(
    agentSource: string_book,
    context: AgentImportContext,
): Promise<ResolvedParentAgentContext> {
    const explicitFromContent = getExplicitFromCommitmentContent(agentSource);
    const hasExplicitFromCommitment = explicitFromContent !== undefined;
    const resolvedParentAgentUrl = await resolveParentAgentUrlFromCommitments(
        agentSource,
        context.agentReferenceResolver,
    );
    const fromResolutionIssues = consumeAgentReferenceResolutionIssues(context.agentReferenceResolver).filter(
        (issue) => issue.commitmentType === 'FROM',
    );
    const parentAgentUrl = determineParentAgentUrl(
        resolvedParentAgentUrl,
        hasExplicitFromCommitment,
        context.adamAgentUrl,
        context.resolutionOptions?.currentAgentUrl,
        context.resolutionOptions?.currentAgentAliases,
        agentSource,
    );
    const isCyclicFrom = Boolean(parentAgentUrl && findResolutionCycle(parentAgentUrl, context.resolutionOptions));
    const effectiveParentAgentUrl = isCyclicFrom ? null : parentAgentUrl;
    const parentAgentSourceCorpus = effectiveParentAgentUrl
        ? await importAgentCorpus(effectiveParentAgentUrl, 'FROM', context)
        : null;

    return {
        // A parent that closes the current lineage has the same runtime effect as `FROM @Null`.
        parentAgentUrl: effectiveParentAgentUrl,
        parentAgentSourceCorpus,
        fromResolutionIssues,
        isCyclicFrom,
    };
}

/**
 * Formats one embedded agent body into the NOTE block used by inheritance/import resolution.
 *
 * @param noteLine - First NOTE line describing the embedded source.
 * @param sourceCorpus - Imported source body to embed.
 * @returns Multi-line NOTE block.
 */
function createEmbeddedAgentSourceNote(noteLine: string, sourceCorpus: string): string {
    return spaceTrim(
        (block) => `

            ${noteLine}
            ${block(sourceCorpus)}

            NOTE ===========
    `,
    );
}

/**
 * Resolves one `IMPORT ...` line into embedded source or leaves non-agent imports untouched.
 *
 * @param line - Current source line.
 * @param context - Shared import context for the current resolution pass.
 * @returns Output lines replacing the original line.
 */
async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
    let importedUrlOrPath = rawImportedUrlOrPath;
    let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];

    if (context.agentReferenceResolver && rawImportedUrlOrPath) {
        try {
            importedUrlOrPath = await context.agentReferenceResolver.resolveCommitmentContent(
                'IMPORT',
                rawImportedUrlOrPath,
            );
        } catch (error) {
            console.warn('[AgentReferenceResolver] Failed to resolve IMPORT commitment references:', error);
        } finally {
            importResolutionIssues = consumeAgentReferenceResolutionIssues(context.agentReferenceResolver).filter(
                (issue) => issue.commitmentType === 'IMPORT' || issue.commitmentType === 'IMPORTS',
            );
        }
    }

    const resolvedChunks: Array<string> = [];
    appendResolutionIssueNotes(resolvedChunks, importResolutionIssues);

    if (!importedUrlOrPath) {
        return resolvedChunks;
    }

    if (!isValidAgentUrl(importedUrlOrPath)) {
        resolvedChunks.push(line);
        return resolvedChunks;
    }

    const importedAgentUrl = importedUrlOrPath as string_agent_url;
    const importedAgentSourceCorpus = await importAgentCorpus(importedAgentUrl, 'IMPORT', context);

    resolvedChunks.push(
        createEmbeddedAgentSourceNote(`NOTE Imported from ${importedAgentUrl}`, importedAgentSourceCorpus),
        '', // <- Note: Add an extra newline for separation
    );

    return resolvedChunks;
}

/**
 * Resolves the effective `FROM ...` line into inherited content or fallback NOTE lines.
 *
 * @param line - Current source line.
 * @param parentContext - Effective parent state computed earlier in the resolution.
 * @returns Output lines and remaining `FROM` issues.
 */
function resolveFromCommitmentLine(
    line: string,
    parentContext: ResolvedParentAgentContext,
): ResolvedFromCommitmentLine {
    if (parentContext.parentAgentUrl === null) {
        const agentSourceChunks = [parentContext.isCyclicFrom ? 'FROM @Null' : line];

        if (parentContext.fromResolutionIssues.length > 0) {
            appendResolutionIssueNotes(agentSourceChunks, parentContext.fromResolutionIssues);
        }

        return {
import type { string_book } from '../../../../src/_packages/types.index'; // <- [🚾]

/**
 * Commitment keyword that declares the parent agent of one book.
 *
 * @private utility of Agents Server inheritance resolution
 */
const FROM_COMMITMENT_TYPE = 'FROM';

/**
 * Matches one standalone `FROM` commitment line and captures its inline content.
 *
 * The Book parser accepts horizontal whitespace between a commitment keyword and its value, so inheritance resolution
 * must do the same. Keeping this pattern here makes every server-side consumer agree on which `FROM` lines are real.
 *
 * @private utility of Agents Server inheritance resolution
 */
const FROM_COMMITMENT_LINE_PATTERN = new RegExp(`^${FROM_COMMITMENT_TYPE}(?:\\s+(.*))?$`);

/**
 * One explicit `FROM` commitment written in a book.
 *
 * @private utility of Agents Server inheritance resolution
 */
export type ExplicitFromCommitment = {
    /**
     * Zero-based index of the line the commitment was found on, within the scanned lines.
     */
    readonly lineIndex: number;

    /**
     * Trimmed commitment content, empty string for a blank `FROM`.
     */
    readonly content: string;
};

/**
 * Collects every explicit single-line `FROM` commitment of one book.
 *
 * This lightweight parser is intentionally limited to the subset needed by inheritance
 * resolution so it stays safe to bundle into the Next.js proxy path.
 *
 * @param sourceLines - Book source already split into lines.
 * @returns Found `FROM` commitments in source order, empty when the book declares no parent.
 *
 * @private utility of Agents Server inheritance resolution
 */
export function collectExplicitFromCommitments(
    sourceLines: ReadonlyArray<string>,
): ReadonlyArray<ExplicitFromCommitment> {
    const explicitFromCommitments: Array<ExplicitFromCommitment> = [];
    let hasSeenTitle = false;
    let isInsideCodeBlock = false;

    for (let lineIndex = 0; lineIndex < sourceLines.length; lineIndex++) {
        const trimmedLine = (sourceLines[lineIndex] || '').trim();

        if (!hasSeenTitle) {
            if (!trimmedLine) {
                continue;
            }

            hasSeenTitle = true;
            continue;
        }

        if (trimmedLine.startsWith('```')) {
            isInsideCodeBlock = !isInsideCodeBlock;
            continue;
        }

        if (isInsideCodeBlock) {
            continue;
        }

        const fromCommitmentMatch = FROM_COMMITMENT_LINE_PATTERN.exec(trimmedLine);
        if (fromCommitmentMatch) {
            explicitFromCommitments.push({
                lineIndex,
                content: (fromCommitmentMatch[1] || '').trim(),
            });
        }
    }

    return explicitFromCommitments;
}

/**
 * Returns the effective explicit `FROM` commitment of one book.
 *
 * A book may repeat `FROM`, in which case the last one wins and overrides every earlier one.
 *
 * @param agentSource - Raw book source.
 * @returns The last explicit `FROM` commitment, or `undefined` when the book declares no parent.
 *
 * @private utility of Agents Server inheritance resolution
 */
export function getEffectiveExplicitFromCommitment(agentSource: string_book): ExplicitFromCommitment | undefined {
    const explicitFromCommitments = collectExplicitFromCommitments(agentSource.split(/\r?\n/));
    return explicitFromCommitments[explicitFromCommitments.length - 1];
}

/**
 * Returns the effective explicit `FROM` commitment content of one book.
 *
 * A book may repeat `FROM`, in which case the last one wins and overrides every earlier one.
 *
 * @param agentSource - Raw book source.
 * @returns Trimmed commitment content, empty string for a blank explicit `FROM`, or `undefined` when `FROM` is absent.
 *
 * @private utility of Agents Server inheritance resolution
 */
export function getExplicitFromCommitmentContent(agentSource: string_book): string | undefined {
    return getEffectiveExplicitFromCommitment(agentSource)?.content;
}

/**
 * Returns true when one book declares no parent at all and therefore implicitly inherits from `@Adam`.
 *
 * Writing no `FROM` commitment is equivalent to writing `FROM @Adam`, so only an explicit
 * `FROM @Null` / `FROM {Void}` turns the inheritance off.
 *
 * @param agentSource - Raw book source.
 * @returns True when the implicit `@Adam` ancestor applies to this book.
 *
 * @private utility of Agents Server inheritance resolution
 */
export function isImplicitAdamInheritance(agentSource: string_book): boolean {
    return getExplicitFromCommitmentContent(agentSource) === undefined;
}
import { access, readFile, readdir } from 'node:fs/promises';
import { resolve } from 'node:path';
import type { string_book } from '../../../../../src/book-2.0/agent-source/string_book';
import { DatabaseError } from '../../../../../src/errors/DatabaseError';
import { spaceTrim } from 'spacetrim';

/**
 * Folder name on disk that contains the bundled `.core` agent books.
 *
 * @private utility of Agents Server default-agent seeding
 */
export const CORE_AGENT_DIRECTORY_NAME = '.core';

/**
 * Options for loading bundled default agent books.
 *
 * @private utility of Agents Server default-agent seeding
 */
export type LoadDefaultAgentBooksOptions = {
    /**
     * Optional explicit directory containing `*.book` files.
     */
    readonly defaultAgentDirectory?: string | null;
};

/**
 * Candidate directories where bundled default agent books can be located.
 *
 * The app test suite runs from `apps/agents-server`, local development usually runs from the repository root, and
 * installer scripts can pass an explicit directory through `LoadDefaultAgentBooksOptions`.
 *
 * @private utility of Agents Server default-agent seeding
 */
const DEFAULT_AGENT_DIRECTORY_CANDIDATES = [
    resolve(process.cwd(), 'agents', 'default'),
    resolve(process.cwd(), '..', '..', 'agents', 'default'),
    resolve(__dirname, '../../../../../agents/default'),
] as const;

/**
 * Loads all default agent books in deterministic filename order.
 *
 * @param options - Optional explicit default-agent directory.
 * @returns Sorted default agent sources.
 *
 * @private utility of Agents Server default-agent seeding
 */
export async function loadDefaultAgentBooks(
    options: LoadDefaultAgentBooksOptions = {},
): Promise<ReadonlyArray<string_book>> {
    const defaultAgentDirectory = await resolveDefaultAgentDirectory(options);
    return loadAgentBooksFromDirectory(defaultAgentDirectory);
}

/**
 * Loads all bundled `.core` agent books in deterministic filename order.
 *
 * @param options - Optional explicit default-agent directory.
 * @returns Sorted core agent sources, or an empty list when the `.core` subfolder is absent.
 *
 * @private utility of Agents Server default-agent seeding
 */
export async function loadCoreAgentBooks(
    options: LoadDefaultAgentBooksOptions = {},
): Promise<ReadonlyArray<string_book>> {
    const defaultAgentDirectory = await resolveDefaultAgentDirectory(options);
    const coreAgentDirectory = resolve(defaultAgentDirectory, CORE_AGENT_DIRECTORY_NAME);

    try {
        await access(coreAgentDirectory);
    } catch {
        return [];
    }

    return loadAgentBooksFromDirectory(coreAgentDirectory);
}

/**
 * Reads `.book` files from one directory in deterministic filename order.
 *
 * @param agentDirectory - Absolute path to the directory containing `.book` files.
 * @returns Sorted agent sources.
 *
 * @private utility of Agents Server default-agent seeding
 */
async function loadAgentBooksFromDirectory(agentDirectory: string): Promise<ReadonlyArray<string_book>> {
    const directoryEntries = await readdir(agentDirectory);
    const agentFilenames = directoryEntries
        .filter((entry) => entry.toLowerCase().endsWith('.book'))
        .sort((leftFilename, rightFilename) => leftFilename.localeCompare(rightFilename));

    return Promise.all(
        agentFilenames.map(async (filename) => {
            const filePath = resolve(agentDirectory, filename);
            return (await readFile(filePath, 'utf-8')) as string_book;
        }),
    );
}

/**
 * Resolves the repository directory that stores default agent books.
 *
 * @param options - Optional explicit default-agent directory.
 * @returns Absolute path to `agents/default`.
 *
 * @private utility of Agents Server default-agent seeding
 */
export async function resolveDefaultAgentDirectory(options: LoadDefaultAgentBooksOptions = {}): Promise<string> {
    const directoryCandidates = [
        ...(options.defaultAgentDirectory ? [resolve(options.defaultAgentDirectory)] : []),
        ...DEFAULT_AGENT_DIRECTORY_CANDIDATES,
    ];

    for (const directoryCandidate of directoryCandidates) {
        try {
import { copyFile, stat } from 'fs/promises';
import { join } from 'path';
import { spaceTrim } from 'spacetrim';
import { NotAllowed } from '../../../errors/NotAllowed';
import type { InitializationStatus } from './boilerplateTemplates';

/**
 * Relative directory path for agents initialized by `ptbk coder init`.
 *
 * @private internal utility of `coder init` command
 */
export const CODER_AGENTS_DIRECTORY_PATH = 'agents';

/**
 * Relative file path to the default developer agent initialized by `ptbk coder init`.
 *
 * @private internal utility of `coder init` command
 */
export const CODER_DEVELOPER_AGENT_FILE_PATH = 'agents/developer.book';

/**
 * Source file path of the bundled developer agent inside the Promptbook repository.
 *
 * @private internal utility of `coder init` command
 */
export const DEFAULT_CODER_DEVELOPER_AGENT_SOURCE_FILE_PATH = 'agents/default/developer.book';

/**
 * Ensures the default developer agent exists in the initialized project.
 *
 * @private function of `initializeCoderProjectConfiguration`
 */
export async function ensureCoderDeveloperAgentFile(projectPath: string): Promise<InitializationStatus> {
    const absoluteFilePath = join(projectPath, CODER_DEVELOPER_AGENT_FILE_PATH);
    if (await isExistingFile(absoluteFilePath)) {
        return 'unchanged';
    }

    await copyFile(await resolveDefaultCoderDeveloperAgentFilePath(), absoluteFilePath);
    return 'created';
}

/**
 * Resolves the bundled developer agent from a source checkout or generated CLI package.
 */
async function resolveDefaultCoderDeveloperAgentFilePath(): Promise<string> {
    const candidates = [
        join(__dirname, '..', DEFAULT_CODER_DEVELOPER_AGENT_SOURCE_FILE_PATH),
        join(__dirname, '..', '..', '..', '..', DEFAULT_CODER_DEVELOPER_AGENT_SOURCE_FILE_PATH),
    ];

    for (const candidate of candidates) {
        if (await isExistingFile(candidate)) {
            return candidate;
        }
    }

    throw new NotAllowed(
        spaceTrim(`
            Cannot find the bundled Promptbook developer agent.

            Checked:
            ${candidates.map((candidate) => `- \`${candidate}\``).join('\n')}
        `),
    );
}

/**
 * Checks whether a path exists and is a file.
 */
async function isExistingFile(path: string): Promise<boolean> {
    try {
        return (await stat(path)).isFile();
    } catch {
        return false;
    }
}

// Note: [🟡] Code for coder init developer agent bootstrapping [ensureCoderDeveloperAgentFile](src/cli/cli-commands/coder/ensureCoderDeveloperAgentFile.ts) should never be published outside of `@promptbook/cli`
import { AGENTS_FILE_PATH, getDefaultCoderAgentsFileContent } from './agentsFile';
import type { CoderPromptTemplateDefinition, InitializationStatus } from './boilerplateTemplates';
import {
    ensureDefaultCoderPromptTemplateFile,
    getDefaultCoderProjectPromptTemplateDefinitions,
} from './boilerplateTemplates';
import { CODER_DEVELOPER_AGENT_FILE_PATH, ensureCoderDeveloperAgentFile } from './ensureCoderDeveloperAgentFile';
import { ensureCoderMarkdownFile } from './ensureCoderMarkdownFile';

/**
 * Status of one artifact referenced by the default coder scripts.
 *
 * `not-referenced` means that no newly added script points at the artifact, so `ptbk coder init` left it alone.
 *
 * @private internal utility of `coder init` command
 */
export type CoderReferencedArtifactStatus = InitializationStatus | 'not-referenced';

/**
 * Result of ensuring one artifact referenced by the default coder scripts.
 *
 * @private internal utility of `coder init` command
 */
export type EnsuredCoderReferencedArtifact = {
    /**
     * Project-relative path of the referenced artifact.
     */
    readonly relativeFilePath: string;

    /**
     * Status describing whether the artifact had to be created.
     */
    readonly status: CoderReferencedArtifactStatus;
};

/**
 * One project artifact which the default coder scripts point at.
 */
type CoderReferencedArtifactDefinition = {
    /**
     * Project-relative path which both identifies the artifact and locates it inside the project.
     */
    readonly relativeFilePath: string;

    /**
     * Creates the artifact when it is missing, never overwriting an existing one.
     */
    readonly ensureArtifactFile: (projectPath: string) => Promise<InitializationStatus>;
};

/**
 * Creates the artifact definition of one project-owned coder prompt template.
 */
function createCoderPromptTemplateArtifactDefinition(
    definition: CoderPromptTemplateDefinition,
): CoderReferencedArtifactDefinition {
    return {
        relativeFilePath: definition.relativeFilePath,
        ensureArtifactFile: (projectPath) => ensureDefaultCoderPromptTemplateFile(projectPath, definition),
    };
}

/**
 * Every artifact which the default coder scripts can reference.
 */
const CODER_REFERENCED_ARTIFACT_DEFINITIONS: ReadonlyArray<CoderReferencedArtifactDefinition> = [
    {
        relativeFilePath: CODER_DEVELOPER_AGENT_FILE_PATH,
        ensureArtifactFile: ensureCoderDeveloperAgentFile,
    },
    {
        relativeFilePath: AGENTS_FILE_PATH,
        ensureArtifactFile: (projectPath) =>
            ensureCoderMarkdownFile(projectPath, AGENTS_FILE_PATH, getDefaultCoderAgentsFileContent()),
    },
    ...getDefaultCoderProjectPromptTemplateDefinitions().map(createCoderPromptTemplateArtifactDefinition),
];

/**
 * Creates only those referenced artifacts which the freshly added coder scripts point at.
 *
 * An artifact of a script which the project already defines is intentionally **not** created - the project-owned
 * script is kept as is, so it does not reference the default artifact in the first place.
 *
 * @private function of `initializeCoderProjectConfiguration`
 */
export async function ensureCoderReferencedArtifacts(
    projectPath: string,
    referencedArtifactPaths: ReadonlySet<string>,
): Promise<ReadonlyArray<EnsuredCoderReferencedArtifact>> {
    const ensuredArtifacts: Array<EnsuredCoderReferencedArtifact> = [];

    for (const { relativeFilePath, ensureArtifactFile } of CODER_REFERENCED_ARTIFACT_DEFINITIONS) {
        if (!referencedArtifactPaths.has(relativeFilePath)) {
            ensuredArtifacts.push({ relativeFilePath, status: 'not-referenced' });
            continue;
        }

        ensuredArtifacts.push({ relativeFilePath, status: await ensureArtifactFile(projectPath) });
    }

    return ensuredArtifacts;
}

// Note: [🟡] Code for coder init referenced artifacts [coderReferencedArtifacts](src/cli/cli-commands/coder/coderReferencedArtifacts.ts) should never be published outside of `@promptbook/cli`
// Note: [💞] Ignore a discrepancy between file name and exported helper names

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content apps/agents-server/src/utils/createLocalAgentSourceImporter.ts; Get-Content apps/agents-server/src/utils/agentReferenceResolver/AgentReferenceResolutionIssue.ts; Get-Content apps/agents-server/src/utils/agentReferenceResolver/extractAgentReferenceTokens.ts; Get-Content apps/agents-server/src/utils/explicitFromCommitment.ts; Get-Content src/book-2.0/agent-source/pseudoAgentReferences.ts -TotalCount 180' in C:\Users\me\work\ai\promptbook
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content apps/agents-server/src/utils/resolveInheritedAgentSource.ts | Select-Object -Skip 620 -First 105; Get-Content src/commitments/FROM/FROM.ts -TotalCount 165; Get-Content src/commitments/IMPORT/IMPORT.ts -TotalCount 180; Get-Content src/book-2.0/agent-source/createAgentModelRequirements.ts -TotalCount 140; rg -n 'resolveCommitmentContent|agentReferenceResolver|teammate' src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments* src/commitments/TEAM/TEAM.ts; rg -n 'agents/default|developer.book' scripts/generate-packages -g '*.ts'; Get-Content agents/default/.core/adam.book -TotalCount 65" in C:\Users\me\work\ai\promptbook
 succeeded in 863ms:
import type { AgentReferenceResolver } from '../../../../src/book-2.0/agent-source/AgentReferenceResolver';
import type { AgentCollection } from '../../../../src/collection/agent-collection/AgentCollection';
import { DEFAULT_MAX_RECURSION } from '../../../../src/config';
import { NotFoundError } from '../../../../src/errors/NotFoundError';
import { ParseError } from '../../../../src/errors/ParseError';
import type { string_agent_url } from '../../../../src/types/typeAliases';
import { spaceTrim } from '../../../../src/utils/organization/spaceTrim';
import type { FederatedAgentImportConfiguration } from '../constants/federatedAgentImport';
import {
    resolveBookScopedAgentContext,
    type ResolvedBookScopedAgentContext,
} from './agentReferenceResolver/bookScopedAgentReferences';
import {
    createLocalAgentUrl,
    normalizeLocalAgentUrlReferences,
    normalizeLocalServerUrls,
    resolveLocalAgentRouteReference,
} from './localAgentRouteReferences';
import { createMissingImportedAgentFallback } from './createMissingImportedAgentFallback';
import {
    resolveInheritedAgentSource,
    type AgentSourceImporter,
} from './resolveInheritedAgentSource';

/**
 * Dependencies needed for importing same-instance agent sources without HTTP.
 */
export type CreateLocalAgentSourceImporterOptions = {
    /**
     * Collection used to load local regular and book-scoped agent sources.
     */
    readonly collection: Pick<AgentCollection, 'getAgentPermanentId' | 'getAgentSource'>;

    /**
     * Server origins that should be treated as this Agents Server instance.
     */
    readonly localServerUrls: ReadonlyArray<string>;

    /**
     * Exact agent URLs that should be treated as local even when their origin is not in `localServerUrls`.
     */
    readonly localAgentUrls?: ReadonlyArray<string_agent_url>;

    /**
     * Adam agent URL used for implicit default inheritance.
     */
    readonly adamAgentUrl: string_agent_url;

    /**
     * Resolver used for compact references that are not book-scoped.
     */
    readonly fallbackResolver?: AgentReferenceResolver;

    /**
     * Retry configuration used when nested imports leave the local server.
     */
    readonly federatedAgentImportConfiguration?: FederatedAgentImportConfiguration;
};

/**
 * Creates an importer that loads same-instance agent URLs directly from the collection.
 *
 * @param options - Same-instance import dependencies.
 * @returns Importer suitable for `resolveInheritedAgentSource`.
 */
export function createLocalAgentSourceImporter(options: CreateLocalAgentSourceImporterOptions): AgentSourceImporter {
    const localServerUrls = normalizeLocalServerUrls(options.localServerUrls);
    const localAgentUrlReferences = normalizeLocalAgentUrlReferences(options.localAgentUrls || []);
    const localAgentSourceImporter: AgentSourceImporter = async (agentUrl, context) => {
        const localRouteReference = resolveLocalAgentRouteReference(agentUrl, localServerUrls, localAgentUrlReferences);

        if (!localRouteReference) {
            return null;
        }

        const nextRecursionLevel = (context.importAgentOptions.recursionLevel || 0) + 1;
        assertRecursionLevel(nextRecursionLevel, agentUrl);

        let resolvedAgentContext: ResolvedBookScopedAgentContext;

        try {
            resolvedAgentContext = await resolveBookScopedAgentContext({
                collection: options.collection,
                agentIdentifier: localRouteReference.agentIdentifier,
                localServerUrl: localRouteReference.localServerUrl,
                fallbackResolver: options.fallbackResolver,
            });
        } catch (error) {
            if (error instanceof NotFoundError) {
                return createMissingImportedAgentFallback(agentUrl, 1, error);
            }

            throw error;
        }

        return resolveInheritedAgentSource(resolvedAgentContext.unresolvedAgentSource, {
            adamAgentUrl: options.adamAgentUrl,
            recursionLevel: nextRecursionLevel,
            inheritancePath: context.importAgentOptions.inheritancePath,
            currentAgentUrl: resolvedAgentContext.canonicalAgentUrl,
            currentAgentAliases: createCurrentAgentAliases(resolvedAgentContext, localServerUrls),
            agentReferenceResolver: resolvedAgentContext.scopedAgentReferenceResolver,
            federatedAgentImportConfiguration:
                options.federatedAgentImportConfiguration || context.federatedAgentImportConfiguration,
            agentSourceImporter: localAgentSourceImporter,
        });
    };

    return localAgentSourceImporter;
}

/**
 * Throws when direct local importing would exceed the configured recursion limit.
 *
 * @param recursionLevel - Next recursion level.
 * @param agentUrl - Local agent URL being imported.
 */
function assertRecursionLevel(recursionLevel: number, agentUrl: string_agent_url): void {
    if (recursionLevel <= DEFAULT_MAX_RECURSION) {
        return;
    }

    throw new ParseError(
        spaceTrim(
            (block) => `
                Recursion depth ${recursionLevel} exceeds maximum allowed ${DEFAULT_MAX_RECURSION} while importing local agent:

                ${block(agentUrl)}
            `,
        ),
    );
}

/**
 * Builds cycle-detection aliases for every same-instance origin.
 *
 * @param resolvedAgentContext - Resolved local/book-scoped agent context.
 * @param localServerUrls - Same-instance server origins.
 * @returns Unique local URL aliases for the imported agent.
 */
function createCurrentAgentAliases(
    resolvedAgentContext: ResolvedBookScopedAgentContext,
    localServerUrls: ReadonlyArray<string>,
): Array<string_agent_url> {
    const agentIdentifiers = [
        resolvedAgentContext.canonicalAgentIdentifier,
        resolvedAgentContext.requestedAgentIdentifier,
        resolvedAgentContext.resolvedAgentName,
    ].filter((agentIdentifier, index, agentIdentifierList) => agentIdentifierList.indexOf(agentIdentifier) === index);
    const agentAliases = new Set<string>(resolvedAgentContext.currentAgentAliases);

    for (const localServerUrl of localServerUrls) {
        for (const agentIdentifier of agentIdentifiers) {
            agentAliases.add(createLocalAgentUrl(localServerUrl, agentIdentifier));
        }
    }

    return [...agentAliases] as Array<string_agent_url>;
}
import type { BookCommitment } from '../../../../../src/commitments/_base/BookCommitment';
import type { AgentReferenceResolver } from '../../../../../src/book-2.0/agent-source/AgentReferenceResolver';

/**
 * Structured issue captured when compact agent reference resolution fails.
 */
export type AgentReferenceResolutionIssue = {
    /**
     * Commitment where the unresolved token appeared.
     */
    readonly commitmentType: BookCommitment;

    /**
     * Original token text (for example `{Unknown Agent}` or `@UnknownAgent`).
     */
    readonly token: string;

    /**
     * Normalized token payload used for lookup.
     */
    readonly reference: string;

    /**
     * Human-readable explanation of why the token could not be resolved.
     */
    readonly message: string;
};

/**
 * Extension implemented by resolvers that can expose accumulated resolution issues.
 */
export type IssueTrackingAgentReferenceResolver = AgentReferenceResolver & {
    /**
     * Returns tracked issues and clears the internal queue.
     */
    consumeResolutionIssues(): Array<AgentReferenceResolutionIssue>;
};

/**
 * Type guard for issue-tracking resolver implementations.
 *
 * @param resolver - Resolver instance to check.
 * @returns True when the resolver provides `consumeResolutionIssues`.
 */
function isIssueTrackingAgentReferenceResolver(
    resolver: AgentReferenceResolver,
): resolver is IssueTrackingAgentReferenceResolver {
    return typeof (resolver as Partial<IssueTrackingAgentReferenceResolver>).consumeResolutionIssues === 'function';
}

/**
 * Drains unresolved compact-reference issues from a resolver when supported.
 *
 * @param resolver - Resolver instance that may implement issue tracking.
 * @returns Collected issues or an empty list.
 */
export function consumeAgentReferenceResolutionIssues(
    resolver?: AgentReferenceResolver,
): Array<AgentReferenceResolutionIssue> {
    if (!resolver) {
        return [];
    }

    if (!isIssueTrackingAgentReferenceResolver(resolver)) {
        return [];
    }

    return resolver.consumeResolutionIssues();
}
/**
 * Matched compact agent reference token inside commitment content.
 */
export type AgentReferenceTokenMatch = {
    /**
     * Original token text, for example `{Agent Name}` or `@AgentId`.
     */
    readonly token: string;

    /**
     * Token payload used for lookup.
     */
    readonly reference: string;

    /**
     * Zero-based character offset of `token` in the scanned content.
     */
    readonly index: number;

    /**
     * Length of `token` in characters.
     */
    readonly length: number;
};

/**
 * Matches supported compact reference token syntaxes in commitment content.
 */
const AGENT_REFERENCE_TOKEN_REGEX = /(\{([^}]+)\}|@([A-Za-z0-9_-]+))/g;

/**
 * Extracts compact agent-reference tokens from commitment content.
 *
 * @param content - Commitment content to inspect.
 * @returns Matched compact reference tokens.
 */
export function extractAgentReferenceTokens(content: string): Array<AgentReferenceTokenMatch> {
    const matches: Array<AgentReferenceTokenMatch> = [];
    let match: RegExpExecArray | null;

    AGENT_REFERENCE_TOKEN_REGEX.lastIndex = 0;

    while ((match = AGENT_REFERENCE_TOKEN_REGEX.exec(content)) !== null) {
        const token = match[1];
        const reference = (match[2] ?? match[3] ?? '').trim();
        const index = match.index;

        if (!token || index === undefined) {
            continue;
        }

        matches.push({
            token,
            reference,
            index,
            length: token.length,
        });
    }

    return matches;
}
import type { string_book } from '../../../../src/_packages/types.index'; // <- [🚾]

/**
 * Commitment keyword that declares the parent agent of one book.
 *
 * @private utility of Agents Server inheritance resolution
 */
const FROM_COMMITMENT_TYPE = 'FROM';

/**
 * Matches one standalone `FROM` commitment line and captures its inline content.
 *
 * The Book parser accepts horizontal whitespace between a commitment keyword and its value, so inheritance resolution
 * must do the same. Keeping this pattern here makes every server-side consumer agree on which `FROM` lines are real.
 *
 * @private utility of Agents Server inheritance resolution
 */
const FROM_COMMITMENT_LINE_PATTERN = new RegExp(`^${FROM_COMMITMENT_TYPE}(?:\\s+(.*))?$`);

/**
 * One explicit `FROM` commitment written in a book.
 *
 * @private utility of Agents Server inheritance resolution
 */
export type ExplicitFromCommitment = {
    /**
     * Zero-based index of the line the commitment was found on, within the scanned lines.
     */
    readonly lineIndex: number;

    /**
     * Trimmed commitment content, empty string for a blank `FROM`.
     */
    readonly content: string;
};

/**
 * Collects every explicit single-line `FROM` commitment of one book.
 *
 * This lightweight parser is intentionally limited to the subset needed by inheritance
 * resolution so it stays safe to bundle into the Next.js proxy path.
 *
 * @param sourceLines - Book source already split into lines.
 * @returns Found `FROM` commitments in source order, empty when the book declares no parent.
 *
 * @private utility of Agents Server inheritance resolution
 */
export function collectExplicitFromCommitments(
    sourceLines: ReadonlyArray<string>,
): ReadonlyArray<ExplicitFromCommitment> {
    const explicitFromCommitments: Array<ExplicitFromCommitment> = [];
    let hasSeenTitle = false;
    let isInsideCodeBlock = false;

    for (let lineIndex = 0; lineIndex < sourceLines.length; lineIndex++) {
        const trimmedLine = (sourceLines[lineIndex] || '').trim();

        if (!hasSeenTitle) {
            if (!trimmedLine) {
                continue;
            }

            hasSeenTitle = true;
            continue;
        }

        if (trimmedLine.startsWith('```')) {
            isInsideCodeBlock = !isInsideCodeBlock;
            continue;
        }

        if (isInsideCodeBlock) {
            continue;
        }

        const fromCommitmentMatch = FROM_COMMITMENT_LINE_PATTERN.exec(trimmedLine);
        if (fromCommitmentMatch) {
            explicitFromCommitments.push({
                lineIndex,
                content: (fromCommitmentMatch[1] || '').trim(),
            });
        }
    }

    return explicitFromCommitments;
}

/**
 * Returns the effective explicit `FROM` commitment of one book.
 *
 * A book may repeat `FROM`, in which case the last one wins and overrides every earlier one.
 *
 * @param agentSource - Raw book source.
 * @returns The last explicit `FROM` commitment, or `undefined` when the book declares no parent.
 *
 * @private utility of Agents Server inheritance resolution
 */
export function getEffectiveExplicitFromCommitment(agentSource: string_book): ExplicitFromCommitment | undefined {
    const explicitFromCommitments = collectExplicitFromCommitments(agentSource.split(/\r?\n/));
    return explicitFromCommitments[explicitFromCommitments.length - 1];
}

/**
 * Returns the effective explicit `FROM` commitment content of one book.
 *
 * A book may repeat `FROM`, in which case the last one wins and overrides every earlier one.
 *
 * @param agentSource - Raw book source.
 * @returns Trimmed commitment content, empty string for a blank explicit `FROM`, or `undefined` when `FROM` is absent.
 *
 * @private utility of Agents Server inheritance resolution
 */
export function getExplicitFromCommitmentContent(agentSource: string_book): string | undefined {
    return getEffectiveExplicitFromCommitment(agentSource)?.content;
}

/**
 * Returns true when one book declares no parent at all and therefore implicitly inherits from `@Adam`.
 *
 * Writing no `FROM` commitment is equivalent to writing `FROM @Adam`, so only an explicit
 * `FROM @Null` / `FROM {Void}` turns the inheritance off.
 *
 * @param agentSource - Raw book source.
 * @returns True when the implicit `@Adam` ancestor applies to this book.
 *
 * @private utility of Agents Server inheritance resolution
 */
export function isImplicitAdamInheritance(agentSource: string_book): boolean {
    return getExplicitFromCommitmentContent(agentSource) === undefined;
}
import type { BookCommitment } from '../../commitments/_base/BookCommitment';
import { normalizeWhitespaces } from '../../utils/normalization/normalizeWhitespaces';
import { generateDeterministicEnglishName } from '../../utils/random/EnglishNamePool';

/**
 * Supported pseudo-agent kinds.
 *
 * @private internal utility of pseudo-agent resolution
 */
export type PseudoAgentKind = 'USER' | 'VOID';

/**
 * Canonical pseudo-agent URL representing the current human user.
 *
 * @private internal utility of pseudo-agent resolution
 */
export const PSEUDO_AGENT_USER_URL = 'https://pseudo-agent.promptbook/user';

/**
 * Canonical pseudo-agent URL representing void / nothingness.
 *
 * @private internal utility of pseudo-agent resolution
 */
export const PSEUDO_AGENT_VOID_URL = 'https://pseudo-agent.promptbook/void';

/**
 * Canonical compact token representation of the `{Void}` pseudo-agent.
 *
 * @private internal utility of pseudo-agent resolution
 */
export const VOID_PSEUDO_AGENT_REFERENCE = '{Void}';

/**
 * Normalized alias keys that should resolve to the `{Void}` pseudo-agent.
 *
 * @private internal utility of pseudo-agent resolution
 */
export const VOID_PSEUDO_AGENT_ALIAS_KEYS = ['void', 'null', 'none', 'nil'] as const;

/**
 * Legacy aliases that historically behaved like `FROM VOID`.
 */
const LEGACY_VOID_ALIASES = new Set<string>(VOID_PSEUDO_AGENT_ALIAS_KEYS);

/**
 * Constant for void alias entries.
 */
const VOID_ALIAS_ENTRIES: Array<[string, PseudoAgentKind]> = VOID_PSEUDO_AGENT_ALIAS_KEYS.map((alias) => [
    alias,
    'VOID',
]);

/**
 * Mapping from normalized pseudo-agent names to their runtime kinds.
 */
const PSEUDO_AGENT_REFERENCE_KIND_MAP: Readonly<Record<string, PseudoAgentKind>> = {
    user: 'USER',
    ...Object.fromEntries(VOID_ALIAS_ENTRIES),
};

/**
 * Maps pseudo-agent kinds to canonical pseudo-agent URLs.
 */
const PSEUDO_AGENT_URL_BY_KIND: Readonly<Record<PseudoAgentKind, string>> = {
    USER: PSEUDO_AGENT_USER_URL,
    VOID: PSEUDO_AGENT_VOID_URL,
};

/**
 * Unwraps a compact reference token (`{Name}` or `@Name`) to plain reference payload.
 *
 * @param rawReference - Raw token payload.
 * @returns Unwrapped reference text.
 */
function unwrapPseudoReference(rawReference: string): string {
    const trimmedReference = rawReference.trim();
    const bracketMatch = /^\{(.+)\}$/.exec(trimmedReference);
    if (bracketMatch?.[1]) {
        return bracketMatch[1].trim();
    }

    if (trimmedReference.startsWith('@')) {
        return trimmedReference.slice(1).trim();
    }

    return trimmedReference;
}

/**
 * Normalizes pseudo-agent reference key for case-insensitive matching.
 *
 * @param rawReference - Raw pseudo-agent reference.
 * @returns Normalized key, for example `User` -> `user`.
 */
function normalizePseudoReferenceKey(rawReference: string): string {
    return unwrapPseudoReference(rawReference)
        .trim()
        .toLowerCase()
        .replace(/[\s_-]+/g, '');
}

/**
 * Resolves pseudo-agent kind from compact reference text.
 *
 * This accepts compact references in all supported syntaxes:
 * `{User}`, `{user}`, `@USER`, `Void`, etc.
 *
 * @param rawReference - Raw reference token or payload.
 * @returns Pseudo-agent kind or `null` when the reference is not pseudo.
 *
 * @private internal utility of pseudo-agent resolution
 */
export function resolvePseudoAgentKindFromReference(rawReference: string): PseudoAgentKind | null {
    if (!rawReference.trim()) {
        return null;
    }

    const normalizedReference = normalizePseudoReferenceKey(rawReference);
    return PSEUDO_AGENT_REFERENCE_KIND_MAP[normalizedReference] || null;
}

/**
 * Resolves pseudo-agent kind from canonical pseudo-agent URL.
 *
 * @param agentUrl - URL to inspect.
 * @returns Pseudo-agent kind or `null` when the URL is not pseudo.
 *
 * @private internal utility of pseudo-agent resolution
 */
export function resolvePseudoAgentKindFromUrl(agentUrl: string): PseudoAgentKind | null {
    const normalizedUrl = agentUrl.trim().toLowerCase();

    if (normalizedUrl === PSEUDO_AGENT_USER_URL) {
        return 'USER';
    }

    if (normalizedUrl === PSEUDO_AGENT_VOID_URL) {
        return 'VOID';
    }

    return null;
}

/**
 * Checks whether a URL points to a pseudo-agent.
 *
 * @param agentUrl - URL to inspect.
 * @returns True when the URL identifies a pseudo-agent.
 *
 * @private internal utility of pseudo-agent resolution
 */
export function isPseudoAgentUrl(agentUrl: string): boolean {
    return resolvePseudoAgentKindFromUrl(agentUrl) !== null;
}

/**
 * Creates canonical pseudo-agent URL by pseudo-agent kind.
 *
 * @param pseudoAgentKind - Pseudo-agent kind.
 * @returns Canonical pseudo-agent URL.
 *
 * @private internal utility of pseudo-agent resolution
 */
export function createPseudoAgentUrl(pseudoAgentKind: PseudoAgentKind): string {
    return PSEUDO_AGENT_URL_BY_KIND[pseudoAgentKind];
}

/**
 * Creates a deterministic human-readable pseudonym for the `{User}` pseudo-agent.
 *
 * The seed is derived from TEAM commitment text with pseudo-user URLs removed so
 * the remaining human instructions influence the resulting name.
 *
 * @param teamCommitmentContent - TEAM commitment content after reference resolution.
 * @returns Deterministic English full name representing pseudo-user in that TEAM context.
 *
 * @private internal utility of TEAM commitment rendering
 */
export function createPseudoUserTeammateLabel(teamCommitmentContent: string): string {
    const seedWithoutPseudoUrls = normalizeWhitespaces(

 succeeded in 4766ms:
        if (parentContext.fromResolutionIssues.length > 0) {
            appendResolutionIssueNotes(agentSourceChunks, parentContext.fromResolutionIssues);
        }

        return {
            agentSourceChunks,
            fromResolutionIssues: [],
        };
    }

    if (parentContext.parentAgentSourceCorpus) {
        return {
            agentSourceChunks: [
                createEmbeddedAgentSourceNote(
                    `NOTE Inherited FROM ${parentContext.parentAgentUrl}`,
                    parentContext.parentAgentSourceCorpus,
                ),
                '', // <- Note: Add an extra newline for separation
            ],
            fromResolutionIssues: parentContext.fromResolutionIssues,
        };
    }

    return {
        agentSourceChunks: [
            `NOTE Parent agent "${parentContext.parentAgentUrl}" was not found or could not be loaded. Inheritance skipped.`,
            '',
        ],
        fromResolutionIssues: parentContext.fromResolutionIssues,
    };
}

/**
 * Finds the `FROM` lines that a later `FROM` takes precedence over.
 *
 * A book is allowed to repeat `FROM`; the last one wins, so every earlier one is dropped instead of being resolved.
 * The Book editor warns about this while the author is still writing the book.
 *
 * @param explicitFromCommitments - Explicit parent declarations found in source order.
 * @returns Zero-based indexes of every overridden `FROM` line, empty when at most one `FROM` is present.
 */
function collectOverriddenFromLineIndexes(
    explicitFromCommitments: ReadonlyArray<{ readonly lineIndex: number }>,
): ReadonlySet<number> {
    return new Set(
        explicitFromCommitments.slice(0, -1).map((explicitFromCommitment) => explicitFromCommitment.lineIndex),
    );
}

/**
 * Rewrites the source body line by line while delegating each branching step to a focused helper.
 *
 * @param agentSource - Raw child agent source.
 * @param context - Shared import context for the current resolution pass.
 * @param parentContext - Effective parent state computed earlier in the resolution.
 * @returns Rewritten chunks plus final `FROM` bookkeeping.
 */
async function resolveAgentSourceBuild(
    agentSource: string_book,
    context: AgentImportContext,
    parentContext: ResolvedParentAgentContext,
): Promise<ResolvedAgentSourceBuild> {
    const agentSourceChunks = spaceTrim(agentSource).split(/\r?\n/);
    const resolvedAgentSourceChunks: Array<string> = [];
    const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
    const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
    const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
    let isFromResolved = false;
    let fromResolutionIssues = parentContext.fromResolutionIssues;
    // <- TODO: [🈲] Simple and encapsulated way to split book into commitments

    for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
        const line = agentSourceChunks[lineIndex] || '';

        // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
        if (overriddenFromLineIndexes.has(lineIndex)) {
            continue;
        }

        if (line.trim().startsWith('IMPORT ')) {
            resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
            continue;
        }

        if (effectiveFromCommitment?.lineIndex === lineIndex) {
            const resolvedFromCommitment = resolveFromCommitmentLine(line, {
                ...parentContext,
                fromResolutionIssues,
            });

            resolvedAgentSourceChunks.push(...resolvedFromCommitment.agentSourceChunks);
            fromResolutionIssues = resolvedFromCommitment.fromResolutionIssues;
            isFromResolved = true;
            continue;
        }

        resolvedAgentSourceChunks.push(line);
    }
    // <- TODO: [🈲] Simple and encapsulated way to split book into commitments

    return {
        agentSourceChunks: resolvedAgentSourceChunks,
            return {
                ...requirements,
                parentAgentUrl: null,
            };
        }

        if (!isValidAgentUrl(trimmedContent)) {
            throw new Error(
                spaceTrim(
                    (block) => `
                        Invalid agent URL in FROM commitment: "${trimmedContent}"

                        \`\`\`book
                        ${block(content)}
                        \`\`\`


                `,
                ),
            );
        }

        const parentAgentUrl: string_agent_url = trimmedContent;

        return {
            ...requirements,
            parentAgentUrl,
        };
    }
}

// Note: [💞] Ignore a discrepancy between file name and entity name
import { spaceTrim } from 'spacetrim';
import type { AgentModelRequirements } from '../../book-2.0/agent-source/AgentModelRequirements';
import type { string_agent_url } from '../../types/string_agent_url';
import { isValidFilePath } from '../../utils/validators/filePath/isValidFilePath';
import { isValidAgentUrl } from '../../utils/validators/url/isValidAgentUrl';
import { isValidUrl } from '../../utils/validators/url/isValidUrl';
import { BaseCommitmentDefinition } from '../_base/BaseCommitmentDefinition';

/**
 * IMPORT commitment definition
 *
 * The IMPORT commitment tells the agent to import content from another agent at the current location.
 *
 * Example usage in agent source:
 *
 * ```book
 * IMPORT https://s6.ptbk.io/benjamin-white
 * ```
 *
 * @private [🪔] Maybe export the commitments through some package
 */
export class ImportCommitmentDefinition extends BaseCommitmentDefinition<'IMPORT' | 'IMPORTS'> {
    public constructor(type: 'IMPORT' | 'IMPORTS' = 'IMPORT') {
        super(type);
    }

    /**
     * Short one-line description of IMPORT.
     */
    get description(): string {
        return 'Import content from another agent or a generic text file.';
    }

    /**
     * Icon for this commitment.
     */
    get icon(): string {
        return '📥';
    }

    /**
     * Markdown documentation for IMPORT commitment.
     */
    get documentation(): string {
        return spaceTrim(`
            # ${this.type}

            Imports content from another agent or a generic text file at the location of the commitment.

            ## Examples

            \`\`\`book
            My AI Agent

            IMPORT https://s6.ptbk.io/benjamin-white
            IMPORT https://example.com/some-text-file.txt
            IMPORT ./path/to/local-file.json
            RULE Speak only in English.
            \`\`\`
        `);
    }

    applyToAgentModelRequirements(requirements: AgentModelRequirements, content: string): AgentModelRequirements {
        const trimmedContent = content.trim();

        if (!trimmedContent) {
            return requirements;
        }

        if (isValidAgentUrl(trimmedContent)) {
            const importedAgentUrl: string_agent_url = trimmedContent;

            return {
                ...requirements,
                importedAgentUrls: [...(requirements.importedAgentUrls || []), importedAgentUrl],
            };
        }

        if (isValidUrl(trimmedContent) || isValidFilePath(trimmedContent)) {
            return {
                ...requirements,
                importedFileUrls: [...(requirements.importedFileUrls || []), trimmedContent],
            };
        }

        throw new Error(
            spaceTrim(
                (block) => `
                    Invalid agent URL or file path in IMPORT commitment: "${trimmedContent}"

                    \`\`\`book
                    ${block(content)}
                    \`\`\`
            `,
            ),
        );
    }
}

// Note: [💞] Ignore a discrepancy between file name and entity name
import type { AvailableModel } from '../../execution/AvailableModel';
import type { LlmExecutionTools } from '../../execution/LlmExecutionTools';
import { preparePersona } from '../../personas/preparePersona';
import type { string_agent_name } from '../../types/string_agent_name';
import type { string_model_name } from '../../types/string_model_name';
import type { string_system_message } from '../../types/string_prompt';
import type { string_url_image } from '../../types/string_url_image';
import type { AgentModelRequirements } from './AgentModelRequirements';
import type { CreateAgentModelRequirementsOptions } from './CreateAgentModelRequirementsOptions';
import { createAgentModelRequirementsWithCommitments } from './createAgentModelRequirementsWithCommitments';
import { parseAgentSource } from './parseAgentSource';
import { parseAgentSourceWithCommitments } from './parseAgentSourceWithCommitments';
import type { string_book } from './string_book';

/**
 * Creates model requirements for an agent based on its source.
 *
 * There are 2 similar functions:
 * - `parseAgentSource` which is a lightweight parser for agent source, it parses basic information and its purpose is to be quick and synchronous. The commitments there are hardcoded.
 * - `createAgentModelRequirements` which is an asynchronous function that creates model requirements it applies each commitment one by one and works asynchronous.
 *
 * @param agentSource - Book describing the agent.
 * @param modelName - Optional override for the agent's model.
 * @param availableModels - Models that could fulfill the agent.
 * @param llmTools - Execution tools used when selecting a best model.
 * @param options - Optional hooks such as the agent reference resolver.
 *
 * @public exported from `@promptbook/core`
 */
export async function createAgentModelRequirements(
    agentSource: string_book,
    modelName?: string_model_name,
    availableModels?: readonly AvailableModel[],
    llmTools?: LlmExecutionTools,
    options?: CreateAgentModelRequirementsOptions,
): Promise<AgentModelRequirements> {
    // If availableModels are provided and no specific modelName is given,
    // use preparePersona to select the best model
    if (availableModels && !modelName && llmTools) {
        const selectedModelName = await selectBestModelUsingPersona(agentSource, llmTools);
        return createAgentModelRequirementsWithCommitments(agentSource, selectedModelName, options);
    }

    // Use the new commitment-based system with provided or default model
    return createAgentModelRequirementsWithCommitments(agentSource, modelName, options);
}

/**
 * Selects the best model using the preparePersona function
 * This directly uses preparePersona to ensure DRY principle
 *
 * @param agentSource The agent source to derive effective profile text from
 * @param llmTools LLM tools for preparing persona
 * @returns The name of the best selected model
 *
 * @private function of `createAgentModelRequirements`
 */
async function selectBestModelUsingPersona(
    agentSource: string_book,
    llmTools: LlmExecutionTools,
): Promise<string_model_name> {
    // Parse agent source to get the effective profile description
    const { agentName, personaDescription } = parseAgentSource(agentSource);

    // Use agent name as fallback if no profile description is available
    const description = personaDescription || agentName || 'AI Agent';

    try {
        // Use preparePersona directly
        const { modelsRequirements } = await preparePersona(description, { llm: llmTools }, { isVerbose: false });

        // Extract the first model name from the requirements
        if (modelsRequirements.length > 0 && modelsRequirements[0]?.modelName) {
            return modelsRequirements[0].modelName;
        }

        // Fallback: get available models and return the first CHAT model
        const availableModels = await llmTools.listModels();
        const chatModels = availableModels.filter(({ modelVariant }) => modelVariant === 'CHAT');

        if (chatModels.length === 0) {
            throw new Error('No CHAT models available for agent model selection');
        }

        return chatModels[0]!.modelName;
    } catch (error) {
        console.warn('Failed to use preparePersona for model selection, falling back to first available model:', error);

        // Fallback: get available models and return the first CHAT model
        const availableModels = await llmTools.listModels();
        const chatModels = availableModels.filter(({ modelVariant }) => modelVariant === 'CHAT');

        if (chatModels.length === 0) {
            throw new Error('No CHAT models available for agent model selection');
        }

        return chatModels[0]!.modelName;
    }
}

/**
 * Extracts MCP servers from agent source
 *
 * @param agentSource The agent source string that may contain `USE MCP` commitments.
 * @returns Array of MCP server identifiers
 *
 * @private TODO: [🧠] Maybe should be public
 */
export function extractMcpServers(agentSource: string_book): string[] {
    return parseAgentSourceWithCommitments(agentSource)
        .commitments.filter((commitment) => commitment.type === 'USE MCP')
        .map((commitment) => commitment.content.trim())
        .filter(Boolean);
}

/**
 * Creates a system message for an agent based on its source
 *
 * @deprecated Use createAgentModelRequirements instead
 *
 * @private
 */
export async function createAgentSystemMessage(agentSource: string_book): Promise<string_system_message> {
    const modelRequirements = await createAgentModelRequirements(agentSource);
    return modelRequirements.systemMessage;
}

/**
 * Extracts the agent name from the first non-empty line of the agent source.
 *
 * @deprecated Use parseAgentSource instead
 *
 * @private
 */
export function extractAgentName(agentSource: string_book): string_agent_name {
    const { agentName } = parseAgentSource(agentSource);

    if (!agentName) {
        throw new Error('Agent source must have at least one line to derive the name');
    }
rg: src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments*: The filename, directory name, or volume label syntax is incorrect. (os error 123)
src/commitments/TEAM/TEAM.ts:32: * Tool registration entry for a teammate.
src/commitments/TEAM/TEAM.ts:38:    teammate: TeamTeammate;
src/commitments/TEAM/TEAM.ts:41:     * Human-readable description of what the teammate does.
src/commitments/TEAM/TEAM.ts:48: * One model-facing detail carried by a teammate entry.
src/commitments/TEAM/TEAM.ts:63:    teammate: {
src/commitments/TEAM/TEAM.ts:80:     * Tool calls executed by the teammate while answering.
src/commitments/TEAM/TEAM.ts:112: * Shared TEAM usage rules appended ahead of teammate listings.
src/commitments/TEAM/TEAM.ts:117:    -   If a teammate is relevant to the request, consult that teammate using the matching tool.
src/commitments/TEAM/TEAM.ts:118:    -   Do not ask the user for information that a listed teammate can provide directly.
src/commitments/TEAM/TEAM.ts:128: * The `TEAM` commitment defines teammates that the agent can consult via tools.
src/commitments/TEAM/TEAM.ts:148:        return 'Enable the agent to consult teammate agents via dedicated tools.';
src/commitments/TEAM/TEAM.ts:172:            Registers teammate agents that the current agent can consult via tools.
src/commitments/TEAM/TEAM.ts:179:            GOAL Get expert software-development advice from the teammate when legal discussion needs technical context.
src/commitments/TEAM/TEAM.ts:191:        // Keep TEAM resilient: unresolved/malformed teammate entries are skipped, valid ones are still registered.
src/commitments/TEAM/TEAM.ts:192:        const teammates: TeamTeammate[] = parseTeamCommitmentContent(trimmedContent, { strict: false });
src/commitments/TEAM/TEAM.ts:193:        if (teammates.length === 0) {
src/commitments/TEAM/TEAM.ts:204:            (requirements._metadata?.teammates as
src/commitments/TEAM/TEAM.ts:218:        const resolvedTeammates: TeamTeammate[] = resolveTeamTeammateLabels(trimmedContent, teammates);
src/commitments/TEAM/TEAM.ts:220:        const teamEntries: TeamToolEntry[] = resolvedTeammates.map((teammate) => {
src/commitments/TEAM/TEAM.ts:221:            const profile = preResolvedProfiles[teammate.url];
src/commitments/TEAM/TEAM.ts:222:            const resolvedLabel = profile?.agentName || teammate.label;
src/commitments/TEAM/TEAM.ts:223:            const existingTeammate = existingTeammates.find((entry) => entry.url === teammate.url);
src/commitments/TEAM/TEAM.ts:226:                    createTeamToolName(teammate.url, resolvedLabel)) as string_javascript_name,
src/commitments/TEAM/TEAM.ts:227:                teammate: { ...teammate, label: resolvedLabel },
src/commitments/TEAM/TEAM.ts:253:                            description: `Question to ask ${entry.teammate.label}.`,
src/commitments/TEAM/TEAM.ts:257:                            description: `Optional background context for ${entry.teammate.label}.`,
src/commitments/TEAM/TEAM.ts:273:            if (updatedTeammates.some((existing) => existing.url === entry.teammate.url)) {
src/commitments/TEAM/TEAM.ts:278:                url: entry.teammate.url,
src/commitments/TEAM/TEAM.ts:279:                label: entry.teammate.label,
src/commitments/TEAM/TEAM.ts:280:                instructions: entry.teammate.instructions || undefined,
src/commitments/TEAM/TEAM.ts:293:                    teammates: updatedTeammates,
src/commitments/TEAM/TEAM.ts:308:     * Gets tool function implementations for teammate tools.
src/commitments/TEAM/TEAM.ts:316: * Resolves teammate labels for special pseudo-agents and keeps regular teammates unchanged.
src/commitments/TEAM/TEAM.ts:318:function resolveTeamTeammateLabels(teamContent: string, teammates: ReadonlyArray<TeamTeammate>): TeamTeammate[] {
src/commitments/TEAM/TEAM.ts:319:    return teammates.map((teammate) => {
src/commitments/TEAM/TEAM.ts:320:        if (resolvePseudoAgentKindFromUrl(teammate.url) !== 'USER') {
src/commitments/TEAM/TEAM.ts:321:            return teammate;
src/commitments/TEAM/TEAM.ts:325:            ...teammate,
src/commitments/TEAM/TEAM.ts:334: * Each teammate is listed with its tool name, TEAM instructions, and optional profile hints.
src/commitments/TEAM/TEAM.ts:337:    const teammateSections = teamEntries.map((entry, index) => {
src/commitments/TEAM/TEAM.ts:338:        const toolLine = `${index + 1}) ${entry.teammate.label} tool \`${entry.toolName}\``;
src/commitments/TEAM/TEAM.ts:357:            ${block(teammateSections.join('\n\n'))}
src/commitments/TEAM/TEAM.ts:363: * Builds the model-visible description for one teammate tool.
src/commitments/TEAM/TEAM.ts:372:            Consult teammate ${entry.teammate.label}
src/commitments/TEAM/TEAM.ts:379: * Collects structured teammate details that should stay visible to the model.
src/commitments/TEAM/TEAM.ts:385:    const instructions = entry.teammate.instructions.trim();
src/commitments/TEAM/TEAM.ts:406: * Formats one teammate detail line for the TEAM system-message section.
src/commitments/TEAM/TEAM.ts:427: * Registers tool function and title for a teammate tool.
src/commitments/TEAM/TEAM.ts:431:    teamToolTitles[entry.toolName] = `Consult ${entry.teammate.label}`;
src/commitments/TEAM/TEAM.ts:435: * Builds teammate metadata for tool results.
src/commitments/TEAM/TEAM.ts:437:function buildTeammateMetadata(entry: TeamToolEntry): TeamToolResult['teammate'] {
src/commitments/TEAM/TEAM.ts:439:        url: entry.teammate.url,
src/commitments/TEAM/TEAM.ts:440:        label: entry.teammate.label,
src/commitments/TEAM/TEAM.ts:441:        instructions: entry.teammate.instructions,
src/commitments/TEAM/TEAM.ts:443:        pseudoAgentKind: resolvePseudoAgentKindFromUrl(entry.teammate.url) || undefined,
src/commitments/TEAM/TEAM.ts:448: * Builds the teammate request text, optionally including context.
src/commitments/TEAM/TEAM.ts:466: * Builds a minimal chat prompt for teammate calls.
src/commitments/TEAM/TEAM.ts:482: * Creates teammate runtime context and marks conversation as team-only memory-disabled.
src/commitments/TEAM/TEAM.ts:503:    const teammateMetadata = buildTeammateMetadata(entry);
src/commitments/TEAM/TEAM.ts:506:        teammate: teammateMetadata,
src/commitments/TEAM/TEAM.ts:521:                name: entry.teammate.label,
src/commitments/TEAM/TEAM.ts:532:    const teammateMetadata = buildTeammateMetadata(entry);
src/commitments/TEAM/TEAM.ts:535:        teammate: teammateMetadata,
src/commitments/TEAM/TEAM.ts:546:                name: entry.teammate.label,
src/commitments/TEAM/TEAM.ts:554: * Resolves a RemoteAgent for the given teammate URL, caching the connection.
src/commitments/TEAM/TEAM.ts:583: * Creates a tool function for consulting a teammate agent.
src/commitments/TEAM/TEAM.ts:588:        const teammateMetadata: TeamToolResult['teammate'] = buildTeammateMetadata(entry);
src/commitments/TEAM/TEAM.ts:592:                error: 'Message is required to contact teammate.',
src/commitments/TEAM/TEAM.ts:593:                teammate: teammateMetadata,
src/commitments/TEAM/TEAM.ts:599:        const pseudoAgentKind = resolvePseudoAgentKindFromUrl(entry.teammate.url);
src/commitments/TEAM/TEAM.ts:615:            const remoteAgent: RemoteAgent = await getRemoteTeammateAgent(entry.teammate.url, runtimeContext);
src/commitments/TEAM/TEAM.ts:617:            const teammateResult: PromptResult = await remoteAgent.callChatModel(prompt);
src/commitments/TEAM/TEAM.ts:618:            response = teammateResult.content || '';
src/commitments/TEAM/TEAM.ts:620:                'toolCalls' in teammateResult && Array.isArray(teammateResult.toolCalls)
src/commitments/TEAM/TEAM.ts:621:                    ? (teammateResult.toolCalls as ReadonlyArray<ToolCall>)
src/commitments/TEAM/TEAM.ts:627:        const teammateReply: string =
src/commitments/TEAM/TEAM.ts:628:            response || (error ? `Unable to reach teammate. Error: ${error}` : 'No response received.');
src/commitments/TEAM/TEAM.ts:631:            teammate: teammateMetadata,
src/commitments/TEAM/TEAM.ts:633:            response: teammateReply,
src/commitments/TEAM/TEAM.ts:644:                    name: entry.teammate.label,
src/commitments/TEAM/TEAM.ts:645:                    content: teammateReply,
scripts/generate-packages\generate-packages.ts:171:        './agents/default/developer.book',
scripts/generate-packages\generate-packages.ts:172:        './packages/cli/agents/default/developer.book',
Adam


META VISIBILITY PRIVATE

FROM @Void


META COLOR #ff0b0bff
META FONT Playfair Display, sans-serif
PERSONA You are a helpful, honest, and intelligent AI assistant. Your goal is to provide accurate, clear, and concise responses while being friendly and engaging. Think step-by-step before answering complex questions.
RULE If you're unsure about something, say so and offer to look it up or clarify.
RULE You can use Markdown formatting in the messages like **bold** or *italic*
RULE You can use Markdown code blocks if needed

For example:

```javascript
console.log('Hello');
```



CLOSED










exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content src/utils/validators/url/isValidAgentUrl.ts; Get-Content src/book-2.0/agent-source/createAgentModelRequirements.ts -TotalCount 100; Get-Content src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.ts -TotalCount 140; Get-Content apps/agents-server/src/utils/agentReferenceResolver/createServerAgentReferenceResolver.ts | Select-Object -Skip 175 -First 100; Get-Content src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/augmentAgentModelRequirementsFromSource.ts -TotalCount 200' in C:\Users\me\work\ai\promptbook
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content scripts/run-codex-prompts/common/resolveCoderAgent.test.ts; Get-Content src/cli/cli-commands/coder/init.test.ts -TotalCount 230; Get-Content scripts/generate-packages/generate-packages.ts | Select-Object -Skip 145 -First 42; Get-Content 'src/cli/"'$initializePromptbookCliProgram.ts'"' -TotalCount 100; rg -n 'init|initialize' src/cli/cli-commands/coder/index.ts; Get-Content apps/coder-landing/src/components/sections/AgentsSection.tsx; Get-Content apps/coder-landing/src/data/advancedFeatures.ts -TotalCount 100" in C:\Users\me\work\ai\promptbook
 succeeded in 1780ms:
import type { string_agent_url } from '../../../types/string_agent_url';
import type { really_unknown } from '../../organization/really_unknown';
import { isValidUrl } from './isValidUrl';

/**
 * Tests if given string is valid agent URL
 *
 * Note: There are few similar functions:
 * - `isValidUrl` which tests any URL
 * - `isValidAgentUrl` *(this one)* which tests just agent URL
 * - `isValidPipelineUrl` which tests just pipeline URL
 *
 * Note: This is a pure structural validator and does not block private/internal network
 * addresses. Callers that fetch the URL server-side from an untrusted network context must
 * additionally apply the `assertSafeUrl` SSRF guard from the Agents Server before any
 * outbound request.
 *
 * @public exported from `@promptbook/utils`
 */
export function isValidAgentUrl(url: really_unknown): url is string_agent_url {
    if (!isValidUrl(url)) {
        return false;
    }

    if (!url.startsWith('https://') && !url.startsWith('http://') /* <- Note: [👣] */) {
        return false;
    }

    if (url.includes('#')) {
        // TODO: [🐠]
        return false;
    }

    return true;
}

// TODO: [🐠] Maybe more info why the URL is invalid
import type { AvailableModel } from '../../execution/AvailableModel';
import type { LlmExecutionTools } from '../../execution/LlmExecutionTools';
import { preparePersona } from '../../personas/preparePersona';
import type { string_agent_name } from '../../types/string_agent_name';
import type { string_model_name } from '../../types/string_model_name';
import type { string_system_message } from '../../types/string_prompt';
import type { string_url_image } from '../../types/string_url_image';
import type { AgentModelRequirements } from './AgentModelRequirements';
import type { CreateAgentModelRequirementsOptions } from './CreateAgentModelRequirementsOptions';
import { createAgentModelRequirementsWithCommitments } from './createAgentModelRequirementsWithCommitments';
import { parseAgentSource } from './parseAgentSource';
import { parseAgentSourceWithCommitments } from './parseAgentSourceWithCommitments';
import type { string_book } from './string_book';

/**
 * Creates model requirements for an agent based on its source.
 *
 * There are 2 similar functions:
 * - `parseAgentSource` which is a lightweight parser for agent source, it parses basic information and its purpose is to be quick and synchronous. The commitments there are hardcoded.
 * - `createAgentModelRequirements` which is an asynchronous function that creates model requirements it applies each commitment one by one and works asynchronous.
 *
 * @param agentSource - Book describing the agent.
 * @param modelName - Optional override for the agent's model.
 * @param availableModels - Models that could fulfill the agent.
 * @param llmTools - Execution tools used when selecting a best model.
 * @param options - Optional hooks such as the agent reference resolver.
 *
 * @public exported from `@promptbook/core`
 */
export async function createAgentModelRequirements(
    agentSource: string_book,
    modelName?: string_model_name,
    availableModels?: readonly AvailableModel[],
    llmTools?: LlmExecutionTools,
    options?: CreateAgentModelRequirementsOptions,
): Promise<AgentModelRequirements> {
    // If availableModels are provided and no specific modelName is given,
    // use preparePersona to select the best model
    if (availableModels && !modelName && llmTools) {
        const selectedModelName = await selectBestModelUsingPersona(agentSource, llmTools);
        return createAgentModelRequirementsWithCommitments(agentSource, selectedModelName, options);
    }

    // Use the new commitment-based system with provided or default model
    return createAgentModelRequirementsWithCommitments(agentSource, modelName, options);
}

/**
 * Selects the best model using the preparePersona function
 * This directly uses preparePersona to ensure DRY principle
 *
 * @param agentSource The agent source to derive effective profile text from
 * @param llmTools LLM tools for preparing persona
 * @returns The name of the best selected model
 *
 * @private function of `createAgentModelRequirements`
 */
async function selectBestModelUsingPersona(
    agentSource: string_book,
    llmTools: LlmExecutionTools,
): Promise<string_model_name> {
    // Parse agent source to get the effective profile description
    const { agentName, personaDescription } = parseAgentSource(agentSource);

    // Use agent name as fallback if no profile description is available
    const description = personaDescription || agentName || 'AI Agent';

    try {
        // Use preparePersona directly
        const { modelsRequirements } = await preparePersona(description, { llm: llmTools }, { isVerbose: false });

        // Extract the first model name from the requirements
        if (modelsRequirements.length > 0 && modelsRequirements[0]?.modelName) {
            return modelsRequirements[0].modelName;
        }

        // Fallback: get available models and return the first CHAT model
        const availableModels = await llmTools.listModels();
        const chatModels = availableModels.filter(({ modelVariant }) => modelVariant === 'CHAT');

        if (chatModels.length === 0) {
            throw new Error('No CHAT models available for agent model selection');
        }

        return chatModels[0]!.modelName;
    } catch (error) {
        console.warn('Failed to use preparePersona for model selection, falling back to first available model:', error);

        // Fallback: get available models and return the first CHAT model
        const availableModels = await llmTools.listModels();
        const chatModels = availableModels.filter(({ modelVariant }) => modelVariant === 'CHAT');

        if (chatModels.length === 0) {
            throw new Error('No CHAT models available for agent model selection');
        }

        return chatModels[0]!.modelName;
    }
}

import { createBasicAgentModelRequirements } from '../../commitments/_base/createEmptyAgentModelRequirements';
import type { string_model_name } from '../../types/string_model_name';
import type { AgentModelRequirements } from './AgentModelRequirements';
import type { CreateAgentModelRequirementsOptions } from './CreateAgentModelRequirementsOptions';
import { applyCommitmentsToAgentModelRequirements } from './createAgentModelRequirementsWithCommitments/applyCommitmentsToAgentModelRequirements';
import { augmentAgentModelRequirementsFromSource } from './createAgentModelRequirementsWithCommitments/augmentAgentModelRequirementsFromSource';
import { filterCommitmentsForAgentModelRequirements } from './createAgentModelRequirementsWithCommitments/filterCommitmentsForAgentModelRequirements';
import { materializeInlineKnowledgeSources } from './createAgentModelRequirementsWithCommitments/materializeInlineKnowledgeSources';
import { deduplicateSystemMessage } from './deduplicateSystemMessage';
import { parseAgentSourceWithCommitments } from './parseAgentSourceWithCommitments';
import { removeCommentsFromSystemMessage } from './removeCommentsFromSystemMessage';
import type { string_book } from './string_book';

/**
 * Creates agent model requirements by parsing commitments, applying them in source order,
 * and finalizing derived sections such as imports, example interactions, and inline knowledge uploads.
 *
 * @param agentSource - Agent source book to parse.
 * @param modelName - Optional override for the agent model name.
 * @param options - Additional options such as reference and teammate resolvers.
 * @returns Fully prepared model requirements for the parsed agent source.
 *
 * @private internal utility of `createAgentModelRequirements`
 */
export async function createAgentModelRequirementsWithCommitments(
    agentSource: string_book,
    modelName?: string_model_name,
    options?: CreateAgentModelRequirementsOptions,
): Promise<AgentModelRequirements> {
    const parseResult = parseAgentSourceWithCommitments(agentSource);
    const filteredCommitments = filterCommitmentsForAgentModelRequirements(parseResult.commitments);

    let requirements = createInitialAgentModelRequirements(parseResult.agentName, modelName);
    requirements = await applyCommitmentsToAgentModelRequirements(requirements, filteredCommitments, options);
    requirements = await augmentAgentModelRequirementsFromSource(requirements, parseResult, agentSource);
    requirements = await materializeInlineKnowledgeSources(requirements, options?.inlineKnowledgeSourceUploader);

    return finalizeRequirements(requirements);
}

/**
 * Creates the initial requirements object with the parsed agent name stored in metadata and an optional model override.
 *
 * @param agentName - Parsed agent name from the source prelude.
 * @param modelName - Optional explicit model name override.
 * @returns Initial requirements before any commitment is applied.
 *
 * @private internal utility of `createAgentModelRequirementsWithCommitments`
 */
function createInitialAgentModelRequirements(
    agentName: string | null,
    modelName?: string_model_name,
): AgentModelRequirements {
    const initialRequirements = createBasicAgentModelRequirements(agentName);
    const requirementsWithMetadata: AgentModelRequirements = {
        ...initialRequirements,
        _metadata: {
            ...initialRequirements._metadata,
            agentName,
        },
    };

    if (!modelName) {
        return requirementsWithMetadata;
    }

    return {
        ...requirementsWithMetadata,
        modelName,
    };
}

/**
 * Performs the final system-message cleanup pass after all other augmentation steps are complete.
 *
 * @param requirements - Fully built requirements before final cleanup.
 * @returns Requirements with comment lines removed and repeated instructions deduplicated in the final system message.
 *
 * @private internal utility of `createAgentModelRequirementsWithCommitments`
 */
function finalizeRequirements(requirements: AgentModelRequirements): AgentModelRequirements {
    return {
        ...requirements,
        systemMessage: deduplicateSystemMessage(removeCommentsFromSystemMessage(requirements.systemMessage)),
    };
}
                this.trackResolutionIssue({
                    commitmentType,
                    token,
                    reference: tokenValue,
                    message: pseudoAgentReplacement.message,
                });
                parts.push(this.getMissingReferenceReplacement(commitmentType));
                continue;
            }

            const resolved = await this.resolveReferenceUrl(tokenValue);
            if (resolved) {
                parts.push(resolved);
                continue;
            }

            hasMissingReference = true;
            this.trackResolutionIssue({
                commitmentType,
                token,
                reference: tokenValue,
                message: `Agent reference "${tokenValue}" was not found`,
            });
            parts.push(this.getMissingReferenceReplacement(commitmentType));
        }

        parts.push(content.slice(lastIndex));
        const resolvedContent = parts.join('');

        if (!hasMissingReference) {
            return resolvedContent;
        }

        if (commitmentType === 'FROM') {
            return 'VOID';
        }

        if (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS') {
            return resolvedContent.trim();
        }

        if (commitmentType === 'TEAM') {
            return resolvedContent;
        }

        return resolvedContent;
    }

    /**
     * Resolves pseudo-agent compact references (`{User}`, `{Void}`, `@user`, ...) for one commitment.
     */
    private resolvePseudoAgentReference(
        commitmentType: BookCommitment,
        tokenValue: string,
    ):
        | {
              status: 'resolved';
              content: string;
          }
        | {
              status: 'disallowed';
              message: string;
          }
        | {
              status: 'not-pseudo';
          } {
        const pseudoAgentKind = resolvePseudoAgentKindFromReference(tokenValue);
        if (!pseudoAgentKind) {
            return { status: 'not-pseudo' };
        }

        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
            return {
                status: 'disallowed',
                message: `Pseudo-agent "${tokenValue}" cannot be used in ${commitmentType} commitment.`,
            };
        }

        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
            return {
                status: 'resolved',
                content: VOID_PSEUDO_AGENT_REFERENCE,
            };
        }

        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
            return {
                status: 'resolved',
                content: '',
            };
        }

        return {
            status: 'resolved',
            content: createPseudoAgentUrl(pseudoAgentKind),
        };
    }

    /**
     * Resolves a single compact reference payload to an absolute agent URL.
import { spaceTrim } from 'spacetrim';
import { $fileImportPlugins } from '../../../import-plugins/$fileImportPlugins';
import { promptbookFetch } from '../../../scrapers/_common/utils/promptbookFetch';
import { isValidUrl } from '../../../utils/validators/url/isValidUrl';
import type { AgentModelRequirements } from '../AgentModelRequirements';
import { extractMcpServers } from '../createAgentModelRequirements';
import type { string_book } from '../string_book';
import type { ParsedAgentSourceWithCommitments } from './ParsedAgentSourceWithCommitments';

/**
 * Regex pattern matching markdown horizontal lines that should not be copied into the final system message.
 *
 * @private internal constant of `augmentAgentModelRequirementsFromSource`
 */
const HORIZONTAL_LINE_PATTERN = /^[\s]*[-_*][\s]*[-_*][\s]*[-_*][\s]*[-_*]*[\s]*$/;

/**
 * MIME type prefixes treated as binary and therefore not eligible for text import plugins.
 *
 * @private internal constant of `augmentAgentModelRequirementsFromSource`
 */
const BINARY_MIME_TYPE_PREFIXES = [
    'image/',
    'video/',
    'audio/',
    'application/octet-stream',
    'application/pdf',
    'application/zip',
];

/**
 * Adds source-derived sections after commitments have been applied.
 *
 * @param requirements - Requirements after commitment application and USE aggregation.
 * @param parseResult - Parsed source used to recover non-commitment prose and examples.
 * @param agentSource - Original source used to recover MCP server declarations.
 * @returns Requirements with source-derived sections appended.
 *
 * @private function of `createAgentModelRequirementsWithCommitments`
 */
export async function augmentAgentModelRequirementsFromSource(
    requirements: AgentModelRequirements,
    parseResult: ParsedAgentSourceWithCommitments,
    agentSource: string_book,
): Promise<AgentModelRequirements> {
    requirements = await importReferencedFiles(requirements);
    requirements = appendMcpServers(requirements, agentSource);
    requirements = appendNonCommitmentContent(requirements, parseResult);
    requirements = appendExampleInteractions(requirements, parseResult);

    return appendUnknownCommitmentContent(requirements, parseResult);
}

/**
 * Imports text files referenced by IMPORT commitments and appends their transformed content to the system message.
 *
 * @param requirements - Requirements possibly containing `importedFileUrls`.
 * @returns Requirements with imported file content appended to the system message.
 *
 * @private internal utility of `augmentAgentModelRequirementsFromSource`
 */
async function importReferencedFiles(requirements: AgentModelRequirements): Promise<AgentModelRequirements> {
    const importedFileUrls = requirements.importedFileUrls;
    if (!importedFileUrls || importedFileUrls.length === 0) {
        return requirements;
    }

    for (const fileUrl of importedFileUrls) {
        try {
            const importedContent = await createImportedFileSystemMessage(fileUrl);
            requirements = appendSystemMessageSection(requirements, importedContent);
        } catch (error) {
            console.warn(`Failed to import file ${fileUrl}:`, error);
        }
    }

    return requirements;
}

/**
 * Loads, validates, and transforms one imported file into a system-message block.
 *
 * @param fileUrl - Remote URL or local path declared in an IMPORT commitment.
 * @returns Imported text ready to append to the system message.
 *
 * @private internal utility of `importReferencedFiles`
 */
async function createImportedFileSystemMessage(fileUrl: string): Promise<string> {
    await mockedSecurityCheck(fileUrl);

    const { content, mimeType } = await readImportedFile(fileUrl);
    if (isBinaryMimeType(mimeType)) {
        throw new Error(`Importing binary files is not allowed: ${mimeType}`);
    }

    const plugin = $fileImportPlugins.find((fileImportPlugin) => fileImportPlugin.canImport(mimeType));
    if (!plugin) {
        throw new Error(`No import plugin found for MIME type: ${mimeType}`);
    }

    return plugin.import(content, mimeType);
}

/**
 * Reads one imported file and normalizes the MIME type expected by file import plugins.
 *
 * @param fileUrl - Remote URL or local path declared in an IMPORT commitment.
 * @returns Plain-text content together with a normalized MIME type.
 *
 * @private internal utility of `createImportedFileSystemMessage`
 */
async function readImportedFile(fileUrl: string): Promise<{ content: string; mimeType: string }> {
    if (isValidUrl(fileUrl)) {
        const response = await promptbookFetch(fileUrl);
        if (!response.ok) {
            throw new Error(`Failed to fetch ${fileUrl}: ${response.statusText}`);
        }

        return {
            content: await response.text(),
            mimeType: normalizeImportedMimeType(response.headers.get('Content-Type')),
        };
    }

    /*
    TODO: !!!! Commented out this case because we need to work in Browser-compatible mode in many packages, use passed `fs` instead
    } else if (isValidFilePath(fileUrl)) {
        // [x🟢x] This code is expected to run in Node environment if local files are used
        const fs = await import('fs/promises');
        content = await fs.readFile(fileUrl, 'utf-8');
        const extension = getFileExtension(fileUrl);
        mimeType = extensionToMimeType(extension as string);
    */

    throw new Error(`Invalid file URL or path: ${fileUrl}`);
}

/**
 * Normalizes MIME types returned by fetch so plugin lookup works on bare MIME values without charset suffixes.
 *
 * @param mimeType - Raw response MIME type header.
 * @returns Normalized MIME type fallbacking to `text/plain`.
 *
 * @private internal utility of `readImportedFile`
 */
function normalizeImportedMimeType(mimeType: string | null): string {
    return (mimeType || 'text/plain').split(';')[0]!.trim();
}

/**
 * Appends extracted MCP server identifiers from the original source.
 *
 * @param requirements - Current requirements snapshot.
 * @param agentSource - Original agent source used for MCP extraction.
 * @returns Requirements with `mcpServers` set when MCP commitments are present.
 *
 * @private internal utility of `augmentAgentModelRequirementsFromSource`
 */
function appendMcpServers(requirements: AgentModelRequirements, agentSource: string_book): AgentModelRequirements {
    const mcpServers = extractMcpServers(agentSource);
    if (mcpServers.length === 0) {
        return requirements;
    }

    return {
        ...requirements,
        mcpServers,
    };
}

/**
 * Appends non-commitment prose from the source after filtering out blank lines and markdown horizontal rules.
 *
 * @param requirements - Current requirements snapshot.
 * @param parseResult - Parsed source including non-commitment lines.
 * @returns Requirements with the remaining prose appended to the system message.
 *
 * @private internal utility of `augmentAgentModelRequirementsFromSource`
 */
function appendNonCommitmentContent(
    requirements: AgentModelRequirements,
    parseResult: ParsedAgentSourceWithCommitments,
): AgentModelRequirements {
    const nonCommitmentContent = getNonCommitmentContent(parseResult);
    if (!nonCommitmentContent) {
        return requirements;
    }

    return appendSystemMessageSection(requirements, nonCommitmentContent);
}

/**
 * Appends unknown commitment blocks as final extra context without applying commitment side effects.
 *
 * @param requirements - Current requirements snapshot.
 * @param parseResult - Parsed source including unknown commitment blocks.
 * @returns Requirements with unknown commitment context appended at the end of the system message.
 *
 * @private internal utility of `augmentAgentModelRequirementsFromSource`
 */

 succeeded in 3283ms:
import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises'; * @private internal utility of package generation
 */
async function assertPackageGenerationWorkingTreeState(isCommitted: boolean): Promise<void> {
    if (isCommitted && !(await isWorkingTreeClean(process.cwd()))) {
        throw new Error(`Working tree is not clean`);
import type { Command } from 'commander';
import { CLAIM } from '../config';
import { PROMPTBOOK_ENGINE_VERSION } from '../version';
import { $initializeAboutCommand } from './cli-commands/about';
import { $initializeAgentCommand } from './cli-commands/agent';
import { $initializeAgentFolderCommand } from './cli-commands/agent-folder';
import { $initializeAgentsServerCommand } from './cli-commands/agents-server';
import { $initializeCoderCommand } from './cli-commands/coder';
import { $initializeHelloCommand } from './cli-commands/hello';
import { $initializeListModelsCommand } from './cli-commands/list-models';
import { $initializeListScrapersCommand } from './cli-commands/list-scrapers';
import { $initializeLoginCommand } from './cli-commands/login';
import { $initializeMakeCommand } from './cli-commands/make';
import { $initializePrettifyCommand } from './cli-commands/prettify';
import { $initializeRunCommand } from './cli-commands/run';
import { $initializeStartAgentsServerCommand } from './cli-commands/start-agents-server';
import { $initializeStartPipelinesServerCommand } from './cli-commands/start-pipelines-server';
import { $initializeTestCommand } from './cli-commands/test-command';
import { $addGlobalOptionsToCommand } from './common/$addGlobalOptionsToCommand';
import { $deprecateCliCommand } from './common/$deprecateCliCommand';

/**
 * Shared deprecation text for top-level CLI commands backed by the old pipeline system.
 */
const OLD_PIPELINE_SYSTEM_DEPRECATION_MESSAGE = 'This command is part of the old pipeline system.';

/**
 * Shared deprecation text for top-level CLI commands backed by the old pre-agent system.
 */
const OLD_SYSTEM_DEPRECATION_MESSAGE = 'This command is part of the old system.';

/**
 * Deprecation guidance for top-level `ptbk` commands that remain for compatibility.
 */
const DEPRECATED_TOP_LEVEL_COMMAND_MESSAGES: Readonly<Record<string, string>> = {
    run: OLD_PIPELINE_SYSTEM_DEPRECATION_MESSAGE,
    login: OLD_PIPELINE_SYSTEM_DEPRECATION_MESSAGE,
    make: OLD_PIPELINE_SYSTEM_DEPRECATION_MESSAGE,
    prettify: OLD_PIPELINE_SYSTEM_DEPRECATION_MESSAGE,
    test: OLD_PIPELINE_SYSTEM_DEPRECATION_MESSAGE,
    'list-models': OLD_SYSTEM_DEPRECATION_MESSAGE,
    'list-scrapers': OLD_SYSTEM_DEPRECATION_MESSAGE,
    'start-agents-server': 'Use `ptbk agents-server start` instead.',
    'start-pipelines-server': OLD_PIPELINE_SYSTEM_DEPRECATION_MESSAGE,
};

/**
 * Registers the CLI commands on a fresh Commander program without parsing arguments or running an action.
 * Tests can configure output and exit handling before registration, so subcommands inherit those settings.
 *
 * @private internal utility of `promptbookCli`
 */
export function $initializePromptbookCliProgram(program: Command): void {
    program.name('promptbook');
    program.alias('ptbk');
    program.version(PROMPTBOOK_ENGINE_VERSION);
    program.description(CLAIM);

    // Commands are listed in registration order. Keep `coder` first and hide deprecated commands below.
    $initializeCoderCommand(program);
    $initializeAgentCommand(program);
    $initializeAgentFolderCommand(program);
    $initializeAgentsServerCommand(program);
    $initializeAboutCommand(program);
    $initializeHelloCommand(program);
    $initializeRunCommand(program);
    $initializeLoginCommand(program);
    $initializeMakeCommand(program);
    $initializePrettifyCommand(program);
    $initializeTestCommand(program);
    $initializeListModelsCommand(program);
    $initializeListScrapersCommand(program);
    $initializeStartAgentsServerCommand(program);
    $initializeStartPipelinesServerCommand(program);

    for (const command of program.commands) {
        const deprecationMessage = DEPRECATED_TOP_LEVEL_COMMAND_MESSAGES[command.name()];

        if (deprecationMessage !== undefined) {
            $deprecateCliCommand(command, deprecationMessage);
        }
    }

    program.commands.forEach($addGlobalOptionsToCommand);
}

// Note: [🟡] Code for CLI command registration [$initializePromptbookCliProgram](src/cli/$initializePromptbookCliProgram.ts) should never be published outside of `@promptbook/cli`
rg: src/cli/cli-commands/coder/index.ts: IO error for operation on src/cli/cli-commands/coder/index.ts: The system cannot find the file specified. (os error 2)
import { AGENT_RUN_COMMAND } from '@/data/commands';
import { DeveloperAgentBookPreview } from '@/components/DeveloperAgentBookPreview/DeveloperAgentBookPreview';
import { TerminalBlock } from '@/components/TerminalBlock/TerminalBlock';

/**
 * Renders the agents section - the `--agent agents/developer.book` option together with
 * a readonly `<BookEditor/>` showing the default developer agent.
 *
 * Note: Specified in [`specs/sections/agent-book.md`](../../../specs/sections/agent-book.md)
 */
export function AgentsSection() {
    return (
        <section id="agents" className="mx-auto max-w-6xl scroll-mt-20 px-4 py-20">
            <h2 className="font-display text-3xl font-bold text-white md:text-4xl">
                Give your agent a <span className="text-promptbook-green">soul</span>, in plain text
            </h2>
            <p className="mt-4 max-w-3xl text-lg text-gray-300">
                Every run can carry an agent written in the{' '}
                <a
                    href="https://github.com/webgptorg/book"
                    className="text-promptbook-blue underline decoration-promptbook-blue-dark underline-offset-4 hover:text-promptbook-green"
                >
                    Book language
                </a>
                , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
                file into the system message of every coding prompt.
            </p>

            <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
                <div>
                    <p className="mb-4 text-gray-400">
                        Point <strong className="text-white">ptbk coder</strong> at any agent file with{' '}
                        <code className="text-promptbook-blue">--agent</code>:
                    </p>
                    <TerminalBlock command={AGENT_RUN_COMMAND} />
                    <p className="mt-6 text-gray-400">
                        <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                        other file in your repository to change how your agent codes.
                    </p>
                    <p className="mt-4 text-gray-400">
                        Route a ready task to that agent with a status line such as{' '}
                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
                        line of the Book all work.
                    </p>
                    <p className="mt-4 text-gray-400">
                        A finished task is signed by the agent, not only by the harness that carried it:{' '}
                        <code className="text-promptbook-blue">[x] by Developer on OpenAI Codex `gpt-5.6-luna`</code>.
                    </p>
                </div>

                <div>
                    <p className="mb-4 font-mono text-sm text-gray-500">readonly preview of agents/developer.book</p>
                    <DeveloperAgentBookPreview />
                </div>
            </div>
        </section>
    );
}
import {
    AGENT_FILTER_COMMAND,
    GENERATE_BOILERPLATES_COMMAND,
    GIT_CHANGES_CONTINUE_COMMAND,
    INIT_GIT_SYNC_COMMAND,
    LIST_COMMAND,
    MODEL_FILTER_COMMAND,
    PING_COMMAND,
    PING_PERIOD_COMMAND,
    TEST_BEFORE_FIX_COMMAND,
    VERIFY_COMMAND,
} from './commands';

/**
 * One advanced feature of `ptbk coder` presented as a card with a terminal snippet.
 */
export type AdvancedFeatureDefinition = {
    /**
     * Short title of the feature
     */
    readonly title: string;

    /**
     * One- or two-sentence description of the feature
     */
    readonly description: string;

    /**
     * Terminal snippet demonstrating the feature
     */
    readonly sampleCommand: string;
};

/**
 * Advanced features of `ptbk coder` shown in the features grid, from everyday to power-user.
 *
 * Note: Specified in [`specs/sections/advanced-features.md`](../../specs/sections/advanced-features.md)
 */
export const ADVANCED_FEATURES: ReadonlyArray<AdvancedFeatureDefinition> = [
    {
        title: 'Verified by your tests',
        description:
            'Run any test command after each prompt. When it fails, ptbk coder hands the output back to the agent, which retries until the tests pass.',
        sampleCommand: 'ptbk coder run --harness claude-code --test npm test',
    },
    {
        title: 'Test before coding',
        description:
            'Run the tests before the first coding prompt. Stop on failures that were already there, or let one repair prompt fix them before the backlog starts.',
        sampleCommand: TEST_BEFORE_FIX_COMMAND,
    },
    {
        title: 'Commits with its own identity',
        description:
            'Every successful round lands under a git identity that belongs to the agent, GPG-signed if you set that up. You can always tell which commits it wrote.',
        sampleCommand: 'CODING_AGENT_GIT_NAME="Promptbook Coding Agent"',
    },
    {
        title: 'Autopilot git',
        description:
            'Pull before prompts and push after commits, so a long-running queue stays in sync with your remote.',
        sampleCommand: 'ptbk coder run --harness claude-code --auto-pull --auto-push',
    },
    {
        title: 'Git-synced housekeeping',
        description:
            'ptbk coder init, add, generate-boilerplates and verify all take the same --commit, --auto-push and --auto-pull switches. Setting up a project, queueing prompts and archiving finished ones leave no uncommitted work behind. Verify pulls and pushes around every single verification.',
        sampleCommand: INIT_GIT_SYNC_COMMAND,
    },
    {
        title: 'Isolated worktrees',
        description:
            'Implement every prompt in its own temporary git worktree with its own environment. Verified work lands back on your branch as one commit. If a task will not merge, ptbk coder marks it failed and keeps its worktree so you can look at it. Deeply nested repositories work on Windows too.',
        sampleCommand: 'ptbk coder run --harness claude-code --isolate',
    },
    {
        title: 'Kanban web UI',
        description:
            'ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser.',
        sampleCommand: 'ptbk coder server --port 4441 --harness claude-code',
    },
    {
        title: 'Prompt priorities',
        description: 'Give prompts a priority and process only the range you want in the current run.',
        sampleCommand: 'ptbk coder run --harness claude-code --min-priority 1 --max-priority 5',
    },
    {
        title: 'Model-specific prompts',
        description:
            'Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it.',
        sampleCommand: MODEL_FILTER_COMMAND,
    },
    {
        title: 'Agent-specific prompts',
        description:
            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
        sampleCommand: AGENT_FILTER_COMMAND,
    },
    {
        title: 'Pacing and retries',

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n 'initializeCoderProjectConfiguration|referencedArtifactStatuses|CODER_DEVELOPER' src/cli -g '*.test.ts'; Get-Content apps/coder-landing/specs/sections/agent-book.md; Get-Content apps/coder-landing/package.json; rg -n 'max-len|jsdoc|naming' .eslintrc*; Get-Content src/book-2.0/agent-source/TeammateProfileResolver.ts; Get-Content src/book-2.0/agent-source/normalizeAgentName.ts" in C:\Users\me\work\ai\promptbook
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content src/commitments/TEAM/TEAM.ts | Select-Object -Skip 328 -First 82; Get-Content src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/applyCommitmentsToAgentModelRequirements.ts -TotalCount 200; Get-Content src/book-2.0/agent-source/parseAgentSourceWithCommitments.ts -TotalCount 155; Get-Content src/cli/cli-commands/coder/printInitializationSummary.ts -TotalCount 125; Get-Content apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts -TotalCount 100' in C:\Users\me\work\ai\promptbook
 succeeded in 3531ms:
}

/**
 * Builds the textual TEAM section body for the final system message.
 *
 * Each teammate is listed with its tool name, TEAM instructions, and optional profile hints.
 */
function buildTeamSystemMessageBody(teamEntries: ReadonlyArray<TeamToolEntry>): string {
    const teammateSections = teamEntries.map((entry, index) => {
        const toolLine = `${index + 1}) ${entry.teammate.label} tool \`${entry.toolName}\``;
        const detailLines = collectTeamEntryDetails(entry).map(formatTeamEntryDetailLine);

        if (detailLines.length === 0) {
            return toolLine;
        }

        return spaceTrim(
            (block) => `
                ${toolLine}
                ${block(detailLines.join('\n'))}
            `,
        );
    });

    return spaceTrim(
        (block) => `
            ${block(TEAM_SYSTEM_MESSAGE_GUIDANCE)}

            ${block(teammateSections.join('\n\n'))}
        `,
    );
}

/**
 * Builds the model-visible description for one teammate tool.
 *
 * @private
 */
function buildTeamToolDescription(entry: TeamToolEntry): string {
    const detailLines = collectTeamEntryDetails(entry).map(({ label, content }) => `${label}: ${content}`);

    return spaceTrim(
        (block) => `
            Consult teammate ${entry.teammate.label}
            ${block(detailLines.join('\n'))}
        `,
    );
}

/**
 * Collects structured teammate details that should stay visible to the model.
 *
 * @private
 */
function collectTeamEntryDetails(entry: TeamToolEntry): TeamEntryDetail[] {
    const details: TeamEntryDetail[] = [];
    const instructions = entry.teammate.instructions.trim();
    const description = entry.description?.trim() || '';

    if (instructions) {
        details.push({
            label: 'TEAM instructions',
            content: instructions,
        });
    }

    if (description) {
        details.push({
            label: 'Profile',
            content: description,
        });
    }

    return details;
}

/**
 * Formats one teammate detail line for the TEAM system-message section.
 *
 * @private
 */
function formatTeamEntryDetailLine(detail: TeamEntryDetail): string {
import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
import type { ParsedCommitment } from '../../../commitments/_base/ParsedCommitment';
import { getCommitmentDefinition } from '../../../commitments/_common/getCommitmentDefinition';
import type { chococake } from '../../../utils/organization/really_any';
import type { AgentModelRequirements } from '../AgentModelRequirements';
import type { CreateAgentModelRequirementsOptions } from '../CreateAgentModelRequirementsOptions';
import { parseTeamCommitmentContent } from '../parseTeamCommitment';
import type { TeammateProfile } from '../TeammateProfileResolver';

/**
 * Commitment types whose content may contain compact agent references that must be resolved before applying the commitment.
 *
 * @private internal constant of `applyCommitmentsToAgentModelRequirements`
 */
const COMMITMENTS_WITH_AGENT_REFERENCES = new Set<BookCommitment>(['FROM', 'IMPORT', 'IMPORTS', 'TEAM']);

/**
 * Applies parsed commitments one by one while keeping the per-commitment steps focused and easy to follow.
 *
 * @param requirements - Current requirements snapshot.
 * @param commitments - Commitments already filtered for DELETE-like invalidations.
 * @param options - Optional reference and teammate resolvers.
 * @returns Requirements after all applicable commitments are processed.
 *
 * @private function of `createAgentModelRequirementsWithCommitments`
 */
export async function applyCommitmentsToAgentModelRequirements(
    requirements: AgentModelRequirements,
    commitments: ReadonlyArray<ParsedCommitment>,
    options?: CreateAgentModelRequirementsOptions,
): Promise<AgentModelRequirements> {
    for (const [index, commitment] of commitments.entries()) {
        if (shouldSkipCommitmentApplication(commitment, index, commitments.length)) {
            continue;
        }

        const commitmentContent = await resolveCommitmentContent(commitment, options?.agentReferenceResolver);
        requirements = await preResolveTeammateProfilesForTeamCommitment(
            requirements,
            commitment,
            commitmentContent,
            options,
        );
        requirements = applyCommitmentDefinitionSafely(requirements, commitment, commitmentContent);
    }

    return requirements;
}

/**
 * Resolves compact agent references for commitment types that support them.
 *
 * @param commitment - Commitment currently being applied.
 * @param agentReferenceResolver - Optional resolver for compact agent references.
 * @returns Original or resolved commitment content.
 *
 * @private internal utility of `applyCommitmentsToAgentModelRequirements`
 */
async function resolveCommitmentContent(
    commitment: ParsedCommitment,
    agentReferenceResolver?: CreateAgentModelRequirementsOptions['agentReferenceResolver'],
): Promise<string> {
    if (!agentReferenceResolver || !isAgentReferenceCommitment(commitment.type)) {
        return commitment.content;
    }

    try {
        return await agentReferenceResolver.resolveCommitmentContent(commitment.type, commitment.content);
    } catch (error) {
        console.warn(
            `Failed to resolve commitment references for ${commitment.type}, falling back to safe defaults:`,
            error,
        );
        return getSafeReferenceCommitmentFallback(commitment.type, commitment.content);
    }
}

/**
 * Returns a safe fallback content when a resolver fails to transform a reference commitment.
 *
 * @param commitmentType - Commitment being resolved.
 * @param originalContent - Original unresolved commitment content.
 * @returns Fallback content that keeps requirement creation resilient.
 *
 * @private internal utility of `applyCommitmentsToAgentModelRequirements`
 */
function getSafeReferenceCommitmentFallback(commitmentType: BookCommitment, originalContent: string): string {
    if (commitmentType === 'FROM') {
        return 'VOID';
    }

    if (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS' || commitmentType === 'TEAM') {
        return '';
    }

    return originalContent;
}

/**
 * Checks whether the commitment content may need agent-reference resolution before application.
 *
 * @param commitmentType - Commitment type to check.
 * @returns `true` when the commitment can contain compact agent references.
 *
 * @private internal utility of `resolveCommitmentContent`
 */
function isAgentReferenceCommitment(commitmentType: ParsedCommitment['type']): boolean {
    return COMMITMENTS_WITH_AGENT_REFERENCES.has(commitmentType);
}

/**
 * Determines whether a commitment should be skipped before resolution or application.
 *
 * @param commitment - Commitment under consideration.
 * @param commitmentIndex - Zero-based position among filtered commitments.
 * @param commitmentCount - Total number of filtered commitments.
 * @returns `true` when the commitment should not be applied.
 *
 * @private internal utility of `applyCommitmentsToAgentModelRequirements`
 */
function shouldSkipCommitmentApplication(
    commitment: ParsedCommitment,
    commitmentIndex: number,
    commitmentCount: number,
): boolean {
    return commitment.type === 'CLOSED' && commitmentIndex !== commitmentCount - 1;
}

/**
 * Pre-resolves teammate profiles for TEAM commitments so the TEAM commitment definition can reuse richer metadata.
 *
 * @param requirements - Current requirements snapshot.
 * @param commitment - Commitment currently being prepared.
 * @param commitmentContent - Already resolved TEAM commitment content.
 * @param options - Optional teammate profile resolvers.
 * @returns Requirements with pre-resolved teammate profiles stored in metadata when possible.
 *
 * @private internal utility of `applyCommitmentsToAgentModelRequirements`
 */
async function preResolveTeammateProfilesForTeamCommitment(
    requirements: AgentModelRequirements,
    commitment: ParsedCommitment,
    commitmentContent: string,
    options?: CreateAgentModelRequirementsOptions,
): Promise<AgentModelRequirements> {
    const profileResolver = options?.teammateProfileResolver ?? options?.agentReferenceResolver;
    if (commitment.type !== 'TEAM' || !profileResolver?.resolveTeammateProfile) {
        return requirements;
    }

    try {
        const parsedTeammates = parseTeamCommitmentContent(commitmentContent, { strict: false });
        const preResolvedTeammateProfiles = clonePreResolvedTeammateProfiles(requirements._metadata);

        for (const teammate of parsedTeammates) {
            if (preResolvedTeammateProfiles[teammate.url]) {
                continue;
            }

            const profile = await profileResolver.resolveTeammateProfile(teammate.url);
            if (profile) {
                preResolvedTeammateProfiles[teammate.url] = profile;
            }
        }

        return {
            ...requirements,
            _metadata: {
                ...requirements._metadata,
                preResolvedTeammateProfiles,
            },
        };
    } catch (error) {
        console.warn('Failed to pre-resolve teammate profiles for TEAM commitment:', error);
        return requirements;
    }
}

/**
 * Clones the metadata bucket used to cache teammate profiles resolved ahead of TEAM commitment application.
 *
 * @param metadata - Current requirements metadata.
 * @returns Mutable copy of the cached teammate profile map.
 *
 * @private internal utility of `preResolveTeammateProfilesForTeamCommitment`
 */
function clonePreResolvedTeammateProfiles(metadata?: Record<string, chococake>): Record<string, TeammateProfile> {
    return {
        ...((metadata?.preResolvedTeammateProfiles as Record<string, TeammateProfile> | undefined) ?? {}),
    };
}

/**
 * Applies the registered commitment definition while isolating the failure handling from the main loop.
 *
 * @param requirements - Current requirements snapshot.
 * @param commitment - Commitment whose definition should be applied.
 * @param commitmentContent - Final content passed into the definition.
 * @returns Updated requirements, or the original requirements when the commitment fails.
 *
import { spaceTrim } from 'spacetrim';
import type { BookCommitment } from '../../commitments/_base/BookCommitment';
import type { ParsedCommitment } from '../../commitments/_base/ParsedCommitment';
import { COMMITMENT_REGISTRY } from '../../commitments/index';
import type { AgentSourceParseResult } from './AgentSourceParseResult';
import { getBookCommitmentLineType } from './getBookCommitmentLineType';
import { parseAgentSourcePrelude } from './parseAgentSourcePrelude';
import type { ParsedUnknownCommitment } from './ParsedUnknownCommitment';
import type { string_book } from './string_book';

/**
 * Regex pattern to match horizontal lines (markdown thematic breaks)
 * Matches 3 or more hyphens, underscores, or asterisks (with optional spaces between)
 */
const HORIZONTAL_LINE_PATTERN = /^[\s]*[-_*][\s]*[-_*][\s]*[-_*][\s]*[-_*]*[\s]*$/;

/**
 * Registered commitment block currently being collected from Book source.
 *
 * @private internal type of `parseAgentSourceWithCommitments`
 */
type CurrentKnownCommitment = {
    readonly kind: 'known';
    readonly type: string;
    readonly startLineNumber: number;
    readonly originalStartLine: string;
    readonly contentLines: string[];
};

/**
 * Unknown commitment block currently being collected from Book source.
 *
 * @private internal type of `parseAgentSourceWithCommitments`
 */
type CurrentUnknownCommitment = {
    readonly kind: 'unknown';
    readonly type: string;
    readonly startLineNumber: number;
    readonly originalStartLine: string;
    readonly sourceLines: string[];
};

/**
 * Commitment-like source block currently being collected.
 *
 * @private internal type of `parseAgentSourceWithCommitments`
 */
type CurrentCommitment = CurrentKnownCommitment | CurrentUnknownCommitment;

/**
 * Parses agent source using the new commitment system with multiline support
 * This function replaces the hardcoded commitment parsing in the original parseAgentSource
 *
 * The first non-empty line is always consumed as plain-text agent name. Commitment parsing
 * starts only after that title line has been fixed.
 *
 * @private internal utility of `parseAgentSource`
 */
export function parseAgentSourceWithCommitments(agentSource: string_book): Omit<AgentSourceParseResult, 'agentHash'> {
    if (!agentSource || !agentSource.trim()) {
        return {
            agentName: null,
            commitments: [],
            unknownCommitments: [],
            nonCommitmentLines: [],
        };
    }

    const { lines, agentName, agentNameLineIndex, agentNameLineNumber } = parseAgentSourcePrelude(agentSource);

    const commitments: ParsedCommitment[] = [];
    const unknownCommitments: ParsedUnknownCommitment[] = [];
    const nonCommitmentLines: string[] = agentNameLineIndex >= 0 ? [lines[agentNameLineIndex]!] : [];

    // Parse commitments with multiline support.
    let currentCommitment: CurrentCommitment | null = null;

    // Process lines starting from after the agent name line
    const startIndex = agentNameLineIndex >= 0 ? agentNameLineIndex + 1 : 0;
    let isInsideCodeBlock = false;
    for (let i = startIndex; i < lines.length; i++) {
        const line = lines[i];
        if (line === undefined) {
            continue;
        }

        const trimmedLine = line.trim();

        // Check if this line starts or ends a code block
        if (trimmedLine.startsWith('```')) {
            isInsideCodeBlock = !isInsideCodeBlock;

            appendLineToCurrentCommitmentOrNonCommitmentLines(currentCommitment, line, nonCommitmentLines);
            continue;
        }

        if (isInsideCodeBlock) {
            appendLineToCurrentCommitmentOrNonCommitmentLines(currentCommitment, line, nonCommitmentLines);
            continue;
        }

        // Check if this line starts a new commitment
        let foundNewCommitment = false;
        for (const definition of COMMITMENT_REGISTRY) {
            const typeRegex = definition.createTypeRegex();
            const match = typeRegex.exec(line.trim());

            if (match && match.groups?.type) {
                appendCurrentCommitment(currentCommitment, commitments, unknownCommitments);

                // Extract the initial content from the commitment line
                const fullRegex = definition.createRegex();
                const fullMatch = fullRegex.exec(line.trim());
                const initialContent = fullMatch?.groups?.contents || '';

                // Start a new commitment
                currentCommitment = {
                    kind: 'known',
                    type: definition.type,
                    startLineNumber: i + 1,
                    originalStartLine: line,
                    contentLines: initialContent ? [initialContent] : [],
                };
                foundNewCommitment = true;
                break;
            }
        }

        if (foundNewCommitment) {
            continue;
        }

        const unknownCommitmentType = getBookCommitmentLineType(line);
        if (unknownCommitmentType) {
            appendCurrentCommitment(currentCommitment, commitments, unknownCommitments);
            currentCommitment = {
                kind: 'unknown',
                type: unknownCommitmentType,
                startLineNumber: i + 1,
                originalStartLine: line,
                sourceLines: [line],
            };
            continue;
        }

        // Check if this is a horizontal line (ends any current commitment)
        const isHorizontalLine = HORIZONTAL_LINE_PATTERN.test(line);
        if (isHorizontalLine) {
            appendCurrentCommitment(currentCommitment, commitments, unknownCommitments);
            currentCommitment = null;
            // Add horizontal line to non-commitment lines
            nonCommitmentLines.push(line);
            continue;
        }

import colors from 'colors';
import type { InitializationStatus } from './boilerplateTemplates';
import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
import { formatDisplayPath } from './formatDisplayPath';
import type { CoderInitializationSummary } from './initializeCoderProjectConfiguration';

/**
 * Prints a readable summary of what was initialized for the user.
 *
 * @private function of `coder init` command
 */
export function printInitializationSummary(summary: CoderInitializationSummary): void {
    console.info(colors.green('Promptbook coder configuration initialized.'));
    printInitializationStatusLine('prompts/', summary.promptsDirectoryStatus);
    printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
    printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
    printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
    printInitializationStatusLine('.env', summary.envFileStatus);
    printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
    printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
    printInitializationStatusLine('.vscode/settings.json', summary.vscodeSettingsFileStatus);

    for (const referencedArtifactStatus of summary.referencedArtifactStatuses) {
        printInitializationStatusLine(
            formatDisplayPath(referencedArtifactStatus.relativeFilePath),
            referencedArtifactStatus.status,
        );
    }

    if (summary.addedPackageJsonScriptNames.length > 0) {
        printInitializationNote(`Added npm scripts: ${summary.addedPackageJsonScriptNames.join(', ')}`, colors.cyan);
    } else {
        printInitializationNote('All Promptbook coder npm scripts are already present.', colors.gray);
    }

    if (summary.initializedEnvVariableNames.length > 0) {
        printInitializationNote(`Added env variables: ${summary.initializedEnvVariableNames.join(', ')}`, colors.cyan);
    } else {
        printInitializationNote('Required coder env variables are already present.', colors.gray);
    }
}

/**
 * Formats one initialization status into a human-readable label.
 */
function formatInitializationStatus(status: InitializationStatus | CoderReferencedArtifactStatus): string {
    if (status === 'created') {
        return 'created';
    }

    if (status === 'updated') {
        return 'updated';
    }

    if (status === 'not-referenced') {
        return 'not referenced by the added scripts, kept as is';
    }

    return 'unchanged';
}

/**
 * Prints one checked initialization-status line.
 */
function printInitializationStatusLine(
    relativePath: string,
    status: InitializationStatus | CoderReferencedArtifactStatus,
): void {
    console.info(colors.gray(`✔ ${relativePath}: ${formatInitializationStatus(status)}`));
}

/**
 * Prints one checked initialization note.
 */
function printInitializationNote(message: string, colorize: (message: string) => string): void {
    console.info(colorize(`✔ ${message}`));
}

// Note: [🟡] Code for coder init summary printing [printInitializationSummary](src/cli/cli-commands/coder/printInitializationSummary.ts) should never be published outside of `@promptbook/cli`
import { afterEach, describe, expect, it, jest } from '@jest/globals';
import { spaceTrim } from 'spacetrim';
import { book, validateBook } from '../../../../src/_packages/core.index'; // <- [🚾]
import type { string_book } from '../../../../src/_packages/types.index'; // <- [🚾]
import type { AgentCollection } from '../../../../src/collection/agent-collection/AgentCollection';
import {
    DEFAULT_FEDERATED_AGENT_IMPORT_CONFIGURATION,
    FEDERATED_AGENT_IMPORT_MAX_ATTEMPTS,
} from '../constants/federatedAgentImport';
import { createServerAgentReferenceResolver } from './agentReferenceResolver/createServerAgentReferenceResolver';
import { resolveInheritedAgentSource } from './resolveInheritedAgentSource';

/**
 * Original fetch implementation restored after each test.
 */
const ORIGINAL_FETCH = global.fetch;

/**
 * Mocked HTTP response definition keyed by normalized URL.
 */
type MockFetchRoute = {
    readonly body: string | Record<string, unknown>;
    readonly contentType?: string;
    readonly status?: number;
    readonly headers?: Record<string, string>;
};

/**
 * Builds a minimal collection mock used by resolver tests.
 *
 * @param agentRecords - Local agents exposed by `listAgents`.
 * @returns Agent collection mock.
 */
function createMockAgentCollection(agentRecords: Array<{ agentName: string; permanentId?: string }>): AgentCollection {
    return {
        listAgents: async () => agentRecords,
    } as unknown as AgentCollection;
}

/**
 * Normalizes URLs used by `importAgent` so tests can ignore revalidation/cycle query params.
 *
 * @param rawUrl - Raw request URL.
 * @returns Stable key for the mocked response map.
 */
function normalizeFetchKey(rawUrl: string): string {
    const url = new URL(rawUrl);

    if (url.pathname.endsWith('/api/book')) {
        return `${url.origin}${url.pathname}?recursionLevel=${url.searchParams.get('recursionLevel') || ''}`;
    }

    return `${url.origin}${url.pathname}`;
}

/**
 * Installs a deterministic `fetch` mock for remote/local agent imports.
 *
 * @param routes - Mocked responses keyed by normalized URL.
 */
function mockFetchRoutes(routes: Record<string, MockFetchRoute>): void {
    global.fetch = jest.fn(async (input: RequestInfo | URL) => {
        const requestUrl = String(input);
        const route = routes[normalizeFetchKey(requestUrl)];

        if (!route) {
            throw new Error(`Unexpected fetch URL: ${requestUrl}`);
        }

        return new Response(typeof route.body === 'string' ? route.body : JSON.stringify(route.body), {
            status: route.status || 200,
            headers: {
                'content-type':
                    route.contentType || (typeof route.body === 'string' ? 'text/plain' : 'application/json'),
                ...(route.headers || {}),
            },
        });
    }) as typeof fetch;
}

afterEach(() => {
    global.fetch = ORIGINAL_FETCH;
    jest.restoreAllMocks();
});

describe('how `resolveInheritedAgentSource` works', () => {
    it('should resolve explicit `FROM` commitment in agent', async () => {
        mockFetchRoutes({
            'https://core-test.ptbk.io/agents/test-0/api/book?recursionLevel=1': {
                body: book`
                    Test Parent

                    FROM VOID
                    NONCE 0
                `,
            },
        });

        await expect(
            resolveInheritedAgentSource(

 succeeded in 3633ms:
src/cli\cli-commands\coder\boilerplateTemplates.test.ts:8:    CODER_DEVELOPER_AGENT_FILE_PATH,
src/cli\cli-commands\coder\boilerplateTemplates.test.ts:9:    DEFAULT_CODER_DEVELOPER_AGENT_SOURCE_FILE_PATH,
src/cli\cli-commands\coder\boilerplateTemplates.test.ts:14:import type { CoderInitializationSummary } from './initializeCoderProjectConfiguration';
src/cli\cli-commands\coder\boilerplateTemplates.test.ts:15:import { initializeCoderProjectConfiguration } from './initializeCoderProjectConfiguration';
src/cli\cli-commands\coder\boilerplateTemplates.test.ts:33:    return summary.referencedArtifactStatuses.find(
src/cli\cli-commands\coder\boilerplateTemplates.test.ts:76:        const summary = await initializeCoderProjectConfiguration(projectPath);
src/cli\cli-commands\coder\boilerplateTemplates.test.ts:80:        expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
src/cli\cli-commands\coder\boilerplateTemplates.test.ts:95:        const developerAgentContent = await readFile(join(projectPath, CODER_DEVELOPER_AGENT_FILE_PATH), 'utf-8');
src/cli\cli-commands\coder\boilerplateTemplates.test.ts:97:            join(process.cwd(), DEFAULT_CODER_DEVELOPER_AGENT_SOURCE_FILE_PATH),
src/cli\cli-commands\coder\boilerplateTemplates.test.ts:148:        await writeFile(join(projectPath, CODER_DEVELOPER_AGENT_FILE_PATH), 'Custom developer agent\n', 'utf-8');
src/cli\cli-commands\coder\boilerplateTemplates.test.ts:156:        const summary = await initializeCoderProjectConfiguration(projectPath);
src/cli\cli-commands\coder\boilerplateTemplates.test.ts:161:        expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('not-referenced');
src/cli\cli-commands\coder\boilerplateTemplates.test.ts:193:        expect(await readFile(join(projectPath, CODER_DEVELOPER_AGENT_FILE_PATH), 'utf-8')).toBe(
src/cli\cli-commands\coder\boilerplateTemplates.test.ts:234:        const summary = await initializeCoderProjectConfiguration(projectPath);
src/cli\cli-commands\coder\boilerplateTemplates.test.ts:258:        expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('not-referenced');
src/cli\cli-commands\coder\boilerplateTemplates.test.ts:259:        await expect(readFile(join(projectPath, CODER_DEVELOPER_AGENT_FILE_PATH), 'utf-8')).rejects.toThrow();
src/cli\cli-commands\coder\boilerplateTemplates.test.ts:273:        await initializeCoderProjectConfiguration(projectPath);
src/cli\cli-commands\coder\boilerplateTemplates.test.ts:274:        const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
# Section: Agent personas (Book)

Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.

## Copy

-   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.

## Layout

Two columns on desktop (stacked on mobile):

### Left — the option

1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.

### Right — the agent itself

1. Caption above (small monospace, gray): `readonly preview of agents/developer.book`
2. The [readonly Book editor embed](../components/book-editor-embed.md) showing the verbatim source from [`../content/developer-agent.md`](../content/developer-agent.md).
{
    "name": "coder-landing",
    "scripts": {
        "predev": "npx kill-port 4025",
        "dev": "next dev -p 4025",
        "test-build": "npm run build",
        "prebuild": "npx kill-port 4025",
        "build": "next build",
        "start": "next start -p 4025",
        "lint": "next lint --max-warnings=0",
        "postinstall": "cd ../../ && npm ci"
    }
}
rg: .eslintrc*: IO error for operation on .eslintrc*: The filename, directory name, or volume label syntax is incorrect. (os error 123)
/**
 * Profile data for a teammate agent resolved from its source or backend.
 *
 * @private internal type for TEAM commitment profile enrichment
 */
export type TeammateProfile = {
    /**
     * The human-readable name of the agent.
     */
    readonly agentName: string;

    /**
     * Short profile text for what the agent does, from the last GOAL commitment
     * or deprecated PERSONA fallback.
     */
    readonly personaDescription: string | null;
};

/**
 * Resolves profile data for teammate agents referenced in TEAM commitments.
 *
 * Implementations are expected to return the actual human-readable name and
 * description of each teammate agent identified by URL, so TEAM tools can be
 * created with meaningful labels and descriptions rather than technical IDs.
 *
 * @private internal type for TEAM commitment profile enrichment
 */
export type TeammateProfileResolver = {
    /**
     * Returns profile data for the given teammate agent URL.
     *
     * @param url - Canonical teammate URL from the resolved TEAM commitment content.
     * @returns Teammate profile or `null` when the URL cannot be resolved locally.
     */
    resolveTeammateProfile(url: string): Promise<TeammateProfile | null>;
};
import { spaceTrim } from 'spacetrim';
import type { string_agent_name } from '../../types/string_agent_name';
import { titleToName } from '../../utils/normalization/titleToName';

/**
 * Normalizes agent name from arbitrary string to valid agent name
 *
 * Note: [🔂] This function is idempotent.
 *
 * @public exported from `@promptbook/core`
 */
export function normalizeAgentName(rawAgentName: string): string_agent_name {
    return titleToName(spaceTrim(rawAgentName));
}

codex
The server’s inheritance resolver already handles Adam defaults, repeated `FROM` clauses, imports, and cycles. I’ll move that logic into the engine and keep a thin server wrapper for HTTP loading. The coder will supply a filesystem resolver that searches books by title and resolves each relative path from the book that declares it.

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content apps/agents-server/src/utils/resolveInheritedAgentSource.ts | Select-Object -Skip 145 -First 90; Get-Content apps/agents-server/src/utils/agentReferenceResolver/createServerAgentReferenceResolver.ts | Select-Object -Skip 174 -First 56; Get-Content src/book-2.0/agent-source/parseAgentSourceWithCommitments.ts -TotalCount 75; Get-Content src/book-2.0/agent-source/CreateAgentModelRequirementsOptions.ts -TotalCount 90; Get-Content scripts/run-codex-prompts/main/runCodexPrompts.ts | Select-Object -Skip 76 -First 72; rg --files src/cli/cli-commands/common | rg '([Bb]ook|[Aa]gentSource)'" in C:\Users\me\work\ai\promptbook
 succeeded in 3618ms:
            ),
        );
    }

    return resolvedParentReference as string_agent_url;
}

/**
 * Shared options for resolving one agent source with inheritance and imports applied.
 */
type ResolveInheritedAgentSourceOptions = ImportAgentOptions & {
    /**
     * The URL of the Adam agent to use as the default ancestor
     *
     * @default 'https://core.ptbk.io/agents/adam'
     */
    readonly adamAgentUrl?: string_agent_url;
    /**
     * Custom resolver used to expand compact agent references.
     */
    readonly agentReferenceResolver?: AgentReferenceResolver;
    /**
     * Canonical URL of the currently resolved agent.
     */
    readonly currentAgentUrl?: string_agent_url;
    /**
     * Additional equivalent URLs that should be treated as the current agent while detecting cycles.
     */
    readonly currentAgentAliases?: ReadonlyArray<string_agent_url>;
    /**
     * Already visited agent URLs in the current resolution stack.
     */
    readonly inheritancePath?: ReadonlyArray<string_agent_url>;
    /**
     * Retry configuration used for federated imported-agent loading.
     */
    readonly federatedAgentImportConfiguration?: FederatedAgentImportConfiguration;
    /**
     * Optional same-instance source importer used before falling back to HTTP import.
     */
    readonly agentSourceImporter?: AgentSourceImporter;
};

/**
 * Context passed to a custom agent source importer.
 */
export type AgentSourceImporterContext = {
    /**
     * Commitment that requested the imported source.
     */
    readonly commitmentType: 'FROM' | 'IMPORT';

    /**
     * Import options propagated from the current resolution pass.
     */
    readonly importAgentOptions: ImportAgentOptions;

    /**
     * Retry configuration used by the default HTTP importer.
     */
    readonly federatedAgentImportConfiguration: FederatedAgentImportConfiguration;
};

/**
 * Imports one agent source before the resolver falls back to the default HTTP importer.
 *
 * Return `null` when the importer does not own the given URL.
 */
export type AgentSourceImporter = (
    agentUrl: string_agent_url,
    context: AgentSourceImporterContext,
) => Promise<string_book | null>;

/**
 * Parent inheritance state derived before rewriting the source body.
 */
type ResolvedParentAgentContext = {
    /**
     * Effective parent URL after resolving explicit `FROM`, `FROM VOID`, or implicit Adam fallback.
     */
    readonly parentAgentUrl: string_agent_url | null;

    /**
     * Parent source body without title/status, ready to be embedded into the child source.
     */
    readonly parentAgentSourceCorpus: string | null;

    /**
     * Unresolved compact-reference issues captured while resolving `FROM`.
     */
                hasMissingReference = true;
                this.trackResolutionIssue({
                    commitmentType,
                    token,
                    reference: tokenValue,
                    message: pseudoAgentReplacement.message,
                });
                parts.push(this.getMissingReferenceReplacement(commitmentType));
                continue;
            }

            const resolved = await this.resolveReferenceUrl(tokenValue);
            if (resolved) {
                parts.push(resolved);
                continue;
            }

            hasMissingReference = true;
            this.trackResolutionIssue({
                commitmentType,
                token,
                reference: tokenValue,
                message: `Agent reference "${tokenValue}" was not found`,
            });
            parts.push(this.getMissingReferenceReplacement(commitmentType));
        }

        parts.push(content.slice(lastIndex));
        const resolvedContent = parts.join('');

        if (!hasMissingReference) {
            return resolvedContent;
        }

        if (commitmentType === 'FROM') {
            return 'VOID';
        }

        if (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS') {
            return resolvedContent.trim();
        }

        if (commitmentType === 'TEAM') {
            return resolvedContent;
        }

        return resolvedContent;
    }

    /**
     * Resolves pseudo-agent compact references (`{User}`, `{Void}`, `@user`, ...) for one commitment.
     */
    private resolvePseudoAgentReference(
        commitmentType: BookCommitment,
        tokenValue: string,
    ):
import { spaceTrim } from 'spacetrim';
import type { BookCommitment } from '../../commitments/_base/BookCommitment';
import type { ParsedCommitment } from '../../commitments/_base/ParsedCommitment';
import { COMMITMENT_REGISTRY } from '../../commitments/index';
import type { AgentSourceParseResult } from './AgentSourceParseResult';
import { getBookCommitmentLineType } from './getBookCommitmentLineType';
import { parseAgentSourcePrelude } from './parseAgentSourcePrelude';
import type { ParsedUnknownCommitment } from './ParsedUnknownCommitment';
import type { string_book } from './string_book';

/**
 * Regex pattern to match horizontal lines (markdown thematic breaks)
 * Matches 3 or more hyphens, underscores, or asterisks (with optional spaces between)
 */
const HORIZONTAL_LINE_PATTERN = /^[\s]*[-_*][\s]*[-_*][\s]*[-_*][\s]*[-_*]*[\s]*$/;

/**
 * Registered commitment block currently being collected from Book source.
 *
 * @private internal type of `parseAgentSourceWithCommitments`
 */
type CurrentKnownCommitment = {
    readonly kind: 'known';
    readonly type: string;
    readonly startLineNumber: number;
    readonly originalStartLine: string;
    readonly contentLines: string[];
};

/**
 * Unknown commitment block currently being collected from Book source.
 *
 * @private internal type of `parseAgentSourceWithCommitments`
 */
type CurrentUnknownCommitment = {
    readonly kind: 'unknown';
    readonly type: string;
    readonly startLineNumber: number;
    readonly originalStartLine: string;
    readonly sourceLines: string[];
};

/**
 * Commitment-like source block currently being collected.
 *
 * @private internal type of `parseAgentSourceWithCommitments`
 */
type CurrentCommitment = CurrentKnownCommitment | CurrentUnknownCommitment;

/**
 * Parses agent source using the new commitment system with multiline support
 * This function replaces the hardcoded commitment parsing in the original parseAgentSource
 *
 * The first non-empty line is always consumed as plain-text agent name. Commitment parsing
 * starts only after that title line has been fixed.
 *
 * @private internal utility of `parseAgentSource`
 */
export function parseAgentSourceWithCommitments(agentSource: string_book): Omit<AgentSourceParseResult, 'agentHash'> {
    if (!agentSource || !agentSource.trim()) {
        return {
            agentName: null,
            commitments: [],
            unknownCommitments: [],
            nonCommitmentLines: [],
        };
    }

    const { lines, agentName, agentNameLineIndex, agentNameLineNumber } = parseAgentSourcePrelude(agentSource);

    const commitments: ParsedCommitment[] = [];
    const unknownCommitments: ParsedUnknownCommitment[] = [];
    const nonCommitmentLines: string[] = agentNameLineIndex >= 0 ? [lines[agentNameLineIndex]!] : [];

    // Parse commitments with multiline support.
import type { AgentReferenceResolver } from './AgentReferenceResolver';
import type { InlineKnowledgeSourceUploader } from '../../utils/knowledge/inlineKnowledgeSource';
import type { TeammateProfileResolver } from './TeammateProfileResolver';

/**
 * Options for `createAgentModelRequirements` and `createAgentModelRequirementsWithCommitments`.
 *
 * @public exported from `@promptbook/core`
 */
export type CreateAgentModelRequirementsOptions = {
    /**
     * Resolver that transforms compact agent references (FROM, IMPORT, TEAM) into concrete URLs.
     */
    readonly agentReferenceResolver?: AgentReferenceResolver;

    /**
     * Optional hook used to upload inline knowledge files before finalizing the requirements.
     */
    readonly inlineKnowledgeSourceUploader?: InlineKnowledgeSourceUploader;

    /**
     * Optional resolver that provides actual agent names and descriptions for teammate URLs.
     *
     * When provided, TEAM tools are created with the agent's real human-readable name and
     * persona description rather than technical IDs derived from URL path segments.
     */
    readonly teammateProfileResolver?: TeammateProfileResolver;
};
    stats: PromptStats;
    nextPrompt?: PromptSelection;
};

/**
 * Main entry point for running prompts with the selected agent.
 *
 * @param providedOptions - Optional pre-parsed options. If not provided, will parse from process.argv
 *
 * @public exported from `@promptbook/cli`
 */
export async function runCodexPrompts(providedOptions?: RunOptions): Promise<void> {
    const options = normalizeRunOptions(providedOptions ?? parseRunOptions(process.argv.slice(2)));
    validateRunCodexPromptOptions(options);
    resetCoderRunControls();

    const runStartDate = moment();
    const { isRichUiEnabled, progressDisplay, uiHandle } = createRunDisplays(options, runStartDate);
    const waitForRequestedPause = createPauseWaiter({
        isRichUiEnabled,
        progressDisplay,
        uiHandle,
        // Note: Every pause checkpoint of the whole run goes through this one waiter, so watching the free disk
        //       space here covers each round, each verification and each runner without repeating the check
        guardFreeDiskSpace: createFreeDiskSpaceGuard({
            inspectedPath: process.cwd(),
            isAskingQuestionsEnabled: options.isAskingQuestionsEnabled ?? true,
        }),
    });

    startPauseListenerIfNeeded(isRichUiEnabled);

    try {
        const resolvedCoderContext = await resolveCoderContext(options.context, process.cwd());
        const resolvedCoderAgent = await resolveCoderAgent(options.agent, process.cwd());
        const resolvedAgentSystemMessage = resolvedCoderAgent?.systemMessage;

        if (await runDryRunIfRequested(options)) {
            return;
        }

        const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
        // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
        //       place where the whole run report of prompt status lines and run traces is put together
        const runnerMetadata: PromptRunnerMetadata = {
            ...harnessRunnerMetadata,
            agentName: resolvedCoderAgent?.agentName,
        };
        const promptRunnerIdentity: PromptRunnerIdentity = {
            harnessName: options.agentName,
            modelName: actualRunnerModel,
            agentReferences: resolvedCoderAgent?.agentReferences,
        };
        console.info(colors.green(`Running prompts with ${runner.name}`));

        initializeRunUi(uiHandle, runner.name, actualRunnerModel, options);
        await initializeRunUiAgentVisual(uiHandle, resolvedCoderAgent?.agentSource);
        await refreshCoderRunUiSubscriptionUsage({
            runner,
            uiState: uiHandle?.state,
        });
        await seedCachedAveragePromptDuration({
            options,
            actualRunnerModel,
            progressDisplay,
            uiHandle,
        });

        let hasShownUpcomingTasks = false;
        let hasWaitedForStart = false;
        let previousRoundStartTime: number | undefined;
        let previousRoundEndTime: number | undefined;
src/cli/cli-commands/common\promptbook-cli\PromptbookCliInstallationStatus.ts
src/cli/cli-commands/common\promptbook-cli\PromptbookCliInstallation.ts
src/cli/cli-commands/common\promptbook-cli\formatPromptbookCliInstallationWarning.ts
src/cli/cli-commands/common\promptbook-cli\buildPromptbookCliInstallCommand.ts
src/cli/cli-commands/common\promptbook-cli\$updatePromptbookCliInstallation.ts
src/cli/cli-commands/common\promptbook-cli\$updatePromptbookCliInstallation.test.ts
src/cli/cli-commands/common\promptbook-cli\$resolvePromptbookCliInstallations.ts
src/cli/cli-commands/common\promptbook-cli\$resolvePromptbookCliInstallations.test.ts
src/cli/cli-commands/common\promptbook-cli\$ensurePromptbookCliInstallations.ts
src/cli/cli-commands/common\promptbook-cli\$ensurePromptbookCliInstallations.test.ts
src/cli/cli-commands/common\promptbook-cli\$checkPromptbookCliInstallations.ts
src/cli/cli-commands/common\promptbook-cli\$checkPromptbookCliInstallations.test.ts

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content src/commitments/_base/ParsedCommitment.ts; Get-Content scripts/spellcheck.sh -TotalCount 80; Get-Content .prettierrc; Get-Content .eslintrc.js -TotalCount 110' in C:\Users\me\work\ai\promptbook
 succeeded in 2223ms:
import type { BookCommitment } from './BookCommitment';

/**
 * Parsed commitment line from agent source
 */

export type ParsedCommitment = {
    /**
     * The commitment type (e.g., 'PERSONA', 'KNOWLEDGE')
     */
    type: BookCommitment;

    /**
     * The content part of the commitment
     */
    content: string;

    /**
     * The original line from the agent source
     */
    originalLine: string;

    /**
     * Line number in the agent source (1-based)
     */
    lineNumber: number;
};
#!/usr/bin/env bash

cspell "src/**/*.{ts,tsx,js,jsx,md,json}" || {
    cat <<EOF
🚨 Spellcheck errors found

Add to the dictionary:
./other/cspell-dictionaries/

↗ Then commit with message:
Fix spellcheck
EOF
    exit 1
}
{
    "endOfLine": "lf",
    "tabWidth": 4,
    "singleQuote": true,
    "trailingComma": "all",
    "arrowParens": "always",
    "printWidth": 120
}
module.exports = {
    // Note: Look more at ./CONTRIBUTING.md for more details
    env: {
        browser: true,
        es2021: true,
        node: true,
    },
    extends: ['eslint:recommended', 'plugin:@typescript-eslint/recommended'],
    parser: '@typescript-eslint/parser',
    parserOptions: {
        ecmaVersion: 12,
        sourceType: 'module',
    },
    plugins: ['@typescript-eslint' /*, 'jest'*/],
    rules: {
        // Note: Indentation is fully managed by Prettier
        // indent: ['error', 4],
        'linebreak-style': ['error', 'unix'],

        // Note: There are places I want to use ${variable} in strings but not using yet
        // quotes: ['error', 'single'],
        semi: ['error', 'always'],
        // 'jest/valid-expect': 'error',

        '@typescript-eslint/consistent-type-definitions': ['error', 'type'],
        '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }],
        // '@typescript-eslint/no-floating-promises': 'error',

        'no-magic-numbers': [
            'error',
            {
                ignore: [
                    -1,
                    0,
                    1,
                    2, // Note: Semantically distinct - common for binary choice or pairs
                    3, // Note: Semantically distinct - common for small counts or headings
                    4, // Note: Semantically distinct - common for indentation or small counts
                    5, // Note: Semantically distinct - common for small counts
                    6, // Note: Semantically distinct - common for small counts
                    7, // Note: Semantically distinct - common for small counts
                    8, // Note: Semantically distinct - common for bytes or bits
                    10, // Note: Semantically distinct - common for base 10 or small offsets
                    16, // Note: Semantically distinct - common for hexadecimal
                    20, // Note: Semantically distinct - common for small lengths
                    30, // Note: Semantically distinct - common for small lengths or seconds
                    60, // Note: Semantically distinct - common for time (seconds/minutes)
                    100, // Note: Semantically distinct - common for percentage
                    255, // Note: Semantically distinct - common for color channels
                    256, // Note: Semantically distinct - common for color channels or bytes
                    500, // Note: Semantically distinct - common for HTTP errors or small delays
                    1000, // Note: Semantically distinct - common for time (milliseconds)
                    1024, // Note: Semantically distinct - common for bytes (1KB)
                    120, // Note: Semantically distinct - common for degrees (1/3 of a circle)
                    180, // Note: Semantically distinct - common for degrees (1/2 of a circle)
                    240, // Note: Semantically distinct - common for degrees (2/3 of a circle)
                    300, // Note: Semantically distinct - common for degrees (5/6 of a circle)
                    360, // Note: Semantically distinct - common for degrees (full circle)
                    400, // Note: Semantically distinct - common for HTTP errors or small delays
                    401, // Note: Semantically distinct - common for HTTP errors
                    404, // Note: Semantically distinct - common for HTTP errors
                    410, // Note: Semantically distinct - common for HTTP errors
                    429, // Note: Semantically distinct - common for HTTP errors
                    502, // Note: Semantically distinct - common for HTTP errors
                    503, // Note: Semantically distinct - common for HTTP errors
                    504, // Note: Semantically distinct - common for HTTP errors
                    2000, // Note: Semantically distinct - common for delays (2 seconds)
                    3500, // Note: Semantically distinct - common for delays (3.5 seconds)
                    5000, // Note: Semantically distinct - common for delays (5 seconds)
                    14, // Note: Semantically distinct - common for days or small counts
                    36, // Note: Semantically distinct - common for hashes or small offsets
                    40, // Note: Semantically distinct - common for worktime or small counts
                    50, // Note: Semantically distinct - common for small counts or offsets
                    63, // Note: Semantically distinct - common for base 64 or small offsets
                    186, // Note: Semantically distinct - common for color threshold
                    200, // Note: Semantically distinct - common for small counts or delays
                    800, // Note: Semantically distinct - common for delays
                    1200, // Note: Semantically distinct - common for delays
                    2500, // Note: Semantically distinct - common for delays
                    3000, // Note: Semantically distinct - common for delays
                    60000, // Note: Semantically distinct - common for time (1 minute)
                    65535, // Note: Semantically distinct - common for ports
                    16777215, // Note: Semantically distinct - common for color (0xFFFFFF)
                    1000000, // Note: Semantically distinct - common for pricing (per million)
                    0.1, // Note: Semantically distinct - common for small offsets or ratios
                    0.2126, // Note: Semantically distinct - common for luminance
                    0.299, // Note: Semantically distinct - common for luminance
                    0.5, // Note: Semantically distinct - common for half
                    0.587, // Note: Semantically distinct - common for luminance
                    0.7152, // Note: Semantically distinct - common for luminance
                    0.0722, // Note: Semantically distinct - common for luminance
                    0.114, // Note: Semantically distinct - common for luminance
                    0.9, // Note: Semantically distinct - common for ratios
                    0.95, // Note: Semantically distinct - common for ratios
                    0.01, // Note: Semantically distinct - common for ratios
                    192, // Note: Semantically distinct - common for private IP addresses
                    168, // Note: Semantically distinct - common for private IP addresses
                    172, // Note: Semantically distinct - common for private IP addresses
                    232, // Note: Semantically distinct - common for base 58
                    8192, // Note: Semantically distinct - common for power of 2 or buffer size
                    15, // Note: Semantically distinct - common for small counts or base
                    31, // Note: Semantically distinct - common for hashes or IP masks
                    58, // Note: Semantically distinct - common for base 58
                    201, // Note: Semantically distinct - common for HTTP status
                    768, // Note: Semantically distinct - common for screen width
                ],
                ignoreArrayIndexes: true,
                ignoreDefaultValues: true,
            },
        ],

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "@'
from pathlib import Path
import re
core = Path('src/book-2.0/agent-source')
server = Path('apps/agents-server/src/utils')
for name, subdir in [('explicitFromCommitment.ts', ''), ('AgentReferenceResolutionIssue.ts', 'agentReferenceResolver'), ('extractAgentReferenceTokens.ts', 'agentReferenceResolver')]:
    source = server / subdir / name
    content = source.read_text(encoding='utf-8')
    content = content.replace(\"import type { string_book } from '../../../../src/_packages/types.index'; // <- [🚾]\", \"import type { string_book } from './string_book';\")
    content = content.replace(\"'../../../../../src/commitments/_base/BookCommitment'\", \"'../../commitments/_base/BookCommitment'\")
    content = content.replace(\"'../../../../../src/book-2.0/agent-source/AgentReferenceResolver'\", \"'./AgentReferenceResolver'\")
    content = re.sub(r'/\\*\\*((?:(?"'!'"\\*/).)*)\\*/\\nexport ', lambda match: '/**' + match[1] + (' * @private internal utility of agent reference resolution\\n ' if '@private' not in match[1] else '') + '*/\\nexport ', content, flags=re.S)
    (core / name).write_text(content, encoding='utf-8')
    relative = '../../../../../src' if subdir else '../../../../src'
    source.write_text(f\"export * from '{relative}/book-2.0/agent-source/{source.stem}';\\n\", encoding='utf-8')
source = (server / 'resolveInheritedAgentSource.ts').read_text(encoding='utf-8')
source = source.replace(\"import type { string_agent_url, string_book } from '../../../../src/_packages/types.index'; // <- [🚾]\", \"import type { string_agent_url } from '../../types/string_agent_url';\\nimport type { string_book } from './string_book';\")
source = source.replace(\"'../../../../src/book-2.0/agent-source/\", \"'./\").replace(\"'../../../../src/\", \"'../../\")
source = source.replace(\"'./agentReferenceResolver/AgentReferenceResolutionIssue'\", \"'./AgentReferenceResolutionIssue'\")
source = re.sub(r'import \\{\\n    DEFAULT_FEDERATED_AGENT_IMPORT_CONFIGURATION,.*?from \\'../constants/federatedAgentImport\\';\\n', '', source, flags=re.S)
source = source.replace(\"import { type ImportAgentOptions } from './importAgent';\\n\", '')
source = source.replace(\"import { importAgentWithFallback } from './importAgentWithFallback';\\n\", '')
source = source.replace('type ResolveInheritedAgentSourceOptions = ImportAgentOptions & {', 'export type ResolveInheritedAgentSourceOptions = {\\n    /** Current recursion depth, forwarded to the source importer. */\\n    readonly recursionLevel?: number;')
source = re.sub(r'    /\\*\\*\\n     \\* Retry (?:configuration|policy).*?\\n    readonly federatedAgentImportConfiguration["'^;]+;'"\\n', '', source, flags=re.S)
source = source.replace('readonly agentSourceImporter?: AgentSourceImporter;', 'readonly agentSourceImporter: AgentSourceImporter;')
source = source.replace('readonly importAgentOptions: ImportAgentOptions;', 'readonly importAgentOptions: {\\n        readonly recursionLevel?: number;\\n        readonly inheritancePath?: ReadonlyArray<string_agent_url>;\\n    };')
source = source.replace(') => Promise<string_book | null>;', ') => Promise<string_book>;')
source = source.replace('    options?: ResolveInheritedAgentSourceOptions,', '    options: ResolveInheritedAgentSourceOptions,')
source = source.replace('function createAgentImportContext(options?: ResolveInheritedAgentSourceOptions)', 'function createAgentImportContext(options: ResolveInheritedAgentSourceOptions)')
source = source.replace('        federatedAgentImportConfiguration:\\n            options?.federatedAgentImportConfiguration || DEFAULT_FEDERATED_AGENT_IMPORT_CONFIGURATION,\\n', '')
source = source.replace('agentSourceImporter: options?.agentSourceImporter,', 'agentSourceImporter: options.agentSourceImporter,')
start = source.index('    const locallyImportedAgentSource =')
end = source.index('    return getAgentSourceCorpus', start)
source = source[:start] + '''    const importedAgentSource = await context.agentSourceImporter(agentUrl, {
        commitmentType,
        importAgentOptions: context.importAgentOptions,
    });

''' + source[end:]
source = source.replace('Imports one agent source before the resolver falls back to the default HTTP importer.\\n *\\n * Return "'`null` when the importer does not own the given URL.'"', 'Loads and recursively resolves one referenced agent using the caller\\'s transport.')
source = source.replace('Optional same-instance source importer used before falling back to HTTP import.', 'Source importer supplied by the host application.')
source = source.replace('Optional importer for same-instance agent sources.', 'Importer supplied by the host application.')
source = source.replace('// TODO: [🐱‍🚀][⏩] This function should be in "'`/src` and exported from `@promptbook/core`'"\\n', '')
source = re.sub(r'/\\*\\*((?:(?"'!'"\\*/).)*)\\*/\\nexport ', lambda match: '/**' + match[1] + (' * @private internal utility of agent source resolution\\n ' if '@private' not in match[1] else '') + '*/\\nexport ', source, flags=re.S)
(core / 'resolveInheritedAgentSource.ts').write_text(source, encoding='utf-8')
'@ | python -" in C:\Users\me\work\ai\promptbook
 succeeded in 2080ms:
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "@'
from pathlib import Path
Path('apps/agents-server/src/utils/resolveInheritedAgentSource.ts').write_text('''import type { string_book } from '../../../../src/book-2.0/agent-source/string_book';
import {
    resolveInheritedAgentSource as resolveAgentSource,
    type ResolveInheritedAgentSourceOptions as SharedResolveInheritedAgentSourceOptions,
    type AgentSourceImporterContext as SharedAgentSourceImporterContext,
} from '../../../../src/book-2.0/agent-source/resolveInheritedAgentSource';
import type { string_agent_url } from '../../../../src/types/string_agent_url';
import {
    DEFAULT_FEDERATED_AGENT_IMPORT_CONFIGURATION,
    type FederatedAgentImportConfiguration,
} from '../constants/federatedAgentImport';
import { importAgentWithFallback } from './importAgentWithFallback';

/**
 * Context passed to a same-instance importer, including the server's HTTP retry policy.
 */
export type AgentSourceImporterContext = SharedAgentSourceImporterContext & {
    readonly federatedAgentImportConfiguration: FederatedAgentImportConfiguration;
};

/**
 * Imports resolved same-instance sources, returning "'`null` for URLs owned by another server.
 */
export type AgentSourceImporter = (
    agentUrl: string_agent_url,
    context: AgentSourceImporterContext,
) => Promise<string_book | null>;

/**
 * Server transport options for the shared Book inheritance resolver.
 */
type ResolveInheritedAgentSourceOptions = Omit<SharedResolveInheritedAgentSourceOptions, '"'agentSourceImporter'> & {
    readonly federatedAgentImportConfiguration?: FederatedAgentImportConfiguration;
    readonly agentSourceImporter?: AgentSourceImporter;
};

/**
 * Applies the shared inheritance rules using same-instance loading and the server's HTTP fallback.
 */
export function resolveInheritedAgentSource(
    agentSource: string_book,
    options: ResolveInheritedAgentSourceOptions = {},
): Promise<string_book> {
    const federatedAgentImportConfiguration =
        options.federatedAgentImportConfiguration || DEFAULT_FEDERATED_AGENT_IMPORT_CONFIGURATION;

    return resolveAgentSource(agentSource, {
        ...options,
        agentSourceImporter: async (agentUrl, context) => {
            const localSource = await options.agentSourceImporter?.(agentUrl, {
                ...context,
                federatedAgentImportConfiguration,
            });

            return localSource ?? importAgentWithFallback(
                agentUrl,
                context.importAgentOptions,
                federatedAgentImportConfiguration,
            );
        },
    });
}
''', encoding='utf-8')
'@ | python -" in C:\Users\me\work\ai\promptbook
 succeeded in 3319ms:
apply patch
patch: completed
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\common\ensureAdamAgentBook.ts
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\common\resolveBundledAgentBookPath.ts
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}

diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n 'originalLine|content:|lineNumber:|unknownCommitments.push' src/book-2.0/agent-source/parseAgentSourceWithCommitments.ts; rg -n 'options:|options\\?|federated|ImportAgentOptions|agentSourceImporter' src/book-2.0/agent-source/resolveInheritedAgentSource.ts; Get-Content src/book-2.0/agent-source/parseAgentSourceWithCommitments.ts | Select-Object -Skip 285 -First 40; Get-Content src/book-2.0/agent-source/ParsedUnknownCommitment.ts" in C:\Users\me\work\ai\promptbook
 succeeded in 2220ms:
189:        unknownCommitments.push({
191:            originalLine: currentCommitment.originalStartLine,
192:            lineNumber: currentCommitment.startLineNumber,
200:        content: spaceTrim(currentCommitment.contentLines.join('\n')),
201:        originalLine: currentCommitment.originalStartLine,
202:        lineNumber: currentCommitment.startLineNumber,
180:    readonly agentSourceImporter: AgentSourceImporter;
263:    readonly agentSourceImporter: AgentSourceImporter;
344:function createResolutionLineage(options?: ResolveInheritedAgentSourceOptions): Array<string_agent_url> {
345:    const lineage = [...(options?.inheritancePath || [])];
347:    if (options?.currentAgentUrl) {
351:    for (const alias of options?.currentAgentAliases || []) {
369:    options: ResolveInheritedAgentSourceOptions,
388:function createAgentImportContext(options: ResolveInheritedAgentSourceOptions): AgentImportContext {
390:        adamAgentUrl: options?.adamAgentUrl || 'https://core.ptbk.io/agents/adam',
391:        agentReferenceResolver: options?.agentReferenceResolver,
393:            recursionLevel: options?.recursionLevel || 0,
396:        agentSourceImporter: options.agentSourceImporter,
467:    const importedAgentSource = await context.agentSourceImporter(agentUrl, {
778:    options: ResolveInheritedAgentSourceOptions,
816:    options: ResolveInheritedAgentSourceOptions,
/**
 * An uppercase Book commitment block whose keyword is not registered by Promptbook.
 *
 * Unknown commitments are retained as system-message context instead of being applied
 * as executable commitments.
 *
 * @private internal type of `parseAgentSourceWithCommitments`
 */
export type ParsedUnknownCommitment = {
    /**
     * Uppercase unknown commitment type, for example `FOO` or `BAR BZZ`.
     */
    readonly type: string;

    /**
     * Original first line, retained for precise editor diagnostics.
     */
    readonly originalLine: string;

    /**
     * Line number in the agent source (1-based).
     */
    readonly lineNumber: number;

    /**
     * Complete unknown commitment block, including its keyword and continuation lines.
     */
    readonly source: string;
};

diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}

apply patch
patch: completed
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\common\LocalAgentBookCollection.ts
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\common\createLocalAgentReferenceResolver.ts
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\common\resolveLocalAgentSource.ts
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..d7fe3340da35061371d1c4d280a9cf5525253f5b
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,194 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(
+        private readonly agentDirectoryPath: string,
+        private readonly currentWorkingDirectory: string,
+    ) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+        if (!this.booksByName.has(normalizeAgentName('Adam'))) {
+            await ensureAdamAgentBook(this.agentDirectoryPath);
+            await this.readBook(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        }
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(spaceTrim((block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `));
+        }
+
+        throw new NotFoundError(spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `));
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        if (isValidAgentUrl(reference)) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath = isRelativeToBook && declaringBook.filePath
+            ? dirname(declaringBook.filePath)
+            : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(canonicalFilePath).href);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`));
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string_book, filePath?: string): LocalAgentBook {
+        const parsedSource = parseAgentSource(source);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9781d4945c6f4c29dcbeffff6b9920dffaa7ee78
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,63 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) => collection.getProfile(url),
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..a88aa9457b916d1c3763e9d6a4f33b2a3c9433ae
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,98 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.findByName('Adam').url;
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(book: LocalAgentBook, inheritancePath: ReadonlyArray<string> = []): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(spaceTrim(`Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${book.filePath || book.url}\`.`));
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) => resolveBook(
+                await collection.getBook(url),
+                context.importAgentOptions.inheritancePath,
+            ),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: createLocalAgentReferenceResolver(collection, primaryBook),
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim() ? `${commitmentType} ${content}\n` : commitmentType === 'FROM' ? 'FROM @Null\n' : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n');
+}

diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..d7fe3340da35061371d1c4d280a9cf5525253f5b
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,194 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(
+        private readonly agentDirectoryPath: string,
+        private readonly currentWorkingDirectory: string,
+    ) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+        if (!this.booksByName.has(normalizeAgentName('Adam'))) {
+            await ensureAdamAgentBook(this.agentDirectoryPath);
+            await this.readBook(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        }
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(spaceTrim((block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `));
+        }
+
+        throw new NotFoundError(spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `));
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        if (isValidAgentUrl(reference)) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath = isRelativeToBook && declaringBook.filePath
+            ? dirname(declaringBook.filePath)
+            : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(canonicalFilePath).href);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`));
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string_book, filePath?: string): LocalAgentBook {
+        const parsedSource = parseAgentSource(source);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9781d4945c6f4c29dcbeffff6b9920dffaa7ee78
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,63 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) => collection.getProfile(url),
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..a88aa9457b916d1c3763e9d6a4f33b2a3c9433ae
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,98 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.findByName('Adam').url;
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(book: LocalAgentBook, inheritancePath: ReadonlyArray<string> = []): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(spaceTrim(`Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${book.filePath || book.url}\`.`));
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) => resolveBook(
+                await collection.getBook(url),
+                context.importAgentOptions.inheritancePath,
+            ),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: createLocalAgentReferenceResolver(collection, primaryBook),
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim() ? `${commitmentType} ${content}\n` : commitmentType === 'FROM' ? 'FROM @Null\n' : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n');
+}

codex
The shared resolver and local book index are in place. Local references now retain the declaring book’s directory as inheritance is expanded, and Adam comes from the same bundled book as the server. Next I’m wiring this into coder startup and initialization, then adding regression tests for the requested forms and nested references.

apply patch
patch: completed
C:\Users\me\work\ai\promptbook\scripts\generate-packages\generate-packages.ts
C:\Users\me\work\ai\promptbook\scripts\run-agent-messages\messages\createAgentRunnerSystemMessage.ts
C:\Users\me\work\ai\promptbook\scripts\run-codex-prompts\common\resolveCoderAgent.ts
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\coder\init.ts
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\coder\initializeCoderProjectConfiguration.ts
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\coder\printInitializationSummary.ts
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\coder\run.ts
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..49ba5343842a175c993afa92df3f477492caa03d
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -101,9 +102,16 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
     };
 }
 
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..d7fe3340da35061371d1c4d280a9cf5525253f5b
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,194 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(
+        private readonly agentDirectoryPath: string,
+        private readonly currentWorkingDirectory: string,
+    ) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+        if (!this.booksByName.has(normalizeAgentName('Adam'))) {
+            await ensureAdamAgentBook(this.agentDirectoryPath);
+            await this.readBook(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        }
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(spaceTrim((block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `));
+        }
+
+        throw new NotFoundError(spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `));
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        if (isValidAgentUrl(reference)) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath = isRelativeToBook && declaringBook.filePath
+            ? dirname(declaringBook.filePath)
+            : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(canonicalFilePath).href);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`));
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string_book, filePath?: string): LocalAgentBook {
+        const parsedSource = parseAgentSource(source);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9781d4945c6f4c29dcbeffff6b9920dffaa7ee78
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,63 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) => collection.getProfile(url),
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..a88aa9457b916d1c3763e9d6a4f33b2a3c9433ae
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,98 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.findByName('Adam').url;
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(book: LocalAgentBook, inheritancePath: ReadonlyArray<string> = []): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(spaceTrim(`Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${book.filePath || book.url}\`.`));
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) => resolveBook(
+                await collection.getBook(url),
+                context.importAgentOptions.inheritancePath,
+            ),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: createLocalAgentReferenceResolver(collection, primaryBook),
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim() ? `${commitmentType} ${content}\n` : commitmentType === 'FROM' ? 'FROM @Null\n' : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n');
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "@'
from pathlib import Path
path = Path('src/cli/cli-commands/coder/ensureCoderDeveloperAgentFile.ts')
source = path.read_text(encoding='utf-8')
source = source.replace(\"import { spaceTrim } from 'spacetrim';\\nimport { NotAllowed } from '../../../errors/NotAllowed';\", \"import { resolveBundledAgentBookPath } from '../common/resolveBundledAgentBookPath';\")
source = source.replace('await resolveDefaultCoderDeveloperAgentFilePath()', 'await resolveBundledAgentBookPath(DEFAULT_CODER_DEVELOPER_AGENT_SOURCE_FILE_PATH)')
start = source.index('/**\\n * Resolves the bundled developer agent')
end = source.index('/**\\n * Checks whether a path exists', start)
source = source[:start] + source[end:]
path.write_text(source, encoding='utf-8')
path = Path('src/book-2.0/agent-source/resolveInheritedAgentSource.ts')
source = path.read_text(encoding='utf-8').replace('    readonly resolutionOptions?: ResolveInheritedAgentSourceOptions;', '    readonly resolutionOptions: ResolveInheritedAgentSourceOptions;')
path.write_text(source, encoding='utf-8')
'@ | python -" in C:\Users\me\work\ai\promptbook
 succeeded in 13316ms:
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..49ba5343842a175c993afa92df3f477492caa03d
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -101,9 +102,16 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
     };
 }
 
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..d7fe3340da35061371d1c4d280a9cf5525253f5b
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,194 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(
+        private readonly agentDirectoryPath: string,
+        private readonly currentWorkingDirectory: string,
+    ) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+        if (!this.booksByName.has(normalizeAgentName('Adam'))) {
+            await ensureAdamAgentBook(this.agentDirectoryPath);
+            await this.readBook(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        }
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(spaceTrim((block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `));
+        }
+
+        throw new NotFoundError(spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `));
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        if (isValidAgentUrl(reference)) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath = isRelativeToBook && declaringBook.filePath
+            ? dirname(declaringBook.filePath)
+            : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(canonicalFilePath).href);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`));
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string_book, filePath?: string): LocalAgentBook {
+        const parsedSource = parseAgentSource(source);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9781d4945c6f4c29dcbeffff6b9920dffaa7ee78
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,63 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) => collection.getProfile(url),
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..a88aa9457b916d1c3763e9d6a4f33b2a3c9433ae
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,98 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.findByName('Adam').url;
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(book: LocalAgentBook, inheritancePath: ReadonlyArray<string> = []): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(spaceTrim(`Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${book.filePath || book.url}\`.`));
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) => resolveBook(
+                await collection.getBook(url),
+                context.importAgentOptions.inheritancePath,
+            ),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: createLocalAgentReferenceResolver(collection, primaryBook),
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim() ? `${commitmentType} ${content}\n` : commitmentType === 'FROM' ? 'FROM @Null\n' : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n');
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm run test-types -- --pretty false' in C:\Users\me\work\ai\promptbook
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts apps/agents-server/src/utils/createLocalAgentSourceImporter.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts' in C:\Users\me\work\ai\promptbook
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..49ba5343842a175c993afa92df3f477492caa03d
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -101,9 +102,16 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
     };
 }
 
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..d7fe3340da35061371d1c4d280a9cf5525253f5b
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,194 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(
+        private readonly agentDirectoryPath: string,
+        private readonly currentWorkingDirectory: string,
+    ) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+        if (!this.booksByName.has(normalizeAgentName('Adam'))) {
+            await ensureAdamAgentBook(this.agentDirectoryPath);
+            await this.readBook(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        }
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(spaceTrim((block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `));
+        }
+
+        throw new NotFoundError(spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `));
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        if (isValidAgentUrl(reference)) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath = isRelativeToBook && declaringBook.filePath
+            ? dirname(declaringBook.filePath)
+            : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(canonicalFilePath).href);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`));
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string_book, filePath?: string): LocalAgentBook {
+        const parsedSource = parseAgentSource(source);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9781d4945c6f4c29dcbeffff6b9920dffaa7ee78
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,63 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) => collection.getProfile(url),
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..a88aa9457b916d1c3763e9d6a4f33b2a3c9433ae
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,98 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.findByName('Adam').url;
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(book: LocalAgentBook, inheritancePath: ReadonlyArray<string> = []): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(spaceTrim(`Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${book.filePath || book.url}\`.`));
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) => resolveBook(
+                await collection.getBook(url),
+                context.importAgentOptions.inheritancePath,
+            ),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: createLocalAgentReferenceResolver(collection, primaryBook),
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim() ? `${commitmentType} ${content}\n` : commitmentType === 'FROM' ? 'FROM @Null\n' : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n');
+}

 exited 1 in 90724ms:

> promptbook@0.114.0-38 test-unit
> node -r ./scripts/ignore-kill-eperm.js ./node_modules/jest/bin/jest.js --forceExit --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts apps/agents-server/src/utils/createLocalAgentSourceImporter.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts

FAIL apps/agents-server/src/utils/createLocalAgentSourceImporter.test.ts
  ● Test suite failed to run

    ENOENT: no such file or directory, open 'C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\createLocalAgentSourceImporter.test.ts'

      at runTestInternal (node_modules/jest-runner/build/runTest.js:170:27)

PASS src/cli/cli-commands/coder/boilerplateTemplates.test.ts (13.398 s)
PASS apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts (30.724 s)
  ● Console

    console.log
      importAgent "https://core-test.ptbk.io/agents/test-0"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/adam"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://local.example/agents/adam-1"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://local.example/agents/adam-1"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://local.example/agents/adam-1"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/effective-parent"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/effective-parent"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://local.example/agents/parent-1"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://federated.example/agents/remote-parent"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/adam"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/adam"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/adam"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.warn
      [importAgentWithFallback] Falling back for "https://core-test.ptbk.io/agents/adam" after 3 attempts: NotFoundError: Failed to import agent from "https://core-test.ptbk.io/agents/adam"
      
      Agent not found
          at deserializeError (C:\Users\me\work\ai\promptbook\src\errors\utils\deserializeError.ts:32:31)
          at C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\importAgent.ts:188:45
          at importAgent (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\importAgent.ts:230:16)
          at retryWithBackoff (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\retryWithBackoff.ts:181:27)
          at C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\importAgentWithFallback.ts:112:28
          at importAgentCorpus (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\resolveInheritedAgentSource.ts:467:33)
          at resolveParentAgentContext (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\resolveInheritedAgentSource.ts:506:11)
          at resolveInheritedAgentSource (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\resolveInheritedAgentSource.ts:819:27)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveInheritedAgentSource.test.ts:409:37) {
        id: undefined
      }

      124 |             return result.value;
      125 |         } catch (error) {
    > 126 |             console.warn(
          |                     ^
      127 |                 `[importAgentWithFallback] Falling back for "${agentUrl}" after ${configuration.maxAttempts} attempts:`,
      128 |                 error,
      129 |             );

      at apps/agents-server/src/utils/importAgentWithFallback.ts:126:21
      at importAgentCorpus (src/book-2.0/agent-source/resolveInheritedAgentSource.ts:467:33)
      at resolveParentAgentContext (src/book-2.0/agent-source/resolveInheritedAgentSource.ts:506:11)
      at resolveInheritedAgentSource (src/book-2.0/agent-source/resolveInheritedAgentSource.ts:819:27)
      at Object.<anonymous> (apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts:409:37)

    console.log
      importAgent "https://federated.example/agents/missing-source"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://federated.example/agents/missing-source"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://federated.example/agents/missing-source"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.warn
      [importAgentWithFallback] Falling back for "https://federated.example/agents/missing-source" after 3 attempts: NotFoundError: Failed to import agent from "https://federated.example/agents/missing-source"
      
      Remote import unavailable
          at deserializeError (C:\Users\me\work\ai\promptbook\src\errors\utils\deserializeError.ts:32:31)
          at C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\importAgent.ts:188:45
          at importAgent (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\importAgent.ts:230:16)
          at retryWithBackoff (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\retryWithBackoff.ts:181:27)
          at C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\importAgentWithFallback.ts:112:28
          at importAgentCorpus (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\resolveInheritedAgentSource.ts:467:33)
          at resolveImportCommitmentLine (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\resolveInheritedAgentSource.ts:577:39)
          at resolveAgentSourceBuild (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\resolveInheritedAgentSource.ts:681:48)
          at resolveInheritedAgentSource (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\resolveInheritedAgentSource.ts:820:27)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveInheritedAgentSource.test.ts:499:37) {
        id: undefined
      }

      124 |             return result.value;
      125 |         } catch (error) {
    > 126 |             console.warn(
          |                     ^
      127 |                 `[importAgentWithFallback] Falling back for "${agentUrl}" after ${configuration.maxAttempts} attempts:`,
      128 |                 error,
      129 |             );

      at apps/agents-server/src/utils/importAgentWithFallback.ts:126:21
      at importAgentCorpus (src/book-2.0/agent-source/resolveInheritedAgentSource.ts:467:33)
      at resolveImportCommitmentLine (src/book-2.0/agent-source/resolveInheritedAgentSource.ts:577:39)
      at resolveAgentSourceBuild (src/book-2.0/agent-source/resolveInheritedAgentSource.ts:681:48)
      at resolveInheritedAgentSource (src/book-2.0/agent-source/resolveInheritedAgentSource.ts:820:27)
      at Object.<anonymous> (apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts:499:37)

PASS scripts/run-codex-prompts/common/resolveCoderAgent.test.ts

Test Suites: 1 failed, 3 passed, 4 total
Tests:       24 passed, 24 total
Snapshots:   0 total
Time:        48.329 s
Ran all test suites within paths "scripts/run-codex-prompts/common/resolveCoderAgent.test.ts", "apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts", "apps/agents-server/src/utils/createLocalAgentSourceImporter.test.ts", "src/cli/cli-commands/coder/boilerplateTemplates.test.ts".
Force exiting Jest: Have you considered using `--detectOpenHandles` to detect async operations that kept running after all tests finished?

 exited 1 in 116581ms:

> promptbook@0.114.0-38 test-types
> tsc --pretty false

src/cli/cli-commands/common/LocalAgentBookCollection.ts(102,38): error TS2339: Property 'replaceAll' does not exist on type 'never'.
src/cli/cli-commands/common/LocalAgentBookCollection.ts(121,43): error TS2345: Argument of type 'string' is not assignable to parameter of type 'string_book'.
  Type 'string' is not assignable to type '{ readonly __type: "book"; }'.
src/cli/cli-commands/common/LocalAgentBookCollection.ts(158,39): error TS2345: Argument of type 'string' is not assignable to parameter of type 'string_book'.
src/cli/cli-commands/common/resolveLocalAgentSource.ts(97,5): error TS2322: Type 'string' is not assignable to type 'string_book'.

apply patch
patch: completed
C:\Users\me\work\ai\promptbook\scripts\run-codex-prompts\common\resolveCoderAgent.references.test.ts
C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\resolveInheritedAgentSource.ts
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\common\createLocalAgentReferenceResolver.ts
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..2a71bdc2b93ce0addc6132265aba74caa02dbbeb
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,189 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(PRIMARY_AGENT_PATH, spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `));
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook('agents/coding/.core/adam.book', spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `));
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook('agents/coding/nested/not-the-agent-name.book', spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `));
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(filePath, spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `));
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(new Response(spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `), { headers: { 'content-type': 'text/plain' } }));
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain('Remote workshop guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({ code: 'ENOENT' });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook('agents/coding/mentors/pavol.book', spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `));
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(async (url: string) => new Response(
+            url.endsWith('/parent.book')
+                ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+        )) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('reports missing %s names without silently dropping instructions', async (commitment) => {
+        await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+    });
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `));
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..49ba5343842a175c993afa92df3f477492caa03d
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -101,9 +102,16 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
     };
 }
 
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..d7fe3340da35061371d1c4d280a9cf5525253f5b
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,194 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(
+        private readonly agentDirectoryPath: string,
+        private readonly currentWorkingDirectory: string,
+    ) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+        if (!this.booksByName.has(normalizeAgentName('Adam'))) {
+            await ensureAdamAgentBook(this.agentDirectoryPath);
+            await this.readBook(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        }
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(spaceTrim((block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `));
+        }
+
+        throw new NotFoundError(spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `));
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        if (isValidAgentUrl(reference)) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath = isRelativeToBook && declaringBook.filePath
+            ? dirname(declaringBook.filePath)
+            : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(canonicalFilePath).href);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`));
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string_book, filePath?: string): LocalAgentBook {
+        const parsedSource = parseAgentSource(source);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..a88aa9457b916d1c3763e9d6a4f33b2a3c9433ae
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,98 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.findByName('Adam').url;
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(book: LocalAgentBook, inheritancePath: ReadonlyArray<string> = []): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(spaceTrim(`Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${book.filePath || book.url}\`.`));
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) => resolveBook(
+                await collection.getBook(url),
+                context.importAgentOptions.inheritancePath,
+            ),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: createLocalAgentReferenceResolver(collection, primaryBook),
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim() ? `${commitmentType} ${content}\n` : commitmentType === 'FROM' ? 'FROM @Null\n' : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n');
+}

diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..2a71bdc2b93ce0addc6132265aba74caa02dbbeb
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,189 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(PRIMARY_AGENT_PATH, spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `));
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook('agents/coding/.core/adam.book', spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `));
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook('agents/coding/nested/not-the-agent-name.book', spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `));
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(filePath, spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `));
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(new Response(spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `), { headers: { 'content-type': 'text/plain' } }));
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain('Remote workshop guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({ code: 'ENOENT' });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook('agents/coding/mentors/pavol.book', spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `));
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(async (url: string) => new Response(
+            url.endsWith('/parent.book')
+                ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+        )) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('reports missing %s names without silently dropping instructions', async (commitment) => {
+        await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+    });
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `));
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..49ba5343842a175c993afa92df3f477492caa03d
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -101,9 +102,16 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
     };
 }
 
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..d7fe3340da35061371d1c4d280a9cf5525253f5b
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,194 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(
+        private readonly agentDirectoryPath: string,
+        private readonly currentWorkingDirectory: string,
+    ) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+        if (!this.booksByName.has(normalizeAgentName('Adam'))) {
+            await ensureAdamAgentBook(this.agentDirectoryPath);
+            await this.readBook(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        }
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(spaceTrim((block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `));
+        }
+
+        throw new NotFoundError(spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `));
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        if (isValidAgentUrl(reference)) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath = isRelativeToBook && declaringBook.filePath
+            ? dirname(declaringBook.filePath)
+            : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(canonicalFilePath).href);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`));
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string_book, filePath?: string): LocalAgentBook {
+        const parsedSource = parseAgentSource(source);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..a88aa9457b916d1c3763e9d6a4f33b2a3c9433ae
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,98 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.findByName('Adam').url;
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(book: LocalAgentBook, inheritancePath: ReadonlyArray<string> = []): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(spaceTrim(`Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${book.filePath || book.url}\`.`));
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) => resolveBook(
+                await collection.getBook(url),
+                context.importAgentOptions.inheritancePath,
+            ),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: createLocalAgentReferenceResolver(collection, primaryBook),
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim() ? `${commitmentType} ${content}\n` : commitmentType === 'FROM' ? 'FROM @Null\n' : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n');
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts' in C:\Users\me\work\ai\promptbook
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..2a71bdc2b93ce0addc6132265aba74caa02dbbeb
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,189 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(PRIMARY_AGENT_PATH, spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `));
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook('agents/coding/.core/adam.book', spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `));
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook('agents/coding/nested/not-the-agent-name.book', spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `));
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(filePath, spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `));
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(new Response(spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `), { headers: { 'content-type': 'text/plain' } }));
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain('Remote workshop guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({ code: 'ENOENT' });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook('agents/coding/mentors/pavol.book', spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `));
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(async (url: string) => new Response(
+            url.endsWith('/parent.book')
+                ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+        )) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('reports missing %s names without silently dropping instructions', async (commitment) => {
+        await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+    });
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `));
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..49ba5343842a175c993afa92df3f477492caa03d
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -101,9 +102,16 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
     };
 }
 
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..d7fe3340da35061371d1c4d280a9cf5525253f5b
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,194 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(
+        private readonly agentDirectoryPath: string,
+        private readonly currentWorkingDirectory: string,
+    ) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+        if (!this.booksByName.has(normalizeAgentName('Adam'))) {
+            await ensureAdamAgentBook(this.agentDirectoryPath);
+            await this.readBook(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        }
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(spaceTrim((block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `));
+        }
+
+        throw new NotFoundError(spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `));
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        if (isValidAgentUrl(reference)) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath = isRelativeToBook && declaringBook.filePath
+            ? dirname(declaringBook.filePath)
+            : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(canonicalFilePath).href);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`));
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string_book, filePath?: string): LocalAgentBook {
+        const parsedSource = parseAgentSource(source);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..a88aa9457b916d1c3763e9d6a4f33b2a3c9433ae
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,98 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.findByName('Adam').url;
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(book: LocalAgentBook, inheritancePath: ReadonlyArray<string> = []): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(spaceTrim(`Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${book.filePath || book.url}\`.`));
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) => resolveBook(
+                await collection.getBook(url),
+                context.importAgentOptions.inheritancePath,
+            ),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: createLocalAgentReferenceResolver(collection, primaryBook),
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim() ? `${commitmentType} ${content}\n` : commitmentType === 'FROM' ? 'FROM @Null\n' : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n');
+}

 succeeded in 19507ms:

> promptbook@0.114.0-38 test-unit
> node -r ./scripts/ignore-kill-eperm.js ./node_modules/jest/bin/jest.js --forceExit --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts

PASS scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts (12.224 s)
  coder agent reference resolution
    √ resolves FROM names recursively from first-line titles (584 ms)
    √ resolves IMPORT names recursively from first-line titles (360 ms)
    √ resolves TEAM names recursively from first-line titles (287 ms)
    √ resolves each supported local path form in FROM (413 ms)
    √ resolves each supported local path form in IMPORT (161 ms)
    √ resolves each supported local path form in TEAM (250 ms)
    √ loads HTTP book references in FROM (71 ms)
    √ loads HTTP book references in IMPORT (38 ms)
    √ loads HTTP book references in TEAM (263 ms)
    √ disables inheritance with FROM @null (95 ms)
    √ disables inheritance with FROM {null} (119 ms)
    √ disables inheritance with FROM @void (47 ms)
    √ disables inheritance with FROM {void} (34 ms)
    √ inherits the local Adam by default and keeps project-owned Adam unchanged (41 ms)
    √ creates a missing Adam from the same bundled source as the server (126 ms)
    √ uses an existing Adam outside .core without creating a second Adam (125 ms)
    √ resolves nested inheritance, imports and multiline TEAM paths from each declaring book (94 ms)
    √ resolves nested direct URL books before compiling the coder prompt (414 ms)
    √ only resolves the last FROM and follows the server behavior for inheritance cycles (226 ms)
    √ rejects cyclic imports instead of recursing indefinitely (434 ms)
    √ reports missing FROM names without silently dropping instructions (69 ms)
    √ reports missing IMPORT names without silently dropping instructions (42 ms)
    √ reports missing TEAM names without silently dropping instructions (87 ms)
    √ reports duplicate titles and allows an explicit path to disambiguate them (154 ms)
    √ does not resolve example commitments inside code fences (29 ms)

Test Suites: 1 passed, 1 total
Tests:       25 passed, 25 total
Snapshots:   0 total
Time:        12.694 s
Ran all test suites within paths "scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts".
Force exiting Jest: Have you considered using `--detectOpenHandles` to detect async operations that kept running after all tests finished?

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n 'gitChanges|working tree|WorkingTree|dirty' scripts/run-codex-prompts/main/runCodexPrompts.ts scripts/run-codex-prompts/git -g '*.ts' -g '"'!*.test.ts'"'; Get-Content src/cli/cli-commands/coder/boilerplateTemplates.test.ts | Select-Object -Skip 65 -First 45; Get-Content src/cli/cli-commands/coder/boilerplateTemplates.test.ts | Select-Object -Skip 265 -First 32" in C:\Users\me\work\ai\promptbook
 succeeded in 722ms:
scripts/run-codex-prompts/main/runCodexPrompts.ts:32:import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
scripts/run-codex-prompts/main/runCodexPrompts.ts:152:        let isContinuingInterruptedPrompt = options.gitChanges === 'continue';
scripts/run-codex-prompts/main/runCodexPrompts.ts:281:            if (isCleanWorkingTreeRequired(options.gitChanges, isContinuingInterruptedPrompt)) {
scripts/run-codex-prompts/main/runCodexPrompts.ts:283:                    checkpointLabel: 'checking the git working tree',
scripts/run-codex-prompts/main/runCodexPrompts.ts:285:                    statusMessage: 'Checking the working tree...',
scripts/run-codex-prompts/main/runCodexPrompts.ts:287:                await ensureWorkingTreeClean();
scripts/run-codex-prompts/main/runCodexPrompts.ts:357:    if (options.noCommit && !options.waitForUser && options.gitChanges !== 'ignore') {
scripts/run-codex-prompts/main/runCodexPrompts.ts:362:                Without commits, the next prompt round would fail the clean working tree check.
scripts/run-codex-prompts/main/runCodexPrompts.ts:372:                Auto-pull keeps the repository up to date between prompt rounds, which requires each successful round to end with a clean committed working tree.
scripts/run-codex-prompts/main/runCodexPrompts.ts:387:    if (options.isIsolated && options.gitChanges === 'continue') {
scripts/run-codex-prompts/main/runCodexPrompts.ts:397:    if (options.gitChanges === 'continue' && options.testBefore === 'yes-and-fix') {
scripts/run-codex-prompts/main/runCodexPrompts.ts:419: * Decides whether the working tree has to be verified clean before the next prompt starts.
scripts/run-codex-prompts/main/runCodexPrompts.ts:424:function isCleanWorkingTreeRequired(gitChanges: GitChangesMode, isContinuingInterruptedPrompt: boolean): boolean {
scripts/run-codex-prompts/main/runCodexPrompts.ts:425:    if (gitChanges === 'ignore') {
scripts/run-codex-prompts/main/runCodexPrompts.ts:546:    if (isCleanWorkingTreeRequired(runOptions.gitChanges, isContinuingInterruptedPrompt)) {
scripts/run-codex-prompts/main/runCodexPrompts.ts:548:            checkpointLabel: 'checking the git working tree before testing',
scripts/run-codex-prompts/main/runCodexPrompts.ts:550:            statusMessage: 'Checking the working tree before testing...',
scripts/run-codex-prompts/main/runCodexPrompts.ts:552:        await ensureWorkingTreeClean();
scripts/run-codex-prompts/git\ensureWorkingTreeClean.ts:3:import { isWorkingTreeClean } from '../../utils/autocommit/isWorkingTreeClean';
scripts/run-codex-prompts/git\ensureWorkingTreeClean.ts:6: * Ensures the git working tree is clean before running the prompt.
scripts/run-codex-prompts/git\ensureWorkingTreeClean.ts:8:export async function ensureWorkingTreeClean(): Promise<void> {
scripts/run-codex-prompts/git\ensureWorkingTreeClean.ts:9:    const isClean = await isWorkingTreeClean(process.cwd());
scripts/run-codex-prompts/git\ensureWorkingTreeClean.ts:13:                Git working tree is not clean.
scripts/run-codex-prompts/git\coderGitSync.ts:42: * Pulls the latest repository changes and captures the working tree state before a `ptbk coder` command
scripts/run-codex-prompts/git\coderGitSync.ts:86: * Note: Only the files this very command has changed are committed, everything else is left in the working tree.
scripts/run-codex-prompts/git\coderCommitScope.ts:1:import type { WorkingTreeChangesSnapshot } from './workingTreeChanges';
scripts/run-codex-prompts/git\coderCommitScope.ts:2:import { captureWorkingTreeChangesSnapshot, listFilesChangedSinceSnapshot } from './workingTreeChanges';
scripts/run-codex-prompts/git\coderCommitScope.ts:19:    readonly snapshotBeforeOperation: WorkingTreeChangesSnapshot;
scripts/run-codex-prompts/git\coderCommitScope.ts:23: * Captures the working tree state before one `ptbk coder` operation changes anything.
scripts/run-codex-prompts/git\coderCommitScope.ts:31:        snapshotBeforeOperation: await captureWorkingTreeChangesSnapshot(projectPath),
scripts/run-codex-prompts/git\coderCommitScope.ts:39: * never part of the result, so they stay in the working tree instead of being swept into the commit.
scripts/run-codex-prompts/git\workingTreeChanges.ts:7: * Git commands used to list changed and untracked files in the working tree.
scripts/run-codex-prompts/git\workingTreeChanges.ts:20: * Snapshot of file hashes for files that were already dirty before one operation started.
scripts/run-codex-prompts/git\workingTreeChanges.ts:25:export type WorkingTreeChangesSnapshot = {
scripts/run-codex-prompts/git\workingTreeChanges.ts:27:     * Hash of every dirty file at the moment the snapshot was taken.
scripts/run-codex-prompts/git\workingTreeChanges.ts:29:     * A file which was dirty but could not be hashed (a deleted file or a non-regular file) is stored as `null`,
scripts/run-codex-prompts/git\workingTreeChanges.ts:30:     * so it can be told apart from a file which was not dirty at all.
scripts/run-codex-prompts/git\workingTreeChanges.ts:36: * Captures hashes for files that are dirty before one operation starts.
scripts/run-codex-prompts/git\workingTreeChanges.ts:38:export async function captureWorkingTreeChangesSnapshot(projectPath: string): Promise<WorkingTreeChangesSnapshot> {
scripts/run-codex-prompts/git\workingTreeChanges.ts:39:    const changedFiles = await listWorkingTreeChangedFiles(projectPath);
scripts/run-codex-prompts/git\workingTreeChanges.ts:43:        changedFileHashes.set(relativePath, await readWorkingTreeFileHash(projectPath, relativePath));
scripts/run-codex-prompts/git\workingTreeChanges.ts:52: * A file which was already dirty before and was not touched afterwards is not listed, so the result contains
scripts/run-codex-prompts/git\workingTreeChanges.ts:57:    snapshot: WorkingTreeChangesSnapshot,
scripts/run-codex-prompts/git\workingTreeChanges.ts:59:    const changedFiles = await listWorkingTreeChangedFiles(projectPath);
scripts/run-codex-prompts/git\workingTreeChanges.ts:63:        const currentFileHash = await readWorkingTreeFileHash(projectPath, relativePath);
scripts/run-codex-prompts/git\workingTreeChanges.ts:78: * Lists dirty tracked files and untracked files in the working tree.
scripts/run-codex-prompts/git\workingTreeChanges.ts:80:export async function listWorkingTreeChangedFiles(projectPath: string): Promise<ReadonlyArray<string>> {
scripts/run-codex-prompts/git\workingTreeChanges.ts:99: * Reads the content hash of one repository-relative working tree file.
scripts/run-codex-prompts/git\workingTreeChanges.ts:103:export async function readWorkingTreeFileHash(projectPath: string, relativePath: string): Promise<string | null> {
scripts/run-codex-prompts/git\commitChanges.ts:29:         * project are left in the working tree. Everything is committed when the paths are not provided at all,
scripts/run-codex-prompts/git\commitChanges.ts:56:        //       a pathspec commits the working tree content of those paths and would ignore unstaging them
        for (const definition of getDefaultCoderPromptTemplateDefinitions()) {
            const content = await readFile(join(process.cwd(), definition.relativeFilePath), 'utf-8');
            expect(normalizeLineEndings(content).trim()).toBe(definition.content);
        }
    });
    */

    it('creates the default template files during coder init', async () => {
        const projectPath = await createTemporaryDirectory(temporaryDirectories);

        const summary = await initializeCoderProjectConfiguration(projectPath);

        expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
        expect(summary.agentsDirectoryStatus).toBe('created');
        expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
        expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
        expect(summary.gitignoreFileStatus).toBe('created');
        expect(summary.packageJsonFileStatus).toBe('created');
        expect(summary.vscodeSettingsFileStatus).toBe('created');

        for (const { relativeFilePath } of getDefaultCoderProjectPromptTemplateDefinitions()) {
            expect(getReferencedArtifactStatus(summary, relativeFilePath)).toBe('created');
        }

        for (const definition of getDefaultCoderProjectPromptTemplateDefinitions()) {
            const content = await readFile(join(projectPath, definition.relativeFilePath), 'utf-8');
            expect(normalizeLineEndings(content).trim()).toBe(definition.content);
        }

        const developerAgentContent = await readFile(join(projectPath, CODER_DEVELOPER_AGENT_FILE_PATH), 'utf-8');
        const sourceDeveloperAgentContent = await readFile(
            join(process.cwd(), DEFAULT_CODER_DEVELOPER_AGENT_SOURCE_FILE_PATH),
            'utf-8',
        );
        expect(normalizeLineEndings(developerAgentContent)).toBe(normalizeLineEndings(sourceDeveloperAgentContent));

        const agentsFileContent = await readFile(join(projectPath, AGENTS_FILE_PATH), 'utf-8');
        expect(normalizeLineEndings(agentsFileContent).trim()).toBe(getDefaultCoderAgentsFileContent());

        await expect(readFile(join(projectPath, 'AGENT_CODING.md'), 'utf-8')).rejects.toThrow();

        await expect(
            readFile(join(projectPath, 'prompts', 'templates', 'agents-server.md'), 'utf-8'),
        ).rejects.toThrow();

            await expect(readFile(join(projectPath, relativeFilePath), 'utf-8')).resolves.toBeTruthy();
        }
    });

    it('does not append duplicate commented coder env variables on repeated init', async () => {
        const projectPath = await createTemporaryDirectory(temporaryDirectories);

        await initializeCoderProjectConfiguration(projectPath);
        const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);

        const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
        expect(repeatedSummary.envFileStatus).toBe('unchanged');
        expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
        expect(envContent.match(/CODING_AGENT_GIT_NAME/gu)).toHaveLength(1);
        expect(envContent.match(/CODING_AGENT_GIT_EMAIL/gu)).toHaveLength(1);
        expect(envContent.match(/CODING_AGENT_GIT_SIGNING_KEY/gu)).toHaveLength(1);
    });

    it('resolves template files relative to the project root', async () => {
        const projectPath = await createTemporaryDirectory(temporaryDirectories);
        const relativeTemplatePath = 'foo/bar/custom.template.md';

        await mkdir(join(projectPath, 'foo', 'bar'), { recursive: true });
        await writeFile(join(projectPath, 'foo', 'bar', 'custom.template.md'), 'Custom template\n', 'utf-8');

        const template = await resolveCoderPromptTemplate({
            projectPath,
            templateOption: relativeTemplatePath,
        });

        expect(template).toEqual({
            identifier: relativeTemplatePath,

diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..2a71bdc2b93ce0addc6132265aba74caa02dbbeb
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,189 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(PRIMARY_AGENT_PATH, spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `));
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook('agents/coding/.core/adam.book', spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `));
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook('agents/coding/nested/not-the-agent-name.book', spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `));
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(filePath, spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `));
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(new Response(spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `), { headers: { 'content-type': 'text/plain' } }));
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain('Remote workshop guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({ code: 'ENOENT' });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook('agents/coding/mentors/pavol.book', spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `));
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(async (url: string) => new Response(
+            url.endsWith('/parent.book')
+                ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+        )) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('reports missing %s names without silently dropping instructions', async (commitment) => {
+        await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+    });
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `));
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..49ba5343842a175c993afa92df3f477492caa03d
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -101,9 +102,16 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
     };
 }
 
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..d7fe3340da35061371d1c4d280a9cf5525253f5b
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,194 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(
+        private readonly agentDirectoryPath: string,
+        private readonly currentWorkingDirectory: string,
+    ) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+        if (!this.booksByName.has(normalizeAgentName('Adam'))) {
+            await ensureAdamAgentBook(this.agentDirectoryPath);
+            await this.readBook(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        }
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(spaceTrim((block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `));
+        }
+
+        throw new NotFoundError(spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `));
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        if (isValidAgentUrl(reference)) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath = isRelativeToBook && declaringBook.filePath
+            ? dirname(declaringBook.filePath)
+            : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(canonicalFilePath).href);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`));
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string_book, filePath?: string): LocalAgentBook {
+        const parsedSource = parseAgentSource(source);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..a88aa9457b916d1c3763e9d6a4f33b2a3c9433ae
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,98 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.findByName('Adam').url;
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(book: LocalAgentBook, inheritancePath: ReadonlyArray<string> = []): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(spaceTrim(`Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${book.filePath || book.url}\`.`));
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) => resolveBook(
+                await collection.getBook(url),
+                context.importAgentOptions.inheritancePath,
+            ),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: createLocalAgentReferenceResolver(collection, primaryBook),
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim() ? `${commitmentType} ${content}\n` : commitmentType === 'FROM' ? 'FROM @Null\n' : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n');
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content scripts/run-codex-prompts/git/ensureWorkingTreeClean.ts; Get-Content scripts/utils/autocommit/isWorkingTreeClean.ts; Get-Content scripts/run-codex-prompts/git/coderCommitScope.ts; rg -n 'createCoderCommitScope|commitScope|resolveAgentSystemMessage|additional.*[Pp]ath' scripts/run-codex-prompts/main/runCodexPrompts.ts scripts/run-codex-prompts/main/runSinglePrompt.ts scripts/run-codex-prompts -g '*.ts' -g '"'!*.test.ts'"' | Select-Object -First 65; Get-Content scripts/run-codex-prompts/main/runCodexPrompts.ts | Select-Object -Skip 190 -First 140" in C:\Users\me\work\ai\promptbook
 succeeded in 402ms:
import { spaceTrim } from 'spacetrim';
import { NotAllowed } from '../../../src/errors/NotAllowed';
import { isWorkingTreeClean } from '../../utils/autocommit/isWorkingTreeClean';

/**
 * Ensures the git working tree is clean before running the prompt.
 */
export async function ensureWorkingTreeClean(): Promise<void> {
    const isClean = await isWorkingTreeClean(process.cwd());
    if (!isClean) {
        throw new NotAllowed(
            spaceTrim(`
                Git working tree is not clean.

                Please commit or stash your changes before running this script
                OR decide what should happen with them:

                - \`--git-changes ignore\` leaves the changes where they are and starts the next prompt anyway
                - \`--git-changes continue\` resumes the single prompt which was left in the middle of its implementation

                Aborting
            `),
        );
    }
}
import { $execCommand } from '../../../src/utils/execCommand/$execCommand';

/**
 * Checks working tree clean.
 */
export async function isWorkingTreeClean(path: string): Promise<boolean> {
    const gitStatus = await $execCommand({
        cwd: path,
        command: `git status`,
    });

    return gitStatus.includes(`working tree clean`);
}

// Note: [🟡] Code for CLI support script [isWorkingTreeClean](scripts/utils/autocommit/isWorkingTreeClean.ts) should never be published outside of `@promptbook/cli`
import type { WorkingTreeChangesSnapshot } from './workingTreeChanges';
import { captureWorkingTreeChangesSnapshot, listFilesChangedSinceSnapshot } from './workingTreeChanges';

/**
 * Working tree state captured right before one `ptbk coder` operation starts changing the project.
 *
 * Note: This is the single mechanism which every `ptbk coder` command uses to commit only the files relevant
 *       for the operation it has just performed, instead of everything which happens to be changed in the project.
 */
export type CoderCommitScope = {
    /**
     * Project the operation runs in.
     */
    readonly projectPath: string;

    /**
     * Files which were already changed before the operation started.
     */
    readonly snapshotBeforeOperation: WorkingTreeChangesSnapshot;
};

/**
 * Captures the working tree state before one `ptbk coder` operation changes anything.
 *
 * The captured scope is passed to the commit of the very same operation, which then commits exactly the files
 * this operation has created, changed, moved or deleted.
 */
export async function captureCoderCommitScope(projectPath: string): Promise<CoderCommitScope> {
    return {
        projectPath,
        snapshotBeforeOperation: await captureWorkingTreeChangesSnapshot(projectPath),
    };
}

/**
 * Resolves the repository-relative paths which one `ptbk coder` operation has really changed.
 *
 * Files which were already changed before the operation started and which the operation did not touch are
 * never part of the result, so they stay in the working tree instead of being swept into the commit.
 */
export async function resolveCoderCommitScopePaths(scope: CoderCommitScope): Promise<ReadonlyArray<string>> {
    return listFilesChangedSinceSnapshot(scope.projectPath, scope.snapshotBeforeOperation);
}
rg: scripts/run-codex-prompts/main/runSinglePrompt.ts: The system cannot find the file specified. (os error 2)
scripts/run-codex-prompts\git\coderGitSync.ts:92:    readonly commitScope: CoderCommitScope;
scripts/run-codex-prompts\git\coderGitSync.ts:94:    const { gitSync, commitMessage, commitScope } = options;
scripts/run-codex-prompts\git\coderGitSync.ts:100:    const relevantPaths = await resolveCoderCommitScopePaths(commitScope);
scripts/run-codex-prompts\git\coderGitSync.ts:107:        projectPath: commitScope.projectPath,
                    progressDisplay,
                    uiHandle,
                    waitForRequestedPause,
                    hasWaitedForStart,
                    isContinuingInterruptedPrompt,
                });
                hasRunTestBefore = true;
            }

            await waitForRequestedPause({
                checkpointLabel: 'loading prompts',
                phase: 'loading',
                statusMessage: 'Loading prompts...',
            });
            const promptQueueSnapshot = await loadPromptQueueSnapshot({
                options,
                isRichUiEnabled,
                progressDisplay,
                uiHandle,
                promptRunnerIdentity,
                isContinuingInterruptedPrompt,
            });

            hasShownUpcomingTasks ||= showUpcomingTasksOnce({
                hasShownUpcomingTasks,
                promptFiles: promptQueueSnapshot.promptFiles,
                stats: promptQueueSnapshot.stats,
                priorityFilter: options.priorityFilter,
                isRichUiEnabled,
                promptRunnerIdentity,
            });

            if (!promptQueueSnapshot.nextPrompt) {
                if (isEndAfterCurrentPromptRequested(completedRunCount)) {
                    finishWhenEndAfterCurrentPromptIsRequested({
                        completedRunCount,
                        isRichUiEnabled,
                        uiHandle,
                    });
                    return;
                }

                if (options.keepAlive) {
                    announceKeepAliveStatus(promptQueueSnapshot, isRichUiEnabled, uiHandle);
                    // Note: The keep-alive poll runs in the `waiting` phase, where `S  Skip current waiting`
                    //       is offered, so pressing `S` looks for new prompts right away
                    await waitForSkippableWorldTimeDeadline({
                        deadlineTimeMs: Date.now() + KEEP_ALIVE_POLL_INTERVAL_MS,
                        pollIntervalMs: KEEP_ALIVE_POLL_INTERVAL_MS,
                    });
                    continue;
                }
                finishWhenNoPromptIsAvailable(promptQueueSnapshot, isRichUiEnabled, uiHandle);
                return;
            }

            const nextPrompt = promptQueueSnapshot.nextPrompt!;
            const promptLabel = buildPromptLabelForDisplay(nextPrompt.file, nextPrompt.section);

            // Wait between prompt rounds (skipped for the first round)
            if (previousRoundStartTime !== undefined && previousRoundEndTime !== undefined) {
                await waitBetweenPromptRoundsIfNeeded({
                    options,
                    previousRoundStartTime,
                    previousRoundEndTime,
                    isRichUiEnabled,
                    progressDisplay,
                    uiHandle,
                });
            }

            if (isEndAfterCurrentPromptRequested(completedRunCount)) {
                finishWhenEndAfterCurrentPromptIsRequested({
                    completedRunCount,
                    isRichUiEnabled,
                    uiHandle,
                });
                return;
            }

            hasWaitedForStart = await waitForPromptConfirmationIfNeeded({
                options,
                nextPrompt,
                promptLabel,
                hasWaitedForStart,
                isRichUiEnabled,
                progressDisplay,
                uiHandle,
            });

            if (isCleanWorkingTreeRequired(options.gitChanges, isContinuingInterruptedPrompt)) {
                await waitForRequestedPause({
                    checkpointLabel: 'checking the git working tree',
                    phase: 'loading',
                    statusMessage: 'Checking the working tree...',
                });
                await ensureWorkingTreeClean();
            }

            const currentRoundStartTime = Date.now();
            // Note: An isolated round implements the prompt in a temporary worktree and merges it back afterwards
            const runCurrentPromptRound = options.isIsolated ? runIsolatedPromptRound : runPromptRound;
            await runCurrentPromptRound({
                options,
                runner,
                runnerMetadata,
                nextPrompt,
                promptLabel,
                resolvedCoderContext,
                resolvedAgentSystemMessage,
                isRichUiEnabled,
                progressDisplay,
                uiHandle,
                waitForRequestedPause,
            });
            isContinuingInterruptedPrompt = false;
            previousRoundStartTime = currentRoundStartTime;
            previousRoundEndTime = Date.now();
            completedRunCount += 1;

            if (isRunLimitReached({ completedRunCount, limit: options.limit })) {
                finishWhenRunLimitIsReached({
                    completedRunCount,
                    isRichUiEnabled,
                    uiHandle,
                });
                return;
            }

            if (isEndAfterCurrentPromptRequested(completedRunCount)) {
                finishWhenEndAfterCurrentPromptIsRequested({
                    completedRunCount,
                    isRichUiEnabled,
                    uiHandle,
                });
                return;
            }
        }
    } finally {
        cleanupRunDisplays(progressDisplay, uiHandle, options);

diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..2a71bdc2b93ce0addc6132265aba74caa02dbbeb
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,189 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(PRIMARY_AGENT_PATH, spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `));
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook('agents/coding/.core/adam.book', spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `));
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook('agents/coding/nested/not-the-agent-name.book', spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `));
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(filePath, spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `));
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(new Response(spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `), { headers: { 'content-type': 'text/plain' } }));
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain('Remote workshop guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({ code: 'ENOENT' });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook('agents/coding/mentors/pavol.book', spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `));
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(async (url: string) => new Response(
+            url.endsWith('/parent.book')
+                ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+        )) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('reports missing %s names without silently dropping instructions', async (commitment) => {
+        await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+    });
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `));
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..49ba5343842a175c993afa92df3f477492caa03d
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -101,9 +102,16 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
     };
 }
 
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..d7fe3340da35061371d1c4d280a9cf5525253f5b
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,194 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(
+        private readonly agentDirectoryPath: string,
+        private readonly currentWorkingDirectory: string,
+    ) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+        if (!this.booksByName.has(normalizeAgentName('Adam'))) {
+            await ensureAdamAgentBook(this.agentDirectoryPath);
+            await this.readBook(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        }
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(spaceTrim((block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `));
+        }
+
+        throw new NotFoundError(spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `));
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        if (isValidAgentUrl(reference)) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath = isRelativeToBook && declaringBook.filePath
+            ? dirname(declaringBook.filePath)
+            : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(canonicalFilePath).href);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`));
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string_book, filePath?: string): LocalAgentBook {
+        const parsedSource = parseAgentSource(source);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..a88aa9457b916d1c3763e9d6a4f33b2a3c9433ae
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,98 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.findByName('Adam').url;
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(book: LocalAgentBook, inheritancePath: ReadonlyArray<string> = []): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(spaceTrim(`Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${book.filePath || book.url}\`.`));
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) => resolveBook(
+                await collection.getBook(url),
+                context.importAgentOptions.inheritancePath,
+            ),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: createLocalAgentReferenceResolver(collection, primaryBook),
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim() ? `${commitmentType} ${content}\n` : commitmentType === 'FROM' ? 'FROM @Null\n' : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n');
+}

apply patch
patch: completed
C:\Users\me\work\ai\promptbook\.vscode\terminals.json
C:\Users\me\work\ai\promptbook\apps\coder-landing\specs\sections\agent-book.md
C:\Users\me\work\ai\promptbook\apps\coder-landing\src\components\sections\AgentsSection.tsx
C:\Users\me\work\ai\promptbook\changelog\_current-preversion.md
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\coder\boilerplateTemplates.test.ts
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\common\LocalAgentBookCollection.ts
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\common\resolveLocalAgentSource.ts
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..3c70ba457e2ceb5ee969562d00d07d6df866f92e
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..2a71bdc2b93ce0addc6132265aba74caa02dbbeb
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,189 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(PRIMARY_AGENT_PATH, spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `));
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook('agents/coding/.core/adam.book', spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `));
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook('agents/coding/nested/not-the-agent-name.book', spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `));
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(filePath, spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `));
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(new Response(spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `), { headers: { 'content-type': 'text/plain' } }));
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain('Remote workshop guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({ code: 'ENOENT' });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook('agents/coding/mentors/pavol.book', spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `));
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(async (url: string) => new Response(
+            url.endsWith('/parent.book')
+                ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+        )) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('reports missing %s names without silently dropping instructions', async (commitment) => {
+        await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+    });
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `));
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..49ba5343842a175c993afa92df3f477492caa03d
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -101,9 +102,16 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
     };
 }
 
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..3b4ab4d40c0ecbefcb222c00bfd248a27bcab0d8
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,196 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(
+        private readonly agentDirectoryPath: string,
+        private readonly currentWorkingDirectory: string,
+    ) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+        if (!this.booksByName.has(normalizeAgentName('Adam'))) {
+            await ensureAdamAgentBook(this.agentDirectoryPath);
+            await this.readBook(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        }
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(spaceTrim((block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `));
+        }
+
+        throw new NotFoundError(spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `));
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath = isRelativeToBook && declaringBook.filePath
+            ? dirname(declaringBook.filePath)
+            : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(canonicalFilePath).href);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`));
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b195a486e65e7a369b9fd8cdce1ae613532c3338
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,98 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.findByName('Adam').url;
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(book: LocalAgentBook, inheritancePath: ReadonlyArray<string> = []): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(spaceTrim(`Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${book.filePath || book.url}\`.`));
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) => resolveBook(
+                await collection.getBook(url),
+                context.importAgentOptions.inheritancePath,
+            ),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: createLocalAgentReferenceResolver(collection, primaryBook),
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim() ? `${commitmentType} ${content}\n` : commitmentType === 'FROM' ? 'FROM @Null\n' : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content scripts/run-codex-prompts/git/commitChanges.ts -TotalCount 150; Get-Content scripts/run-codex-prompts/main/runCodexPrompts.ts -TotalCount 72; Get-Content scripts/run-codex-prompts/git/coderGitSync.ts | Select-Object -Skip 85 -First 50' in C:\Users\me\work\ai\promptbook
 succeeded in 433ms:
import { mkdir, realpath, unlink, writeFile } from 'fs/promises';
import { basename, dirname, relative, resolve } from 'path';
import { spaceTrim } from 'spacetrim';
import { $execCommand } from '../../../src/utils/execCommand/$execCommand';
import { resolvePromptbookTemporaryPath } from '../../../src/utils/filesystem/promptbookTemporaryPath';
import { buildAgentGitEnv, buildAgentGitSigningFlag } from './agentGitIdentity';
import { hasUpstreamBranch, listGitRemotes, readCurrentBranchName, readOptionalGitConfig } from './gitBranchContext';
import { runGitCommand } from './runGitCommand';

/**
 * Commits staged changes with the provided message using the dedicated coding-agent identity when configured,
 * otherwise falls back to the default Git configuration. Remote pushing is opt-in via `options.autoPush`,
 * `options.relevantPaths` restricts both the staging and the commit to the files of the current operation,
 * `options.excludePaths` can keep temporary artifacts out of the created commit and
 * `options.isEmptyCommitAllowed` keeps a round without any file change from failing.
 *
 * Note: The temporary commit message file is written inside the project, so it is always excluded from the commit
 *       itself for projects which do not keep the Promptbook temporary directory out of version control.
 */
export async function commitChanges(
    message: string,
    options?: {
        autoPush?: boolean;

        /**
         * Repository-relative paths which are relevant for the current operation.
         *
         * Only these paths are staged and only they end up in the created commit, so unrelated changes of the
         * project are left in the working tree. Everything is committed when the paths are not provided at all,
         * while an empty list commits nothing but the file changes already staged before.
         */
        relevantPaths?: ReadonlyArray<string>;
        excludePaths?: ReadonlyArray<string>;
        projectPath?: string;
        isEmptyCommitAllowed?: boolean;
    },
): Promise<void> {
    const projectPath = options?.projectPath || process.cwd();
    const commitMessagePath = resolvePromptbookTemporaryPath(
        projectPath,
        'ptbk-coder',
        'commit-messages',
        `COMMIT_MESSAGE_${Date.now()}.txt`,
    );
    await mkdir(dirname(commitMessagePath), { recursive: true });
    await writeFile(commitMessagePath, message, 'utf-8');

    try {
        const agentEnv = buildAgentGitEnv();
        const signingFlag = buildAgentGitSigningFlag();
        const excludedGitPaths = await normalizeExcludedGitPaths(projectPath, [
            commitMessagePath,
            ...(options?.excludePaths ?? []),
        ]);
        // Note: An excluded path must be dropped from the relevant paths as well, because a commit restricted by
        //       a pathspec commits the working tree content of those paths and would ignore unstaging them
        const relevantPaths = excludeGitPaths(options?.relevantPaths, excludedGitPaths);

        await stageCommitChanges(projectPath, agentEnv, relevantPaths, excludedGitPaths);

        await runGitCommand({
            command: buildGitCommitCommand({
                commitMessagePath,
                signingFlag,
                relevantPaths,
                isEmptyCommitAllowed: options?.isEmptyCommitAllowed,
            }),
            cwd: projectPath,
            env: agentEnv,
        });

        if (options?.autoPush) {
            await pushCommittedChanges(projectPath, agentEnv);
        }
    } finally {
        await unlink(commitMessagePath).catch(() => undefined);
    }
}

/**
 * Stages the relevant repository changes and unstages temporary files that should not end up inside the commit.
 */
async function stageCommitChanges(
    projectPath: string,
    agentEnv: Record<string, string> | undefined,
    relevantPaths: ReadonlyArray<string> | undefined,
    excludedGitPaths: ReadonlyArray<string>,
): Promise<void> {
    // Note: An operation which changed nothing relevant has nothing to stage
    if (relevantPaths === undefined || relevantPaths.length > 0) {
        await runGitCommand({
            command: buildGitAddCommand(relevantPaths),
            cwd: projectPath,
            env: agentEnv,
        });
    }

    if (excludedGitPaths.length === 0) {
        return;
    }

    await runGitCommand({
        command: `git reset --quiet HEAD -- ${excludedGitPaths.map(quoteShellPath).join(' ')}`,
        cwd: projectPath,
        env: agentEnv,
        isVerbose: false,
    });
}

/**
 * Builds the git add command for either the whole tree or the relevant paths of the current operation.
 */
function buildGitAddCommand(relevantPaths: ReadonlyArray<string> | undefined): string {
    if (!relevantPaths || relevantPaths.length === 0) {
        return 'git add .';
    }

    return `git add --all -- ${relevantPaths.map(quoteShellPath).join(' ')}`;
}

/**
 * Removes the excluded repository paths from the relevant paths of the current operation.
 */
function excludeGitPaths(
    relevantPaths: ReadonlyArray<string> | undefined,
    excludedGitPaths: ReadonlyArray<string>,
): ReadonlyArray<string> | undefined {
    if (relevantPaths === undefined || excludedGitPaths.length === 0) {
        return relevantPaths;
    }

    const excludedGitPathSet = new Set(excludedGitPaths);
    return relevantPaths.filter((relevantPath) => !excludedGitPathSet.has(normalizeGitPathSeparators(relevantPath)));
}

/**
 * Normalizes path separators so a relevant path can be matched against a repository-relative Git path.
 */
function normalizeGitPathSeparators(path: string): string {
    return path.replace(/\\/gu, '/');
}

/**
 * Converts excluded filesystem paths into unique repository-relative Git paths.
 */
async function normalizeExcludedGitPaths(
    projectPath: string,
    excludePaths: ReadonlyArray<string> | undefined,
): Promise<ReadonlyArray<string>> {
    if (!excludePaths || excludePaths.length === 0) {
import colors from 'colors';
import moment from 'moment';
import { join } from 'path';
import { spaceTrim } from 'spacetrim';
import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
import type { GitChangesMode } from '../../../src/cli/cli-commands/coder/GitChangesMode';
import { DatabaseError } from '../../../src/errors/DatabaseError';
import { NotAllowed } from '../../../src/errors/NotAllowed';
import { just } from '../../../src/utils/organization/just';
import type { RunOptions } from '../cli/RunOptions';
import { parseRunOptions } from '../cli/parseRunOptions';
import type { CoderRunPauseCheckpointOptions, WaitForCoderRunPauseCheckpoint } from '../common/CoderRunPauseCheckpoint';
import { CliProgressDisplay } from '../common/cliProgressDisplay';
import { loadCachedAveragePromptDurationMs } from '../common/coderRunEstimateCache';
import { createFreeDiskSpaceGuard, type FreeDiskSpaceGuard } from '../common/createFreeDiskSpaceGuard';
import type { PromptRunnerMetadata } from '../common/PromptRunnerMetadata';
import { resolveCoderAgent } from '../common/resolveCoderAgent';
import { sleepWithCountdown } from '../common/sleepWithCountdown';
import { resolveCoderContext } from '../common/resolveCoderContext';
import { listenForCoderRunControls } from '../common/listenForCoderRunControls';
import {
    announcePauseTargetLabel,
    checkPause,
    getEndAfterCurrentPromptState,
    resetCoderRunControls,
    resetPauseTargetLabel,
} from '../common/waitForPause';
import { waitForSkippableWorldTimeDeadline } from '../common/waitForSkippableWorldTimeDeadline';
import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
import { commitChanges } from '../git/commitChanges';
import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
import { pullLatestChanges } from '../git/pullLatestChanges';
import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
import { buildPromptLabelForDisplay } from '../prompts/buildPromptLabelForDisplay';
import { buildPromptSummary } from '../prompts/buildPromptSummary';
import { findNextTodoPrompt } from '../prompts/findNextTodoPrompt';
import type { PromptRunnerIdentity } from '../prompts/isPromptCompatibleWithRunner';
import { listUpcomingTasks } from '../prompts/listUpcomingTasks';
import { loadPromptFiles } from '../prompts/loadPromptFiles';
import { printPromptsToBeWritten } from '../prompts/printPromptsToBeWritten';
import { printStats } from '../prompts/printStats';
import { normalizePriorityFilter, type PriorityFilter } from '../prompts/priorityFilter';
import { printUpcomingTasks } from '../prompts/printUpcomingTasks';
import { resolveInterruptedPrompt } from '../prompts/resolveInterruptedPrompt';
import { summarizePrompts } from '../prompts/summarizePrompts';
import type { PromptFile } from '../prompts/types/PromptFile';
import type { PromptSelection } from '../prompts/types/PromptSelection';
import type { PromptStats } from '../prompts/types/PromptStats';
import { waitForPromptStart } from '../prompts/waitForPromptStart';
import type { PromptRunner } from '../runners/types/PromptRunner';
import { buildCoderRunAgentVisual } from '../ui/buildCoderRunAgentVisual';
import { renderCoderRunUi, type CoderRunUiHandle } from '../ui/renderCoderRunUi';
import { refreshCoderRunUiSubscriptionUsage } from '../ui/refreshCoderRunUiSubscriptionUsage';
import { createTestBeforeRepairPrompt } from '../testing/createTestBeforeRepairPrompt';
import { DEFAULT_CODER_TEST_COMMAND, isTestBeforeMode, type TestBeforeMode } from '../testing/TestBeforeMode';
import { limitTestOutput } from '../testing/limitTestOutput';
import { runTestBefore } from '../testing/runTestBefore';
import { resolvePromptRunner } from './resolvePromptRunner';
import { runPromptRound } from './runPromptRound';

/**
 * Constant for prompts dir.
 */
const PROMPTS_DIR = join(process.cwd(), 'prompts');

/**
 * Commit message for files changed by a successful or failed pre-coding test in repair mode.
 */
const PRE_CODING_TEST_CHANGES_COMMIT_MESSAGE = 'test: Apply changes made by pre-coding tests';

/**
 * Note: Only the files this very command has changed are committed, everything else is left in the working tree.
 * Note: A command which changed nothing is left alone instead of creating an empty commit.
 */
export async function $commitCoderChanges(options: {
    readonly gitSync: CoderGitSyncOptions;
    readonly commitMessage: string;
    readonly commitScope: CoderCommitScope;
}): Promise<void> {
    const { gitSync, commitMessage, commitScope } = options;

    if (!gitSync.isCommitEnabled) {
        return;
    }

    const relevantPaths = await resolveCoderCommitScopePaths(commitScope);
    if (relevantPaths.length === 0) {
        console.info(colors.gray('Nothing to commit, this command has not changed any file'));
        return;
    }

    await commitChanges(commitMessage, {
        projectPath: commitScope.projectPath,
        relevantPaths,
        autoPush: gitSync.isAutoPushEnabled,
    });

    console.info(
        colors.green(`✓ ${gitSync.isAutoPushEnabled ? 'Committed and pushed' : 'Committed'}: ${commitMessage}`),
    );
}

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..3c70ba457e2ceb5ee969562d00d07d6df866f92e
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..2a71bdc2b93ce0addc6132265aba74caa02dbbeb
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,189 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(PRIMARY_AGENT_PATH, spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `));
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook('agents/coding/.core/adam.book', spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `));
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook('agents/coding/nested/not-the-agent-name.book', spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `));
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(filePath, spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `));
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(new Response(spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `), { headers: { 'content-type': 'text/plain' } }));
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain('Remote workshop guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({ code: 'ENOENT' });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook('agents/coding/mentors/pavol.book', spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `));
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(async (url: string) => new Response(
+            url.endsWith('/parent.book')
+                ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+        )) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('reports missing %s names without silently dropping instructions', async (commitment) => {
+        await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+    });
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `));
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..49ba5343842a175c993afa92df3f477492caa03d
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -101,9 +102,16 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
     };
 }
 
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..3b4ab4d40c0ecbefcb222c00bfd248a27bcab0d8
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,196 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(
+        private readonly agentDirectoryPath: string,
+        private readonly currentWorkingDirectory: string,
+    ) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+        if (!this.booksByName.has(normalizeAgentName('Adam'))) {
+            await ensureAdamAgentBook(this.agentDirectoryPath);
+            await this.readBook(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        }
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(spaceTrim((block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `));
+        }
+
+        throw new NotFoundError(spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `));
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath = isRelativeToBook && declaringBook.filePath
+            ? dirname(declaringBook.filePath)
+            : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(canonicalFilePath).href);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`));
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b195a486e65e7a369b9fd8cdce1ae613532c3338
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,98 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.findByName('Adam').url;
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(book: LocalAgentBook, inheritancePath: ReadonlyArray<string> = []): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(spaceTrim(`Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${book.filePath || book.url}\`.`));
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) => resolveBook(
+                await collection.getBook(url),
+                context.importAgentOptions.inheritancePath,
+            ),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: createLocalAgentReferenceResolver(collection, primaryBook),
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim() ? `${commitmentType} ${content}\n` : commitmentType === 'FROM' ? 'FROM @Null\n' : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

codex
The 25 new regression cases pass, including nested paths, HTTP books, all four null/void forms, duplicate names, and cycles. The existing server inheritance tests also pass. I found a startup detail to address: a newly created Adam book would trigger the coder’s clean-tree check, so I’m integrating it with the coder’s existing commit behavior.

apply patch
patch: completed
C:\Users\me\work\ai\promptbook\scripts\run-codex-prompts\common\resolveCoderAgent.ts
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\common\LocalAgentBookCollection.ts
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\common\resolveLocalAgentSource.ts
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..3c70ba457e2ceb5ee969562d00d07d6df866f92e
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..2a71bdc2b93ce0addc6132265aba74caa02dbbeb
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,189 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(PRIMARY_AGENT_PATH, spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `));
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook('agents/coding/.core/adam.book', spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `));
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook('agents/coding/nested/not-the-agent-name.book', spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `));
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(filePath, spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `));
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(new Response(spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `), { headers: { 'content-type': 'text/plain' } }));
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain('Remote workshop guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({ code: 'ENOENT' });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook('agents/coding/mentors/pavol.book', spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `));
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(async (url: string) => new Response(
+            url.endsWith('/parent.book')
+                ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+        )) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('reports missing %s names without silently dropping instructions', async (commitment) => {
+        await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+    });
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `));
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..403acb29fc0da146cd18511a0567a72aa44d0aa5
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -40,6 +41,8 @@
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +104,17 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9e5da08ed1da02fd7da37ff18bc832b6f717fb09
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,200 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(
+        private readonly agentDirectoryPath: string,
+        private readonly currentWorkingDirectory: string,
+    ) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+        if (!this.booksByName.has(normalizeAgentName('Adam'))) {
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+            }
+            await this.readBook(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        }
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(spaceTrim((block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `));
+        }
+
+        throw new NotFoundError(spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `));
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath = isRelativeToBook && declaringBook.filePath
+            ? dirname(declaringBook.filePath)
+            : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(canonicalFilePath).href);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`));
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5082663208993b60dcb2608f185d4da819d5a621
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,116 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.findByName('Adam').url;
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(book: LocalAgentBook, inheritancePath: ReadonlyArray<string> = []): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(spaceTrim(`Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${book.filePath || book.url}\`.`));
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) => resolveBook(
+                await collection.getBook(url),
+                context.importAgentOptions.inheritancePath,
+            ),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim() ? `${commitmentType} ${content}\n` : commitmentType === 'FROM' ? 'FROM @Null\n' : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content scripts/run-codex-prompts/main/runCodexPrompts.ts -TotalCount 10; rg -n 'resolvedCoderAgent|resolveCoderAgent' scripts/run-codex-prompts/main/runCodexPrompts.test.ts; rg --files scripts/run-codex-prompts | rg 'runCodexPrompts.*test|runPromptRound.*test'" in C:\Users\me\work\ai\promptbook
 succeeded in 310ms:
import colors from 'colors';
import moment from 'moment';
import { join } from 'path';
import { spaceTrim } from 'spacetrim';
import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
import type { GitChangesMode } from '../../../src/cli/cli-commands/coder/GitChangesMode';
import { DatabaseError } from '../../../src/errors/DatabaseError';
import { NotAllowed } from '../../../src/errors/NotAllowed';
import { just } from '../../../src/utils/organization/just';
import type { RunOptions } from '../cli/RunOptions';
scripts/run-codex-prompts\main\runCodexPrompts.test.ts
scripts/run-codex-prompts\main\runPromptRound.test.ts

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..3c70ba457e2ceb5ee969562d00d07d6df866f92e
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..2a71bdc2b93ce0addc6132265aba74caa02dbbeb
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,189 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(PRIMARY_AGENT_PATH, spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `));
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook('agents/coding/.core/adam.book', spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `));
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook('agents/coding/nested/not-the-agent-name.book', spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `));
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(filePath, spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `));
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(new Response(spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `), { headers: { 'content-type': 'text/plain' } }));
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain('Remote workshop guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({ code: 'ENOENT' });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook('agents/coding/mentors/pavol.book', spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `));
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(async (url: string) => new Response(
+            url.endsWith('/parent.book')
+                ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+        )) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('reports missing %s names without silently dropping instructions', async (commitment) => {
+        await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+    });
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `));
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..403acb29fc0da146cd18511a0567a72aa44d0aa5
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -40,6 +41,8 @@
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +104,17 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9e5da08ed1da02fd7da37ff18bc832b6f717fb09
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,200 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(
+        private readonly agentDirectoryPath: string,
+        private readonly currentWorkingDirectory: string,
+    ) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+        if (!this.booksByName.has(normalizeAgentName('Adam'))) {
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+            }
+            await this.readBook(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        }
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(spaceTrim((block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `));
+        }
+
+        throw new NotFoundError(spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `));
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath = isRelativeToBook && declaringBook.filePath
+            ? dirname(declaringBook.filePath)
+            : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(canonicalFilePath).href);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`));
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5082663208993b60dcb2608f185d4da819d5a621
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,116 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.findByName('Adam').url;
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(book: LocalAgentBook, inheritancePath: ReadonlyArray<string> = []): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(spaceTrim(`Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${book.filePath || book.url}\`.`));
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) => resolveBook(
+                await collection.getBook(url),
+                context.importAgentOptions.inheritancePath,
+            ),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim() ? `${commitmentType} ${content}\n` : commitmentType === 'FROM' ? 'FROM @Null\n' : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content scripts/run-codex-prompts/git/workingTreeChanges.ts -TotalCount 110; Get-Content scripts/run-codex-prompts/main/runCodexPrompts.test.ts -TotalCount 120; Get-Content src/cli/cli-commands/coder/agentCliOptions.ts -TotalCount 65' in C:\Users\me\work\ai\promptbook
 succeeded in 325ms:
import { createHash } from 'crypto';
import { readFile, stat } from 'fs/promises';
import { resolve } from 'path';
import { $execCommand } from '../../../src/utils/execCommand/$execCommand';

/**
 * Git commands used to list changed and untracked files in the working tree.
 *
 * Note: None of them ever lists a file ignored by `.gitignore`, so a path taken from here can always be staged.
 * Note: `--no-renames` is used because a detected rename would be reported as its destination path only, which
 *       would leave the source path of a moved file out of the commit.
 */
const GIT_CHANGED_FILE_COMMANDS: ReadonlyArray<string> = [
    'git diff --name-only --no-renames --',
    'git diff --name-only --no-renames --cached --',
    'git ls-files --others --exclude-standard',
];

/**
 * Snapshot of file hashes for files that were already dirty before one operation started.
 *
 * Note: This is the single source of truth about which files one `ptbk coder` operation has really changed,
 *       used both for normalizing line endings and for committing only the relevant files.
 */
export type WorkingTreeChangesSnapshot = {
    /**
     * Hash of every dirty file at the moment the snapshot was taken.
     *
     * A file which was dirty but could not be hashed (a deleted file or a non-regular file) is stored as `null`,
     * so it can be told apart from a file which was not dirty at all.
     */
    readonly changedFileHashes: ReadonlyMap<string, string | null>;
};

/**
 * Captures hashes for files that are dirty before one operation starts.
 */
export async function captureWorkingTreeChangesSnapshot(projectPath: string): Promise<WorkingTreeChangesSnapshot> {
    const changedFiles = await listWorkingTreeChangedFiles(projectPath);
    const changedFileHashes = new Map<string, string | null>();

    for (const relativePath of changedFiles) {
        changedFileHashes.set(relativePath, await readWorkingTreeFileHash(projectPath, relativePath));
    }

    return { changedFileHashes };
}

/**
 * Lists the repository-relative paths which really changed since the snapshot was taken.
 *
 * A file which was already dirty before and was not touched afterwards is not listed, so the result contains
 * exactly the files changed by the operation which the snapshot wraps.
 */
export async function listFilesChangedSinceSnapshot(
    projectPath: string,
    snapshot: WorkingTreeChangesSnapshot,
): Promise<ReadonlyArray<string>> {
    const changedFiles = await listWorkingTreeChangedFiles(projectPath);
    const changedFilesSinceSnapshot: string[] = [];

    for (const relativePath of changedFiles) {
        const currentFileHash = await readWorkingTreeFileHash(projectPath, relativePath);
        const hashBeforeOperation = snapshot.changedFileHashes.get(relativePath);
        const wasDirtyBeforeOperation = snapshot.changedFileHashes.has(relativePath);

        if (wasDirtyBeforeOperation && hashBeforeOperation === currentFileHash) {
            continue;
        }

        changedFilesSinceSnapshot.push(relativePath);
    }

    return changedFilesSinceSnapshot;
}

/**
 * Lists dirty tracked files and untracked files in the working tree.
 */
export async function listWorkingTreeChangedFiles(projectPath: string): Promise<ReadonlyArray<string>> {
    const changedFiles = new Set<string>();

    for (const command of GIT_CHANGED_FILE_COMMANDS) {
        const output = await $execCommand({
            command,
            cwd: projectPath,
            isVerbose: false,
        });

        for (const filePath of output.split('\n').map(normalizeGitFilePath).filter(Boolean)) {
            changedFiles.add(filePath);
        }
    }

    return [...changedFiles.values()];
}

/**
 * Reads the content hash of one repository-relative working tree file.
 *
 * @returns Hash of a regular file, otherwise `null` for a deleted or non-regular file.
 */
export async function readWorkingTreeFileHash(projectPath: string, relativePath: string): Promise<string | null> {
    try {
        const absolutePath = resolve(projectPath, relativePath);
        const fileStats = await stat(absolutePath);
        if (!fileStats.isFile()) {
            return null;
        }

import { NotAllowed } from '../../../src/errors/NotAllowed';
import { NotFoundError } from '../../../src/errors/NotFoundError';
import { createFreeDiskSpaceGuard } from '../common/createFreeDiskSpaceGuard';
import { resolveCoderContext } from '../common/resolveCoderContext';
import { toggleEndAfterCurrentPromptState } from '../common/waitForPause';
import type { RunOptions } from '../cli/RunOptions';
import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
import { commitChanges } from '../git/commitChanges';
import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
import { pullLatestChanges } from '../git/pullLatestChanges';
import { findNextTodoPrompt } from '../prompts/findNextTodoPrompt';
import { loadPromptFiles } from '../prompts/loadPromptFiles';
import { resolveInterruptedPrompt } from '../prompts/resolveInterruptedPrompt';
import { summarizePrompts } from '../prompts/summarizePrompts';
import type { PromptFile } from '../prompts/types/PromptFile';
import type { PromptSection } from '../prompts/types/PromptSection';
import type { PromptSelection } from '../prompts/types/PromptSelection';
import type { PromptRunner } from '../runners/types/PromptRunner';
import { resolvePromptRunner } from './resolvePromptRunner';
import { runCodexPrompts } from './runCodexPrompts';
import { runPromptRound } from './runPromptRound';
import { createTestBeforeRepairPrompt } from '../testing/createTestBeforeRepairPrompt';
import { runTestBefore } from '../testing/runTestBefore';

jest.mock('../common/resolveCoderContext', () => ({
    resolveCoderContext: jest.fn(async () => undefined),
}));

jest.mock('../common/createFreeDiskSpaceGuard', () => ({
    // Note: The real guard measures the disk of the machine which runs the tests, which is not a stable input
    createFreeDiskSpaceGuard: jest.fn(() => async () => undefined),
}));

jest.mock('../git/ensureWorkingTreeClean', () => ({
    ensureWorkingTreeClean: jest.fn(async () => undefined),
}));

jest.mock('../git/coderCommitScope', () => ({
    captureCoderCommitScope: jest.fn(),
    resolveCoderCommitScopePaths: jest.fn(),
}));

jest.mock('../git/commitChanges', () => ({
    commitChanges: jest.fn(),
}));

jest.mock('../git/pullLatestChanges', () => ({
    pullLatestChanges: jest.fn(async () => undefined),
}));

jest.mock('../prompts/findNextTodoPrompt', () => ({
    findNextTodoPrompt: jest.fn(),
}));

jest.mock('../prompts/loadPromptFiles', () => ({
    loadPromptFiles: jest.fn(async () => []),
}));

jest.mock('../prompts/resolveInterruptedPrompt', () => ({
    resolveInterruptedPrompt: jest.fn(),
}));

jest.mock('../prompts/summarizePrompts', () => ({
    summarizePrompts: jest.fn(),
}));

jest.mock('./resolvePromptRunner', () => ({
    resolvePromptRunner: jest.fn(),
}));

jest.mock('./runPromptRound', () => ({
    runPromptRound: jest.fn(async () => undefined),
}));

jest.mock('../testing/runTestBefore', () => ({
    runTestBefore: jest.fn(),
}));

jest.mock('../testing/createTestBeforeRepairPrompt', () => ({
    createTestBeforeRepairPrompt: jest.fn(),
}));

/**
 * Commit scope captured before the pre-coding test in focused run-loop tests.
 */
const TEST_BEFORE_COMMIT_SCOPE: CoderCommitScope = {
    projectPath: process.cwd(),
    snapshotBeforeOperation: { changedFileHashes: new Map() },
};

/**
 * Builds a complete set of run options for focused validation tests.
 */
function createRunOptions(overrides: Partial<RunOptions> = {}): RunOptions {
    return {
        dryRun: false,
        context: undefined,
        testCommand: undefined,
        testBefore: 'no',
        preserveLogs: false,
        noUi: true,
        thinkingLevel: undefined,
        waitForUser: true,
        waitAfterPrompt: 0,
        waitBetweenPrompts: 0,
        waitAfterError: 0,
        noCommit: false,
        gitChanges: 'fail',
        normalizeLineEndings: true,
        allowCredits: false,
        autoMigrate: false,
        allowDestructiveAutoMigrate: false,
        autoPush: false,
        autoPull: false,
        agentName: 'github-copilot',
        model: 'gpt-5.4',
        priority: 0,
        ...overrides,
    };
}
import type {
    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
} from 'commander';

/**
 * Commander option bag for an optional Book agent used by coder commands.
 *
 * @private internal utility of `ptbk coder`
 */
export type CoderAgentCliOptions = {
    readonly agent?: string;
};

/**
 * Description shared by coder commands which accept an agent Book.
 *
 * @private internal utility of `ptbk coder`
 */
export const CODER_AGENT_OPTION_DESCRIPTION =
    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';

/**
 * Registers the shared `--agent` option for coder commands.
 *
 * @private internal utility of `ptbk coder`
 */
export function addCoderAgentOption(command: Program): void {
    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
}

// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
// Note: [💞] Ignore a discrepancy between file name and exported helper names

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..3c70ba457e2ceb5ee969562d00d07d6df866f92e
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..2a71bdc2b93ce0addc6132265aba74caa02dbbeb
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,189 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(PRIMARY_AGENT_PATH, spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `));
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook('agents/coding/.core/adam.book', spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `));
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook('agents/coding/nested/not-the-agent-name.book', spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `));
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(filePath, spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `));
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(new Response(spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `), { headers: { 'content-type': 'text/plain' } }));
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain('Remote workshop guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({ code: 'ENOENT' });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook('agents/coding/mentors/pavol.book', spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `));
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(async (url: string) => new Response(
+            url.endsWith('/parent.book')
+                ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+        )) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('reports missing %s names without silently dropping instructions', async (commitment) => {
+        await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+    });
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `));
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..403acb29fc0da146cd18511a0567a72aa44d0aa5
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -40,6 +41,8 @@
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +104,17 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9e5da08ed1da02fd7da37ff18bc832b6f717fb09
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,200 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(
+        private readonly agentDirectoryPath: string,
+        private readonly currentWorkingDirectory: string,
+    ) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+        if (!this.booksByName.has(normalizeAgentName('Adam'))) {
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+            }
+            await this.readBook(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        }
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(spaceTrim((block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `));
+        }
+
+        throw new NotFoundError(spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `));
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath = isRelativeToBook && declaringBook.filePath
+            ? dirname(declaringBook.filePath)
+            : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(canonicalFilePath).href);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`));
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5082663208993b60dcb2608f185d4da819d5a621
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,116 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.findByName('Adam').url;
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(book: LocalAgentBook, inheritancePath: ReadonlyArray<string> = []): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(spaceTrim(`Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${book.filePath || book.url}\`.`));
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) => resolveBook(
+                await collection.getBook(url),
+                context.importAgentOptions.inheritancePath,
+            ),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim() ? `${commitmentType} ${content}\n` : commitmentType === 'FROM' ? 'FROM @Null\n' : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

apply patch
patch: completed
C:\Users\me\work\ai\promptbook\scripts\run-codex-prompts\git\commitInitializedAgentBooks.test.ts
C:\Users\me\work\ai\promptbook\scripts\run-codex-prompts\git\commitInitializedAgentBooks.ts
C:\Users\me\work\ai\promptbook\scripts\run-codex-prompts\main\runCodexPrompts.ts
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..3c70ba457e2ceb5ee969562d00d07d6df866f92e
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..2a71bdc2b93ce0addc6132265aba74caa02dbbeb
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,189 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(PRIMARY_AGENT_PATH, spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `));
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook('agents/coding/.core/adam.book', spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `));
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook('agents/coding/nested/not-the-agent-name.book', spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `));
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(filePath, spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `));
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(new Response(spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `), { headers: { 'content-type': 'text/plain' } }));
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain('Remote workshop guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({ code: 'ENOENT' });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook('agents/coding/mentors/pavol.book', spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `));
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(async (url: string) => new Response(
+            url.endsWith('/parent.book')
+                ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+        )) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('reports missing %s names without silently dropping instructions', async (commitment) => {
+        await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+    });
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `));
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..403acb29fc0da146cd18511a0567a72aa44d0aa5
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -40,6 +41,8 @@
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +104,17 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9e5da08ed1da02fd7da37ff18bc832b6f717fb09
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,200 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(
+        private readonly agentDirectoryPath: string,
+        private readonly currentWorkingDirectory: string,
+    ) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+        if (!this.booksByName.has(normalizeAgentName('Adam'))) {
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+            }
+            await this.readBook(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        }
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(spaceTrim((block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `));
+        }
+
+        throw new NotFoundError(spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `));
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath = isRelativeToBook && declaringBook.filePath
+            ? dirname(declaringBook.filePath)
+            : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(canonicalFilePath).href);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`));
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5082663208993b60dcb2608f185d4da819d5a621
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,116 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.findByName('Adam').url;
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(book: LocalAgentBook, inheritancePath: ReadonlyArray<string> = []): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(spaceTrim(`Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${book.filePath || book.url}\`.`));
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) => resolveBook(
+                await collection.getBook(url),
+                context.importAgentOptions.inheritancePath,
+            ),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim() ? `${commitmentType} ${content}\n` : commitmentType === 'FROM' ? 'FROM @Null\n' : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm run test-types -- --pretty false' in C:\Users\me\work\ai\promptbook
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts scripts/run-codex-prompts/main/runCodexPrompts.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts src/cli/cli-commands/coder/init.test.ts' in C:\Users\me\work\ai\promptbook
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "git diff --stat; rg -n 'export |@private|TODO|optional|Optional' src/book-2.0/agent-source/resolveInheritedAgentSource.ts; Get-Content src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts -TotalCount 6" in C:\Users\me\work\ai\promptbook
 succeeded in 911ms:
warning: in the working copy of 'apps/agents-server/src/utils/agentReferenceResolver/AgentReferenceResolutionIssue.ts', CRLF will be replaced by LF the next time Git touches it
warning: in the working copy of 'apps/agents-server/src/utils/agentReferenceResolver/extractAgentReferenceTokens.ts', CRLF will be replaced by LF the next time Git touches it
warning: in the working copy of 'apps/agents-server/src/utils/explicitFromCommitment.ts', CRLF will be replaced by LF the next time Git touches it
warning: in the working copy of 'apps/agents-server/src/utils/resolveInheritedAgentSource.ts', CRLF will be replaced by LF the next time Git touches it
warning: in the working copy of 'src/cli/cli-commands/coder/ensureCoderDeveloperAgentFile.ts', CRLF will be replaced by LF the next time Git touches it
 .vscode/terminals.json                             |   7 +
 .../AgentReferenceResolutionIssue.ts               |  70 +-
 .../extractAgentReferenceTokens.ts                 |  62 +-
 .../src/utils/explicitFromCommitment.ts            | 131 +---
 .../src/utils/resolveInheritedAgentSource.ts       | 850 +--------------------
 apps/coder-landing/specs/sections/agent-book.md    |   2 +-
 .../src/components/sections/AgentsSection.tsx      |  25 +-
 changelog/_current-preversion.md                   |   8 +
 prompts/2026-09-0260-ptbk-coder-working-from.md    |   3 +-
 scripts/generate-packages/generate-packages.ts     |   4 +
 .../messages/createAgentRunnerSystemMessage.ts     |  10 +-
 .../run-codex-prompts/common/resolveCoderAgent.ts  |  13 +-
 scripts/run-codex-prompts/main/runCodexPrompts.ts  |   5 +
 .../coder/boilerplateTemplates.test.ts             |   8 +
 .../coder/ensureCoderDeveloperAgentFile.ts         |  30 +-
 src/cli/cli-commands/coder/init.ts                 |   4 +-
 .../coder/initializeCoderProjectConfiguration.ts   |   5 +
 .../coder/printInitializationSummary.ts            |   6 +
 src/cli/cli-commands/coder/run.ts                  |   2 +
 19 files changed, 130 insertions(+), 1115 deletions(-)
25:    // <- TODO: [🈲] Simple and encapsulated way to get book corpus
29:    // <- TODO: [🈲] Simple and encapsulated way to get book corpus
105: * @param agentReferenceResolver - Optional compact-reference resolver.
150:  * @private internal utility of agent source resolution
152:export type ResolveInheritedAgentSourceOptions = {
185:  * @private internal utility of agent source resolution
187:export type AgentSourceImporterContext = {
205:  * @private internal utility of agent source resolution
207:export type AgentSourceImporter = (
309: * @private internal helper for Agents Server inherited/imported agent resolution
365: * @private internal helper for Agents Server inherited/imported agent resolution
671:    // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
709:    // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
822:  * @private internal utility of agent source resolution
824:export async function resolveInheritedAgentSource(
836:// TODO: [🈲] Create a function that can manipulate books by modifying commitments, splitting the book up into commitments or syntactic tokens, and editing or deleting these via object methods.
import type { InitializationStatus } from './boilerplateTemplates';
import {
    PROMPTS_DIRECTORY_PATH,
    PROMPTS_DONE_DIRECTORY_PATH,
    PROMPTS_TEMPLATES_DIRECTORY_PATH,
} from './boilerplateTemplates';

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..3c70ba457e2ceb5ee969562d00d07d6df866f92e
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..2a71bdc2b93ce0addc6132265aba74caa02dbbeb
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,189 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(PRIMARY_AGENT_PATH, spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `));
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook('agents/coding/.core/adam.book', spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `));
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook('agents/coding/nested/not-the-agent-name.book', spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `));
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(filePath, spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `));
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(new Response(spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `), { headers: { 'content-type': 'text/plain' } }));
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain('Remote workshop guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({ code: 'ENOENT' });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook('agents/coding/mentors/pavol.book', spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `));
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(async (url: string) => new Response(
+            url.endsWith('/parent.book')
+                ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+        )) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('reports missing %s names without silently dropping instructions', async (commitment) => {
+        await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+    });
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `));
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..403acb29fc0da146cd18511a0567a72aa44d0aa5
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -40,6 +41,8 @@
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +104,17 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9e5da08ed1da02fd7da37ff18bc832b6f717fb09
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,200 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(
+        private readonly agentDirectoryPath: string,
+        private readonly currentWorkingDirectory: string,
+    ) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+        if (!this.booksByName.has(normalizeAgentName('Adam'))) {
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+            }
+            await this.readBook(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        }
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(spaceTrim((block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `));
+        }
+
+        throw new NotFoundError(spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `));
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath = isRelativeToBook && declaringBook.filePath
+            ? dirname(declaringBook.filePath)
+            : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(canonicalFilePath).href);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`));
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5082663208993b60dcb2608f185d4da819d5a621
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,116 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.findByName('Adam').url;
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(book: LocalAgentBook, inheritancePath: ReadonlyArray<string> = []): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(spaceTrim(`Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${book.filePath || book.url}\`.`));
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) => resolveBook(
+                await collection.getBook(url),
+                context.importAgentOptions.inheritancePath,
+            ),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim() ? `${commitmentType} ${content}\n` : commitmentType === 'FROM' ? 'FROM @Null\n' : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

 succeeded in 12965ms:

> promptbook@0.114.0-38 test-unit
> node -r ./scripts/ignore-kill-eperm.js ./node_modules/jest/bin/jest.js --forceExit --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts scripts/run-codex-prompts/main/runCodexPrompts.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts src/cli/cli-commands/coder/init.test.ts

PASS scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
PASS src/cli/cli-commands/coder/boilerplateTemplates.test.ts
PASS scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
PASS scripts/run-codex-prompts/main/runCodexPrompts.test.ts
  ● Console

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:967:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:967:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:967:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [90mPulling latest changes before the next prompt...[39m

      at pullLatestChangesIfEnabled (scripts/run-codex-prompts/main/runCodexPrompts.ts:448:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [90mPulling latest changes before the next prompt...[39m

      at pullLatestChangesIfEnabled (scripts/run-codex-prompts/main/runCodexPrompts.ts:448:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:967:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:967:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:967:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:967:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:967:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:967:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mRun limit reached after 2 prompt runs.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:967:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [32mEnd requested after 1 prompt run.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:967:17)

PASS src/cli/cli-commands/coder/init.test.ts

Test Suites: 5 passed, 5 total
Tests:       57 passed, 57 total
Snapshots:   0 total
Time:        9.909 s, estimated 28 s
Ran all test suites within paths "scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts", "scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts", "scripts/run-codex-prompts/main/runCodexPrompts.test.ts", "src/cli/cli-commands/coder/boilerplateTemplates.test.ts", "src/cli/cli-commands/coder/init.test.ts".
Force exiting Jest: Have you considered using `--detectOpenHandles` to detect async operations that kept running after all tests finished?

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts -Tail 8; Get-Content changelog/_current-preversion.md -Tail 15; Get-Content changelog/_current-preversion.md -TotalCount 13; Get-Content src/book-2.0/agent-source/resolveInheritedAgentSource.ts -TotalCount 30; Get-Content src/book-2.0/agent-source/resolveInheritedAgentSource.ts | Select-Object -Skip 145 -First 75; Get-Content src/cli/cli-commands/common/LocalAgentBookCollection.ts; Get-Content src/cli/cli-commands/common/resolveLocalAgentSource.ts' in C:\Users\me\work\ai\promptbook
 succeeded in 675ms:
        referencedArtifactStatuses,
        initializedEnvVariableNames,
    };
}

// Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
import { join } from 'path';
import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
-   Improved the Agents Server **Book language manual** so its linked table of contents navigates directly to each commitment, and each end-to-end example links its used commitments back to their sections. The manual now describes `@Foo` and `{Foo foo}` as agent references rather than parameters, renames the `---` primitive to **Multiple agent**, documents code fences as ` ``` `, links every generated-from source to the Promptbook repository, hides parser/runtime implementation details and unfinished commitments, and keeps each commitment example focused on its canonical keyword with only `GOAL` and `CLOSED` as supporting syntax.

-   Fixed the published `@promptbook/components` package, which broke the production build of every browser application that installs `ptbk`, `promptbook` or `@promptbook/components`. Rendered markdown is sanitized by `DOMPurify`, which needs a DOM, so `renderMarkdown` creates one through `jsdom` whenever it runs outside of a browser - for example during server-side rendering. That code path is unreachable in a browser, but bundlers resolve `require('jsdom')` statically, so `next build` followed it into `jsdom` and failed with **Module not found: Can't resolve `fs`** together with the same error for `child_process` and `net`. It failed even for an application that imports `<MarkdownContent/>` with `ssr: false`, because a bundler resolves a module it will never execute just as eagerly as one it will:

    -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
    -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
    -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
    test preset in VS Code and documented the reference rules on the coder landing page.
-   Fixed the React 19 peer dependency warnings during `npm install` by replacing `swagger-ui-react` with the
    standalone `swagger-ui-dist` bundle, shared with the Express API docs. The Swagger page still loads on demand
    and keeps its API-key panel, saved authorization and display settings.
    Test builds now skip Sentry source-map uploads to avoid temporary upload bundles and Windows cleanup failures.
    CLI help tests use the same command registration as the executable without starting a new process for every
    subcommand. CLI startup and version checks still run in child processes, with a timeout that stops stuck children.

-   A generated system message no longer repeats the same instructions. Commitments are applied one by one, so a
    commitment used several times in one book emitted its whole section every time, including the shared guidance
    paragraph which introduces it. Two `WRITING RULES` blocks produced two `## Writing rules` headings each preceded by
    the same four-line explanation of what a writing rule is, two `USE PRIVACY` blocks produced the `## Privacy`
    instructions twice, and the same held for `WRITING SAMPLE`, `SAMPLE`, `LANGUAGE`, `USE POPUP`,
    `USE IMAGE GENERATOR`, `USE USER LOCATION` and `TEAM`. The final system message is now deduplicated once for all
import type { string_agent_url } from '../../types/string_agent_url';
import type { string_book } from './string_book';
import type { AgentReferenceResolver } from './AgentReferenceResolver';
import { padBook } from './padBook';
import { isVoidPseudoAgentReference } from './pseudoAgentReferences';
import { validateBook } from './string_book';
import { ParseError } from '../../errors/ParseError';
import { spaceTrim } from '../../utils/organization/spaceTrim';
import { isValidAgentUrl } from '../../utils/validators/url/isValidAgentUrl';
import {
    type AgentReferenceResolutionIssue,
    consumeAgentReferenceResolutionIssues,
} from './AgentReferenceResolutionIssue';
import { collectExplicitFromCommitments, getExplicitFromCommitmentContent } from './explicitFromCommitment';

/**
 * Gets the corpus of an agent source (removes title and trailing status)
 *
 * @param agentSource The agent source
 * @returns The agent source corpus
 */
function getAgentSourceCorpus(agentSource: string_book): string {
    // Remove trailing OPEN or CLOSED if present
    const agentSourceWithoutStatus = agentSource.replace(/\n?(OPEN|CLOSED)\s*$/i, '') as string_book;
    // <- TODO: [🈲] Simple and encapsulated way to get book corpus

    // Remove the first line (title) from agent source
    const agentSourceCorpus = spaceTrim(agentSourceWithoutStatus.replace(/^.*$/m, ''));
    // <- TODO: [🈲] Simple and encapsulated way to get book corpus

}

/**
 * Shared options for resolving one agent source with inheritance and imports applied.
  * @private internal utility of agent source resolution
 */
export type ResolveInheritedAgentSourceOptions = {
    /** Current recursion depth, forwarded to the source importer. */
    readonly recursionLevel?: number;
    /**
     * The URL of the Adam agent to use as the default ancestor
     *
     * @default 'https://core.ptbk.io/agents/adam'
     */
    readonly adamAgentUrl?: string_agent_url;
    /**
     * Custom resolver used to expand compact agent references.
     */
    readonly agentReferenceResolver?: AgentReferenceResolver;
    /**
     * Canonical URL of the currently resolved agent.
     */
    readonly currentAgentUrl?: string_agent_url;
    /**
     * Additional equivalent URLs that should be treated as the current agent while detecting cycles.
     */
    readonly currentAgentAliases?: ReadonlyArray<string_agent_url>;
    /**
     * Already visited agent URLs in the current resolution stack.
     */
    readonly inheritancePath?: ReadonlyArray<string_agent_url>;
    /**
     * Source importer supplied by the host application.
     */
    readonly agentSourceImporter: AgentSourceImporter;
};

/**
 * Context passed to a custom agent source importer.
  * @private internal utility of agent source resolution
 */
export type AgentSourceImporterContext = {
    /**
     * Commitment that requested the imported source.
     */
    readonly commitmentType: 'FROM' | 'IMPORT';

    /**
     * Import options propagated from the current resolution pass.
     */
    readonly importAgentOptions: {
        readonly recursionLevel?: number;
        readonly inheritancePath?: ReadonlyArray<string_agent_url>;
    };

};

/**
 * Loads and recursively resolves one referenced agent using the caller's transport.
  * @private internal utility of agent source resolution
 */
export type AgentSourceImporter = (
    agentUrl: string_agent_url,
    context: AgentSourceImporterContext,
) => Promise<string_book>;

/**
 * Parent inheritance state derived before rewriting the source body.
 */
type ResolvedParentAgentContext = {
    /**
     * Effective parent URL after resolving explicit `FROM`, `FROM VOID`, or implicit Adam fallback.
     */
    readonly parentAgentUrl: string_agent_url | null;

import { readFile, readdir, realpath } from 'fs/promises';
import { dirname, isAbsolute, join, resolve } from 'path';
import { pathToFileURL } from 'url';
import { spaceTrim } from 'spacetrim';
import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
import type { string_book } from '../../../book-2.0/agent-source/string_book';
import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
import { NotFoundError } from '../../../errors/NotFoundError';
import { ParseError } from '../../../errors/ParseError';
import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';

/**
 * A book's source and identity, retaining its location for nested relative references.
 *
 * @private internal type of CLI agent resolution
 */
export type LocalAgentBook = {
    readonly url: string;
    readonly filePath?: string;
    readonly source: string_book;
    readonly profile: TeammateProfile;
};

/**
 * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
 * These identifiers are always intercepted by this collection and are never fetched.
 */
const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';

/**
 * Directories which cannot contain project-owned agent definitions.
 */
const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);

/**
 * Reads repository books and indexes their first-line names for CLI reference resolution.
 *
 * @private internal utility of CLI agent resolution
 */
export class LocalAgentBookCollection {
    private readonly booksByUrl = new Map<string, LocalAgentBook>();
    private readonly booksByName = new Map<string, LocalAgentBook[]>();
    /** Files initialized by this collection, for the coder's normal commit handling. */
    public readonly createdAgentBookPaths: string[] = [];

    /** Creates a collection rooted at the primary book's directory. */
    public constructor(
        private readonly agentDirectoryPath: string,
        private readonly currentWorkingDirectory: string,
    ) {}

    /** Discovers nested books, including the hidden `.core` directory. */
    public async initialize(): Promise<void> {
        await this.readDirectory(this.agentDirectoryPath);
        if (!this.booksByName.has(normalizeAgentName('Adam'))) {
            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
                this.createdAgentBookPaths.push(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
            }
            await this.readBook(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
        }
    }

    /** Resolves a unique first-line name, refusing ambiguous matches. */
    public findByName(name: string): LocalAgentBook {
        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
        if (matches.length === 1) {
            return matches[0]!;
        }

        if (matches.length > 1) {
            throw new ParseError(spaceTrim((block) => `
                Agent reference \`${name}\` is ambiguous. Use an explicit book path.

                Matching books:
                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
            `));
        }

        throw new NotFoundError(spaceTrim(`
            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.

            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
        `));
    }

    /** Looks up an indexed profile without changing the source-resolution stack. */
    public getProfile(url: string): TeammateProfile | null {
        return this.booksByUrl.get(url)?.profile || null;
    }

    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
        const isRemoteReference = Boolean(isValidAgentUrl(reference));
        if (isRemoteReference) {
            return reference;
        }

        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
        if (!isBookPath) {
            return this.findByName(reference).url;
        }

        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
        if (!declaringBook.filePath && isRelativeToBook) {
            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
        }

        const directoryPath = isRelativeToBook && declaringBook.filePath
            ? dirname(declaringBook.filePath)
            : this.currentWorkingDirectory;
        return (await this.readBook(resolve(directoryPath, reference))).url;
    }

    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
    public async readBook(filePath: string): Promise<LocalAgentBook> {
        try {
            const canonicalFilePath = await realpath(filePath);
            const url = LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(canonicalFilePath).href);
            const existingBook = this.booksByUrl.get(url);
            if (existingBook) {
                return existingBook;
            }

            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
        } catch (error) {
            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
            }
            throw error;
        }
    }

    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
    public async getBook(url: string): Promise<LocalAgentBook> {
        const existingBook = this.booksByUrl.get(url);
        if (existingBook) {
            return existingBook;
        }
        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
        }

        const bookUrl = new URL(url);
        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
        }
        const response = await fetch(bookUrl.href);
        if (!response.ok) {
            throw new NotFoundError(spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`));
        }
        let source: unknown;
        if (response.headers.get('content-type')?.includes('application/json')) {
            const payload: unknown = await response.json();
            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
        } else {
            source = await response.text();
        }
        if (typeof source !== 'string') {
            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
        }
        return this.registerBook(url, source);
    }

    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
        const agentSource = source as string_book;
        const parsedSource = parseAgentSource(agentSource);
        const book: LocalAgentBook = {
            url,
            filePath,
            source: agentSource,
            profile: {
                agentName: parsedSource.meta.fullname || parsedSource.agentName,
                personaDescription: parsedSource.personaDescription,
            },
        };
        this.booksByUrl.set(url, book);
        // Remote titles must not shadow repository-local names.
        if (filePath) {
            const agentName = normalizeAgentName(parsedSource.agentName);
            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
        }
        return book;
    }

    /** Walks real directories in deterministic order without following directory symlink loops. */
    private async readDirectory(directoryPath: string): Promise<void> {
        const entries = await readdir(directoryPath, { withFileTypes: true });
        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
            const entryPath = join(directoryPath, entry.name);
            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
                await this.readDirectory(entryPath);
            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
                await this.readBook(entryPath);
            }
        }
    }
}
import { dirname } from 'path';
import { spaceTrim } from 'spacetrim';
import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
import type { string_book } from '../../../book-2.0/agent-source/string_book';
import { DEFAULT_MAX_RECURSION } from '../../../config';
import { ParseError } from '../../../errors/ParseError';
import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';

/**
 * Resolved source together with the profile resolver used when compiling TEAM commitments.
 *
 * @private internal type of CLI agent resolution
 */
export type ResolvedLocalAgentSource = {
    readonly agentSource: string_book;
    readonly agentReferenceResolver: AgentReferenceResolver;
    /** Newly created books which the coder may include in its initialization commit. */
    readonly createdAgentBookPaths: ReadonlyArray<string>;
};

/**
 * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
 *
 * @private internal utility of CLI agent resolution
 */
export async function resolveLocalAgentSource(
    agentBookPath: string,
    currentWorkingDirectory: string,
): Promise<ResolvedLocalAgentSource> {
    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
    await collection.initialize();
    const primaryBook = await collection.readBook(agentBookPath);
    const adamAgentUrl = collection.findByName('Adam').url;

    /** Resolves each dependency with its own location and the current inheritance stack. */
    async function resolveBook(book: LocalAgentBook, inheritancePath: ReadonlyArray<string> = []): Promise<string_book> {
        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
            throw new ParseError(spaceTrim(`Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${book.filePath || book.url}\`.`));
        }
        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
        return resolveInheritedAgentSource(source, {
            adamAgentUrl,
            currentAgentUrl: book.url,
            inheritancePath,
            agentSourceImporter: async (url, context) => resolveBook(
                await collection.getBook(url),
                context.importAgentOptions.inheritancePath,
            ),
        });
    }

    return {
        agentSource: await resolveBook(primaryBook),
        agentReferenceResolver: {
            ...createLocalAgentReferenceResolver(collection, primaryBook),
            resolveTeammateProfile: async (url) => {
                if (isPseudoAgentUrl(url)) {
                    return null;
                }
                const book = await collection.getBook(url);
                const parsedSource = parseAgentSource(await resolveBook(book));
                return {
                    agentName: book.profile.agentName,
                    personaDescription: parsedSource.personaDescription,
                };
            },
        },
        createdAgentBookPaths: collection.createdAgentBookPaths,
    };
}

/**
 * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
 * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
 */
async function resolveBookCommitmentReferences(
    source: string_book,
    resolver: AgentReferenceResolver,
): Promise<string_book> {
    const parsedSource = parseAgentSourceWithCommitments(source);
    const lines = source.split(/\r?\n/);
    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
        .map((commitment) => commitment.lineNumber - 1)
        .sort((left, right) => left - right);
    const effectiveFrom = getEffectiveExplicitFromCommitment(source);

    for (const commitment of [...parsedSource.commitments].reverse()) {
        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
            continue;
        }
        const startLine = commitment.lineNumber - 1;
        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
            continue;
        }
        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
        // A horizontal rule also terminates a commitment before the next registered keyword.
        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
                endLine = lineIndex;
                break;
            }
        }
        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
        const replacement = content.trim() ? `${commitmentType} ${content}\n` : commitmentType === 'FROM' ? 'FROM @Null\n' : '';
        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
    }
    return lines.join('\n') as string_book;
}

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..3c70ba457e2ceb5ee969562d00d07d6df866f92e
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..2a71bdc2b93ce0addc6132265aba74caa02dbbeb
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,189 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(PRIMARY_AGENT_PATH, spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `));
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook('agents/coding/.core/adam.book', spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `));
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook('agents/coding/nested/not-the-agent-name.book', spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `));
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(filePath, spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `));
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(new Response(spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `), { headers: { 'content-type': 'text/plain' } }));
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain('Remote workshop guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({ code: 'ENOENT' });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook('agents/coding/mentors/pavol.book', spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `));
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(async (url: string) => new Response(
+            url.endsWith('/parent.book')
+                ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+        )) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('reports missing %s names without silently dropping instructions', async (commitment) => {
+        await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+    });
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `));
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..403acb29fc0da146cd18511a0567a72aa44d0aa5
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -40,6 +41,8 @@
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +104,17 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9e5da08ed1da02fd7da37ff18bc832b6f717fb09
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,200 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(
+        private readonly agentDirectoryPath: string,
+        private readonly currentWorkingDirectory: string,
+    ) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+        if (!this.booksByName.has(normalizeAgentName('Adam'))) {
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+            }
+            await this.readBook(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        }
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(spaceTrim((block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `));
+        }
+
+        throw new NotFoundError(spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `));
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath = isRelativeToBook && declaringBook.filePath
+            ? dirname(declaringBook.filePath)
+            : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(canonicalFilePath).href);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`));
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5082663208993b60dcb2608f185d4da819d5a621
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,116 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.findByName('Adam').url;
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(book: LocalAgentBook, inheritancePath: ReadonlyArray<string> = []): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(spaceTrim(`Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${book.filePath || book.url}\`.`));
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) => resolveBook(
+                await collection.getBook(url),
+                context.importAgentOptions.inheritancePath,
+            ),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim() ? `${commitmentType} ${content}\n` : commitmentType === 'FROM' ? 'FROM @Null\n' : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

 succeeded in 24567ms:

> promptbook@0.114.0-38 test-types
> tsc --pretty false


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "@'
from pathlib import Path
path = Path('src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts')
source = path.read_text(encoding='utf-8')
imports = \"import { join } from 'path';\\nimport { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';\\n\"
source = imports + source.replace(imports, '')
path.write_text(source, encoding='utf-8')
path = Path('changelog/_current-preversion.md')
source = path.read_text(encoding='utf-8')
start = source.index('-   "'`ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books'"')
entry = source[start:].strip()
entry = entry.replace('    test preset in VS Code and documented the reference rules on the coder landing page.', '    test preset in VS Code and documented the reference rules on the coder landing page. Normal committing runs\\n    commit a newly created Adam book before checking for a clean working tree; "'`--no-commit` leaves it uncommitted.'"')
path.write_text(entry + '\\n\\n' + source[:start].rstrip() + '\\n', encoding='utf-8')
for name in ['resolveInheritedAgentSource.ts', 'AgentReferenceResolutionIssue.ts', 'extractAgentReferenceTokens.ts']:
    path = Path('src/book-2.0/agent-source') / name
    source = path.read_text(encoding='utf-8').replace('  * @private', ' *\\n * @private')
    path.write_text(source, encoding='utf-8')
'@ | python -
npx prettier --write src/book-2.0/agent-source/resolveInheritedAgentSource.ts src/book-2.0/agent-source/AgentReferenceResolutionIssue.ts src/book-2.0/agent-source/extractAgentReferenceTokens.ts src/book-2.0/agent-source/explicitFromCommitment.ts src/cli/cli-commands/common/LocalAgentBookCollection.ts src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts src/cli/cli-commands/common/resolveLocalAgentSource.ts src/cli/cli-commands/common/resolveBundledAgentBookPath.ts src/cli/cli-commands/common/ensureAdamAgentBook.ts apps/agents-server/src/utils/resolveInheritedAgentSource.ts scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts" in C:\Users\me\work\ai\promptbook
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg --files apps/agents-server/src/utils | rg '(resolve.*Agent.*\\.test|agentReferenceResolver.*\\.test)'" in C:\Users\me\work\ai\promptbook
 succeeded in 656ms:
apps/agents-server/src/utils\agentGoalChat\resolveEffectiveAgentGoal.test.ts
apps/agents-server/src/utils\agentRouting\resolveAgentRouteTarget.test.ts
apps/agents-server/src/utils\agentBrowserProfile\resolveAgentBrowserProfileDirectory.test.ts
apps/agents-server/src/utils\defaultAgents\resolveDefaultAgentsStatus.test.ts
apps/agents-server/src/utils\userWallet\resolveWalletAgentPermanentId.test.ts
apps/agents-server/src/utils\agentReferenceResolver\resolveTeamCapabilitiesFromAgentSource.test.ts
apps/agents-server/src/utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.test.ts
apps/agents-server/src/utils\agentReferenceResolver\createUnresolvedAgentReferenceDiagnostics.test.ts
apps/agents-server/src/utils\agentReferenceResolver\createServerAgentReferenceResolver.test.ts
apps/agents-server/src/utils\agentReferenceResolver\createAgentInheritanceDiagnostics.test.ts
apps/agents-server/src/utils\agentReferenceResolver\bookScopedAgentReferences.test.ts
apps/agents-server/src/utils\agentReferenceResolver\$provideAgentReferenceResolver.test.ts
apps/agents-server/src/utils\agentProjects\resolveAgentProjectFaviconRelativePath.test.ts
apps/agents-server/src/utils\agentProjects\resolveAgentProjectProfile.test.ts
apps/agents-server/src/utils\agentProjects\resolveAgentProjectInfo.test.ts
apps/agents-server/src/utils\resolveAppendOnlySelfLearningAgentSource.test.ts
apps/agents-server/src/utils\resolveAgentStateFromSource.test.ts
apps/agents-server/src/utils\resolveAgentStateFromSource.storedAgents.test.ts
apps/agents-server/src/utils\resolveInheritedAgentSource.test.ts
apps/agents-server/src/utils\transpilers\resolveTranspiledAgentCodeExport.test.ts

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..3c70ba457e2ceb5ee969562d00d07d6df866f92e
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..2a71bdc2b93ce0addc6132265aba74caa02dbbeb
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,189 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(PRIMARY_AGENT_PATH, spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `));
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook('agents/coding/.core/adam.book', spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `));
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook('agents/coding/nested/not-the-agent-name.book', spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `));
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(filePath, spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `));
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(new Response(spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `), { headers: { 'content-type': 'text/plain' } }));
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain('Remote workshop guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({ code: 'ENOENT' });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook('agents/coding/mentors/pavol.book', spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `));
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(async (url: string) => new Response(
+            url.endsWith('/parent.book')
+                ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+        )) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('reports missing %s names without silently dropping instructions', async (commitment) => {
+        await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+    });
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `));
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..403acb29fc0da146cd18511a0567a72aa44d0aa5
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -40,6 +41,8 @@
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +104,17 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9e5da08ed1da02fd7da37ff18bc832b6f717fb09
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,200 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(
+        private readonly agentDirectoryPath: string,
+        private readonly currentWorkingDirectory: string,
+    ) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+        if (!this.booksByName.has(normalizeAgentName('Adam'))) {
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+            }
+            await this.readBook(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        }
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(spaceTrim((block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `));
+        }
+
+        throw new NotFoundError(spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `));
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath = isRelativeToBook && declaringBook.filePath
+            ? dirname(declaringBook.filePath)
+            : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(canonicalFilePath).href);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`));
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5082663208993b60dcb2608f185d4da819d5a621
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,116 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.findByName('Adam').url;
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(book: LocalAgentBook, inheritancePath: ReadonlyArray<string> = []): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(spaceTrim(`Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${book.filePath || book.url}\`.`));
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) => resolveBook(
+                await collection.getBook(url),
+                context.importAgentOptions.inheritancePath,
+            ),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim() ? `${commitmentType} ${content}\n` : commitmentType === 'FROM' ? 'FROM @Null\n' : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

 succeeded in 13987ms:
src\book-2.0\agent-source\resolveInheritedAgentSource.ts 587ms
src\book-2.0\agent-source\AgentReferenceResolutionIssue.ts 10ms
src\book-2.0\agent-source\extractAgentReferenceTokens.ts 11ms
src\book-2.0\agent-source\explicitFromCommitment.ts 14ms
src\cli\cli-commands\common\LocalAgentBookCollection.ts 54ms
src\cli\cli-commands\common\createLocalAgentReferenceResolver.ts 11ms
src\cli\cli-commands\common\resolveLocalAgentSource.ts 19ms
src\cli\cli-commands\common\resolveBundledAgentBookPath.ts 5ms
src\cli\cli-commands\common\ensureAdamAgentBook.ts 7ms
apps\agents-server\src\utils\resolveInheritedAgentSource.ts 6ms
scripts\run-codex-prompts\common\resolveCoderAgent.references.test.ts 30ms
scripts\run-codex-prompts\git\commitInitializedAgentBooks.ts 5ms
scripts\run-codex-prompts\git\commitInitializedAgentBooks.test.ts 8ms

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm test *> .tmp/coder-from-npm-test.log; $coderTestExitCode = $LASTEXITCODE; Get-Content .tmp/coder-from-npm-test.log -Tail 65; exit $coderTestExitCode' in C:\Users\me\work\ai\promptbook
 exited 1 in 6430ms:

> promptbook@0.114.0-38 test
> echo "Note: Running from most lightweight to heaviest tests" && npm run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm run test-books && npm run test-book-components-build && npm run test-package-generation && npm run test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

"Note: Running from most lightweight to heaviest tests" 

> promptbook@0.114.0-38 test-name-discrepancies
> ts-node ./scripts/find-name-discrepancies/find-name-discrepancies.ts

🔎🩹 Find name discrepancies
C:/Users/me/work/ai/promptbook/src/book-2.0/agent-source/explicitFromCommitment.ts <- File name not matching any of exported names
Found 1 files with name discrepancies

Review the files listed above:
1) Rename the entity in the file according to the file name
2) Rename file to match the entity name
3) Add Note: [💞] Ignore a discrepancy between file name and entity name

↗ Then commit with message:
Fix name discrepancies

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..3c70ba457e2ceb5ee969562d00d07d6df866f92e
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..2a71bdc2b93ce0addc6132265aba74caa02dbbeb
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,189 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(PRIMARY_AGENT_PATH, spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `));
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook('agents/coding/.core/adam.book', spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `));
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook('agents/coding/nested/not-the-agent-name.book', spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `));
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(filePath, spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `));
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(new Response(spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `), { headers: { 'content-type': 'text/plain' } }));
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain('Remote workshop guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({ code: 'ENOENT' });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook('agents/coding/mentors/pavol.book', spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `));
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(async (url: string) => new Response(
+            url.endsWith('/parent.book')
+                ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+        )) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('reports missing %s names without silently dropping instructions', async (commitment) => {
+        await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+    });
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `));
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..403acb29fc0da146cd18511a0567a72aa44d0aa5
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -40,6 +41,8 @@
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +104,17 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9e5da08ed1da02fd7da37ff18bc832b6f717fb09
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,200 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(
+        private readonly agentDirectoryPath: string,
+        private readonly currentWorkingDirectory: string,
+    ) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+        if (!this.booksByName.has(normalizeAgentName('Adam'))) {
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+            }
+            await this.readBook(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        }
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(spaceTrim((block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `));
+        }
+
+        throw new NotFoundError(spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `));
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath = isRelativeToBook && declaringBook.filePath
+            ? dirname(declaringBook.filePath)
+            : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(canonicalFilePath).href);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`));
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5082663208993b60dcb2608f185d4da819d5a621
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,116 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.findByName('Adam').url;
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(book: LocalAgentBook, inheritancePath: ReadonlyArray<string> = []): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(spaceTrim(`Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${book.filePath || book.url}\`.`));
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) => resolveBook(
+                await collection.getBook(url),
+                context.importAgentOptions.inheritancePath,
+            ),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim() ? `${commitmentType} ${content}\n` : commitmentType === 'FROM' ? 'FROM @Null\n' : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

apply patch
patch: completed
C:\Users\me\work\ai\promptbook\apps\coder-landing\specs\sections\agent-book.md
C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\explicitFromCommitment.ts
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..2a71bdc2b93ce0addc6132265aba74caa02dbbeb
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,189 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(PRIMARY_AGENT_PATH, spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `));
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook('agents/coding/.core/adam.book', spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `));
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook('agents/coding/nested/not-the-agent-name.book', spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `));
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(filePath, spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `));
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(new Response(spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `), { headers: { 'content-type': 'text/plain' } }));
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain('Remote workshop guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({ code: 'ENOENT' });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook('agents/coding/mentors/pavol.book', spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `));
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(async (url: string) => new Response(
+            url.endsWith('/parent.book')
+                ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+        )) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('reports missing %s names without silently dropping instructions', async (commitment) => {
+        await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+    });
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `));
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..403acb29fc0da146cd18511a0567a72aa44d0aa5
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -40,6 +41,8 @@
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +104,17 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9e5da08ed1da02fd7da37ff18bc832b6f717fb09
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,200 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(
+        private readonly agentDirectoryPath: string,
+        private readonly currentWorkingDirectory: string,
+    ) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+        if (!this.booksByName.has(normalizeAgentName('Adam'))) {
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+            }
+            await this.readBook(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        }
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(spaceTrim((block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `));
+        }
+
+        throw new NotFoundError(spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `));
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath = isRelativeToBook && declaringBook.filePath
+            ? dirname(declaringBook.filePath)
+            : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(canonicalFilePath).href);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`));
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5082663208993b60dcb2608f185d4da819d5a621
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,116 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.findByName('Adam').url;
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(book: LocalAgentBook, inheritancePath: ReadonlyArray<string> = []): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(spaceTrim(`Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${book.filePath || book.url}\`.`));
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) => resolveBook(
+                await collection.getBook(url),
+                context.importAgentOptions.inheritancePath,
+            ),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim() ? `${commitmentType} ${content}\n` : commitmentType === 'FROM' ? 'FROM @Null\n' : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm test *> .tmp/coder-from-npm-test.log; $coderTestExitCode = $LASTEXITCODE; Get-Content .tmp/coder-from-npm-test.log -Tail 65; exit $coderTestExitCode' in C:\Users\me\work\ai\promptbook
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm run test-unit -- --runInBand --silent --runTestsByPath apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts apps/agents-server/src/utils/resolveAgentStateFromSource.test.ts apps/agents-server/src/utils/resolveAgentStateFromSource.storedAgents.test.ts apps/agents-server/src/utils/agentReferenceResolver/createServerAgentReferenceResolver.test.ts apps/agents-server/src/utils/agentReferenceResolver/createAgentInheritanceDiagnostics.test.ts apps/agents-server/src/utils/agentReferenceResolver/bookScopedAgentReferences.test.ts' in C:\Users\me\work\ai\promptbook
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npx tsc --project apps/coder-landing/tsconfig.json --noEmit --pretty false --incremental false' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..2a71bdc2b93ce0addc6132265aba74caa02dbbeb
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,189 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(PRIMARY_AGENT_PATH, spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `));
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook('agents/coding/.core/adam.book', spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `));
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook('agents/coding/nested/not-the-agent-name.book', spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `));
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(filePath, spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `));
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(new Response(spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `), { headers: { 'content-type': 'text/plain' } }));
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain('Remote workshop guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({ code: 'ENOENT' });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook('agents/coding/mentors/pavol.book', spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `));
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(async (url: string) => new Response(
+            url.endsWith('/parent.book')
+                ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+        )) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('reports missing %s names without silently dropping instructions', async (commitment) => {
+        await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+    });
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `));
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..403acb29fc0da146cd18511a0567a72aa44d0aa5
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -40,6 +41,8 @@
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +104,17 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9e5da08ed1da02fd7da37ff18bc832b6f717fb09
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,200 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(
+        private readonly agentDirectoryPath: string,
+        private readonly currentWorkingDirectory: string,
+    ) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+        if (!this.booksByName.has(normalizeAgentName('Adam'))) {
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+            }
+            await this.readBook(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        }
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(spaceTrim((block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `));
+        }
+
+        throw new NotFoundError(spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `));
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath = isRelativeToBook && declaringBook.filePath
+            ? dirname(declaringBook.filePath)
+            : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(canonicalFilePath).href);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`));
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5082663208993b60dcb2608f185d4da819d5a621
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,116 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.findByName('Adam').url;
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(book: LocalAgentBook, inheritancePath: ReadonlyArray<string> = []): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(spaceTrim(`Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${book.filePath || book.url}\`.`));
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) => resolveBook(
+                await collection.getBook(url),
+                context.importAgentOptions.inheritancePath,
+            ),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim() ? `${commitmentType} ${content}\n` : commitmentType === 'FROM' ? 'FROM @Null\n' : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

 succeeded in 14920ms:

> promptbook@0.114.0-38 test-unit
> node -r ./scripts/ignore-kill-eperm.js ./node_modules/jest/bin/jest.js --forceExit --runInBand --silent --runTestsByPath apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts apps/agents-server/src/utils/resolveAgentStateFromSource.test.ts apps/agents-server/src/utils/resolveAgentStateFromSource.storedAgents.test.ts apps/agents-server/src/utils/agentReferenceResolver/createServerAgentReferenceResolver.test.ts apps/agents-server/src/utils/agentReferenceResolver/createAgentInheritanceDiagnostics.test.ts apps/agents-server/src/utils/agentReferenceResolver/bookScopedAgentReferences.test.ts

PASS apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts
PASS apps/agents-server/src/utils/resolveAgentStateFromSource.storedAgents.test.ts
PASS apps/agents-server/src/utils/resolveAgentStateFromSource.test.ts
PASS apps/agents-server/src/utils/agentReferenceResolver/bookScopedAgentReferences.test.ts
PASS apps/agents-server/src/utils/agentReferenceResolver/createAgentInheritanceDiagnostics.test.ts
PASS apps/agents-server/src/utils/agentReferenceResolver/createServerAgentReferenceResolver.test.ts

Test Suites: 6 passed, 6 total
Tests:       44 passed, 44 total
Snapshots:   0 total
Time:        11.464 s, estimated 34 s
Force exiting Jest: Have you considered using `--detectOpenHandles` to detect async operations that kept running after all tests finished?

 succeeded in 21034ms:
 exited 1 in 71366ms:
  19:4    error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  20:69   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  21:3    error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  22:53   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  23:4    error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  24:43   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  25:59   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  26:63   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  27:68   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  28:58   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  29:56   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  30:50   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  31:56   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  32:58   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  33:61   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  34:65   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  35:88   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  36:65   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  37:3    error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  38:1    error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  39:4    error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  40:89   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  41:3    error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  42:117  error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  43:116  error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  44:3    error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  45:53   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  46:4    error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  47:118  error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  48:95   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  49:104  error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  50:114  error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  51:99   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  52:107  error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  53:98   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  54:77   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  55:91   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  56:55   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  57:87   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  58:77   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  59:21   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  60:91   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  61:7    error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  62:1    error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  63:13   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  64:32   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  65:36   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  66:41   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  67:31   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  68:29   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  69:23   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  70:29   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  71:31   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  72:34   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  73:37   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  74:36   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  75:37   error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  76:7    error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  77:2    error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  78:1    error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style
  79:216  error  Expected linebreaks to be 'LF' but found 'CRLF'  linebreak-style

✖ 132 problems (132 errors, 0 warnings)
  132 errors and 0 warnings potentially fixable with the `--fix` option.


apply patch
patch: completed
C:\Users\me\work\ai\promptbook\scripts\run-codex-prompts\common\resolveCoderAgent.references.test.ts
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\common\LocalAgentBookCollection.ts
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\common\resolveLocalAgentSource.ts
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..18d7a8dadba67af5647bc36f61b79eacd5dd9ede
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,223 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({ code: 'ENOENT' });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..403acb29fc0da146cd18511a0567a72aa44d0aa5
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -40,6 +41,8 @@
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +104,17 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b3977a169b1c14e94366d3010b896d82359bfa9d
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,222 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        return this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..18d7a8dadba67af5647bc36f61b79eacd5dd9ede
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,223 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({ code: 'ENOENT' });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..403acb29fc0da146cd18511a0567a72aa44d0aa5
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -40,6 +41,8 @@
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +104,17 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b3977a169b1c14e94366d3010b896d82359bfa9d
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,222 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        return this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

codex
The coder, initialization, and server regression suites pass, along with TypeScript checks for the engine and landing page. Adam is now created only when inheritance needs it, so null/void agents remain standalone. The full `npm test` run reached lint and found line-ending issues in two edited files; I’m fixing those and rerunning it.

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "@'
from pathlib import Path
for name in ['src/cli/cli-commands/coder/ensureCoderDeveloperAgentFile.ts', 'src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts']:
    path = Path(name)
    path.write_text(path.read_text(encoding='utf-8'), encoding='utf-8', newline='\\n')
'@ | python -
npx prettier --write src/cli/cli-commands/common/LocalAgentBookCollection.ts src/cli/cli-commands/common/resolveLocalAgentSource.ts scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts" in C:\Users\me\work\ai\promptbook
 succeeded in 2368ms:
Traceback (most recent call last):
  File "<stdin>", line 4, in <module>
TypeError: write_text() got an unexpected keyword argument 'newline'
src\cli\cli-commands\common\LocalAgentBookCollection.ts 309ms
src\cli\cli-commands\common\resolveLocalAgentSource.ts 31ms
scripts\run-codex-prompts\common\resolveCoderAgent.references.test.ts 40ms

apply patch
patch: completed
C:\Users\me\work\ai\promptbook\scripts\run-codex-prompts\common\resolveCoderAgent.references.test.ts
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..adf6a295e6d2e49270205a9f4a253e6696e1fa6f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,242 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('resolves a teammate profile through the teammate own inheritance chain', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nPERSONA Experienced workshop mentor.');
+        await writeBook('agents/coding/reviewer.book', 'Reviewer\nFROM @Pavol');
+        expect(await compileAgent('TEAM @Reviewer')).toContain('Experienced workshop mentor.');
+    });
+
+    it('accepts IMPORTS and tab-separated references', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nRULE Imported guidance.');
+        expect(await compileAgent('FROM\t@null\nIMPORTS\t{./pavol.book}')).toContain('Imported guidance.');
+    });
+
+    it('reports a missing relative book with the resolved path', async () => {
+        await expect(compileAgent('FROM {./missing.book}')).rejects.toThrow(
+            join(projectPath, 'agents/coding/missing.book'),
+        );
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..403acb29fc0da146cd18511a0567a72aa44d0aa5
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -40,6 +41,8 @@
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +104,17 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b3977a169b1c14e94366d3010b896d82359bfa9d
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,222 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        return this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm test *> .tmp/coder-from-npm-test.log; $coderTestExitCode = $LASTEXITCODE; Get-Content .tmp/coder-from-npm-test.log -Tail 70; exit $coderTestExitCode' in C:\Users\me\work\ai\promptbook
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git diff --check; git status --short; Get-Content .tmp/coder-from-npm-test.log -Tail 8' in C:\Users\me\work\ai\promptbook
 succeeded in 657ms:
warning: in the working copy of 'apps/agents-server/src/utils/agentReferenceResolver/AgentReferenceResolutionIssue.ts', CRLF will be replaced by LF the next time Git touches it
warning: in the working copy of 'apps/agents-server/src/utils/agentReferenceResolver/extractAgentReferenceTokens.ts', CRLF will be replaced by LF the next time Git touches it
warning: in the working copy of 'apps/agents-server/src/utils/explicitFromCommitment.ts', CRLF will be replaced by LF the next time Git touches it
warning: in the working copy of 'changelog/_current-preversion.md', CRLF will be replaced by LF the next time Git touches it
warning: in the working copy of 'src/cli/cli-commands/coder/ensureCoderDeveloperAgentFile.ts', CRLF will be replaced by LF the next time Git touches it
warning: in the working copy of 'src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts', CRLF will be replaced by LF the next time Git touches it
prompts/2026-09-0260-ptbk-coder-working-from.md:46: new blank line at EOF.
 M .vscode/terminals.json
 M apps/agents-server/src/utils/agentReferenceResolver/AgentReferenceResolutionIssue.ts
 M apps/agents-server/src/utils/agentReferenceResolver/extractAgentReferenceTokens.ts
 M apps/agents-server/src/utils/explicitFromCommitment.ts
 M apps/agents-server/src/utils/resolveInheritedAgentSource.ts
 M apps/coder-landing/specs/sections/agent-book.md
 M apps/coder-landing/src/components/sections/AgentsSection.tsx
 M changelog/_current-preversion.md
 M prompts/2026-09-0260-ptbk-coder-working-from.md
 M scripts/generate-packages/generate-packages.ts
 M scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
 M scripts/run-codex-prompts/common/resolveCoderAgent.ts
 M scripts/run-codex-prompts/main/runCodexPrompts.ts
 M src/cli/cli-commands/coder/boilerplateTemplates.test.ts
 M src/cli/cli-commands/coder/ensureCoderDeveloperAgentFile.ts
 M src/cli/cli-commands/coder/init.ts
 M src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
 M src/cli/cli-commands/coder/printInitializationSummary.ts
 M src/cli/cli-commands/coder/run.ts
?? scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
?? scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
?? scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
?? src/book-2.0/agent-source/AgentReferenceResolutionIssue.ts
?? src/book-2.0/agent-source/explicitFromCommitment.ts
?? src/book-2.0/agent-source/extractAgentReferenceTokens.ts
?? src/book-2.0/agent-source/resolveInheritedAgentSource.ts
?? src/cli/cli-commands/common/LocalAgentBookCollection.ts
?? src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
?? src/cli/cli-commands/common/ensureAdamAgentBook.ts
?? src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
?? src/cli/cli-commands/common/resolveLocalAgentSource.ts

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..adf6a295e6d2e49270205a9f4a253e6696e1fa6f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,242 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('resolves a teammate profile through the teammate own inheritance chain', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nPERSONA Experienced workshop mentor.');
+        await writeBook('agents/coding/reviewer.book', 'Reviewer\nFROM @Pavol');
+        expect(await compileAgent('TEAM @Reviewer')).toContain('Experienced workshop mentor.');
+    });
+
+    it('accepts IMPORTS and tab-separated references', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nRULE Imported guidance.');
+        expect(await compileAgent('FROM\t@null\nIMPORTS\t{./pavol.book}')).toContain('Imported guidance.');
+    });
+
+    it('reports a missing relative book with the resolved path', async () => {
+        await expect(compileAgent('FROM {./missing.book}')).rejects.toThrow(
+            join(projectPath, 'agents/coding/missing.book'),
+        );
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..403acb29fc0da146cd18511a0567a72aa44d0aa5
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -40,6 +41,8 @@
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +104,17 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b3977a169b1c14e94366d3010b896d82359bfa9d
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,222 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        return this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "@'
from pathlib import Path
paths = [
    'src/cli/cli-commands/coder/ensureCoderDeveloperAgentFile.ts',
    'src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts',
    'apps/agents-server/src/utils/agentReferenceResolver/AgentReferenceResolutionIssue.ts',
    'apps/agents-server/src/utils/agentReferenceResolver/extractAgentReferenceTokens.ts',
    'apps/agents-server/src/utils/explicitFromCommitment.ts',
    'changelog/_current-preversion.md',
]
for name in paths:
    path = Path(name)
    path.write_bytes(path.read_text(encoding='utf-8').encode('utf-8'))
'@ | python -
Get-Content .tmp/coder-from-npm-test.log -Tail 10" in C:\Users\me\work\ai\promptbook
 succeeded in 405ms:
2082/2086 src/wizard/test/sub/books/test.book.md 1.12ms
2083/2086 src/wizard/test/sub/subsub/books/test.book.md 1.07ms
2084/2086 src/wizard/test/sub/subsub/subsubsub/books/test.book.md 1.09ms
2085/2086 src/wizard/test/sub/subsub/subsubsub/README.md 1.00ms
2086/2086 src/wizard/wizard.ts 6.28ms
CSpell: Files checked: 2086, Issues found: 0 in 0 files.

> promptbook@0.114.0-38 test-lint
> eslint src


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm run test-unit -- --runInBand --silent --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts' in C:\Users\me\work\ai\promptbook
 succeeded in 6549ms:

> promptbook@0.114.0-38 test-unit
> node -r ./scripts/ignore-kill-eperm.js ./node_modules/jest/bin/jest.js --forceExit --runInBand --silent --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts

PASS scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts

Test Suites: 1 passed, 1 total
Tests:       28 passed, 28 total
Snapshots:   0 total
Time:        4.132 s
Force exiting Jest: Have you considered using `--detectOpenHandles` to detect async operations that kept running after all tests finished?

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..adf6a295e6d2e49270205a9f4a253e6696e1fa6f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,242 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('resolves a teammate profile through the teammate own inheritance chain', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nPERSONA Experienced workshop mentor.');
+        await writeBook('agents/coding/reviewer.book', 'Reviewer\nFROM @Pavol');
+        expect(await compileAgent('TEAM @Reviewer')).toContain('Experienced workshop mentor.');
+    });
+
+    it('accepts IMPORTS and tab-separated references', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nRULE Imported guidance.');
+        expect(await compileAgent('FROM\t@null\nIMPORTS\t{./pavol.book}')).toContain('Imported guidance.');
+    });
+
+    it('reports a missing relative book with the resolved path', async () => {
+        await expect(compileAgent('FROM {./missing.book}')).rejects.toThrow(
+            join(projectPath, 'agents/coding/missing.book'),
+        );
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..403acb29fc0da146cd18511a0567a72aa44d0aa5
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -40,6 +41,8 @@
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +104,17 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b3977a169b1c14e94366d3010b896d82359bfa9d
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,222 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        return this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content .tmp/coder-from-npm-test.log -Tail 22; git diff --numstat; Get-Content scripts/generate-packages/generate-packages.ts | Select-Object -Skip 505 -First 5; rg -n 'copyCliRuntimePathToCliPackage|\\.core|isDot|dot:' scripts/generate-packages -g '*.ts' | Select-Object -First 20; Get-Content agents-messages/2026-08-0400-jest-worker-force-exit-crashes-on-windows.message.md -TotalCount 75" in C:\Users\me\work\ai\promptbook
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git diff -- scripts/run-codex-prompts/common/resolveCoderAgent.ts scripts/run-codex-prompts/main/runCodexPrompts.ts src/cli/cli-commands/coder/ensureCoderDeveloperAgentFile.ts src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts; Get-Content apps/agents-server/src/utils/resolveInheritedAgentSource.ts' in C:\Users\me\work\ai\promptbook
 succeeded in 859ms:
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5..403acb29f 100644
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@ import { readFile } from 'fs/promises';
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -40,6 +41,8 @@ export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +104,17 @@ export async function resolveCoderAgent(
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a6009..92ee4968c 100644
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@ import { waitForSkippableWorldTimeDeadline } from '../common/waitForSkippableWor
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@ export async function runCodexPrompts(providedOptions?: RunOptions): Promise<voi
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/cli/cli-commands/coder/ensureCoderDeveloperAgentFile.ts b/src/cli/cli-commands/coder/ensureCoderDeveloperAgentFile.ts
index 9a215ae43..834fa57c7 100644
--- a/src/cli/cli-commands/coder/ensureCoderDeveloperAgentFile.ts
+++ b/src/cli/cli-commands/coder/ensureCoderDeveloperAgentFile.ts
@@ -1,7 +1,6 @@
 import { copyFile, stat } from 'fs/promises';
 import { join } from 'path';
-import { spaceTrim } from 'spacetrim';
-import { NotAllowed } from '../../../errors/NotAllowed';
+import { resolveBundledAgentBookPath } from '../common/resolveBundledAgentBookPath';
 import type { InitializationStatus } from './boilerplateTemplates';
 
 /**
@@ -36,35 +35,10 @@ export async function ensureCoderDeveloperAgentFile(projectPath: string): Promis
         return 'unchanged';
     }
 
-    await copyFile(await resolveDefaultCoderDeveloperAgentFilePath(), absoluteFilePath);
+    await copyFile(await resolveBundledAgentBookPath(DEFAULT_CODER_DEVELOPER_AGENT_SOURCE_FILE_PATH), absoluteFilePath);
     return 'created';
 }
 
-/**
- * Resolves the bundled developer agent from a source checkout or generated CLI package.
- */
-async function resolveDefaultCoderDeveloperAgentFilePath(): Promise<string> {
-    const candidates = [
-        join(__dirname, '..', DEFAULT_CODER_DEVELOPER_AGENT_SOURCE_FILE_PATH),
-        join(__dirname, '..', '..', '..', '..', DEFAULT_CODER_DEVELOPER_AGENT_SOURCE_FILE_PATH),
-    ];
-
-    for (const candidate of candidates) {
-        if (await isExistingFile(candidate)) {
-            return candidate;
-        }
-    }
-
-    throw new NotAllowed(
-        spaceTrim(`
-            Cannot find the bundled Promptbook developer agent.
-
-            Checked:
-            ${candidates.map((candidate) => `- \`${candidate}\``).join('\n')}
-        `),
-    );
-}
-
 /**
  * Checks whether a path exists and is a file.
  */
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b..d26e2e748 100644
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -1,3 +1,5 @@
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
 import type { InitializationStatus } from './boilerplateTemplates';
 import {
     PROMPTS_DIRECTORY_PATH,
@@ -24,6 +26,7 @@ export type CoderInitializationSummary = {
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +49,7 @@ export async function initializeCoderProjectConfiguration(projectPath: string):
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +65,7 @@ export async function initializeCoderProjectConfiguration(projectPath: string):
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
import type { string_book } from '../../../../src/book-2.0/agent-source/string_book';
import {
    resolveInheritedAgentSource as resolveAgentSource,
    type ResolveInheritedAgentSourceOptions as SharedResolveInheritedAgentSourceOptions,
    type AgentSourceImporterContext as SharedAgentSourceImporterContext,
} from '../../../../src/book-2.0/agent-source/resolveInheritedAgentSource';
import type { string_agent_url } from '../../../../src/types/string_agent_url';
import {
    DEFAULT_FEDERATED_AGENT_IMPORT_CONFIGURATION,
    type FederatedAgentImportConfiguration,
} from '../constants/federatedAgentImport';
import { importAgentWithFallback } from './importAgentWithFallback';

/**
 * Context passed to a same-instance importer, including the server's HTTP retry policy.
 */
export type AgentSourceImporterContext = SharedAgentSourceImporterContext & {
    readonly federatedAgentImportConfiguration: FederatedAgentImportConfiguration;
};

/**
 * Imports resolved same-instance sources, returning `null` for URLs owned by another server.
 */
export type AgentSourceImporter = (
    agentUrl: string_agent_url,
    context: AgentSourceImporterContext,
) => Promise<string_book | null>;

/**
 * Server transport options for the shared Book inheritance resolver.
 */
type ResolveInheritedAgentSourceOptions = Omit<SharedResolveInheritedAgentSourceOptions, 'agentSourceImporter'> & {
    readonly federatedAgentImportConfiguration?: FederatedAgentImportConfiguration;
    readonly agentSourceImporter?: AgentSourceImporter;
};

/**
 * Applies the shared inheritance rules using same-instance loading and the server's HTTP fallback.
 */
export function resolveInheritedAgentSource(
    agentSource: string_book,
    options: ResolveInheritedAgentSourceOptions = {},
): Promise<string_book> {
    const federatedAgentImportConfiguration =
        options.federatedAgentImportConfiguration || DEFAULT_FEDERATED_AGENT_IMPORT_CONFIGURATION;

    return resolveAgentSource(agentSource, {
        ...options,
        agentSourceImporter: async (agentUrl, context) => {
            const localSource = await options.agentSourceImporter?.(agentUrl, {
                ...context,
                federatedAgentImportConfiguration,
            });

            return (
                localSource ??
                importAgentWithFallback(agentUrl, context.importAgentOptions, federatedAgentImportConfiguration)
            );
        },
    });
}

 succeeded in 1007ms:
2074/2086 src/utils/validators/url/normalizeDomainForMatching.test.ts 1.44ms
2075/2086 src/utils/validators/url/normalizeDomainForMatching.ts 2.41ms
2076/2086 src/utils/validators/uuid/isValidUuid.test.ts 1.61ms
2077/2086 src/utils/validators/uuid/isValidUuid.ts 1.38ms
2078/2086 src/version.ts 1.71ms
2079/2086 src/wizard/$getCompiledBook.ts 7.80ms
2080/2086 src/wizard/test/books/test.book.md 1.17ms
2081/2086 src/wizard/test/README.md 1.13ms
2082/2086 src/wizard/test/sub/books/test.book.md 1.12ms
2083/2086 src/wizard/test/sub/subsub/books/test.book.md 1.07ms
2084/2086 src/wizard/test/sub/subsub/subsubsub/books/test.book.md 1.09ms
2085/2086 src/wizard/test/sub/subsub/subsubsub/README.md 1.00ms
2086/2086 src/wizard/wizard.ts 6.28ms
CSpell: Files checked: 2086, Issues found: 0 in 0 files.

> promptbook@0.114.0-38 test-lint
> eslint src


> promptbook@0.114.0-38 test-types
> tsc

7	0	.vscode/terminals.json
1	69	apps/agents-server/src/utils/agentReferenceResolver/AgentReferenceResolutionIssue.ts
1	61	apps/agents-server/src/utils/agentReferenceResolver/extractAgentReferenceTokens.ts
1	130	apps/agents-server/src/utils/explicitFromCommitment.ts
32	817	apps/agents-server/src/utils/resolveInheritedAgentSource.ts
2	2	apps/coder-landing/specs/sections/agent-book.md
21	4	apps/coder-landing/src/components/sections/AgentsSection.tsx
10	0	changelog/_current-preversion.md
2	1	prompts/2026-09-0260-ptbk-coder-working-from.md
4	0	scripts/generate-packages/generate-packages.ts
8	2	scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
12	1	scripts/run-codex-prompts/common/resolveCoderAgent.ts
5	0	scripts/run-codex-prompts/main/runCodexPrompts.ts
8	0	src/cli/cli-commands/coder/boilerplateTemplates.test.ts
2	28	src/cli/cli-commands/coder/ensureCoderDeveloperAgentFile.ts
3	1	src/cli/cli-commands/coder/init.ts
5	0	src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
6	0	src/cli/cli-commands/coder/printInitializationSummary.ts
2	0	src/cli/cli-commands/coder/run.ts
scripts/generate-packages\generate-packages.ts:170:    await copyCliRuntimePathToCliPackage(
scripts/generate-packages\generate-packages.ts:174:    await copyCliRuntimePathToCliPackage(
scripts/generate-packages\generate-packages.ts:175:        './agents/default/.core/adam.book',
scripts/generate-packages\generate-packages.ts:176:        './packages/cli/agents/default/.core/adam.book',
scripts/generate-packages\generate-packages.ts:314:async function copyCliRuntimePathToCliPackage(sourcePath: string, destinationPath: string): Promise<void> {
# `npm run test-unit` intermittently crashes on Windows while Jest force-exits its workers

While verifying the `ptbk` CLI startup optimization, the `test-unit` step of `npm run test-for-ptbk-coder` failed even though every test had passed:

```text
node:events:496
      throw er; // Unhandled 'error' event
      ^

Error: kill EPERM
    at ChildProcess.kill (node:internal/child_process:511:26)
    at ChildProcessWorker.killChild (node_modules/jest-worker/build/workers/ChildProcessWorker.js:411:17)
    at ChildProcessWorker.forceExit (node_modules/jest-worker/build/workers/ChildProcessWorker.js:416:33)
    at Timeout._onTimeout (node_modules/jest-worker/build/base/BaseWorkerPool.js:137:16)
```

A local rerun of the very same command finished with `Test Suites: 659 passed, 659 total`, `Tests: 7 todo, 2810 passed, 2817 total` and exit code `0`, but it still printed:

```text
A worker process has failed to exit gracefully and has been force exited.
```

## Why it happens

`BaseWorkerPool.end()` gives every worker `500 ms` to shut down and then calls `ChildProcessWorker.forceExit()`, which runs `childProcess.kill('SIGTERM')`. On Windows a `kill` that lands on a process which is already terminating fails with `EPERM`, and Node re-emits that as an unhandled `'error'` event on the `ChildProcess`, so the whole Jest run dies with exit code `1` after all the tests already passed.

So there are two independent facts:

1. A worker of this repository regularly needs **more than 500 ms** to exit after the last test - the warning above is printed on healthy runs too.
2. Whenever the force kill happens to race with the worker's own exit on Windows, the run crashes instead of reporting the (successful) results.

This is **not** caused by the CLI startup optimization - none of the packages that were made lazy (`jsdom`, `@openai/agents`, `openai`, `@anthropic-ai/sdk`, `@azure/openai`, `jszip`, `socket.io-client`, `prompts`, `@mozilla/readability`) keeps a Node process alive when it is required, and the optimization only ever *removes* eagerly loaded modules. The `--forceExit` flag together with the `_TODO-0` note in [`package.json`](../package.json) *("now its present to ensure return code 0 when all tests")* was added long before this task, which documents that the slow worker shutdown already existed.

## Resolved on 2026-08-07 (the crash, not the leak)

`test-unit` now runs as `node -r ./scripts/ignore-kill-eperm.js ./node_modules/jest/bin/jest.js --forceExit`, so the run can no longer die on this race. The guard which the Agents Server build already used for the very same Windows race moved from `apps/agents-server/scripts/` to [`scripts/ignore-kill-eperm.js`](../scripts/ignore-kill-eperm.js) and is preloaded into the Jest main process, where `BaseWorkerPool.end()` and its force-kill timer run. It turns the `EPERM` (and `ESRCH`) failure of `ChildProcess.kill` back into the `false` return value which `kill` documents, instead of letting the unhandled `error` event escape a bare `setTimeout` callback. Both kill attempts of `forceExit()` are covered — the `SIGTERM` and the `SIGKILL` which follows it — because both go through the same patched method.

**Fact 2 is therefore fixed and fact 1 is not:** a worker of this repository still regularly needs more than 500 ms to exit, so `A worker process has failed to exit gracefully and has been force exited.` is still printed on a healthy run. The suggested next step below stays open, and `--forceExit` still cannot be dropped as `_TODO-0` asks.

## Suggested next step

Find the handle that keeps the worker alive after the last test - running `npx jest --detectOpenHandles` over the process-spawning and database-backed suites is the cheapest starting point - and release it in the relevant `afterAll`. Once the workers exit on their own, both the warning and this Windows-only crash disappear and `--forceExit` can finally be dropped as `_TODO-0` asks. Switching the pool to `workerThreads: true` in [`jest.config.js`](../jest.config.js) would also remove the `kill EPERM` class of failures, but it changes worker isolation for all 659 suites, so it should be evaluated separately.

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..adf6a295e6d2e49270205a9f4a253e6696e1fa6f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,242 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('resolves a teammate profile through the teammate own inheritance chain', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nPERSONA Experienced workshop mentor.');
+        await writeBook('agents/coding/reviewer.book', 'Reviewer\nFROM @Pavol');
+        expect(await compileAgent('TEAM @Reviewer')).toContain('Experienced workshop mentor.');
+    });
+
+    it('accepts IMPORTS and tab-separated references', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nRULE Imported guidance.');
+        expect(await compileAgent('FROM\t@null\nIMPORTS\t{./pavol.book}')).toContain('Imported guidance.');
+    });
+
+    it('reports a missing relative book with the resolved path', async () => {
+        await expect(compileAgent('FROM {./missing.book}')).rejects.toThrow(
+            join(projectPath, 'agents/coding/missing.book'),
+        );
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..403acb29fc0da146cd18511a0567a72aa44d0aa5
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -40,6 +41,8 @@
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +104,17 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b3977a169b1c14e94366d3010b896d82359bfa9d
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,222 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        return this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

apply patch
patch: completed
C:\Users\me\work\ai\promptbook\scripts\run-codex-prompts\common\resolveCoderAgent.ts
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..adf6a295e6d2e49270205a9f4a253e6696e1fa6f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,242 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('resolves a teammate profile through the teammate own inheritance chain', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nPERSONA Experienced workshop mentor.');
+        await writeBook('agents/coding/reviewer.book', 'Reviewer\nFROM @Pavol');
+        expect(await compileAgent('TEAM @Reviewer')).toContain('Experienced workshop mentor.');
+    });
+
+    it('accepts IMPORTS and tab-separated references', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nRULE Imported guidance.');
+        expect(await compileAgent('FROM\t@null\nIMPORTS\t{./pavol.book}')).toContain('Imported guidance.');
+    });
+
+    it('reports a missing relative book with the resolved path', async () => {
+        await expect(compileAgent('FROM {./missing.book}')).rejects.toThrow(
+            join(projectPath, 'agents/coding/missing.book'),
+        );
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..bfd23665f0abd54e50371c86092c0c8190d681aa
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -36,10 +37,15 @@
  * Agent data resolved from the optional `--agent` book file.
  */
 export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /** Source with inheritance and imports applied, also used for the coder's agent visual. */
+    readonly agentSource: string_book;
+
     /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +107,18 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        agentSource: resolvedSource.agentSource,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b3977a169b1c14e94366d3010b896d82359bfa9d
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,222 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        return this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content .tmp/coder-from-npm-test.log -Tail 25; Get-Content scripts/generate-packages/generate-packages.ts | Select-Object -Skip 307 -First 35; rg -n 'src.*cli|cli-commands|node-only' scripts/generate-packages/assertGeneratedBundlesArePublishSafe.ts scripts/generate-packages -g '*.ts' | Select-Object -First 15; git diff --check -- . ':"'!prompts/2026-09-0260-ptbk-coder-working-from.md'"'" in C:\Users\me\work\ai\promptbook
 succeeded in 969ms:
Skipping the bundler
5️⃣  Postprocess the generated bundle
Skipping postprocessing
6️⃣  Add dependencies for each package
Bundle file ./packages/editable/esm/index.es.js does not exist
Bundle file ./packages/fake-llm/esm/index.es.js does not exist
Bundle file ./packages/google/esm/index.es.js does not exist
Bundle file ./packages/javascript/esm/index.es.js does not exist
Bundle file ./packages/legacy-documents/esm/index.es.js does not exist
Bundle file ./packages/markdown-utils/esm/index.es.js does not exist
Bundle file ./packages/markitdown/esm/index.es.js does not exist
Bundle file ./packages/node/esm/index.es.js does not exist
Bundle file ./packages/ollama/esm/index.es.js does not exist
Bundle file ./packages/openai/esm/index.es.js does not exist
Bundle file ./packages/pdf/esm/index.es.js does not exist
Bundle file ./packages/remote-client/esm/index.es.js does not exist
Bundle file ./packages/remote-server/esm/index.es.js does not exist
Bundle file ./packages/templates/esm/index.es.js does not exist
Bundle file ./packages/types/esm/index.es.js does not exist
Bundle file ./packages/utils/esm/index.es.js does not exist
Bundle file ./packages/vercel/esm/index.es.js does not exist
Bundle file ./packages/website-crawler/esm/index.es.js does not exist
Bundle file ./packages/wizard/esm/index.es.js does not exist
7️⃣  Copy agents-server app to CLI package
Copying ./apps/agents-server to ./packages/cli/apps/agents-server
 * Copies one non-bundled runtime asset into the generated CLI package after removing stale output.
 *
 * @param sourcePath - Path in the monorepo runtime layout
 * @param destinationPath - Equivalent path below `packages/cli`
 * @private internal utility of package generation
 */
async function copyCliRuntimePathToCliPackage(sourcePath: string, destinationPath: string): Promise<void> {
    console.info(`Copying ${sourcePath} to ${destinationPath}`);

    await rm(destinationPath, { recursive: true, force: true });
    await mkdir(dirname(destinationPath), { recursive: true });
    await cp(sourcePath, destinationPath, { recursive: true });
}

/**
 * Copies one runtime path into the generated CLI package after removing stale output.
 *
 * @param sourcePath - Path in the monorepo runtime layout
 * @param destinationPath - Equivalent path below `packages/cli`
 * @private internal utility of package generation
 */
async function copyAgentsServerRuntimePathToCliPackage(sourcePath: string, destinationPath: string): Promise<void> {
    console.info(`Copying ${sourcePath} to ${destinationPath}`);

    await rm(destinationPath, { recursive: true, force: true });
    await mkdir(dirname(destinationPath), { recursive: true });
    await cp(sourcePath, destinationPath, {
        recursive: true,
        filter: (currentSourcePath) => shouldCopyAgentsServerRuntimePath(currentSourcePath, sourcePath),
    });
}

/**
 * Excludes build artifacts, private env files, and duplicate test sources from packaged runtime files.
 *
scripts/generate-packages/assertGeneratedBundlesArePublishSafe.ts:291:                    A) If the code is indeed node-only, fix the consuming code
scripts/generate-packages/assertGeneratedBundlesArePublishSafe.ts:292:                    B) If the code has some node-only parts but is not fully node-only, consider splitting it into separate files and marking only the truly node-only code with [🟢]
scripts/generate-packages/assertGeneratedBundlesArePublishSafe.ts:293:                    C) If the code is accidentally marked as node-only, remove the [🟢] marker from the source code
scripts/generate-packages/assertGeneratedBundlesArePublishSafe.ts:295:                Note: Purpose of this mechanism is to prevent accidental leaks of node-only code into packages that could be imported into browser environments
scripts/generate-packages/assertGeneratedBundlesArePublishSafe.ts:296:                For example, if we accidentally import \`process\` into a package that is not marked as node-only, it could cause runtime errors when someone tries to use that package in a browser environment
scripts/generate-packages\generate-packages.ts:299:    await copyAgentsServerRuntimePathToCliPackage('./src', './packages/cli/src');
scripts/generate-packages\assertGeneratedBundlesArePublishSafe.ts:291:                    A) If the code is indeed node-only, fix the consuming code
scripts/generate-packages\assertGeneratedBundlesArePublishSafe.ts:292:                    B) If the code has some node-only parts but is not fully node-only, consider splitting it into separate files and marking only the truly node-only code with [🟢]
scripts/generate-packages\assertGeneratedBundlesArePublishSafe.ts:293:                    C) If the code is accidentally marked as node-only, remove the [🟢] marker from the source code
scripts/generate-packages\assertGeneratedBundlesArePublishSafe.ts:295:                Note: Purpose of this mechanism is to prevent accidental leaks of node-only code into packages that could be imported into browser environments
scripts/generate-packages\assertGeneratedBundlesArePublishSafe.ts:296:                For example, if we accidentally import \`process\` into a package that is not marked as node-only, it could cause runtime errors when someone tries to use that package in a browser environment

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..adf6a295e6d2e49270205a9f4a253e6696e1fa6f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,242 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('resolves a teammate profile through the teammate own inheritance chain', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nPERSONA Experienced workshop mentor.');
+        await writeBook('agents/coding/reviewer.book', 'Reviewer\nFROM @Pavol');
+        expect(await compileAgent('TEAM @Reviewer')).toContain('Experienced workshop mentor.');
+    });
+
+    it('accepts IMPORTS and tab-separated references', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nRULE Imported guidance.');
+        expect(await compileAgent('FROM\t@null\nIMPORTS\t{./pavol.book}')).toContain('Imported guidance.');
+    });
+
+    it('reports a missing relative book with the resolved path', async () => {
+        await expect(compileAgent('FROM {./missing.book}')).rejects.toThrow(
+            join(projectPath, 'agents/coding/missing.book'),
+        );
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..bfd23665f0abd54e50371c86092c0c8190d681aa
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -36,10 +37,15 @@
  * Agent data resolved from the optional `--agent` book file.
  */
 export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /** Source with inheritance and imports applied, also used for the coder's agent visual. */
+    readonly agentSource: string_book;
+
     /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +107,18 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        agentSource: resolvedSource.agentSource,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b3977a169b1c14e94366d3010b896d82359bfa9d
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,222 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        return this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content .tmp/coder-from-npm-test.log -Tail 24; git status --short | Select-Object -Last 32; Get-Content src/book-2.0/agent-source/resolveInheritedAgentSource.ts | Select-Object -Skip 148 -First 61; Get-Content src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts' in C:\Users\me\work\ai\promptbook
 succeeded in 1097ms:
Bundle file ./packages/remote-server/esm/index.es.js does not exist
Bundle file ./packages/templates/esm/index.es.js does not exist
Bundle file ./packages/types/esm/index.es.js does not exist
Bundle file ./packages/utils/esm/index.es.js does not exist
Bundle file ./packages/vercel/esm/index.es.js does not exist
Bundle file ./packages/website-crawler/esm/index.es.js does not exist
Bundle file ./packages/wizard/esm/index.es.js does not exist
7️⃣  Copy agents-server app to CLI package
Copying ./apps/agents-server to ./packages/cli/apps/agents-server
Copying ./apps/_common to ./packages/cli/apps/_common
Copying ./src to ./packages/cli/src
Copying ./books to ./packages/cli/books
Copying ./servers.ts to ./packages/cli/servers.ts
Copying ./security.config.ts to ./packages/cli/security.config.ts
Agents-server app copied successfully
Copying ./agents/default/developer.book to ./packages/cli/agents/default/developer.book
Copying ./agents/default/.core/adam.book to ./packages/cli/agents/default/.core/adam.book
8️⃣  Test that nothing what should not be published is published
Skipping the bundler, skipping the tests for bundle content
9️⃣  Make publishing instructions for Github Actions

> promptbook@0.114.0-38 test-unit
> node -r ./scripts/ignore-kill-eperm.js ./node_modules/jest/bin/jest.js --forceExit

 M .vscode/terminals.json
 M apps/agents-server/src/utils/agentReferenceResolver/AgentReferenceResolutionIssue.ts
 M apps/agents-server/src/utils/agentReferenceResolver/extractAgentReferenceTokens.ts
 M apps/agents-server/src/utils/explicitFromCommitment.ts
 M apps/agents-server/src/utils/resolveInheritedAgentSource.ts
 M apps/coder-landing/specs/sections/agent-book.md
 M apps/coder-landing/src/components/sections/AgentsSection.tsx
 M changelog/_current-preversion.md
 M prompts/2026-09-0260-ptbk-coder-working-from.md
 M scripts/generate-packages/generate-packages.ts
 M scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
 M scripts/run-codex-prompts/common/resolveCoderAgent.ts
 M scripts/run-codex-prompts/main/runCodexPrompts.ts
 M src/cli/cli-commands/coder/boilerplateTemplates.test.ts
 M src/cli/cli-commands/coder/ensureCoderDeveloperAgentFile.ts
 M src/cli/cli-commands/coder/init.ts
 M src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
 M src/cli/cli-commands/coder/printInitializationSummary.ts
 M src/cli/cli-commands/coder/run.ts
?? scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
?? scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
?? scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
?? src/book-2.0/agent-source/AgentReferenceResolutionIssue.ts
?? src/book-2.0/agent-source/explicitFromCommitment.ts
?? src/book-2.0/agent-source/extractAgentReferenceTokens.ts
?? src/book-2.0/agent-source/resolveInheritedAgentSource.ts
?? src/cli/cli-commands/common/LocalAgentBookCollection.ts
?? src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
?? src/cli/cli-commands/common/ensureAdamAgentBook.ts
?? src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
?? src/cli/cli-commands/common/resolveLocalAgentSource.ts
 * Shared options for resolving one agent source with inheritance and imports applied.
 *
 * @private internal utility of agent source resolution
 */
export type ResolveInheritedAgentSourceOptions = {
    /** Current recursion depth, forwarded to the source importer. */
    readonly recursionLevel?: number;
    /**
     * The URL of the Adam agent to use as the default ancestor
     *
     * @default 'https://core.ptbk.io/agents/adam'
     */
    readonly adamAgentUrl?: string_agent_url;
    /**
     * Custom resolver used to expand compact agent references.
     */
    readonly agentReferenceResolver?: AgentReferenceResolver;
    /**
     * Canonical URL of the currently resolved agent.
     */
    readonly currentAgentUrl?: string_agent_url;
    /**
     * Additional equivalent URLs that should be treated as the current agent while detecting cycles.
     */
    readonly currentAgentAliases?: ReadonlyArray<string_agent_url>;
    /**
     * Already visited agent URLs in the current resolution stack.
     */
    readonly inheritancePath?: ReadonlyArray<string_agent_url>;
    /**
     * Source importer supplied by the host application.
     */
    readonly agentSourceImporter: AgentSourceImporter;
};

/**
 * Context passed to a custom agent source importer.
 *
 * @private internal utility of agent source resolution
 */
export type AgentSourceImporterContext = {
    /**
     * Commitment that requested the imported source.
     */
    readonly commitmentType: 'FROM' | 'IMPORT';

    /**
     * Import options propagated from the current resolution pass.
     */
    readonly importAgentOptions: {
        readonly recursionLevel?: number;
        readonly inheritancePath?: ReadonlyArray<string_agent_url>;
    };
};

/**
 * Loads and recursively resolves one referenced agent using the caller's transport.
 *
 * @private internal utility of agent source resolution
 */
export type AgentSourceImporter = (
import { spaceTrim } from 'spacetrim';
import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
import {
    createPseudoAgentUrl,
    isPseudoAgentAllowedInCommitment,
    isPseudoAgentUrl,
    resolvePseudoAgentKindFromReference,
    VOID_PSEUDO_AGENT_REFERENCE,
} from '../../../book-2.0/agent-source/pseudoAgentReferences';
import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
import { ParseError } from '../../../errors/ParseError';
import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';

/**
 * Creates a resolver scoped to the book which declares each reference.
 *
 * @private internal utility of CLI agent resolution
 */
export function createLocalAgentReferenceResolver(
    collection: LocalAgentBookCollection,
    declaringBook: LocalAgentBook,
): AgentReferenceResolver {
    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
        if (!pseudoAgentKind) {
            return collection.resolveReference(reference, declaringBook);
        }
        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
        }
        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
            return VOID_PSEUDO_AGENT_REFERENCE;
        }
        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
            return '';
        }
        return createPseudoAgentUrl(pseudoAgentKind);
    }

    return {
        resolveTeammateProfile: async (url) =>
            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
        resolveCommitmentContent: async (commitmentType, content) => {
            const tokens = extractAgentReferenceTokens(content);
            if (tokens.length === 0) {
                if (commitmentType !== 'TEAM' && content.trim()) {
                    return resolveReference(commitmentType, content.trim());
                }
                return content;
            }

            const parts: string[] = [];
            let previousEnd = 0;
            for (const token of tokens) {
                parts.push(content.slice(previousEnd, token.index));
                parts.push(await resolveReference(commitmentType, token.reference));
                previousEnd = token.index + token.length;
            }
            parts.push(content.slice(previousEnd));
            return parts.join('');
        },
    };
}

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..adf6a295e6d2e49270205a9f4a253e6696e1fa6f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,242 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('resolves a teammate profile through the teammate own inheritance chain', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nPERSONA Experienced workshop mentor.');
+        await writeBook('agents/coding/reviewer.book', 'Reviewer\nFROM @Pavol');
+        expect(await compileAgent('TEAM @Reviewer')).toContain('Experienced workshop mentor.');
+    });
+
+    it('accepts IMPORTS and tab-separated references', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nRULE Imported guidance.');
+        expect(await compileAgent('FROM\t@null\nIMPORTS\t{./pavol.book}')).toContain('Imported guidance.');
+    });
+
+    it('reports a missing relative book with the resolved path', async () => {
+        await expect(compileAgent('FROM {./missing.book}')).rejects.toThrow(
+            join(projectPath, 'agents/coding/missing.book'),
+        );
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..bfd23665f0abd54e50371c86092c0c8190d681aa
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -36,10 +37,15 @@
  * Agent data resolved from the optional `--agent` book file.
  */
 export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /** Source with inheritance and imports applied, also used for the coder's agent visual. */
+    readonly agentSource: string_book;
+
     /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +107,18 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        agentSource: resolvedSource.agentSource,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b3977a169b1c14e94366d3010b896d82359bfa9d
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,222 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        return this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

codex
The full check has passed spelling, lint, TypeScript, and package generation. The generated CLI package includes Adam’s bundled book, and the complete unit suite is running now. The focused coder tests also confirm that teammate profiles resolve through their own inheritance chains.

apply patch
patch: completed
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\common\LocalAgentBookCollection.ts
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..adf6a295e6d2e49270205a9f4a253e6696e1fa6f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,242 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('resolves a teammate profile through the teammate own inheritance chain', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nPERSONA Experienced workshop mentor.');
+        await writeBook('agents/coding/reviewer.book', 'Reviewer\nFROM @Pavol');
+        expect(await compileAgent('TEAM @Reviewer')).toContain('Experienced workshop mentor.');
+    });
+
+    it('accepts IMPORTS and tab-separated references', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nRULE Imported guidance.');
+        expect(await compileAgent('FROM\t@null\nIMPORTS\t{./pavol.book}')).toContain('Imported guidance.');
+    });
+
+    it('reports a missing relative book with the resolved path', async () => {
+        await expect(compileAgent('FROM {./missing.book}')).rejects.toThrow(
+            join(projectPath, 'agents/coding/missing.book'),
+        );
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..bfd23665f0abd54e50371c86092c0c8190d681aa
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -36,10 +37,15 @@
  * Agent data resolved from the optional `--agent` book file.
  */
 export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /** Source with inheritance and imports applied, also used for the coder's agent visual. */
+    readonly agentSource: string_book;
+
     /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +107,18 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        agentSource: resolvedSource.agentSource,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..81438ea5941d748c69769cb01be8d23ecf8c6591
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,225 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Stable default ancestor selected from the initial repository scan. */
+    private adamAgentUrl: string | undefined;
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        this.adamAgentUrl ??= this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        return this.adamAgentUrl;
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content .tmp/coder-from-npm-test.log -Tail 20; Get-Item packages/cli/agents/default/.core/adam.book | Select-Object Length; rg -n 'FAIL|Test Suites:|Tests:|error TS|Error:' .tmp/coder-from-npm-test.log | Select-Object -Last 15" in C:\Users\me\work\ai\promptbook
 exited 1 in 891ms:
    console.info
      runner stdout

      at printLiveScriptChunk (scripts/run-codex-prompts/common/runGoScript/printLiveScriptChunk.ts:21:13)

    console.warn
      test failure

      13 |     if (source === 'stderr') {
      14 |         if (chunk.trim()) {
    > 15 |             console.warn(chunk);
         |                     ^
      16 |         }
      17 |
      18 |         return;

      at printLiveScriptChunk (scripts/run-codex-prompts/common/runGoScript/printLiveScriptChunk.ts:15:21)
      at Socket.<anonymous> (scripts/run-codex-prompts/common/runGoScript/runBashScriptWithOutput.ts:72:33)

PASS apps/agents-server/src/utils/bookLanguageDocumentation/createBookLanguageDocumentationPdfResponse.test.ts (29.051 s)

Length
------
   606


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..adf6a295e6d2e49270205a9f4a253e6696e1fa6f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,242 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('resolves a teammate profile through the teammate own inheritance chain', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nPERSONA Experienced workshop mentor.');
+        await writeBook('agents/coding/reviewer.book', 'Reviewer\nFROM @Pavol');
+        expect(await compileAgent('TEAM @Reviewer')).toContain('Experienced workshop mentor.');
+    });
+
+    it('accepts IMPORTS and tab-separated references', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nRULE Imported guidance.');
+        expect(await compileAgent('FROM\t@null\nIMPORTS\t{./pavol.book}')).toContain('Imported guidance.');
+    });
+
+    it('reports a missing relative book with the resolved path', async () => {
+        await expect(compileAgent('FROM {./missing.book}')).rejects.toThrow(
+            join(projectPath, 'agents/coding/missing.book'),
+        );
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..bfd23665f0abd54e50371c86092c0c8190d681aa
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -36,10 +37,15 @@
  * Agent data resolved from the optional `--agent` book file.
  */
 export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /** Source with inheritance and imports applied, also used for the coder's agent visual. */
+    readonly agentSource: string_book;
+
     /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +107,18 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        agentSource: resolvedSource.agentSource,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..81438ea5941d748c69769cb01be8d23ecf8c6591
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,225 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Stable default ancestor selected from the initial repository scan. */
+    private adamAgentUrl: string | undefined;
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        this.adamAgentUrl ??= this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        return this.adamAgentUrl;
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Looks up an indexed profile without changing the source-resolution stack. */
+    public getProfile(url: string): TeammateProfile | null {
+        return this.booksByUrl.get(url)?.profile || null;
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fb2564da44d746a32cc5d27ad0210522b5282c41
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,65 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    isPseudoAgentUrl,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveTeammateProfile: async (url) =>
+            isPseudoAgentUrl(url) ? null : collection.getProfile(url) || (await collection.getBook(url)).profile,
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

apply patch
patch: completed
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\common\LocalAgentBookCollection.ts
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\common\createLocalAgentReferenceResolver.ts
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..adf6a295e6d2e49270205a9f4a253e6696e1fa6f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,242 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('resolves a teammate profile through the teammate own inheritance chain', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nPERSONA Experienced workshop mentor.');
+        await writeBook('agents/coding/reviewer.book', 'Reviewer\nFROM @Pavol');
+        expect(await compileAgent('TEAM @Reviewer')).toContain('Experienced workshop mentor.');
+    });
+
+    it('accepts IMPORTS and tab-separated references', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nRULE Imported guidance.');
+        expect(await compileAgent('FROM\t@null\nIMPORTS\t{./pavol.book}')).toContain('Imported guidance.');
+    });
+
+    it('reports a missing relative book with the resolved path', async () => {
+        await expect(compileAgent('FROM {./missing.book}')).rejects.toThrow(
+            join(projectPath, 'agents/coding/missing.book'),
+        );
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..bfd23665f0abd54e50371c86092c0c8190d681aa
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -36,10 +37,15 @@
  * Agent data resolved from the optional `--agent` book file.
  */
 export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /** Source with inheritance and imports applied, also used for the coder's agent visual. */
+    readonly agentSource: string_book;
+
     /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +107,18 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        agentSource: resolvedSource.agentSource,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5cd4508af0cfc397b63abd1dcaeaa9c3e406ced0
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,220 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Stable default ancestor selected from the initial repository scan. */
+    private adamAgentUrl: string | undefined;
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        this.adamAgentUrl ??= this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        return this.adamAgentUrl;
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f948435f67250dad69bbdbeec07d7e54f023bf9
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,62 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content .tmp/coder-from-npm-test.log -Tail 12; rg -c '"'^PASS '"' .tmp/coder-from-npm-test.log; Get-Content src/cli/cli-commands/common/resolveLocalAgentSource.ts -TotalCount 100" in C:\Users\me\work\ai\promptbook
 succeeded in 690ms:

    console.info
      🚀 Script evaluated successfully, result:   
      DIVAD02_OLLE_H_DIVAD02_OLLE_H_DIVAD02_OLLE_H_EGASSEM_ECIRHT_TAHT_YA_S_3_DIVAD02_OLLE_H_DIVAD02_OLLE_H_EGASSEM_ECIWT_TAHT_YA_S_2_DIVAD02_OLLE_H_EGASSEM_NIAGA_TAHT_YA_S_1_DIVAD_OLLE_H_DIAS_UO_Y

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

PASS src/cli/test/ptbk.test.ts (103.851 s)
PASS apps/agents-server/src/utils/errorReporting/sendApplicationErrorReportToSentry.test.ts (17.604 s)
PASS scripts/run-codex-prompts/common/runGoScript/$spawnLoggedBashScript.test.ts (10.663 s)
PASS scripts/run-codex-prompts/git/coderCommitScope.test.ts (8.083 s)
7
import { dirname } from 'path';
import { spaceTrim } from 'spacetrim';
import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
import type { string_book } from '../../../book-2.0/agent-source/string_book';
import { DEFAULT_MAX_RECURSION } from '../../../config';
import { ParseError } from '../../../errors/ParseError';
import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';

/**
 * Resolved source together with the profile resolver used when compiling TEAM commitments.
 *
 * @private internal type of CLI agent resolution
 */
export type ResolvedLocalAgentSource = {
    readonly agentSource: string_book;
    readonly agentReferenceResolver: AgentReferenceResolver;
    /** Newly created books which the coder may include in its initialization commit. */
    readonly createdAgentBookPaths: ReadonlyArray<string>;
};

/**
 * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
 *
 * @private internal utility of CLI agent resolution
 */
export async function resolveLocalAgentSource(
    agentBookPath: string,
    currentWorkingDirectory: string,
): Promise<ResolvedLocalAgentSource> {
    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
    await collection.initialize();
    const primaryBook = await collection.readBook(agentBookPath);
    const adamAgentUrl = collection.getAdamAgentUrl();

    /** Resolves each dependency with its own location and the current inheritance stack. */
    async function resolveBook(
        book: LocalAgentBook,
        inheritancePath: ReadonlyArray<string> = [],
    ): Promise<string_book> {
        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
            throw new ParseError(
                spaceTrim(
                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
                        book.filePath || book.url
                    }\`.`,
                ),
            );
        }
        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
        return resolveInheritedAgentSource(source, {
            adamAgentUrl,
            currentAgentUrl: book.url,
            inheritancePath,
            agentSourceImporter: async (url, context) =>
                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
        });
    }

    return {
        agentSource: await resolveBook(primaryBook),
        agentReferenceResolver: {
            ...createLocalAgentReferenceResolver(collection, primaryBook),
            resolveTeammateProfile: async (url) => {
                if (isPseudoAgentUrl(url)) {
                    return null;
                }
                const book = await collection.getBook(url);
                const parsedSource = parseAgentSource(await resolveBook(book));
                return {
                    agentName: book.profile.agentName,
                    personaDescription: parsedSource.personaDescription,
                };
            },
        },
        createdAgentBookPaths: collection.createdAgentBookPaths,
    };
}

/**
 * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
 * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
 */
async function resolveBookCommitmentReferences(
    source: string_book,
    resolver: AgentReferenceResolver,
): Promise<string_book> {
    const parsedSource = parseAgentSourceWithCommitments(source);
    const lines = source.split(/\r?\n/);
    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
        .map((commitment) => commitment.lineNumber - 1)
        .sort((left, right) => left - right);
    const effectiveFrom = getEffectiveExplicitFromCommitment(source);


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..adf6a295e6d2e49270205a9f4a253e6696e1fa6f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,242 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('resolves a teammate profile through the teammate own inheritance chain', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nPERSONA Experienced workshop mentor.');
+        await writeBook('agents/coding/reviewer.book', 'Reviewer\nFROM @Pavol');
+        expect(await compileAgent('TEAM @Reviewer')).toContain('Experienced workshop mentor.');
+    });
+
+    it('accepts IMPORTS and tab-separated references', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nRULE Imported guidance.');
+        expect(await compileAgent('FROM\t@null\nIMPORTS\t{./pavol.book}')).toContain('Imported guidance.');
+    });
+
+    it('reports a missing relative book with the resolved path', async () => {
+        await expect(compileAgent('FROM {./missing.book}')).rejects.toThrow(
+            join(projectPath, 'agents/coding/missing.book'),
+        );
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..bfd23665f0abd54e50371c86092c0c8190d681aa
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -36,10 +37,15 @@
  * Agent data resolved from the optional `--agent` book file.
  */
 export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /** Source with inheritance and imports applied, also used for the coder's agent visual. */
+    readonly agentSource: string_book;
+
     /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +107,18 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        agentSource: resolvedSource.agentSource,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5cd4508af0cfc397b63abd1dcaeaa9c3e406ced0
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,220 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Stable default ancestor selected from the initial repository scan. */
+    private adamAgentUrl: string | undefined;
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        this.adamAgentUrl ??= this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        return this.adamAgentUrl;
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f948435f67250dad69bbdbeec07d7e54f023bf9
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,62 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content .tmp/coder-from-npm-test.log -Tail 12; rg -c '"'^PASS '"' .tmp/coder-from-npm-test.log; rg -n '"'^FAIL|''^Test Suites:|''^Tests:'"' .tmp/coder-from-npm-test.log" in C:\Users\me\work\ai\promptbook
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm run test-unit -- --runInBand --silent --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts scripts/run-codex-prompts/common/resolveCoderAgent.test.ts' in C:\Users\me\work\ai\promptbook
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npx eslint src/cli/cli-commands/common/LocalAgentBookCollection.ts src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts src/cli/cli-commands/common/resolveLocalAgentSource.ts; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm run test-types -- --pretty false' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..adf6a295e6d2e49270205a9f4a253e6696e1fa6f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,242 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('resolves a teammate profile through the teammate own inheritance chain', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nPERSONA Experienced workshop mentor.');
+        await writeBook('agents/coding/reviewer.book', 'Reviewer\nFROM @Pavol');
+        expect(await compileAgent('TEAM @Reviewer')).toContain('Experienced workshop mentor.');
+    });
+
+    it('accepts IMPORTS and tab-separated references', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nRULE Imported guidance.');
+        expect(await compileAgent('FROM\t@null\nIMPORTS\t{./pavol.book}')).toContain('Imported guidance.');
+    });
+
+    it('reports a missing relative book with the resolved path', async () => {
+        await expect(compileAgent('FROM {./missing.book}')).rejects.toThrow(
+            join(projectPath, 'agents/coding/missing.book'),
+        );
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..bfd23665f0abd54e50371c86092c0c8190d681aa
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -36,10 +37,15 @@
  * Agent data resolved from the optional `--agent` book file.
  */
 export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /** Source with inheritance and imports applied, also used for the coder's agent visual. */
+    readonly agentSource: string_book;
+
     /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +107,18 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        agentSource: resolvedSource.agentSource,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5cd4508af0cfc397b63abd1dcaeaa9c3e406ced0
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,220 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Stable default ancestor selected from the initial repository scan. */
+    private adamAgentUrl: string | undefined;
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        this.adamAgentUrl ??= this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        return this.adamAgentUrl;
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f948435f67250dad69bbdbeec07d7e54f023bf9
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,62 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

 succeeded in 15956ms:

> promptbook@0.114.0-38 test-unit
> node -r ./scripts/ignore-kill-eperm.js ./node_modules/jest/bin/jest.js --forceExit --runInBand --silent --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts scripts/run-codex-prompts/common/resolveCoderAgent.test.ts

PASS scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts (8.65 s)
PASS scripts/run-codex-prompts/common/resolveCoderAgent.test.ts

Test Suites: 2 passed, 2 total
Tests:       32 passed, 32 total
Snapshots:   0 total
Time:        9.475 s
Force exiting Jest: Have you considered using `--detectOpenHandles` to detect async operations that kept running after all tests finished?

 exited 1 in 20262ms:

      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

PASS src/scripting/javascript/utils/extractVariablesFromScript.test.ts
  ● Console

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log



diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..adf6a295e6d2e49270205a9f4a253e6696e1fa6f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,242 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('resolves a teammate profile through the teammate own inheritance chain', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nPERSONA Experienced workshop mentor.');
+        await writeBook('agents/coding/reviewer.book', 'Reviewer\nFROM @Pavol');
+        expect(await compileAgent('TEAM @Reviewer')).toContain('Experienced workshop mentor.');
+    });
+
+    it('accepts IMPORTS and tab-separated references', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nRULE Imported guidance.');
+        expect(await compileAgent('FROM\t@null\nIMPORTS\t{./pavol.book}')).toContain('Imported guidance.');
+    });
+
+    it('reports a missing relative book with the resolved path', async () => {
+        await expect(compileAgent('FROM {./missing.book}')).rejects.toThrow(
+            join(projectPath, 'agents/coding/missing.book'),
+        );
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..bfd23665f0abd54e50371c86092c0c8190d681aa
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -36,10 +37,15 @@
  * Agent data resolved from the optional `--agent` book file.
  */
 export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /** Source with inheritance and imports applied, also used for the coder's agent visual. */
+    readonly agentSource: string_book;
+
     /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +107,18 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        agentSource: resolvedSource.agentSource,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5cd4508af0cfc397b63abd1dcaeaa9c3e406ced0
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,220 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Stable default ancestor selected from the initial repository scan. */
+    private adamAgentUrl: string | undefined;
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        this.adamAgentUrl ??= this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        return this.adamAgentUrl;
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f948435f67250dad69bbdbeec07d7e54f023bf9
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,62 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Item .tmp/coder-from-npm-test.log | Select-Object Length,LastWriteTime; Get-Content src/scripting/javascript/utils/extractVariablesFromJavascript.test.ts -TotalCount 200; Get-Content src/scripting/javascript/utils/extractVariablesFromJavascript.ts -TotalCount 90' in C:\Users\me\work\ai\promptbook
 succeeded in 1514ms:

 Length LastWriteTime
 ------ -------------
1557190 9/17/2026 2:31:07 AM
import { describe, expect, it } from '@jest/globals';
import { extractVariablesFromJavascript } from './extractVariablesFromJavascript';

describe('extractVariablesFromJavascript', () => {
    it('should work in supersimple case without any variables', () => {
        expect([...extractVariablesFromJavascript('')]).toEqual([]);
        expect([...extractVariablesFromJavascript('"Hello"')]).toEqual([]);
        expect([...extractVariablesFromJavascript('const a = 1;')]).toEqual([]);
    });

    it('should parse one variable', () => {
        expect([...extractVariablesFromJavascript('const a = name;')]).toEqual(['name']);
        expect([...extractVariablesFromJavascript('console.log(name);')]).toEqual(['name']);
        expect([...extractVariablesFromJavascript('const a = name; const b = name;')]).toEqual(['name']);
        expect([...extractVariablesFromJavascript('const a = 1; const b = name; const c = name;')]).toEqual(['name']);
    });

    it('should NOT parse custom function', () => {
        expect([...extractVariablesFromJavascript('foo();')]).toEqual([]);
        expect([...extractVariablesFromJavascript('foo(name);')]).toEqual(['name']);
        expect([...extractVariablesFromJavascript('console.log(name);')]).toEqual(['name']);
    });

    it('should parse multiple variables', () => {
        expect([...extractVariablesFromJavascript('console.log(`${greeting} ${name}`);')]).toEqual([
            'greeting',
            'name',
        ]);
    });
});
import { spaceTrim } from 'spacetrim';
import { LOOP_LIMIT } from '../../../config';
import { assertsError } from '../../../errors/assertsError';
import { ParseError } from '../../../errors/ParseError';
import type { string_javascript } from '../../../types/string_markdown';
import type { string_javascript_name } from '../../../types/string_person_fullname';
/**
 * Parses the given script and returns the list of all used variables that are not defined in the script
 *
 * @param script from which to extract the variables
 * @returns the list of variable names
 * @throws {ParseError} if the script is invalid
 *
 * @public exported from `@promptbook/javascript`
 */
export function extractVariablesFromJavascript(script: string_javascript): Set<string_javascript_name> {
    const variables = new Set<string_javascript_name>();

    const originalScript = script;
    script = `(()=>{${script}})()`;

    try {
        for (let i = 0; i < LOOP_LIMIT; i++)
            try {
                eval(script); // <- TODO: Use `JavascriptExecutionTools.execute` here
            } catch (error) {
                if (!(error instanceof ReferenceError)) {
                    throw error;
                }

                /*
                Note: Parsing the error
                      🌟 Most devices:
                      [PipelineUrlError: thing is not defined]

                      🍏 iPhone`s Safari:
                      [PipelineUrlError: Can't find variable: thing]
                */

                let variableName: string | undefined = undefined;

                if (error.message.startsWith(`Can't`)) {
                    // 🍏 Case
                    variableName = error.message.split(' ').pop();
                } else {
                    // 🌟 Case
                    variableName = error.message.split(' ').shift();
                }

                if (variableName === undefined) {
                    throw error;
                }

                if (script.includes(variableName + '(')) {
                    script = `const ${variableName} = ()=>'';` + script;
                } else {
                    variables.add(variableName);
                    script = `const ${variableName} = '';` + script;
                }
            }
    } catch (error) {
        assertsError(error);

        throw new ParseError(
            spaceTrim(
                (block) => `
                    Can not extract variables from the script
                    ${block((error as Error).stack || (error as Error).message)}

                    Found variables:
                    ${Array.from(variables)
                        .map((variableName, i) => `${i + 1}) ${variableName}`)
                        .join('\n')}


                    The script:

                    \`\`\`javascript
                    ${block(originalScript)}
                    \`\`\`
                `,
                // <- TODO: [🚞] Pass from consumer(s) of `extractVariablesFromJavascript`
            ),
        );
    }

    return variables;
}

// TODO: [🔣] Support for multiple languages - python, java,...


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..adf6a295e6d2e49270205a9f4a253e6696e1fa6f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,242 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('resolves a teammate profile through the teammate own inheritance chain', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nPERSONA Experienced workshop mentor.');
+        await writeBook('agents/coding/reviewer.book', 'Reviewer\nFROM @Pavol');
+        expect(await compileAgent('TEAM @Reviewer')).toContain('Experienced workshop mentor.');
+    });
+
+    it('accepts IMPORTS and tab-separated references', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nRULE Imported guidance.');
+        expect(await compileAgent('FROM\t@null\nIMPORTS\t{./pavol.book}')).toContain('Imported guidance.');
+    });
+
+    it('reports a missing relative book with the resolved path', async () => {
+        await expect(compileAgent('FROM {./missing.book}')).rejects.toThrow(
+            join(projectPath, 'agents/coding/missing.book'),
+        );
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..bfd23665f0abd54e50371c86092c0c8190d681aa
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -36,10 +37,15 @@
  * Agent data resolved from the optional `--agent` book file.
  */
 export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /** Source with inheritance and imports applied, also used for the coder's agent visual. */
+    readonly agentSource: string_book;
+
     /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +107,18 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        agentSource: resolvedSource.agentSource,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5cd4508af0cfc397b63abd1dcaeaa9c3e406ced0
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,220 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Stable default ancestor selected from the initial repository scan. */
+    private adamAgentUrl: string | undefined;
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        this.adamAgentUrl ??= this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        return this.adamAgentUrl;
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f948435f67250dad69bbdbeec07d7e54f023bf9
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,62 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

 succeeded in 43765ms:

> promptbook@0.114.0-38 test-types
> tsc --pretty false


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "@'
from pathlib import Path
path = Path('.tmp/coder-from-npm-test.log')
with path.open('rb') as log:
    size = path.stat().st_size
    log.seek(max(0, size - 10000))
    tail = log.read(10000).decode('utf-8', errors='replace')
lines = tail.splitlines()
print('\\n'.join(lines[-18:]))
'@ | python -" in C:\Users\me\work\ai\promptbook
 succeeded in 693ms:

      at POST (apps/agents-server/src/app/api/emails/incoming/sendgrid/route.ts:52:17)
      at Object.<anonymous> (apps/agents-server/src/app/api/emails/incoming/sendgrid/route.test.ts:107:26)

PASS src/book-components/Chat/Chat/ChatRatingModal.test.tsx
PASS apps/agents-server/src/app/admin/messages/send-email/actions.test.ts
PASS src/prepare/isPipelinePrepared.test.ts
PASS apps/agents-server/src/utils/agentProjects/agentProjectRuntimeDomains.test.ts
PASS src/book-2.0/agent-source/parseAgentSourceWithCommitments.title.test.ts
PASS apps/agents-server/src/app/api/admin/servers/[serverId]/route.test.ts
PASS apps/agents-server/src/utils/userChat/expiredRunningUserChatJobDiagnostics.test.ts
PASS src/collection/pipeline-collection/pipelineCollectionToJson.test.ts
PASS src/utils/agents/terminalAgentAvatarVisual.test.ts
PASS apps/agents-server/src/components/ChatEnterBehavior/ChatEnterBehaviorPreferencesProvider.test.tsx
PASS apps/agents-server/src/utils/agentProjects/agentProjectsPaths.test.ts
PASS src/llm-providers/_common/register/createLlmToolsFromConfiguration.test.ts
PASS src/book-2.0/agent-source/parseAgentSourceWithCommitments.blocks.test.ts
PASS apps/agents-server/src/components/NewAgentDialog/ManGoNewAgentWizard/config/bookSections.test.ts

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..adf6a295e6d2e49270205a9f4a253e6696e1fa6f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,242 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('resolves a teammate profile through the teammate own inheritance chain', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nPERSONA Experienced workshop mentor.');
+        await writeBook('agents/coding/reviewer.book', 'Reviewer\nFROM @Pavol');
+        expect(await compileAgent('TEAM @Reviewer')).toContain('Experienced workshop mentor.');
+    });
+
+    it('accepts IMPORTS and tab-separated references', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nRULE Imported guidance.');
+        expect(await compileAgent('FROM\t@null\nIMPORTS\t{./pavol.book}')).toContain('Imported guidance.');
+    });
+
+    it('reports a missing relative book with the resolved path', async () => {
+        await expect(compileAgent('FROM {./missing.book}')).rejects.toThrow(
+            join(projectPath, 'agents/coding/missing.book'),
+        );
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..bfd23665f0abd54e50371c86092c0c8190d681aa
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -36,10 +37,15 @@
  * Agent data resolved from the optional `--agent` book file.
  */
 export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /** Source with inheritance and imports applied, also used for the coder's agent visual. */
+    readonly agentSource: string_book;
+
     /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +107,18 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        agentSource: resolvedSource.agentSource,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5cd4508af0cfc397b63abd1dcaeaa9c3e406ced0
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,220 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Stable default ancestor selected from the initial repository scan. */
+    private adamAgentUrl: string | undefined;
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        this.adamAgentUrl ??= this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        return this.adamAgentUrl;
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f948435f67250dad69bbdbeec07d7e54f023bf9
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,62 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

codex
The final focused rerun passed all 32 coder-agent tests, and the landing page typecheck passed. The full suite is still progressing through the repository’s integration tests; no failures have been reported so far.

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "@'
from pathlib import Path
import re
path = Path('.tmp/coder-from-npm-test.log')
size = path.stat().st_size
with path.open('rb') as log:
    data = log.read(size).decode('utf-8', errors='replace')
print('Passed suites so far:', len(re.findall(r'"'^PASS '"', data, re.M)))
print('\\n'.join(re.findall(r'"'^(?:FAIL |Test Suites:|Tests:|Time:|> promptbook@|> agents-server@).*'"', data, re.M)[-12:]))
print('\\n'.join(data.splitlines()[-8:]))
'@ | python -" in C:\Users\me\work\ai\promptbook
 succeeded in 496ms:
Passed suites so far: 765
> promptbook@0.114.0-38 test-name-discrepancies
> promptbook@0.114.0-38 test-spellcheck
> promptbook@0.114.0-38 test-lint
> promptbook@0.114.0-38 test-types
> promptbook@0.114.0-38 test-books
> promptbook@0.114.0-38 test-book-components-build
> promptbook@0.114.0-38 test-package-generation
> promptbook@0.114.0-38 test-unit
Test Suites: 765 passed, 765 total
Tests:       7 todo, 3400 passed, 3407 total
Time:        265.877 s
> promptbook@0.114.0-38 test-app-agents-server

> promptbook@0.114.0-38 test-app-agents-server
> (cd ./apps/agents-server/ && npm run test)


> test
> npm run test-e2e


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..adf6a295e6d2e49270205a9f4a253e6696e1fa6f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,242 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('resolves a teammate profile through the teammate own inheritance chain', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nPERSONA Experienced workshop mentor.');
+        await writeBook('agents/coding/reviewer.book', 'Reviewer\nFROM @Pavol');
+        expect(await compileAgent('TEAM @Reviewer')).toContain('Experienced workshop mentor.');
+    });
+
+    it('accepts IMPORTS and tab-separated references', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nRULE Imported guidance.');
+        expect(await compileAgent('FROM\t@null\nIMPORTS\t{./pavol.book}')).toContain('Imported guidance.');
+    });
+
+    it('reports a missing relative book with the resolved path', async () => {
+        await expect(compileAgent('FROM {./missing.book}')).rejects.toThrow(
+            join(projectPath, 'agents/coding/missing.book'),
+        );
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..bfd23665f0abd54e50371c86092c0c8190d681aa
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -36,10 +37,15 @@
  * Agent data resolved from the optional `--agent` book file.
  */
 export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /** Source with inheritance and imports applied, also used for the coder's agent visual. */
+    readonly agentSource: string_book;
+
     /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +107,18 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        agentSource: resolvedSource.agentSource,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5cd4508af0cfc397b63abd1dcaeaa9c3e406ced0
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,220 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Stable default ancestor selected from the initial repository scan. */
+    private adamAgentUrl: string | undefined;
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        this.adamAgentUrl ??= this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        return this.adamAgentUrl;
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f948435f67250dad69bbdbeec07d7e54f023bf9
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,62 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "node -e \"const p=require('./apps/agents-server/package.json'); process.stdout.write(JSON.stringify(p.scripts,null,2))\"; rg -n 'test-e2e' apps/agents-server/scripts -g '*.ts' -g '*.js' -g '*.json'; git status --short | Select-Object -Last 15" in C:\Users\me\work\ai\promptbook
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "git diff --check -- . ':"'!prompts/2026-09-0260-ptbk-coder-working-from.md'"'; node -e \"JSON.parse(require('fs').readFileSync('.vscode/terminals.json','utf8')); console.log('Terminal presets parse successfully')\"" in C:\Users\me\work\ai\promptbook
 succeeded in 1192ms:
Terminal presets parse successfully

 succeeded in 1811ms:
{
  "generate-reserved-paths": "ts-node --transpile-only ./scripts/generate-reserved-paths/generate-reserved-paths.ts",
  "prebuild": "npm run generate-reserved-paths && node ./scripts/clearNextGeneratedTypes.js && node ./scripts/kill-port.js 4440",
  "predev": "npm run generate-reserved-paths && node ./scripts/kill-port.js 4440",
  "dev": "next dev -p 4440",
  "test": "npm run test-e2e",
  "test-e2e": "node ./scripts/run-e2e-tests.js",
  "pretest-build": "node ./scripts/kill-port.js 4021 4440",
  "test-build": "npm run build -- --no-sentry-upload",
  "pretest-e2e": "node ./scripts/kill-port.js 4021 4440",
  "build": "node ./scripts/build-agents-server.js",
  "build-e2e": "node ./scripts/build-e2e.js",
  "start": "next start -p 4440",
  "lint": "eslint src/ --max-warnings=0 --cache --cache-location ./node_modules/.cache/eslint/agents-server-src.json",
  "postinstall": "node ./scripts/ensure-root-dependencies.js"
} M src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
 M src/cli/cli-commands/coder/printInitializationSummary.ts
 M src/cli/cli-commands/coder/run.ts
?? scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
?? scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
?? scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
?? src/book-2.0/agent-source/AgentReferenceResolutionIssue.ts
?? src/book-2.0/agent-source/explicitFromCommitment.ts
?? src/book-2.0/agent-source/extractAgentReferenceTokens.ts
?? src/book-2.0/agent-source/resolveInheritedAgentSource.ts
?? src/cli/cli-commands/common/LocalAgentBookCollection.ts
?? src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
?? src/cli/cli-commands/common/ensureAdamAgentBook.ts
?? src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
?? src/cli/cli-commands/common/resolveLocalAgentSource.ts

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..adf6a295e6d2e49270205a9f4a253e6696e1fa6f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,242 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('resolves a teammate profile through the teammate own inheritance chain', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nPERSONA Experienced workshop mentor.');
+        await writeBook('agents/coding/reviewer.book', 'Reviewer\nFROM @Pavol');
+        expect(await compileAgent('TEAM @Reviewer')).toContain('Experienced workshop mentor.');
+    });
+
+    it('accepts IMPORTS and tab-separated references', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nRULE Imported guidance.');
+        expect(await compileAgent('FROM\t@null\nIMPORTS\t{./pavol.book}')).toContain('Imported guidance.');
+    });
+
+    it('reports a missing relative book with the resolved path', async () => {
+        await expect(compileAgent('FROM {./missing.book}')).rejects.toThrow(
+            join(projectPath, 'agents/coding/missing.book'),
+        );
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..bfd23665f0abd54e50371c86092c0c8190d681aa
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -36,10 +37,15 @@
  * Agent data resolved from the optional `--agent` book file.
  */
 export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /** Source with inheritance and imports applied, also used for the coder's agent visual. */
+    readonly agentSource: string_book;
+
     /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +107,18 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        agentSource: resolvedSource.agentSource,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5cd4508af0cfc397b63abd1dcaeaa9c3e406ced0
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,220 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Stable default ancestor selected from the initial repository scan. */
+    private adamAgentUrl: string | undefined;
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        this.adamAgentUrl ??= this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        return this.adamAgentUrl;
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f948435f67250dad69bbdbeec07d7e54f023bf9
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,62 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

codex
All 765 unit-test suites passed: 3,400 tests passed, with 7 existing TODOs. The full `npm test` command has moved on to the Agent Server’s end-to-end stage, its last check.

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "@'
from pathlib import Path
path = Path('.tmp/coder-from-npm-test.log')
size = path.stat().st_size
with path.open('rb') as log:
    log.seek(max(0, size - 10000))
    print('\\n'.join(log.read(10000).decode('utf-8', errors='replace').splitlines()[-28:]))
'@ | python -" in C:\Users\me\work\ai\promptbook
 exited 1 in 1026ms:
Traceback (most recent call last):
  File "<stdin>", line 6, in <module>
  File "C:\Python39\lib\encodings\cp1250.py", line 19, in encode
    return codecs.charmap_encode(input,self.errors,encoding_table)[0]
UnicodeEncodeError: 'charmap' codec can't encode character '\u26a0' in position 578: character maps to <undefined>

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..adf6a295e6d2e49270205a9f4a253e6696e1fa6f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,242 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('resolves a teammate profile through the teammate own inheritance chain', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nPERSONA Experienced workshop mentor.');
+        await writeBook('agents/coding/reviewer.book', 'Reviewer\nFROM @Pavol');
+        expect(await compileAgent('TEAM @Reviewer')).toContain('Experienced workshop mentor.');
+    });
+
+    it('accepts IMPORTS and tab-separated references', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nRULE Imported guidance.');
+        expect(await compileAgent('FROM\t@null\nIMPORTS\t{./pavol.book}')).toContain('Imported guidance.');
+    });
+
+    it('reports a missing relative book with the resolved path', async () => {
+        await expect(compileAgent('FROM {./missing.book}')).rejects.toThrow(
+            join(projectPath, 'agents/coding/missing.book'),
+        );
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..bfd23665f0abd54e50371c86092c0c8190d681aa
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -36,10 +37,15 @@
  * Agent data resolved from the optional `--agent` book file.
  */
 export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /** Source with inheritance and imports applied, also used for the coder's agent visual. */
+    readonly agentSource: string_book;
+
     /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +107,18 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        agentSource: resolvedSource.agentSource,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5cd4508af0cfc397b63abd1dcaeaa9c3e406ced0
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,220 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Stable default ancestor selected from the initial repository scan. */
+    private adamAgentUrl: string | undefined;
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        this.adamAgentUrl ??= this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        return this.adamAgentUrl;
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f948435f67250dad69bbdbeec07d7e54f023bf9
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,62 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "@'
from pathlib import Path
import sys
sys.stdout.reconfigure(encoding='utf-8')
path = Path('.tmp/coder-from-npm-test.log')
size = path.stat().st_size
with path.open('rb') as log:
    log.seek(max(0, size - 10000))
    print('\\n'.join(log.read(10000).decode('utf-8', errors='replace').splitlines()[-25:]))
'@ | python -" in C:\Users\me\work\ai\promptbook
 succeeded in 1654ms:


> prebuild
> npm run generate-reserved-paths && node ./scripts/clearNextGeneratedTypes.js && node ./scripts/kill-port.js 4440


> generate-reserved-paths
> ts-node --transpile-only ./scripts/generate-reserved-paths/generate-reserved-paths.ts

Kept C:\Users\me\work\ai\promptbook\apps\agents-server\src\generated\reservedPaths.ts unchanged with 31 reserved paths.
No process is listening on port 4440.

> build
> node ./scripts/build-agents-server.js --no-homepage-prerender --no-sentry-upload --no-lint

 ⚠ Linting is disabled.
   ▲ Next.js 15.4.11
   - Environments: .env
   - Experiments (use with caution):
     ✓ externalDir
     ✓ webpackBuildWorker
     · clientTraceMetadata
     ✓ parallelServerBuildTraces

   Creating an optimized production build ...

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..adf6a295e6d2e49270205a9f4a253e6696e1fa6f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,242 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('resolves a teammate profile through the teammate own inheritance chain', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nPERSONA Experienced workshop mentor.');
+        await writeBook('agents/coding/reviewer.book', 'Reviewer\nFROM @Pavol');
+        expect(await compileAgent('TEAM @Reviewer')).toContain('Experienced workshop mentor.');
+    });
+
+    it('accepts IMPORTS and tab-separated references', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nRULE Imported guidance.');
+        expect(await compileAgent('FROM\t@null\nIMPORTS\t{./pavol.book}')).toContain('Imported guidance.');
+    });
+
+    it('reports a missing relative book with the resolved path', async () => {
+        await expect(compileAgent('FROM {./missing.book}')).rejects.toThrow(
+            join(projectPath, 'agents/coding/missing.book'),
+        );
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..bfd23665f0abd54e50371c86092c0c8190d681aa
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -36,10 +37,15 @@
  * Agent data resolved from the optional `--agent` book file.
  */
 export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /** Source with inheritance and imports applied, also used for the coder's agent visual. */
+    readonly agentSource: string_book;
+
     /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +107,18 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        agentSource: resolvedSource.agentSource,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5cd4508af0cfc397b63abd1dcaeaa9c3e406ced0
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,220 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Stable default ancestor selected from the initial repository scan. */
+    private adamAgentUrl: string | undefined;
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        this.adamAgentUrl ??= this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        return this.adamAgentUrl;
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f948435f67250dad69bbdbeec07d7e54f023bf9
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,62 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "@'
from pathlib import Path
import sys
sys.stdout.reconfigure(encoding='utf-8')
path = Path('.tmp/coder-from-npm-test.log')
size = path.stat().st_size
with path.open('rb') as log:
    log.seek(max(0, size - 10000))
    print('\\n'.join(log.read(10000).decode('utf-8', errors='replace').splitlines()[-22:]))
'@ | python -" in C:\Users\me\work\ai\promptbook
 succeeded in 640ms:
> npm run generate-reserved-paths && node ./scripts/clearNextGeneratedTypes.js && node ./scripts/kill-port.js 4440


> generate-reserved-paths
> ts-node --transpile-only ./scripts/generate-reserved-paths/generate-reserved-paths.ts

Kept C:\Users\me\work\ai\promptbook\apps\agents-server\src\generated\reservedPaths.ts unchanged with 31 reserved paths.
No process is listening on port 4440.

> build
> node ./scripts/build-agents-server.js --no-homepage-prerender --no-sentry-upload --no-lint

 ⚠ Linting is disabled.
   ▲ Next.js 15.4.11
   - Environments: .env
   - Experiments (use with caution):
     ✓ externalDir
     ✓ webpackBuildWorker
     · clientTraceMetadata
     ✓ parallelServerBuildTraces

   Creating an optimized production build ...

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..adf6a295e6d2e49270205a9f4a253e6696e1fa6f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,242 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('resolves a teammate profile through the teammate own inheritance chain', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nPERSONA Experienced workshop mentor.');
+        await writeBook('agents/coding/reviewer.book', 'Reviewer\nFROM @Pavol');
+        expect(await compileAgent('TEAM @Reviewer')).toContain('Experienced workshop mentor.');
+    });
+
+    it('accepts IMPORTS and tab-separated references', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nRULE Imported guidance.');
+        expect(await compileAgent('FROM\t@null\nIMPORTS\t{./pavol.book}')).toContain('Imported guidance.');
+    });
+
+    it('reports a missing relative book with the resolved path', async () => {
+        await expect(compileAgent('FROM {./missing.book}')).rejects.toThrow(
+            join(projectPath, 'agents/coding/missing.book'),
+        );
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..bfd23665f0abd54e50371c86092c0c8190d681aa
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -36,10 +37,15 @@
  * Agent data resolved from the optional `--agent` book file.
  */
 export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /** Source with inheritance and imports applied, also used for the coder's agent visual. */
+    readonly agentSource: string_book;
+
     /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +107,18 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        agentSource: resolvedSource.agentSource,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5cd4508af0cfc397b63abd1dcaeaa9c3e406ced0
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,220 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Stable default ancestor selected from the initial repository scan. */
+    private adamAgentUrl: string | undefined;
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        this.adamAgentUrl ??= this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        return this.adamAgentUrl;
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f948435f67250dad69bbdbeec07d7e54f023bf9
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,62 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

codex
The end-to-end check is building the Agent Server before it can run the browser tests. The source checks and unit tests are complete; I’m waiting for that build to finish.

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..adf6a295e6d2e49270205a9f4a253e6696e1fa6f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,242 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('resolves a teammate profile through the teammate own inheritance chain', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nPERSONA Experienced workshop mentor.');
+        await writeBook('agents/coding/reviewer.book', 'Reviewer\nFROM @Pavol');
+        expect(await compileAgent('TEAM @Reviewer')).toContain('Experienced workshop mentor.');
+    });
+
+    it('accepts IMPORTS and tab-separated references', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nRULE Imported guidance.');
+        expect(await compileAgent('FROM\t@null\nIMPORTS\t{./pavol.book}')).toContain('Imported guidance.');
+    });
+
+    it('reports a missing relative book with the resolved path', async () => {
+        await expect(compileAgent('FROM {./missing.book}')).rejects.toThrow(
+            join(projectPath, 'agents/coding/missing.book'),
+        );
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..bfd23665f0abd54e50371c86092c0c8190d681aa
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -36,10 +37,15 @@
  * Agent data resolved from the optional `--agent` book file.
  */
 export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /** Source with inheritance and imports applied, also used for the coder's agent visual. */
+    readonly agentSource: string_book;
+
     /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +107,18 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        agentSource: resolvedSource.agentSource,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5cd4508af0cfc397b63abd1dcaeaa9c3e406ced0
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,220 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Stable default ancestor selected from the initial repository scan. */
+    private adamAgentUrl: string | undefined;
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        this.adamAgentUrl ??= this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        return this.adamAgentUrl;
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f948435f67250dad69bbdbeec07d7e54f023bf9
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,62 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "@'
from pathlib import Path
import sys
sys.stdout.reconfigure(encoding='utf-8')
path = Path('.tmp/coder-from-npm-test.log')
size = path.stat().st_size
with path.open('rb') as log:
    log.seek(max(0, size - 10000))
    print('\\n'.join(log.read(10000).decode('utf-8', errors='replace').splitlines()[-26:]))
'@ | python -" in C:\Users\me\work\ai\promptbook
 succeeded in 1109ms:
> node ./scripts/build-e2e.js --no-lint


> prebuild
> npm run generate-reserved-paths && node ./scripts/clearNextGeneratedTypes.js && node ./scripts/kill-port.js 4440


> generate-reserved-paths
> ts-node --transpile-only ./scripts/generate-reserved-paths/generate-reserved-paths.ts

Kept C:\Users\me\work\ai\promptbook\apps\agents-server\src\generated\reservedPaths.ts unchanged with 31 reserved paths.
No process is listening on port 4440.

> build
> node ./scripts/build-agents-server.js --no-homepage-prerender --no-sentry-upload --no-lint

 ⚠ Linting is disabled.
   ▲ Next.js 15.4.11
   - Environments: .env
   - Experiments (use with caution):
     ✓ externalDir
     ✓ webpackBuildWorker
     · clientTraceMetadata
     ✓ parallelServerBuildTraces

   Creating an optimized production build ...

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..adf6a295e6d2e49270205a9f4a253e6696e1fa6f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,242 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('resolves a teammate profile through the teammate own inheritance chain', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nPERSONA Experienced workshop mentor.');
+        await writeBook('agents/coding/reviewer.book', 'Reviewer\nFROM @Pavol');
+        expect(await compileAgent('TEAM @Reviewer')).toContain('Experienced workshop mentor.');
+    });
+
+    it('accepts IMPORTS and tab-separated references', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nRULE Imported guidance.');
+        expect(await compileAgent('FROM\t@null\nIMPORTS\t{./pavol.book}')).toContain('Imported guidance.');
+    });
+
+    it('reports a missing relative book with the resolved path', async () => {
+        await expect(compileAgent('FROM {./missing.book}')).rejects.toThrow(
+            join(projectPath, 'agents/coding/missing.book'),
+        );
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..bfd23665f0abd54e50371c86092c0c8190d681aa
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -36,10 +37,15 @@
  * Agent data resolved from the optional `--agent` book file.
  */
 export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /** Source with inheritance and imports applied, also used for the coder's agent visual. */
+    readonly agentSource: string_book;
+
     /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +107,18 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        agentSource: resolvedSource.agentSource,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5cd4508af0cfc397b63abd1dcaeaa9c3e406ced0
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,220 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Stable default ancestor selected from the initial repository scan. */
+    private adamAgentUrl: string | undefined;
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        this.adamAgentUrl ??= this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        return this.adamAgentUrl;
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f948435f67250dad69bbdbeec07d7e54f023bf9
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,62 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n 'timeout|TIMEOUT|retry|spawn|no-lint|build|playwright' apps/agents-server/scripts/run-e2e-tests.js apps/agents-server/scripts/build-e2e.js apps/agents-server/scripts/build-agents-server.js" in C:\Users\me\work\ai\promptbook
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Item .tmp/coder-from-npm-test.log | Select-Object LastWriteTime,Length; Get-CimInstance Win32_Process -Filter \"Name = 'node.exe'\" | Where-Object { "'$_.CommandLine -match '"'build-agents-server|webpack-build-worker|run-e2e|next.+build' } | ForEach-Object { Get-Process -Id "'$_.ProcessId -ErrorAction SilentlyContinue | Select-Object Id,CPU,WorkingSet,StartTime }' in C:\Users\me\work\ai\promptbook
 succeeded in 768ms:
apps/agents-server/scripts/run-e2e-tests.js:15: * depend on the production build. Both run at the same time and the flow waits for the slower one,
apps/agents-server/scripts/run-e2e-tests.js:16: * which is always the build. The build then skips its duplicate lint pass, and Playwright starts
apps/agents-server/scripts/run-e2e-tests.js:17: * that same build instead of compiling a second `.next-e2e` output through `webServer`.
apps/agents-server/scripts/run-e2e-tests.js:25:    const buildStatus = await startNpm(['run', 'build-e2e', '--', '--no-lint'], commandOptions);
apps/agents-server/scripts/run-e2e-tests.js:28:    console.log('\n> lint (ran next to the production build)\n');
apps/agents-server/scripts/run-e2e-tests.js:35:    if (buildStatus !== 0) {
apps/agents-server/scripts/run-e2e-tests.js:36:        process.exit(buildStatus);
apps/agents-server/scripts/run-e2e-tests.js:39:    runNpm(['exec', '--', 'playwright', 'test', ...process.argv.slice(2)], {
apps/agents-server/scripts/build-agents-server.js:3:const { spawnSync } = require('node:child_process');
apps/agents-server/scripts/build-agents-server.js:12: * Root directory of the repository, which owns the shared build and test scripts.
apps/agents-server/scripts/build-agents-server.js:17: * Maximum Node.js heap size used by the Next.js production build.
apps/agents-server/scripts/build-agents-server.js:22: * Runs the shared Agents Server production build and its homepage prerender step.
apps/agents-server/scripts/build-agents-server.js:24: * The optional `--no-lint` argument is used by the E2E runner after the same app
apps/agents-server/scripts/build-agents-server.js:26: * argument is used by builds whose caller starts the very same production server right
apps/agents-server/scripts/build-agents-server.js:28: * Test builds pass `--no-sentry-upload` to avoid publishing source maps and creating
apps/agents-server/scripts/build-agents-server.js:31:function buildAgentsServer() {
apps/agents-server/scripts/build-agents-server.js:36:        'build',
apps/agents-server/scripts/build-agents-server.js:39:    if (process.argv.includes('--no-lint')) {
apps/agents-server/scripts/build-agents-server.js:40:        nextBuildArguments.push('--no-lint');
apps/agents-server/scripts/build-agents-server.js:44:        // Note: Next.js compiles each webpack target in its own build worker process, and a worker
apps/agents-server/scripts/build-agents-server.js:46:        //       the build process which spawned it. The same merge is done for the CLI-owned
apps/agents-server/scripts/build-agents-server.js:47:        //       production build in `src/cli/cli-commands/agents-server/buildAgentsServer/runNextBuild.ts`.
apps/agents-server/scripts/build-agents-server.js:63: * @returns `NODE_OPTIONS` value for the build process.
apps/agents-server/scripts/build-agents-server.js:83:    const result = spawnSync(executablePath, argumentsList, {
apps/agents-server/scripts/build-agents-server.js:102:buildAgentsServer();
apps/agents-server/scripts/build-e2e.js:13: * Production build arguments which every E2E build applies.
apps/agents-server/scripts/build-e2e.js:16: * snapshot which nothing reads afterwards. Every E2E build is started by Playwright right after it
apps/agents-server/scripts/build-e2e.js:28:function buildE2eServer() {
apps/agents-server/scripts/build-e2e.js:29:    const buildArguments = ['run', 'build', '--', ...E2E_BUILD_ARGUMENTS];
apps/agents-server/scripts/build-e2e.js:31:    if (process.argv.includes('--no-lint')) {
apps/agents-server/scripts/build-e2e.js:32:        buildArguments.push('--no-lint');
apps/agents-server/scripts/build-e2e.js:35:    runNpm(buildArguments, {
apps/agents-server/scripts/build-e2e.js:48:buildE2eServer();

 succeeded in 1468ms:

LastWriteTime         Length
-------------         ------
9/17/2026 2:32:27 AM 1761544
                           1
                           1
                           1


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..adf6a295e6d2e49270205a9f4a253e6696e1fa6f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,242 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('resolves a teammate profile through the teammate own inheritance chain', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nPERSONA Experienced workshop mentor.');
+        await writeBook('agents/coding/reviewer.book', 'Reviewer\nFROM @Pavol');
+        expect(await compileAgent('TEAM @Reviewer')).toContain('Experienced workshop mentor.');
+    });
+
+    it('accepts IMPORTS and tab-separated references', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nRULE Imported guidance.');
+        expect(await compileAgent('FROM\t@null\nIMPORTS\t{./pavol.book}')).toContain('Imported guidance.');
+    });
+
+    it('reports a missing relative book with the resolved path', async () => {
+        await expect(compileAgent('FROM {./missing.book}')).rejects.toThrow(
+            join(projectPath, 'agents/coding/missing.book'),
+        );
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..bfd23665f0abd54e50371c86092c0c8190d681aa
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -36,10 +37,15 @@
  * Agent data resolved from the optional `--agent` book file.
  */
 export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /** Source with inheritance and imports applied, also used for the coder's agent visual. */
+    readonly agentSource: string_book;
+
     /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +107,18 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        agentSource: resolvedSource.agentSource,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5cd4508af0cfc397b63abd1dcaeaa9c3e406ced0
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,220 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Stable default ancestor selected from the initial repository scan. */
+    private adamAgentUrl: string | undefined;
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        this.adamAgentUrl ??= this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        return this.adamAgentUrl;
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f948435f67250dad69bbdbeec07d7e54f023bf9
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,62 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-CimInstance Win32_Process -Filter \"Name = 'node.exe'\" | Where-Object { "'$_.CommandLine -match '"'build-agents-server|webpack-build-worker|run-e2e|next.+build' } | ForEach-Object { Get-Process -Id "'$_.ProcessId -ErrorAction SilentlyContinue | Select-Object Id,CPU,WorkingSet,StartTime } | ConvertTo-Json -Compress; Get-Date -Format o' in C:\Users\me\work\ai\promptbook
 succeeded in 2141ms:
[{"Id":11068,"CPU":0.078125,"WorkingSet":43601920,"StartTime":"2026-09-17T02:32:09.1189978+02:00"},{"Id":1192,"CPU":0.03125,"WorkingSet":42864640,"StartTime":"2026-09-17T02:32:12.6725228+02:00"},{"Id":51212,"CPU":5.515625,"WorkingSet":122912768,"StartTime":"2026-09-17T02:32:12.7648438+02:00"}]
2026-09-17T02:36:06.7433926+02:00

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..adf6a295e6d2e49270205a9f4a253e6696e1fa6f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,242 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('resolves a teammate profile through the teammate own inheritance chain', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nPERSONA Experienced workshop mentor.');
+        await writeBook('agents/coding/reviewer.book', 'Reviewer\nFROM @Pavol');
+        expect(await compileAgent('TEAM @Reviewer')).toContain('Experienced workshop mentor.');
+    });
+
+    it('accepts IMPORTS and tab-separated references', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nRULE Imported guidance.');
+        expect(await compileAgent('FROM\t@null\nIMPORTS\t{./pavol.book}')).toContain('Imported guidance.');
+    });
+
+    it('reports a missing relative book with the resolved path', async () => {
+        await expect(compileAgent('FROM {./missing.book}')).rejects.toThrow(
+            join(projectPath, 'agents/coding/missing.book'),
+        );
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..bfd23665f0abd54e50371c86092c0c8190d681aa
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -36,10 +37,15 @@
  * Agent data resolved from the optional `--agent` book file.
  */
 export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /** Source with inheritance and imports applied, also used for the coder's agent visual. */
+    readonly agentSource: string_book;
+
     /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +107,18 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        agentSource: resolvedSource.agentSource,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5cd4508af0cfc397b63abd1dcaeaa9c3e406ced0
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,220 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Stable default ancestor selected from the initial repository scan. */
+    private adamAgentUrl: string | undefined;
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        this.adamAgentUrl ??= this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        return this.adamAgentUrl;
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f948435f67250dad69bbdbeec07d7e54f023bf9
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,62 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

codex
The production build is still running and has not reported an error. The complete unit suite has passed, and the generated CLI package contains the Adam book.

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "@'
from pathlib import Path
import sys
sys.stdout.reconfigure(encoding='utf-8')
path = Path('.tmp/coder-from-npm-test.log')
size = path.stat().st_size
with path.open('rb') as log:
    log.seek(max(0, size - 10000))
    print('\\n'.join(log.read(10000).decode('utf-8', errors='replace').splitlines()[-22:]))
'@ | python -" in C:\Users\me\work\ai\promptbook
 succeeded in 1676ms:
> npm run generate-reserved-paths && node ./scripts/clearNextGeneratedTypes.js && node ./scripts/kill-port.js 4440


> generate-reserved-paths
> ts-node --transpile-only ./scripts/generate-reserved-paths/generate-reserved-paths.ts

Kept C:\Users\me\work\ai\promptbook\apps\agents-server\src\generated\reservedPaths.ts unchanged with 31 reserved paths.
No process is listening on port 4440.

> build
> node ./scripts/build-agents-server.js --no-homepage-prerender --no-sentry-upload --no-lint

 ⚠ Linting is disabled.
   ▲ Next.js 15.4.11
   - Environments: .env
   - Experiments (use with caution):
     ✓ externalDir
     ✓ webpackBuildWorker
     · clientTraceMetadata
     ✓ parallelServerBuildTraces

   Creating an optimized production build ...

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index 61287114ed3b56e31a400e249ee2dce95be17dd1..882b212885eafdc24ad999e643ae615056ef0f7b
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -30,6 +30,13 @@
             "focus": false
         },
         {
+            "name": "🐙🧬 Test ptbk coder FROM, IMPORT and TEAM (💤)",
+            "command": "npm run test-unit -- --runInBand --runTestsByPath scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts src/cli/cli-commands/coder/boilerplateTemplates.test.ts",
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
             "commands": [
                 "set -a && source .env && set +a",
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 72cacc019dfa9b29a1c3eba9d8caeb43e0e53d54..0607fa305e8d8be94a16b6b95886d5c91dc3efe7
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -5,7 +5,7 @@
 ## Copy
 
 -   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
--   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.
+-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in `.book` files. ptbk coder resolves the selected book's inheritance and imports into the system message of every coding prompt.
 
 ## Layout
 
@@ -15,7 +15,7 @@
 
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
-3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+3. Follow-up paragraphs: `ptbk coder init` creates the developer agent at `agents/developer.book` and Adam at `agents/.core/adam.book`. `FROM`, `IMPORT` and `TEAM` resolve `@Name` and `{Name}` by first-line book titles discovered recursively under the selected agent's directory. Paths beginning with `./` or `../` are relative to the declaring book, other paths to cwd, and HTTP(S) book URLs are supported. Missing Adam is created in `.core` beside the selected agent and inherited by default; `FROM @null`, `FROM @void` and their brace forms disable inheritance.
 4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 5. Attribution paragraph: a finished task is signed by the agent, not only by the harness, as ``[x] by Developer on OpenAI Codex `gpt-5.6-luna` ``.
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 10a34fbbf0f76fe7afecd9b228769545efc125ae..e9a11a02c582626b51663581356a2bb4ffaf1dd8
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -23,8 +23,8 @@
                     Book language
                 </a>
                 , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
-                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
-                file into the system message of every coding prompt.
+                knows live in <code className="text-promptbook-blue">.book</code> files. ptbk coder resolves the selected
+                book&apos;s inheritance and imports into the system message of every coding prompt.
             </p>
 
             <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
@@ -36,8 +36,25 @@
                     <TerminalBlock command={AGENT_RUN_COMMAND} />
                     <p className="mt-6 text-gray-400">
                         <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
-                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
-                        other file in your repository to change how your agent codes.
+                        agent at <code className="text-promptbook-blue">agents/developer.book</code> and its shared
+                        ancestor at <code className="text-promptbook-blue">agents/.core/adam.book</code>. Edit these
+                        files to change how your agent codes.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Use <code className="text-promptbook-blue">FROM @Pavol</code> or{' '}
+                        <code className="text-promptbook-blue">{'FROM {Pavol}'}</code> to inherit from another book by
+                        its first-line name. Books are discovered recursively beneath the selected agent&apos;s folder.
+                        <code className="text-promptbook-blue"> IMPORT</code> and{' '}
+                        <code className="text-promptbook-blue">TEAM</code> use the same reference rules.
+                    </p>
+                    <p className="mt-4 text-gray-400">
+                        Paths starting with <code className="text-promptbook-blue">./</code> or{' '}
+                        <code className="text-promptbook-blue">../</code> are relative to the book declaring them;
+                        other paths are relative to your current directory. HTTP and HTTPS book URLs work too.
+                        Adam is inherited by default and created in the selected book&apos;s{' '}
+                        <code className="text-promptbook-blue">.core</code> folder when missing. Use{' '}
+                        <code className="text-promptbook-blue">FROM @null</code> or{' '}
+                        <code className="text-promptbook-blue">FROM @void</code> to inherit from nothing; braces work too.
                     </p>
                     <p className="mt-4 text-gray-400">
                         Route a ready task to that agent with a status line such as{' '}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 592cb2c88167c2fcd12e7a98f46ce9a16096234c..30551b63043b695b9a8d77f7c59d2343af911749
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -4955,3 +4955,11 @@
     -   The generated manifest of every package that is not Node-only now declares `"browser": { "jsdom": false }`, which is the standard way to tell a browser bundler to replace a Node-only dependency with an empty module. Browser builds therefore no longer follow `jsdom` (and no longer ship it), while Node.js ignores the `browser` field and keeps resolving the real library for server-side rendering. The stubs are derived from the dependencies which the generated package really has, so a Node-only dependency which appears in a browser-facing package in the future is covered without another manual change.
     -   Creating the server-side DOM moved from `renderMarkdown` into `$provideServerDomWindow`, which is now the single place where browser-facing code touches `jsdom`. A runtime that can provide neither the DOM of a browser nor `jsdom` is now reported as a branded `EnvironmentMismatchError` explaining why markdown can not be sanitized, instead of failing with `JSDOM is not a constructor`.
     -   The list of Node-only packages is now shared through `isNodeOnlyPackage` between the publish-safety check and the dependency step of the package generation, instead of being written down twice.
+-   `ptbk coder` now resolves `FROM`, `IMPORT`, and `TEAM` references across local agent books before compiling coding
+    instructions. Names in `@Name` and `{Name}` match first-line book titles discovered recursively beneath the selected
+    agent's folder. `./` and `../` paths resolve from the declaring book, other paths from the working directory, and
+    HTTP(S) book URLs work too, including nested inheritance and imports. The coder and Agent Server share the same
+    inheritance implementation. Agents inherit from local Adam by default; a missing Adam is copied from the bundled
+    server book into `.core/adam.book`. `FROM @null`, `FROM {null}`, `FROM @void`, and `FROM {void}` disable inheritance.
+    `ptbk coder init` also creates `agents/.core/adam.book` without overwriting an existing book. Added a reference-resolution
+    test preset in VS Code and documented the reference rules on the coder landing page.
diff --git a/scripts/generate-packages/generate-packages.ts b/scripts/generate-packages/generate-packages.ts
index a0b3aefbf90d417fd77f17dfa773f6ac68e48539..c610b167cebb4c3499b7b9f5a087408c1a337f2c
--- a/scripts/generate-packages/generate-packages.ts
+++ b/scripts/generate-packages/generate-packages.ts
@@ -171,6 +171,10 @@
         './agents/default/developer.book',
         './packages/cli/agents/default/developer.book',
     );
+    await copyCliRuntimePathToCliPackage(
+        './agents/default/.core/adam.book',
+        './packages/cli/agents/default/.core/adam.book',
+    );
     await assertGeneratedBundlesArePublishSafe(packageGenerationContext.packagesMetadata, isBundlerSkipped);
     await writePublishWorkflow(packageGenerationContext.packagesMetadata);
     await maybeCommitGeneratedPackages(isCommitted, packageGenerationContext.mainPackageVersion);
diff --git a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
index 191c92ce81f04576da9acb928bb4bc600b8b9bb7..a6704912f47f10e54a74894f502c4b24e9cad8b4
--- a/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
+++ b/scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts
@@ -1,13 +1,19 @@
 import { spaceTrim } from 'spacetrim';
 import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
 import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
+import type { CreateAgentModelRequirementsOptions } from '../../../src/book-2.0/agent-source/CreateAgentModelRequirementsOptions';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 
 /**
  * Compiles agent source into the behavior text passed to CLI harness runners.
  */
-export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
-    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
+export async function createAgentRunnerSystemMessage(
+    agentSource: string_book,
+    options?: CreateAgentModelRequirementsOptions,
+): Promise<string> {
+    return formatAgentModelRequirementsForRunner(
+        await createAgentModelRequirements(agentSource, undefined, undefined, undefined, options),
+    );
 }
 
 /**
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..adf6a295e6d2e49270205a9f4a253e6696e1fa6f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts
@@ -0,0 +1,242 @@
+import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { dirname, join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { ParseError } from '../../../src/errors/ParseError';
+import { resolveCoderAgent } from './resolveCoderAgent';
+
+/** Primary agent path used to distinguish book-relative paths from project-relative paths. */
+const PRIMARY_AGENT_PATH = 'agents/coding/pavol-workshops.book';
+
+/** Original network implementation restored after every test. */
+const ORIGINAL_FETCH = global.fetch;
+
+describe('coder agent reference resolution', () => {
+    let projectPath: string;
+
+    /** Writes a fixture without assuming that its parent directories already exist. */
+    async function writeBook(relativePath: string, source: string): Promise<void> {
+        const filePath = join(projectPath, relativePath);
+        await mkdir(dirname(filePath), { recursive: true });
+        await writeFile(filePath, spaceTrim(source), 'utf-8');
+    }
+
+    /** Compiles the real coder prompt after choosing the fixture's primary book. */
+    async function compileAgent(commitment: string): Promise<string> {
+        await writeBook(
+            PRIMARY_AGENT_PATH,
+            spaceTrim(`
+            Pavol Workshops
+            ${commitment}
+            RULE Workshop-specific guidance.
+        `),
+        );
+        return (await resolveCoderAgent(PRIMARY_AGENT_PATH, projectPath))!.systemMessage;
+    }
+
+    beforeEach(async () => {
+        projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-references-'));
+        await writeBook(
+            'agents/coding/.core/adam.book',
+            spaceTrim(`
+            Adam
+            FROM @Null
+            RULE Shared Adam guidance.
+        `),
+        );
+        global.fetch = jest.fn().mockRejectedValue(new Error('Unexpected network request'));
+    });
+
+    afterEach(async () => {
+        global.fetch = ORIGINAL_FETCH;
+        await rm(projectPath, { recursive: true, force: true });
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves %s names recursively from first-line titles', async (commitment) => {
+        await writeBook(
+            'agents/coding/nested/not-the-agent-name.book',
+            spaceTrim(`
+            Pavol
+            PERSONA Mentor for workshop preparation.
+            RULE Inherited workshop guidance.
+        `),
+        );
+        for (const reference of ['@Pavol', '{Pavol}']) {
+            const systemMessage = await compileAgent(`${commitment} ${reference}`);
+            expect(systemMessage).toContain('Mentor for workshop preparation.');
+            expect(systemMessage).toContain('Workshop-specific guidance.');
+            if (commitment !== 'TEAM') {
+                expect(systemMessage).toContain('Inherited workshop guidance.');
+            }
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('resolves each supported local path form in %s', async (commitment) => {
+        const references = [
+            ['./foo/bar/agent.book', 'agents/coding/foo/bar/agent.book'],
+            ['../foo/bar/agent.book', 'agents/foo/bar/agent.book'],
+            ['foo/agent.book', 'foo/agent.book'],
+        ] as const;
+
+        for (const [reference, filePath] of references) {
+            await writeBook(
+                filePath,
+                spaceTrim(`
+                Mentor ${reference}
+                FROM @Null
+                PERSONA Guidance from ${reference}.
+            `),
+            );
+            const systemMessage = await compileAgent(`${commitment} {${reference}}`);
+            expect(systemMessage).toContain(`Guidance from ${reference}.`);
+        }
+        expect(global.fetch).not.toHaveBeenCalled();
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])('loads HTTP book references in %s', async (commitment) => {
+        global.fetch = jest.fn().mockResolvedValue(
+            new Response(
+                spaceTrim(`
+            Remote Mentor
+            FROM @void
+            PERSONA Remote workshop guidance.
+        `),
+                { headers: { 'content-type': 'text/plain' } },
+            ),
+        );
+        expect(await compileAgent(`${commitment} {http://example.com/agent.book}`)).toContain(
+            'Remote workshop guidance.',
+        );
+        expect(global.fetch).toHaveBeenCalledWith('http://example.com/agent.book');
+    });
+
+    it.each(['@null', '{null}', '@void', '{void}'])('disables inheritance with FROM %s', async (reference) => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent(`FROM ${reference}`);
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Workshop-specific guidance.');
+        expect(global.fetch).not.toHaveBeenCalled();
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('inherits the local Adam by default and keeps project-owned Adam unchanged', async () => {
+        const adamPath = join(projectPath, 'agents/coding/.core/adam.book');
+        const originalAdamSource = await readFile(adamPath, 'utf-8');
+        expect(await compileAgent('')).toContain('Shared Adam guidance.');
+        expect(await readFile(adamPath, 'utf-8')).toBe(originalAdamSource);
+    });
+
+    it('creates a missing Adam from the same bundled source as the server', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        const systemMessage = await compileAgent('');
+        const bundledAdamSource = await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8');
+        expect(await readFile(join(projectPath, 'agents/coding/.core/adam.book'), 'utf-8')).toBe(bundledAdamSource);
+        expect(systemMessage).toContain('You are a helpful, honest, and intelligent AI assistant.');
+    });
+
+    it('uses an existing Adam outside .core without creating a second Adam', async () => {
+        await rm(join(projectPath, 'agents/coding/.core/adam.book'));
+        await writeBook('agents/coding/shared/foundation.book', 'Adam\nFROM @Null\nRULE Custom foundation.');
+        expect(await compileAgent('')).toContain('Custom foundation.');
+        await expect(readFile(join(projectPath, 'agents/coding/.core/adam.book'))).rejects.toMatchObject({
+            code: 'ENOENT',
+        });
+    });
+
+    it('resolves nested inheritance, imports and multiline TEAM paths from each declaring book', async () => {
+        await writeBook(
+            'agents/coding/mentors/pavol.book',
+            spaceTrim(`
+            Pavol
+            FROM {../foundation.book}
+            IMPORT {./rules.book}
+            TEAM Ask the reviewer about changes:
+            {./reviewer.book}
+        `),
+        );
+        await writeBook('agents/coding/foundation.book', 'Foundation\nRULE Foundation guidance.');
+        await writeBook('agents/coding/mentors/rules.book', 'Rules\nRULE Imported guidance.');
+        await writeBook('agents/coding/mentors/reviewer.book', 'Reviewer\nPERSONA Reviews workshop materials.');
+        const systemMessage = await compileAgent('FROM @Pavol');
+        expect(systemMessage).toContain('Shared Adam guidance.');
+        expect(systemMessage).toContain('Foundation guidance.');
+        expect(systemMessage).toContain('Imported guidance.');
+        expect(systemMessage).toContain('Reviews workshop materials.');
+    });
+
+    it('resolves nested direct URL books before compiling the coder prompt', async () => {
+        global.fetch = jest.fn(
+            async (url: string) =>
+                new Response(
+                    url.endsWith('/parent.book')
+                        ? 'Remote Parent\nFROM {./base.book}\nRULE Remote parent guidance.'
+                        : 'Remote Base\nFROM @null\nRULE Remote base guidance.',
+                ),
+        ) as typeof fetch;
+        const systemMessage = await compileAgent('FROM {https://example.com/books/parent.book}');
+        expect(systemMessage).toContain('Remote parent guidance.');
+        expect(systemMessage).toContain('Remote base guidance.');
+        expect(global.fetch).toHaveBeenCalledWith('https://example.com/books/base.book');
+    });
+
+    it('resolves a teammate profile through the teammate own inheritance chain', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nPERSONA Experienced workshop mentor.');
+        await writeBook('agents/coding/reviewer.book', 'Reviewer\nFROM @Pavol');
+        expect(await compileAgent('TEAM @Reviewer')).toContain('Experienced workshop mentor.');
+    });
+
+    it('accepts IMPORTS and tab-separated references', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nRULE Imported guidance.');
+        expect(await compileAgent('FROM\t@null\nIMPORTS\t{./pavol.book}')).toContain('Imported guidance.');
+    });
+
+    it('reports a missing relative book with the resolved path', async () => {
+        await expect(compileAgent('FROM {./missing.book}')).rejects.toThrow(
+            join(projectPath, 'agents/coding/missing.book'),
+        );
+    });
+
+    it('only resolves the last FROM and follows the server behavior for inheritance cycles', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM {Pavol Workshops}\nRULE Parent guidance.');
+        const systemMessage = await compileAgent('FROM @Missing\nFROM @Pavol');
+        expect(systemMessage).toContain('Parent guidance.');
+        expect(systemMessage).not.toContain('Shared Adam guidance.');
+    });
+
+    it('rejects cyclic imports instead of recursing indefinitely', async () => {
+        await writeBook('agents/coding/pavol.book', 'Pavol\nFROM @null\nIMPORT {Pavol Workshops}');
+        await expect(compileAgent('FROM @null\nIMPORT @Pavol')).rejects.toBeInstanceOf(ParseError);
+    });
+
+    it.each(['FROM', 'IMPORT', 'TEAM'])(
+        'reports missing %s names without silently dropping instructions',
+        async (commitment) => {
+            await expect(compileAgent(`${commitment} @Missing`)).rejects.toBeInstanceOf(NotFoundError);
+        },
+    );
+
+    it('reports duplicate titles and allows an explicit path to disambiguate them', async () => {
+        await writeBook('agents/coding/one.book', 'Pavol\nFROM @null\nRULE First parent.');
+        await writeBook('agents/coding/two.book', 'Pavol\nFROM @null\nRULE Second parent.');
+        await expect(compileAgent('FROM @Pavol')).rejects.toThrow('ambiguous');
+        expect(await compileAgent('FROM {./two.book}')).toContain('Second parent.');
+    });
+
+    it('does not resolve example commitments inside code fences', async () => {
+        const systemMessage = await compileAgent(
+            spaceTrim(`
+            FROM @null
+            RULE Preserve this example:
+            \`\`\`book
+            IMPORT @Missing
+            TEAM {Unknown}
+            \`\`\`
+        `),
+        );
+        expect(systemMessage).toContain('IMPORT @Missing');
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index 9287bbcb5b39db77418bad2d831ed7f3e7a1ba1a..bfd23665f0abd54e50371c86092c0c8190d681aa
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -2,6 +2,7 @@
 import { basename, extname, relative, resolve } from 'path';
 import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { resolveLocalAgentSource } from '../../../src/cli/cli-commands/common/resolveLocalAgentSource';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
@@ -36,10 +37,15 @@
  * Agent data resolved from the optional `--agent` book file.
  */
 export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /** Source with inheritance and imports applied, also used for the coder's agent visual. */
+    readonly agentSource: string_book;
+
     /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
+    /** Books created during resolution, committed according to the coder's normal commit setting. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
 };
 
 /**
@@ -101,9 +107,18 @@
         return undefined;
     }
 
+    const resolvedSource = await resolveLocalAgentSource(
+        resolve(currentWorkingDirectory, agentBookReference!.trim()),
+        currentWorkingDirectory,
+    );
+
     return {
         ...resolvedAgentBook,
-        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+        agentSource: resolvedSource.agentSource,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedSource.agentSource, {
+            agentReferenceResolver: resolvedSource.agentReferenceResolver,
+        }),
+        createdAgentBookPaths: resolvedSource.createdAgentBookPaths,
     };
 }
 
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..db6a515baceb6092eaa24f1247694daaf84a3fa4
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
@@ -0,0 +1,32 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { commitInitializedAgentBooks } from './commitInitializedAgentBooks';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+jest.mock('./commitChanges', () => ({ commitChanges: jest.fn() }));
+jest.mock('./workingTreeChanges', () => ({ listWorkingTreeChangedFiles: jest.fn() }));
+
+describe('commitInitializedAgentBooks', () => {
+    beforeEach(() => jest.clearAllMocks());
+
+    it('commits only the Adam file created by the current run', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['agents/.core/adam.book', 'unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).toHaveBeenCalledWith('Initialize Adam agent', {
+            projectPath: process.cwd(),
+            relevantPaths: ['agents/.core/adam.book'],
+        });
+    });
+
+    it('does not commit ignored books or pre-existing changes', async () => {
+        jest.mocked(listWorkingTreeChangedFiles).mockResolvedValue(['unrelated.txt']);
+        await commitInitializedAgentBooks(process.cwd(), [resolve('agents/.core/adam.book')]);
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+
+    it('does not access Git when no book was created', async () => {
+        await commitInitializedAgentBooks(process.cwd(), []);
+        expect(listWorkingTreeChangedFiles).not.toHaveBeenCalled();
+        expect(commitChanges).not.toHaveBeenCalled();
+    });
+});
diff --git a/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f241690ab3a21c7974f7a4ae55874a5aac3f630
--- /dev/null
+++ b/scripts/run-codex-prompts/git/commitInitializedAgentBooks.ts
@@ -0,0 +1,25 @@
+import { resolve } from 'path';
+import { commitChanges } from './commitChanges';
+import { listWorkingTreeChangedFiles } from './workingTreeChanges';
+
+/**
+ * Commits newly initialized, non-ignored agent books before the coder's clean-tree checkpoint.
+ * Only books created by this run are eligible; existing books and unrelated changes remain outside the commit.
+ */
+export async function commitInitializedAgentBooks(
+    projectPath: string,
+    createdAgentBookPaths: ReadonlyArray<string>,
+): Promise<void> {
+    if (createdAgentBookPaths.length === 0) {
+        return;
+    }
+
+    const createdPaths = new Set(createdAgentBookPaths.map((filePath) => resolve(filePath)));
+    const changedPaths = await listWorkingTreeChangedFiles(projectPath);
+    const relevantPaths = changedPaths.filter((filePath) => createdPaths.has(resolve(projectPath, filePath)));
+    if (relevantPaths.length === 0) {
+        return;
+    }
+
+    await commitChanges('Initialize Adam agent', { projectPath, relevantPaths });
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 9503a60093efb667d52c90ea761e743a53708e8d..92ee4968c3114cfbf72bba10d7d34cd97b2d25f2
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@
 import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
 import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
 import { commitChanges } from '../git/commitChanges';
+import { commitInitializedAgentBooks } from '../git/commitInitializedAgentBooks';
 import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
 import { pullLatestChanges } from '../git/pullLatestChanges';
 import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
@@ -115,6 +116,10 @@
             return;
         }
 
+        if (!options.noCommit && resolvedCoderAgent) {
+            await commitInitializedAgentBooks(process.cwd(), resolvedCoderAgent.createdAgentBookPaths);
+        }
+
         const { runner, actualRunnerModel, runnerMetadata: harnessRunnerMetadata } = resolvePromptRunner(options);
         // Note: The harness only knows itself, so the Book agent it runs as is joined here - this is the single
         //       place where the whole run report of prompt status lines and run traces is put together
diff --git a/src/book-2.0/agent-source/explicitFromCommitment.ts b/src/book-2.0/agent-source/explicitFromCommitment.ts
index cc60c1ab39e68288590ebdb5800bb2c7d429fc57..4aa41461e6ff36dd33fc13ea6fbe02564640ed27
--- a/src/book-2.0/agent-source/explicitFromCommitment.ts
+++ b/src/book-2.0/agent-source/explicitFromCommitment.ts
@@ -128,3 +128,5 @@
 export function isImplicitAdamInheritance(agentSource: string_book): boolean {
     return getExplicitFromCommitmentContent(agentSource) === undefined;
 }
+
+// Note: [💞] Ignore a discrepancy between file name and exported helper names.
diff --git a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
index 299f2f484533100a3b91cc04f2fa9697a8e98779..be6233e5be0fab8240659be48b36dec5fbacfb4a
--- a/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
+++ b/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
@@ -542,7 +542,7 @@
  * @returns Output lines replacing the original line.
  */
 async function resolveImportCommitmentLine(line: string, context: AgentImportContext): Promise<Array<string>> {
-    const rawImportedUrlOrPath = line.trim().substring('IMPORT '.length).trim();
+    const rawImportedUrlOrPath = line.trim().replace(/^IMPORTS?\s+/, '').trim();
     let importedUrlOrPath = rawImportedUrlOrPath;
     let importResolutionIssues: Array<AgentReferenceResolutionIssue> = [];
 
@@ -665,19 +665,29 @@
     const explicitFromCommitments = collectExplicitFromCommitments(agentSourceChunks);
     const effectiveFromCommitment = explicitFromCommitments[explicitFromCommitments.length - 1];
     const overriddenFromLineIndexes = collectOverriddenFromLineIndexes(explicitFromCommitments);
-    let isFromResolved = false;
+    let isFromResolved = false;
+    let isInsideCodeBlock = false;
     let fromResolutionIssues = parentContext.fromResolutionIssues;
     // <- TODO: [🈲] Simple and encapsulated way to split book into commitments
 
     for (let lineIndex = 0; lineIndex < agentSourceChunks.length; lineIndex++) {
-        const line = agentSourceChunks[lineIndex] || '';
+        const line = agentSourceChunks[lineIndex] || '';
+
+        if (line.trim().startsWith('```')) {
+            isInsideCodeBlock = !isInsideCodeBlock;
+        }
+
+        if (isInsideCodeBlock || line.trim().startsWith('```')) {
+            resolvedAgentSourceChunks.push(line);
+            continue;
+        }
 
         // Note: A repeated `FROM` is overridden by the last one, so the outdated line is dropped from the resolved source.
         if (overriddenFromLineIndexes.has(lineIndex)) {
             continue;
         }
 
-        if (line.trim().startsWith('IMPORT ')) {
+        if (lineIndex > 0 && /^IMPORTS?\s+/.test(line.trim())) {
             resolvedAgentSourceChunks.push(...(await resolveImportCommitmentLine(line, context)));
             continue;
         }
diff --git a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
index dd5d6149265dffb329bb430d11913fc3db66e0ce..772a183fbb6915025b0a70ac845173fd8149c0e2
--- a/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
+++ b/src/cli/cli-commands/coder/boilerplateTemplates.test.ts
@@ -77,6 +77,10 @@
 
         expect(summary.promptsTemplatesDirectoryStatus).toBe('created');
         expect(summary.agentsDirectoryStatus).toBe('created');
+        expect(summary.adamAgentFileStatus).toBe('created');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toBe(
+            await readFile(join(process.cwd(), 'agents/default/.core/adam.book'), 'utf-8'),
+        );
         expect(getReferencedArtifactStatus(summary, CODER_DEVELOPER_AGENT_FILE_PATH)).toBe('created');
         expect(getReferencedArtifactStatus(summary, AGENTS_FILE_PATH)).toBe('created');
         expect(summary.gitignoreFileStatus).toBe('created');
@@ -271,8 +275,12 @@
         const projectPath = await createTemporaryDirectory(temporaryDirectories);
 
         await initializeCoderProjectConfiguration(projectPath);
+        await writeFile(join(projectPath, 'agents/.core/adam.book'), 'Adam\nFROM @Null\nRULE Custom foundation.\n');
         const repeatedSummary = await initializeCoderProjectConfiguration(projectPath);
 
+        expect(repeatedSummary.adamAgentFileStatus).toBe('unchanged');
+        expect(await readFile(join(projectPath, 'agents/.core/adam.book'), 'utf-8')).toContain('Custom foundation.');
+
         const envContent = await readFile(join(projectPath, '.env'), 'utf-8');
         expect(repeatedSummary.envFileStatus).toBe('unchanged');
         expect(repeatedSummary.initializedEnvVariableNames).toEqual([]);
diff --git a/src/cli/cli-commands/coder/init.ts b/src/cli/cli-commands/coder/init.ts
index 1835f850f354c97e84ea5e5ffd3e72b920b6ce41..b8ba47495e555db944585c4921823deb42a5a896
--- a/src/cli/cli-commands/coder/init.ts
+++ b/src/cli/cli-commands/coder/init.ts
@@ -3,6 +3,7 @@
 } from 'commander';
 import { spaceTrim } from 'spacetrim';
 import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
 import type { CoderGitSyncCliOptions } from '../common/coderGitSyncCliOptions';
 import {
     addCoderGitSyncOptions,
@@ -22,7 +23,7 @@
 import { AGENTS_FILE_PATH } from './agentsFile';
 import { DEFAULT_BOILERPLATE_COUNT } from './boilerplateCount';
 import { getDefaultCoderProjectPromptTemplateDefinitions, PROMPTS_DIRECTORY_PATH } from './boilerplateTemplates';
-import { CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
+import { CODER_AGENTS_DIRECTORY_PATH, CODER_DEVELOPER_AGENT_FILE_PATH } from './ensureCoderDeveloperAgentFile';
 import { isDirectoryEmpty } from './ensureDirectory';
 import { formatDisplayPath } from './formatDisplayPath';
 import { generatePromptBoilerplate } from './generate-boilerplates';
@@ -61,6 +62,7 @@
                 - prompts/done/
                 ${block(listDefaultCoderProjectPromptTemplateDisplayPaths())}
                 - ${CODER_DEVELOPER_AGENT_FILE_PATH}
+                - ${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}
                 - ${AGENTS_FILE_PATH}
                 - .gitignore with local artifacts from every supported harness
                 - package.json
diff --git a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
index 0b353007b74b0b4d5681317dcb2c11b5ed07593b..e2da160df4efdb411d88a4b7bb8c38c1e73808d0
--- a/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
+++ b/src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts
@@ -24,6 +24,7 @@
     readonly promptsDoneDirectoryStatus: InitializationStatus;
     readonly promptsTemplatesDirectoryStatus: InitializationStatus;
     readonly agentsDirectoryStatus: InitializationStatus;
+    readonly adamAgentFileStatus: InitializationStatus;
     readonly envFileStatus: InitializationStatus;
     readonly gitignoreFileStatus: InitializationStatus;
     readonly packageJsonFileStatus: InitializationStatus;
@@ -46,6 +47,7 @@
     const promptsDoneDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_DONE_DIRECTORY_PATH);
     const promptsTemplatesDirectoryStatus = await ensureDirectory(projectPath, PROMPTS_TEMPLATES_DIRECTORY_PATH);
     const agentsDirectoryStatus = await ensureDirectory(projectPath, CODER_AGENTS_DIRECTORY_PATH);
+    const adamAgentFileStatus = await ensureAdamAgentBook(join(projectPath, CODER_AGENTS_DIRECTORY_PATH));
     const { envFileStatus, initializedEnvVariableNames } = await ensureCoderEnvFile(projectPath);
     const gitignoreFileStatus = await ensureCoderGitignoreFile(projectPath);
     const { status: packageJsonFileStatus, addedEntryKeys: addedPackageJsonScriptNames } =
@@ -61,6 +63,7 @@
         promptsDoneDirectoryStatus,
         promptsTemplatesDirectoryStatus,
         agentsDirectoryStatus,
+        adamAgentFileStatus,
         envFileStatus,
         gitignoreFileStatus,
         packageJsonFileStatus,
@@ -72,3 +75,5 @@
 }
 
 // Note: [🟡] Code for coder init project bootstrapping [initializeCoderProjectConfiguration](src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts) should never be published outside of `@promptbook/cli`
+import { join } from 'path';
+import { ensureAdamAgentBook } from '../common/ensureAdamAgentBook';
diff --git a/src/cli/cli-commands/coder/printInitializationSummary.ts b/src/cli/cli-commands/coder/printInitializationSummary.ts
index 6d1754041027a9c171d993fdd1e8b7a283a2cfcb..d7485e067e50535b8286cb0dad80ec6f18470fba
--- a/src/cli/cli-commands/coder/printInitializationSummary.ts
+++ b/src/cli/cli-commands/coder/printInitializationSummary.ts
@@ -1,4 +1,6 @@
 import colors from 'colors';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH } from '../common/ensureAdamAgentBook';
+import { CODER_AGENTS_DIRECTORY_PATH } from './ensureCoderDeveloperAgentFile';
 import type { InitializationStatus } from './boilerplateTemplates';
 import type { CoderReferencedArtifactStatus } from './coderReferencedArtifacts';
 import { formatDisplayPath } from './formatDisplayPath';
@@ -15,6 +17,10 @@
     printInitializationStatusLine('prompts/done/', summary.promptsDoneDirectoryStatus);
     printInitializationStatusLine('prompts/templates/', summary.promptsTemplatesDirectoryStatus);
     printInitializationStatusLine('agents/', summary.agentsDirectoryStatus);
+    printInitializationStatusLine(
+        `${CODER_AGENTS_DIRECTORY_PATH}/${ADAM_AGENT_BOOK_RELATIVE_PATH}`,
+        summary.adamAgentFileStatus,
+    );
     printInitializationStatusLine('.env', summary.envFileStatus);
     printInitializationStatusLine('.gitignore', summary.gitignoreFileStatus);
     printInitializationStatusLine('package.json', summary.packageJsonFileStatus);
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 6d64511cb287462cd44d8a2daa7a07e0e5bf478d..577c874384905dfc2f01d96e70abbb9c135ba2ca
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -54,6 +54,8 @@
             ${block(QUESTIONS_DESCRIPTION)}
 
             Features:
+            - Resolves FROM, IMPORT and TEAM by agent name, book-relative path, project path or URL
+            - Creates a missing .core/adam.book beside the selected agent for default inheritance
             - Automatically stages and commits changes with agent identity unless --no-commit is used
             - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
             - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
diff --git a/src/cli/cli-commands/common/LocalAgentBookCollection.ts b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5cd4508af0cfc397b63abd1dcaeaa9c3e406ced0
--- /dev/null
+++ b/src/cli/cli-commands/common/LocalAgentBookCollection.ts
@@ -0,0 +1,220 @@
+import { readFile, readdir, realpath } from 'fs/promises';
+import { dirname, isAbsolute, join, resolve } from 'path';
+import { pathToFileURL } from 'url';
+import { spaceTrim } from 'spacetrim';
+import { normalizeAgentName } from '../../../book-2.0/agent-source/normalizeAgentName';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import type { TeammateProfile } from '../../../book-2.0/agent-source/TeammateProfileResolver';
+import { NotFoundError } from '../../../errors/NotFoundError';
+import { ParseError } from '../../../errors/ParseError';
+import { isValidAgentUrl } from '../../../utils/validators/url/isValidAgentUrl';
+import { ADAM_AGENT_BOOK_RELATIVE_PATH, ensureAdamAgentBook } from './ensureAdamAgentBook';
+
+/**
+ * A book's source and identity, retaining its location for nested relative references.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type LocalAgentBook = {
+    readonly url: string;
+    readonly filePath?: string;
+    readonly source: string_book;
+    readonly profile: TeammateProfile;
+};
+
+/**
+ * Internal URLs allow the shared Book commitments to identify local books without an HTTP server.
+ * These identifiers are always intercepted by this collection and are never fetched.
+ */
+const LOCAL_AGENT_URL_PREFIX = 'https://local-agent.promptbook/';
+
+/**
+ * Directories which cannot contain project-owned agent definitions.
+ */
+const IGNORED_DIRECTORY_NAMES = new Set(['.git', 'node_modules']);
+
+/**
+ * Reads repository books and indexes their first-line names for CLI reference resolution.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export class LocalAgentBookCollection {
+    private readonly booksByUrl = new Map<string, LocalAgentBook>();
+    private readonly booksByName = new Map<string, LocalAgentBook[]>();
+    /** Stable default ancestor selected from the initial repository scan. */
+    private adamAgentUrl: string | undefined;
+    /** Files initialized by this collection, for the coder's normal commit handling. */
+    public readonly createdAgentBookPaths: string[] = [];
+
+    /** Creates a collection rooted at the primary book's directory. */
+    public constructor(private readonly agentDirectoryPath: string, private readonly currentWorkingDirectory: string) {}
+
+    /** Discovers nested books, including the hidden `.core` directory. */
+    public async initialize(): Promise<void> {
+        await this.readDirectory(this.agentDirectoryPath);
+    }
+
+    /** Identifies the default ancestor without creating an unused book for FROM null/void. */
+    public getAdamAgentUrl(): string {
+        this.adamAgentUrl ??= this.booksByName.has(normalizeAgentName('Adam'))
+            ? this.findByName('Adam').url
+            : createLocalAgentBookUrl(join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH));
+        return this.adamAgentUrl;
+    }
+
+    /** Resolves a unique first-line name, refusing ambiguous matches. */
+    public findByName(name: string): LocalAgentBook {
+        const matches = this.booksByName.get(normalizeAgentName(name)) || [];
+        if (matches.length === 1) {
+            return matches[0]!;
+        }
+
+        if (matches.length > 1) {
+            throw new ParseError(
+                spaceTrim(
+                    (block) => `
+                Agent reference \`${name}\` is ambiguous. Use an explicit book path.
+
+                Matching books:
+                ${block(matches.map((book) => `- \`${book.filePath || book.url}\``).join('\n'))}
+            `,
+                ),
+            );
+        }
+
+        throw new NotFoundError(
+            spaceTrim(`
+            Agent \`${name}\` was not found in \`${this.agentDirectoryPath}\` or its subdirectories.
+
+            References use the agent's **first-line name**. You can also use an explicit \`.book\` path or URL.
+        `),
+        );
+    }
+
+    /** Resolves a name, a path relative to its declaring book, or a path relative to the CLI cwd. */
+    public async resolveReference(reference: string, declaringBook: LocalAgentBook): Promise<string> {
+        const isRemoteReference = Boolean(isValidAgentUrl(reference));
+        if (isRemoteReference) {
+            return reference;
+        }
+
+        const isBookPath = /\.book$/i.test(reference) || isAbsolute(reference);
+        if (!isBookPath) {
+            if (normalizeAgentName(reference) === normalizeAgentName('Adam')) {
+                return this.getAdamAgentUrl();
+            }
+            return this.findByName(reference).url;
+        }
+
+        const isRelativeToBook = /^\.{1,2}[\\/]/.test(reference);
+        if (!declaringBook.filePath && isRelativeToBook) {
+            return new URL(reference.replaceAll('\\', '/'), declaringBook.url).href;
+        }
+
+        const directoryPath =
+            isRelativeToBook && declaringBook.filePath ? dirname(declaringBook.filePath) : this.currentWorkingDirectory;
+        return (await this.readBook(resolve(directoryPath, reference))).url;
+    }
+
+    /** Reads a local book once, canonicalizing symlinks so aliases cannot bypass cycle detection. */
+    public async readBook(filePath: string): Promise<LocalAgentBook> {
+        try {
+            const canonicalFilePath = await realpath(filePath);
+            const url = createLocalAgentBookUrl(canonicalFilePath);
+            const existingBook = this.booksByUrl.get(url);
+            if (existingBook) {
+                return existingBook;
+            }
+
+            return this.registerBook(url, await readFile(canonicalFilePath, 'utf-8'), canonicalFilePath);
+        } catch (error) {
+            if (['ENOENT', 'EISDIR', 'ENOTDIR'].includes((error as NodeJS.ErrnoException).code || '')) {
+                throw new NotFoundError(spaceTrim(`Agent book \`${filePath}\` was not found or is not a file.`));
+            }
+            throw error;
+        }
+    }
+
+    /** Loads a referenced remote book, while keeping local identifiers entirely inside the collection. */
+    public async getBook(url: string): Promise<LocalAgentBook> {
+        const existingBook = this.booksByUrl.get(url);
+        if (existingBook) {
+            return existingBook;
+        }
+        if (url === this.getAdamAgentUrl()) {
+            const adamBookPath = join(this.agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+            if ((await ensureAdamAgentBook(this.agentDirectoryPath)) === 'created') {
+                this.createdAgentBookPaths.push(adamBookPath);
+            }
+            const adamBook = await this.readBook(adamBookPath);
+            this.booksByUrl.set(url, adamBook);
+            return adamBook;
+        }
+        if (url.startsWith(LOCAL_AGENT_URL_PREFIX)) {
+            throw new NotFoundError(spaceTrim(`Local agent book \`${url}\` is not registered.`));
+        }
+
+        const bookUrl = new URL(url);
+        if (!/\.(book|md)$/i.test(bookUrl.pathname) && !bookUrl.pathname.endsWith('/api/book')) {
+            bookUrl.pathname = bookUrl.pathname.replace(/\/$/, '') + '/api/book';
+        }
+        const response = await fetch(bookUrl.href);
+        if (!response.ok) {
+            throw new NotFoundError(
+                spaceTrim(`Cannot load agent book \`${url}\`: **${response.status} ${response.statusText}**.`),
+            );
+        }
+        let source: unknown;
+        if (response.headers.get('content-type')?.includes('application/json')) {
+            const payload: unknown = await response.json();
+            source = typeof payload === 'string' ? payload : (payload as { source?: unknown } | null)?.source;
+        } else {
+            source = await response.text();
+        }
+        if (typeof source !== 'string') {
+            throw new ParseError(spaceTrim(`Agent book \`${url}\` did not return a text source.`));
+        }
+        return this.registerBook(url, source);
+    }
+
+    /** Adds a book under its normalized first-line name and preserves the display name for TEAM. */
+    private registerBook(url: string, source: string, filePath?: string): LocalAgentBook {
+        const agentSource = source as string_book;
+        const parsedSource = parseAgentSource(agentSource);
+        const book: LocalAgentBook = {
+            url,
+            filePath,
+            source: agentSource,
+            profile: {
+                agentName: parsedSource.meta.fullname || parsedSource.agentName,
+                personaDescription: parsedSource.personaDescription,
+            },
+        };
+        this.booksByUrl.set(url, book);
+        // Remote titles must not shadow repository-local names.
+        if (filePath) {
+            const agentName = normalizeAgentName(parsedSource.agentName);
+            this.booksByName.set(agentName, [...(this.booksByName.get(agentName) || []), book]);
+        }
+        return book;
+    }
+
+    /** Walks real directories in deterministic order without following directory symlink loops. */
+    private async readDirectory(directoryPath: string): Promise<void> {
+        const entries = await readdir(directoryPath, { withFileTypes: true });
+        for (const entry of entries.sort((left, right) => left.name.localeCompare(right.name))) {
+            const entryPath = join(directoryPath, entry.name);
+            if (entry.isDirectory() && !IGNORED_DIRECTORY_NAMES.has(entry.name)) {
+                await this.readDirectory(entryPath);
+            } else if (entry.isFile() && entry.name.toLowerCase().endsWith('.book')) {
+                await this.readBook(entryPath);
+            }
+        }
+    }
+}
+
+/** Creates an internal URL for a local path without requiring that the file already exists. */
+function createLocalAgentBookUrl(filePath: string): string {
+    return LOCAL_AGENT_URL_PREFIX + encodeURIComponent(pathToFileURL(filePath).href);
+}
diff --git a/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9f948435f67250dad69bbdbeec07d7e54f023bf9
--- /dev/null
+++ b/src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts
@@ -0,0 +1,62 @@
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { extractAgentReferenceTokens } from '../../../book-2.0/agent-source/extractAgentReferenceTokens';
+import {
+    createPseudoAgentUrl,
+    isPseudoAgentAllowedInCommitment,
+    resolvePseudoAgentKindFromReference,
+    VOID_PSEUDO_AGENT_REFERENCE,
+} from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import type { BookCommitment } from '../../../commitments/_base/BookCommitment';
+import { ParseError } from '../../../errors/ParseError';
+import type { LocalAgentBook, LocalAgentBookCollection } from './LocalAgentBookCollection';
+
+/**
+ * Creates a resolver scoped to the book which declares each reference.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export function createLocalAgentReferenceResolver(
+    collection: LocalAgentBookCollection,
+    declaringBook: LocalAgentBook,
+): AgentReferenceResolver {
+    /** Resolves pseudo-agents using the same aliases and restrictions as the Agent Server. */
+    async function resolveReference(commitmentType: BookCommitment, reference: string): Promise<string> {
+        const pseudoAgentKind = resolvePseudoAgentKindFromReference(reference);
+        if (!pseudoAgentKind) {
+            return collection.resolveReference(reference, declaringBook);
+        }
+        if (!isPseudoAgentAllowedInCommitment(pseudoAgentKind, commitmentType)) {
+            throw new ParseError(spaceTrim(`Pseudo-agent \`${reference}\` cannot be used in \`${commitmentType}\`.`));
+        }
+        if (pseudoAgentKind === 'VOID' && commitmentType === 'FROM') {
+            return VOID_PSEUDO_AGENT_REFERENCE;
+        }
+        if (pseudoAgentKind === 'VOID' && (commitmentType === 'IMPORT' || commitmentType === 'IMPORTS')) {
+            return '';
+        }
+        return createPseudoAgentUrl(pseudoAgentKind);
+    }
+
+    return {
+        resolveCommitmentContent: async (commitmentType, content) => {
+            const tokens = extractAgentReferenceTokens(content);
+            if (tokens.length === 0) {
+                if (commitmentType !== 'TEAM' && content.trim()) {
+                    return resolveReference(commitmentType, content.trim());
+                }
+                return content;
+            }
+
+            const parts: string[] = [];
+            let previousEnd = 0;
+            for (const token of tokens) {
+                parts.push(content.slice(previousEnd, token.index));
+                parts.push(await resolveReference(commitmentType, token.reference));
+                previousEnd = token.index + token.length;
+            }
+            parts.push(content.slice(previousEnd));
+            return parts.join('');
+        },
+    };
+}
diff --git a/src/cli/cli-commands/common/ensureAdamAgentBook.ts b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
new file mode 100644
index 0000000000000000000000000000000000000000..abc6328977b1b5d9427ead2ed090a877e488952c
--- /dev/null
+++ b/src/cli/cli-commands/common/ensureAdamAgentBook.ts
@@ -0,0 +1,35 @@
+import { constants } from 'fs';
+import { copyFile, mkdir, stat } from 'fs/promises';
+import { dirname, join } from 'path';
+import { resolveBundledAgentBookPath } from './resolveBundledAgentBookPath';
+
+/**
+ * Adam's location relative to a directory of local agents.
+ *
+ * @private internal constant of CLI agent initialization
+ */
+export const ADAM_AGENT_BOOK_RELATIVE_PATH = '.core/adam.book';
+
+/**
+ * Creates the same Adam book used by the Agent Server without overwriting a project-owned book.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function ensureAdamAgentBook(agentDirectoryPath: string): Promise<'created' | 'unchanged'> {
+    const adamBookPath = join(agentDirectoryPath, ADAM_AGENT_BOOK_RELATIVE_PATH);
+    if (await stat(adamBookPath).then((entry) => entry.isFile(), () => false)) {
+        return 'unchanged';
+    }
+
+    const bundledAdamBookPath = await resolveBundledAgentBookPath('agents/default/.core/adam.book');
+    await mkdir(dirname(adamBookPath), { recursive: true });
+    try {
+        await copyFile(bundledAdamBookPath, adamBookPath, constants.COPYFILE_EXCL);
+        return 'created';
+    } catch (error) {
+        if ((error as NodeJS.ErrnoException).code === 'EEXIST') {
+            return 'unchanged';
+        }
+        throw error;
+    }
+}
diff --git a/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
new file mode 100644
index 0000000000000000000000000000000000000000..5a07c2de2db24f4d946ad89decec19dca49d2673
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveBundledAgentBookPath.ts
@@ -0,0 +1,31 @@
+import { stat } from 'fs/promises';
+import { join } from 'path';
+import { spaceTrim } from 'spacetrim';
+import { NotFoundError } from '../../../errors/NotFoundError';
+
+/**
+ * Locates a bundled agent in either the source checkout or the generated CLI package.
+ *
+ * @private internal utility of CLI agent initialization
+ */
+export async function resolveBundledAgentBookPath(relativeFilePath: string): Promise<string> {
+    const candidates = [
+        join(__dirname, '..', relativeFilePath),
+        join(__dirname, '..', '..', '..', '..', relativeFilePath),
+    ];
+
+    for (const candidate of candidates) {
+        if (await stat(candidate).then((entry) => entry.isFile(), () => false)) {
+            return candidate;
+        }
+    }
+
+    throw new NotFoundError(
+        spaceTrim((block) => `
+            Cannot find the bundled agent book \`${relativeFilePath}\`.
+
+            Checked:
+            ${block(candidates.map((candidate) => `- \`${candidate}\``).join('\n'))}
+        `),
+    );
+}
diff --git a/src/cli/cli-commands/common/resolveLocalAgentSource.ts b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
new file mode 100644
index 0000000000000000000000000000000000000000..8a16cc37e4f8b8b118de09d1148516e6ea7bf4b5
--- /dev/null
+++ b/src/cli/cli-commands/common/resolveLocalAgentSource.ts
@@ -0,0 +1,127 @@
+import { dirname } from 'path';
+import { spaceTrim } from 'spacetrim';
+import type { AgentReferenceResolver } from '../../../book-2.0/agent-source/AgentReferenceResolver';
+import { getEffectiveExplicitFromCommitment } from '../../../book-2.0/agent-source/explicitFromCommitment';
+import { parseAgentSourceWithCommitments } from '../../../book-2.0/agent-source/parseAgentSourceWithCommitments';
+import { parseAgentSource } from '../../../book-2.0/agent-source/parseAgentSource';
+import { isPseudoAgentUrl } from '../../../book-2.0/agent-source/pseudoAgentReferences';
+import { resolveInheritedAgentSource } from '../../../book-2.0/agent-source/resolveInheritedAgentSource';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { DEFAULT_MAX_RECURSION } from '../../../config';
+import { ParseError } from '../../../errors/ParseError';
+import { createLocalAgentReferenceResolver } from './createLocalAgentReferenceResolver';
+import { LocalAgentBookCollection, type LocalAgentBook } from './LocalAgentBookCollection';
+
+/**
+ * Resolved source together with the profile resolver used when compiling TEAM commitments.
+ *
+ * @private internal type of CLI agent resolution
+ */
+export type ResolvedLocalAgentSource = {
+    readonly agentSource: string_book;
+    readonly agentReferenceResolver: AgentReferenceResolver;
+    /** Newly created books which the coder may include in its initialization commit. */
+    readonly createdAgentBookPaths: ReadonlyArray<string>;
+};
+
+/**
+ * Applies the shared Agent Server inheritance rules to a repository's local and remote books.
+ *
+ * @private internal utility of CLI agent resolution
+ */
+export async function resolveLocalAgentSource(
+    agentBookPath: string,
+    currentWorkingDirectory: string,
+): Promise<ResolvedLocalAgentSource> {
+    const collection = new LocalAgentBookCollection(dirname(agentBookPath), currentWorkingDirectory);
+    await collection.initialize();
+    const primaryBook = await collection.readBook(agentBookPath);
+    const adamAgentUrl = collection.getAdamAgentUrl();
+
+    /** Resolves each dependency with its own location and the current inheritance stack. */
+    async function resolveBook(
+        book: LocalAgentBook,
+        inheritancePath: ReadonlyArray<string> = [],
+    ): Promise<string_book> {
+        if (inheritancePath.length > DEFAULT_MAX_RECURSION) {
+            throw new ParseError(
+                spaceTrim(
+                    `Agent inheritance exceeds **${DEFAULT_MAX_RECURSION}** levels at \`${
+                        book.filePath || book.url
+                    }\`.`,
+                ),
+            );
+        }
+        const agentReferenceResolver = createLocalAgentReferenceResolver(collection, book);
+        const source = await resolveBookCommitmentReferences(book.source, agentReferenceResolver);
+        return resolveInheritedAgentSource(source, {
+            adamAgentUrl,
+            currentAgentUrl: book.url,
+            inheritancePath,
+            agentSourceImporter: async (url, context) =>
+                resolveBook(await collection.getBook(url), context.importAgentOptions.inheritancePath),
+        });
+    }
+
+    return {
+        agentSource: await resolveBook(primaryBook),
+        agentReferenceResolver: {
+            ...createLocalAgentReferenceResolver(collection, primaryBook),
+            resolveTeammateProfile: async (url) => {
+                if (isPseudoAgentUrl(url)) {
+                    return null;
+                }
+                const book = await collection.getBook(url);
+                const parsedSource = parseAgentSource(await resolveBook(book));
+                return {
+                    agentName: book.profile.agentName,
+                    personaDescription: parsedSource.personaDescription,
+                };
+            },
+        },
+        createdAgentBookPaths: collection.createdAgentBookPaths,
+    };
+}
+
+/**
+ * Resolves reference blocks before embedding dependencies so inherited TEAM paths keep their original directory.
+ * Parser-provided locations keep examples, unknown commitments and ordinary prose untouched.
+ */
+async function resolveBookCommitmentReferences(
+    source: string_book,
+    resolver: AgentReferenceResolver,
+): Promise<string_book> {
+    const parsedSource = parseAgentSourceWithCommitments(source);
+    const lines = source.split(/\r?\n/);
+    const commitmentStartLines = [...parsedSource.commitments, ...(parsedSource.unknownCommitments || [])]
+        .map((commitment) => commitment.lineNumber - 1)
+        .sort((left, right) => left - right);
+    const effectiveFrom = getEffectiveExplicitFromCommitment(source);
+
+    for (const commitment of [...parsedSource.commitments].reverse()) {
+        if (!['FROM', 'IMPORT', 'IMPORTS', 'TEAM'].includes(commitment.type)) {
+            continue;
+        }
+        const startLine = commitment.lineNumber - 1;
+        if (commitment.type === 'FROM' && effectiveFrom?.lineIndex !== startLine) {
+            continue;
+        }
+        let endLine = commitmentStartLines.find((lineNumber) => lineNumber > startLine) ?? lines.length;
+        // A horizontal rule also terminates a commitment before the next registered keyword.
+        for (let lineIndex = startLine + 1; lineIndex < endLine; lineIndex++) {
+            if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(lines[lineIndex] || '')) {
+                endLine = lineIndex;
+                break;
+            }
+        }
+        const content = await resolver.resolveCommitmentContent(commitment.type, commitment.content);
+        const commitmentType = commitment.type === 'IMPORTS' ? 'IMPORT' : commitment.type;
+        const replacement = content.trim()
+            ? `${commitmentType} ${content}\n`
+            : commitmentType === 'FROM'
+            ? 'FROM @Null\n'
+            : '';
+        lines.splice(startLine, endLine - startLine, ...replacement.split('\n'));
+    }
+    return lines.join('\n') as string_book;
+}

ERROR: You've hit your usage limit. Upgrade to Pro (https://chatgpt.com/explore/pro), visit https://chatgpt.com/codex/settings/usage to purchase more credits or try again at 7:06 AM.
ERROR: You've hit your usage limit. Upgrade to Pro (https://chatgpt.com/explore/pro), visit https://chatgpt.com/codex/settings/usage to purchase more credits or try again at 7:06 AM.
2026-09-17T00:37:08.870361Z ERROR codex_core::session: failed to record rollout items: thread 01a0acaf-1e3e-7952-b68c-7585ce323386 not found
tokens used
223,687

=== runner shell finished at 2026-09-17T00:37:09.996Z ===
Status: succeeded

=== test shell started at 2026-09-17T00:37:10.054Z ===
Script path: /c/Users/me/work/ai/promptbook/.promptbook/coder-prompts/2026-09-0260-ptbk-coder-working-from-1.test.sh

--- raw input ---
cd "/c/Users/me/work/ai/promptbook"
npm run test-for-ptbk-coder

--- raw output ---

> promptbook@0.114.0-38 test-for-ptbk-coder
> node ./scripts/test-for-ptbk-coder.js

Note: Running from most lightweight to heaviest tests

> promptbook@0.114.0-38 test-name-discrepancies
> ts-node ./scripts/find-name-discrepancies/find-name-discrepancies.ts

🔎🩹 Find name discrepancies
[32mNo name discrepancies found[39m

> promptbook@0.114.0-38 test-spellcheck
> bash ./scripts/spellcheck.sh

   1/2086 src/_packages/anthropic-claude.index.ts 964.96ms
   2/2086 src/_packages/anthropic-claude.readme.md 100.03ms
   3/2086 src/_packages/azure-openai.index.ts 4.86ms
   4/2086 src/_packages/azure-openai.readme.md 18.82ms
   5/2086 src/_packages/browser.index.ts 9.63ms
   6/2086 src/_packages/browser.readme.md 13.31ms
   7/2086 src/_packages/cli.index.ts 21.59ms
   8/2086 src/_packages/cli.readme.md 19.92ms
   9/2086 src/_packages/color.index.ts 9.44ms
  10/2086 src/_packages/color.readme.md 94.87ms
  11/2086 src/_packages/components.index.ts 20.62ms
  12/2086 src/_packages/components.readme.md 11.78ms
  13/2086 src/_packages/content/$provideExecutionToolsForNode.md 5.13ms
  14/2086 src/_packages/content/providers.md 2.91ms
  15/2086 src/_packages/core.index.ts 77.61ms
  16/2086 src/_packages/core.readme.md 37.51ms
  17/2086 src/_packages/deepseek.index.ts 3.00ms
  18/2086 src/_packages/deepseek.readme.md 1.66ms
  19/2086 src/_packages/documents.index.ts 3.49ms
  20/2086 src/_packages/documents.readme.md 1.77ms
  21/2086 src/_packages/editable.index.ts 10.37ms
  22/2086 src/_packages/editable.readme.md 3.16ms
  23/2086 src/_packages/fake-llm.index.ts 2.57ms
  24/2086 src/_packages/fake-llm.readme.md 2.50ms
  25/2086 src/_packages/google.index.ts 4.00ms
  26/2086 src/_packages/google.readme.md 8.45ms
  27/2086 src/_packages/javascript.index.ts 3.92ms
  28/2086 src/_packages/javascript.readme.md 5.00ms
  29/2086 src/_packages/legacy-documents.index.ts 3.71ms
  30/2086 src/_packages/legacy-documents.readme.md 2.51ms
  31/2086 src/_packages/markdown-utils.index.ts 10.16ms
  32/2086 src/_packages/markdown-utils.readme.md 11.77ms
  33/2086 src/_packages/markitdown.index.ts 2.23ms
  34/2086 src/_packages/markitdown.readme.md 3.52ms
  35/2086 src/_packages/node.index.ts 7.35ms
  36/2086 src/_packages/node.readme.md 11.91ms
  37/2086 src/_packages/ollama.index.ts 3.68ms
  38/2086 src/_packages/ollama.readme.md 9.83ms
  39/2086 src/_packages/openai.index.ts 6.28ms
  40/2086 src/_packages/openai.readme.md 19.62ms
  41/2086 src/_packages/pdf.index.ts 7.17ms
  42/2086 src/_packages/pdf.readme.md 2.19ms
  43/2086 src/_packages/promptbook.readme.md 4.82ms
  44/2086 src/_packages/ptbk.readme.md 8.11ms
  45/2086 src/_packages/README.md 2.12ms
  46/2086 src/_packages/remote-client.index.ts 3.27ms
  47/2086 src/_packages/remote-client.readme.md 1.64ms
  48/2086 src/_packages/remote-server.index.ts 21.79ms
  49/2086 src/_packages/remote-server.readme.md 4.70ms
  50/2086 src/_packages/templates.index.ts 2.10ms
  51/2086 src/_packages/templates.readme.md 4.48ms
  52/2086 src/_packages/types.index.ts 76.88ms
  53/2086 src/_packages/types.readme.md 24.63ms
  54/2086 src/_packages/utils.index.ts 20.55ms
  55/2086 src/_packages/utils.readme.md 54.61ms
  56/2086 src/_packages/vercel.index.ts 5.91ms
  57/2086 src/_packages/vercel.readme.md 4.71ms
  58/2086 src/_packages/website-crawler.index.ts 2.33ms
  59/2086 src/_packages/website-crawler.readme.md 1.49ms
  60/2086 src/_packages/wizard.index.ts 8.82ms
  61/2086 src/_packages/wizard.readme.md 2.40ms
  62/2086 src/avatars/Avatar.tsx 41.31ms
  63/2086 src/avatars/avatarAnimationScheduler.ts 10.62ms
  64/2086 src/avatars/avatarInteractionUtils.test.ts 5.79ms
  65/2086 src/avatars/avatarInteractionUtils.ts 14.05ms
  66/2086 src/avatars/AvatarOrImage.tsx 8.02ms
  67/2086 src/avatars/avatarPointerTracking.ts 12.73ms
  68/2086 src/avatars/avatarRenderingUtils.test.ts 7.74ms
  69/2086 src/avatars/avatarRenderingUtils.ts 18.64ms
  70/2086 src/avatars/avatarVisibilityTracking.ts 24.54ms
  71/2086 src/avatars/index.ts 7.13ms
  72/2086 src/avatars/renderAvatarVisual.ts 5.31ms
  73/2086 src/avatars/renderAvatarVisualAsciiArt.ts 12.29ms
  74/2086 src/avatars/renderAvatarVisualTerminalText.test.ts 8.38ms
  75/2086 src/avatars/renderAvatarVisualTerminalText.ts 16.29ms
  76/2086 src/avatars/types/AvatarDefinition.ts 2.75ms
  77/2086 src/avatars/types/AvatarVisualDefinition.ts 9.03ms
  78/2086 src/avatars/visuals/asciiOctopusAvatarVisual.ts 90.10ms
  79/2086 src/avatars/visuals/avatar3dProjectionShared.ts 12.48ms
  80/2086 src/avatars/visuals/avatarVisualRegistry.test.ts 4.22ms
  81/2086 src/avatars/visuals/avatarVisualRegistry.ts 5.41ms
  82/2086 src/avatars/visuals/fractalAvatarVisual.ts 18.15ms
  83/2086 src/avatars/visuals/minecraft2AvatarVisual.ts 22.05ms
  84/2086 src/avatars/visuals/minecraftAvatarVisual.ts 9.51ms
  85/2086 src/avatars/visuals/minecraftAvatarVisualShared.ts 8.81ms
  86/2086 src/avatars/visuals/octopus2AvatarVisual.ts 15.70ms
  87/2086 src/avatars/visuals/octopus3AvatarVisual.test.ts 4.70ms
  88/2086 src/avatars/visuals/octopus3AvatarVisual.ts 34.56ms
  89/2086 src/avatars/visuals/octopus3d2AvatarVisual.ts 37.11ms
  90/2086 src/avatars/visuals/octopus3d3AvatarVisual.ts 44.31ms
  91/2086 src/avatars/visuals/octopus3d4AvatarVisual.ts 53.82ms
  92/2086 src/avatars/visuals/octopus3dAvatarVisual.ts 28.49ms
  93/2086 src/avatars/visuals/octopus3dAvatarVisualShared.ts 12.09ms
  94/2086 src/avatars/visuals/octopusAvatarVisual.ts 14.67ms
  95/2086 src/avatars/visuals/octopusAvatarVisualShared.test.ts 16.63ms
  96/2086 src/avatars/visuals/octopusAvatarVisualShared.ts 29.91ms
  97/2086 src/avatars/visuals/orbAvatarVisual.test.ts 4.61ms
  98/2086 src/avatars/visuals/orbAvatarVisual.ts 26.38ms
  99/2086 src/avatars/visuals/pixelArtAvatarVisual.ts 7.40ms
 100/2086 src/book-2.0/agent-source/AgentBasicInformation.ts 12.64ms
 101/2086 src/book-2.0/agent-source/AgentModelRequirements.ts 7.25ms
 102/2086 src/book-2.0/agent-source/AgentReferenceResolutionIssue.ts 5.17ms
 103/2086 src/book-2.0/agent-source/AgentReferenceResolver.ts 6.37ms
 104/2086 src/book-2.0/agent-source/AgentSourceParseResult.ts 6.09ms
 105/2086 src/book-2.0/agent-source/agentSourceVisibility.test.ts 20.19ms
 106/2086 src/book-2.0/agent-source/agentSourceVisibility.ts 12.02ms
 107/2086 src/book-2.0/agent-source/BookEditable.ts 4.40ms
 108/2086 src/book-2.0/agent-source/communication-samples.test.ts 12.63ms
 109/2086 src/book-2.0/agent-source/computeAgentHash.test.ts 2.98ms
 110/2086 src/book-2.0/agent-source/computeAgentHash.ts 2.85ms
 111/2086 src/book-2.0/agent-source/createAgentModelRequirements.commitmentAggregation.test.ts 7.26ms
 112/2086 src/book-2.0/agent-source/createAgentModelRequirements.deduplication.test.ts 9.58ms
 113/2086 src/book-2.0/agent-source/createAgentModelRequirements.tools.test.ts 19.70ms
 114/2086 src/book-2.0/agent-source/createAgentModelRequirements.ts 15.09ms
 115/2086 src/book-2.0/agent-source/createAgentModelRequirements.writing.test.ts 4.37ms
 116/2086 src/book-2.0/agent-source/CreateAgentModelRequirementsOptions.ts 4.58ms
 117/2086 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts 5.83ms
 118/2086 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.blocks.test.ts 3.10ms
 119/2086 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.closed.test.ts 3.55ms
 120/2086 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.delete.test.ts 7.01ms
 121/2086 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.import.test.ts 3.38ms
 122/2086 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.knowledge.test.ts 4.33ms
 123/2086 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.ts 6.44ms
 124/2086 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.unknown.test.ts 4.73ms
 125/2086 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/applyCommitmentsToAgentModelRequirements.ts 14.60ms
 126/2086 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/augmentAgentModelRequirementsFromSource.ts 32.18ms
 127/2086 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/filterCommitmentsForAgentModelRequirements.ts 18.89ms
 128/2086 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/materializeInlineKnowledgeSources.ts 9.66ms
 129/2086 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/ParsedAgentSourceWithCommitments.ts 8.06ms
 130/2086 src/book-2.0/agent-source/createCommitmentRegex.ts 30.43ms
 131/2086 src/book-2.0/agent-source/createDefaultAgentName.ts 2.76ms
 132/2086 src/book-2.0/agent-source/createTeamToolName.ts 5.48ms
 133/2086 src/book-2.0/agent-source/deduplicateSystemMessage.test.ts 30.94ms
 134/2086 src/book-2.0/agent-source/deduplicateSystemMessage.ts 21.34ms
 135/2086 src/book-2.0/agent-source/explicitFromCommitment.ts 6.31ms
 136/2086 src/book-2.0/agent-source/extractAgentReferenceTokens.ts 5.44ms
 137/2086 src/book-2.0/agent-source/extractMetaLinks.ts 3.02ms
 138/2086 src/book-2.0/agent-source/extractOpenTeacherInstructions.test.ts 2.84ms
 139/2086 src/book-2.0/agent-source/extractOpenTeacherInstructions.ts 3.09ms
 140/2086 src/book-2.0/agent-source/getBookCommitmentLineType.ts 4.75ms
 141/2086 src/book-2.0/agent-source/normalizeAgentName.test.ts 3.68ms
 142/2086 src/book-2.0/agent-source/normalizeAgentName.ts 2.70ms
 143/2086 src/book-2.0/agent-source/padBook.ts 3.10ms
 144/2086 src/book-2.0/agent-source/parseAgentSource.import.test.ts 3.81ms
 145/2086 src/book-2.0/agent-source/parseAgentSource.test.ts 17.12ms
 146/2086 src/book-2.0/agent-source/parseAgentSource.ts 9.35ms
 147/2086 src/book-2.0/agent-source/parseAgentSource/applyMetaCommitment.ts 18.76ms
 148/2086 src/book-2.0/agent-source/parseAgentSource/consumeConversationSampleCommitment.ts 14.50ms
 149/2086 src/book-2.0/agent-source/parseAgentSource/createCapabilitiesFromCommitment.ts 16.90ms
 150/2086 src/book-2.0/agent-source/parseAgentSource/ensureMetaFullname.ts 8.57ms
 151/2086 src/book-2.0/agent-source/parseAgentSource/extractAgentProfileText.ts 3.70ms
 152/2086 src/book-2.0/agent-source/parseAgentSource/extractInitialMessage.ts 3.74ms
 153/2086 src/book-2.0/agent-source/parseAgentSource/extractParsedAgentProfile.ts 12.58ms
 154/2086 src/book-2.0/agent-source/parseAgentSource/ParseAgentSourceState.ts 3.19ms
 155/2086 src/book-2.0/agent-source/parseAgentSource/ParsedAgentProfile.ts 3.76ms
 156/2086 src/book-2.0/agent-source/parseAgentSourcePrelude.ts 4.57ms
 157/2086 src/book-2.0/agent-source/parseAgentSourceWithCommitments.blocks.test.ts 3.78ms
 158/2086 src/book-2.0/agent-source/parseAgentSourceWithCommitments.title.test.ts 5.99ms
 159/2086 src/book-2.0/agent-source/parseAgentSourceWithCommitments.ts 8.54ms
 160/2086 src/book-2.0/agent-source/parseAgentSourceWithCommitments.unknown.test.ts 4.26ms
 161/2086 src/book-2.0/agent-source/parseAgentSourceWithCommitments.use.test.ts 52.49ms
 162/2086 src/book-2.0/agent-source/ParsedUnknownCommitment.ts 4.15ms
 163/2086 src/book-2.0/agent-source/parseParameters.ts 7.09ms
 164/2086 src/book-2.0/agent-source/parseTeamCommitment.ts 11.07ms
 165/2086 src/book-2.0/agent-source/pseudoAgentReferences.ts 12.78ms
 166/2086 src/book-2.0/agent-source/README.md 14.49ms
 167/2086 src/book-2.0/agent-source/removeCommentsFromSystemMessage.ts 4.04ms
 168/2086 src/book-2.0/agent-source/resolveInheritedAgentSource.ts 58.75ms
 169/2086 src/book-2.0/agent-source/string_book.ts 5.21ms
 170/2086 src/book-2.0/agent-source/TeammateProfileResolver.ts 6.30ms
 171/2086 src/book-2.0/book-language-documentation/BookLanguageDocumentationExample.ts 2.78ms
 172/2086 src/book-2.0/book-language-documentation/bookLanguageDocumentationExamples.ts 7.24ms
 173/2086 src/book-2.0/book-language-documentation/BookLanguageManualDictionary.ts 19.18ms
 174/2086 src/book-2.0/book-language-documentation/createStandaloneBookLanguageMarkdown.test.ts 13.78ms
 175/2086 src/book-2.0/book-language-documentation/createStandaloneBookLanguageMarkdown.ts 32.01ms
 176/2086 src/book-2.0/book-language-documentation/englishBookLanguageManualDictionary.ts 23.36ms
 177/2086 src/book-2.0/book-language-documentation/getBookLanguageManualCommitmentGroups.ts 8.06ms
 178/2086 src/book-2.0/book-language-documentation/getBookLanguageManualDictionary.ts 21.73ms
 179/2086 src/book-2.0/book-language-documentation/getSafeCodeBlock.ts 6.42ms
 180/2086 src/book-2.0/book-language-documentation/renderCommitmentCatalogSection.ts 5.28ms
 181/2086 src/book-2.0/book-language-documentation/renderGroupedCommitmentDocumentationMarkdown.test.ts 4.74ms
 182/2086 src/book-2.0/book-language-documentation/renderGroupedCommitmentDocumentationMarkdown.ts 19.45ms
 183/2086 src/book-2.0/book-language-documentation/toStableAnchorId.ts 14.95ms
 184/2086 src/book-2.0/README.md 12.81ms
 185/2086 src/book-2.0/utils/generatePlaceholderAgentProfileImageUrl.ts 5.69ms
 186/2086 src/book-3.0/agentFolderPaths.ts 7.65ms
 187/2086 src/book-3.0/AgentMessageRunReport.test.ts 7.57ms
 188/2086 src/book-3.0/AgentMessageRunReport.ts 16.54ms
 189/2086 src/book-3.0/AgentPlannedMessagesSidecar.test.ts 5.32ms
 190/2086 src/book-3.0/AgentPlannedMessagesSidecar.ts 14.05ms
 191/2086 src/book-3.0/AgentTeamConversationWorkspace.ts 10.04ms
 192/2086 src/book-3.0/Book.test.ts 5.60ms
 193/2086 src/book-3.0/Book.ts 8.17ms
 194/2086 src/book-3.0/BookNodeAgentSource.ts 6.75ms
 195/2086 src/book-3.0/CliAgent.test.ts 7.79ms
 196/2086 src/book-3.0/CliAgent.ts 13.58ms
 197/2086 src/book-3.0/cliAgentEnv.ts 3.39ms
 198/2086 src/book-3.0/codexLoginMethod.ts 4.58ms
 199/2086 src/book-3.0/createAgentMessageSidecarBaseName.ts 5.09ms
 200/2086 src/book-3.0/describeAgentPlannedMessageSchedule.ts 5.64ms
 201/2086 src/book-3.0/LiteAgent.test.ts 8.82ms
 202/2086 src/book-3.0/LiteAgent.ts 17.52ms
 203/2086 src/book-components/_common/Dropdown/Dropdown.tsx 3.86ms
 204/2086 src/book-components/_common/HamburgerMenu/HamburgerMenu.tsx 2.95ms
 205/2086 src/book-components/_common/MenuHoisting/MenuHoistingContext.tsx 4.87ms
 206/2086 src/book-components/_common/Modal/Modal.tsx 2.96ms
 207/2086 src/book-components/_common/MonacoEditorWithShadowDom.tsx 2.92ms
 208/2086 src/book-components/_common/react-utils/classNames.ts 2.83ms
 209/2086 src/book-components/_common/react-utils/collectCssTextsForClass.tsx 4.77ms
 210/2086 src/book-components/_common/react-utils/escapeRegex.tsx 2.49ms
 211/2086 src/book-components/_common/Tooltip/Tooltip.tsx 11.62ms
 212/2086 src/book-components/AvatarProfile/AvatarChip/AvatarChip.tsx 6.74ms
 213/2086 src/book-components/AvatarProfile/AvatarChip/AvatarChipFromSource.tsx 3.11ms
 214/2086 src/book-components/AvatarProfile/AvatarChip/index.ts 1.57ms
 215/2086 src/book-components/AvatarProfile/AvatarProfile/AvatarProfile.tsx 5.12ms
 216/2086 src/book-components/AvatarProfile/AvatarProfile/AvatarProfileFromSource.tsx 3.10ms
 217/2086 src/book-components/AvatarProfile/AvatarProfile/AvatarProfileTooltip.tsx 2.85ms
 218/2086 src/book-components/AvatarProfile/README.md 1.69ms
 219/2086 src/book-components/BookEditor/BookEditor.tsx 19.09ms
 220/2086 src/book-components/BookEditor/BookEditorAboutPromptbookInformation.tsx 4.41ms
 221/2086 src/book-components/BookEditor/BookEditorActionbar.tsx 12.50ms
 222/2086 src/book-components/BookEditor/BookEditorBrowserConfig.ts 1.78ms
 223/2086 src/book-components/BookEditor/BookEditorForClient.tsx 5.42ms
 224/2086 src/book-components/BookEditor/BookEditorMonaco.tsx 28.16ms
 225/2086 src/book-components/BookEditor/BookEditorMonacoConstants.ts 2.09ms
 226/2086 src/book-components/BookEditor/BookEditorMonacoFormatting.ts 3.39ms
 227/2086 src/book-components/BookEditor/BookEditorMonacoTokenization.test.ts 3.90ms
 228/2086 src/book-components/BookEditor/BookEditorMonacoTokenization.ts 13.24ms
 229/2086 src/book-components/BookEditor/BookEditorMonacoUploadPanel.tsx 8.34ms
 230/2086 src/book-components/BookEditor/BookEditorSyntaxTokenStyles.ts 5.78ms
 231/2086 src/book-components/BookEditor/BookEditorTheme.ts 5.96ms
 232/2086 src/book-components/BookEditor/createBookSourceHighlightStyles.ts 4.63ms
 233/2086 src/book-components/BookEditor/createDeprecatedCommitmentDiagnostics.browser.ts 3.06ms
 234/2086 src/book-components/BookEditor/createDeprecatedCommitmentDiagnostics.test.ts 7.93ms
 235/2086 src/book-components/BookEditor/createDeprecatedCommitmentDiagnostics.ts 5.16ms
 236/2086 src/book-components/BookEditor/createDuplicateFromCommitmentDiagnostics.test.ts 4.81ms
 237/2086 src/book-components/BookEditor/createDuplicateFromCommitmentDiagnostics.ts 5.00ms
 238/2086 src/book-components/BookEditor/createUnknownCommitmentDiagnostics.test.ts 3.08ms
 239/2086 src/book-components/BookEditor/createUnknownCommitmentDiagnostics.ts 6.48ms
 240/2086 src/book-components/BookEditor/highlightBookSourceToHtml.ts 9.00ms
 241/2086 src/book-components/BookEditor/useBookEditorMonacoDecorations.ts 8.15ms
 242/2086 src/book-components/BookEditor/useBookEditorMonacoDiagnostics.ts 3.94ms
 243/2086 src/book-components/BookEditor/useBookEditorMonacoInteractions.ts 13.12ms
 244/2086 src/book-components/BookEditor/useBookEditorMonacoLanguage.test.ts 10.89ms
 245/2086 src/book-components/BookEditor/useBookEditorMonacoLanguage.ts 20.62ms
 246/2086 src/book-components/BookEditor/useBookEditorMonacoLifecycle.ts 11.36ms
 247/2086 src/book-components/BookEditor/useBookEditorMonacoStyles.ts 5.26ms
 248/2086 src/book-components/BookEditor/useBookEditorMonacoUploads.ts 5.67ms
 249/2086 src/book-components/BookEditor/useBookEditorMonacoUploads/bookEditorMonacoUploadTypes.ts 6.95ms
 250/2086 src/book-components/BookEditor/useBookEditorMonacoUploads/clearScheduledTimer.ts 2.31ms
 251/2086 src/book-components/BookEditor/useBookEditorMonacoUploads/enqueueFilesForUpload.ts 10.54ms
 252/2086 src/book-components/BookEditor/useBookEditorMonacoUploads/useBookEditorMonacoUploadEditorSync.ts 6.34ms
 253/2086 src/book-components/BookEditor/useBookEditorMonacoUploads/useBookEditorMonacoUploadItemsState.ts 5.42ms
 254/2086 src/book-components/BookEditor/useBookEditorMonacoUploads/useBookEditorMonacoUploadProgressQueue.ts 6.79ms
 255/2086 src/book-components/BookEditor/useBookEditorMonacoUploads/useBookEditorMonacoUploadQueue.ts 15.30ms
 256/2086 src/book-components/BookEditor/useBookEditorMonacoUploads/useCompletedUploadsAutoClear.ts 4.49ms
 257/2086 src/book-components/Chat/AgentChat/AgentChat.test.tsx 9.48ms
 258/2086 src/book-components/Chat/AgentChat/AgentChat.tsx 8.40ms
 259/2086 src/book-components/Chat/AgentChat/AgentChatProps.tsx 3.39ms
 260/2086 src/book-components/Chat/AgentChip/AgentChip.tsx 7.61ms
 261/2086 src/book-components/Chat/AgentChip/index.ts 1.40ms
 262/2086 src/book-components/Chat/Chat/Chat.tsx 27.33ms
 263/2086 src/book-components/Chat/Chat/ChatActionsBar.test.tsx 8.05ms
 264/2086 src/book-components/Chat/Chat/ChatActionsBar.tsx 14.77ms
 265/2086 src/book-components/Chat/Chat/ChatCitationModal.test.tsx 5.55ms
 266/2086 src/book-components/Chat/Chat/ChatCitationModal.tsx 13.54ms
 267/2086 src/book-components/Chat/Chat/chatCssClassNames.ts 9.55ms
 268/2086 src/book-components/Chat/Chat/ChatFeedbackStar.tsx 4.34ms
 269/2086 src/book-components/Chat/Chat/ChatImageAttachmentModal.tsx 6.03ms
 270/2086 src/book-components/Chat/Chat/ChatInputArea.test.tsx 10.55ms
 271/2086 src/book-components/Chat/Chat/ChatInputArea.tsx 36.31ms
 272/2086 src/book-components/Chat/Chat/ChatInputAreaDictationPanel.tsx 11.01ms
 273/2086 src/book-components/Chat/Chat/ChatInputUploadedFile.ts 2.15ms
 274/2086 src/book-components/Chat/Chat/ChatMessageAttachments.tsx 10.17ms
 275/2086 src/book-components/Chat/Chat/ChatMessageItem.test.tsx 25.77ms
 276/2086 src/book-components/Chat/Chat/ChatMessageItem.tsx 56.33ms
 277/2086 src/book-components/Chat/Chat/ChatMessageList.tsx 10.39ms
 278/2086 src/book-components/Chat/Chat/ChatMessageMap.tsx 18.01ms
 279/2086 src/book-components/Chat/Chat/ChatMessageRichContent.tsx 14.45ms
 280/2086 src/book-components/Chat/Chat/ChatMessageToolCallChips.tsx 7.16ms
 281/2086 src/book-components/Chat/Chat/ChatProps.tsx 42.37ms
 282/2086 src/book-components/Chat/Chat/ChatRatingModal.test.tsx 8.74ms
 283/2086 src/book-components/Chat/Chat/ChatRatingModal.tsx 14.85ms
 284/2086 src/book-components/Chat/Chat/ChatReplyPreview.tsx 11.43ms
 285/2086 src/book-components/Chat/Chat/ChatSelfLearningSummary.ts 18.16ms
 286/2086 src/book-components/Chat/Chat/ChatSoundToggle.tsx 17.06ms
 287/2086 src/book-components/Chat/Chat/ChatToolCallModal.test.tsx 14.00ms
 288/2086 src/book-components/Chat/Chat/ChatToolCallModal.tsx 13.80ms
 289/2086 src/book-components/Chat/Chat/ChatToolCallModalComponents.tsx 6.23ms
 290/2086 src/book-components/Chat/Chat/ChatToolCallModalContent.tsx 7.57ms
 291/2086 src/book-components/Chat/Chat/CitationIframePreview.test.tsx 9.87ms
 292/2086 src/book-components/Chat/Chat/CitationIframePreview.tsx 6.00ms
 293/2086 src/book-components/Chat/Chat/ClockIcon.tsx 7.50ms
 294/2086 src/book-components/Chat/Chat/constants.tsx 3.03ms
 295/2086 src/book-components/Chat/Chat/createChatMessageToolCallRenderModel.ts 14.51ms
 296/2086 src/book-components/Chat/Chat/createProgressCardChecklistMarkdown.ts 5.68ms
 297/2086 src/book-components/Chat/Chat/ImagePromptRenderer.tsx 7.19ms
 298/2086 src/book-components/Chat/Chat/insertDictationChunk.test.ts 5.78ms
 299/2086 src/book-components/Chat/Chat/insertDictationChunk.ts 3.55ms
 300/2086 src/book-components/Chat/Chat/learnDictationDictionary.test.ts 3.76ms
 301/2086 src/book-components/Chat/Chat/learnDictationDictionary.ts 3.18ms
 302/2086 src/book-components/Chat/Chat/pagePreview/createPagePreviewSessionId.ts 4.30ms
 303/2086 src/book-components/Chat/Chat/pagePreview/LiveBrowserPreview.test.tsx 20.57ms
 304/2086 src/book-components/Chat/Chat/pagePreview/LiveBrowserPreview.tsx 30.75ms
 305/2086 src/book-components/Chat/Chat/pagePreview/LiveBrowserPreviewToolbar.tsx 11.82ms
 306/2086 src/book-components/Chat/Chat/pagePreview/normalizePagePreviewInputEvent.test.ts 5.51ms
 307/2086 src/book-components/Chat/Chat/pagePreview/normalizePagePreviewInputEvent.ts 8.87ms
 308/2086 src/book-components/Chat/Chat/pagePreview/PagePreviewInputEvent.ts 11.02ms
 309/2086 src/book-components/Chat/Chat/pagePreview/PagePreviewInputQueue.test.ts 10.57ms
 310/2086 src/book-components/Chat/Chat/pagePreview/PagePreviewInputQueue.ts 8.58ms
 311/2086 src/book-components/Chat/Chat/pagePreview/PagePreviewSessionState.ts 3.45ms
 312/2086 src/book-components/Chat/Chat/pagePreview/PagePreviewViewport.ts 7.00ms
 313/2086 src/book-components/Chat/Chat/refineFinalDictationChunk.test.ts 3.49ms
 314/2086 src/book-components/Chat/Chat/refineFinalDictationChunk.ts 11.61ms
 315/2086 src/book-components/Chat/Chat/renderAdvancedToolCallDetails.tsx 25.09ms
 316/2086 src/book-components/Chat/Chat/renderAgentProjectDiff.tsx 12.82ms
 317/2086 src/book-components/Chat/Chat/renderAgentProjectToolCallDetails.tsx 12.13ms
 318/2086 src/book-components/Chat/Chat/renderEmailToolCallDetails.tsx 13.25ms
 319/2086 src/book-components/Chat/Chat/renderMemoryToolCallDetails.tsx 17.80ms
 320/2086 src/book-components/Chat/Chat/renderPopupToolCallDetails.tsx 6.40ms
 321/2086 src/book-components/Chat/Chat/renderRunBrowserToolCallDetails.tsx 19.82ms
 322/2086 src/book-components/Chat/Chat/renderSearchToolCallDetails.tsx 5.68ms
 323/2086 src/book-components/Chat/Chat/renderSelfLearningToolCallDetails.tsx 8.85ms
 324/2086 src/book-components/Chat/Chat/renderTimeoutToolCallDetails.tsx 17.15ms
 325/2086 src/book-components/Chat/Chat/renderTimeToolCallDetails.tsx 7.46ms
 326/2086 src/book-components/Chat/Chat/renderToolCallClockPanel.tsx 3.73ms
 327/2086 src/book-components/Chat/Chat/renderToolCallDetails.tsx 23.48ms
 328/2086 src/book-components/Chat/Chat/renderToolCallProgressPlaceholder.tsx 3.92ms
 329/2086 src/book-components/Chat/Chat/renderWalletCredentialToolCallDetails.tsx 9.55ms
 330/2086 src/book-components/Chat/Chat/resolveChatRatingMessageKey.ts 2.36ms
 331/2086 src/book-components/Chat/Chat/resolveRunBrowserToolCallDetailsState.ts 13.27ms
 332/2086 src/book-components/Chat/Chat/resolveSpeechRecognitionUiDescriptor.ts 3.50ms
 333/2086 src/book-components/Chat/Chat/resolveToolCallProgressMessage.ts 4.08ms
 334/2086 src/book-components/Chat/Chat/StreamingFeaturePlaceholder.tsx 4.97ms
 335/2086 src/book-components/Chat/Chat/TeamToolCallModalContent.test.tsx 9.93ms
 336/2086 src/book-components/Chat/Chat/TeamToolCallModalContent.tsx 21.93ms
 337/2086 src/book-components/Chat/Chat/useChatInputAreaAttachments.ts 6.23ms
 338/2086 src/book-components/Chat/Chat/useChatInputAreaComposer.draftMessage.test.tsx 4.43ms
 339/2086 src/book-components/Chat/Chat/useChatInputAreaComposer.ts 22.87ms
 340/2086 src/book-components/Chat/Chat/useChatInputAreaDictation.ts 21.56ms
 341/2086 src/book-components/Chat/Chat/useChatInputAreaDictationPersistence.ts 4.97ms
 342/2086 src/book-components/Chat/Chat/useChatInputAreaDictationSupport.ts 4.36ms
 343/2086 src/book-components/Chat/Chat/useChatMessageAvatarTooltip.ts 5.48ms
 344/2086 src/book-components/Chat/Chat/useChatMessageSpeechPlayback.ts 7.37ms
 345/2086 src/book-components/Chat/Chat/useChatPostprocessedMessages.ts 4.13ms
 346/2086 src/book-components/Chat/Chat/useChatScrollState.ts 11.46ms
 347/2086 src/book-components/Chat/Chat/useChatToolCallModalState.ts 14.52ms
 348/2086 src/book-components/Chat/Chat/useChatToolCallState.ts 5.84ms
 349/2086 src/book-components/Chat/CodeBlock/CodeBlock.tsx 13.49ms
 350/2086 src/book-components/Chat/CodeBlock/resolveCodeBlockLanguage.test.ts 2.64ms
 351/2086 src/book-components/Chat/CodeBlock/resolveCodeBlockLanguage.ts 5.26ms
 352/2086 src/book-components/Chat/effects/ChatEffectsSystem.tsx 9.02ms
 353/2086 src/book-components/Chat/effects/components/ConfettiEffect.tsx 4.19ms
 354/2086 src/book-components/Chat/effects/components/HeartsEffect.tsx 4.63ms
 355/2086 src/book-components/Chat/effects/configs/defaultEffectConfigs.ts 2.36ms
 356/2086 src/book-components/Chat/effects/index.ts 2.83ms
 357/2086 src/book-components/Chat/effects/types/ChatEffect.ts 2.89ms
 358/2086 src/book-components/Chat/effects/types/ChatEffectConfig.ts 2.85ms
 359/2086 src/book-components/Chat/effects/types/ChatEffectsSystemProps.ts 11.02ms
 360/2086 src/book-components/Chat/effects/types/ChatEffectType.ts 3.62ms
 361/2086 src/book-components/Chat/effects/utils/detectEffects.ts 12.93ms
 362/2086 src/book-components/Chat/hooks/index.ts 1.61ms
 363/2086 src/book-components/Chat/hooks/useChatActionsOverlap.ts 12.05ms
 364/2086 src/book-components/Chat/hooks/useChatAutoScroll.test.tsx 11.72ms
 365/2086 src/book-components/Chat/hooks/useChatAutoScroll.ts 16.94ms
 366/2086 src/book-components/Chat/hooks/useChatCompleteNotification.test.tsx 10.64ms
 367/2086 src/book-components/Chat/hooks/useChatCompleteNotification.ts 10.52ms
 368/2086 src/book-components/Chat/hooks/useChatRatings.ts 14.59ms
 369/2086 src/book-components/Chat/hooks/useResolvedCitationLabel.test.tsx 4.47ms
 370/2086 src/book-components/Chat/hooks/useResolvedCitationLabel.ts 146.38ms
 371/2086 src/book-components/Chat/hooks/useSendMessageToLlmChat.ts 7.19ms
 372/2086 src/book-components/Chat/LlmChat/defaults.ts 2.24ms
 373/2086 src/book-components/Chat/LlmChat/FriendlyErrorMessage.ts 2.06ms
 374/2086 src/book-components/Chat/LlmChat/LlmChat.test.tsx 13.04ms
 375/2086 src/book-components/Chat/LlmChat/LlmChat.tsx 29.84ms
 376/2086 src/book-components/Chat/LlmChat/LlmChatProps.tsx 11.74ms
 377/2086 src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts 31.25ms
 378/2086 src/book-components/Chat/LlmChat/useLlmChatMessages.ts 6.49ms
 379/2086 src/book-components/Chat/LlmChat/useLlmChatState.ts 19.10ms
 380/2086 src/book-components/Chat/MarkdownContent/MarkdownContent.test.tsx 13.81ms
 381/2086 src/book-components/Chat/MarkdownContent/MarkdownContent.tsx 20.02ms
 382/2086 src/book-components/Chat/MockedChat/constants.ts 4.67ms
 383/2086 src/book-components/Chat/MockedChat/MockedChat.test.tsx 10.02ms
 384/2086 src/book-components/Chat/MockedChat/MockedChat.tsx 28.61ms
 385/2086 src/book-components/Chat/README.md 6.81ms
 386/2086 src/book-components/Chat/save/_common/chatExportRendering.ts 19.95ms
 387/2086 src/book-components/Chat/save/_common/ChatSaveFormatDefinition.ts 7.38ms
 388/2086 src/book-components/Chat/save/_common/ChatSaveFormatHandler.ts 3.01ms
 389/2086 src/book-components/Chat/save/_common/createChatExportFilename.ts 3.23ms
 390/2086 src/book-components/Chat/save/_common/getChatSaveFormatDefinitions.ts 3.13ms
 391/2086 src/book-components/Chat/save/_common/getPromptbookExportBranding.ts 14.22ms
 392/2086 src/book-components/Chat/save/_common/string_chat_format_name.ts 1.97ms
 393/2086 src/book-components/Chat/save/html/htmlSaveFormatDefinition.test.ts 7.01ms
 394/2086 src/book-components/Chat/save/html/htmlSaveFormatDefinition.ts 46.27ms
 395/2086 src/book-components/Chat/save/index.ts 8.41ms
 396/2086 src/book-components/Chat/save/json/jsonSaveFormatDefinition.ts 4.18ms
 397/2086 src/book-components/Chat/save/markdown/mdSaveFormatDefinition.test.ts 6.37ms
 398/2086 src/book-components/Chat/save/markdown/mdSaveFormatDefinition.ts 9.41ms
 399/2086 src/book-components/Chat/save/pdf/buildChatPdf.ts 17.98ms
 400/2086 src/book-components/Chat/save/pdf/pdfSaveFormatDefinition.test.ts 14.34ms
 401/2086 src/book-components/Chat/save/pdf/pdfSaveFormatDefinition.ts 4.57ms
 402/2086 src/book-components/Chat/save/react/exports/chat-preview-2025-10-13 (1).jsx 6.78ms
 403/2086 src/book-components/Chat/save/react/reactSaveFormatDefinition.test.ts 4.18ms
 404/2086 src/book-components/Chat/save/react/reactSaveFormatDefinition.ts 5.61ms
 405/2086 src/book-components/Chat/save/text/txtSaveFormatDefinition.ts 2.30ms
 406/2086 src/book-components/Chat/SourceChip/index.ts 2.31ms
 407/2086 src/book-components/Chat/SourceChip/SourceChip.test.tsx 10.81ms
 408/2086 src/book-components/Chat/SourceChip/SourceChip.tsx 14.74ms
 409/2086 src/book-components/Chat/types/ChatMessage.ts 14.22ms
 410/2086 src/book-components/Chat/types/ChatParticipant.ts 4.12ms
 411/2086 src/book-components/Chat/types/CitationLabelResolver.ts 2.00ms
 412/2086 src/book-components/Chat/utils/$provideServerDomWindow.ts 8.38ms
 413/2086 src/book-components/Chat/utils/addUtmParamsToUrl.ts 2.57ms
 414/2086 src/book-components/Chat/utils/agentProjectToolCall.test.ts 6.46ms
 415/2086 src/book-components/Chat/utils/agentProjectToolCall.ts 8.53ms
 416/2086 src/book-components/Chat/utils/ChatPersistence.test.ts 5.70ms
 417/2086 src/book-components/Chat/utils/ChatPersistence.ts 6.71ms
 418/2086 src/book-components/Chat/utils/citationHelpers.test.ts 10.70ms
 419/2086 src/book-components/Chat/utils/citationHelpers.ts 9.53ms
 420/2086 src/book-components/Chat/utils/collectTeamToolCallSummary.ts 10.62ms
 421/2086 src/book-components/Chat/utils/createCitationFootnoteRenderModel.test.ts 3.56ms
 422/2086 src/book-components/Chat/utils/createCitationFootnoteRenderModel.ts 11.61ms
 423/2086 src/book-components/Chat/utils/createShortLinkForChat.ts 2.50ms
 424/2086 src/book-components/Chat/utils/createTeamToolNameFromUrl.ts 2.79ms
 425/2086 src/book-components/Chat/utils/decodeJsonUnicodeEscapesInMarkdownText.test.ts 3.91ms
 426/2086 src/book-components/Chat/utils/decodeJsonUnicodeEscapesInMarkdownText.ts 10.73ms
 427/2086 src/book-components/Chat/utils/downloadFile.ts 3.74ms
 428/2086 src/book-components/Chat/utils/exportChatHistory.ts 8.23ms
 429/2086 src/book-components/Chat/utils/ExportFormat.ts 1.59ms
 430/2086 src/book-components/Chat/utils/externalSourceToolCall.ts 4.06ms
 431/2086 src/book-components/Chat/utils/formatToolCallDateTime.ts 5.18ms
 432/2086 src/book-components/Chat/utils/formatToolCallLocalTime.ts 2.21ms
 433/2086 src/book-components/Chat/utils/formatToolCallTranslationTemplate.ts 2.17ms
 434/2086 src/book-components/Chat/utils/generatePdfContent.ts 6.45ms
 435/2086 src/book-components/Chat/utils/generateQrDataUrl.ts 1.85ms
 436/2086 src/book-components/Chat/utils/getChatMessageTimingDisplay.ts 3.53ms
 437/2086 src/book-components/Chat/utils/getPromptbookBranding.ts 2.00ms
 438/2086 src/book-components/Chat/utils/getToolCallChipletInfo.test.ts 4.21ms
 439/2086 src/book-components/Chat/utils/getToolCallChipletInfo.timeout.test.ts 4.30ms
 440/2086 src/book-components/Chat/utils/getToolCallChipletInfo.ts 23.50ms
 441/2086 src/book-components/Chat/utils/isVisibleChatToolCall.ts 2.09ms
 442/2086 src/book-components/Chat/utils/loadAgentProfile.ts 7.14ms
 443/2086 src/book-components/Chat/utils/messagesToHtml.ts 6.58ms
 444/2086 src/book-components/Chat/utils/messagesToJson.ts 2.62ms
 445/2086 src/book-components/Chat/utils/messagesToMarkdown.ts 4.21ms
 446/2086 src/book-components/Chat/utils/messagesToText.ts 2.75ms
 447/2086 src/book-components/Chat/utils/parseCitationMarker.ts 4.79ms
 448/2086 src/book-components/Chat/utils/parseCitationsFromContent.test.ts 4.22ms
 449/2086 src/book-components/Chat/utils/parseCitationsFromContent.ts 5.06ms
 450/2086 src/book-components/Chat/utils/parseImagePrompts.test.ts 2.40ms
 451/2086 src/book-components/Chat/utils/parseImagePrompts.ts 5.33ms
 452/2086 src/book-components/Chat/utils/parseMessageButtons.test.ts 3.08ms
 453/2086 src/book-components/Chat/utils/parseMessageButtons.ts 3.78ms
 454/2086 src/book-components/Chat/utils/renderMarkdown.test.ts 5.34ms
 455/2086 src/book-components/Chat/utils/renderMarkdown.ts 44.54ms
 456/2086 src/book-components/Chat/utils/resolveChatMessageReplyPreviewText.ts 5.07ms
 457/2086 src/book-components/Chat/utils/resolveChatMessageReplySenderLabel.ts 2.61ms
 458/2086 src/book-components/Chat/utils/resolveCitationSourceDisplay.ts 21.57ms
 459/2086 src/book-components/Chat/utils/resolveCitationUrl.test.ts 4.65ms
 460/2086 src/book-components/Chat/utils/resolveCitationUrl.ts 7.29ms
 461/2086 src/book-components/Chat/utils/resolveToolCallFromChatMessages.ts 2.84ms
 462/2086 src/book-components/Chat/utils/resolveToolCallState.ts 2.04ms
 463/2086 src/book-components/Chat/utils/sanitizeStreamingMessageContent.test.ts 3.73ms
 464/2086 src/book-components/Chat/utils/sanitizeStreamingMessageContent.ts 5.51ms
 465/2086 src/book-components/Chat/utils/splitMessageContentIntoSegments.test.ts 4.15ms
 466/2086 src/book-components/Chat/utils/splitMessageContentIntoSegments.ts 12.94ms
 467/2086 src/book-components/Chat/utils/thinkingMessageVariants.test.ts 4.49ms
 468/2086 src/book-components/Chat/utils/thinkingMessageVariants.ts 4.23ms
 469/2086 src/book-components/Chat/utils/timeoutToolCallPresentation.test.ts 5.90ms
 470/2086 src/book-components/Chat/utils/timeoutToolCallPresentation.ts 20.55ms
 471/2086 src/book-components/Chat/utils/toolCallParsing.test.ts 5.35ms
 472/2086 src/book-components/Chat/utils/toolCallParsing.ts 4.80ms
 473/2086 src/book-components/Chat/utils/toolCallParsing/extractSearchResults.ts 4.89ms
 474/2086 src/book-components/Chat/utils/toolCallParsing/getToolCallResultDate.ts 2.60ms
 475/2086 src/book-components/Chat/utils/toolCallParsing/getToolCallTimestamp.ts 3.27ms
 476/2086 src/book-components/Chat/utils/toolCallParsing/parseRunBrowserToolResult.ts 14.65ms
 477/2086 src/book-components/Chat/utils/toolCallParsing/parseTeamToolResult.ts 2.12ms
 478/2086 src/book-components/Chat/utils/toolCallParsing/parseToolCallArguments.ts 2.17ms
 479/2086 src/book-components/Chat/utils/toolCallParsing/parseToolCallResult.ts 2.67ms
 480/2086 src/book-components/Chat/utils/toolCallParsing/resolveRunBrowserArtifactUrl.ts 2.95ms
 481/2086 src/book-components/Chat/utils/toolCallParsing/RunBrowserToolResult.ts 4.79ms
 482/2086 src/book-components/Chat/utils/toolCallParsing/TeamToolResult.ts 3.64ms
 483/2086 src/book-components/Chat/utils/walletCredentialToolCall.test.ts 6.99ms
 484/2086 src/book-components/Chat/utils/walletCredentialToolCall.ts 18.57ms
 485/2086 src/book-components/fonts/README.md 1.77ms
 486/2086 src/book-components/icons/AboutIcon.tsx 2.64ms
 487/2086 src/book-components/icons/ArrowIcon.tsx 4.42ms
 488/2086 src/book-components/icons/AttachmentIcon.tsx 3.37ms
 489/2086 src/book-components/icons/CameraIcon.tsx 4.05ms
 490/2086 src/book-components/icons/CloseIcon.tsx 2.22ms
 491/2086 src/book-components/icons/DownloadIcon.tsx 2.74ms
 492/2086 src/book-components/icons/EmailIcon.tsx 2.52ms
 493/2086 src/book-components/icons/ExitFullscreenIcon.tsx 3.86ms
 494/2086 src/book-components/icons/FullscreenIcon.tsx 2.47ms
 495/2086 src/book-components/icons/MenuIcon.tsx 3.79ms
 496/2086 src/book-components/icons/MicIcon.tsx 1.95ms
 497/2086 src/book-components/icons/PauseIcon.tsx 1.72ms
 498/2086 src/book-components/icons/PlayIcon.tsx 2.17ms
 499/2086 src/book-components/icons/ResetIcon.tsx 2.13ms
 500/2086 src/book-components/icons/SaveIcon.tsx 3.25ms
 501/2086 src/book-components/icons/SendIcon.tsx 2.46ms
 502/2086 src/book-components/icons/SolidArrowButton.tsx 3.42ms
 503/2086 src/book-components/icons/StopIcon.tsx 1.79ms
 504/2086 src/book-components/icons/TeacherIcon.tsx 4.84ms
 505/2086 src/book-components/icons/TemplateIcon.tsx 1.87ms
 506/2086 src/book-components/PromptbookAgent/PromptbookAgentIntegration.tsx 6.44ms
 507/2086 src/book-components/PromptbookAgent/PromptbookAgentSeamlessIntegration.tsx 24.57ms
 508/2086 src/book-components/Qr/BrandedQrCode.tsx 2.65ms
 509/2086 src/book-components/Qr/GenericQrCode.tsx 2.04ms
 510/2086 src/book-components/Qr/PromptbookQrCode.tsx 44.95ms
 511/2086 src/book-components/Qr/useQrCode.ts 9.20ms
 512/2086 src/cli/$initializePromptbookCliProgram.ts 6.27ms
 513/2086 src/cli/$runPromptbookCli.ts 2.40ms
 514/2086 src/cli/cli-commands/_boilerplate.ts 3.29ms
 515/2086 src/cli/cli-commands/about.ts 4.33ms
 516/2086 src/cli/cli-commands/agent-folder.ts 74.19ms
 517/2086 src/cli/cli-commands/agent-folder/agentProjectPaths.ts 20.15ms
 518/2086 src/cli/cli-commands/agent-folder/agentRunCliOptions.ts 7.67ms
 519/2086 src/cli/cli-commands/agent-folder/init.ts 4.21ms
 520/2086 src/cli/cli-commands/agent-folder/initializeAgentProjectConfiguration.ts 9.90ms
 521/2086 src/cli/cli-commands/agent-folder/initializeAgentRunnerCommand.ts 12.44ms
 522/2086 src/cli/cli-commands/agent-folder/printAgentInitializationSummary.ts 7.08ms
 523/2086 src/cli/cli-commands/agent-folder/run.test.ts 10.91ms
 524/2086 src/cli/cli-commands/agent-folder/run.ts 3.89ms
 525/2086 src/cli/cli-commands/agent-folder/runMultiple.ts 33.90ms
 526/2086 src/cli/cli-commands/agent-folder/tick.ts 4.32ms
 527/2086 src/cli/cli-commands/agent.ts 3.45ms
 528/2086 src/cli/cli-commands/agent/agentCliOptions.ts 5.15ms
 529/2086 src/cli/cli-commands/agent/chat.ts 4.75ms
 530/2086 src/cli/cli-commands/agent/exec.ts 7.62ms
 531/2086 src/cli/cli-commands/agent/run.test.ts 7.50ms
 532/2086 src/cli/cli-commands/agents-server.ts 3.60ms
 533/2086 src/cli/cli-commands/agents-server/buildAgentsServer.test.ts 14.57ms
 534/2086 src/cli/cli-commands/agents-server/buildAgentsServer.ts 17.03ms
 535/2086 src/cli/cli-commands/agents-server/buildAgentsServer/AGENTS_SERVER_BUILD_CACHE_FILENAME.ts 1.94ms
 536/2086 src/cli/cli-commands/agents-server/buildAgentsServer/AGENTS_SERVER_BUILD_CACHE_VERSION.ts 1.58ms
 537/2086 src/cli/cli-commands/agents-server/buildAgentsServer/AGENTS_SERVER_NEXT_BUILD_ID_FILENAME.ts 1.72ms
 538/2086 src/cli/cli-commands/agents-server/buildAgentsServer/AgentsServerBuildCache.ts 4.93ms
 539/2086 src/cli/cli-commands/agents-server/buildAgentsServer/AgentsServerBuildCacheOptions.ts 2.13ms
 540/2086 src/cli/cli-commands/agents-server/buildAgentsServer/createAgentsServerBuildSourceFingerprint.ts 19.91ms
 541/2086 src/cli/cli-commands/agents-server/buildAgentsServer/createAgentsServerRuntimeEnvironment.ts 17.87ms
 542/2086 src/cli/cli-commands/agents-server/buildAgentsServer/ensureAgentsServerBuild.ts 8.35ms
 543/2086 src/cli/cli-commands/agents-server/buildAgentsServer/getAgentsServerBuildInputRelativePaths.ts 3.47ms
 544/2086 src/cli/cli-commands/agents-server/buildAgentsServer/isAgentsServerAppPath.ts 2.91ms
 545/2086 src/cli/cli-commands/agents-server/buildAgentsServer/isAgentsServerAppPathMaterialized.ts 2.51ms
 546/2086 src/cli/cli-commands/agents-server/buildAgentsServer/isAgentsServerBuildCacheCurrent.ts 9.68ms
 547/2086 src/cli/cli-commands/agents-server/buildAgentsServer/isAgentsServerBuildInputTestFile.ts 8.82ms
 548/2086 src/cli/cli-commands/agents-server/buildAgentsServer/isExcludedAgentsServerBuildInputDirectoryName.ts 3.02ms
 549/2086 src/cli/cli-commands/agents-server/buildAgentsServer/isExcludedAgentsServerBuildInputPath.ts 9.57ms
 550/2086 src/cli/cli-commands/agents-server/buildAgentsServer/isExcludedAgentsServerRuntimeSourcePath.ts 8.38ms
 551/2086 src/cli/cli-commands/agents-server/buildAgentsServer/isFile.ts 8.53ms
 552/2086 src/cli/cli-commands/agents-server/buildAgentsServer/isPathInsideNodeModules.ts 20.04ms
 553/2086 src/cli/cli-commands/agents-server/buildAgentsServer/NODE_MODULES_DIRECTORY_NAME.ts 1.90ms
 554/2086 src/cli/cli-commands/agents-server/buildAgentsServer/normalizeAgentsServerBuildInputPath.ts 2.31ms
 555/2086 src/cli/cli-commands/agents-server/buildAgentsServer/prepareAgentsServerRuntime.ts 6.32ms
 556/2086 src/cli/cli-commands/agents-server/buildAgentsServer/PreparedAgentsServerRuntime.ts 3.06ms
 557/2086 src/cli/cli-commands/agents-server/buildAgentsServer/readAgentsServerBuildCache.ts 9.63ms
 558/2086 src/cli/cli-commands/agents-server/buildAgentsServer/resolveAgentsServerAppPath.ts 2.61ms
 559/2086 src/cli/cli-commands/agents-server/buildAgentsServer/resolveAgentsServerBuildAppPath.ts 4.28ms
 560/2086 src/cli/cli-commands/agents-server/buildAgentsServer/resolveAgentsServerBuildOutputPath.ts 7.94ms
 561/2086 src/cli/cli-commands/agents-server/buildAgentsServer/resolveNextCliPath.ts 4.74ms
 562/2086 src/cli/cli-commands/agents-server/buildAgentsServer/resolveNodeModulesPath.ts 4.86ms
 563/2086 src/cli/cli-commands/agents-server/buildAgentsServer/runNextBuild.ts 11.95ms
 564/2086 src/cli/cli-commands/agents-server/buildAgentsServer/shouldCopyAgentsServerRuntimePath.ts 7.36ms
 565/2086 src/cli/cli-commands/agents-server/buildAgentsServer/synchronizeMaterializedAgentsServerRuntime.ts 12.71ms
 566/2086 src/cli/cli-commands/agents-server/buildAgentsServer/writeAgentsServerBuildCache.ts 2.99ms
 567/2086 src/cli/cli-commands/agents-server/ensureAgentsServerEnvFile.ts 6.30ms
 568/2086 src/cli/cli-commands/agents-server/ensureAgentsServerGitignoreFile.ts 3.76ms
 569/2086 src/cli/cli-commands/agents-server/init.test.ts 7.92ms
 570/2086 src/cli/cli-commands/agents-server/init.ts 3.43ms
 571/2086 src/cli/cli-commands/agents-server/initializeAgentsServerProjectConfiguration.ts 4.22ms
 572/2086 src/cli/cli-commands/agents-server/printAgentsServerInitializationSummary.ts 5.57ms
 573/2086 src/cli/cli-commands/agents-server/run.test.ts 13.12ms
 574/2086 src/cli/cli-commands/agents-server/run.ts 10.16ms
 575/2086 src/cli/cli-commands/agents-server/startAgentsServer.ts 9.63ms
 576/2086 src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerChildEnvironment.test.ts 4.48ms
 577/2086 src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerChildEnvironment.ts 4.96ms
 578/2086 src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerLogStreams.ts 4.24ms
 579/2086 src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerRuntimePaths.ts 4.15ms
 580/2086 src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerSupervisorState.ts 3.70ms
 581/2086 src/cli/cli-commands/agents-server/startAgentsServer/createInternalRouteErrorMessage.ts 2.52ms
 582/2086 src/cli/cli-commands/agents-server/startAgentsServer/createLocalAgentRunOptions.ts 3.72ms
 583/2086 src/cli/cli-commands/agents-server/startAgentsServer/forwardChildOutput.ts 2.72ms
 584/2086 src/cli/cli-commands/agents-server/startAgentsServer/loadAgentsServerProjectEnvironment.ts 1.92ms
 585/2086 src/cli/cli-commands/agents-server/startAgentsServer/LocalAgentRunnerLimits.ts 1.88ms
 586/2086 src/cli/cli-commands/agents-server/startAgentsServer/prepareAgentsServerLaunch.ts 6.46ms
 587/2086 src/cli/cli-commands/agents-server/startAgentsServer/PreparedAgentsServerLaunch.ts 2.92ms
 588/2086 src/cli/cli-commands/agents-server/startAgentsServer/readInternalRouteErrorDetails.ts 2.88ms
 589/2086 src/cli/cli-commands/agents-server/startAgentsServer/resolveAgentsServerChildHostname.ts 3.36ms
 590/2086 src/cli/cli-commands/agents-server/startAgentsServer/StartAgentsServerOptions.ts 2.82ms
 591/2086 src/cli/cli-commands/agents-server/startAgentsServer/startNextServer.ts 5.79ms
 592/2086 src/cli/cli-commands/agents-server/startAgentsServer/startUserChatJobWorkerPump.ts 7.40ms
 593/2086 src/cli/cli-commands/agents-server/startAgentsServer/stopChildProcess.ts 1.78ms
 594/2086 src/cli/cli-commands/agents-server/startAgentsServer/waitForLocalAgentRunnerLimits.ts 8.56ms
 595/2086 src/cli/cli-commands/coder.ts 5.11ms
 596/2086 src/cli/cli-commands/coder/$ensureCoderHarnessGitignoreRules.test.ts 4.90ms
 597/2086 src/cli/cli-commands/coder/$ensureCoderHarnessGitignoreRules.ts 4.52ms
 598/2086 src/cli/cli-commands/coder/add.test.ts 8.45ms
 599/2086 src/cli/cli-commands/coder/add.ts 17.86ms
 600/2086 src/cli/cli-commands/coder/agentCliOptions.ts 2.54ms
 601/2086 src/cli/cli-commands/coder/agentsFile.ts 2.80ms
 602/2086 src/cli/cli-commands/coder/boilerplateCount.test.ts 2.89ms
 603/2086 src/cli/cli-commands/coder/boilerplateCount.ts 4.81ms
 604/2086 src/cli/cli-commands/coder/boilerplateTemplates.test.ts 13.23ms
 605/2086 src/cli/cli-commands/coder/boilerplateTemplates.ts 12.99ms
 606/2086 src/cli/cli-commands/coder/coderReferencedArtifacts.ts 7.60ms
 607/2086 src/cli/cli-commands/coder/ensureCoderDeveloperAgentFile.ts 3.63ms
 608/2086 src/cli/cli-commands/coder/ensureCoderEnvFile.ts 7.91ms
 609/2086 src/cli/cli-commands/coder/ensureCoderGitignoreFile.ts 5.47ms
 610/2086 src/cli/cli-commands/coder/ensureCoderMarkdownFile.ts 2.96ms
 611/2086 src/cli/cli-commands/coder/ensureCoderPackageJsonFile.ts 4.77ms
 612/2086 src/cli/cli-commands/coder/ensureCoderVscodeSettingsFile.ts 4.02ms
 613/2086 src/cli/cli-commands/coder/ensureDirectory.ts 4.38ms
 614/2086 src/cli/cli-commands/coder/find-fresh-emoji-tags.ts 3.59ms
 615/2086 src/cli/cli-commands/coder/find-refactor-candidates.test.ts 4.65ms
 616/2086 src/cli/cli-commands/coder/find-refactor-candidates.ts 6.94ms
 617/2086 src/cli/cli-commands/coder/find-unwritten.ts 4.05ms
 618/2086 src/cli/cli-commands/coder/formatDisplayPath.ts 2.25ms
 619/2086 src/cli/cli-commands/coder/generate-boilerplates.test.ts 6.41ms
 620/2086 src/cli/cli-commands/coder/generate-boilerplates.ts 10.84ms
 621/2086 src/cli/cli-commands/coder/getDefaultCoderPackageJsonScripts.ts 7.39ms
 622/2086 src/cli/cli-commands/coder/getDefaultCoderVscodeSettings.ts 2.59ms
 623/2086 src/cli/cli-commands/coder/getTypescriptModule.test.ts 2.28ms
 624/2086 src/cli/cli-commands/coder/getTypescriptModule.ts 2.75ms
 625/2086 src/cli/cli-commands/coder/GitChangesMode.ts 2.46ms
 626/2086 src/cli/cli-commands/coder/init.test.ts 8.60ms
 627/2086 src/cli/cli-commands/coder/init.ts 12.18ms
 628/2086 src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts 8.90ms
 629/2086 src/cli/cli-commands/coder/list.test.ts 6.06ms
 630/2086 src/cli/cli-commands/coder/list.ts 5.74ms
 631/2086 src/cli/cli-commands/coder/mergeStringRecordJsonFile.test.ts 5.52ms
 632/2086 src/cli/cli-commands/coder/mergeStringRecordJsonFile.ts 11.61ms
 633/2086 src/cli/cli-commands/coder/ping.test.ts 9.23ms
 634/2086 src/cli/cli-commands/coder/ping.ts 7.59ms
 635/2086 src/cli/cli-commands/coder/printCoderRunFailure.ts 1.82ms
 636/2086 src/cli/cli-commands/coder/printInitializationSummary.ts 6.39ms
 637/2086 src/cli/cli-commands/coder/run.test.ts 10.51ms
 638/2086 src/cli/cli-commands/coder/run.ts 15.99ms
 639/2086 src/cli/cli-commands/coder/server.test.ts 5.60ms
 640/2086 src/cli/cli-commands/coder/server.ts 15.27ms
 641/2086 src/cli/cli-commands/coder/ThinkingLevel.ts 3.68ms
 642/2086 src/cli/cli-commands/coder/verify.test.ts 6.58ms
 643/2086 src/cli/cli-commands/coder/verify.ts 5.87ms
 644/2086 src/cli/cli-commands/coder/waitOptions.ts 3.92ms
 645/2086 src/cli/cli-commands/common/$askForConfirmation.test.ts 2.93ms
 646/2086 src/cli/cli-commands/common/$askForConfirmation.ts 2.92ms
 647/2086 src/cli/cli-commands/common/coderGitSyncCliOptions.test.ts 4.47ms
 648/2086 src/cli/cli-commands/common/coderGitSyncCliOptions.ts 5.90ms
 649/2086 src/cli/cli-commands/common/createLocalAgentReferenceResolver.ts 5.28ms
 650/2086 src/cli/cli-commands/common/createNonNegativeIntegerOptionParser.ts 3.60ms
 651/2086 src/cli/cli-commands/common/createPositiveIntegerOptionParser.ts 3.70ms
 652/2086 src/cli/cli-commands/common/disk-space/$assertSufficientFreeDiskSpace.test.ts 5.32ms
 653/2086 src/cli/cli-commands/common/disk-space/$assertSufficientFreeDiskSpace.ts 3.83ms
 654/2086 src/cli/cli-commands/common/disk-space/$readFreeDiskSpaceStatus.test.ts 4.32ms
 655/2086 src/cli/cli-commands/common/disk-space/$readFreeDiskSpaceStatus.ts 3.95ms
 656/2086 src/cli/cli-commands/common/disk-space/formatFreeDiskSpaceBytes.test.ts 1.95ms
 657/2086 src/cli/cli-commands/common/disk-space/formatFreeDiskSpaceBytes.ts 2.99ms
 658/2086 src/cli/cli-commands/common/disk-space/formatLowFreeDiskSpaceWarning.ts 4.03ms
 659/2086 src/cli/cli-commands/common/disk-space/freeDiskSpaceConstants.ts 2.99ms
 660/2086 src/cli/cli-commands/common/disk-space/FreeDiskSpaceStatus.ts 3.00ms
 661/2086 src/cli/cli-commands/common/disk-space/resolveFreeDiskSpaceLevel.test.ts 2.04ms
 662/2086 src/cli/cli-commands/common/disk-space/resolveFreeDiskSpaceLevel.ts 2.43ms
 663/2086 src/cli/cli-commands/common/ensureAdamAgentBook.ts 3.36ms
 664/2086 src/cli/cli-commands/common/handleActionErrors.ts 2.71ms
 665/2086 src/cli/cli-commands/common/harness/$applyHarnessInstallationStatus.ts 6.11ms
 666/2086 src/cli/cli-commands/common/harness/$checkHarnessInstallation.test.ts 5.86ms
 667/2086 src/cli/cli-commands/common/harness/$checkHarnessInstallation.ts 6.19ms
 668/2086 src/cli/cli-commands/common/harness/$ensureHarnessInstallations.ts 5.30ms
 669/2086 src/cli/cli-commands/common/harness/$resolveHarnessCommandPath.ts 3.50ms
 670/2086 src/cli/cli-commands/common/harness/$resolveHarnessInstallationOrigin.ts 3.88ms
 671/2086 src/cli/cli-commands/common/harness/$resolveInstalledHarnessVersion.ts 2.94ms
 672/2086 src/cli/cli-commands/common/harness/$runHarnessInstallationCommand.ts 5.03ms
 673/2086 src/cli/cli-commands/common/harness/buildHarnessInstallCommand.ts 3.08ms
 674/2086 src/cli/cli-commands/common/harness/formatHarnessInstallationWarning.ts 4.04ms
 675/2086 src/cli/cli-commands/common/harness/formatHarnessManualUpdateInstruction.test.ts 6.47ms
 676/2086 src/cli/cli-commands/common/harness/formatHarnessManualUpdateInstruction.ts 4.45ms
 677/2086 src/cli/cli-commands/common/harness/HarnessDefinition.test.ts 6.86ms
 678/2086 src/cli/cli-commands/common/harness/HarnessDefinition.ts 6.82ms
 679/2086 src/cli/cli-commands/common/harness/HarnessInstallationOrigin.ts 3.61ms
 680/2086 src/cli/cli-commands/common/harness/HarnessInstallationStatus.ts 3.37ms
 681/2086 src/cli/cli-commands/common/harness/HarnessUpdatePlan.ts 2.47ms
 682/2086 src/cli/cli-commands/common/harness/resolveHarnessInstallationMethodFromPaths.test.ts 4.44ms
 683/2086 src/cli/cli-commands/common/harness/resolveHarnessInstallationMethodFromPaths.ts 7.26ms
 684/2086 src/cli/cli-commands/common/harness/resolveHarnessUpdatePlan.test.ts 3.14ms
 685/2086 src/cli/cli-commands/common/harness/resolveHarnessUpdatePlan.ts 5.26ms
 686/2086 src/cli/cli-commands/common/LocalAgentBookCollection.ts 11.21ms
 687/2086 src/cli/cli-commands/common/npm/$askForNpmPackageInstallationApproval.ts 2.87ms
 688/2086 src/cli/cli-commands/common/npm/$resolveLatestNpmPackageVersion.test.ts 3.50ms
 689/2086 src/cli/cli-commands/common/npm/$resolveLatestNpmPackageVersion.ts 5.92ms
 690/2086 src/cli/cli-commands/common/npm/$resolveNpmGlobalPrefixPath.ts 3.79ms
 691/2086 src/cli/cli-commands/common/npm/buildNpmPackageInstallCommand.test.ts 1.78ms
 692/2086 src/cli/cli-commands/common/npm/buildNpmPackageInstallCommand.ts 3.63ms
 693/2086 src/cli/cli-commands/common/npm/extractNpmPackageVersionFromOutput.test.ts 3.35ms
 694/2086 src/cli/cli-commands/common/npm/extractNpmPackageVersionFromOutput.ts 2.88ms
 695/2086 src/cli/cli-commands/common/npm/isNpmPackageVersionOutdated.test.ts 2.91ms
 696/2086 src/cli/cli-commands/common/npm/isNpmPackageVersionOutdated.ts 9.08ms
 697/2086 src/cli/cli-commands/common/projectInitialization.ts 9.38ms
 698/2086 src/cli/cli-commands/common/promptbook-cli/$checkPromptbookCliInstallations.test.ts 4.92ms
 699/2086 src/cli/cli-commands/common/promptbook-cli/$checkPromptbookCliInstallations.ts 5.09ms
 700/2086 src/cli/cli-commands/common/promptbook-cli/$ensurePromptbookCliInstallations.test.ts 7.98ms
 701/2086 src/cli/cli-commands/common/promptbook-cli/$ensurePromptbookCliInstallations.ts 7.00ms
 702/2086 src/cli/cli-commands/common/promptbook-cli/$resolvePromptbookCliInstallations.test.ts 5.60ms
 703/2086 src/cli/cli-commands/common/promptbook-cli/$resolvePromptbookCliInstallations.ts 15.44ms
 704/2086 src/cli/cli-commands/common/promptbook-cli/$updatePromptbookCliInstallation.test.ts 5.52ms
 705/2086 src/cli/cli-commands/common/promptbook-cli/$updatePromptbookCliInstallation.ts 6.11ms
 706/2086 src/cli/cli-commands/common/promptbook-cli/buildPromptbookCliInstallCommand.ts 2.63ms
 707/2086 src/cli/cli-commands/common/promptbook-cli/formatPromptbookCliInstallationWarning.ts 5.87ms
 708/2086 src/cli/cli-commands/common/promptbook-cli/PromptbookCliInstallation.ts 12.37ms
 709/2086 src/cli/cli-commands/common/promptbook-cli/PromptbookCliInstallationStatus.ts 3.09ms
 710/2086 src/cli/cli-commands/common/promptPriorityCliOptions.ts 5.76ms
 711/2086 src/cli/cli-commands/common/promptRunnerCliOptions.ts 12.69ms
 712/2086 src/cli/cli-commands/common/questionsCliOptions.test.ts 4.16ms
 713/2086 src/cli/cli-commands/common/questionsCliOptions.ts 5.94ms
 714/2086 src/cli/cli-commands/common/resolveBundledAgentBookPath.ts 3.01ms
 715/2086 src/cli/cli-commands/common/resolveLocalAgentSource.ts 7.75ms
 716/2086 src/cli/cli-commands/hello.ts 5.76ms
 717/2086 src/cli/cli-commands/list-models.ts 3.89ms
 718/2086 src/cli/cli-commands/list-scrapers.ts 4.67ms
 719/2086 src/cli/cli-commands/login.ts 3.99ms
 720/2086 src/cli/cli-commands/make.ts 22.30ms
 721/2086 src/cli/cli-commands/prettify.ts 6.14ms
 722/2086 src/cli/cli-commands/run.ts 5.22ms
 723/2086 src/cli/cli-commands/run/prepareRunCommandResources.ts 13.87ms
 724/2086 src/cli/cli-commands/run/resolveRunInputParameters.ts 6.71ms
 725/2086 src/cli/cli-commands/run/runCommandAction.ts 7.52ms
 726/2086 src/cli/cli-commands/run/runPipelineExecution.ts 6.82ms
 727/2086 src/cli/cli-commands/runInteractiveChatbot.ts 17.15ms
 728/2086 src/cli/cli-commands/start-agents-server.ts 6.94ms
 729/2086 src/cli/cli-commands/start-pipelines-server.ts 9.69ms
 730/2086 src/cli/cli-commands/test-command.ts 9.64ms
 731/2086 src/cli/common/$addGlobalOptionsToCommand.ts 3.81ms
 732/2086 src/cli/common/$deprecateCliCommand.test.ts 4.04ms
 733/2086 src/cli/common/$deprecateCliCommand.ts 3.44ms
 734/2086 src/cli/common/$hideCliCommandFromHelp.test.ts 3.25ms
 735/2086 src/cli/common/$hideCliCommandFromHelp.ts 2.64ms
 736/2086 src/cli/common/$provideLlmToolsForCli.ts 9.80ms
 737/2086 src/cli/common/$requireCliSubcommand.test.ts 3.93ms
 738/2086 src/cli/common/$requireCliSubcommand.ts 4.57ms
 739/2086 src/cli/common/loadPromptsModule.ts 2.29ms
 740/2086 src/cli/main.ts 2.15ms
 741/2086 src/cli/other/install.test.ts 4.19ms
 742/2086 src/cli/other/vpsInstall.test.ts 30.64ms
 743/2086 src/cli/promptbookCli.ts 5.41ms
 744/2086 src/cli/test/ptbk.test.ts 12.48ms
 745/2086 src/cli/test/ptbk.ts 1.66ms
 746/2086 src/cli/test/tsconfig.json 3.29ms
 747/2086 src/collection/agent-collection/AgentCollection.ts 6.08ms
 748/2086 src/collection/agent-collection/constructors/agent-collection-in-supabase/AgentCollectionInSupabase.ts 26.24ms
 749/2086 src/collection/agent-collection/constructors/agent-collection-in-supabase/AgentCollectionInSupabaseOptions.ts 2.77ms
 750/2086 src/collection/agent-collection/constructors/agent-collection-in-supabase/AgentsDatabaseSchema.ts 16.25ms
 751/2086 src/collection/agent-collection/constructors/agent-collection-in-supabase/buildAgentNameOrPermanentIdFilter.test.ts 5.91ms
 752/2086 src/collection/agent-collection/constructors/agent-collection-in-supabase/buildAgentNameOrPermanentIdFilter.ts 3.97ms
 753/2086 src/collection/agent-collection/constructors/agent-collection-in-supabase/createAgentPersistenceRecords.test.ts 5.33ms
 754/2086 src/collection/agent-collection/constructors/agent-collection-in-supabase/createAgentPersistenceRecords.ts 10.38ms
 755/2086 src/collection/agent-collection/constructors/agent-collection-in-supabase/prepareAgentSourceForPersistence.ts 9.38ms
 756/2086 src/collection/agent-collection/CreateAgentInput.test.ts 3.10ms
 757/2086 src/collection/agent-collection/CreateAgentInput.ts 14.05ms
 758/2086 src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts 4.42ms
 759/2086 src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts 18.54ms
 760/2086 src/collection/pipeline-collection/constructors/createPipelineCollectionFromJson.test.ts 4.23ms
 761/2086 src/collection/pipeline-collection/constructors/createPipelineCollectionFromJson.ts 2.98ms
 762/2086 src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.test.ts 3.63ms
 763/2086 src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts 4.91ms
 764/2086 src/collection/pipeline-collection/constructors/createPipelineCollectionFromUrl.ts 3.97ms
 765/2086 src/collection/pipeline-collection/constructors/createPipelineSubcollection.ts 6.48ms
 766/2086 src/collection/pipeline-collection/PipelineCollection.ts 3.70ms
 767/2086 src/collection/pipeline-collection/pipelineCollectionToJson.test.ts 4.17ms
 768/2086 src/collection/pipeline-collection/pipelineCollectionToJson.ts 2.75ms
 769/2086 src/collection/pipeline-collection/SimplePipelineCollection.ts 5.64ms
 770/2086 src/commands/_BOILERPLATE/boilerplateCommand.test.ts 4.37ms
 771/2086 src/commands/_BOILERPLATE/BoilerplateCommand.ts 2.43ms
 772/2086 src/commands/_BOILERPLATE/boilerplateCommandParser.ts 5.78ms
 773/2086 src/commands/_common/getParserForCommand.test.ts 2.70ms
 774/2086 src/commands/_common/getParserForCommand.ts 2.50ms
 775/2086 src/commands/_common/parseCommand.ts 6.98ms
 776/2086 src/commands/_common/stringifyCommand.test.ts 3.70ms
 777/2086 src/commands/_common/stringifyCommand.ts 2.32ms
 778/2086 src/commands/_common/types/Command.ts 1.71ms
 779/2086 src/commands/_common/types/CommandParser.ts 10.65ms
 780/2086 src/commands/_common/types/CommandType.ts 2.20ms
 781/2086 src/commands/_common/types/CommandUsagePlaces.ts 1.79ms
 782/2086 src/commands/BOOK_VERSION/bookVersionCommand.test.ts 2.92ms
 783/2086 src/commands/BOOK_VERSION/BookVersionCommand.ts 2.08ms
 784/2086 src/commands/BOOK_VERSION/bookVersionCommandParser.ts 5.90ms
 785/2086 src/commands/EXPECT/expectCommand.test.ts 4.93ms
 786/2086 src/commands/EXPECT/ExpectCommand.ts 2.66ms
 787/2086 src/commands/EXPECT/expectCommandParser.ts 9.10ms
 788/2086 src/commands/FOREACH/foreachCommand.test.ts 5.25ms
 789/2086 src/commands/FOREACH/ForeachCommand.ts 1.68ms
 790/2086 src/commands/FOREACH/foreachCommandParser.ts 16.05ms
 791/2086 src/commands/FOREACH/ForeachJson.ts 2.94ms
 792/2086 src/commands/FORMAT/formatCommand.test.ts 4.77ms
 793/2086 src/commands/FORMAT/FormatCommand.ts 1.90ms
 794/2086 src/commands/FORMAT/formatCommandParser.ts 5.27ms
 795/2086 src/commands/FORMFACTOR/formfactorCommand.test.ts 2.90ms
 796/2086 src/commands/FORMFACTOR/FormfactorCommand.ts 1.99ms
 797/2086 src/commands/FORMFACTOR/formfactorCommandParser.ts 6.45ms
 798/2086 src/commands/index.ts 3.99ms
 799/2086 src/commands/JOKER/jokerCommand.test.ts 2.98ms
 800/2086 src/commands/JOKER/JokerCommand.ts 2.08ms
 801/2086 src/commands/JOKER/jokerCommandParser.ts 7.40ms
 802/2086 src/commands/KNOWLEDGE/knowledgeCommand.test.ts 6.09ms
 803/2086 src/commands/KNOWLEDGE/KnowledgeCommand.ts 1.72ms
 804/2086 src/commands/KNOWLEDGE/knowledgeCommandParser.ts 6.50ms
 805/2086 src/commands/KNOWLEDGE/utils/knowledgeSourceContentToName.test.ts 2.59ms
 806/2086 src/commands/KNOWLEDGE/utils/knowledgeSourceContentToName.ts 2.85ms
 807/2086 src/commands/MODEL/modelCommand.test.ts 3.17ms
 808/2086 src/commands/MODEL/ModelCommand.ts 1.97ms
 809/2086 src/commands/MODEL/modelCommandParser.ts 8.33ms
 810/2086 src/commands/PARAMETER/parameterCommand.test.ts 6.53ms
 811/2086 src/commands/PARAMETER/ParameterCommand.ts 2.03ms
 812/2086 src/commands/PARAMETER/parameterCommandParser.ts 6.87ms
 813/2086 src/commands/PERSONA/personaCommand.test.ts 4.09ms
 814/2086 src/commands/PERSONA/PersonaCommand.ts 1.94ms
 815/2086 src/commands/PERSONA/personaCommandParser.ts 9.49ms
 816/2086 src/commands/POSTPROCESS/postprocessCommand.test.ts 2.97ms
 817/2086 src/commands/POSTPROCESS/PostprocessCommand.ts 1.89ms
 818/2086 src/commands/POSTPROCESS/postprocessCommandParser.ts 8.96ms
 819/2086 src/commands/README.md 1.81ms
 820/2086 src/commands/SECTION/sectionCommand.test.ts 3.45ms
 821/2086 src/commands/SECTION/SectionCommand.ts 4.52ms
 822/2086 src/commands/SECTION/sectionCommandParser.ts 15.16ms
 823/2086 src/commands/URL/urlCommand.test.ts 4.41ms
 824/2086 src/commands/URL/UrlCommand.ts 2.39ms
 825/2086 src/commands/URL/urlCommandParser.ts 7.10ms
 826/2086 src/commands/X_ACTION/actionCommand.test.ts 3.76ms
 827/2086 src/commands/X_ACTION/ActionCommand.ts 1.67ms
 828/2086 src/commands/X_ACTION/actionCommandParser.ts 6.51ms
 829/2086 src/commands/X_INSTRUMENT/instrumentCommand.test.ts 2.59ms
 830/2086 src/commands/X_INSTRUMENT/InstrumentCommand.ts 1.84ms
 831/2086 src/commands/X_INSTRUMENT/instrumentCommandParser.ts 6.20ms
 832/2086 src/commitments/_base/BaseCommitmentDefinition.ts 12.38ms
 833/2086 src/commitments/_base/BookCommitment.ts 1.64ms
 834/2086 src/commitments/_base/CommitmentDefinition.ts 7.75ms
 835/2086 src/commitments/_base/createEmptyAgentModelRequirements.ts 4.85ms
 836/2086 src/commitments/_base/formatOptionalInstructionBlock.ts 2.20ms
 837/2086 src/commitments/_base/NotYetImplementedCommitmentDefinition.ts 8.40ms
 838/2086 src/commitments/_base/ParsedCommitment.ts 2.27ms
 839/2086 src/commitments/_common/commitmentToolFunctions.ts 6.71ms
 840/2086 src/commitments/_common/createWritingCommitmentSections.ts 4.25ms
 841/2086 src/commitments/_common/getAllCommitmentDefinitions.test.ts 2.94ms
 842/2086 src/commitments/_common/getAllCommitmentDefinitions.ts 4.90ms
 843/2086 src/commitments/_common/getAllCommitmentsToolFunctionsForBrowser.ts 3.27ms
 844/2086 src/commitments/_common/getAllCommitmentsToolFunctionsForNode.ts 3.42ms
 845/2086 src/commitments/_common/getAllCommitmentsToolTitles.ts 3.18ms
 846/2086 src/commitments/_common/getAllCommitmentTypes.ts 6.13ms
 847/2086 src/commitments/_common/getCommitmentDefinition.ts 2.43ms
 848/2086 src/commitments/_common/getCommitmentNoticeMetadata.test.ts 6.20ms
 849/2086 src/commitments/_common/getCommitmentNoticeMetadata.ts 36.09ms
 850/2086 src/commitments/_common/getGroupedCommitmentDefinitions.openClosed.test.ts 2.61ms
 851/2086 src/commitments/_common/getGroupedCommitmentDefinitions.order.test.ts 29.70ms
 852/2086 src/commitments/_common/getGroupedCommitmentDefinitions.ts 6.79ms
 853/2086 src/commitments/_common/getGroupedCommitmentDefinitions.use.test.ts 2.84ms
 854/2086 src/commitments/_common/getGroupedCommitmentDefinitions.writing.test.ts 4.28ms
 855/2086 src/commitments/_common/isCommitmentSupported.ts 4.38ms
 856/2086 src/commitments/_common/sortCommitmentDefinitions.ts 6.25ms
 857/2086 src/commitments/_common/teamInternalAgentAccess.ts 6.89ms
 858/2086 src/commitments/_common/toolExecutionEnvelope.ts 3.54ms
 859/2086 src/commitments/_common/toolRuntimeContext.ts 10.05ms
 860/2086 src/commitments/ACTION/ACTION.test.ts 3.28ms
 861/2086 src/commitments/ACTION/ACTION.ts 5.99ms
 862/2086 src/commitments/CLOSED/CLOSED.test.ts 2.65ms
 863/2086 src/commitments/CLOSED/CLOSED.ts 4.59ms
 864/2086 src/commitments/DELETE/DELETE.test.ts 3.43ms
 865/2086 src/commitments/DELETE/DELETE.ts 7.13ms
 866/2086 src/commitments/DICTIONARY/DICTIONARY.ts 6.99ms
 867/2086 src/commitments/FORMAT/FORMAT.test.ts 3.36ms
 868/2086 src/commitments/FORMAT/FORMAT.ts 4.75ms
 869/2086 src/commitments/FROM/FROM.ts 6.01ms
 870/2086 src/commitments/GOAL/GOAL.ts 8.79ms
 871/2086 src/commitments/IMPORT/IMPORT.ts 5.23ms
 872/2086 src/commitments/index.test.ts 3.47ms
 873/2086 src/commitments/index.ts 12.38ms
 874/2086 src/commitments/KNOWLEDGE/KNOWLEDGE.ts 7.95ms
 875/2086 src/commitments/LANGUAGE/LANGUAGE.ts 3.78ms
 876/2086 src/commitments/MESSAGE_SUFFIX/MESSAGE_SUFFIX.test.ts 4.08ms
 877/2086 src/commitments/MESSAGE_SUFFIX/MESSAGE_SUFFIX.ts 3.65ms
 878/2086 src/commitments/MESSAGE/AgentMessageCommitmentDefinition.ts 4.33ms
 879/2086 src/commitments/MESSAGE/InitialMessageCommitmentDefinition.ts 3.76ms
 880/2086 src/commitments/MESSAGE/InternalMessageCommitmentDefinition.ts 4.45ms
 881/2086 src/commitments/MESSAGE/MESSAGE.ts 7.94ms
 882/2086 src/commitments/MESSAGE/UserMessageCommitmentDefinition.ts 3.89ms
 883/2086 src/commitments/META_AVATAR/META_AVATAR.ts 5.22ms
 884/2086 src/commitments/META_COLOR/META_COLOR.ts 6.25ms
 885/2086 src/commitments/META_DESCRIPTION/META_DESCRIPTION.ts 4.13ms
 886/2086 src/commitments/META_DISCLAIMER/META_DISCLAIMER.test.ts 4.27ms
 887/2086 src/commitments/META_DISCLAIMER/META_DISCLAIMER.ts 4.14ms
 888/2086 src/commitments/META_DOMAIN/META_DOMAIN.ts 6.27ms
 889/2086 src/commitments/META_FONT/META_FONT.ts 7.91ms
 890/2086 src/commitments/META_FULLNAME/META_FULLNAME.ts 4.29ms
 891/2086 src/commitments/META_ID/META_ID.ts 5.50ms
 892/2086 src/commitments/META_IMAGE/META_IMAGE.ts 5.02ms
 893/2086 src/commitments/META_INPUT_PLACEHOLDER/META_INPUT_PLACEHOLDER.ts 4.91ms
 894/2086 src/commitments/META_LINK/META_LINK.ts 8.74ms
 895/2086 src/commitments/META_THINKING_MESSAGE/META_THINKING_MESSAGE.ts 7.87ms
 896/2086 src/commitments/META_VISIBILITY/META_VISIBILITY.ts 7.09ms
 897/2086 src/commitments/META_VOICE/META_VOICE.ts 6.63ms
 898/2086 src/commitments/MODEL/MODEL.test.ts 4.59ms
 899/2086 src/commitments/MODEL/MODEL.ts 13.25ms
 900/2086 src/commitments/NOTE/NOTE.ts 15.79ms
 901/2086 src/commitments/OPEN/OPEN.ts 4.96ms
 902/2086 src/commitments/PERSONA/persona-verification-example.md 8.25ms
 903/2086 src/commitments/PERSONA/PERSONA.ts 11.10ms
 904/2086 src/commitments/README.md 2.16ms
 905/2086 src/commitments/RULE/RULE.ts 7.00ms
 906/2086 src/commitments/SAMPLE/SAMPLE.ts 6.33ms
 907/2086 src/commitments/SCENARIO/SCENARIO.ts 7.86ms
 908/2086 src/commitments/STYLE/STYLE.ts 9.83ms
 909/2086 src/commitments/TEAM/TEAM.ts 27.93ms
 910/2086 src/commitments/TEMPLATE/README.md 6.31ms
 911/2086 src/commitments/TEMPLATE/TEMPLATE.test.ts 13.52ms
 912/2086 src/commitments/TEMPLATE/TEMPLATE.ts 8.56ms
 913/2086 src/commitments/USE_CALENDAR/calendarReference.ts 11.49ms
 914/2086 src/commitments/USE_CALENDAR/callGoogleCalendarApi.ts 8.20ms
 915/2086 src/commitments/USE_CALENDAR/createUseCalendarToolFunctions.ts 17.72ms
 916/2086 src/commitments/USE_CALENDAR/createUseCalendarTools.ts 7.21ms
 917/2086 src/commitments/USE_CALENDAR/getUseCalendarToolTitles.ts 3.12ms
 918/2086 src/commitments/USE_CALENDAR/normalizeConfiguredCalendars.ts 3.99ms
 919/2086 src/commitments/USE_CALENDAR/resolveUseCalendarToolRuntimeOrWalletCredentialResult.ts 11.65ms
 920/2086 src/commitments/USE_CALENDAR/USE_CALENDAR.test.ts 6.03ms
 921/2086 src/commitments/USE_CALENDAR/USE_CALENDAR.ts 7.72ms
 922/2086 src/commitments/USE_CALENDAR/UseCalendarToolNames.ts 2.19ms
 923/2086 src/commitments/USE_CALENDAR/UseCalendarWallet.ts 1.97ms
 924/2086 src/commitments/USE_IMAGE_GENERATOR/USE_IMAGE_GENERATOR.test.ts 2.96ms
 925/2086 src/commitments/USE_IMAGE_GENERATOR/USE_IMAGE_GENERATOR.ts 6.22ms
 926/2086 src/commitments/USE_MCP/USE_MCP.ts 4.44ms
 927/2086 src/commitments/USE_POPUP/USE_POPUP.ts 6.58ms
 928/2086 src/commitments/USE_PRIVACY/USE_PRIVACY.test.ts 6.94ms
 929/2086 src/commitments/USE_PRIVACY/USE_PRIVACY.ts 11.10ms
 930/2086 src/commitments/USE_PROJECT/callGitHubApi.ts 7.74ms
 931/2086 src/commitments/USE_PROJECT/createUseProjectToolFunctions.ts 15.92ms
 932/2086 src/commitments/USE_PROJECT/createUseProjectTools.ts 5.64ms
 933/2086 src/commitments/USE_PROJECT/getUseProjectToolTitles.ts 3.20ms
 934/2086 src/commitments/USE_PROJECT/normalizeConfiguredProjects.ts 3.95ms
 935/2086 src/commitments/USE_PROJECT/normalizeOptionalToolText.ts 1.86ms
 936/2086 src/commitments/USE_PROJECT/projectReference.test.ts 2.84ms
 937/2086 src/commitments/USE_PROJECT/projectReference.ts 6.96ms
 938/2086 src/commitments/USE_PROJECT/resolveUseProjectToolRuntimeOrWalletCredentialResult.ts 7.14ms
 939/2086 src/commitments/USE_PROJECT/USE_PROJECT.test.ts 10.38ms
 940/2086 src/commitments/USE_PROJECT/USE_PROJECT.ts 9.35ms
 941/2086 src/commitments/USE_PROJECT/UseProjectToolNames.ts 2.60ms
 942/2086 src/commitments/USE_PROJECT/UseProjectWallet.ts 2.54ms
 943/2086 src/commitments/USE_USER_LOCATION/USE_USER_LOCATION.test.ts 5.27ms
 944/2086 src/commitments/USE_USER_LOCATION/USE_USER_LOCATION.ts 10.18ms
 945/2086 src/commitments/WRITING_RULES/WRITING_RULES.ts 5.14ms
 946/2086 src/commitments/WRITING_SAMPLE/WRITING_SAMPLE.ts 5.59ms
 947/2086 src/config.test.ts 3.05ms
 948/2086 src/config.ts 13.24ms
 949/2086 src/constants.ts 7.73ms
 950/2086 src/constants/streaming.ts 2.24ms
 951/2086 src/conversion/archive/loadArchive.ts 3.44ms
 952/2086 src/conversion/archive/loadJsZipModule.ts 2.06ms
 953/2086 src/conversion/archive/saveArchive.ts 5.78ms
 954/2086 src/conversion/compilePipeline.test.ts 3.43ms
 955/2086 src/conversion/compilePipeline.ts 3.63ms
 956/2086 src/conversion/compilePipelineOnRemoteServer.ts 4.46ms
 957/2086 src/conversion/parsePipeline.test.ts 3.96ms
 958/2086 src/conversion/parsePipeline.ts 7.65ms
 959/2086 src/conversion/parsePipeline/applyPipelineHead.ts 4.73ms
 960/2086 src/conversion/parsePipeline/createInitialPipelineJson.ts 3.29ms
 961/2086 src/conversion/parsePipeline/createUniqueSectionNameResolver.ts 4.92ms
 962/2086 src/conversion/parsePipeline/defineParameter.ts 4.80ms
 963/2086 src/conversion/parsePipeline/extractPipelineDescription.ts 2.85ms
 964/2086 src/conversion/parsePipeline/finalizeParsedPipeline.ts 6.71ms
 965/2086 src/conversion/parsePipeline/getPipelineIdentification.ts 2.19ms
 966/2086 src/conversion/parsePipeline/parsePreparedPipelineSections.ts 4.41ms
 967/2086 src/conversion/parsePipeline/preparePipelineString.ts 6.41ms
 968/2086 src/conversion/parsePipeline/processPipelineSection.ts 14.03ms
 969/2086 src/conversion/pipelineJsonToString.test.ts 6.68ms
 970/2086 src/conversion/pipelineJsonToString.ts 4.66ms
 971/2086 src/conversion/pipelineJsonToString/appendMarkdownBlock.ts 2.13ms
 972/2086 src/conversion/pipelineJsonToString/createPipelineCommands.ts 2.98ms
 973/2086 src/conversion/pipelineJsonToString/createPipelineIntroduction.ts 2.99ms
 974/2086 src/conversion/pipelineJsonToString/createTaskSerialization.ts 5.67ms
 975/2086 src/conversion/pipelineJsonToString/stringifyCommands.ts 1.88ms
 976/2086 src/conversion/pipelineJsonToString/stringifyTask.ts 4.16ms
 977/2086 src/conversion/prettify/PrettifyOptions.ts 2.09ms
 978/2086 src/conversion/prettify/prettifyPipelineString.ts 4.68ms
 979/2086 src/conversion/prettify/renderPipelineMermaidOptions.ts 7.46ms
 980/2086 src/conversion/utils/extractParameterNamesFromTask.test.ts 1.98ms
 981/2086 src/conversion/utils/extractParameterNamesFromTask.ts 4.79ms
 982/2086 src/conversion/validation/_importPipeline.test.ts 5.15ms
 983/2086 src/conversion/validation/pipelineStringToJson-parseErrors.test.ts 3.17ms
 984/2086 src/conversion/validation/validatePipeline-logicErrors.test.ts 3.16ms
 985/2086 src/conversion/validation/validatePipeline.test.ts 3.42ms
 986/2086 src/conversion/validation/validatePipeline.ts 4.84ms
 987/2086 src/conversion/validation/validatePipeline/createPipelineValidationContext.ts 2.89ms
 988/2086 src/conversion/validation/validatePipeline/validatePipelineCollectionsStructure.ts 4.13ms
 989/2086 src/conversion/validation/validatePipeline/validatePipelineDependencyResolution.ts 7.75ms
 990/2086 src/conversion/validation/validatePipeline/validatePipelineMetadata.ts 4.04ms
 991/2086 src/conversion/validation/validatePipeline/validatePipelineParameters.ts 5.70ms
 992/2086 src/conversion/validation/validatePipeline/validatePipelineTasks.ts 6.79ms
 993/2086 src/dialogs/callback/CallbackInterfaceTools.ts 2.95ms
 994/2086 src/dialogs/callback/CallbackInterfaceToolsOptions.ts 4.09ms
 995/2086 src/dialogs/simple-prompt/SimplePromptInterfaceTools.ts 4.67ms
 996/2086 src/dialogs/user-interface-execution-tools.test.ts 5.24ms
 997/2086 src/errors/0-BoilerplateError.ts 2.52ms
 998/2086 src/errors/0-index.ts 5.45ms
 999/2086 src/errors/AbstractFormatError.ts 3.08ms
1000/2086 src/errors/assertsError.ts 3.29ms
1001/2086 src/errors/AuthenticationError.ts 1.76ms
1002/2086 src/errors/CollectionError.ts 2.16ms
1003/2086 src/errors/ConflictError.ts 1.76ms
1004/2086 src/errors/DatabaseError.ts 2.39ms
1005/2086 src/errors/EnvironmentMismatchError.ts 1.88ms
1006/2086 src/errors/ExpectError.ts 2.93ms
1007/2086 src/errors/KnowledgeScrapeError.ts 1.82ms
1008/2086 src/errors/LimitReachedError.ts 1.60ms
1009/2086 src/errors/MissingToolsError.ts 4.92ms
1010/2086 src/errors/NotAllowed.ts 2.90ms
1011/2086 src/errors/NotFoundError.ts 2.10ms
1012/2086 src/errors/NotYetImplementedError.ts 2.99ms
1013/2086 src/errors/ParseError.ts 1.99ms
1014/2086 src/errors/PipelineExecutionError.ts 2.33ms
1015/2086 src/errors/PipelineLogicError.ts 1.87ms
1016/2086 src/errors/PipelineUrlError.ts 1.51ms
1017/2086 src/errors/PromptbookFetchError.ts 2.65ms
1018/2086 src/errors/UnexpectedError.ts 2.66ms
1019/2086 src/errors/utils/deserializeError.test.ts 2.28ms
1020/2086 src/errors/utils/deserializeError.ts 2.54ms
1021/2086 src/errors/utils/ErrorJson.ts 2.78ms
1022/2086 src/errors/utils/getErrorReportUrl.ts 2.56ms
1023/2086 src/errors/utils/serializeError.test.ts 2.50ms
1024/2086 src/errors/utils/serializeError.ts 2.43ms
1025/2086 src/errors/WrappedError.ts 3.11ms
1026/2086 src/executables/$provideExecutablesForNode.ts 4.10ms
1027/2086 src/executables/apps/locateLibreoffice.test.ts 6.15ms
1028/2086 src/executables/apps/locateLibreoffice.ts 2.48ms
1029/2086 src/executables/apps/locatePandoc.test.ts 1.77ms
1030/2086 src/executables/apps/locatePandoc.ts 3.06ms
1031/2086 src/executables/apps/locateVscode.test.ts 4.00ms
1032/2086 src/executables/apps/locateVscode.ts 3.36ms
1033/2086 src/executables/browsers/locateBrowser.test.ts 2.96ms
1034/2086 src/executables/browsers/locateBrowser.ts 3.22ms
1035/2086 src/executables/browsers/locateChrome.test.ts 1.62ms
1036/2086 src/executables/browsers/locateChrome.ts 3.15ms
1037/2086 src/executables/browsers/locateDefaultSystemBrowser.test.ts 3.36ms
1038/2086 src/executables/browsers/locateDefaultSystemBrowser.ts 2.08ms
1039/2086 src/executables/browsers/locateEdge.test.ts 1.86ms
1040/2086 src/executables/browsers/locateEdge.ts 2.34ms
1041/2086 src/executables/browsers/locateFirefox.test.ts 1.75ms
1042/2086 src/executables/browsers/locateFirefox.ts 2.52ms
1043/2086 src/executables/browsers/locateInternetExplorer.ts 1.74ms
1044/2086 src/executables/browsers/locateSafari.ts 1.66ms
1045/2086 src/executables/locateApp.test.ts 2.04ms
1046/2086 src/executables/locateApp.ts 3.86ms
1047/2086 src/executables/platforms/locateAppOnLinux.ts 2.80ms
1048/2086 src/executables/platforms/locateAppOnMacOs.ts 4.43ms
1049/2086 src/executables/platforms/locateAppOnWindows.test.ts 3.83ms
1050/2086 src/executables/platforms/locateAppOnWindows.ts 5.13ms
1051/2086 src/executables/README.md 1.49ms
1052/2086 src/execution/AbstractTaskResult.ts 3.00ms
1053/2086 src/execution/assertsTaskSuccessful.ts 3.70ms
1054/2086 src/execution/AvailableModel.ts 3.81ms
1055/2086 src/execution/CommonToolsOptions.ts 2.79ms
1056/2086 src/execution/createPipelineExecutor/$OngoingTaskResult.ts 4.22ms
1057/2086 src/execution/createPipelineExecutor/00-createPipelineExecutor.ts 14.53ms
1058/2086 src/execution/createPipelineExecutor/00-CreatePipelineExecutorOptions.ts 3.54ms
1059/2086 src/execution/createPipelineExecutor/10-executePipeline.ts 17.17ms
1060/2086 src/execution/createPipelineExecutor/20-executeTask.ts 13.34ms
1061/2086 src/execution/createPipelineExecutor/30-executeFormatSubvalues.ts 9.64ms
1062/2086 src/execution/createPipelineExecutor/40-executeAttempts.ts 10.38ms
1063/2086 src/execution/createPipelineExecutor/computeCosineSimilarity.ts 2.20ms
1064/2086 src/execution/createPipelineExecutor/executeSingleAttempt.ts 15.56ms
1065/2086 src/execution/createPipelineExecutor/filterJustOutputParameters.ts 5.59ms
1066/2086 src/execution/createPipelineExecutor/getContextForTask.ts 2.60ms
1067/2086 src/execution/createPipelineExecutor/getExamplesForTask.ts 2.61ms
1068/2086 src/execution/createPipelineExecutor/getKnowledgeForTask.ts 7.22ms
1069/2086 src/execution/createPipelineExecutor/getReservedParametersForTask.ts 4.68ms
1070/2086 src/execution/createPipelineExecutor/handleAttemptFailure.ts 5.45ms
1071/2086 src/execution/createPipelineExecutor/knowledgePiecesToString.ts 2.64ms
1072/2086 src/execution/createPipelineExecutor/reportPromptExecution.ts 5.07ms
1073/2086 src/execution/EmbeddingVector.ts 3.17ms
1074/2086 src/execution/embeddingVectorToString.ts 2.11ms
1075/2086 src/execution/Executables.ts 2.51ms
1076/2086 src/execution/execution-report/countWorkingDuration.test.ts 2.82ms
1077/2086 src/execution/execution-report/countWorkingDuration.ts 1.86ms
1078/2086 src/execution/execution-report/ExecutionPromptReportJson.ts 2.36ms
1079/2086 src/execution/execution-report/ExecutionReportJson.ts 3.64ms
1080/2086 src/execution/execution-report/executionReportJsonToString.ts 9.39ms
1081/2086 src/execution/execution-report/ExecutionReportString.ts 2.17ms
1082/2086 src/execution/execution-report/ExecutionReportStringOptions.ts 2.26ms
1083/2086 src/execution/ExecutionTask.ts 14.53ms
1084/2086 src/execution/ExecutionTools.ts 4.26ms
1085/2086 src/execution/FilesystemTools.ts 2.08ms
1086/2086 src/execution/LlmExecutionTools.ts 8.74ms
1087/2086 src/execution/LlmExecutionToolsConstructor.ts 2.49ms
1088/2086 src/execution/PipelineExecutor.ts 2.15ms
1089/2086 src/execution/PipelineExecutorResult.ts 2.85ms
1090/2086 src/execution/PromptbookFetch.test-type.ts 2.73ms
1091/2086 src/execution/PromptbookFetch.ts 1.78ms
1092/2086 src/execution/PromptResult.ts 5.93ms
1093/2086 src/execution/resolveTaskTldr.test.ts 2.76ms
1094/2086 src/execution/resolveTaskTldr.ts 6.42ms
1095/2086 src/execution/ScriptExecutionTools.ts 3.14ms
1096/2086 src/execution/translation/automatic-translate/automatic-translators/AutomaticTranslator.ts 1.80ms
1097/2086 src/execution/translation/automatic-translate/automatic-translators/DebugAutomaticTranslator.ts 3.62ms
1098/2086 src/execution/translation/automatic-translate/automatic-translators/FakeAutomaticTranslator.ts 1.78ms
1099/2086 src/execution/translation/automatic-translate/automatic-translators/LindatAutomaticTranslator.ts 3.84ms
1100/2086 src/execution/translation/automatic-translate/automatic-translators/README.md 1.96ms
1101/2086 src/execution/translation/automatic-translate/automatic-translators/TranslatorOptions.ts 3.50ms
1102/2086 src/execution/translation/automatic-translate/automatic-translators/utils/extractMultiplicatedOccurrence.test.ts 2.04ms
1103/2086 src/execution/translation/automatic-translate/automatic-translators/utils/extractMultiplicatedOccurrence.ts 2.49ms
1104/2086 src/execution/translation/automatic-translate/translateMessages.ts 4.54ms
1105/2086 src/execution/UncertainNumber.ts 12.68ms
1106/2086 src/execution/Usage.ts 11.43ms
1107/2086 src/execution/UserInterfaceTools.ts 11.86ms
1108/2086 src/execution/utils/$provideExecutionToolsForNode.ts 5.14ms
1109/2086 src/execution/utils/addUsage.test.ts 4.62ms
1110/2086 src/execution/utils/addUsage.ts 6.95ms
1111/2086 src/execution/utils/checkExpectations.test.ts 10.42ms
1112/2086 src/execution/utils/checkExpectations.ts 13.99ms
1113/2086 src/execution/utils/computeUsageCounts.ts 8.29ms
1114/2086 src/execution/utils/forEachAsync.ts 4.41ms
1115/2086 src/execution/utils/formatUsagePrice.test.ts 5.11ms
1116/2086 src/execution/utils/formatUsagePrice.ts 2.27ms
1117/2086 src/execution/utils/uncertainNumber.ts 2.09ms
1118/2086 src/execution/utils/usage-constants.ts 3.00ms
1119/2086 src/execution/utils/usageToHuman.ts 9.06ms
1120/2086 src/execution/utils/usageToWorktime.test.ts 3.00ms
1121/2086 src/execution/utils/usageToWorktime.ts 3.09ms
1122/2086 src/execution/utils/validatePromptResult.ts 9.63ms
1123/2086 src/expectations/README.md 1.69ms
1124/2086 src/file-security-checkers/FileSecurityChecker.ts 2.78ms
1125/2086 src/file-security-checkers/FileSecurityCheckResult.ts 6.40ms
1126/2086 src/file-security-checkers/virustotal/VirusTotalFileSecurityChecker.test.ts 6.63ms
1127/2086 src/file-security-checkers/virustotal/VirusTotalFileSecurityChecker.ts 9.08ms
1128/2086 src/formats/_common/FormatParser.ts 6.61ms
1129/2086 src/formats/_common/FormatSubvalueParser.ts 11.08ms
1130/2086 src/formats/csv/CsvFormatError.ts 2.26ms
1131/2086 src/formats/csv/CsvFormatParser.ts 6.72ms
1132/2086 src/formats/csv/CsvSettings.ts 3.90ms
1133/2086 src/formats/csv/utils/csvParse.ts 2.96ms
1134/2086 src/formats/csv/utils/isValidCsvString.test.ts 2.66ms
1135/2086 src/formats/csv/utils/isValidCsvString.ts 2.55ms
1136/2086 src/formats/index.ts 4.09ms
1137/2086 src/formats/json/JsonFormatParser.ts 5.66ms
1138/2086 src/formats/json/utils/isValidJsonString.test.ts 5.25ms
1139/2086 src/formats/json/utils/isValidJsonString.ts 4.70ms
1140/2086 src/formats/json/utils/jsonParse.ts 2.08ms
1141/2086 src/formats/README.md 4.32ms
1142/2086 src/formats/text/TextFormatParser.ts 5.68ms
1143/2086 src/formats/xml/utils/isValidXmlString.test.ts 4.01ms
1144/2086 src/formats/xml/utils/isValidXmlString.ts 3.30ms
1145/2086 src/formats/xml/XmlFormatParser.ts 4.70ms
1146/2086 src/formfactors/_boilerplate/BoilerplateFormfactorDefinition.ts 3.86ms
1147/2086 src/formfactors/_common/AbstractFormfactorDefinition.ts 4.16ms
1148/2086 src/formfactors/_common/FormfactorDefinition.ts 4.14ms
1149/2086 src/formfactors/_common/string_formfactor_name.ts 3.69ms
1150/2086 src/formfactors/chatbot/ChatbotFormfactorDefinition.ts 3.43ms
1151/2086 src/formfactors/completion/CompletionFormfactorDefinition.ts 3.38ms
1152/2086 src/formfactors/generator/GeneratorFormfactorDefinition.ts 3.80ms
1153/2086 src/formfactors/generic/GenericFormfactorDefinition.ts 2.35ms
1154/2086 src/formfactors/image-generator/ImageGeneratorFormfactorDefinition.ts 3.86ms
1155/2086 src/formfactors/index.ts 3.05ms
1156/2086 src/formfactors/matcher/MatcherFormfactorDefinition.ts 3.68ms
1157/2086 src/formfactors/README.md 1.25ms
1158/2086 src/formfactors/sheets/SheetsFormfactorDefinition.ts 3.13ms
1159/2086 src/formfactors/translator/TranslatorFormfactorDefinition.ts 3.37ms
1160/2086 src/globals.d.ts 1.81ms
1161/2086 src/high-level-abstractions/_common/HighLevelAbstraction.ts 3.75ms
1162/2086 src/high-level-abstractions/implicit-formfactor/ImplicitFormfactorHla.ts 5.03ms
1163/2086 src/high-level-abstractions/index.ts 3.51ms
1164/2086 src/high-level-abstractions/quick-chatbot/QuickChatbotHla.ts 7.50ms
1165/2086 src/high-level-abstractions/README.md 1.55ms
1166/2086 src/import-plugins/$fileImportPlugins.ts 2.13ms
1167/2086 src/import-plugins/AgentFileImportPlugin.ts 3.00ms
1168/2086 src/import-plugins/FileImportPlugin.ts 1.78ms
1169/2086 src/import-plugins/JsonFileImportPlugin.ts 2.92ms
1170/2086 src/import-plugins/TextFileImportPlugin.ts 2.42ms
1171/2086 src/llm-providers/_common/filterModels.ts 5.14ms
1172/2086 src/llm-providers/_common/register/$llmToolsMetadataRegister.ts 2.27ms
1173/2086 src/llm-providers/_common/register/$llmToolsRegister.ts 3.37ms
1174/2086 src/llm-providers/_common/register/$provideEnvFilename.ts 3.77ms
1175/2086 src/llm-providers/_common/register/$provideLlmToolsConfigurationFromEnv.ts 7.18ms
1176/2086 src/llm-providers/_common/register/$provideLlmToolsForTestingAndScriptsAndPlayground.ts 5.79ms
1177/2086 src/llm-providers/_common/register/$provideLlmToolsForWizardOrCli.ts 11.02ms
1178/2086 src/llm-providers/_common/register/$provideLlmToolsFromEnv.ts 5.55ms
1179/2086 src/llm-providers/_common/register/$registeredLlmToolsMessage.ts 8.05ms
1180/2086 src/llm-providers/_common/register/$registeredLlmToolsMessage/createRegisteredLlmToolConfigurationStatusMessage.ts 3.81ms
1181/2086 src/llm-providers/_common/register/$registeredLlmToolsMessage/createRegisteredLlmToolEntryKey.ts 1.88ms
1182/2086 src/llm-providers/_common/register/$registeredLlmToolsMessage/createRegisteredLlmToolInstallationStatusMessage.ts 3.19ms
1183/2086 src/llm-providers/_common/register/$registeredLlmToolsMessage/createRegisteredLlmToolsMessageContext.ts 4.08ms
1184/2086 src/llm-providers/_common/register/$registeredLlmToolsMessage/createRegisteredLlmToolStatus.ts 5.34ms
1185/2086 src/llm-providers/_common/register/$registeredLlmToolsMessage/getAvailableRegisteredLlmToolsEnvironmentVariables.ts 2.73ms
1186/2086 src/llm-providers/_common/register/$registeredLlmToolsMessage/getRegisteredLlmToolRegisters.ts 2.26ms
1187/2086 src/llm-providers/_common/register/$registeredLlmToolsMessage/hasRegisteredLlmToolEntry.ts 3.42ms
1188/2086 src/llm-providers/_common/register/$registeredLlmToolsMessage/listRegisteredLlmToolEntries.ts 3.62ms
1189/2086 src/llm-providers/_common/register/$registeredLlmToolsMessage/listRegisteredLlmToolStatuses.ts 3.10ms
1190/2086 src/llm-providers/_common/register/$registeredLlmToolsMessage/RegisteredLlmToolsMessageContext.ts 4.74ms
1191/2086 src/llm-providers/_common/register/$registeredLlmToolsMessage/renderAvailableRegisteredLlmTools.ts 4.95ms
1192/2086 src/llm-providers/_common/register/$registeredLlmToolsMessage/renderNoRegisteredLlmToolsMessage.ts 2.09ms
1193/2086 src/llm-providers/_common/register/$registeredLlmToolsMessage/renderRegisteredLlmToolsMessage.ts 3.29ms
1194/2086 src/llm-providers/_common/register/$registeredLlmToolsMessage/renderRelevantRegisteredLlmToolsEnvironmentVariables.ts 4.58ms
1195/2086 src/llm-providers/_common/register/$registeredLlmToolsMessage/USED_ENV_FILENAME.ts 2.89ms
1196/2086 src/llm-providers/_common/register/createLlmToolsFromConfiguration.test.ts 3.04ms
1197/2086 src/llm-providers/_common/register/createLlmToolsFromConfiguration.ts 6.47ms
1198/2086 src/llm-providers/_common/register/LlmToolsConfiguration.ts 2.96ms
1199/2086 src/llm-providers/_common/register/LlmToolsMetadata.ts 4.85ms
1200/2086 src/llm-providers/_common/register/LlmToolsOptions.ts 2.64ms
1201/2086 src/llm-providers/_common/utils/assertUniqueModels.ts 3.05ms
1202/2086 src/llm-providers/_common/utils/cache/CacheItem.ts 4.39ms
1203/2086 src/llm-providers/_common/utils/cache/cacheLlmTools.ts 13.84ms
1204/2086 src/llm-providers/_common/utils/cache/CacheLlmToolsOptions.ts 2.95ms
1205/2086 src/llm-providers/_common/utils/count-total-usage/countUsage.ts 6.47ms
1206/2086 src/llm-providers/_common/utils/count-total-usage/limitTotalUsage.ts 6.43ms
1207/2086 src/llm-providers/_common/utils/count-total-usage/LlmExecutionToolsWithTotalUsage.ts 2.34ms
1208/2086 src/llm-providers/_common/utils/pricing.test.ts 2.61ms
1209/2086 src/llm-providers/_common/utils/pricing.ts 1.95ms
1210/2086 src/llm-providers/_common/utils/removeUnsupportedModelRequirements.test.ts 3.58ms
1211/2086 src/llm-providers/_common/utils/removeUnsupportedModelRequirements.ts 4.82ms
1212/2086 src/llm-providers/_multiple/getSingleLlmExecutionTools.ts 2.95ms
1213/2086 src/llm-providers/_multiple/joinLlmExecutionTools.ts 5.08ms
1214/2086 src/llm-providers/_multiple/MultipleLlmExecutionTools.ts 10.90ms
1215/2086 src/llm-providers/_multiple/playground/playground.ts 8.03ms
1216/2086 src/llm-providers/_multiple/playground/tsconfig.json 2.62ms
1217/2086 src/llm-providers/agent/Agent.test.ts 8.79ms
1218/2086 src/llm-providers/agent/Agent.ts 17.94ms
1219/2086 src/llm-providers/agent/AgentLlmExecutionTools.test.ts 11.22ms
1220/2086 src/llm-providers/agent/AgentLlmExecutionTools.ts 22.75ms
1221/2086 src/llm-providers/agent/AgentLlmExecutionToolsAgentKitRunner.ts 13.62ms
1222/2086 src/llm-providers/agent/AgentLlmExecutionToolsOpenAiAssistantRunner.ts 12.34ms
1223/2086 src/llm-providers/agent/AgentLlmExecutionToolsPromptPreparer.ts 12.92ms
1224/2086 src/llm-providers/agent/AgentOptions.ts 3.90ms
1225/2086 src/llm-providers/agent/createAgentLlmExecutionTools.ts 2.66ms
1226/2086 src/llm-providers/agent/CreateAgentLlmExecutionToolsOptions.ts 3.88ms
1227/2086 src/llm-providers/agent/emitAgentLlmExecutionToolsAssistantPreparationProgress.ts 3.98ms
1228/2086 src/llm-providers/agent/playground/playground.ts 10.37ms
1229/2086 src/llm-providers/agent/playground/tsconfig.json 2.79ms
1230/2086 src/llm-providers/agent/register-configuration.ts 3.55ms
1231/2086 src/llm-providers/agent/register-constructor.ts 3.06ms
1232/2086 src/llm-providers/agent/RemoteAgent.test.ts 8.72ms
1233/2086 src/llm-providers/agent/RemoteAgent.ts 23.90ms
1234/2086 src/llm-providers/agent/RemoteAgentOptions.ts 2.17ms
1235/2086 src/llm-providers/agent/self-learning/SelfLearningManager.test.ts 5.97ms
1236/2086 src/llm-providers/agent/self-learning/SelfLearningManager.ts 22.44ms
1237/2086 src/llm-providers/anthropic-claude/anthropic-claude-models.ts 13.71ms
1238/2086 src/llm-providers/anthropic-claude/AnthropicClaudeExecutionTools.ts 13.70ms
1239/2086 src/llm-providers/anthropic-claude/AnthropicClaudeExecutionToolsOptions.ts 3.30ms
1240/2086 src/llm-providers/anthropic-claude/computeAnthropicClaudeUsage.test.ts 3.24ms
1241/2086 src/llm-providers/anthropic-claude/computeAnthropicClaudeUsage.ts 7.10ms
1242/2086 src/llm-providers/anthropic-claude/createAnthropicClaudeExecutionTools.ts 3.88ms
1243/2086 src/llm-providers/anthropic-claude/playground/playground.ts 6.81ms
1244/2086 src/llm-providers/anthropic-claude/playground/tsconfig.json 2.34ms
1245/2086 src/llm-providers/anthropic-claude/register-configuration.ts 5.82ms
1246/2086 src/llm-providers/anthropic-claude/register-constructor.ts 4.00ms
1247/2086 src/llm-providers/azure-openai/AzureOpenAiExecutionTools.ts 13.71ms
1248/2086 src/llm-providers/azure-openai/AzureOpenAiExecutionToolsOptions.ts 3.91ms
1249/2086 src/llm-providers/azure-openai/createAzureOpenAiExecutionTools.ts 2.62ms
1250/2086 src/llm-providers/azure-openai/playground/playground.ts 6.69ms
1251/2086 src/llm-providers/azure-openai/playground/tsconfig.json 2.40ms
1252/2086 src/llm-providers/azure-openai/register-configuration.ts 5.94ms
1253/2086 src/llm-providers/azure-openai/register-constructor.ts 3.15ms
1254/2086 src/llm-providers/deepseek/createDeepseekExecutionTools.ts 4.11ms
1255/2086 src/llm-providers/deepseek/deepseek-models.ts 9.67ms
1256/2086 src/llm-providers/deepseek/DeepseekExecutionToolsOptions.ts 2.29ms
1257/2086 src/llm-providers/deepseek/other/compatibility-issue.md 2.78ms
1258/2086 src/llm-providers/deepseek/register-configuration.ts 5.10ms
1259/2086 src/llm-providers/deepseek/register-constructor.ts 5.82ms
1260/2086 src/llm-providers/google/createGoogleExecutionTools.ts 16.11ms
1261/2086 src/llm-providers/google/google-models.ts 11.10ms
1262/2086 src/llm-providers/google/GoogleExecutionToolsOptions.ts 2.70ms
1263/2086 src/llm-providers/google/other/compatibility-issue.md 2.44ms
1264/2086 src/llm-providers/google/register-configuration.ts 4.39ms
1265/2086 src/llm-providers/google/register-constructor.ts 2.82ms
1266/2086 src/llm-providers/mocked/$fakeTextToExpectations.ts 5.24ms
1267/2086 src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts 9.52ms
1268/2086 src/llm-providers/mocked/MockedFackedLlmExecutionTools.ts 7.71ms
1269/2086 src/llm-providers/mocked/test/faked-completion.test.ts 7.43ms
1270/2086 src/llm-providers/mocked/test/fakeTextToExpectations.test.ts 9.45ms
1271/2086 src/llm-providers/mocked/test/joker.test.ts 6.74ms
1272/2086 src/llm-providers/mocked/test/mocked-chat.test.ts 8.80ms
1273/2086 src/llm-providers/mocked/test/mocked-completion.test.ts 24.06ms
1274/2086 src/llm-providers/ollama/createOllamaExecutionTools.ts 4.58ms
1275/2086 src/llm-providers/ollama/ollama-models.ts 12.02ms
1276/2086 src/llm-providers/ollama/OllamaExecutionTools.ts 7.86ms
1277/2086 src/llm-providers/ollama/OllamaExecutionToolsOptions.ts 3.76ms
1278/2086 src/llm-providers/ollama/playground/playground.ts 8.26ms
1279/2086 src/llm-providers/ollama/playground/tsconfig.json 4.32ms
1280/2086 src/llm-providers/ollama/register-configuration.ts 4.00ms
1281/2086 src/llm-providers/ollama/register-constructor.ts 2.65ms
1282/2086 src/llm-providers/openai/computeOpenAiUsage.test.ts 4.43ms
1283/2086 src/llm-providers/openai/computeOpenAiUsage.ts 6.03ms
1284/2086 src/llm-providers/openai/createOpenAiAssistantExecutionTools.ts 3.72ms
1285/2086 src/llm-providers/openai/createOpenAiCompatibleExecutionTools.ts 15.95ms
1286/2086 src/llm-providers/openai/createOpenAiExecutionTools.ts 18.21ms
1287/2086 src/llm-providers/openai/openai-models.test.ts 9.03ms
1288/2086 src/llm-providers/openai/openai-models.ts 30.47ms
1289/2086 src/llm-providers/openai/OpenAiAgentKitExecutionTools.test.ts 4.61ms
1290/2086 src/llm-providers/openai/OpenAiAgentKitExecutionTools.ts 41.80ms
1291/2086 src/llm-providers/openai/OpenAiAgentKitExecutionToolsInputBuilder.ts 7.18ms
1292/2086 src/llm-providers/openai/OpenAiAgentKitExecutionToolsOptions.ts 2.70ms
1293/2086 src/llm-providers/openai/OpenAiAgentKitExecutionToolsOutputTypeMapper.ts 5.90ms
1294/2086 src/llm-providers/openai/OpenAiAgentKitExecutionToolsToolBuilder.ts 22.56ms
1295/2086 src/llm-providers/openai/OpenAiAssistantExecutionTools.ts 22.00ms
1296/2086 src/llm-providers/openai/OpenAiAssistantExecutionToolsOptions.ts 3.43ms
1297/2086 src/llm-providers/openai/OpenAiAssistantExecutionToolsProgressReporter.ts 9.53ms
1298/2086 src/llm-providers/openai/OpenAiAssistantExecutionToolsPromptBuilder.ts 8.76ms
1299/2086 src/llm-providers/openai/OpenAiAssistantExecutionToolsStreamRunner.ts 14.68ms
1300/2086 src/llm-providers/openai/OpenAiAssistantExecutionToolsToolRunner.ts 27.35ms
1301/2086 src/llm-providers/openai/OpenAiCompatibleExecutionTools.ts 10.62ms
1302/2086 src/llm-providers/openai/OpenAiCompatibleExecutionToolsOptions.ts 6.82ms
1303/2086 src/llm-providers/openai/OpenAiCompatibleModelCatalog.ts 7.39ms
1304/2086 src/llm-providers/openai/OpenAiCompatibleNonChatPromptCaller.ts 14.86ms
1305/2086 src/llm-providers/openai/OpenAiCompatibleRequestManager.ts 9.51ms
1306/2086 src/llm-providers/openai/OpenAiExecutionTools.ts 4.23ms
1307/2086 src/llm-providers/openai/OpenAiExecutionToolsOptions.ts 1.83ms
1308/2086 src/llm-providers/openai/OpenAiVectorStoreFileBatchHandler.ts 14.95ms
1309/2086 src/llm-providers/openai/OpenAiVectorStoreFileBatchPoller.ts 21.14ms
1310/2086 src/llm-providers/openai/OpenAiVectorStoreHandler.ts 9.99ms
1311/2086 src/llm-providers/openai/OpenAiVectorStoreKnowledgeSourcePreparer.ts 7.56ms
1312/2086 src/llm-providers/openai/playground/playground.ts 78.21ms
1313/2086 src/llm-providers/openai/playground/tsconfig.json 12.71ms
1314/2086 src/llm-providers/openai/register-configuration.ts 8.94ms
1315/2086 src/llm-providers/openai/register-constructor.ts 5.27ms
1316/2086 src/llm-providers/openai/utils/buildToolInvocationScript.ts 2.81ms
1317/2086 src/llm-providers/openai/utils/callOpenAiCompatibleChatModel.ts 12.29ms
1318/2086 src/llm-providers/openai/utils/loadOpenAiAgentsModule.ts 3.04ms
1319/2086 src/llm-providers/openai/utils/mapToolsToOpenAi.ts 2.48ms
1320/2086 src/llm-providers/openai/utils/OpenAiCompatibleChatProgressReporter.ts 9.07ms
1321/2086 src/llm-providers/openai/utils/OpenAiCompatibleChatPromptBuilder.ts 8.32ms
1322/2086 src/llm-providers/openai/utils/OpenAiCompatibleChatToolCaller.ts 11.38ms
1323/2086 src/llm-providers/openai/utils/OpenAiCompatibleUnsupportedParameterRetrier.ts 6.56ms
1324/2086 src/llm-providers/openai/utils/uploadFilesToOpenAi.ts 2.59ms
1325/2086 src/llm-providers/README.md 3.22ms
1326/2086 src/llm-providers/remote/playground/playground.ts 8.08ms
1327/2086 src/llm-providers/remote/playground/tsconfig.json 3.97ms
1328/2086 src/llm-providers/remote/RemoteLlmExecutionTools.ts 7.76ms
1329/2086 src/llm-providers/vercel/createExecutionToolsFromVercelProvider.ts 7.89ms
1330/2086 src/llm-providers/vercel/playground/playground.ts 7.64ms
1331/2086 src/llm-providers/vercel/playground/tsconfig.json 2.44ms
1332/2086 src/llm-providers/vercel/VercelExecutionToolsOptions.ts 5.29ms
1333/2086 src/llm-providers/vercel/VercelProvider.ts 2.28ms
1334/2086 src/migrations/migratePipeline.ts 3.80ms
1335/2086 src/other/README.md 1.09ms
1336/2086 src/other/templates/getBookTemplates.ts 6.83ms
1337/2086 src/personas/preparePersona.ts 6.81ms
1338/2086 src/personas/README.md 1.32ms
1339/2086 src/pipeline/book-notation.ts 3.08ms
1340/2086 src/pipeline/isValidPipelineString.test.ts 1.93ms
1341/2086 src/pipeline/isValidPipelineString.ts 2.37ms
1342/2086 src/pipeline/PipelineInterface/constants.ts 2.35ms
1343/2086 src/pipeline/PipelineInterface/getPipelineInterface.ts 3.47ms
1344/2086 src/pipeline/PipelineInterface/isPipelineImplementingInterface.ts 2.51ms
1345/2086 src/pipeline/PipelineInterface/isPipelineInterfacesEqual.ts 2.89ms
1346/2086 src/pipeline/PipelineInterface/PipelineInterface.ts 2.14ms
1347/2086 src/pipeline/PipelineInterface/README.md 1.41ms
1348/2086 src/pipeline/PipelineJson/CommonTaskJson.ts 4.74ms
1349/2086 src/pipeline/PipelineJson/DialogTaskJson.ts 2.12ms
1350/2086 src/pipeline/PipelineJson/Expectations.ts 2.23ms
1351/2086 src/pipeline/PipelineJson/KnowledgePieceJson.ts 3.40ms
1352/2086 src/pipeline/PipelineJson/KnowledgeSourceJson.ts 3.46ms
1353/2086 src/pipeline/PipelineJson/ParameterJson.ts 3.41ms
1354/2086 src/pipeline/PipelineJson/PersonaJson.ts 2.60ms
1355/2086 src/pipeline/PipelineJson/PipelineJson.ts 5.95ms
1356/2086 src/pipeline/PipelineJson/PreparationJson.ts 2.20ms
1357/2086 src/pipeline/PipelineJson/PromptTaskJson.ts 3.21ms
1358/2086 src/pipeline/PipelineJson/ScriptTaskJson.ts 2.78ms
1359/2086 src/pipeline/PipelineJson/SimpleTaskJson.ts 2.11ms
1360/2086 src/pipeline/PipelineJson/TaskJson.ts 1.92ms
1361/2086 src/pipeline/PipelineString.ts 1.74ms
1362/2086 src/pipeline/prompt-notation.test.ts 4.56ms
1363/2086 src/pipeline/prompt-notation.ts 7.52ms
1364/2086 src/pipeline/prompt-notation/helpers/ParameterEscaping.ts 4.51ms
1365/2086 src/pipeline/prompt-notation/helpers/ParameterNaming.ts 4.36ms
1366/2086 src/pipeline/prompt-notation/helpers/ParameterSection.ts 3.24ms
1367/2086 src/pipeline/validatePipelineString.ts 4.32ms
1368/2086 src/playground/permanent/_boilerplate.ts 1.78ms
1369/2086 src/playground/permanent/agent-with-browser-playground.ts 2.50ms
1370/2086 src/playground/permanent/error-handling-playground.ts 2.35ms
1371/2086 src/playground/playground.ts 2.29ms
1372/2086 src/playground/tsconfig.json 2.32ms
1373/2086 src/postprocessing/README.md 1.43ms
1374/2086 src/postprocessing/utils/extractBlock.ts 3.30ms
1375/2086 src/postprocessing/utils/extractJsonBlock.test.ts 3.21ms
1376/2086 src/postprocessing/utils/extractJsonBlock.ts 3.23ms
1377/2086 src/prepare/isPipelinePrepared.test.ts 2.44ms
1378/2086 src/prepare/isPipelinePrepared.ts 3.75ms
1379/2086 src/prepare/PrepareAndScrapeOptions.ts 3.09ms
1380/2086 src/prepare/preparePipeline.ts 11.12ms
1381/2086 src/prepare/preparePipelineOnRemoteServer.ts 3.53ms
1382/2086 src/prepare/prepareTasks.ts 6.05ms
1383/2086 src/prepare/unpreparePipeline.ts 3.90ms
1384/2086 src/remote-server/createRemoteClient.ts 4.25ms
1385/2086 src/remote-server/http-sample/README.md 1.49ms
1386/2086 src/remote-server/openapi-types.ts 8.61ms
1387/2086 src/remote-server/openapi.ts 9.61ms
1388/2086 src/remote-server/README.md 1.89ms
1389/2086 src/remote-server/RemoteServer.ts 2.09ms
1390/2086 src/remote-server/socket-types/_common/PromptbookServer_Error.ts 3.91ms
1391/2086 src/remote-server/socket-types/_subtypes/Identification.ts 4.41ms
1392/2086 src/remote-server/socket-types/_subtypes/identificationToPromptbookToken.ts 2.62ms
1393/2086 src/remote-server/socket-types/_subtypes/promptbookTokenToIdentification.ts 4.62ms
1394/2086 src/remote-server/socket-types/listModels/PromptbookServer_ListModels_Request.ts 2.12ms
1395/2086 src/remote-server/socket-types/listModels/PromptbookServer_ListModels_Response.ts 2.59ms
1396/2086 src/remote-server/socket-types/prepare/PromptbookServer_PreparePipeline_Request.ts 2.12ms
1397/2086 src/remote-server/socket-types/prepare/PromptbookServer_PreparePipeline_Response.ts 2.02ms
1398/2086 src/remote-server/socket-types/prompt/PromptbookServer_Prompt_Request.ts 1.69ms
1399/2086 src/remote-server/socket-types/prompt/PromptbookServer_Prompt_Response.ts 2.29ms
1400/2086 src/remote-server/startAgentServer.ts 4.16ms
1401/2086 src/remote-server/startRemoteServer.ts 6.33ms
1402/2086 src/remote-server/startRemoteServer/createRemoteServerExpressApp.ts 2.35ms
1403/2086 src/remote-server/startRemoteServer/createRemoteServerHandle.ts 2.54ms
1404/2086 src/remote-server/startRemoteServer/createSocketServer.ts 3.59ms
1405/2086 src/remote-server/startRemoteServer/getExecutionToolsFromIdentification.ts 5.94ms
1406/2086 src/remote-server/startRemoteServer/registerBookRoutes.ts 4.05ms
1407/2086 src/remote-server/startRemoteServer/registerExecutionRoutes.ts 7.71ms
1408/2086 src/remote-server/startRemoteServer/registerListModelsSocketHandler.ts 3.59ms
1409/2086 src/remote-server/startRemoteServer/registerLoginRoute.ts 3.54ms
1410/2086 src/remote-server/startRemoteServer/registerNotFoundRoute.ts 1.99ms
1411/2086 src/remote-server/startRemoteServer/registerOpenAiCompatibleChatCompletionsRoute.ts 5.85ms
1412/2086 src/remote-server/startRemoteServer/registerOpenApiRoutes.ts 3.54ms
1413/2086 src/remote-server/startRemoteServer/registerPreparePipelineSocketHandler.ts 5.60ms
1414/2086 src/remote-server/startRemoteServer/registerPromptSocketHandler.ts 5.60ms
1415/2086 src/remote-server/startRemoteServer/registerRemoteServerHttpRoutes.ts 7.65ms
1416/2086 src/remote-server/startRemoteServer/registerRemoteServerSocketHandlers.ts 2.92ms
1417/2086 src/remote-server/startRemoteServer/registerServerIndexRoute.ts 8.78ms
1418/2086 src/remote-server/startRemoteServer/RemoteServerRuntime.ts 2.33ms
1419/2086 src/remote-server/startRemoteServer/resolveStartRemoteServerConfiguration.ts 3.49ms
1420/2086 src/remote-server/startRemoteServer/respondToSocketRequest.ts 3.08ms
1421/2086 src/remote-server/startRemoteServer/SocketResponse.ts 1.51ms
1422/2086 src/remote-server/startRemoteServer/startListening.ts 2.28ms
1423/2086 src/remote-server/startRemoteServer/StartRemoteServerConfiguration.ts 2.92ms
1424/2086 src/remote-server/types/RemoteClientOptions.ts 2.58ms
1425/2086 src/remote-server/types/RemoteServerOptions.ts 7.49ms
1426/2086 src/remote-server/ui/renderServerIndexHtml.ts 8.61ms
1427/2086 src/remote-server/ui/ServerApp.tsx 8.42ms
1428/2086 src/remote-server/ui/types.ts 3.19ms
1429/2086 src/scrapers/_boilerplate/BoilerplateScraper.ts 9.32ms
1430/2086 src/scrapers/_boilerplate/createBoilerplateScraper.ts 3.42ms
1431/2086 src/scrapers/_boilerplate/playground/boilerplate-scraper-playground.ts 4.28ms
1432/2086 src/scrapers/_boilerplate/playground/tsconfig.json 2.29ms
1433/2086 src/scrapers/_boilerplate/README.md 2.10ms
1434/2086 src/scrapers/_boilerplate/register-constructor.ts 1.99ms
1435/2086 src/scrapers/_boilerplate/register-metadata.ts 3.43ms
1436/2086 src/scrapers/_common/Converter.ts 2.77ms
1437/2086 src/scrapers/_common/prepareKnowledgePieces.test.ts 3.67ms
1438/2086 src/scrapers/_common/prepareKnowledgePieces.ts 8.16ms
1439/2086 src/scrapers/_common/register/$provideFilesystemForNode.ts 3.21ms
1440/2086 src/scrapers/_common/register/$provideScrapersForBrowser.ts 3.61ms
1441/2086 src/scrapers/_common/register/$provideScrapersForNode.ts 4.95ms
1442/2086 src/scrapers/_common/register/$provideScriptingForNode.ts 3.65ms
1443/2086 src/scrapers/_common/register/$registeredScrapersMessage.ts 5.43ms
1444/2086 src/scrapers/_common/register/$scrapersMetadataRegister.ts 4.57ms
1445/2086 src/scrapers/_common/register/$scrapersRegister.ts 2.16ms
1446/2086 src/scrapers/_common/register/ScraperAndConverterMetadata.ts 4.00ms
1447/2086 src/scrapers/_common/register/ScraperConstructor.ts 2.28ms
1448/2086 src/scrapers/_common/Scraper.ts 4.09ms
1449/2086 src/scrapers/_common/ScraperIntermediateSource.ts 2.05ms
1450/2086 src/scrapers/_common/utils/fetchUrlContent.ts 6.55ms
1451/2086 src/scrapers/_common/utils/getScraperIntermediateSource.test.ts 2.88ms
1452/2086 src/scrapers/_common/utils/getScraperIntermediateSource.ts 6.75ms
1453/2086 src/scrapers/_common/utils/makeKnowledgeSourceHandler.ts 15.35ms
1454/2086 src/scrapers/_common/utils/promptbookFetch.test.ts 2.60ms
1455/2086 src/scrapers/_common/utils/promptbookFetch.ts 3.15ms
1456/2086 src/scrapers/document-legacy/createLegacyDocumentScraper.ts 4.27ms
1457/2086 src/scrapers/document-legacy/examples/10-simple.doc.knowledge.json 40.16ms
1458/2086 src/scrapers/document-legacy/examples/10-simple.rtf.knowledge.json 18.59ms
1459/2086 src/scrapers/document-legacy/LegacyDocumentScraper.test.ts 20.25ms
1460/2086 src/scrapers/document-legacy/LegacyDocumentScraper.ts 12.21ms
1461/2086 src/scrapers/document-legacy/playground/legacy-document-scraper-playground.ts 6.74ms
1462/2086 src/scrapers/document-legacy/playground/tsconfig.json 8.71ms
1463/2086 src/scrapers/document-legacy/README.md 3.67ms
1464/2086 src/scrapers/document-legacy/register-constructor.ts 3.33ms
1465/2086 src/scrapers/document-legacy/register-metadata.ts 6.81ms
1466/2086 src/scrapers/document/createDocumentScraper.ts 3.35ms
1467/2086 src/scrapers/document/DocumentScraper.test.ts 8.15ms
1468/2086 src/scrapers/document/DocumentScraper.ts 15.85ms
1469/2086 src/scrapers/document/examples/10-simple.docx.knowledge.json 28.04ms
1470/2086 src/scrapers/document/examples/10-simple.odt.knowledge.json 29.77ms
1471/2086 src/scrapers/document/playground/document-scraper-playground.ts 5.29ms
1472/2086 src/scrapers/document/playground/tsconfig.json 4.85ms
1473/2086 src/scrapers/document/README.md 1.80ms
1474/2086 src/scrapers/document/register-constructor.ts 2.39ms
1475/2086 src/scrapers/document/register-metadata.ts 6.58ms
1476/2086 src/scrapers/markdown/createMarkdownScraper.ts 2.99ms
1477/2086 src/scrapers/markdown/MarkdownScraper.test.ts 6.63ms
1478/2086 src/scrapers/markdown/MarkdownScraper.ts 15.09ms
1479/2086 src/scrapers/markdown/playground/markdown-scraper-playground.ts 8.90ms
1480/2086 src/scrapers/markdown/playground/tsconfig.json 2.72ms
1481/2086 src/scrapers/markdown/README.md 2.77ms
1482/2086 src/scrapers/markdown/register-constructor.ts 3.49ms
1483/2086 src/scrapers/markdown/register-metadata.ts 25.26ms
1484/2086 src/scrapers/markitdown/createMarkitdownScraper.ts 8.31ms
1485/2086 src/scrapers/markitdown/examples/10-simple.pdf.knowledge.json 18.30ms
1486/2086 src/scrapers/markitdown/examples/collboard-fra1-cdn-digitaloceanspaces-com-ptbk-user-knowledge-source-b-d-10-simple-pdf.knowledge.json 13.80ms
1487/2086 src/scrapers/markitdown/MarkitdownScraper.ts 10.83ms
1488/2086 src/scrapers/markitdown/playground/markitdown-scraper-playground.ts 4.99ms
1489/2086 src/scrapers/markitdown/playground/tsconfig.json 6.08ms
1490/2086 src/scrapers/markitdown/README.md 1.23ms
1491/2086 src/scrapers/markitdown/register-constructor.ts 2.62ms
1492/2086 src/scrapers/markitdown/register-metadata.ts 9.39ms
1493/2086 src/scrapers/pdf/createPdfScraper.ts 2.99ms
1494/2086 src/scrapers/pdf/lib/pdf2md/models/Annotation.js 1.88ms
1495/2086 src/scrapers/pdf/lib/pdf2md/models/HeadlineFinder.js 3.30ms
1496/2086 src/scrapers/pdf/lib/pdf2md/models/LineConverter.js 15.94ms
1497/2086 src/scrapers/pdf/lib/pdf2md/models/LineItem.js 12.29ms
1498/2086 src/scrapers/pdf/lib/pdf2md/models/LineItemBlock.js 4.94ms
1499/2086 src/scrapers/pdf/lib/pdf2md/models/markdown/BlockType.js 14.66ms
1500/2086 src/scrapers/pdf/lib/pdf2md/models/markdown/WordFormat.js 4.39ms
1501/2086 src/scrapers/pdf/lib/pdf2md/models/markdown/WordType.js 2.47ms
1502/2086 src/scrapers/pdf/lib/pdf2md/models/Metadata.js 14.92ms
1503/2086 src/scrapers/pdf/lib/pdf2md/models/Page.js 6.45ms
1504/2086 src/scrapers/pdf/lib/pdf2md/models/PageItem.js 1.96ms
1505/2086 src/scrapers/pdf/lib/pdf2md/models/ParsedElements.js 2.14ms
1506/2086 src/scrapers/pdf/lib/pdf2md/models/ParseResult.js 2.16ms
1507/2086 src/scrapers/pdf/lib/pdf2md/models/StashingStream.js 3.63ms
1508/2086 src/scrapers/pdf/lib/pdf2md/models/TextItem.js 2.32ms
1509/2086 src/scrapers/pdf/lib/pdf2md/models/TextItemLineGrouper.js 19.00ms
1510/2086 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item-block/DetectCodeQuoteBlocks.js 3.34ms
1511/2086 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item-block/DetectListLevels.js 6.22ms
1512/2086 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item-block/GatherBlocks.js 6.92ms
1513/2086 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/CompactLines.js 11.32ms
1514/2086 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/DetectHeaders.js 9.65ms
1515/2086 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/DetectListItems.js 5.03ms
1516/2086 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/DetectTOC.js 14.18ms
1517/2086 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/RemoveRepetitiveElements.js 7.16ms
1518/2086 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/VerticalToHorizontal.js 5.21ms
1519/2086 src/scrapers/pdf/lib/pdf2md/models/transformations/text-item/CalculateGlobalStats.js 7.17ms
1520/2086 src/scrapers/pdf/lib/pdf2md/models/transformations/ToLineItemBlockTransformation.js 4.78ms
1521/2086 src/scrapers/pdf/lib/pdf2md/models/transformations/ToLineItemTransformation.js 3.66ms
1522/2086 src/scrapers/pdf/lib/pdf2md/models/transformations/ToMarkdown.js 5.15ms
1523/2086 src/scrapers/pdf/lib/pdf2md/models/transformations/ToTextBlocks.js 6.70ms
1524/2086 src/scrapers/pdf/lib/pdf2md/models/transformations/ToTextItemTransformation.js 2.84ms
1525/2086 src/scrapers/pdf/lib/pdf2md/models/transformations/Transformation.js 3.98ms
1526/2086 src/scrapers/pdf/lib/pdf2md/models/Word.js 2.42ms
1527/2086 src/scrapers/pdf/lib/pdf2md/pdf2md-cli.js 4.34ms
1528/2086 src/scrapers/pdf/lib/pdf2md/pdf2md.js 3.85ms
1529/2086 src/scrapers/pdf/lib/pdf2md/README.md 2.42ms
1530/2086 src/scrapers/pdf/lib/pdf2md/util/cli.js 3.40ms
1531/2086 src/scrapers/pdf/lib/pdf2md/util/dom-stubs.js 3.58ms
1532/2086 src/scrapers/pdf/lib/pdf2md/util/dom-stubs/createDocumentStub.js 3.82ms
1533/2086 src/scrapers/pdf/lib/pdf2md/util/dom-stubs/DOMElement.js 11.42ms
1534/2086 src/scrapers/pdf/lib/pdf2md/util/dom-stubs/DOMElementSerializer.js 3.78ms
1535/2086 src/scrapers/pdf/lib/pdf2md/util/dom-stubs/Image.js 2.43ms
1536/2086 src/scrapers/pdf/lib/pdf2md/util/dom-stubs/xmlEncode.js 2.12ms
1537/2086 src/scrapers/pdf/lib/pdf2md/util/page-item-functions.js 2.61ms
1538/2086 src/scrapers/pdf/lib/pdf2md/util/page-number-functions.js 5.25ms
1539/2086 src/scrapers/pdf/lib/pdf2md/util/pdf.js 8.14ms
1540/2086 src/scrapers/pdf/lib/pdf2md/util/string-functions.js 4.40ms
1541/2086 src/scrapers/pdf/lib/pdf2md/util/transformations.js 5.21ms
1542/2086 src/scrapers/pdf/PdfScraper.ts 5.67ms
1543/2086 src/scrapers/pdf/playground/pdf-scraper-playground.ts 4.51ms
1544/2086 src/scrapers/pdf/playground/tsconfig.json 2.53ms
1545/2086 src/scrapers/pdf/README.md 1.83ms
1546/2086 src/scrapers/pdf/register-constructor.ts 2.05ms
1547/2086 src/scrapers/pdf/register-metadata.ts 4.10ms
1548/2086 src/scrapers/README.md 1.34ms
1549/2086 src/scrapers/website/createWebsiteScraper.ts 2.53ms
1550/2086 src/scrapers/website/playground/tsconfig.json 2.32ms
1551/2086 src/scrapers/website/playground/website-scraper-playground.ts 6.10ms
1552/2086 src/scrapers/website/README.md 1.61ms
1553/2086 src/scrapers/website/register-constructor.ts 2.06ms
1554/2086 src/scrapers/website/register-metadata.ts 6.04ms
1555/2086 src/scrapers/website/utils/createShowdownConverter.test.ts 3.20ms
1556/2086 src/scrapers/website/utils/createShowdownConverter.ts 3.14ms
1557/2086 src/scrapers/website/WebsiteScraper.ts 11.63ms
1558/2086 src/scripting/_test/custom-function-async.test.ts.test.ts 5.68ms
1559/2086 src/scripting/_test/custom-function-missing.test.ts 4.54ms
1560/2086 src/scripting/_test/custom-function-with-dependencies.test.ts 6.50ms
1561/2086 src/scripting/_test/custom-function.test.ts 6.37ms
1562/2086 src/scripting/_test/postprocessing.test.ts 7.12ms
1563/2086 src/scripting/_test/script-execution-errors.test.ts 10.26ms
1564/2086 src/scripting/_test/script-execution-tools.test.ts 5.35ms
1565/2086 src/scripting/javascript/JavascriptEvalExecutionTools.test.ts 5.52ms
1566/2086 src/scripting/javascript/JavascriptEvalExecutionTools.ts 10.98ms
1567/2086 src/scripting/javascript/JavascriptExecutionTools.ts 1.67ms
1568/2086 src/scripting/javascript/JavascriptExecutionToolsOptions.ts 3.24ms
1569/2086 src/scripting/javascript/postprocessing-functions.ts 4.66ms
1570/2086 src/scripting/javascript/utils/extractVariablesFromJavascript.test.ts 2.05ms
1571/2086 src/scripting/javascript/utils/extractVariablesFromJavascript.ts 5.37ms
1572/2086 src/scripting/javascript/utils/extractVariablesFromScript.test.ts 2.38ms
1573/2086 src/scripting/python/PythonExecutionTools.ts 3.57ms
1574/2086 src/scripting/typescript/TypescriptExecutionTools.ts 2.94ms
1575/2086 src/search-engines/_index.ts 2.95ms
1576/2086 src/search-engines/bing/BingSearchEngine.ts 5.59ms
1577/2086 src/search-engines/dummy/DummySearchEngine.ts 4.83ms
1578/2086 src/search-engines/google/GoogleSearchEngine.ts 3.64ms
1579/2086 src/search-engines/SearchEngine.ts 2.02ms
1580/2086 src/search-engines/SearchResult.ts 1.55ms
1581/2086 src/search-engines/serp/SerpSearchEngine.ts 3.52ms
1582/2086 src/speech-recognition/BrowserSpeechRecognition.ts 4.74ms
1583/2086 src/speech-recognition/OpenAiSpeechRecognition.test.ts 3.80ms
1584/2086 src/speech-recognition/OpenAiSpeechRecognition.ts 21.13ms
1585/2086 src/storage/_common/PromptbookStorage.test-type.ts 2.05ms
1586/2086 src/storage/_common/PromptbookStorage.ts 3.22ms
1587/2086 src/storage/blackhole/BlackholeStorage.ts 3.35ms
1588/2086 src/storage/env-storage/$EnvStorage.ts 5.09ms
1589/2086 src/storage/file-cache-storage/FileCacheStorage.ts 7.94ms
1590/2086 src/storage/file-cache-storage/FileCacheStorageOptions.ts 2.47ms
1591/2086 src/storage/file-cache-storage/utils/nameToSubfolderPath.test.ts 1.85ms
1592/2086 src/storage/file-cache-storage/utils/nameToSubfolderPath.ts 4.55ms
1593/2086 src/storage/local-storage/getIndexedDbStorage.ts 3.61ms
1594/2086 src/storage/local-storage/getLocalStorage.ts 2.79ms
1595/2086 src/storage/local-storage/getSessionStorage.ts 6.07ms
1596/2086 src/storage/local-storage/utils/IndexedDbStorageOptions.ts 2.27ms
1597/2086 src/storage/local-storage/utils/makePromptbookStorageFromIndexedDb.ts 3.41ms
1598/2086 src/storage/local-storage/utils/makePromptbookStorageFromWebStorage.ts 6.43ms
1599/2086 src/storage/memory/MemoryStorage.ts 3.21ms
1600/2086 src/storage/utils/PrefixStorage.ts 3.96ms
1601/2086 src/transpilers/_common/BookTranspiler.ts 3.57ms
1602/2086 src/transpilers/_common/BookTranspilerOptions.ts 6.40ms
1603/2086 src/transpilers/_common/createTranspiledTeamRuntimeSection.ts 19.41ms
1604/2086 src/transpilers/_common/createZodSchemaSource.ts 8.38ms
1605/2086 src/transpilers/_common/formatUsedToolFunctions.test.ts 3.58ms
1606/2086 src/transpilers/_common/formatUsedToolFunctions.ts 6.92ms
1607/2086 src/transpilers/_common/prepareSdkTranspilerContext.ts 12.80ms
1608/2086 src/transpilers/_common/register/$bookTranspilersRegister.ts 2.51ms
1609/2086 src/transpilers/_common/resolveClaudeModelName.ts 4.56ms
1610/2086 src/transpilers/_common/TranspiledTeamExport.ts 5.08ms
1611/2086 src/transpilers/_common/transpiledTeamTranspilers.test.ts 11.65ms
1612/2086 src/transpilers/agent-os/AgentOsTranspiler.test.ts 7.50ms
1613/2086 src/transpilers/agent-os/AgentOsTranspiler.ts 23.21ms
1614/2086 src/transpilers/agent-os/register.ts 2.23ms
1615/2086 src/transpilers/anthropic-claude-managed/AnthropicClaudeManagedTranspiler.test.ts 5.34ms
1616/2086 src/transpilers/anthropic-claude-managed/AnthropicClaudeManagedTranspiler.ts 24.05ms
1617/2086 src/transpilers/anthropic-claude-managed/register.ts 2.36ms
1618/2086 src/transpilers/anthropic-claude-sdk/AnthropicClaudeSdkTranspiler.test.ts 20.74ms
1619/2086 src/transpilers/anthropic-claude-sdk/AnthropicClaudeSdkTranspiler.ts 14.48ms
1620/2086 src/transpilers/anthropic-claude-sdk/register.ts 2.89ms
1621/2086 src/transpilers/e2b/E2BTranspiler.test.ts 8.27ms
1622/2086 src/transpilers/e2b/E2BTranspiler.ts 8.42ms
1623/2086 src/transpilers/e2b/register.ts 2.24ms
1624/2086 src/transpilers/formatted-book-in-markdown/FormattedBookInMarkdownTranspiler.ts 5.99ms
1625/2086 src/transpilers/formatted-book-in-markdown/register.ts 9.50ms
1626/2086 src/transpilers/openai-agents/OpenAiAgentsTranspiler.test.ts 9.54ms
1627/2086 src/transpilers/openai-agents/OpenAiAgentsTranspiler.ts 16.64ms
1628/2086 src/transpilers/openai-agents/register.ts 2.63ms
1629/2086 src/transpilers/openai-sdk/OpenAiSdkTranspiler.test.ts 8.91ms
1630/2086 src/transpilers/openai-sdk/OpenAiSdkTranspiler.ts 12.25ms
1631/2086 src/transpilers/openai-sdk/playground/playground.ts 2.38ms
1632/2086 src/transpilers/openai-sdk/playground/tsconfig.json 2.59ms
1633/2086 src/transpilers/openai-sdk/register.ts 5.84ms
1634/2086 src/types/Arrayable.ts 2.80ms
1635/2086 src/types/InputParameters_private.ts 5.95ms
1636/2086 src/types/IntermediateFilesStrategy.ts 2.67ms
1637/2086 src/types/LlmCall.ts 5.85ms
1638/2086 src/types/LlmToolDefinition.ts 2.90ms
1639/2086 src/types/Message.ts 10.35ms
1640/2086 src/types/ModelRequirements.ts 24.51ms
1641/2086 src/types/ModelVariant.ts 2.59ms
1642/2086 src/types/NonEmptyArray.ts 1.71ms
1643/2086 src/types/number_bytes.ts 1.55ms
1644/2086 src/types/number_id.ts 1.61ms
1645/2086 src/types/number_likeness.ts 1.28ms
1646/2086 src/types/number_milliseconds.ts 1.99ms
1647/2086 src/types/number_percent.ts 3.03ms
1648/2086 src/types/number_positive.ts 2.27ms
1649/2086 src/types/number_usd.ts 2.08ms
1650/2086 src/types/Parameters_private.ts 2.21ms
1651/2086 src/types/Parameters.ts 2.63ms
1652/2086 src/types/Prompt.ts 6.91ms
1653/2086 src/types/ReservedParameters_private.ts 2.41ms
1654/2086 src/types/ScriptLanguage.ts 2.10ms
1655/2086 src/types/SectionType.ts 2.36ms
1656/2086 src/types/SpeechRecognition.ts 4.43ms
1657/2086 src/types/string_agent_hash_private.ts 1.59ms
1658/2086 src/types/string_agent_name_in_book_private.ts 2.83ms
1659/2086 src/types/string_agent_name_private.ts 1.49ms
1660/2086 src/types/string_agent_name.ts 2.30ms
1661/2086 src/types/string_agent_permanent_id_private.ts 1.45ms
1662/2086 src/types/string_agent_url_private.ts 1.53ms
1663/2086 src/types/string_agent_url.ts 1.22ms
1664/2086 src/types/string_base_url_private.ts 1.26ms
1665/2086 src/types/string_base_url.ts 1.50ms
1666/2086 src/types/string_base64_private.ts 3.28ms
1667/2086 src/types/string_base64.ts 1.82ms
1668/2086 src/types/string_business_category_name_private.ts 1.27ms
1669/2086 src/types/string_business_category_name.ts 1.37ms
1670/2086 src/types/string_char_private.ts 1.52ms
1671/2086 src/types/string_chat_prompt_private.ts 1.36ms
1672/2086 src/types/string_completion_prompt_private.ts 2.21ms
1673/2086 src/types/string_email_private.ts 1.45ms
1674/2086 src/types/string_email.ts 1.63ms
1675/2086 src/types/string_filename.ts 2.21ms
1676/2086 src/types/string_host_private.ts 1.53ms
1677/2086 src/types/string_host.ts 1.67ms
1678/2086 src/types/string_href_private.ts 2.63ms
1679/2086 src/types/string_href.ts 1.39ms
1680/2086 src/types/string_knowledge_source_content.ts 1.93ms
1681/2086 src/types/string_markdown.ts 3.65ms
1682/2086 src/types/string_mime_type_private.ts 1.73ms
1683/2086 src/types/string_mime_type.ts 1.51ms
1684/2086 src/types/string_model_description_private.ts 1.40ms
1685/2086 src/types/string_model_name_private.ts 1.67ms
1686/2086 src/types/string_model_name.ts 1.07ms
1687/2086 src/types/string_name_private.ts 1.98ms
1688/2086 src/types/string_name.ts 1.94ms
1689/2086 src/types/string_page_private.ts 1.21ms
1690/2086 src/types/string_page.ts 1.19ms
1691/2086 src/types/string_parameter_name.ts 1.85ms
1692/2086 src/types/string_parameter_value_private.ts 1.59ms
1693/2086 src/types/string_person_fullname.ts 3.59ms
1694/2086 src/types/string_persona_description_private.ts 1.41ms
1695/2086 src/types/string_persona_description.ts 1.41ms
1696/2086 src/types/string_pipeline_root_url_private.ts 1.16ms
1697/2086 src/types/string_pipeline_root_url.ts 1.88ms
1698/2086 src/types/string_pipeline_url_private.ts 1.91ms
1699/2086 src/types/string_pipeline_url.ts 1.35ms
1700/2086 src/types/string_prompt_image_private.ts 1.18ms
1701/2086 src/types/string_prompt_private.ts 1.13ms
1702/2086 src/types/string_prompt.ts 2.84ms
1703/2086 src/types/string_promptbook_server_url_private.ts 2.79ms
1704/2086 src/types/string_promptbook_server_url.ts 1.52ms
1705/2086 src/types/string_reserved_parameter_name_private.ts 2.78ms
1706/2086 src/types/string_sha256.ts 2.89ms
1707/2086 src/types/string_system_message_private.ts 1.52ms
1708/2086 src/types/string_template_private.ts 2.57ms
1709/2086 src/types/string_text_prompt_private.ts 1.53ms
1710/2086 src/types/string_title_private.ts 1.25ms
1711/2086 src/types/string_title.ts 1.63ms
1712/2086 src/types/string_token.ts 2.35ms
1713/2086 src/types/string_url_image_private.ts 1.62ms
1714/2086 src/types/string_url_image.ts 1.30ms
1715/2086 src/types/string_url_private.ts 1.19ms
1716/2086 src/types/string_url.ts 2.12ms
1717/2086 src/types/TaskType.ts 1.91ms
1718/2086 src/types/ToolCall.ts 5.60ms
1719/2086 src/types/typeAliasEmoji.ts 4.43ms
1720/2086 src/types/typeAliases.ts 5.25ms
1721/2086 src/types/Updatable.ts 2.42ms
1722/2086 src/utils/agent-message-runtime/AgentMessageProjectChange.ts 7.65ms
1723/2086 src/utils/agent-message-runtime/agentMessageRuntimePaths.ts 6.75ms
1724/2086 src/utils/agent-message-runtime/AgentMessageTouchedExternalSource.ts 5.90ms
1725/2086 src/utils/agent-message-runtime/parseAgentMessageRuntimeLogEvents.ts 6.98ms
1726/2086 src/utils/agent-message-runtime/resolveAgentMessageRuntimeActivity.test.ts 6.74ms
1727/2086 src/utils/agent-message-runtime/resolveAgentMessageRuntimeActivity.ts 8.97ms
1728/2086 src/utils/agent-message-runtime/resolveAgentMessageTouchedExternalSources.test.ts 6.11ms
1729/2086 src/utils/agent-message-runtime/resolveAgentMessageTouchedExternalSources.ts 12.03ms
1730/2086 src/utils/agent-message-runtime/resolveAgentMessageTouchedProjectNames.test.ts 3.18ms
1731/2086 src/utils/agent-message-runtime/resolveAgentMessageTouchedProjectNames.ts 7.34ms
1732/2086 src/utils/agents/resolveAgentAvatarImageUrl.test.ts 3.90ms
1733/2086 src/utils/agents/resolveAgentAvatarImageUrl.ts 8.08ms
1734/2086 src/utils/agents/terminalAgentAvatarVisual.test.ts 3.63ms
1735/2086 src/utils/agents/terminalAgentAvatarVisual.ts 12.35ms
1736/2086 src/utils/ascii-art/$detectTerminalAnsiColorDepth.ts 3.57ms
1737/2086 src/utils/ascii-art/convertImageDataToAsciiArt.ts 13.04ms
1738/2086 src/utils/ascii-art/createAnsiColorCode.ts 5.53ms
1739/2086 src/utils/chat/chatAttachments.test.ts 9.04ms
1740/2086 src/utils/chat/chatAttachments.ts 7.01ms
1741/2086 src/utils/chat/chatAttachments/appendChatAttachmentContext.ts 3.03ms
1742/2086 src/utils/chat/chatAttachments/appendChatAttachmentContextWithContent.ts 3.29ms
1743/2086 src/utils/chat/chatAttachments/appendChatContextSections.ts 4.05ms
1744/2086 src/utils/chat/chatAttachments/formatChatAttachmentContentContext.ts 4.96ms
1745/2086 src/utils/chat/chatAttachments/formatChatAttachmentContext.ts 4.79ms
1746/2086 src/utils/chat/chatAttachments/normalizeChatAttachments.ts 6.58ms
1747/2086 src/utils/chat/chatAttachments/resolveChatAttachmentContent.ts 6.84ms
1748/2086 src/utils/chat/chatAttachments/resolveChatAttachmentContents.ts 4.38ms
1749/2086 src/utils/chat/constants.ts 2.00ms
1750/2086 src/utils/chat/decodeChatStreamWhitespaceFromTransport.ts 2.27ms
1751/2086 src/utils/chat/encodeChatStreamWhitespaceForTransport.ts 2.28ms
1752/2086 src/utils/chat/escapeRegExp.ts 1.83ms
1753/2086 src/utils/clientVersion.ts 3.99ms
1754/2086 src/utils/color/$randomColor.ts 2.39ms
1755/2086 src/utils/color/Color.test.ts 3.00ms
1756/2086 src/utils/color/Color.ts 5.90ms
1757/2086 src/utils/color/ColorValue.ts 3.11ms
1758/2086 src/utils/color/css-colors.ts 6.21ms
1759/2086 src/utils/color/internal-utils/checkChannelValue.ts 1.91ms
1760/2086 src/utils/color/internal-utils/hslToRgb.ts 2.33ms
1761/2086 src/utils/color/internal-utils/rgbToHsl.ts 1.95ms
1762/2086 src/utils/color/isHexColorString.ts 2.25ms
1763/2086 src/utils/color/operators/ColorTransformer.ts 1.40ms
1764/2086 src/utils/color/operators/darken.ts 1.84ms
1765/2086 src/utils/color/operators/furthest.ts 2.31ms
1766/2086 src/utils/color/operators/grayscale.ts 1.98ms
1767/2086 src/utils/color/operators/lighten.ts 2.14ms
1768/2086 src/utils/color/operators/mixWithColor.ts 2.03ms
1769/2086 src/utils/color/operators/nearest.ts 2.46ms
1770/2086 src/utils/color/operators/negative.ts 2.24ms
1771/2086 src/utils/color/operators/negativeLightness.ts 1.95ms
1772/2086 src/utils/color/operators/README.md 1.39ms
1773/2086 src/utils/color/operators/saturate.ts 2.83ms
1774/2086 src/utils/color/operators/withAlpha.ts 1.60ms
1775/2086 src/utils/color/parseColorString.ts 4.30ms
1776/2086 src/utils/color/parsers/ColorChannelSet.ts 1.47ms
1777/2086 src/utils/color/parsers/parseHexColor.ts 2.92ms
1778/2086 src/utils/color/parsers/parseHslColor.ts 5.77ms
1779/2086 src/utils/color/parsers/parseRgbColor.ts 2.80ms
1780/2086 src/utils/color/utils/areColorsEqual.ts 1.77ms
1781/2086 src/utils/color/utils/colorDistance.ts 2.14ms
1782/2086 src/utils/color/utils/colorHue.ts 2.25ms
1783/2086 src/utils/color/utils/colorHueDistance.test.ts 2.44ms
1784/2086 src/utils/color/utils/colorHueDistance.ts 1.57ms
1785/2086 src/utils/color/utils/colorLuminance.ts 1.91ms
1786/2086 src/utils/color/utils/colorSatulightion.ts 4.87ms
1787/2086 src/utils/color/utils/colorSaturation.ts 1.70ms
1788/2086 src/utils/color/utils/colorToDataUrl.ts 3.20ms
1789/2086 src/utils/color/utils/mixColors.ts 1.96ms
1790/2086 src/utils/database/uniqueConstraint.ts 3.91ms
1791/2086 src/utils/DEFAULT_THINKING_MESSAGES.ts 2.11ms
1792/2086 src/utils/editable/edit-pipeline-string/addPipelineCommand.test.ts 2.57ms
1793/2086 src/utils/editable/edit-pipeline-string/addPipelineCommand.ts 4.44ms
1794/2086 src/utils/editable/edit-pipeline-string/deflatePipeline.test.ts 2.64ms
1795/2086 src/utils/editable/edit-pipeline-string/deflatePipeline.ts 3.37ms
1796/2086 src/utils/editable/edit-pipeline-string/removePipelineCommand.test.ts 2.64ms
1797/2086 src/utils/editable/edit-pipeline-string/removePipelineCommand.ts 3.66ms
1798/2086 src/utils/editable/types/PipelineEditableSerialized.ts 3.17ms
1799/2086 src/utils/editable/utils/isFlatPipeline.test.ts 2.89ms
1800/2086 src/utils/editable/utils/isFlatPipeline.ts 3.04ms
1801/2086 src/utils/editable/utils/renamePipelineParameter.test.ts 6.02ms
1802/2086 src/utils/editable/utils/renamePipelineParameter.ts 4.74ms
1803/2086 src/utils/editable/utils/stringifyPipelineJson.ts 7.32ms
1804/2086 src/utils/environment/$detectRuntimeEnvironment.ts 2.24ms
1805/2086 src/utils/environment/$getGlobalScope.ts 1.56ms
1806/2086 src/utils/environment/$isRunningInBrowser.ts 2.36ms
1807/2086 src/utils/environment/$isRunningInJest.ts 2.60ms
1808/2086 src/utils/environment/$isRunningInNode.ts 2.02ms
1809/2086 src/utils/environment/$isRunningInWebWorker.ts 2.48ms
1810/2086 src/utils/execCommand/$execCommand.ts 5.64ms
1811/2086 src/utils/execCommand/$execCommandNormalizeOptions.ts 3.38ms
1812/2086 src/utils/execCommand/$execCommands.ts 2.46ms
1813/2086 src/utils/execCommand/execCommand.test.ts 1.96ms
1814/2086 src/utils/execCommand/execCommandNormalizeOptions.test.ts 3.77ms
1815/2086 src/utils/execCommand/ExecCommandOptions.ts 3.67ms
1816/2086 src/utils/expectation-counters/constants.ts 2.18ms
1817/2086 src/utils/expectation-counters/countCharacters.test.ts 2.30ms
1818/2086 src/utils/expectation-counters/countCharacters.ts 4.18ms
1819/2086 src/utils/expectation-counters/countLines.test.ts 1.89ms
1820/2086 src/utils/expectation-counters/countLines.ts 2.45ms
1821/2086 src/utils/expectation-counters/countPages.test.ts 6.20ms
1822/2086 src/utils/expectation-counters/countPages.ts 2.27ms
1823/2086 src/utils/expectation-counters/countParagraphs.test.ts 2.08ms
1824/2086 src/utils/expectation-counters/countParagraphs.ts 1.98ms
1825/2086 src/utils/expectation-counters/countSentences.test.ts 1.90ms
1826/2086 src/utils/expectation-counters/countSentences.ts 2.16ms
1827/2086 src/utils/expectation-counters/countWords.test.ts 2.77ms
1828/2086 src/utils/expectation-counters/countWords.ts 3.33ms
1829/2086 src/utils/expectation-counters/index.ts 2.96ms
1830/2086 src/utils/expectation-counters/other/countPages.md 4.19ms
1831/2086 src/utils/files/$induceBookDownload.ts 3.70ms
1832/2086 src/utils/files/$induceFileDownload.ts 3.06ms
1833/2086 src/utils/files/decodeAttachmentAsText.test.ts 7.25ms
1834/2086 src/utils/files/decodeAttachmentAsText.ts 21.00ms
1835/2086 src/utils/files/extensionToMimeType.test.ts 3.61ms
1836/2086 src/utils/files/extensionToMimeType.ts 2.58ms
1837/2086 src/utils/files/getFileExtension.test.ts 1.92ms
1838/2086 src/utils/files/getFileExtension.ts 1.70ms
1839/2086 src/utils/files/isDirectoryExisting.test.ts 1.75ms
1840/2086 src/utils/files/isDirectoryExisting.ts 2.81ms
1841/2086 src/utils/files/isExecutable.ts 2.74ms
1842/2086 src/utils/files/isFileExisting.test.ts 2.41ms
1843/2086 src/utils/files/isFileExisting.ts 3.17ms
1844/2086 src/utils/files/listAllFiles.test.ts 5.57ms
1845/2086 src/utils/files/listAllFiles.ts 3.77ms
1846/2086 src/utils/files/mimeTypeToExtension.test.ts 3.01ms
1847/2086 src/utils/files/mimeTypeToExtension.ts 1.99ms
1848/2086 src/utils/files/ObjectUrl.ts 4.46ms
1849/2086 src/utils/files/readResponseBytes.ts 5.33ms
1850/2086 src/utils/filesystem/promptbookTemporaryPath.test.ts 3.41ms
1851/2086 src/utils/filesystem/promptbookTemporaryPath.ts 3.81ms
1852/2086 src/utils/html/escapeHtml.ts 1.80ms
1853/2086 src/utils/isTimingSafeEqualString.ts 3.02ms
1854/2086 src/utils/knowledge/inlineKnowledgeSource.test.ts 3.29ms
1855/2086 src/utils/knowledge/inlineKnowledgeSource.ts 8.11ms
1856/2086 src/utils/knowledge/simplifyKnowledgeLabel.test.ts 4.22ms
1857/2086 src/utils/knowledge/simplifyKnowledgeLabel.ts 11.21ms
1858/2086 src/utils/language/getBrowserPreferredSpeechRecognitionLanguage.ts 10.96ms
1859/2086 src/utils/linguistic-hash/linguisticHash.test.ts 10.00ms
1860/2086 src/utils/linguistic-hash/linguisticHash.ts 5.56ms
1861/2086 src/utils/linguistic-hash/LinguisticHashLanguage.ts 3.46ms
1862/2086 src/utils/linguistic-hash/linguisticHashTypes.ts 7.14ms
1863/2086 src/utils/linguistic-hash/linguisticHashWordCount.ts 4.70ms
1864/2086 src/utils/linguistic-hash/linguisticHashWords.cs.ts 4.09ms
1865/2086 src/utils/linguistic-hash/linguisticHashWords.en.ts 55.29ms
1866/2086 src/utils/linguistic-hash/linguisticHashWordSelection.ts 43.78ms
1867/2086 src/utils/markdown/addAutoGeneratedSection.test.ts 3.92ms
1868/2086 src/utils/markdown/addAutoGeneratedSection.ts 8.42ms
1869/2086 src/utils/markdown/createMarkdownChart.test.ts 4.66ms
1870/2086 src/utils/markdown/createMarkdownChart.ts 4.40ms
1871/2086 src/utils/markdown/createMarkdownTable.test.ts 3.34ms
1872/2086 src/utils/markdown/createMarkdownTable.ts 3.47ms
1873/2086 src/utils/markdown/escapeMarkdownBlock.test.ts 2.04ms
1874/2086 src/utils/markdown/escapeMarkdownBlock.ts 3.48ms
1875/2086 src/utils/markdown/extractAllBlocksFromMarkdown-real.test.ts 24.01ms
1876/2086 src/utils/markdown/extractAllBlocksFromMarkdown.test.ts 12.96ms
1877/2086 src/utils/markdown/extractAllBlocksFromMarkdown.ts 4.42ms
1878/2086 src/utils/markdown/extractAllListItemsFromMarkdown.test.ts 3.78ms
1879/2086 src/utils/markdown/extractAllListItemsFromMarkdown.ts 3.49ms
1880/2086 src/utils/markdown/extractOneBlockFromMarkdown.test.ts 5.38ms
1881/2086 src/utils/markdown/extractOneBlockFromMarkdown.ts 3.26ms
1882/2086 src/utils/markdown/flattenMarkdown.test.ts 5.91ms
1883/2086 src/utils/markdown/flattenMarkdown.ts 6.88ms
1884/2086 src/utils/markdown/humanizeAiText.test.ts 10.10ms
1885/2086 src/utils/markdown/humanizeAiText.ts 7.75ms
1886/2086 src/utils/markdown/humanizeAiTextEllipsis.ts 2.29ms
1887/2086 src/utils/markdown/humanizeAiTextEmdashed.ts 3.15ms
1888/2086 src/utils/markdown/humanizeAiTextQuotes.ts 2.21ms
1889/2086 src/utils/markdown/humanizeAiTextSources.ts 2.12ms
1890/2086 src/utils/markdown/humanizeAiTextWhitespace.ts 2.22ms
1891/2086 src/utils/markdown/parseMarkdownSection.test.ts 2.67ms
1892/2086 src/utils/markdown/parseMarkdownSection.ts 4.11ms
1893/2086 src/utils/markdown/prettifyMarkdown.ts 2.34ms
1894/2086 src/utils/markdown/promptbookifyAiText.test.ts 2.91ms
1895/2086 src/utils/markdown/promptbookifyAiText.ts 4.16ms
1896/2086 src/utils/markdown/README.md 1.73ms
1897/2086 src/utils/markdown/removeMarkdownComments.test.ts 7.36ms
1898/2086 src/utils/markdown/removeMarkdownComments.ts 2.19ms
1899/2086 src/utils/markdown/removeMarkdownFormatting.test.ts 6.81ms
1900/2086 src/utils/markdown/removeMarkdownFormatting.ts 2.27ms
1901/2086 src/utils/markdown/removeMarkdownLinks.test.ts 2.01ms
1902/2086 src/utils/markdown/removeMarkdownLinks.ts 1.88ms
1903/2086 src/utils/markdown/shiftMarkdownHeadingLevels.ts 3.22ms
1904/2086 src/utils/markdown/splitMarkdownIntoSections.test.ts 4.38ms
1905/2086 src/utils/markdown/splitMarkdownIntoSections.ts 3.33ms
1906/2086 src/utils/markdown/trimCodeBlock.test.ts 3.79ms
1907/2086 src/utils/markdown/trimCodeBlock.ts 2.89ms
1908/2086 src/utils/markdown/trimEndOfCodeBlock.test.ts 2.40ms
1909/2086 src/utils/markdown/trimEndOfCodeBlock.ts 1.90ms
1910/2086 src/utils/misc/$getCurrentDate.ts 3.62ms
1911/2086 src/utils/misc/$Register.ts 5.29ms
1912/2086 src/utils/misc/aboutPromptbookInformation.ts 8.99ms
1913/2086 src/utils/misc/arrayableToArray.test.ts 2.02ms
1914/2086 src/utils/misc/arrayableToArray.ts 2.25ms
1915/2086 src/utils/misc/computeHash.test.ts 2.10ms
1916/2086 src/utils/misc/computeHash.ts 4.11ms
1917/2086 src/utils/misc/createLazyModuleLoader.ts 3.41ms
1918/2086 src/utils/misc/debounce.test.ts 3.17ms
1919/2086 src/utils/misc/debounce.ts 2.80ms
1920/2086 src/utils/misc/emojis.ts 5.54ms
1921/2086 src/utils/misc/FromtoItems.ts 1.76ms
1922/2086 src/utils/misc/injectCssModuleIntoShadowRoot.tsx 4.63ms
1923/2086 src/utils/misc/parseNumber.test.ts 4.25ms
1924/2086 src/utils/misc/parseNumber.ts 3.20ms
1925/2086 src/utils/misc/xAboutPromptbookInformation.tsx 2.61ms
1926/2086 src/utils/normalization/capitalize.test.ts 1.72ms
1927/2086 src/utils/normalization/capitalize.ts 2.24ms
1928/2086 src/utils/normalization/constructImageFilename.test.ts 2.55ms
1929/2086 src/utils/normalization/constructImageFilename.ts 2.56ms
1930/2086 src/utils/normalization/decapitalize.test.ts 2.86ms
1931/2086 src/utils/normalization/decapitalize.ts 3.26ms
1932/2086 src/utils/normalization/DIACRITIC_VARIANTS_LETTERS.ts 6.68ms
1933/2086 src/utils/normalization/IKeywords.ts 2.01ms
1934/2086 src/utils/normalization/index.ts 1.14ms
1935/2086 src/utils/normalization/isValidKeyword.test.ts 1.97ms
1936/2086 src/utils/normalization/isValidKeyword.ts 1.96ms
1937/2086 src/utils/normalization/nameToUriPart.test.ts 2.02ms
1938/2086 src/utils/normalization/nameToUriPart.ts 2.35ms
1939/2086 src/utils/normalization/nameToUriParts.test.ts 2.20ms
1940/2086 src/utils/normalization/nameToUriParts.ts 1.79ms
1941/2086 src/utils/normalization/normalize-to-kebab-case.test.ts 2.19ms
1942/2086 src/utils/normalization/normalize-to-kebab-case.ts 3.92ms
1943/2086 src/utils/normalization/normalizeMessageText.test.ts 1.80ms
1944/2086 src/utils/normalization/normalizeMessageText.ts 1.99ms
1945/2086 src/utils/normalization/normalizeTo_camelCase.test.ts 1.88ms
1946/2086 src/utils/normalization/normalizeTo_camelCase.ts 3.29ms
1947/2086 src/utils/normalization/normalizeTo_PascalCase.test.ts 2.34ms
1948/2086 src/utils/normalization/normalizeTo_PascalCase.ts 1.99ms
1949/2086 src/utils/normalization/normalizeTo_SCREAMING_CASE.test.ts 2.24ms
1950/2086 src/utils/normalization/normalizeTo_SCREAMING_CASE.ts 4.82ms
1951/2086 src/utils/normalization/normalizeTo_snake_case.test.ts 2.11ms
1952/2086 src/utils/normalization/normalizeTo_snake_case.ts 1.75ms
1953/2086 src/utils/normalization/normalizeWhitespaces.test.ts 2.17ms
1954/2086 src/utils/normalization/normalizeWhitespaces.ts 2.52ms
1955/2086 src/utils/normalization/orderJson.test.ts 2.36ms
1956/2086 src/utils/normalization/orderJson.ts 4.08ms
1957/2086 src/utils/normalization/parseKeywords.test.ts 2.28ms
1958/2086 src/utils/normalization/parseKeywords.ts 3.38ms
1959/2086 src/utils/normalization/parseKeywordsFromString.test.ts 3.70ms
1960/2086 src/utils/normalization/parseKeywordsFromString.ts 1.98ms
1961/2086 src/utils/normalization/removeDiacritics.test.ts 2.61ms
1962/2086 src/utils/normalization/removeDiacritics.ts 2.06ms
1963/2086 src/utils/normalization/removeEmojis.test.ts 2.38ms
1964/2086 src/utils/normalization/removeEmojis.ts 1.73ms
1965/2086 src/utils/normalization/removeQuotes.test.ts 1.81ms
1966/2086 src/utils/normalization/removeQuotes.ts 4.44ms
1967/2086 src/utils/normalization/searchKeywords.test.ts 2.84ms
1968/2086 src/utils/normalization/searchKeywords.ts 2.08ms
1969/2086 src/utils/normalization/suffixUrl.test.ts 1.97ms
1970/2086 src/utils/normalization/suffixUrl.ts 1.84ms
1971/2086 src/utils/normalization/titleToName.test.ts 2.34ms
1972/2086 src/utils/normalization/titleToName.ts 3.08ms
1973/2086 src/utils/normalization/unwrapResult.test.ts 5.13ms
1974/2086 src/utils/normalization/unwrapResult.ts 6.18ms
1975/2086 src/utils/organization/___and___.ts 1.31ms
1976/2086 src/utils/organization/___or___.ts 1.11ms
1977/2086 src/utils/organization/$side_effect.ts 1.72ms
1978/2086 src/utils/organization/$sideEffect.ts 5.26ms
1979/2086 src/utils/organization/empty_object.ts 2.65ms
1980/2086 src/utils/organization/just_empty_object.ts 1.47ms
1981/2086 src/utils/organization/just.ts 1.59ms
1982/2086 src/utils/organization/keepImported.ts 2.97ms
1983/2086 src/utils/organization/keepTypeImported.ts 2.29ms
1984/2086 src/utils/organization/keepUnused.ts 2.11ms
1985/2086 src/utils/organization/preserve.ts 2.01ms
1986/2086 src/utils/organization/README.md 2.57ms
1987/2086 src/utils/organization/really_any.ts 2.73ms
1988/2086 src/utils/organization/really_unknown.ts 2.26ms
1989/2086 src/utils/organization/spaceTrim.ts 1.79ms
1990/2086 src/utils/organization/TODO_any.ts 1.42ms
1991/2086 src/utils/organization/TODO_narrow.ts 1.27ms
1992/2086 src/utils/organization/TODO_object.ts 1.69ms
1993/2086 src/utils/organization/TODO_remove_as.ts 2.13ms
1994/2086 src/utils/organization/TODO_string.ts 1.77ms
1995/2086 src/utils/organization/TODO_unknown.ts 1.84ms
1996/2086 src/utils/organization/TODO_USE.ts 1.68ms
1997/2086 src/utils/parameters/extractParameterNames.test.ts 3.59ms
1998/2086 src/utils/parameters/extractParameterNames.ts 3.60ms
1999/2086 src/utils/parameters/mapAvailableToExpectedParameters.test.ts 4.63ms
2000/2086 src/utils/parameters/mapAvailableToExpectedParameters.ts 7.05ms
2001/2086 src/utils/parameters/numberToString.test.ts 1.86ms
2002/2086 src/utils/parameters/numberToString.ts 2.03ms
2003/2086 src/utils/parameters/templateParameters.test.ts 5.45ms
2004/2086 src/utils/parameters/templateParameters.ts 6.00ms
2005/2086 src/utils/parameters/valueToString.test.ts 2.23ms
2006/2086 src/utils/parameters/valueToString.ts 4.29ms
2007/2086 src/utils/random/$generateBookBoilerplate.test.ts 2.09ms
2008/2086 src/utils/random/$generateBookBoilerplate.ts 4.51ms
2009/2086 src/utils/random/$randomAgentPersona.ts 4.21ms
2010/2086 src/utils/random/$randomAgentRule.ts 2.79ms
2011/2086 src/utils/random/$randomBase58.ts 2.34ms
2012/2086 src/utils/random/$randomFullnameWithColor.ts 3.51ms
2013/2086 src/utils/random/$randomItem.ts 1.81ms
2014/2086 src/utils/random/$randomSeed.ts 2.59ms
2015/2086 src/utils/random/$randomToken.ts 2.14ms
2016/2086 src/utils/random/CzechNamePool.ts 9.69ms
2017/2086 src/utils/random/EnglishNamePool.ts 4.72ms
2018/2086 src/utils/random/getNamePool.ts 2.41ms
2019/2086 src/utils/random/NamePool.ts 1.68ms
2020/2086 src/utils/serialization/$deepFreeze.ts 3.02ms
2021/2086 src/utils/serialization/asSerializable.test.ts 1.85ms
2022/2086 src/utils/serialization/asSerializable.ts 3.38ms
2023/2086 src/utils/serialization/checkSerializableAsJson.ts 8.56ms
2024/2086 src/utils/serialization/clonePipeline.ts 7.17ms
2025/2086 src/utils/serialization/deepClone.test.ts 2.72ms
2026/2086 src/utils/serialization/deepClone.ts 2.94ms
2027/2086 src/utils/serialization/exportJson.ts 4.65ms
2028/2086 src/utils/serialization/isSerializableAsJson.test.ts 3.43ms
2029/2086 src/utils/serialization/isSerializableAsJson.ts 2.21ms
2030/2086 src/utils/serialization/jsonStringsToJsons.test.ts 1.98ms
2031/2086 src/utils/serialization/jsonStringsToJsons.ts 2.17ms
2032/2086 src/utils/serialization/serializeToPromptbookJavascript.test.ts 1.80ms
2033/2086 src/utils/serialization/serializeToPromptbookJavascript.ts 4.32ms
2034/2086 src/utils/sets/difference.test.ts 2.56ms
2035/2086 src/utils/sets/difference.ts 2.33ms
2036/2086 src/utils/sets/intersection.test.ts 1.93ms
2037/2086 src/utils/sets/intersection.ts 3.10ms
2038/2086 src/utils/sets/union.test.ts 3.72ms
2039/2086 src/utils/sets/union.ts 1.91ms
2040/2086 src/utils/take/classes/TakeChain.ts 2.02ms
2041/2086 src/utils/take/interfaces/ITakeChain.ts 3.26ms
2042/2086 src/utils/take/interfaces/Takeable.ts 2.94ms
2043/2086 src/utils/take/take.test.ts 3.11ms
2044/2086 src/utils/take/take.ts 2.32ms
2045/2086 src/utils/toolCalls/getToolCallIdentity.ts 2.46ms
2046/2086 src/utils/toolCalls/mergeToolCalls.ts 7.70ms
2047/2086 src/utils/toolCalls/resolveToolCallIdempotencyKey.ts 3.28ms
2048/2086 src/utils/validators/email/isValidEmail.test.ts 1.80ms
2049/2086 src/utils/validators/email/isValidEmail.ts 1.75ms
2050/2086 src/utils/validators/filePath/isRootPath.test.ts 1.83ms
2051/2086 src/utils/validators/filePath/isRootPath.ts 1.97ms
2052/2086 src/utils/validators/filePath/isValidFilePath.test.ts 7.56ms
2053/2086 src/utils/validators/filePath/isValidFilePath.ts 3.80ms
2054/2086 src/utils/validators/javascriptName/isValidJavascriptName.test.ts 3.76ms
2055/2086 src/utils/validators/javascriptName/isValidJavascriptName.ts 2.51ms
2056/2086 src/utils/validators/parameterName/validateParameterName.test.ts 4.93ms
2057/2086 src/utils/validators/parameterName/validateParameterName.ts 5.67ms
2058/2086 src/utils/validators/semanticVersion/isValidPromptbookVersion.test.ts 3.23ms
2059/2086 src/utils/validators/semanticVersion/isValidPromptbookVersion.ts 4.00ms
2060/2086 src/utils/validators/semanticVersion/isValidSemanticVersion.test.ts 2.04ms
2061/2086 src/utils/validators/semanticVersion/isValidSemanticVersion.ts 2.05ms
2062/2086 src/utils/validators/url/extractUrlsFromText.test.ts 3.55ms
2063/2086 src/utils/validators/url/extractUrlsFromText.ts 4.72ms
2064/2086 src/utils/validators/url/isHostnameOnPrivateNetwork.test.ts 2.50ms
2065/2086 src/utils/validators/url/isHostnameOnPrivateNetwork.ts 2.70ms
2066/2086 src/utils/validators/url/isUrlOnPrivateNetwork.test.ts 1.99ms
2067/2086 src/utils/validators/url/isUrlOnPrivateNetwork.ts 2.76ms
2068/2086 src/utils/validators/url/isValidAgentUrl.test.ts 3.03ms
2069/2086 src/utils/validators/url/isValidAgentUrl.ts 3.05ms
2070/2086 src/utils/validators/url/isValidPipelineUrl.test.ts 2.46ms
2071/2086 src/utils/validators/url/isValidPipelineUrl.ts 2.59ms
2072/2086 src/utils/validators/url/isValidUrl.test.ts 2.47ms
2073/2086 src/utils/validators/url/isValidUrl.ts 2.62ms
2074/2086 src/utils/validators/url/normalizeDomainForMatching.test.ts 3.15ms
2075/2086 src/utils/validators/url/normalizeDomainForMatching.ts 3.67ms
2076/2086 src/utils/validators/uuid/isValidUuid.test.ts 2.16ms
2077/2086 src/utils/validators/uuid/isValidUuid.ts 2.24ms
2078/2086 src/version.ts 2.53ms
2079/2086 src/wizard/$getCompiledBook.ts 10.74ms
2080/2086 src/wizard/test/books/test.book.md 1.60ms
2081/2086 src/wizard/test/README.md 1.48ms
2082/2086 src/wizard/test/sub/books/test.book.md 1.69ms
2083/2086 src/wizard/test/sub/subsub/books/test.book.md 1.61ms
2084/2086 src/wizard/test/sub/subsub/subsubsub/books/test.book.md 1.38ms
2085/2086 src/wizard/test/sub/subsub/subsubsub/README.md 1.32ms
2086/2086 src/wizard/wizard.ts 8.11ms
CSpell: Files checked: 2086, Issues found: 0 in 0 files.

> promptbook@0.114.0-38 test-lint
> eslint src


> promptbook@0.114.0-38 test-types
> tsc


> promptbook@0.114.0-38 test-books
> echo "Note: [⚡️] Skipping the book test because it tests just the old pipeline books which are deprecated"

"Note: [??] Skipping the book test because it tests just the old pipeline books which are deprecated"

> promptbook@0.114.0-38 test-book-components-build
> echo "Note: Skipping book components build tests because of constants problems with this test"

"Note: Skipping book components build tests because of constants problems with this test"

> promptbook@0.114.0-38 test-package-generation
> ts-node ./scripts/generate-packages/generate-packages.ts --skip-bundler

📦  Generating packages
[36m0️⃣  Prepare the needed information about the packages[39m
Promptbook version 0.114.0-38
[36m1️⃣  Generate entry file for each package[39m
[32mGenerated index file ./src/_packages/anthropic-claude.index.ts[39m
[32mGenerated index file ./src/_packages/azure-openai.index.ts[39m
[32mGenerated index file ./src/_packages/browser.index.ts[39m
[32mGenerated index file ./src/_packages/cli.index.ts[39m
[32mGenerated index file ./src/_packages/color.index.ts[39m
[32mGenerated index file ./src/_packages/components.index.ts[39m
[32mGenerated index file ./src/_packages/core.index.ts[39m
[32mGenerated index file ./src/_packages/deepseek.index.ts[39m
[32mGenerated index file ./src/_packages/documents.index.ts[39m
[32mGenerated index file ./src/_packages/editable.index.ts[39m
[32mGenerated index file ./src/_packages/fake-llm.index.ts[39m
[32mGenerated index file ./src/_packages/google.index.ts[39m
[32mGenerated index file ./src/_packages/javascript.index.ts[39m
[32mGenerated index file ./src/_packages/legacy-documents.index.ts[39m
[32mGenerated index file ./src/_packages/markdown-utils.index.ts[39m
[32mGenerated index file ./src/_packages/markitdown.index.ts[39m
[32mGenerated index file ./src/_packages/node.index.ts[39m
[32mGenerated index file ./src/_packages/ollama.index.ts[39m
[32mGenerated index file ./src/_packages/openai.index.ts[39m
[32mGenerated index file ./src/_packages/pdf.index.ts[39m
[32mGenerated index file ./src/_packages/remote-client.index.ts[39m
[32mGenerated index file ./src/_packages/remote-server.index.ts[39m
[32mGenerated index file ./src/_packages/templates.index.ts[39m
[32mGenerated index file ./src/_packages/types.index.ts[39m
[32mGenerated index file ./src/_packages/utils.index.ts[39m
[32mGenerated index file ./src/_packages/vercel.index.ts[39m
[32mGenerated index file ./src/_packages/website-crawler.index.ts[39m
[32mGenerated index file ./src/_packages/wizard.index.ts[39m
[36m2️⃣  Generate package.json, README and other crucial files for each package[39m
[36m3️⃣  Cleanup build directories for each package[39m
[33mSkipping the cleanup for bundler[39m
[36m4️⃣  Generate bundle for each package[39m
[33mSkipping the bundler[39m
[36m5️⃣  Postprocess the generated bundle[39m
[33mSkipping postprocessing[39m
[36m6️⃣  Add dependencies for each package[39m
[33mBundle file ./packages/editable/esm/index.es.js does not exist[39m
[33mBundle file ./packages/fake-llm/esm/index.es.js does not exist[39m
[33mBundle file ./packages/google/esm/index.es.js does not exist[39m
[33mBundle file ./packages/javascript/esm/index.es.js does not exist[39m
[33mBundle file ./packages/legacy-documents/esm/index.es.js does not exist[39m
[33mBundle file ./packages/markdown-utils/esm/index.es.js does not exist[39m
[33mBundle file ./packages/markitdown/esm/index.es.js does not exist[39m
[33mBundle file ./packages/node/esm/index.es.js does not exist[39m
[33mBundle file ./packages/ollama/esm/index.es.js does not exist[39m
[33mBundle file ./packages/openai/esm/index.es.js does not exist[39m
[33mBundle file ./packages/pdf/esm/index.es.js does not exist[39m
[33mBundle file ./packages/remote-client/esm/index.es.js does not exist[39m
[33mBundle file ./packages/remote-server/esm/index.es.js does not exist[39m
[33mBundle file ./packages/templates/esm/index.es.js does not exist[39m
[33mBundle file ./packages/types/esm/index.es.js does not exist[39m
[33mBundle file ./packages/utils/esm/index.es.js does not exist[39m
[33mBundle file ./packages/vercel/esm/index.es.js does not exist[39m
[33mBundle file ./packages/website-crawler/esm/index.es.js does not exist[39m
[33mBundle file ./packages/wizard/esm/index.es.js does not exist[39m
[36m7️⃣  Copy agents-server app to CLI package[39m
Copying ./apps/agents-server to ./packages/cli/apps/agents-server
Copying ./apps/_common to ./packages/cli/apps/_common
Copying ./src to ./packages/cli/src
Copying ./books to ./packages/cli/books
Copying ./servers.ts to ./packages/cli/servers.ts
Copying ./security.config.ts to ./packages/cli/security.config.ts
[32mAgents-server app copied successfully[39m
Copying ./agents/default/developer.book to ./packages/cli/agents/default/developer.book
Copying ./agents/default/.core/adam.book to ./packages/cli/agents/default/.core/adam.book
[36m8️⃣  Test that nothing what should not be published is published[39m
[33mSkipping the bundler, skipping the tests for bundle content[39m
[36m9️⃣  Make publishing instructions for Github Actions[39m

> promptbook@0.114.0-38 test-unit
> node -r ./scripts/ignore-kill-eperm.js ./node_modules/jest/bin/jest.js --forceExit

PASS scripts/run-codex-prompts/common/runGoScript/runBashScriptWithOutput.test.ts (61.467 s)
  ● Console

    console.info
      runner stdout

      at printLiveScriptChunk (scripts/run-codex-prompts/common/runGoScript/printLiveScriptChunk.ts:21:13)

    console.warn
      runner stderr

    [0m [90m 13 |[39m     [36mif[39m (source [33m===[39m [32m'stderr'[39m) {
     [90m 14 |[39m         [36mif[39m (chunk[33m.[39mtrim()) {
    [31m[1m>[22m[39m[90m 15 |[39m             console[33m.[39mwarn(chunk)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 16 |[39m         }
     [90m 17 |[39m
     [90m 18 |[39m         [36mreturn[39m[33m;[39m[0m

      at printLiveScriptChunk (scripts/run-codex-prompts/common/runGoScript/printLiveScriptChunk.ts:15:21)
      at Socket.<anonymous> (scripts/run-codex-prompts/common/runGoScript/runBashScriptWithOutput.ts:72:33)

    console.info
      runner stdout

      at printLiveScriptChunk (scripts/run-codex-prompts/common/runGoScript/printLiveScriptChunk.ts:21:13)

    console.warn
      test failure

    [0m [90m 13 |[39m     [36mif[39m (source [33m===[39m [32m'stderr'[39m) {
     [90m 14 |[39m         [36mif[39m (chunk[33m.[39mtrim()) {
    [31m[1m>[22m[39m[90m 15 |[39m             console[33m.[39mwarn(chunk)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 16 |[39m         }
     [90m 17 |[39m
     [90m 18 |[39m         [36mreturn[39m[33m;[39m[0m

      at printLiveScriptChunk (scripts/run-codex-prompts/common/runGoScript/printLiveScriptChunk.ts:15:21)
      at Socket.<anonymous> (scripts/run-codex-prompts/common/runGoScript/runBashScriptWithOutput.ts:72:33)

PASS apps/agents-server/src/utils/bookLanguageDocumentation/createBookLanguageDocumentationPdfResponse.test.ts (31.46 s)
PASS apps/agents-server/src/utils/errorReporting/sendApplicationErrorReportToSentry.test.ts (19.177 s)
PASS src/scripting/_test/postprocessing.test.ts (13.563 s)
  ● Console

    console.info
      💬 Mocked callChatModel call

      at MockedEchoLlmExecutionTools.callChatModel (src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts:81:21)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = "You said:\n„Hello Paůl!“\n\n[1️⃣ Say that again!](?message=Hello%20Pa%C5%AFl!)\n[2️⃣ Say that twice!](?message=Hello%20Pa%C5%AFl!Hello%20Pa%C5%AFl!)\n[3️⃣ Say that thrice!](?message=Hello%20Pa%C5%AFl!Hello%20Pa%C5%AFl!Hello%20Pa%C5%AFl!)";
      (async ()=>{ return reverse(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      )!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH=egassem?(]!ecirht taht yaS ⃣️3[
      )!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH=egassem?(]!eciwt taht yaS ⃣️2[
      )!lFA%5C%aP02%olleH=egassem?(]!niaga taht yaS ⃣️1[
      
      “!lůaP olleH„
      :dias uoY

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = ")!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH=egassem?(]!ecirht taht yaS ⃣️3[\n)!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH=egassem?(]!eciwt taht yaS ⃣️2[\n)!lFA%5C%aP02%olleH=egassem?(]!niaga taht yaS ⃣️1[\n\n“!lůaP olleH„\n:dias uoY";
      (async ()=>{ return removeDiacritics(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      )!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH=egassem?(]!ecirht taht yaS ⃣️3[
      )!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH=egassem?(]!eciwt taht yaS ⃣️2[
      )!lFA%5C%aP02%olleH=egassem?(]!niaga taht yaS ⃣️1[
      
      “!luaP olleH„
      :dias uoY

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = ")!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH=egassem?(]!ecirht taht yaS ⃣️3[\n)!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH=egassem?(]!eciwt taht yaS ⃣️2[\n)!lFA%5C%aP02%olleH=egassem?(]!niaga taht yaS ⃣️1[\n\n“!luaP olleH„\n:dias uoY";
      (async ()=>{ return normalizeTo_SCREAMING_CASE(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      L_FA_5C_A_P02_OLLE_H_L_FA_5C_A_P02_OLLE_H_L_FA_5C_A_P02_OLLE_H_EGASSEM_ECIRHT_TAHT_YA_S_3_L_FA_5C_A_P02_OLLE_H_L_FA_5C_A_P02_OLLE_H_EGASSEM_ECIWT_TAHT_YA_S_2_L_FA_5C_A_P02_OLLE_H_EGASSEM_NIAGA_TAHT_YA_S_1_LUA_P_OLLE_H_DIAS_UO_Y

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      💬 Mocked callChatModel call

      at MockedEchoLlmExecutionTools.callChatModel (src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts:81:21)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = "You said:\n„Hello Adam!“\n\n[1️⃣ Say that again!](?message=Hello%20Adam!)\n[2️⃣ Say that twice!](?message=Hello%20Adam!Hello%20Adam!)\n[3️⃣ Say that thrice!](?message=Hello%20Adam!Hello%20Adam!Hello%20Adam!)";
      (async ()=>{ return reverse(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      )!madA02%olleH!madA02%olleH!madA02%olleH=egassem?(]!ecirht taht yaS ⃣️3[
      )!madA02%olleH!madA02%olleH=egassem?(]!eciwt taht yaS ⃣️2[
      )!madA02%olleH=egassem?(]!niaga taht yaS ⃣️1[
      
      “!madA olleH„
      :dias uoY

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = ")!madA02%olleH!madA02%olleH!madA02%olleH=egassem?(]!ecirht taht yaS ⃣️3[\n)!madA02%olleH!madA02%olleH=egassem?(]!eciwt taht yaS ⃣️2[\n)!madA02%olleH=egassem?(]!niaga taht yaS ⃣️1[\n\n“!madA olleH„\n:dias uoY";
      (async ()=>{ return removeDiacritics(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      )!madA02%olleH!madA02%olleH!madA02%olleH=egassem?(]!ecirht taht yaS ⃣️3[
      )!madA02%olleH!madA02%olleH=egassem?(]!eciwt taht yaS ⃣️2[
      )!madA02%olleH=egassem?(]!niaga taht yaS ⃣️1[
      
      “!madA olleH„
      :dias uoY

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = ")!madA02%olleH!madA02%olleH!madA02%olleH=egassem?(]!ecirht taht yaS ⃣️3[\n)!madA02%olleH!madA02%olleH=egassem?(]!eciwt taht yaS ⃣️2[\n)!madA02%olleH=egassem?(]!niaga taht yaS ⃣️1[\n\n“!madA olleH„\n:dias uoY";
      (async ()=>{ return normalizeTo_SCREAMING_CASE(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      MAD_A02_OLLE_H_MAD_A02_OLLE_H_MAD_A02_OLLE_H_EGASSEM_ECIRHT_TAHT_YA_S_3_MAD_A02_OLLE_H_MAD_A02_OLLE_H_EGASSEM_ECIWT_TAHT_YA_S_2_MAD_A02_OLLE_H_EGASSEM_NIAGA_TAHT_YA_S_1_MAD_A_OLLE_H_DIAS_UO_Y

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      💬 Mocked callChatModel call

      at MockedEchoLlmExecutionTools.callChatModel (src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts:81:21)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = "You said:\n„Hello John!“\n\n[1️⃣ Say that again!](?message=Hello%20John!)\n[2️⃣ Say that twice!](?message=Hello%20John!Hello%20John!)\n[3️⃣ Say that thrice!](?message=Hello%20John!Hello%20John!Hello%20John!)";
      (async ()=>{ return reverse(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      )!nhoJ02%olleH!nhoJ02%olleH!nhoJ02%olleH=egassem?(]!ecirht taht yaS ⃣️3[
      )!nhoJ02%olleH!nhoJ02%olleH=egassem?(]!eciwt taht yaS ⃣️2[
      )!nhoJ02%olleH=egassem?(]!niaga taht yaS ⃣️1[
      
      “!nhoJ olleH„
      :dias uoY

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = ")!nhoJ02%olleH!nhoJ02%olleH!nhoJ02%olleH=egassem?(]!ecirht taht yaS ⃣️3[\n)!nhoJ02%olleH!nhoJ02%olleH=egassem?(]!eciwt taht yaS ⃣️2[\n)!nhoJ02%olleH=egassem?(]!niaga taht yaS ⃣️1[\n\n“!nhoJ olleH„\n:dias uoY";
      (async ()=>{ return removeDiacritics(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      )!nhoJ02%olleH!nhoJ02%olleH!nhoJ02%olleH=egassem?(]!ecirht taht yaS ⃣️3[
      )!nhoJ02%olleH!nhoJ02%olleH=egassem?(]!eciwt taht yaS ⃣️2[
      )!nhoJ02%olleH=egassem?(]!niaga taht yaS ⃣️1[
      
      “!nhoJ olleH„
      :dias uoY

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = ")!nhoJ02%olleH!nhoJ02%olleH!nhoJ02%olleH=egassem?(]!ecirht taht yaS ⃣️3[\n)!nhoJ02%olleH!nhoJ02%olleH=egassem?(]!eciwt taht yaS ⃣️2[\n)!nhoJ02%olleH=egassem?(]!niaga taht yaS ⃣️1[\n\n“!nhoJ olleH„\n:dias uoY";
      (async ()=>{ return normalizeTo_SCREAMING_CASE(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      NHO_J02_OLLE_H_NHO_J02_OLLE_H_NHO_J02_OLLE_H_EGASSEM_ECIRHT_TAHT_YA_S_3_NHO_J02_OLLE_H_NHO_J02_OLLE_H_EGASSEM_ECIWT_TAHT_YA_S_2_NHO_J02_OLLE_H_EGASSEM_NIAGA_TAHT_YA_S_1_NHO_J_OLLE_H_DIAS_UO_Y

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      💬 Mocked callChatModel call

      at MockedEchoLlmExecutionTools.callChatModel (src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts:81:21)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = "You said:\n„Hello DAVID!“\n\n[1️⃣ Say that again!](?message=Hello%20DAVID!)\n[2️⃣ Say that twice!](?message=Hello%20DAVID!Hello%20DAVID!)\n[3️⃣ Say that thrice!](?message=Hello%20DAVID!Hello%20DAVID!Hello%20DAVID!)";
      (async ()=>{ return reverse(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      )!DIVAD02%olleH!DIVAD02%olleH!DIVAD02%olleH=egassem?(]!ecirht taht yaS ⃣️3[
      )!DIVAD02%olleH!DIVAD02%olleH=egassem?(]!eciwt taht yaS ⃣️2[
      )!DIVAD02%olleH=egassem?(]!niaga taht yaS ⃣️1[
      
      “!DIVAD olleH„
      :dias uoY

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = ")!DIVAD02%olleH!DIVAD02%olleH!DIVAD02%olleH=egassem?(]!ecirht taht yaS ⃣️3[\n)!DIVAD02%olleH!DIVAD02%olleH=egassem?(]!eciwt taht yaS ⃣️2[\n)!DIVAD02%olleH=egassem?(]!niaga taht yaS ⃣️1[\n\n“!DIVAD olleH„\n:dias uoY";
      (async ()=>{ return removeDiacritics(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      )!DIVAD02%olleH!DIVAD02%olleH!DIVAD02%olleH=egassem?(]!ecirht taht yaS ⃣️3[
      )!DIVAD02%olleH!DIVAD02%olleH=egassem?(]!eciwt taht yaS ⃣️2[
      )!DIVAD02%olleH=egassem?(]!niaga taht yaS ⃣️1[
      
      “!DIVAD olleH„
      :dias uoY

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = ")!DIVAD02%olleH!DIVAD02%olleH!DIVAD02%olleH=egassem?(]!ecirht taht yaS ⃣️3[\n)!DIVAD02%olleH!DIVAD02%olleH=egassem?(]!eciwt taht yaS ⃣️2[\n)!DIVAD02%olleH=egassem?(]!niaga taht yaS ⃣️1[\n\n“!DIVAD olleH„\n:dias uoY";
      (async ()=>{ return normalizeTo_SCREAMING_CASE(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      DIVAD02_OLLE_H_DIVAD02_OLLE_H_DIVAD02_OLLE_H_EGASSEM_ECIRHT_TAHT_YA_S_3_DIVAD02_OLLE_H_DIVAD02_OLLE_H_EGASSEM_ECIWT_TAHT_YA_S_2_DIVAD02_OLLE_H_EGASSEM_NIAGA_TAHT_YA_S_1_DIVAD_OLLE_H_DIAS_UO_Y

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

PASS scripts/utils/findAllProjectEntities.test.ts (14.252 s)
  ● Console

    console.error
      Duplicate entity name "listAllFiles" found in files:
       - C:/Users/me/work/ai/promptbook/src/utils/files/listAllFiles.ts
       - C:/Users/me/work/ai/promptbook/scripts/delete-openai-resources/listAllOpenAiResources.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:5:26)

    console.error
      Duplicate entity name "BOOK_LANGUAGE_VERSION" found in files:
       - C:/Users/me/work/ai/promptbook/src/version.ts
       - C:/Users/me/work/ai/promptbook/scripts/update-version-in-config/update-version-in-config.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:5:26)

    console.error
      Duplicate entity name "PROMPTBOOK_ENGINE_VERSION" found in files:
       - C:/Users/me/work/ai/promptbook/src/version.ts
       - C:/Users/me/work/ai/promptbook/scripts/update-version-in-config/update-version-in-config.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:5:26)

    console.error
      Duplicate entity name "string_promptbook_version" found in files:
       - C:/Users/me/work/ai/promptbook/src/version.ts
       - C:/Users/me/work/ai/promptbook/scripts/update-version-in-config/update-version-in-config.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:5:26)

    console.error
      Duplicate entity name "value$" found in files:
       - C:/Users/me/work/ai/promptbook/scripts/find-refactor-candidates/analyzeSourceFileForRefactorCandidate.test.ts
       - C:/Users/me/work/ai/promptbook/scripts/find-refactor-candidates/findRefactorCandidatesInProject.test.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:5:26)

    console.error
      Duplicate entity name "AgentSourceImporterContext" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/resolveInheritedAgentSource.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "AgentSourceImporter" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/resolveInheritedAgentSource.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "resolveInheritedAgentSource" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-2.0/agent-source/resolveInheritedAgentSource.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/resolveInheritedAgentSource.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "MonacoEditorWithShadowDom" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-components/_common/MonacoEditorWithShadowDom.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/_utils/MonacoEditorWithShadowDom.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "BookEditorUploadProgressCallback" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-components/BookEditor/BookEditor.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/upload/createBookEditorUploadHandler.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "BookEditorUploadOptions" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-components/BookEditor/BookEditor.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/upload/createBookEditorUploadHandler.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "ChatFeedbackMode" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-components/Chat/Chat/ChatProps.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/chatFeedbackMode.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "ChatVisualMode" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-components/Chat/Chat/ChatProps.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/constants/chatVisualMode.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "FriendlyErrorMessage" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-components/Chat/LlmChat/FriendlyErrorMessage.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/errorMessages.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "ChatMessage" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-components/Chat/types/ChatMessage.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/NewAgentDialog/ManGoNewAgentWizard/types.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "RunBrowserToolError" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-components/Chat/utils/toolCallParsing/RunBrowserToolResult.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/tools/runBrowserErrors.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "LocalAgentRunnerLimits" found in files:
       - C:/Users/me/work/ai/promptbook/src/cli/cli-commands/agents-server/startAgentsServer/LocalAgentRunnerLimits.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/serverLimits.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "Json" found in files:
       - C:/Users/me/work/ai/promptbook/src/collection/agent-collection/constructors/agent-collection-in-supabase/AgentsDatabaseSchema.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/database/schema.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "TIME_INTERVALS" found in files:
       - C:/Users/me/work/ai/promptbook/src/constants.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/constants.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "HTTP_STATUS_CODES" found in files:
       - C:/Users/me/work/ai/promptbook/src/constants.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/constants.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "CHAT_STREAM_KEEP_ALIVE_TOKEN" found in files:
       - C:/Users/me/work/ai/promptbook/src/constants/streaming.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/constants/streaming.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "CHAT_STREAM_KEEP_ALIVE_INTERVAL_MS" found in files:
       - C:/Users/me/work/ai/promptbook/src/constants/streaming.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/constants/streaming.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "nameToSubfolderPath" found in files:
       - C:/Users/me/work/ai/promptbook/src/storage/file-cache-storage/utils/nameToSubfolderPath.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/cdn/utils/nameToSubfolderPath.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "id" found in files:
       - C:/Users/me/work/ai/promptbook/src/types/string_token.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/export/route.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "async" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/[agentName]/[...rest]/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/about/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/api-tokens/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/backup/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/browser-test/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/chat-feedback/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/chat-history/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/core-agents/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/custom-css/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/custom-js/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/email-server/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/error-simulation/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/files/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/image-generator-test/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/images/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/internal-s3/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/limits/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/login-methods/shibboleth/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/messages/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/messages/send-email/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/metadata/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/models/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/planned-messages/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/projects/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/search-engine-test/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/task-manager-vps/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/task-manager/[taskId]/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/task-manager/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/tool-limits/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/transcriptions/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/usage/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/users/[userId]/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/users/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/voice-input-test/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/book/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/book+chat/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/chat/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/goal/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/history/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/iframe/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/images/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/integration/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/layout.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/manifest.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/opengraph-image.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/projects/[projectName]/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/projects/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/system-message/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/textarea/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/website-integration/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/dashboard/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/docs/[docId]/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/docs/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/layout.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/manifest.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/recycle-bin/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/cli-access/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/database/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/email-server/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/environment/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/harness-auth/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/internal-s3/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/logs/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/resource-monitor/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/servers/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/task-manager/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/update/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/swagger/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/system/profile/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/system/user-memory/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/system/user-wallet/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/system/utilities/mocked-chats/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/system/utilities/mocked-chats/view/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/system/utilities/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/test/og-image/opengraph-image.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "function" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/[agentName]/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/book/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/book+chat/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/chat/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/history/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/images/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/integration/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/system-message/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/textarea/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/website-integration/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/docs/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/embed/layout.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/embed/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/error.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/experiments/story/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/global-error.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/not-found.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/recycle-bin/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/restricted/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/search/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/story/[[...story]]/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/swagger/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/system/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/system/settings/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/test/og-image/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/VercelDeploymentCard/VercelDeploymentCard.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/pages/_app.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/pages/_document.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/pages/500.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "dynamic" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/core-agents/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/email-server/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/internal-s3/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/projects/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/download/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/api/download/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/projects/[projectName]/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/projects/[projectName]/vscode/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/projects/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/backups/books/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/backups/server/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/chat-tasks/[taskId]/terminal/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/cli-access/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/database/studio/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/harness-auth/authentication/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/commits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/log/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/versions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-project-runtime-auth/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-project-vscode-auth/[sessionId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/export/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/import/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/browser-test/scroll-facebook/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book-language.md/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book-language.pdf/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book.md/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/federated-agents/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/health/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/metadata/export/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/metadata/import/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/page-preview/state/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/page-preview/stream/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/search/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/dashboard/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/humans.txt/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/robots.txt/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/security.txt/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/sitemap.xml/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/internal-s3/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/resource-monitor/page.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "parseBooleanFlag" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/_utils.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/constants/analyticsMetadata.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "AgentProfile" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/AgentProfileWrapper.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/AgentProfile/AgentProfile.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "runtime" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/download/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/api/download/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/opengraph-image.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/projects/[projectName]/vscode/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/backups/books/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/backups/server/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/cli-access/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/database/studio/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/harness-auth/authentication/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/planned-messages/[timeoutId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/commits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/log/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/versions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-project-runtime-auth/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-project-vscode-auth/[sessionId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/export/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/import/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat/export/pdf/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book-language.pdf/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/health/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/agent-goal-chat-planned-messages/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/agent-project-runtimes/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/test/og-image/opengraph-image.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "GET" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/download/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/history/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/calendar-connections/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/calendar-events/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/mcp/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/meta-disclaimer/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/model-requirements/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/model-requirements/system-message/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/openai/models/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/openai/v1/models/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/profile/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/timeouts/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/stream/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/api/download/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/api/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/images/default-avatar.png/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/images/icon-256.png/route.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/images/screenshot-fullhd.png/route.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/images/screenshot-phone.png/route.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/projects/[projectName]/files/[...filePath]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/projects/[projectName]/vscode/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin-email/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/backups/books/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/backups/server/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/chat-tasks/[taskId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/chat-tasks/[taskId]/terminal/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/chat-tasks/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/cli-access/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/environment/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/error-simulation/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/harness-auth/authentication/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/harness-auth/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/limits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/logs/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/planned-messages/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/servers/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/tool-limits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/commits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/log/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/vps/chat-tasks/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-organization/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-project-runtime-auth/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-project-vscode-auth/[sessionId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/export/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/api-tokens/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/auth/shibboleth/login/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/auth/shibboleth/metadata/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/auth/shibboleth/status/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/browser-artifacts/[artifactName]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/browser-test/screenshot/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/browser-test/scroll-facebook/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/calendar-oauth/callback/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/calendar-oauth/connect/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/calendar-oauth/status/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-feedback/export/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-feedback/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-history/export/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-history/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-history/threads/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-streaming/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/custom-css/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/custom-js/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book-language.md/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book-language.pdf/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book.md/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/embed.js/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/federated-agents/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/github-app/callback/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/github-app/connect/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/github-app/status/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/health/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/images/[filename]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/agent-runner-limits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/user-chat-jobs/run/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/user-chat-timeouts/run/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/long-running-task/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/messages/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/metadata/export/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/metadata/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/openai/v1/models/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/page-preview/check/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/page-preview/screenshot/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/page-preview/state/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/page-preview/stream/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/profile/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/scrape/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/search/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/settings/keybindings/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/settings/notifications/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/settings/theme/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/story/export/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/system/mocked-chats/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/team-agent-profile/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/usage/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/user-memory/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/user-wallet/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/users/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/agents/[agentId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/agents/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/folders/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/instance/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/me/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/humans.txt/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/openapi.json/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/robots.txt/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/s3/[first]/[second]/[hash]/[filename]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/security.txt/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/sitemap.xml/route.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "POST" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/history/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/missing-agent/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/reference-diagnostics/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/calendar-connections/[connectionId]/disconnect/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/calendar-events/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/chat/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/feedback/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/mcp/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/meta-disclaimer/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/openai/chat/completions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/openai/v1/chat/completions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/openrouter/chat/completions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/share-target/[shareTargetId]/consume/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/jobs/[jobId]/cancel/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/messages/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/timeouts/[timeoutId]/cancel/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/voice/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/api/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/share-target/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/chat-tasks/[taskId]/cancel/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/chat-tasks/[taskId]/retry/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/chat-tasks/cancel-all/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/cli-access/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/database/studio/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/default-agents/reinstate/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/dns-records/cloudflare/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/harness-auth/authentication/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/servers/[serverId]/migrate/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/servers/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/vps/chat-tasks/cancel-all/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-folders/[folderId]/restore/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-folders/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-organization/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/[agentName]/clone/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/[agentName]/restore/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/import/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/api-tokens/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/auth/change-password/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/auth/login/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/auth/logout/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/auth/shibboleth/acs/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/browser-test/act/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/calendar-oauth/refresh/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/calendar-oauth/revoke/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat/citation-label/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat/export/pdf/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/custom-css/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/custom-js/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/elevenlabs/tts/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/emails/incoming/sendgrid/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/emails/incoming/stalwart/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/error-reports/application/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/agent-goal-chat-planned-messages/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/agent-project-runtimes/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/agent-runner-limits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/user-chat-jobs/run/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/user-chat-timeouts/run/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/metadata/import/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/metadata/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/onboarding/book/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/onboarding/evaluate/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/onboarding/test/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/openai/v1/audio/transcriptions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/openai/v1/chat/completions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/page-preview/input/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/send-email/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/spawn-agent/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/upload/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/user-memory/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/user-wallet/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/users/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/agents/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/folders/[folderId]/agents/[agentId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/folders/route.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "PUT" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/limits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/tool-limits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/custom-css/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/custom-js/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/metadata/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/push-subscriptions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/settings/keybindings/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/settings/notifications/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/settings/theme/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/system/mocked-chats/route.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "maxDuration" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/chat/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/stream/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/voice/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/browser-test/act/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/user-chat-jobs/run/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/user-chat-timeouts/run/route.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "OPTIONS" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/chat/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/profile/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/voice/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/elevenlabs/tts/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/agents/[agentId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/agents/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/folders/[folderId]/agents/[agentId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/folders/[folderId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/folders/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/instance/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/me/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/openapi.json/route.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "DELETE" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/timeouts/[timeoutId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/cli-access/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/harness-auth/authentication/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/planned-messages/[timeoutId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/servers/[serverId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/versions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-folders/[folderId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/[agentName]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/api-tokens/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-feedback/[id]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-feedback/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-history/[id]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-history/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/custom-css/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/custom-js/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/metadata/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/push-subscriptions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/user-memory/[memoryId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/user-wallet/[walletId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/users/[username]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/agents/[agentId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/folders/[folderId]/route.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "PATCH" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/draft/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/cli-access/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/environment/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/harness-auth/authentication/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/harness-auth/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/planned-messages/[timeoutId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/servers/[serverId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-folders/[folderId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-folders/[folderId]/visibility/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/[agentName]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/profile/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/push-subscriptions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/user-memory/[memoryId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/user-wallet/[walletId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/users/[username]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/agents/[agentId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/folders/[folderId]/route.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "as" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/chat/CanonicalAgentChatSurface.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat/export/pdf/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/chatExport/downloadChatPdfFromServer.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "generateMetadata" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/chat/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/history/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/layout.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/embed/layout.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/layout.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "UserChatsSnapshot" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/chat/useAgentChatHistorySyncState.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChatClient.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "header" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/AgentCodePageHeader.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/useAgentCodeExportState.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "banner" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/TranspiledCodeErrorBanner.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/TranspiledCodeExportWarningBanner.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "YourComponent" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/integration/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/website-integration/page.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "alt" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/opengraph-image.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/test/og-image/opengraph-image.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "size" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/opengraph-image.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/test/og-image/opengraph-image.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "contentType" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/opengraph-image.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/test/og-image/opengraph-image.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "revalidate" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book-language.md/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book-language.pdf/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book.md/route.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "metadata" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/recycle-bin/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/test/og-image/page.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "ControlPanelOptionAvailability" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/Header/ControlPanel/ControlPanelContentState.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/getControlPanelOptionAvailability.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "collectDescendantFolderIds" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/Homepage/agentOrganizationUtils.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/agentOrganization/folderTree.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "findAgentByIdentifier" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/Homepage/agentOrganizationUtils.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/agentOwnership.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "are" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/Homepage/AgentsListHeader.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/transpilers/resolveTranspiledAgentCodeExport.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "Card" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/Homepage/Card.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/NewAgentDialog/ManGoNewAgentWizard/components/ui/Card.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "normalizeServerUrl" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/Homepage/normalizeServerUrl.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/search/createDefaultServerSearchProviders/normalizeServerUrl.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "Button" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/NewAgentDialog/ManGoNewAgentWizard/components/ui/Button.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/agentProjects/listAgentProjectDirectoryEntries.test.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "quoteIdentifier" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/database/sqlite/localSqliteSupabase/localSqliteSql.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/serverManagement/createManagedServer/createSqlRecorder.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChatTimeout/userChatTimeoutStore/quoteIdentifier.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "UserChatRow" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/backup/serverBackup/serverBackupTypes.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChat/UserChatRow.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "ChatFeedbackRow" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/backup/serverBackup/serverBackupTypes.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/chatFeedbackAdmin.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "CalendarProvider" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/calendars/calendarConnections/CalendarConnectionRecord.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/calendars/providers/CalendarProvider.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "MetaDisclaimerStatus" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/metaDisclaimer.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/metaDisclaimerClient.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "listRegisteredServersUsingServiceRole" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/serverRegistry.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/serverRegistryNode.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "UsageAnalyticsQuery" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/usageAdmin.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/usageAnalytics/UsageAnalyticsQuery.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "createUserChat" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChat/createUserChat.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChatClient/createUserChat.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "UserChatSummary" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChat/UserChatRecord.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChatClient.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "cancelUserChatTimeout" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChatClient/cancelUserChatTimeout.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChatTimeout/userChatTimeoutStore/cancelUserChatTimeout.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

PASS src/cli/test/ptbk.test.ts (154.209 s)
PASS apps/agents-server/src/app/agents/[agentName]/AgentProfileChat.optimisticNavigation.test.tsx (7.661 s)
PASS scripts/run-codex-prompts/common/runGoScript/$spawnLoggedBashScript.test.ts (22.024 s)
PASS scripts/run-codex-prompts/git/coderCommitScope.test.ts (18.206 s)
PASS src/llm-providers/openai/OpenAiAgentKitExecutionTools.test.ts (8.029 s)
PASS apps/agents-server/src/tools/$provideCdnForServer.test.ts (5.101 s)
PASS scripts/run-agent-messages/git/commitAgentProjectChanges.test.ts (8.288 s)
PASS src/scripting/_test/custom-function-async.test.ts.test.ts (5.048 s)
  ● Console

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      
      // The script:
      const resultString = "Matthew the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Matthew the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      
      // The script:
      const resultString = "Mark the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Mark the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      
      // The script:
      const resultString = "Luke the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Luke the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      
      // The script:
      const resultString = "John the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello John the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

PASS apps/agents-server/src/tools/agentGoalChatTimeoutTools.test.ts
PASS apps/agents-server/src/message-providers/email/stalwart/parseInboundStalwartEmail.test.ts
PASS src/llm-providers/mocked/test/mocked-completion.test.ts
  ● Console

    console.info
      🖋 Mocked callCompletionModel call

      at MockedEchoLlmExecutionTools.callCompletionModel (src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts:141:21)
          at async Promise.all (index 0)

PASS src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts
  ● Console

    console.info
      [36mCreating pipeline collection from path ./examples/pipelines[39m

      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:251:17)

    console.info
      [90mSkipped file examples/pipelines/01-simple.ptbk.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/25-simple-knowledge.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/25-simple-knowledge.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/27-document-knowledge.docx – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/27-document-knowledge.pdf – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-advanced.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-advanced.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:325:47)
      at loadPipelinesFromFiles (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:302:41)
      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:257:32)
      at load (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:44:35)
      at Object.listPipelines (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:49:9)
      at createPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:153:9)
      at Object.<anonymous> (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts:10:28)

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:325:47)
      at loadPipelinesFromFiles (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:302:41)
      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:257:32)
      at load (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:44:35)
      at Object.listPipelines (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:49:9)
      at createPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:153:9)
      at Object.<anonymous> (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts:10:28)

    console.info
      [90mSkipped file examples/pipelines/50-nesting.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-non-standard-order-of-commands.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/80-foreach-list.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/80-foreach-list.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach-csv.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach-csv.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach.csv – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/alternative-notations.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/README.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/TODO.txt – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [33mCan not load pipeline from examples/pipelines/00-flat.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/01-simple.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/05-comments.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/10-single.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/11-picking-model.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/20-two.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/25-simple-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/30-escaping.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/41-jokers.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-expect-json.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-rare-expectations.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/00-flat.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/01-simple.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [32mLoaded pipeline examples/pipelines/26-markdown-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/27-document-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/28-website-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/29-mixed-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/40-postprocessing.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/41-postprocessing.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-expectations.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/50-advanced.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [90mSkipped pipeline examples/pipelines/05-comments.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/10-single.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/11-picking-model.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/20-two.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/25-simple-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/26-markdown-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/27-document-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/28-website-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/29-mixed-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/30-escaping.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/40-postprocessing.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/41-jokers.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/41-postprocessing.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-expect-json.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-expectations.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-rare-expectations.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/50-advanced.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-name.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-variant.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-persona-2.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/50-nesting.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-0.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-1.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/51-empty-pipeline-2.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/60-json-mode.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/70-parameter-used-in-reverse-order.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/80-foreach-list.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/85-foreach-csv.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/advanced-chatbot.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/implicit-formfactor.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/koralkykatlas-chat.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/quick-chatbot.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-name.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-variant.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-persona-2.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/50-nesting.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-0.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-1.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/51-empty-pipeline-2.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/60-json-mode.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/70-parameter-used-in-reverse-order.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/80-foreach-list.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/85-foreach-csv.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/advanced-chatbot.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/implicit-formfactor.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/koralkykatlas-chat.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/quick-chatbot.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [36mCreating pipeline collection from path ./examples/pipelines[39m

      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:251:17)

    console.info
      [90mSkipped file examples/pipelines/01-simple.ptbk.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/25-simple-knowledge.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/25-simple-knowledge.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/27-document-knowledge.docx – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/27-document-knowledge.pdf – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-advanced.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-advanced.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:325:47)
      at loadPipelinesFromFiles (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:302:41)
      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:257:32)
      at load (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:44:35)
      at Object.getPipelineByUrl (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:53:9)
      at Object.<anonymous> (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts:43:38)

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:325:47)
      at loadPipelinesFromFiles (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:302:41)
      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:257:32)
      at load (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:44:35)
      at Object.getPipelineByUrl (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:53:9)
      at Object.<anonymous> (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts:43:38)

    console.info
      [90mSkipped file examples/pipelines/50-nesting.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-non-standard-order-of-commands.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/80-foreach-list.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/80-foreach-list.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach-csv.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach-csv.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach.csv – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/alternative-notations.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/README.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/TODO.txt – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [33mCan not load pipeline from examples/pipelines/00-flat.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/01-simple.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/05-comments.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/10-single.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/11-picking-model.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/20-two.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/25-simple-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/30-escaping.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/41-jokers.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-expect-json.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-rare-expectations.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/00-flat.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/01-simple.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [32mLoaded pipeline examples/pipelines/26-markdown-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/27-document-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/28-website-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/29-mixed-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/40-postprocessing.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/41-postprocessing.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-expectations.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/50-advanced.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [90mSkipped pipeline examples/pipelines/05-comments.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/10-single.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/11-picking-model.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/20-two.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/25-simple-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/26-markdown-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/27-document-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/28-website-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/29-mixed-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/30-escaping.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/40-postprocessing.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/41-jokers.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/41-postprocessing.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-expect-json.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-expectations.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-rare-expectations.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/50-advanced.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-name.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-variant.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-persona-2.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/50-nesting.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-0.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-1.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/51-empty-pipeline-2.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/60-json-mode.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/70-parameter-used-in-reverse-order.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/80-foreach-list.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/85-foreach-csv.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/advanced-chatbot.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/implicit-formfactor.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/koralkykatlas-chat.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/quick-chatbot.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-name.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-variant.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-persona-2.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/50-nesting.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-0.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-1.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/51-empty-pipeline-2.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/60-json-mode.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/70-parameter-used-in-reverse-order.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/80-foreach-list.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/85-foreach-csv.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/advanced-chatbot.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/implicit-formfactor.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/koralkykatlas-chat.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/quick-chatbot.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [36mCreating pipeline collection from path ./examples/pipelines[39m

      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:251:17)

    console.info
      [90mSkipped file examples/pipelines/01-simple.ptbk.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/25-simple-knowledge.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/25-simple-knowledge.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/27-document-knowledge.docx – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/27-document-knowledge.pdf – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-advanced.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-advanced.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:325:47)
      at loadPipelinesFromFiles (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:302:41)
      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:257:32)
      at load (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:44:35)
      at Object.listPipelines (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:49:9)
      at createPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:153:9)
      at Object.<anonymous> (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts:56:28)

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:325:47)
      at loadPipelinesFromFiles (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:302:41)
      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:257:32)
      at load (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:44:35)
      at Object.listPipelines (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:49:9)
      at createPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:153:9)
      at Object.<anonymous> (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts:56:28)

    console.info
      [90mSkipped file examples/pipelines/50-nesting.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-non-standard-order-of-commands.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/80-foreach-list.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/80-foreach-list.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach-csv.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach-csv.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach.csv – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/alternative-notations.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/README.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/TODO.txt – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [33mCan not load pipeline from examples/pipelines/00-flat.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/01-simple.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/05-comments.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/10-single.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/11-picking-model.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/20-two.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/25-simple-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/30-escaping.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/41-jokers.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-expect-json.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-rare-expectations.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/00-flat.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/01-simple.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [32mLoaded pipeline examples/pipelines/26-markdown-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/27-document-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/28-website-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/29-mixed-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/40-postprocessing.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/41-postprocessing.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-expectations.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/50-advanced.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [90mSkipped pipeline examples/pipelines/05-comments.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/10-single.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/11-picking-model.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/20-two.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/25-simple-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/26-markdown-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/27-document-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/28-website-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/29-mixed-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/30-escaping.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/40-postprocessing.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/41-jokers.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/41-postprocessing.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-expect-json.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-expectations.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-rare-expectations.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/50-advanced.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-name.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-variant.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-persona-2.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/50-nesting.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-0.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-1.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/51-empty-pipeline-2.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/60-json-mode.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/70-parameter-used-in-reverse-order.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/80-foreach-list.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/85-foreach-csv.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/advanced-chatbot.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/implicit-formfactor.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/koralkykatlas-chat.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/quick-chatbot.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-name.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-variant.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-persona-2.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/50-nesting.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-0.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-1.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/51-empty-pipeline-2.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/60-json-mode.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/70-parameter-used-in-reverse-order.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/80-foreach-list.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/85-foreach-csv.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/advanced-chatbot.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/implicit-formfactor.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/koralkykatlas-chat.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/quick-chatbot.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [36mCreating pipeline collection from path ./examples/pipelines[39m

      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:251:17)

    console.info
      [90mSkipped file examples/pipelines/01-simple.ptbk.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/25-simple-knowledge.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/25-simple-knowledge.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/27-document-knowledge.docx – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/27-document-knowledge.pdf – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-advanced.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-advanced.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:325:47)
      at loadPipelinesFromFiles (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:302:41)
      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:257:32)
      at load (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:44:35)
      at Object.listPipelines (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:49:9)
      at createPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:153:9)
      at src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts:94:36

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:325:47)
      at loadPipelinesFromFiles (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:302:41)
      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:257:32)
      at load (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:44:35)
      at Object.listPipelines (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:49:9)
      at createPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:153:9)
      at src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts:94:36

    console.info
      [90mSkipped file examples/pipelines/50-nesting.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-non-standard-order-of-commands.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/80-foreach-list.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/80-foreach-list.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach-csv.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach-csv.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach.csv – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/alternative-notations.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/README.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/TODO.txt – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/errors/TODO.txt – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/errors/logic/model-variant-mismatch.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/errors/logic/sample-dont-pass-expectations.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/errors/logic/void-knowledge.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

PASS src/utils/markdown/humanizeAiText.test.ts (7.402 s)
PASS src/scrapers/markdown/MarkdownScraper.test.ts (5.857 s)
PASS src/book-components/Chat/Chat/ChatToolCallModal.test.tsx
PASS src/book-components/Chat/LlmChat/LlmChat.test.tsx
  ● Console

    console.error
      `ReactDOMTestUtils.act` is deprecated in favor of `React.act`. Import `act` from `react` instead of `react-dom/test-utils`. See https://react.dev/warnings/react-dom-test-utils for more info.

    [0m [90m 68 |[39m         document[33m.[39mbody[33m.[39mappendChild(container)[33m;[39m
     [90m 69 |[39m
    [31m[1m>[22m[39m[90m 70 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m    |[39m                  [31m[1m^[22m[39m
     [90m 71 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
     [90m 72 |[39m             root[33m.[39mrender(
     [90m 73 |[39m                 [33m<[39m[33mLlmChat[39m title[33m=[39m[32m"Test"[39m llmTools[33m=[39m{customLlmTools [36mas[39m [33mTODO_any[39m} thread[33m=[39m{thread} layout[33m=[39m[32m"STANDALONE"[39m [33m/[39m[33m>[39m[33m,[39m[0m

      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:19:17)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:70:18)

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 70 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m 71 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
    [31m[1m>[22m[39m[90m 72 |[39m             root[33m.[39mrender(
     [90m    |[39m                  [31m[1m^[22m[39m
     [90m 73 |[39m                 [33m<[39m[33mLlmChat[39m title[33m=[39m[32m"Test"[39m llmTools[33m=[39m{customLlmTools [36mas[39m [33mTODO_any[39m} thread[33m=[39m{thread} layout[33m=[39m[32m"STANDALONE"[39m [33m/[39m[33m>[39m[33m,[39m
     [90m 74 |[39m             )[33m;[39m
     [90m 75 |[39m         })[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:72:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:70:18)

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 611 |[39m             })[33m;[39m
     [90m 612 |[39m
    [31m[1m>[22m[39m[90m 613 |[39m             setMessages((previousMessages) [33m=>[39m [[33m...[39mpreviousMessages[33m,[39m userMessage[33m,[39m loadingMessage])[33m;[39m
     [90m     |[39m             [31m[1m^[22m[39m
     [90m 614 |[39m
     [90m 615 |[39m             [36mconst[39m taskId [33m=[39m [32m`llm_call_${Date.now()}`[39m[33m;[39m
     [90m 616 |[39m             setTasksProgress([{ id[33m:[39m taskId[33m,[39m name[33m:[39m [32m'Generating response...'[39m[33m,[39m progress[33m:[39m [35m0[39m }])[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at Object.onMessage (src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts:613:13)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:82:28
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:81:18)

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 614 |[39m
     [90m 615 |[39m             [36mconst[39m taskId [33m=[39m [32m`llm_call_${Date.now()}`[39m[33m;[39m
    [31m[1m>[22m[39m[90m 616 |[39m             setTasksProgress([{ id[33m:[39m taskId[33m,[39m name[33m:[39m [32m'Generating response...'[39m[33m,[39m progress[33m:[39m [35m0[39m }])[33m;[39m
     [90m     |[39m             [31m[1m^[22m[39m
     [90m 617 |[39m
     [90m 618 |[39m             [36mconst[39m generationStartedAtMs [33m=[39m [33mDate[39m[33m.[39mnow()[33m;[39m
     [90m 619 |[39m             [36mlet[39m streamingAbortController[33m:[39m [33mAbortController[39m [33m|[39m [36mnull[39m [33m=[39m [36mnull[39m[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at Object.onMessage (src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts:616:13)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:82:28
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:81:18)

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 629 |[39m                 })[33m;[39m
     [90m 630 |[39m
    [31m[1m>[22m[39m[90m 631 |[39m                 setTasksProgress([{ id[33m:[39m taskId[33m,[39m name[33m:[39m [32m'Generating response...'[39m[33m,[39m progress[33m:[39m [35m50[39m }])[33m;[39m
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 632 |[39m
     [90m 633 |[39m                 [36mconst[39m execution [33m=[39m [36mawait[39m executeLlmChatPrompt({
     [90m 634 |[39m                     assistantMessageStartedAt[33m,[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at Object.onMessage (src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts:631:17)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:82:28
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:81:18)

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 644 |[39m
     [90m 645 |[39m                 streamingAbortController [33m=[39m execution[33m.[39mstreamingAbortController[33m;[39m
    [31m[1m>[22m[39m[90m 646 |[39m                 setTasksProgress([{ id[33m:[39m taskId[33m,[39m name[33m:[39m [32m'Response generated'[39m[33m,[39m progress[33m:[39m [35m100[39m }])[33m;[39m
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 647 |[39m
     [90m 648 |[39m                 [36mif[39m ([33m![39mexecution[33m.[39mresult[33m.[39mcontent[33m.[39mtrim()) {
     [90m 649 |[39m                     [36mthrow[39m [36mnew[39m [33mError[39m([32m'The agent did not respond.'[39m)[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at Object.onMessage (src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts:646:17)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:82:13

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 659 |[39m                 })[33m;[39m
     [90m 660 |[39m
    [31m[1m>[22m[39m[90m 661 |[39m                 setMessages((previousMessages) [33m=>[39m
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 662 |[39m                     updateMessageById(previousMessages[33m,[39m loadingMessage[33m.[39mid[33m,[39m () [33m=>[39m assistantMessage)[33m,[39m
     [90m 663 |[39m                 )[33m;[39m
     [90m 664 |[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at Object.onMessage (src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts:661:17)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:82:13

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 702 |[39m                 }
     [90m 703 |[39m
    [31m[1m>[22m[39m[90m 704 |[39m                 setIsStreaming([36mfalse[39m)[33m;[39m
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 705 |[39m                 requestInFlightRef[33m.[39mcurrent [33m=[39m [36mfalse[39m[33m;[39m
     [90m 706 |[39m             }
     [90m 707 |[39m         }[33m,[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at Object.onMessage (src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts:704:17)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:82:13

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 262 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m 263 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
    [31m[1m>[22m[39m[90m 264 |[39m             root[33m.[39mrender(
     [90m     |[39m                  [31m[1m^[22m[39m
     [90m 265 |[39m                 [33m<[39m[33mLlmChat[39m
     [90m 266 |[39m                     title[33m=[39m[32m"Delegated reset"[39m
     [90m 267 |[39m                     llmTools[33m=[39m{mockLlmTools}[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:264:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:262:18)

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 304 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m 305 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
    [31m[1m>[22m[39m[90m 306 |[39m             root[33m.[39mrender(
     [90m     |[39m                  [31m[1m^[22m[39m
     [90m 307 |[39m                 [33m<[39m[33mLlmChat[39m title[33m=[39m[32m"Test"[39m llmTools[33m=[39m{mockLlmTools} initialMessages[33m=[39m{initialMessages} layout[33m=[39m[32m"STANDALONE"[39m [33m/[39m[33m>[39m[33m,[39m
     [90m 308 |[39m             )[33m;[39m
     [90m 309 |[39m         })[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:306:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:304:18)

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 310 |[39m         }
     [90m 311 |[39m
    [31m[1m>[22m[39m[90m 312 |[39m         setMessages(buildInitialMessages())[33m;[39m
     [90m     |[39m         [31m[1m^[22m[39m
     [90m 313 |[39m         setTasksProgress([])[33m;[39m
     [90m 314 |[39m         hasUserInteractedRef[33m.[39mcurrent [33m=[39m [36mfalse[39m[33m;[39m
     [90m 315 |[39m         clearLastFailedMessage()[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at Object.onReset (src/book-components/Chat/LlmChat/useLlmChatState.ts:312:9)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:323:30
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:322:18)

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 311 |[39m
     [90m 312 |[39m         setMessages(buildInitialMessages())[33m;[39m
    [31m[1m>[22m[39m[90m 313 |[39m         setTasksProgress([])[33m;[39m
     [90m     |[39m         [31m[1m^[22m[39m
     [90m 314 |[39m         hasUserInteractedRef[33m.[39mcurrent [33m=[39m [36mfalse[39m[33m;[39m
     [90m 315 |[39m         clearLastFailedMessage()[33m;[39m
     [90m 316 |[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at Object.onReset (src/book-components/Chat/LlmChat/useLlmChatState.ts:313:9)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:323:30
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:322:18)

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 723 |[39m
     [90m 724 |[39m     [36mconst[39m clearLastFailedMessage [33m=[39m useCallback(() [33m=>[39m {
    [31m[1m>[22m[39m[90m 725 |[39m         setLastFailedMessage([36mnull[39m)[33m;[39m
     [90m     |[39m         [31m[1m^[22m[39m
     [90m 726 |[39m     }[33m,[39m [])[33m;[39m
     [90m 727 |[39m
     [90m 728 |[39m     [36mconst[39m handleRetry [33m=[39m useCallback(() [33m=>[39m {[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts:725:9
      at Object.onReset (src/book-components/Chat/LlmChat/useLlmChatState.ts:315:9)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:323:30
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:322:18)

PASS apps/agents-server/src/utils/agentProjects/agentProjectRuntimeRegistry.test.ts
PASS scripts/run-agent-messages/main/runMultipleAgentMessages.test.ts
  ● Console

    console.info
      [32mWatching direct child agent repositories for queued messages.[39m

      at runMultipleAgentMessages (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:74:29)

    console.info
      [34mProcessing agent-b/messages/queued/question.book with Agent B.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [32mWatching direct child agent repositories for queued messages.[39m

      at runMultipleAgentMessages (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:74:29)

    console.info
      [34mProcessing agent-a/messages/queued/question-a.book with Agent A.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [34mProcessing agent-b/messages/queued/question-b.book with Agent B.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [32mWatching direct child agent repositories for queued messages.[39m

      at runMultipleAgentMessages (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:74:29)

    console.info
      [34mProcessing agent-a/messages/queued/first.book with Agent A.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [34mProcessing agent-a/messages/queued/second.book with Agent A.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [32mWatching direct child agent repositories for queued messages.[39m

      at runMultipleAgentMessages (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:74:29)

    console.info
      [34mProcessing agent-active/messages/queued/question.book with Active Agent.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [32mWatching direct child agent repositories for queued messages.[39m

      at runMultipleAgentMessages (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:74:29)

    console.info
      [90mCloned 1 new agent repository: agent-new[39m

      at MultipleAgentGithubSynchronizer.synchronizeIfNeeded (scripts/run-agent-messages/main/runMultipleAgentMessages/MultipleAgentGithubSynchronizer.ts:71:21)

    console.info
      [34mProcessing agent-new/messages/queued/question.book with Agent new.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [32mWatching direct child agent repositories for queued messages.[39m

      at runMultipleAgentMessages (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:74:29)

    console.info
      [90mPulling latest changes for 2 watched agent repositories...[39m

      at MultipleAgentAutoPuller.pullIfNeeded (scripts/run-agent-messages/main/runMultipleAgentMessages/MultipleAgentAutoPuller.ts:56:21)

    console.info
      [34mProcessing agent-b/messages/queued/question.book with Agent B.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [32mWatching direct child agent repositories for queued messages.[39m

      at runMultipleAgentMessages (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:74:29)

    console.info
      [34mProcessing agent-a/messages/queued/question-a.book with Agent A.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [34mProcessing agent-b/messages/queued/question-b.book with Agent B.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [32mWatching direct child agent repositories for queued messages.[39m

      at runMultipleAgentMessages (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:74:29)

    console.info
      [34mProcessing agent-a/messages/queued/question.book with Agent A.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [34mProcessing agent-a/messages/queued/question.book with Agent A.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

PASS scripts/run-agent-messages/main/runAgentMessages.test.ts
PASS src/transpilers/anthropic-claude-sdk/AnthropicClaudeSdkTranspiler.test.ts
PASS apps/agents-server/scripts/sync-vercel-domains.test.ts
PASS src/book-3.0/LiteAgent.test.ts
PASS apps/agents-server/src/utils/agentsTransfer/importAgentsFromFiles.test.ts
PASS scripts/run-codex-prompts/common/resolveCoderAgent.references.test.ts (5.253 s)
PASS src/llm-providers/mocked/test/mocked-chat.test.ts
  ● Console

    console.info
      💬 Mocked callChatModel call

      at MockedEchoLlmExecutionTools.callChatModel (src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts:81:21)
          at async Promise.all (index 0)

PASS apps/agents-server/src/utils/backup/createServerBackupZipStream.test.ts
PASS src/book-components/Chat/save/html/htmlSaveFormatDefinition.test.ts
PASS src/book-2.0/book-language-documentation/createStandaloneBookLanguageMarkdown.test.ts
PASS apps/agents-server/src/app/superadmin/servers/ServersRegistryTable.test.tsx
PASS apps/agents-server/src/utils/cancelAllActiveAdminChatTasks.test.ts
PASS apps/agents-server/src/app/api/chat/export/pdf/route.test.ts
PASS apps/agents-server/src/tools/run_browser.test.ts
  ● Console

    console.info
      [run_browser][metric] {
        tool: 'run_browser',
        mode: 'remote-browser',
        sessionId: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000',
        event: 'run_browser_failed',
        errorCode: 'RUN_BROWSER_VALIDATION_ERROR',
        errorCodeCount: 1,
        connectDurationMs: null,
        initialNavigationDurationMs: null,
        timeToFirstByteMs: null
      }

      at Object.logRunBrowserMetric (apps/agents-server/src/tools/runBrowserObservability.ts:66:17)

    console.info
      [run_browser][metric] {
        tool: 'run_browser',
        mode: 'remote-browser',
        sessionId: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000',
        event: 'run_browser_success',
        actions: 0,
        connectDurationMs: 0,
        initialNavigationDurationMs: 0,
        timeToFirstByteMs: null
      }

      at Object.logRunBrowserMetric (apps/agents-server/src/tools/runBrowserObservability.ts:66:17)

    console.warn
      [run_browser] Full-page snapshot failed, retrying viewport-only screenshot {
        sessionId: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000',
        snapshotFilename: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000-initial.png',
        error: 'Screenshot is too large'
      }

    [0m [90m 78 |[39m                 [36mawait[39m page[33m.[39mscreenshot({ path[33m:[39m snapshotPath[33m,[39m fullPage[33m:[39m [36mtrue[39m })[33m;[39m
     [90m 79 |[39m             } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 80 |[39m                 console[33m.[39mwarn([32m'[run_browser] Full-page snapshot failed, retrying viewport-only screenshot'[39m[33m,[39m {
     [90m    |[39m                         [31m[1m^[22m[39m
     [90m 81 |[39m                     sessionId[33m,[39m
     [90m 82 |[39m                     snapshotFilename[33m,[39m
     [90m 83 |[39m                     error[33m:[39m getErrorMessage(error)[33m,[39m[0m

      at Object.captureSnapshot (apps/agents-server/src/tools/runBrowserArtifacts.ts:80:25)
      at Object.captureSnapshotArtifact (apps/agents-server/src/tools/runBrowserArtifacts.ts:133:22)
      at run_browser (apps/agents-server/src/tools/run_browser.ts:131:33)
      at Object.<anonymous> (apps/agents-server/src/tools/run_browser.test.ts:201:24)

    console.info
      [run_browser][metric] {
        tool: 'run_browser',
        mode: 'remote-browser',
        sessionId: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000',
        event: 'run_browser_success',
        actions: 0,
        connectDurationMs: 0,
        initialNavigationDurationMs: 0,
        timeToFirstByteMs: null
      }

      at Object.logRunBrowserMetric (apps/agents-server/src/tools/runBrowserObservability.ts:66:17)

    console.info
      [run_browser][metric] {
        tool: 'run_browser',
        mode: 'remote-browser',
        sessionId: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000',
        event: 'run_browser_success',
        actions: 5,
        connectDurationMs: 0,
        initialNavigationDurationMs: 0,
        timeToFirstByteMs: null
      }

      at Object.logRunBrowserMetric (apps/agents-server/src/tools/runBrowserObservability.ts:66:17)

    console.info
      [run_browser][metric] {
        tool: 'run_browser',
        mode: 'remote-browser',
        sessionId: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000',
        event: 'run_browser_failed',
        errorCode: 'RUN_BROWSER_VALIDATION_ERROR',
        errorCodeCount: 2,
        connectDurationMs: null,
        initialNavigationDurationMs: null,
        timeToFirstByteMs: null
      }

      at Object.logRunBrowserMetric (apps/agents-server/src/tools/runBrowserObservability.ts:66:17)

    console.info
      [run_browser][metric] {
        tool: 'run_browser',
        mode: 'fallback',
        sessionId: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000',
        event: 'run_browser_fallback_used',
        errorCode: 'REMOTE_BROWSER_UNAVAILABLE',
        errorCodeCount: 1,
        fallbackRuns: 1,
        totalRuns: 6,
        fallbackRate: 0.16666666666666666
      }

      at Object.logRunBrowserMetric (apps/agents-server/src/tools/runBrowserObservability.ts:66:17)

    console.info
      [run_browser][metric] {
        tool: 'run_browser',
        mode: 'remote-browser',
        sessionId: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000',
        event: 'run_browser_failed',
        errorCode: 'RUN_BROWSER_NAVIGATION_FAILED',
        errorCodeCount: 1,
        connectDurationMs: null,
        initialNavigationDurationMs: null,
        timeToFirstByteMs: null
      }

      at Object.logRunBrowserMetric (apps/agents-server/src/tools/runBrowserObservability.ts:66:17)

PASS src/book-components/Chat/Chat/ChatMessageItem.test.tsx
PASS src/transpilers/_common/transpiledTeamTranspilers.test.ts
PASS src/scrapers/_common/prepareKnowledgePieces.test.ts
PASS scripts/find-fresh-emoji-tags/utils/$shuffleItems.test.ts
PASS src/transpilers/openai-sdk/OpenAiSdkTranspiler.test.ts
PASS src/book-components/Chat/Chat/pagePreview/LiveBrowserPreview.test.tsx
PASS apps/agents-server/src/tools/getAllToolFunctionsForServer.test.ts
PASS apps/agents-server/src/database/sqlite/$provideLocalSqliteSupabase.test.ts
PASS src/book-components/Chat/Chat/ChatInputArea.test.tsx
PASS scripts/run-agent-messages/main/tickAgentMessages.test.ts
PASS src/transpilers/agent-os/AgentOsTranspiler.test.ts
PASS src/scripting/javascript/utils/extractVariablesFromJavascript.test.ts
  ● Console

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

PASS src/transpilers/anthropic-claude-managed/AnthropicClaudeManagedTranspiler.test.ts
PASS scripts/find-refactor-candidates/analyzeSourceFileForRefactorCandidate.test.ts
PASS apps/agents-server/src/utils/resolveAgentStateFromSource.storedAgents.test.ts
PASS apps/agents-server/src/instrumentation-node.test.ts
  ● Console

    console.info
      🗂️ VPS server registry ready with 0 registered server(s) in standalone SQLite mode.

      at registerNodeRuntimeInstrumentation (apps/agents-server/src/instrumentation-node.ts:29:21)

PASS apps/agents-server/src/utils/transpilers/resolveTranspiledAgentCodeExport.test.ts
PASS apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.test.ts
  ● Console

    console.info
      [local-chat-runner] planned_message_set {
        chatId: 'goal-agent1234',
        jobId: 'job-1',
        timeoutId: 'timeout-2',
        dueAt: '2026-08-14T11:00:00.000Z'
      }

      at applyLocalAgentPlannedMessageCommand (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.ts:133:21)

    console.info
      [local-chat-runner] planned_message_cancel {
        chatId: 'goal-agent1234',
        jobId: 'job-1',
        timeoutId: 'timeout-1',
        status: 'cancelled'
      }

      at applyLocalAgentPlannedMessageCommand (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.ts:167:17)

    console.info
      [local-chat-runner] planned_message_set {
        chatId: 'goal-agent1234',
        jobId: 'job-1',
        timeoutId: 'timeout-2',
        dueAt: '2026-08-14T11:00:00.000Z'
      }

      at applyLocalAgentPlannedMessageCommand (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.ts:133:21)

    console.error
      [local-chat-runner] planned_message_command_failed {
        chatId: 'goal-agent1234',
        jobId: 'job-1',
        action: 'set',
        error: {
          name: 'Error',
          message: 'Invalid planned-message delay.',
          stack: 'Error: Invalid planned-message delay.\n' +
            '    at Object.<anonymous> (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\utils\\localChatRunner\\applyLocalAgentPlannedMessageCommands.test.ts:96:36)',
          id: undefined
        }
      }

    [0m [90m 174 |[39m         [36mreturn[39m cancelledPlannedMessage[33m.[39mstatus [33m===[39m [32m'cancelled'[39m [33m?[39m cancelledPlannedMessage [33m:[39m [36mnull[39m[33m;[39m
     [90m 175 |[39m     } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 176 |[39m         console[33m.[39merror([32m'[local-chat-runner]'[39m[33m,[39m [32m'planned_message_command_failed'[39m[33m,[39m {
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 177 |[39m             chatId[33m:[39m options[33m.[39mjob[33m.[39mchatId[33m,[39m
     [90m 178 |[39m             jobId[33m:[39m options[33m.[39mjob[33m.[39mid[33m,[39m
     [90m 179 |[39m             action[33m:[39m options[33m.[39mcommand[33m.[39maction[33m,[39m[0m

      at applyLocalAgentPlannedMessageCommand (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.ts:176:17)
      at applyLocalAgentPlannedMessageCommands (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.ts:78:24)
      at Object.<anonymous> (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.test.ts:99:33)

    console.info
      [local-chat-runner] planned_message_set {
        chatId: 'goal-agent1234',
        jobId: 'job-1',
        timeoutId: 'timeout-2',
        dueAt: '2026-08-14T11:00:00.000Z'
      }

      at applyLocalAgentPlannedMessageCommand (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.ts:133:21)

    console.info
      [local-chat-runner] planned_message_update {
        chatId: 'goal-agent1234',
        jobId: 'job-1',
        timeoutId: 'timeout-1',
        status: 'updated',
        dueAt: '2026-08-14T12:00:00.000Z'
      }

      at applyLocalAgentPlannedMessageCommand (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.ts:151:21)

    console.info
      [local-chat-runner] planned_message_update {
        chatId: 'goal-agent1234',
        jobId: 'job-1',
        timeoutId: 'timeout-1',
        status: 'not_found',
        dueAt: '2026-08-14T12:00:00.000Z'
      }

      at applyLocalAgentPlannedMessageCommand (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.ts:151:21)

PASS src/cli/cli-commands/agents-server/buildAgentsServer.test.ts
PASS src/scripting/_test/script-execution-errors.test.ts
  ● Console

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "a cup of coffee";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      b cup of coffee

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "arrow";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:
      brrow

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "aaa";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:
      bbb

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #2

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at Object.asPromise (src/execution/ExecutionTask.ts:261:34)
      at Object.<anonymous> (src/scripting/_test/script-execution-errors.test.ts:53:13)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #2

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "apples";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #3

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "apples";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #4

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at Object.asPromise (src/execution/ExecutionTask.ts:261:34)
      at Object.<anonymous> (src/scripting/_test/script-execution-errors.test.ts:53:13)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #4

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "an apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #5

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "an apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #6

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at Object.asPromise (src/execution/ExecutionTask.ts:261:34)
      at Object.<anonymous> (src/scripting/_test/script-execution-errors.test.ts:53:13)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #6

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "Apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #7

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "Apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #8

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at Object.asPromise (src/execution/ExecutionTask.ts:261:34)
      at Object.<anonymous> (src/scripting/_test/script-execution-errors.test.ts:53:13)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #8

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "The Apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #9

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "The Apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #10

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at Object.asPromise (src/execution/ExecutionTask.ts:261:34)
      at Object.<anonymous> (src/scripting/_test/script-execution-errors.test.ts:53:13)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #10

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "🍏 Apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #11

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "🍏 Apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #12

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at Object.asPromise (src/execution/ExecutionTask.ts:261:34)
      at Object.<anonymous> (src/scripting/_test/script-execution-errors.test.ts:53:13)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #12

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "Apple 🍎";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #13

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "Apple 🍎";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #14

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at Object.asPromise (src/execution/ExecutionTask.ts:261:34)
      at Object.<anonymous> (src/scripting/_test/script-execution-errors.test.ts:53:13)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #14

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

PASS src/scripting/javascript/utils/extractVariablesFromScript.test.ts
  ● Console

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

PASS src/transpilers/e2b/E2BTranspiler.test.ts
PASS apps/agents-server/src/utils/agentGoalChat/scheduleAgentGoalChatModifiedNote.test.ts
PASS src/transpilers/openai-agents/OpenAiAgentsTranspiler.test.ts
PASS apps/agents-server/src/utils/agentProjects/startDefaultAgentProjectRuntimes.test.ts
PASS apps/agents-server/src/components/WalletRecordDialog/WalletRecordDialog.test.tsx
PASS apps/agents-server/src/utils/userChat/persistUserChatJobTerminalState.test.ts
  ● Console

    console.error
      [ChatHistory] Failed to record durable user-chat message. {
        agentPermanentId: 'agent-123',
        chatId: 'email-chat-123',
        taskId: 'job-123',
        userId: 3,
        error: Error: `headers` was called outside a request scope. Read more: https://nextjs.org/docs/messages/next-dynamic-api-wrong-context
            at throwForMissingRequestStore (C:\Users\me\work\ai\promptbook\node_modules\next\src\server\app-render\work-unit-async-storage.external.ts:273:9)
            at throwForMissingRequestStore (C:\Users\me\work\ai\promptbook\node_modules\next\src\server\app-render\work-unit-async-storage.external.ts:240:5)
            at headers (C:\Users\me\work\ai\promptbook\node_modules\next\src\server\request\headers.ts:132:47)
            at C:\Users\me\work\ai\promptbook\apps\agents-server\src\tools\$provideServer.ts:38:38
            at C:\Users\me\work\ai\promptbook\node_modules\react\cjs\react.development.js:895:19
            at $provideServer (C:\Users\me\work\ai\promptbook\apps\agents-server\src\tools\$provideServer.ts:137:12)
            at $getTableName (C:\Users\me\work\ai\promptbook\apps\agents-server\src\database\$getTableName.ts:16:49)
            at resolveAgentHash (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveAgentHash.ts:22:43)
            at recordUserChatMessageInChatHistory (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\userChat\recordUserChatMessageInChatHistory.ts:83:49)
            at persistUserChatJobTerminalState (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\userChat\persistUserChatJobTerminalState.ts:83:52)
            at processTicksAndRejections (node:internal/process/task_queues:103:5)
            at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\userChat\persistUserChatJobTerminalState.test.ts:208:9)
      }

    [0m [90m  97 |[39m         })[33m;[39m
     [90m  98 |[39m     } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m  99 |[39m         console[33m.[39merror([32m'[ChatHistory] Failed to record durable user-chat message.'[39m[33m,[39m {
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 100 |[39m             agentPermanentId[33m,[39m
     [90m 101 |[39m             chatId[33m,[39m
     [90m 102 |[39m             taskId[33m,[39m[0m

      at recordUserChatMessageInChatHistory (apps/agents-server/src/utils/userChat/recordUserChatMessageInChatHistory.ts:99:17)

    console.error
      [ChatHistory] Failed to record durable user-chat message. {
        agentPermanentId: 'agent-123',
        chatId: 'email-chat-123',
        taskId: 'job-123',
        userId: 3,
        error: Error: `headers` was called outside a request scope. Read more: https://nextjs.org/docs/messages/next-dynamic-api-wrong-context
            at throwForMissingRequestStore (C:\Users\me\work\ai\promptbook\node_modules\next\src\server\app-render\work-unit-async-storage.external.ts:273:9)
            at throwForMissingRequestStore (C:\Users\me\work\ai\promptbook\node_modules\next\src\server\app-render\work-unit-async-storage.external.ts:240:5)
            at headers (C:\Users\me\work\ai\promptbook\node_modules\next\src\server\request\headers.ts:132:47)
            at C:\Users\me\work\ai\promptbook\apps\agents-server\src\tools\$provideServer.ts:38:38
            at C:\Users\me\work\ai\promptbook\node_modules\react\cjs\react.development.js:895:19
            at $provideServer (C:\Users\me\work\ai\promptbook\apps\agents-server\src\tools\$provideServer.ts:137:12)
            at $getTableName (C:\Users\me\work\ai\promptbook\apps\agents-server\src\database\$getTableName.ts:16:49)
            at resolveAgentHash (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveAgentHash.ts:22:43)
            at recordUserChatMessageInChatHistory (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\userChat\recordUserChatMessageInChatHistory.ts:83:49)
            at persistUserChatJobTerminalState (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\userChat\persistUserChatJobTerminalState.ts:83:52)
            at processTicksAndRejections (node:internal/process/task_queues:103:5)
            at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\userChat\persistUserChatJobTerminalState.test.ts:242:9)
      }

    [0m [90m  97 |[39m         })[33m;[39m
     [90m  98 |[39m     } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m  99 |[39m         console[33m.[39merror([32m'[ChatHistory] Failed to record durable user-chat message.'[39m[33m,[39m {
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 100 |[39m             agentPermanentId[33m,[39m
     [90m 101 |[39m             chatId[33m,[39m
     [90m 102 |[39m             taskId[33m,[39m[0m

      at recordUserChatMessageInChatHistory (apps/agents-server/src/utils/userChat/recordUserChatMessageInChatHistory.ts:99:17)

PASS apps/agents-server/src/utils/chatMessageChips/createAnsweredMessageChipToolCalls.test.ts
PASS apps/agents-server/src/utils/resolveAgentStateFromSource.test.ts
  ● Console

    console.log
      importAgent "https://example.com/agents/parent-metadata"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://example.com/agents/parent-overrides"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://example.com/agents/parent-goal"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

PASS src/conversion/compilePipeline.test.ts
  ● Console

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at src/conversion/compilePipeline.test.ts:19:58

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at src/conversion/compilePipeline.test.ts:19:58

PASS apps/agents-server/src/components/Header/buildDocumentationDropdownItems.test.ts
PASS src/llm-providers/mocked/test/faked-completion.test.ts
  ● Console

    console.info
      💬 Mocked faked prompt {
        title: 'Prompt',
        pipelineUrl: 'https://promptbook.studio/examples/pipeline.book#prompt',
        parameters: { thing: 'a cup of coffee' },
        content: 'One day I went to the shop and bought {thing}.\nNow I have {thing}.',
        modelRequirements: { modelVariant: 'COMPLETION', modelName: 'gpt-3.5-turbo-instruct' },
        expectations: { lines: { min: 2, max: 5 }, words: { min: 10 } },
        format: undefined,
        postprocessingFunctionNames: undefined
      }

      at MockedFackedLlmExecutionTools.callChatModel (src/llm-providers/mocked/MockedFackedLlmExecutionTools.ts:87:21)
          at async Promise.all (index 0)

    console.info
      { expectations: { lines: { min: 2, max: 5 }, words: { min: 10 } } }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)
          at async Promise.all (index 0)

    console.info
      💬 Mocked faked result {
        content: 'Proident nostrud aute et est nostrud quis laborum laboris. Officia',
        modelName: 'mocked-facked',
        timing: {
          start: '2026-09-17T00:45:15.923Z',
          complete: '2026-09-17T00:45:15.923Z'
        },
        usage: {
          price: { value: 0 },
          duration: { value: 0 },
          input: {
            tokensCount: [Object],
            charactersCount: [Object],
            wordsCount: [Object],
            sentencesCount: [Object],
            linesCount: [Object],
            paragraphsCount: [Object],
            pagesCount: [Object]
          },
          output: {
            tokensCount: [Object],
            charactersCount: [Object],
            wordsCount: [Object],
            sentencesCount: [Object],
            linesCount: [Object],
            paragraphsCount: [Object],
            pagesCount: [Object]
          }
        },
        rawPromptContent: 'One day I went to the shop and bought a cup of coffee.\n' +
          'Now I have a cup of coffee.',
        rawRequest: null,
        rawResponse: { note: 'This is mocked echo' }
      }

      at MockedFackedLlmExecutionTools.callChatModel (src/llm-providers/mocked/MockedFackedLlmExecutionTools.ts:122:21)

PASS apps/agents-server/src/database/seedDefaultAgents.test.ts
PASS scripts/repair-imports/utils/repairImportUtils.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/api/book/download/route.test.ts
PASS apps/agents-server/src/utils/agentsTransfer/createAgentsExportZipStream.test.ts
PASS scripts/generate-packages/getPackagesMetadataForRollup.test.ts
PASS apps/agents-server/src/utils/internalS3/readInternalS3Directory.test.ts
PASS apps/agents-server/src/components/Header/buildAgentMenuStructure.test.tsx
PASS src/book-components/Chat/MarkdownContent/MarkdownContent.test.tsx
  ● Console

    console.error
      Attempted to synchronously unmount a root while React was already rendering. React cannot finish unmounting the root until the current render has completed, which may lead to a race condition.

    [0m [90m 377 |[39m             pendingToggleFallbackTimeoutIds[33m.[39mforEach((timeoutId) [33m=>[39m window[33m.[39mclearTimeout(timeoutId))[33m;[39m
     [90m 378 |[39m             pendingToggleFallbackTimeoutIds[33m.[39mclear()[33m;[39m
    [31m[1m>[22m[39m[90m 379 |[39m             rootsRef[33m.[39mcurrent[33m.[39mforEach((root) [33m=>[39m root[33m.[39munmount())[33m;[39m
     [90m     |[39m                                                     [31m[1m^[22m[39m
     [90m 380 |[39m             rootsRef[33m.[39mcurrent [33m=[39m [][33m;[39m
     [90m 381 |[39m         }[33m;[39m
     [90m 382 |[39m     }[33m,[39m [htmlContent[33m,[39m inlineReferences[33m,[39m resolvedTheme])[33m;[39m[0m

      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.unmount.ReactDOMRoot.unmount [as unmount] (node_modules/react-dom/cjs/react-dom-client.development.js:24773:21)
      at src/book-components/Chat/MarkdownContent/MarkdownContent.tsx:379:53
          at Array.forEach (<anonymous>)
      at src/book-components/Chat/MarkdownContent/MarkdownContent.tsx:379:30
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23965:13)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at commitHookEffectListUnmount (node_modules/react-dom/cjs/react-dom-client.development.js:11996:17)
      at commitHookPassiveUnmountEffects (node_modules/react-dom/cjs/react-dom-client.development.js:12043:11)
      at commitPassiveUnmountEffectsInsideOfDeletedTree_begin (node_modules/react-dom/cjs/react-dom-client.development.js:14269:13)
      at recursivelyTraversePassiveUnmountEffects (node_modules/react-dom/cjs/react-dom-client.development.js:14167:13)
      at commitPassiveUnmountOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14194:11)
      at flushPassiveEffects (node_modules/react-dom/cjs/react-dom-client.development.js:15736:9)
      at flushPendingEffects (node_modules/react-dom/cjs/react-dom-client.development.js:15702:14)
      at flushSpawnedWork (node_modules/react-dom/cjs/react-dom-client.development.js:15668:44)
      at commitRoot (node_modules/react-dom/cjs/react-dom-client.development.js:15403:9)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14526:15)
      at performSyncWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:16231:7)
      at flushSyncWorkAcrossRoots_impl (node_modules/react-dom/cjs/react-dom-client.development.js:16079:21)
      at flushSyncWork$1 (node_modules/react-dom/cjs/react-dom-client.development.js:14721:12)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.unmount.ReactDOMRoot.unmount [as unmount] (node_modules/react-dom/cjs/react-dom-client.development.js:24777:11)
      at Object.unmount (node_modules/@testing-library/react/dist/pure.js:163:12)
      at node_modules/@testing-library/react/dist/pure.js:307:12
      at node_modules/@testing-library/react/dist/act-compat.js:47:24
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at node_modules/@testing-library/react/dist/act-compat.js:46:25
      at node_modules/@testing-library/react/dist/pure.js:306:28
          at Array.forEach (<anonymous>)
      at cleanup (node_modules/@testing-library/react/dist/pure.js:302:22)
      at Object.<anonymous> (node_modules/@testing-library/react/dist/index.js:28:25)

PASS apps/agents-server/src/utils/chat/resolveCitationSourceLabel.test.ts
PASS src/book-components/Chat/AgentChat/AgentChat.test.tsx
  ● Console

    console.error
      `ReactDOMTestUtils.act` is deprecated in favor of `React.act`. Import `act` from `react` instead of `react-dom/test-utils`. See https://react.dev/warnings/react-dom-test-utils for more info.

    [0m [90m 67 |[39m         document[33m.[39mbody[33m.[39mappendChild(container)[33m;[39m
     [90m 68 |[39m
    [31m[1m>[22m[39m[90m 69 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m    |[39m                  [31m[1m^[22m[39m
     [90m 70 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
     [90m 71 |[39m             root[33m.[39mrender([33m<[39m[33mAgentChat[39m agent[33m=[39m{agent} layout[33m=[39m[32m"STANDALONE"[39m [33m/[39m[33m>[39m)[33m;[39m
     [90m 72 |[39m         })[33m;[39m[0m

      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:19:17)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:69:18)

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 69 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m 70 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
    [31m[1m>[22m[39m[90m 71 |[39m             root[33m.[39mrender([33m<[39m[33mAgentChat[39m agent[33m=[39m{agent} layout[33m=[39m[32m"STANDALONE"[39m [33m/[39m[33m>[39m)[33m;[39m
     [90m    |[39m                  [31m[1m^[22m[39m
     [90m 72 |[39m         })[33m;[39m
     [90m 73 |[39m
     [90m 74 |[39m         [36mconst[39m capturedProps [33m=[39m (globalThis [36mas[39m { __lastLlmChatProps[33m?[39m[33m:[39m [33mCapturedLlmChatProps[39m })[33m.[39m__lastLlmChatProps[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:71:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:69:18)

    console.warn
      Color.fromSafe error:
      Can not create a new Color instance from string "{2}".
      
      Returning default PROMPTBOOK_COLOR.

    [0m [90m 74 |[39m             [90m// <- Note: Can not use `assertsError(error)` here because it causes circular dependency[39m
     [90m 75 |[39m
    [31m[1m>[22m[39m[90m 76 |[39m             console[33m.[39mwarn(
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 77 |[39m                 spaceTrim(
     [90m 78 |[39m                     (block) [33m=>[39m [32m`[39m
     [90m 79 |[39m [32m                        Color.fromSafe error:[39m[0m

      at Function.fromSafe (src/utils/color/Color.ts:76:21)
      at AgentChat (src/book-components/Chat/AgentChat/AgentChat.tsx:37:30)
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23863:20)
      at renderWithHooks (node_modules/react-dom/cjs/react-dom-client.development.js:5529:22)
      at updateFunctionComponent (node_modules/react-dom/cjs/react-dom-client.development.js:8897:19)
      at beginWork (node_modules/react-dom/cjs/react-dom-client.development.js:10522:18)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at performUnitOfWork (node_modules/react-dom/cjs/react-dom-client.development.js:15140:22)
      at workLoopSync (node_modules/react-dom/cjs/react-dom-client.development.js:14956:41)
      at renderRootSync (node_modules/react-dom/cjs/react-dom-client.development.js:14936:11)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14419:13)
      at performWorkOnRootViaSchedulerTask (node_modules/react-dom/cjs/react-dom-client.development.js:16216:7)
      at flushActQueue (node_modules/react/cjs/react.development.js:566:34)
      at node_modules/react/cjs/react.development.js:823:21

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 86 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m 87 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
    [31m[1m>[22m[39m[90m 88 |[39m             root[33m.[39mrender([33m<[39m[33mAgentChat[39m agent[33m=[39m{agent} title[33m=[39m[32m"Custom Chat Title"[39m layout[33m=[39m[32m"STANDALONE"[39m [33m/[39m[33m>[39m)[33m;[39m
     [90m    |[39m                  [31m[1m^[22m[39m
     [90m 89 |[39m         })[33m;[39m
     [90m 90 |[39m
     [90m 91 |[39m         [36mconst[39m capturedProps [33m=[39m (globalThis [36mas[39m { __lastLlmChatProps[33m?[39m[33m:[39m [33mCapturedLlmChatProps[39m })[33m.[39m__lastLlmChatProps[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:88:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:86:18)

    console.warn
      Color.fromSafe error:
      Can not create a new Color instance from string "{2}".
      
      Returning default PROMPTBOOK_COLOR.

    [0m [90m 74 |[39m             [90m// <- Note: Can not use `assertsError(error)` here because it causes circular dependency[39m
     [90m 75 |[39m
    [31m[1m>[22m[39m[90m 76 |[39m             console[33m.[39mwarn(
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 77 |[39m                 spaceTrim(
     [90m 78 |[39m                     (block) [33m=>[39m [32m`[39m
     [90m 79 |[39m [32m                        Color.fromSafe error:[39m[0m

      at Function.fromSafe (src/utils/color/Color.ts:76:21)
      at AgentChat (src/book-components/Chat/AgentChat/AgentChat.tsx:37:30)
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23863:20)
      at renderWithHooks (node_modules/react-dom/cjs/react-dom-client.development.js:5529:22)
      at updateFunctionComponent (node_modules/react-dom/cjs/react-dom-client.development.js:8897:19)
      at beginWork (node_modules/react-dom/cjs/react-dom-client.development.js:10522:18)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at performUnitOfWork (node_modules/react-dom/cjs/react-dom-client.development.js:15140:22)
      at workLoopSync (node_modules/react-dom/cjs/react-dom-client.development.js:14956:41)
      at renderRootSync (node_modules/react-dom/cjs/react-dom-client.development.js:14936:11)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14419:13)
      at performWorkOnRootViaSchedulerTask (node_modules/react-dom/cjs/react-dom-client.development.js:16216:7)
      at flushActQueue (node_modules/react/cjs/react.development.js:566:34)
      at node_modules/react/cjs/react.development.js:823:21

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 100 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m 101 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
    [31m[1m>[22m[39m[90m 102 |[39m             root[33m.[39mrender([33m<[39m[33mAgentChat[39m agent[33m=[39m{agent} layout[33m=[39m[32m"STANDALONE"[39m [33m/[39m[33m>[39m)[33m;[39m
     [90m     |[39m                  [31m[1m^[22m[39m
     [90m 103 |[39m         })[33m;[39m
     [90m 104 |[39m
     [90m 105 |[39m         [36mconst[39m capturedProps [33m=[39m (globalThis [36mas[39m { __lastLlmChatProps[33m?[39m[33m:[39m [33mCapturedLlmChatProps[39m })[33m.[39m__lastLlmChatProps[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:102:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:100:18)

    console.warn
      Color.fromSafe error:
      Can not create a new Color instance from string "{2}".
      
      Returning default PROMPTBOOK_COLOR.

    [0m [90m 74 |[39m             [90m// <- Note: Can not use `assertsError(error)` here because it causes circular dependency[39m
     [90m 75 |[39m
    [31m[1m>[22m[39m[90m 76 |[39m             console[33m.[39mwarn(
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 77 |[39m                 spaceTrim(
     [90m 78 |[39m                     (block) [33m=>[39m [32m`[39m
     [90m 79 |[39m [32m                        Color.fromSafe error:[39m[0m

      at Function.fromSafe (src/utils/color/Color.ts:76:21)
      at AgentChat (src/book-components/Chat/AgentChat/AgentChat.tsx:37:30)
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23863:20)
      at renderWithHooks (node_modules/react-dom/cjs/react-dom-client.development.js:5529:22)
      at updateFunctionComponent (node_modules/react-dom/cjs/react-dom-client.development.js:8897:19)
      at beginWork (node_modules/react-dom/cjs/react-dom-client.development.js:10522:18)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at performUnitOfWork (node_modules/react-dom/cjs/react-dom-client.development.js:15140:22)
      at workLoopSync (node_modules/react-dom/cjs/react-dom-client.development.js:14956:41)
      at renderRootSync (node_modules/react-dom/cjs/react-dom-client.development.js:14936:11)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14419:13)
      at performWorkOnRootViaSchedulerTask (node_modules/react-dom/cjs/react-dom-client.development.js:16216:7)
      at flushActQueue (node_modules/react/cjs/react.development.js:566:34)
      at node_modules/react/cjs/react.development.js:823:21

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 131 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m 132 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
    [31m[1m>[22m[39m[90m 133 |[39m             root[33m.[39mrender([33m<[39m[33mAgentChat[39m agent[33m=[39m{agent} layout[33m=[39m[32m"STANDALONE"[39m [33m/[39m[33m>[39m)[33m;[39m
     [90m     |[39m                  [31m[1m^[22m[39m
     [90m 134 |[39m         })[33m;[39m
     [90m 135 |[39m
     [90m 136 |[39m         [36mconst[39m capturedProps [33m=[39m (globalThis [36mas[39m { __lastLlmChatProps[33m?[39m[33m:[39m [33mCapturedLlmChatProps[39m })[33m.[39m__lastLlmChatProps[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:133:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:131:18)

    console.warn
      Color.fromSafe error:
      Can not create a new Color instance from string "{2}".
      
      Returning default PROMPTBOOK_COLOR.

    [0m [90m 74 |[39m             [90m// <- Note: Can not use `assertsError(error)` here because it causes circular dependency[39m
     [90m 75 |[39m
    [31m[1m>[22m[39m[90m 76 |[39m             console[33m.[39mwarn(
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 77 |[39m                 spaceTrim(
     [90m 78 |[39m                     (block) [33m=>[39m [32m`[39m
     [90m 79 |[39m [32m                        Color.fromSafe error:[39m[0m

      at Function.fromSafe (src/utils/color/Color.ts:76:21)
      at AgentChat (src/book-components/Chat/AgentChat/AgentChat.tsx:37:30)
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23863:20)
      at renderWithHooks (node_modules/react-dom/cjs/react-dom-client.development.js:5529:22)
      at updateFunctionComponent (node_modules/react-dom/cjs/react-dom-client.development.js:8897:19)
      at beginWork (node_modules/react-dom/cjs/react-dom-client.development.js:10522:18)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at performUnitOfWork (node_modules/react-dom/cjs/react-dom-client.development.js:15140:22)
      at workLoopSync (node_modules/react-dom/cjs/react-dom-client.development.js:14956:41)
      at renderRootSync (node_modules/react-dom/cjs/react-dom-client.development.js:14936:11)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14419:13)
      at performWorkOnRootViaSchedulerTask (node_modules/react-dom/cjs/react-dom-client.development.js:16216:7)
      at flushActQueue (node_modules/react/cjs/react.development.js:566:34)
      at node_modules/react/cjs/react.development.js:823:21

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 155 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m 156 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
    [31m[1m>[22m[39m[90m 157 |[39m             root[33m.[39mrender(
     [90m     |[39m                  [31m[1m^[22m[39m
     [90m 158 |[39m                 [33m<[39m[33mAgentChat[39m
     [90m 159 |[39m                     agent[33m=[39m{agent}
     [90m 160 |[39m                     onChange[33m=[39m{mockOnChange}[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:157:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:155:18)

    console.warn
      Color.fromSafe error:
      Can not create a new Color instance from string "{2}".
      
      Returning default PROMPTBOOK_COLOR.

    [0m [90m 74 |[39m             [90m// <- Note: Can not use `assertsError(error)` here because it causes circular dependency[39m
     [90m 75 |[39m
    [31m[1m>[22m[39m[90m 76 |[39m             console[33m.[39mwarn(
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 77 |[39m                 spaceTrim(
     [90m 78 |[39m                     (block) [33m=>[39m [32m`[39m
     [90m 79 |[39m [32m                        Color.fromSafe error:[39m[0m

      at Function.fromSafe (src/utils/color/Color.ts:76:21)
      at AgentChat (src/book-components/Chat/AgentChat/AgentChat.tsx:37:30)
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23863:20)
      at renderWithHooks (node_modules/react-dom/cjs/react-dom-client.development.js:5529:22)
      at updateFunctionComponent (node_modules/react-dom/cjs/react-dom-client.development.js:8897:19)
      at beginWork (node_modules/react-dom/cjs/react-dom-client.development.js:10522:18)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at performUnitOfWork (node_modules/react-dom/cjs/react-dom-client.development.js:15140:22)
      at workLoopSync (node_modules/react-dom/cjs/react-dom-client.development.js:14956:41)
      at renderRootSync (node_modules/react-dom/cjs/react-dom-client.development.js:14936:11)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14419:13)
      at performWorkOnRootViaSchedulerTask (node_modules/react-dom/cjs/react-dom-client.development.js:16216:7)
      at flushActQueue (node_modules/react/cjs/react.development.js:566:34)
      at node_modules/react/cjs/react.development.js:823:21

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 182 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m 183 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
    [31m[1m>[22m[39m[90m 184 |[39m             root[33m.[39mrender([33m<[39m[33mAgentChat[39m agent[33m=[39m{agent} layout[33m=[39m[32m"STANDALONE"[39m [33m/[39m[33m>[39m)[33m;[39m
     [90m     |[39m                  [31m[1m^[22m[39m
     [90m 185 |[39m         })[33m;[39m
     [90m 186 |[39m
     [90m 187 |[39m         [36mconst[39m capturedProps [33m=[39m (globalThis [36mas[39m { __lastLlmChatProps[33m?[39m[33m:[39m [33mCapturedLlmChatProps[39m })[33m.[39m__lastLlmChatProps[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:184:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:182:18)

    console.warn
      Color.fromSafe error:
      Can not create a new Color instance from string "{2}".
      
      Returning default PROMPTBOOK_COLOR.

    [0m [90m 74 |[39m             [90m// <- Note: Can not use `assertsError(error)` here because it causes circular dependency[39m
     [90m 75 |[39m
    [31m[1m>[22m[39m[90m 76 |[39m             console[33m.[39mwarn(
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 77 |[39m                 spaceTrim(
     [90m 78 |[39m                     (block) [33m=>[39m [32m`[39m
     [90m 79 |[39m [32m                        Color.fromSafe error:[39m[0m

      at Function.fromSafe (src/utils/color/Color.ts:76:21)
      at AgentChat (src/book-components/Chat/AgentChat/AgentChat.tsx:37:30)
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23863:20)
      at renderWithHooks (node_modules/react-dom/cjs/react-dom-client.development.js:5529:22)
      at updateFunctionComponent (node_modules/react-dom/cjs/react-dom-client.development.js:8897:19)
      at beginWork (node_modules/react-dom/cjs/react-dom-client.development.js:10522:18)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at performUnitOfWork (node_modules/react-dom/cjs/react-dom-client.development.js:15140:22)
      at workLoopSync (node_modules/react-dom/cjs/react-dom-client.development.js:14956:41)
      at renderRootSync (node_modules/react-dom/cjs/react-dom-client.development.js:14936:11)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14419:13)
      at performWorkOnRootViaSchedulerTask (node_modules/react-dom/cjs/react-dom-client.development.js:16216:7)
      at flushActQueue (node_modules/react/cjs/react.development.js:566:34)
      at node_modules/react/cjs/react.development.js:823:21

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 208 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m 209 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
    [31m[1m>[22m[39m[90m 210 |[39m             root[33m.[39mrender([33m<[39m[33mAgentChat[39m agent[33m=[39m{minimalAgent} layout[33m=[39m[32m"STANDALONE"[39m [33m/[39m[33m>[39m)[33m;[39m
     [90m     |[39m                  [31m[1m^[22m[39m
     [90m 211 |[39m         })[33m;[39m
     [90m 212 |[39m
     [90m 213 |[39m         [36mconst[39m capturedProps [33m=[39m (globalThis [36mas[39m { __lastLlmChatProps[33m?[39m[33m:[39m [33mCapturedLlmChatProps[39m })[33m.[39m__lastLlmChatProps[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:210:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:208:18)

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 240 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m 241 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
    [31m[1m>[22m[39m[90m 242 |[39m             root[33m.[39mrender([33m<[39m[33mAgentChat[39m agent[33m=[39m{agent} persistenceKey[33m=[39m[32m"test-persistence-key"[39m layout[33m=[39m[32m"STANDALONE"[39m [33m/[39m[33m>[39m)[33m;[39m
     [90m     |[39m                  [31m[1m^[22m[39m
     [90m 243 |[39m         })[33m;[39m
     [90m 244 |[39m
     [90m 245 |[39m         [36mconst[39m capturedProps [33m=[39m (globalThis [36mas[39m { __lastLlmChatProps[33m?[39m[33m:[39m [33mCapturedLlmChatProps[39m })[33m.[39m__lastLlmChatProps[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:242:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:240:18)

    console.warn
      Color.fromSafe error:
      Can not create a new Color instance from string "{2}".
      
      Returning default PROMPTBOOK_COLOR.

    [0m [90m 74 |[39m             [90m// <- Note: Can not use `assertsError(error)` here because it causes circular dependency[39m
     [90m 75 |[39m
    [31m[1m>[22m[39m[90m 76 |[39m             console[33m.[39mwarn(
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 77 |[39m                 spaceTrim(
     [90m 78 |[39m                     (block) [33m=>[39m [32m`[39m
     [90m 79 |[39m [32m                        Color.fromSafe error:[39m[0m

      at Function.fromSafe (src/utils/color/Color.ts:76:21)
      at AgentChat (src/book-components/Chat/AgentChat/AgentChat.tsx:37:30)
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23863:20)
      at renderWithHooks (node_modules/react-dom/cjs/react-dom-client.development.js:5529:22)
      at updateFunctionComponent (node_modules/react-dom/cjs/react-dom-client.development.js:8897:19)
      at beginWork (node_modules/react-dom/cjs/react-dom-client.development.js:10522:18)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at performUnitOfWork (node_modules/react-dom/cjs/react-dom-client.development.js:15140:22)
      at workLoopSync (node_modules/react-dom/cjs/react-dom-client.development.js:14956:41)
      at renderRootSync (node_modules/react-dom/cjs/react-dom-client.development.js:14936:11)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14419:13)
      at performWorkOnRootViaSchedulerTask (node_modules/react-dom/cjs/react-dom-client.development.js:16216:7)
      at flushActQueue (node_modules/react/cjs/react.development.js:566:34)
      at node_modules/react/cjs/react.development.js:823:21

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 254 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m 255 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
    [31m[1m>[22m[39m[90m 256 |[39m             root[33m.[39mrender([33m<[39m[33mAgentChat[39m agent[33m=[39m{agent} layout[33m=[39m[32m"STANDALONE"[39m [33m/[39m[33m>[39m)[33m;[39m
     [90m     |[39m                  [31m[1m^[22m[39m
     [90m 257 |[39m         })[33m;[39m
     [90m 258 |[39m
     [90m 259 |[39m         [36mconst[39m capturedProps [33m=[39m (globalThis [36mas[39m { __lastLlmChatProps[33m?[39m[33m:[39m [33mCapturedLlmChatProps[39m })[33m.[39m__lastLlmChatProps[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:256:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:254:18)

    console.warn
      Color.fromSafe error:
      Can not create a new Color instance from string "{2}".
      
      Returning default PROMPTBOOK_COLOR.

    [0m [90m 74 |[39m             [90m// <- Note: Can not use `assertsError(error)` here because it causes circular dependency[39m
     [90m 75 |[39m
    [31m[1m>[22m[39m[90m 76 |[39m             console[33m.[39mwarn(
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 77 |[39m                 spaceTrim(
     [90m 78 |[39m                     (block) [33m=>[39m [32m`[39m
     [90m 79 |[39m [32m                        Color.fromSafe error:[39m[0m

      at Function.fromSafe (src/utils/color/Color.ts:76:21)
      at AgentChat (src/book-components/Chat/AgentChat/AgentChat.tsx:37:30)
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23863:20)
      at renderWithHooks (node_modules/react-dom/cjs/react-dom-client.development.js:5529:22)
      at updateFunctionComponent (node_modules/react-dom/cjs/react-dom-client.development.js:8897:19)
      at beginWork (node_modules/react-dom/cjs/react-dom-client.development.js:10522:18)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at performUnitOfWork (node_modules/react-dom/cjs/react-dom-client.development.js:15140:22)
      at workLoopSync (node_modules/react-dom/cjs/react-dom-client.development.js:14956:41)
      at renderRootSync (node_modules/react-dom/cjs/react-dom-client.development.js:14936:11)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14419:13)
      at performWorkOnRootViaSchedulerTask (node_modules/react-dom/cjs/react-dom-client.development.js:16216:7)
      at flushActQueue (node_modules/react/cjs/react.development.js:566:34)
      at node_modules/react/cjs/react.development.js:823:21

PASS src/scripting/_test/script-execution-tools.test.ts
  ● Console

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const thing = "apple";
      (async ()=>{ return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:
      bpple

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const thing = "a cup of coffee";
      (async ()=>{ return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      b cup of coffee

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

PASS apps/agents-server/src/database/reinstateDefaultAgents.test.ts
PASS src/conversion/parsePipeline.test.ts
  ● Console

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at src/conversion/parsePipeline.test.ts:19:56

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at src/conversion/parsePipeline.test.ts:19:56

PASS apps/agents-server/src/utils/customDomainRouting.test.ts
  ● Console

    console.log
      importAgent "https://core-test.ptbk.io/agents/parent-agent"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

PASS apps/agents-server/src/components/AgentProfile/AgentProfile.test.tsx
PASS apps/agents-server/src/instrumentation.test.ts
PASS apps/agents-server/src/utils/importAgent.test.ts
  ● Console

    console.log
      importAgent "https://core-test.ptbk.io/agents/test-0"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/foobarhululu"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

PASS apps/agents-server/src/utils/agentReferenceResolver/resolveTeamCapabilitiesFromAgentSource.test.ts
PASS apps/agents-server/src/utils/agentGoalChat/enqueueAgentGoalChatTurn.test.ts
PASS src/scrapers/website/utils/createShowdownConverter.test.ts
PASS src/conversion/validation/pipelineStringToJson-parseErrors.test.ts
PASS src/cli/cli-commands/coder/boilerplateTemplates.test.ts
PASS apps/agents-server/src/utils/chatExport/sanitizeChatPdfExportHtml.test.ts
PASS apps/agents-server/src/components/AgentNaming/translateLegacyAgentText.test.ts
PASS apps/agents-server/src/database/sqlite/standaloneServerRegistryStore.test.ts
PASS src/book-components/Chat/utils/renderMarkdown.test.ts
PASS src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.test.ts
PASS apps/agents-server/src/utils/agentReferenceResolver/bookScopedAgentReferences.test.ts
PASS apps/agents-server/src/utils/agentRouting/resolveAgentRouteTarget.test.ts
PASS apps/agents-server/src/search/createDefaultServerSearchProviders/createDocumentationSearchProvider.test.ts
PASS apps/agents-server/src/utils/externalChatRunner/externalChatMessageBook.test.ts
PASS apps/agents-server/src/utils/errorReporting/registerServerErrorSentryLogging.test.ts
PASS apps/agents-server/src/utils/resolveAppendOnlySelfLearningAgentSource.test.ts
  ● Console

    console.log
      importAgent "https://core-test.ptbk.io/agents/adam"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://local.example/agents/parent-1"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

PASS scripts/run-agent-chat/executeAgentChatTurn.test.ts
PASS apps/agents-server/src/components/AgentProfile/AgentCapabilityChips.test.tsx
PASS apps/agents-server/src/components/CloudflareDnsWizard/CloudflareDnsWizard.test.tsx
PASS apps/agents-server/src/app/admin/backup/BackupClient.test.tsx
PASS src/conversion/validation/validatePipeline.test.ts
  ● Console

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at src/conversion/validation/validatePipeline.test.ts:24:67
      at Object.<anonymous> (src/conversion/validation/validatePipeline.test.ts:20:13)

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at src/conversion/validation/validatePipeline.test.ts:24:67
      at Object.<anonymous> (src/conversion/validation/validatePipeline.test.ts:20:13)

PASS scripts/find-refactor-candidates/findRefactorCandidatesInProject.test.ts
PASS apps/agents-server/src/utils/getAdminChatTasksResponse/getAdminChatTasks.test.ts
PASS apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts
  ● Console

    console.log
      importAgent "https://core-test.ptbk.io/agents/test-0"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/adam"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://local.example/agents/adam-1"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://local.example/agents/adam-1"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://local.example/agents/adam-1"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/effective-parent"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/effective-parent"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://local.example/agents/parent-1"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://federated.example/agents/remote-parent"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/adam"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/adam"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/adam"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.warn
      [importAgentWithFallback] Falling back for "https://core-test.ptbk.io/agents/adam" after 3 attempts: NotFoundError: Failed to import agent from "https://core-test.ptbk.io/agents/adam"
      
      Agent not found
          at deserializeError (C:\Users\me\work\ai\promptbook\src\errors\utils\deserializeError.ts:32:31)
          at C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\importAgent.ts:188:45
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at importAgent (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\importAgent.ts:230:16)
          at retryWithBackoff (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\retryWithBackoff.ts:181:27)
          at C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\importAgentWithFallback.ts:112:28
          at importAgentCorpus (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\resolveInheritedAgentSource.ts:468:33)
          at resolveParentAgentContext (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\resolveInheritedAgentSource.ts:507:11)
          at resolveInheritedAgentSource (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\resolveInheritedAgentSource.ts:834:27)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveInheritedAgentSource.test.ts:409:37) {
        id: undefined
      }

    [0m [90m 124 |[39m             [36mreturn[39m result[33m.[39mvalue[33m;[39m
     [90m 125 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 126 |[39m             console[33m.[39mwarn(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 127 |[39m                 [32m`[importAgentWithFallback] Falling back for "${agentUrl}" after ${configuration.maxAttempts} attempts:`[39m[33m,[39m
     [90m 128 |[39m                 error[33m,[39m
     [90m 129 |[39m             )[33m;[39m[0m

      at apps/agents-server/src/utils/importAgentWithFallback.ts:126:21
      at importAgentCorpus (src/book-2.0/agent-source/resolveInheritedAgentSource.ts:468:33)
      at resolveParentAgentContext (src/book-2.0/agent-source/resolveInheritedAgentSource.ts:507:11)
      at resolveInheritedAgentSource (src/book-2.0/agent-source/resolveInheritedAgentSource.ts:834:27)
      at Object.<anonymous> (apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts:409:37)

    console.log
      importAgent "https://federated.example/agents/missing-source"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://federated.example/agents/missing-source"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://federated.example/agents/missing-source"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.warn
      [importAgentWithFallback] Falling back for "https://federated.example/agents/missing-source" after 3 attempts: NotFoundError: Failed to import agent from "https://federated.example/agents/missing-source"
      
      Remote import unavailable
          at deserializeError (C:\Users\me\work\ai\promptbook\src\errors\utils\deserializeError.ts:32:31)
          at C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\importAgent.ts:188:45
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at importAgent (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\importAgent.ts:230:16)
          at retryWithBackoff (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\retryWithBackoff.ts:181:27)
          at C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\importAgentWithFallback.ts:112:28
          at importAgentCorpus (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\resolveInheritedAgentSource.ts:468:33)
          at resolveImportCommitmentLine (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\resolveInheritedAgentSource.ts:581:39)
          at resolveAgentSourceBuild (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\resolveInheritedAgentSource.ts:695:48)
          at resolveInheritedAgentSource (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\resolveInheritedAgentSource.ts:835:27)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveInheritedAgentSource.test.ts:499:37) {
        id: undefined
      }

    [0m [90m 124 |[39m             [36mreturn[39m result[33m.[39mvalue[33m;[39m
     [90m 125 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 126 |[39m             console[33m.[39mwarn(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 127 |[39m                 [32m`[importAgentWithFallback] Falling back for "${agentUrl}" after ${configuration.maxAttempts} attempts:`[39m[33m,[39m
     [90m 128 |[39m                 error[33m,[39m
     [90m 129 |[39m             )[33m;[39m[0m

      at apps/agents-server/src/utils/importAgentWithFallback.ts:126:21
      at importAgentCorpus (src/book-2.0/agent-source/resolveInheritedAgentSource.ts:468:33)
      at resolveImportCommitmentLine (src/book-2.0/agent-source/resolveInheritedAgentSource.ts:581:39)
      at resolveAgentSourceBuild (src/book-2.0/agent-source/resolveInheritedAgentSource.ts:695:48)
      at resolveInheritedAgentSource (src/book-2.0/agent-source/resolveInheritedAgentSource.ts:835:27)
      at Object.<anonymous> (apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts:499:37)

PASS scripts/run-codex-prompts/main/runCodexPrompts.test.ts
  ● Console

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:967:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:967:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:967:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [90mPulling latest changes before the next prompt...[39m

      at pullLatestChangesIfEnabled (scripts/run-codex-prompts/main/runCodexPrompts.ts:448:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [90mPulling latest changes before the next prompt...[39m

      at pullLatestChangesIfEnabled (scripts/run-codex-prompts/main/runCodexPrompts.ts:448:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:967:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:967:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:967:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:967:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:967:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:967:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mRun limit reached after 2 prompt runs.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:967:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:135:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [32mEnd requested after 1 prompt run.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:967:17)

PASS src/book-components/Chat/save/pdf/pdfSaveFormatDefinition.test.ts
PASS src/llm-providers/agent/RemoteAgent.test.ts
  ● Console

    console.warn
      You have not provided any `LlmExecutionTools`
      This means that you won't be able to execute any prompts that require large language models like GPT-4 or Anthropic's Claude.
      
      Technically, it's not an error, but it's probably not what you want because it does not make sense to use Promptbook without language models.

    [0m [90m 37 |[39m
     [90m 38 |[39m         [90m// TODO: [🟥] Detect browser / node and make it colorful[39m
    [31m[1m>[22m[39m[90m 39 |[39m         console[33m.[39mwarn(warningMessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 40 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 41 |[39m
     [90m 42 |[39m         [90m/*[39m[0m

      at joinLlmExecutionTools (src/llm-providers/_multiple/joinLlmExecutionTools.ts:39:17)
      at getSingleLlmExecutionTools (src/llm-providers/_multiple/getSingleLlmExecutionTools.ts:18:36)
      at new Agent (src/llm-providers/agent/Agent.ts:164:49)
      at new RemoteAgent (src/llm-providers/agent/RemoteAgent.ts:306:9)
      at Function.connect (src/llm-providers/agent/RemoteAgent.ts:245:29)
      at Object.<anonymous> (src/llm-providers/agent/RemoteAgent.test.ts:128:33)

    console.warn
      You have not provided any `LlmExecutionTools`
      This means that you won't be able to execute any prompts that require large language models like GPT-4 or Anthropic's Claude.
      
      Technically, it's not an error, but it's probably not what you want because it does not make sense to use Promptbook without language models.

    [0m [90m 37 |[39m
     [90m 38 |[39m         [90m// TODO: [🟥] Detect browser / node and make it colorful[39m
    [31m[1m>[22m[39m[90m 39 |[39m         console[33m.[39mwarn(warningMessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 40 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 41 |[39m
     [90m 42 |[39m         [90m/*[39m[0m

      at joinLlmExecutionTools (src/llm-providers/_multiple/joinLlmExecutionTools.ts:39:17)
      at getSingleLlmExecutionTools (src/llm-providers/_multiple/getSingleLlmExecutionTools.ts:18:36)
      at new Agent (src/llm-providers/agent/Agent.ts:164:49)
      at new RemoteAgent (src/llm-providers/agent/RemoteAgent.ts:306:9)
      at Function.connect (src/llm-providers/agent/RemoteAgent.ts:245:29)
      at Object.<anonymous> (src/llm-providers/agent/RemoteAgent.test.ts:173:33)

PASS src/avatars/renderAvatarVisualTerminalText.test.ts
PASS apps/agents-server/src/components/ApplicationErrorPage/ApplicationErrorPage.test.tsx
PASS apps/agents-server/src/components/NewAgentDialog/createNewAgentWizardSource.test.ts
PASS apps/agents-server/src/components/Header/buildHeaderSystemMenuItems.test.ts
PASS apps/agents-server/src/utils/agentProjects/resolveAgentProjectInfo.test.ts
PASS apps/agents-server/src/app/admin/messages/MessageTableCells.test.tsx
PASS apps/agents-server/src/utils/thinkingMessages.test.ts
PASS src/scrapers/_common/utils/promptbookFetch.test.ts
PASS src/scripting/_test/custom-function.test.ts
  ● Console

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      
      // The script:
      const resultString = "Matthew the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Matthew the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      
      // The script:
      const resultString = "Mark the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Mark the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      
      // The script:
      const resultString = "Luke the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Luke the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      
      // The script:
      const resultString = "John the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello John the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

PASS src/scripting/_test/custom-function-with-dependencies.test.ts
  ● Console

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      const withStatistics = customFunctions.withStatistics;
      
      // The script:
      const resultString = "Matthew the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Matthew the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      const withStatistics = customFunctions.withStatistics;
      
      // The script:
      const resultString = "Hello Matthew the Evangelist";
      (async ()=>{ return withStatistics(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Matthew the Evangelist (28 characters, 4 words)

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      const withStatistics = customFunctions.withStatistics;
      
      // The script:
      const resultString = "Mark the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Mark the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      const withStatistics = customFunctions.withStatistics;
      
      // The script:
      const resultString = "Hello Mark the Evangelist";
      (async ()=>{ return withStatistics(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Mark the Evangelist (25 characters, 4 words)

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      const withStatistics = customFunctions.withStatistics;
      
      // The script:
      const resultString = "Luke the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Luke the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      const withStatistics = customFunctions.withStatistics;
      
      // The script:
      const resultString = "Hello Luke the Evangelist";
      (async ()=>{ return withStatistics(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Luke the Evangelist (25 characters, 4 words)

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      const withStatistics = customFunctions.withStatistics;
      
      // The script:
      const resultString = "John the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello John the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      const withStatistics = customFunctions.withStatistics;
      
      // The script:
      const resultString = "Hello John the Evangelist";
      (async ()=>{ return withStatistics(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello John the Evangelist (25 characters, 4 words)

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

PASS apps/agents-server/src/utils/agentProjects/agentProjectAccess.test.ts
PASS src/scrapers/document-legacy/LegacyDocumentScraper.test.ts
PASS apps/agents-server/src/components/UsersList/PasswordGeneratorDialog.test.tsx
PASS src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.import.test.ts
  ● Console

    console.warn
      Failed to import file ./local/path/data.json: Error: Invalid file URL or path: ./local/path/data.json
          at readImportedFile (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments\augmentAgentModelRequirementsFromSource.ts:135:11)
          at createImportedFileSystemMessage (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments\augmentAgentModelRequirementsFromSource.ts:91:41)
          at importReferencedFiles (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments\augmentAgentModelRequirementsFromSource.ts:70:37)
          at augmentAgentModelRequirementsFromSource (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments\augmentAgentModelRequirementsFromSource.ts:46:20)
          at createAgentModelRequirementsWithCommitments (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.ts:35:20)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.import.test.ts:28:24)

    [0m [90m 71 |[39m             requirements [33m=[39m appendSystemMessageSection(requirements[33m,[39m importedContent)[33m;[39m
     [90m 72 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 73 |[39m             console[33m.[39mwarn([32m`Failed to import file ${fileUrl}:`[39m[33m,[39m error)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 74 |[39m         }
     [90m 75 |[39m     }
     [90m 76 |[39m[0m

      at importReferencedFiles (src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/augmentAgentModelRequirementsFromSource.ts:73:21)
      at augmentAgentModelRequirementsFromSource (src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/augmentAgentModelRequirementsFromSource.ts:46:20)
      at createAgentModelRequirementsWithCommitments (src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.ts:35:20)
      at Object.<anonymous> (src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.import.test.ts:28:24)

PASS src/llm-providers/agent/Agent.test.ts
PASS apps/agents-server/src/utils/userChat/expiredRunningUserChatJobDiagnostics.test.ts
PASS scripts/run-agent-chat/runAgentChat.test.ts
PASS apps/agents-server/src/utils/serverManagement/standaloneVpsServerMetadata.test.ts
PASS src/book-components/Chat/Chat/TeamToolCallModalContent.test.tsx
PASS apps/agents-server/src/utils/metadataConfigurationTransfer.test.ts
PASS apps/agents-server/src/utils/userChat/createUserChatSummary.agentGoalChat.test.ts
PASS apps/agents-server/src/components/Homepage/ExternalAgentsSectionClient.test.tsx
PASS src/llm-providers/agent/AgentLlmExecutionTools.test.ts
PASS src/book-components/Chat/hooks/useResolvedCitationLabel.test.tsx
PASS src/commands/FORMFACTOR/formfactorCommand.test.ts
PASS scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
PASS apps/agents-server/src/utils/userChat/listUserChats.test.ts
PASS apps/agents-server/src/components/DnsRecordsInstructions/DnsRecordsInstructions.test.tsx
PASS src/scripting/javascript/JavascriptEvalExecutionTools.test.ts
  ● Console

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const get_user_location = customFunctions.get_user_location;
      const calendar_list_events = customFunctions.calendar_list_events;
      const calendar_get_event = customFunctions.calendar_get_event;
      const calendar_create_event = customFunctions.calendar_create_event;
      const calendar_update_event = customFunctions.calendar_update_event;
      const calendar_delete_event = customFunctions.calendar_delete_event;
      const calendar_invite_guests = customFunctions.calendar_invite_guests;
      const open_popup = customFunctions.open_popup;
      const turn_privacy_on = customFunctions.turn_privacy_on;
      const project_list_files = customFunctions.project_list_files;
      const project_read_file = customFunctions.project_read_file;
      const project_upsert_file = customFunctions.project_upsert_file;
      const project_delete_file = customFunctions.project_delete_file;
      const project_create_branch = customFunctions.project_create_branch;
      const project_create_pull_request = customFunctions.project_create_pull_request;
      
      // The script:
      const animal = "cat";
      (async ()=>{ return animal })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:
      cat

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const get_user_location = customFunctions.get_user_location;
      const calendar_list_events = customFunctions.calendar_list_events;
      const calendar_get_event = customFunctions.calendar_get_event;
      const calendar_create_event = customFunctions.calendar_create_event;
      const calendar_update_event = customFunctions.calendar_update_event;
      const calendar_delete_event = customFunctions.calendar_delete_event;
      const calendar_invite_guests = customFunctions.calendar_invite_guests;
      const open_popup = customFunctions.open_popup;
      const turn_privacy_on = customFunctions.turn_privacy_on;
      const project_list_files = customFunctions.project_list_files;
      const project_read_file = customFunctions.project_read_file;
      const project_upsert_file = customFunctions.project_upsert_file;
      const project_delete_file = customFunctions.project_delete_file;
      const project_create_branch = customFunctions.project_create_branch;
      const project_create_pull_request = customFunctions.project_create_pull_request;
      
      // The script:
      const animal = "cat";
      (async ()=>{ return animal })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:
      cat

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const get_user_location = customFunctions.get_user_location;
      const calendar_list_events = customFunctions.calendar_list_events;
      const calendar_get_event = customFunctions.calendar_get_event;
      const calendar_create_event = customFunctions.calendar_create_event;
      const calendar_update_event = customFunctions.calendar_update_event;
      const calendar_delete_event = customFunctions.calendar_delete_event;
      const calendar_invite_guests = customFunctions.calendar_invite_guests;
      const open_popup = customFunctions.open_popup;
      const turn_privacy_on = customFunctions.turn_privacy_on;
      const project_list_files = customFunctions.project_list_files;
      const project_read_file = customFunctions.project_read_file;
      const project_upsert_file = customFunctions.project_upsert_file;
      const project_delete_file = customFunctions.project_delete_file;
      const project_create_branch = customFunctions.project_create_branch;
      const project_create_pull_request = customFunctions.project_create_pull_request;
      
      // The script:
      const animal = "cat";
      (async ()=>{ return animal.split('').reverse().join('-') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:
      t-a-c

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const get_user_location = customFunctions.get_user_location;
      const calendar_list_events = customFunctions.calendar_list_events;
      const calendar_get_event = customFunctions.calendar_get_event;
      const calendar_create_event = customFunctions.calendar_create_event;
      const calendar_update_event = customFunctions.calendar_update_event;
      const calendar_delete_event = customFunctions.calendar_delete_event;
      const calendar_invite_guests = customFunctions.calendar_invite_guests;
      const open_popup = customFunctions.open_popup;
      const turn_privacy_on = customFunctions.turn_privacy_on;
      const project_list_files = customFunctions.project_list_files;
      const project_read_file = customFunctions.project_read_file;
      const project_upsert_file = customFunctions.project_upsert_file;
      const project_delete_file = customFunctions.project_delete_file;
      const project_create_branch = customFunctions.project_create_branch;
      const project_create_pull_request = customFunctions.project_create_pull_request;
      
      // The script:
      const animal = "\"cat\"";
      (async ()=>{ return removeQuotes(animal) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:
      cat

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const get_user_location = customFunctions.get_user_location;
      const calendar_list_events = customFunctions.calendar_list_events;
      const calendar_get_event = customFunctions.calendar_get_event;
      const calendar_create_event = customFunctions.calendar_create_event;
      const calendar_update_event = customFunctions.calendar_update_event;
      const calendar_delete_event = customFunctions.calendar_delete_event;
      const calendar_invite_guests = customFunctions.calendar_invite_guests;
      const open_popup = customFunctions.open_popup;
      const turn_privacy_on = customFunctions.turn_privacy_on;
      const project_list_files = customFunctions.project_list_files;
      const project_read_file = customFunctions.project_read_file;
      const project_upsert_file = customFunctions.project_upsert_file;
      const project_delete_file = customFunctions.project_delete_file;
      const project_create_branch = customFunctions.project_create_branch;
      const project_create_pull_request = customFunctions.project_create_pull_request;
      
      // The script:
      const animal = "The animal is: \"dog\"";
      (async ()=>{ return unwrapResult(animal) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:
      dog

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const get_user_location = customFunctions.get_user_location;
      const calendar_list_events = customFunctions.calendar_list_events;
      const calendar_get_event = customFunctions.calendar_get_event;
      const calendar_create_event = customFunctions.calendar_create_event;
      const calendar_update_event = customFunctions.calendar_update_event;
      const calendar_delete_event = customFunctions.calendar_delete_event;
      const calendar_invite_guests = customFunctions.calendar_invite_guests;
      const open_popup = customFunctions.open_popup;
      const turn_privacy_on = customFunctions.turn_privacy_on;
      const project_list_files = customFunctions.project_list_files;
      const project_read_file = customFunctions.project_read_file;
      const project_upsert_file = customFunctions.project_upsert_file;
      const project_delete_file = customFunctions.project_delete_file;
      const project_create_branch = customFunctions.project_create_branch;
      const project_create_pull_request = customFunctions.project_create_pull_request;
      
      // The script:
      
      (async ()=>{ return open_popup({ url: "https://ptbk.io" }) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.log
      !!!! [Tool] open_popup called { args: { url: 'https://ptbk.io' } }

      at open_popup (src/commitments/USE_POPUP/USE_POPUP.ts:143:25)

    console.info
      🚀 Script evaluated successfully, result:   
      Popup window with URL "https://ptbk.io" was requested.
      
      Note: The agent is currently running on the server, so the popup cannot be opened automatically.
      The user can open it manually from the tool call details in the chat.

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      const get_user_location = customFunctions.get_user_location;
                      const calendar_list_events = customFunctions.calendar_list_events;
                      const calendar_get_event = customFunctions.calendar_get_event;
                      const calendar_create_event = customFunctions.calendar_create_event;
                      const calendar_update_event = customFunctions.calendar_update_event;
                      const calendar_delete_event = customFunctions.calendar_delete_event;
                      const calendar_invite_guests = customFunctions.calendar_invite_guests;
                      const open_popup = customFunctions.open_popup;
                      const turn_privacy_on = customFunctions.turn_privacy_on;
                      const project_list_files = customFunctions.project_list_files;
                      const project_read_file = customFunctions.project_read_file;
                      const project_upsert_file = customFunctions.project_upsert_file;
                      const project_delete_file = customFunctions.project_delete_file;
                      const project_create_branch = customFunctions.project_create_branch;
                      const project_create_pull_request = customFunctions.project_create_pull_request;
      
                      // The script:
                      const animal = "cat";
                      const sound = "meow";
                      (async ()=>{ function makeSentence(animal, sound) {
          return animal + ' makes ' + sound + '.';
      }
      return makeSentence(animal, sound); })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      cat makes meow.

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

PASS src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.unknown.test.ts
PASS apps/agents-server/src/components/AgentProjects/useAgentProjectMarkdownReferences.test.ts
PASS apps/agents-server/src/components/ChatEnterBehavior/ChatEnterBehaviorPreferencesProvider.test.tsx
PASS src/dialogs/user-interface-execution-tools.test.ts
  ● Console

    console.info
      📖 Thing
      👤 Answer to question "Thing: What is your favorite apple to buy?" is not apple but Pear.

      at CallbackInterfaceTools.promptDialog (src/dialogs/callback/CallbackInterfaceTools.ts:22:21)

    console.info
      📖 Thing
      👤 Answer to question "Thing: What is your favorite a cup of coffee to buy?" is not a cup of coffee but Pear.

      at CallbackInterfaceTools.promptDialog (src/dialogs/callback/CallbackInterfaceTools.ts:22:21)

PASS apps/agents-server/src/utils/transpilers/createTranspiledAgentExportZipBuffer.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.blocks.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirements.deduplication.test.ts
PASS src/book-2.0/agent-source/communication-samples.test.ts
PASS apps/agents-server/src/utils/externalChatRunner/ensureExternalAgentRepository.test.ts
PASS apps/agents-server/src/utils/agentProjects/resolveAgentProjectProfile.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/chat/OptimisticAgentChatRouteLoading.test.tsx
PASS apps/agents-server/src/utils/chat/messageSuffix.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts
  ● Console

    console.warn
      Failed to resolve commitment references for FROM, falling back to safe defaults: Error: Unable to resolve agent reference "foo"
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts:32:23)
          at C:\Users\me\work\ai\promptbook\node_modules\jest-mock\build\index.js:397:39
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\node_modules\jest-mock\build\index.js:404:13)
          at Object.mockConstructor [as resolveCommitmentContent] (C:\Users\me\work\ai\promptbook\node_modules\jest-mock\build\index.js:148:19)
          at resolveCommitmentContent (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments\applyCommitmentsToAgentModelRequirements.ts:68:45)
          at applyCommitmentsToAgentModelRequirements (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments\applyCommitmentsToAgentModelRequirements.ts:37:41)
          at createAgentModelRequirementsWithCommitments (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.ts:34:66)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts:44:84)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 68 |[39m         [36mreturn[39m [36mawait[39m agentReferenceResolver[33m.[39mresolveCommitmentContent(commitment[33m.[39mtype[33m,[39m commitment[33m.[39mcontent)[33m;[39m
     [90m 69 |[39m     } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 70 |[39m         console[33m.[39mwarn(
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 71 |[39m             [32m`Failed to resolve commitment references for ${commitment.type}, falling back to safe defaults:`[39m[33m,[39m
     [90m 72 |[39m             error[33m,[39m
     [90m 73 |[39m         )[33m;[39m[0m

      at resolveCommitmentContent (src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/applyCommitmentsToAgentModelRequirements.ts:70:17)
      at applyCommitmentsToAgentModelRequirements (src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/applyCommitmentsToAgentModelRequirements.ts:37:35)
      at createAgentModelRequirementsWithCommitments (src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.ts:34:20)
      at Object.<anonymous> (src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts:44:35)

PASS src/collection/pipeline-collection/pipelineCollectionToJson.test.ts
PASS src/book-2.0/agent-source/parseAgentSource.test.ts
PASS apps/agents-server/src/utils/agentProjects/listAllAgentProjectSummaries.test.ts
PASS src/executables/browsers/locateBrowser.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.closed.test.ts
PASS src/llm-providers/agent/self-learning/SelfLearningManager.test.ts
  ● Console

    console.info
      [46m[Self-learning][49m[36m Sampling[39m

      at SelfLearningManager.appendSample (src/llm-providers/agent/self-learning/SelfLearningManager.ts:129:17)

    console.info
      [46m[Self-learning][49m[36m Sampling[39m

      at SelfLearningManager.appendSample (src/llm-providers/agent/self-learning/SelfLearningManager.ts:129:17)

    console.info
      [46m[Self-learning][49m[36m Teacher[39m

      at SelfLearningManager.callTeacher (src/llm-providers/agent/self-learning/SelfLearningManager.ts:173:17)

    console.log
      !!!! teacherResult {
        content: '```book\nINITIAL MESSAGE Hello! I am your helpful assistant.\n```',
        modelName: 'teacher-model',
        timing: {
          start: '2026-02-24T12:00:00.000Z',
          complete: '2026-02-24T12:00:01.000Z'
        },
        usage: {
          price: { value: 0, isUncertain: true },
          duration: { value: 0, isUncertain: true },
          input: {
            tokensCount: [Object],
            charactersCount: [Object],
            wordsCount: [Object],
            sentencesCount: [Object],
            linesCount: [Object],
            paragraphsCount: [Object],
            pagesCount: [Object]
          },
          output: {
            tokensCount: [Object],
            charactersCount: [Object],
            wordsCount: [Object],
            sentencesCount: [Object],
            linesCount: [Object],
            paragraphsCount: [Object],
            pagesCount: [Object]
          }
        },
        rawPromptContent: 'You are a teacher agent helping another agent to learn from its interactions.\n' +
          '\n' +
          'Here is your current client which you are teaching:\n' +
          '\n' +
          '```book\n' +
          '# My Agent\n' +
          '\n' +
          'PERSONA A helpful assistant.\n' +
          '\n' +
          '            USER MESSAGE\n' +
          '            Hello\n' +
          '            \n' +
          'INTERNAL MESSAGE\n' +
          '{\n' +
          '    "kind": "MODEL_REQUEST",\n' +
          '    "rawPromptContent": "Hello",\n' +
          '    "rawRequest": null\n' +
          '}\n' +
          '\n' +
          'INTERNAL MESSAGE\n' +
          '{\n' +
          '    "kind": "MODEL_RESPONSE",\n' +
          '    "rawResponse": {},\n' +
          '    "usage": {\n' +
          '        "price": {\n' +
          '            "value": 0,\n' +
          '            "isUncertain": true\n' +
          '        },\n' +
          '        "duration": {\n' +
          '            "value": 0,\n' +
          '            "isUncertain": true\n' +
          '        },\n' +
          '        "input": {\n' +
          '            "tokensCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "charactersCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "wordsCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "sentencesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "linesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "paragraphsCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "pagesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            }\n' +
          '        },\n' +
          '        "output": {\n' +
          '            "tokensCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "charactersCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "wordsCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "sentencesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "linesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "paragraphsCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "pagesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            }\n' +
          '        }\n' +
          '    },\n' +
          '    "timing": {\n' +
          '        "start": "2026-02-24T12:00:00.000Z",\n' +
          '        "complete": "2026-02-24T12:00:01.000Z"\n' +
          '    }\n' +
          '}\n' +
          '\n' +
          '            AGENT MESSAGE\n' +
          '            Hi there!\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '```\n' +
          '\n' +
          '**And here is the latest interaction:**\n' +
          '\n' +
          '**User:**\n' +
          'Hello\n' +
          '\n' +
          '**Agent:**\n' +
          'Hi there!\n' +
          '\n' +
          '\n' +
          '\n' +
          '**Rules:**\n' +
          '\n' +
          '- Decide what the agent should learn from this interaction.\n' +
          '- Append new commitments at the end of the agent source.\n' +
          '- Return only newly learned commitments, never repeat commitments that are already present.\n' +
          '- Do not modify the current agent source, just return new commitments (KNOWLEDGE, RULE, etc.).\n' +
          '- If there is nothing new to learn, return empty book code block\n' +
          '- Wrap the commitments in a book code block.\n' +
          '- Do not explain anything, just return the commitments wrapped in a book code block.\n' +
          '- Write the learned commitments in the same style and language as in the original agent source.\n' +
          '\n' +
          '- The agent source does not have an INITIAL MESSAGE defined, generate one.\n' +
          '- The INITIAL MESSAGE should be welcoming, informative about the agent capabilities and also should give some quick options to start the conversation with the agent.\n' +
          '- The quick option looks like `[👋 Hello](?message=Hello, how are you?)`\n' +
          '- A browser action option looks like `[🖨️ Print](?action=window.print%28%29)` and runs only after the user clicks it.\n' +
          '\n' +
          '\n' +
          'This is how book code block looks like:\n' +
          '\n' +
          '```book\n' +
          'KNOWLEDGE The sky is blue.\n' +
          'RULE Always be polite.\n' +
          '```',
        rawRequest: null,
        rawResponse: {}
      }

      at SelfLearningManager.callTeacher (src/llm-providers/agent/self-learning/SelfLearningManager.ts:262:17)

    console.info
      [46m[Self-learning][49m[36m Sampling[39m

      at SelfLearningManager.appendSample (src/llm-providers/agent/self-learning/SelfLearningManager.ts:129:17)

    console.info
      [46m[Self-learning][49m[36m Teacher[39m

      at SelfLearningManager.callTeacher (src/llm-providers/agent/self-learning/SelfLearningManager.ts:173:17)

    console.log
      !!!! teacherResult {
        content: '```book\nKNOWLEDGE New info.\n```',
        modelName: 'teacher-model',
        timing: {
          start: '2026-02-24T12:00:00.000Z',
          complete: '2026-02-24T12:00:01.000Z'
        },
        usage: {
          price: { value: 0, isUncertain: true },
          duration: { value: 0, isUncertain: true },
          input: {
            tokensCount: [Object],
            charactersCount: [Object],
            wordsCount: [Object],
            sentencesCount: [Object],
            linesCount: [Object],
            paragraphsCount: [Object],
            pagesCount: [Object]
          },
          output: {
            tokensCount: [Object],
            charactersCount: [Object],
            wordsCount: [Object],
            sentencesCount: [Object],
            linesCount: [Object],
            paragraphsCount: [Object],
            pagesCount: [Object]
          }
        },
        rawPromptContent: 'You are a teacher agent helping another agent to learn from its interactions.\n' +
          '\n' +
          'Here is your current client which you are teaching:\n' +
          '\n' +
          '```book\n' +
          '# My Agent\n' +
          '\n' +
          'PERSONA A helpful assistant.\n' +
          'INITIAL MESSAGE Welcome!\n' +
          '\n' +
          '            USER MESSAGE\n' +
          '            Hello\n' +
          '            \n' +
          'INTERNAL MESSAGE\n' +
          '{\n' +
          '    "kind": "MODEL_REQUEST",\n' +
          '    "rawPromptContent": "Hello",\n' +
          '    "rawRequest": null\n' +
          '}\n' +
          '\n' +
          'INTERNAL MESSAGE\n' +
          '{\n' +
          '    "kind": "MODEL_RESPONSE",\n' +
          '    "rawResponse": {},\n' +
          '    "usage": {\n' +
          '        "price": {\n' +
          '            "value": 0,\n' +
          '            "isUncertain": true\n' +
          '        },\n' +
          '        "duration": {\n' +
          '            "value": 0,\n' +
          '            "isUncertain": true\n' +
          '        },\n' +
          '        "input": {\n' +
          '            "tokensCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "charactersCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "wordsCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "sentencesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "linesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "paragraphsCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "pagesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            }\n' +
          '        },\n' +
          '        "output": {\n' +
          '            "tokensCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "charactersCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "wordsCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "sentencesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "linesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "paragraphsCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "pagesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            }\n' +
          '        }\n' +
          '    },\n' +
          '    "timing": {\n' +
          '        "start": "2026-02-24T12:00:00.000Z",\n' +
          '        "complete": "2026-02-24T12:00:01.000Z"\n' +
          '    }\n' +
          '}\n' +
          '\n' +
          '            AGENT MESSAGE\n' +
          '            Hi there!\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '```\n' +
          '\n' +
          '**And here is the latest interaction:**\n' +
          '\n' +
          '**User:**\n' +
          'Hello\n' +
          '\n' +
          '**Agent:**\n' +
          'Hi there!\n' +
          '\n' +
          '\n' +
          '\n' +
          '**Rules:**\n' +
          '\n' +
          '- Decide what the agent should learn from this interaction.\n' +
          '- Append new commitments at the end of the agent source.\n' +
          '- Return only newly learned commitments, never repeat commitments that are already present.\n' +
          '- Do not modify the current agent source, just return new commitments (KNOWLEDGE, RULE, etc.).\n' +
          '- If there is nothing new to learn, return empty book code block\n' +
          '- Wrap the commitments in a book code block.\n' +
          '- Do not explain anything, just return the commitments wrapped in a book code block.\n' +
          '- Write the learned commitments in the same style and language as in the original agent source.\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          'This is how book code block looks like:\n' +
          '\n' +
          '```book\n' +
          'KNOWLEDGE The sky is blue.\n' +
          'RULE Always be polite.\n' +
          '```',
        rawRequest: null,
        rawResponse: {}
      }

      at SelfLearningManager.callTeacher (src/llm-providers/agent/self-learning/SelfLearningManager.ts:262:17)

PASS scripts/run-codex-prompts/common/waitForPause.test.ts
PASS scripts/verify-prompts/verify-prompts.test.ts
PASS src/commitments/USE_USER_LOCATION/USE_USER_LOCATION.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/chat/ExternalUserChatAdminActions.test.tsx
PASS src/utils/agent-message-runtime/resolveAgentMessageRuntimeActivity.test.ts
PASS apps/agents-server/src/utils/stalwart/readStalwartEmailSnapshot.test.ts
PASS src/book-components/BookEditor/createUnknownCommitmentDiagnostics.test.ts
PASS src/book-2.0/agent-source/parseAgentSourceWithCommitments.title.test.ts
PASS src/book-components/Chat/Chat/ChatCitationModal.test.tsx
PASS apps/agents-server/src/components/CronJobConfiguration/CronJobConfiguration.test.tsx
PASS src/utils/agents/terminalAgentAvatarVisual.test.ts
PASS src/cli/cli-commands/coder/init.test.ts
PASS src/book-components/Chat/Chat/ChatActionsBar.test.tsx
PASS src/book-2.0/agent-source/createAgentModelRequirements.commitmentAggregation.test.ts
PASS src/cli/cli-commands/coder/mergeStringRecordJsonFile.test.ts
PASS apps/agents-server/src/utils/userChat/userChatJobRunReport.test.ts
PASS apps/agents-server/src/components/Homepage/HomepageOptimisticNavigation.test.tsx
PASS src/scrapers/document/DocumentScraper.test.ts
PASS apps/agents-server/src/utils/userChat/runImmediateUserChatAnswer.test.ts
PASS scripts/run-agent-messages/main/synchronizeGithubAgentRunnerRepositories.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirements.tools.test.ts
PASS apps/agents-server/src/utils/serverLimits.test.ts
PASS src/collection/pipeline-collection/constructors/createPipelineCollectionFromJson.test.ts
PASS apps/agents-server/src/utils/userWallet/resolveWalletAgentPermanentId.test.ts
PASS src/book-components/Chat/Chat/ChatRatingModal.test.tsx
PASS apps/agents-server/src/utils/userChat/createUserChatHarnessProgressCard.test.ts
PASS src/book-2.0/agent-source/parseAgentSourceWithCommitments.unknown.test.ts
PASS src/errors/utils/deserializeError.test.ts
PASS apps/agents-server/src/components/_utils/headlessParam.test.tsx
PASS apps/agents-server/src/utils/serverManagement/createManagedServer/seedServerCoreAgents.test.ts
PASS apps/agents-server/src/utils/defaultAgents/resolveDefaultAgentsStatus.test.ts
PASS src/book-components/Chat/Chat/CitationIframePreview.test.tsx
PASS src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.knowledge.test.ts
PASS src/llm-providers/mocked/test/fakeTextToExpectations.test.ts
  ● Console

    console.info
      { expectations: { characters: { min: 10, max: 50 } } }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)

    console.info
      { expectations: { words: { min: 1, max: 5 } } }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)

    console.info
      { expectations: { sentences: { min: 1, max: 5 } } }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)

    console.info
      { expectations: { paragraphs: { min: 1, max: 5 } } }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)

    console.info
      { expectations: { lines: { min: 2 } } }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)

    console.info
      { expectations: { pages: { min: 2 } } }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)

    console.info
      {
        expectations: {
          characters: { min: 10, max: 500 },
          words: { min: 1, max: 5 },
          sentences: { min: 1, max: 5 }
        }
      }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)

    console.info
      { expectations: { words: { min: 10 }, lines: { min: 3 } } }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)

PASS src/llm-providers/mocked/test/joker.test.ts
  ● Console

    console.info
      💬 Mocked callChatModel call

      at MockedEchoLlmExecutionTools.callChatModel (src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts:81:21)

PASS src/scripting/_test/custom-function-missing.test.ts
  ● Console

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHelloWithTypo = customFunctions.addHelloWithTypo;
      
      // The script:
      const resultString = "Matthew the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

PASS src/cli/cli-commands/coder/run.test.ts
PASS src/conversion/validation/validatePipeline-logicErrors.test.ts

  ●  Cannot log after tests are done. Did you forget to wait for something async in your test?
    Attempted to log "Parameter `{greeting}` should be an output parameter, but it was not generated during pipeline execution

    Note: This is a warning which happened after the pipeline was executed, and `{greeting}` was not for some reason defined in output parameters

    All parameters:
    1) `{yourName}` `[input parameter]` Name of the hero
    2) `{greeting}` `[output parameter]` <- Warning: Should be in the output but its not |".

    [0m [90m 18 |[39m )[33m:[39m [36mvoid[39m {
     [90m 19 |[39m     [36mconst[39m { isSuccessful[33m,[39m errors[33m,[39m warnings } [33m=[39m executionResult[33m;[39m
    [31m[1m>[22m[39m[90m 20 |[39m
     [90m    |[39m [31m[1m^[22m[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
     [90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m[0m

      at console.warn (node_modules/@jest/console/build/BufferedConsole.js:191:10)
      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:20:17)
      at Object.asPromise (src/execution/ExecutionTask.ts:138:63)


  ●  Cannot log after tests are done. Did you forget to wait for something async in your test?
    Attempted to log "Parameter `{greeting}` should be an output parameter, but it was not generated during pipeline execution

    Note: This is a warning which happened after the pipeline was executed, and `{greeting}` was not for some reason defined in output parameters

    All parameters:
    1) `{yourName}` `[input parameter]` Name of the hero
    2) `{greeting}` `[output parameter]` <- Warning: Should be in the output but its not |".

    [0m [90m 18 |[39m )[33m:[39m [36mvoid[39m {
     [90m 19 |[39m     [36mconst[39m { isSuccessful[33m,[39m errors[33m,[39m warnings } [33m=[39m executionResult[33m;[39m
    [31m[1m>[22m[39m[90m 20 |[39m
     [90m    |[39m [31m[1m^[22m[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
     [90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m[0m

      at console.warn (node_modules/@jest/console/build/BufferedConsole.js:191:10)
      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:20:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:102:55)
      at src/execution/ExecutionTask.ts:80:17

PASS scripts/run-codex-prompts/main/runPromptRound.test.ts
  ● Console

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:358:13)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:358:13)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:358:13)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:358:13)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:358:13)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:358:13)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:358:13)

    console.warn
      [33mPrompt round failed (retry 1/3): The harness died[39m

    [0m [90m 304 |[39m
     [90m 305 |[39m     [36mif[39m ([33m![39misRichUiEnabled) {
    [31m[1m>[22m[39m[90m 306 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 307 |[39m             colors[33m.[39myellow(
     [90m 308 |[39m                 [32m`Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`[39m[33m,[39m
     [90m 309 |[39m             )[33m,[39m[0m

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:306:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:197:27)

    console.warn
      [33mPrompt round failed (retry 2/3): The harness died[39m

    [0m [90m 304 |[39m
     [90m 305 |[39m     [36mif[39m ([33m![39misRichUiEnabled) {
    [31m[1m>[22m[39m[90m 306 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 307 |[39m             colors[33m.[39myellow(
     [90m 308 |[39m                 [32m`Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`[39m[33m,[39m
     [90m 309 |[39m             )[33m,[39m[0m

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:306:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:197:27)
      at runPromptRound (scripts/run-codex-prompts/main/runPromptRound.ts:131:5)
      at Object.<anonymous> (scripts/run-codex-prompts/main/runPromptRound.test.ts:550:9)

    console.warn
      [33mPrompt round failed (retry 3/3): The harness died[39m

    [0m [90m 304 |[39m
     [90m 305 |[39m     [36mif[39m ([33m![39misRichUiEnabled) {
    [31m[1m>[22m[39m[90m 306 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 307 |[39m             colors[33m.[39myellow(
     [90m 308 |[39m                 [32m`Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`[39m[33m,[39m
     [90m 309 |[39m             )[33m,[39m[0m

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:306:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:197:27)
      at runPromptRound (scripts/run-codex-prompts/main/runPromptRound.ts:131:5)
      at Object.<anonymous> (scripts/run-codex-prompts/main/runPromptRound.test.ts:550:9)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:358:13)

    console.warn
      [33mPrompt round failed (retry 1/3): The harness died[39m

    [0m [90m 304 |[39m
     [90m 305 |[39m     [36mif[39m ([33m![39misRichUiEnabled) {
    [31m[1m>[22m[39m[90m 306 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 307 |[39m             colors[33m.[39myellow(
     [90m 308 |[39m                 [32m`Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`[39m[33m,[39m
     [90m 309 |[39m             )[33m,[39m[0m

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:306:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:197:27)
      at runPromptRound (scripts/run-codex-prompts/main/runPromptRound.ts:131:5)
      at Object.<anonymous> (scripts/run-codex-prompts/main/runPromptRound.test.ts:602:9)

    console.warn
      [33mPrompt round failed (retry 2/3): The harness died[39m

    [0m [90m 304 |[39m
     [90m 305 |[39m     [36mif[39m ([33m![39misRichUiEnabled) {
    [31m[1m>[22m[39m[90m 306 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 307 |[39m             colors[33m.[39myellow(
     [90m 308 |[39m                 [32m`Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`[39m[33m,[39m
     [90m 309 |[39m             )[33m,[39m[0m

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:306:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:197:27)
      at runPromptRound (scripts/run-codex-prompts/main/runPromptRound.ts:131:5)
      at Object.<anonymous> (scripts/run-codex-prompts/main/runPromptRound.test.ts:602:9)

    console.warn
      [33mPrompt round failed (retry 3/3): The harness died[39m

    [0m [90m 304 |[39m
     [90m 305 |[39m     [36mif[39m ([33m![39misRichUiEnabled) {
    [31m[1m>[22m[39m[90m 306 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 307 |[39m             colors[33m.[39myellow(
     [90m 308 |[39m                 [32m`Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`[39m[33m,[39m
     [90m 309 |[39m             )[33m,[39m[0m

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:306:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:197:27)
      at runPromptRound (scripts/run-codex-prompts/main/runPromptRound.ts:131:5)
      at Object.<anonymous> (scripts/run-codex-prompts/main/runPromptRound.test.ts:602:9)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:358:13)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:358:13)

    console.warn
      [33mPrompt round failed (retry 1/3): The harness died[39m

    [0m [90m 304 |[39m
     [90m 305 |[39m     [36mif[39m ([33m![39misRichUiEnabled) {
    [31m[1m>[22m[39m[90m 306 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 307 |[39m             colors[33m.[39myellow(
     [90m 308 |[39m                 [32m`Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`[39m[33m,[39m
     [90m 309 |[39m             )[33m,[39m[0m

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:306:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:197:27)

    console.warn
      [33mPrompt round failed (retry 2/3): The harness died[39m

    [0m [90m 304 |[39m
     [90m 305 |[39m     [36mif[39m ([33m![39misRichUiEnabled) {
    [31m[1m>[22m[39m[90m 306 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 307 |[39m             colors[33m.[39myellow(
     [90m 308 |[39m                 [32m`Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`[39m[33m,[39m
     [90m 309 |[39m             )[33m,[39m[0m

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:306:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:197:27)
      at runPromptRound (scripts/run-codex-prompts/main/runPromptRound.ts:131:5)
      at Object.<anonymous> (scripts/run-codex-prompts/main/runPromptRound.test.ts:686:9)

    console.warn
      [33mPrompt round failed (retry 3/3): The harness died[39m

    [0m [90m 304 |[39m
     [90m 305 |[39m     [36mif[39m ([33m![39misRichUiEnabled) {
    [31m[1m>[22m[39m[90m 306 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 307 |[39m             colors[33m.[39myellow(
     [90m 308 |[39m                 [32m`Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`[39m[33m,[39m
     [90m 309 |[39m             )[33m,[39m[0m

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:306:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:197:27)
      at runPromptRound (scripts/run-codex-prompts/main/runPromptRound.ts:131:5)
      at Object.<anonymous> (scripts/run-codex-prompts/main/runPromptRound.test.ts:686:9)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:358:13)

PASS apps/agents-server/src/utils/agentProjects/agentProjectRuntimeDomains.test.ts
PASS src/commitments/USE_IMAGE_GENERATOR/USE_IMAGE_GENERATOR.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirements.writing.test.ts
PASS scripts/run-codex-prompts/common/coderRunEstimateCache.test.ts
PASS src/commands/KNOWLEDGE/knowledgeCommand.test.ts
PASS apps/agents-server/src/utils/serverManagement/createManagedServer/normalizeCreateServerInput.test.ts
PASS apps/agents-server/src/message-providers/email/sendgrid/verifySendgridInboundParseWebhook.test.ts
PASS apps/agents-server/src/utils/knowledge/resolveWebsiteKnowledgeSourcesForServer.test.ts
PASS src/utils/editable/utils/renamePipelineParameter.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.delete.test.ts
PASS apps/agents-server/src/app/api/auth/change-password/route.test.ts
PASS apps/agents-server/src/utils/serverManagement/createManagedServer/seedServerDefaultAgents.test.ts
PASS apps/agents-server/src/utils/agentProjects/createAgentProjectMarkdownReferences.test.ts
PASS apps/agents-server/src/components/NewAgentDialog/ManGoNewAgentWizard/config/bookSections.test.ts
PASS apps/agents-server/src/app/api/internal/agent-goal-chat-planned-messages/route.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/chat/generateChatMetadata.test.ts
PASS apps/agents-server/src/utils/userChat/createUserChatJobHeartbeatController.test.ts
PASS apps/agents-server/src/utils/transpilers/getTranspiledAgentExportWarnings.test.ts
PASS scripts/run-codex-prompts/prompts/markPromptAttempts.test.ts
PASS apps/agents-server/src/utils/chat/executeQuickActionButton.test.ts
PASS apps/agents-server/src/utils/agentProjects/agentProjectsPaths.test.ts
PASS src/cli/cli-commands/coder/add.test.ts
  ● Console

    console.info
      [32m✓ Added prompt [✨🦨] in prompts\2026-09-0000-add-a-dark-mode-toggle.md[39m

      at addCoderPrompt (src/cli/cli-commands/coder/add.ts:225:13)

    console.info
      [32m✓ Added prompt [✨💼] in prompts\2026-09-0000-task-with-priority.md[39m

      at addCoderPrompt (src/cli/cli-commands/coder/add.ts:225:13)

    console.info
      [32m✓ Added prompt [✨👎] in prompts\2026-09-0000-first-line-summary.md[39m

      at addCoderPrompt (src/cli/cli-commands/coder/add.ts:225:13)

    console.info
      [32m✓ Added prompt [✨🚼] in prompts\2026-09-0000-first-feature.md[39m

      at addCoderPrompt (src/cli/cli-commands/coder/add.ts:225:13)

    console.info
      [32m✓ Added prompt [✨🕋] in prompts\2026-09-0010-second-feature.md[39m

      at addCoderPrompt (src/cli/cli-commands/coder/add.ts:225:13)

PASS src/utils/validators/url/isHostnameOnPrivateNetwork.test.ts
PASS src/cli/other/vpsInstall.test.ts
PASS apps/agents-server/src/utils/userChat/createUserChatJobFailureDetails.test.ts
PASS apps/agents-server/src/app/api/admin/servers/[serverId]/route.test.ts
PASS src/commitments/USE_PRIVACY/USE_PRIVACY.test.ts
PASS apps/agents-server/src/utils/userChat/userChatJobState.test.ts
PASS src/book-components/BookEditor/useBookEditorMonacoLanguage.test.ts
PASS scripts/run-agent-messages/messages/resolveAgentProjectRuntimePromptApi.test.ts
PASS src/prepare/isPipelinePrepared.test.ts
PASS apps/agents-server/src/app/api/emails/incoming/sendgrid/route.test.ts
  ● Console

    console.error
      Error processing inbound email NotAllowed: SendGrid Inbound Parse webhook signature verification failed.
          at verifySendgridInboundParseWebhook (C:\Users\me\work\ai\promptbook\apps\agents-server\src\message-providers\email\sendgrid\verifySendgridInboundParseWebhook.ts:108:15)
          at POST (C:\Users\me\work\ai\promptbook\apps\agents-server\src\app\api\emails\incoming\sendgrid\route.ts:16:42)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\app\api\emails\incoming\sendgrid\route.test.ts:107:26)

    [0m [90m 50 |[39m         [36mreturn[39m [33mNextResponse[39m[33m.[39mjson({ success[33m:[39m [36mtrue[39m })[33m;[39m
     [90m 51 |[39m     } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 52 |[39m         console[33m.[39merror([32m'Error processing inbound email'[39m[33m,[39m error)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 53 |[39m         [36mreturn[39m [33mNextResponse[39m[33m.[39mjson(
     [90m 54 |[39m             { error[33m:[39m error [36minstanceof[39m [33mError[39m [33m?[39m error[33m.[39mmessage [33m:[39m [32m'Unknown error'[39m }[33m,[39m
     [90m 55 |[39m             { status[33m:[39m resolveSendgridInboundParseWebhookErrorStatus(error) }[33m,[39m[0m

      at POST (apps/agents-server/src/app/api/emails/incoming/sendgrid/route.ts:52:17)
      at Object.<anonymous> (apps/agents-server/src/app/api/emails/incoming/sendgrid/route.test.ts:107:26)

PASS apps/agents-server/src/utils/renameAgentSource.test.ts
PASS scripts/run-codex-prompts/git/commitChanges.test.ts
PASS apps/agents-server/src/components/AgentAvatar/AgentAvatar.test.tsx
PASS apps/agents-server/src/utils/vpsSelfUpdate/readPersistedVpsSelfUpdateJob.test.ts
PASS apps/agents-server/src/database/ensureAutomaticDatabaseMigrations.test.ts
  ● Console

    console.info
      🚀 Checking database migrations automatically for prefix "server_Test_"

      at runAutomaticDatabaseMigrationsForPrefix (apps/agents-server/src/database/ensureAutomaticDatabaseMigrations.ts:86:13)

    console.info
      ✅ Automatic database migration check finished for prefix "server_Test_"

      at runAutomaticDatabaseMigrationsForPrefix (apps/agents-server/src/database/ensureAutomaticDatabaseMigrations.ts:103:13)

    console.info
      🚀 Checking database migrations automatically for prefix "server_Test_"

      at runAutomaticDatabaseMigrationsForPrefix (apps/agents-server/src/database/ensureAutomaticDatabaseMigrations.ts:86:13)

    console.info
      🚀 Checking database migrations automatically for prefix "server_Test_"

      at runAutomaticDatabaseMigrationsForPrefix (apps/agents-server/src/database/ensureAutomaticDatabaseMigrations.ts:86:13)

    console.info
      ✅ Automatic database migration check finished for prefix "server_Test_"

      at runAutomaticDatabaseMigrationsForPrefix (apps/agents-server/src/database/ensureAutomaticDatabaseMigrations.ts:103:13)

PASS apps/agents-server/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource.test.ts
PASS apps/agents-server/src/utils/agentProjects/hasAgentProjects.test.ts
PASS src/commands/SECTION/sectionCommand.test.ts
PASS src/book-components/Chat/Chat/useChatInputAreaComposer.draftMessage.test.tsx
PASS apps/agents-server/src/utils/vpsConfiguration.test.ts
PASS src/collection/agent-collection/constructors/agent-collection-in-supabase/createAgentPersistenceRecords.test.ts
PASS src/llm-providers/_common/register/createLlmToolsFromConfiguration.test.ts
PASS apps/agents-server/src/tools/createChatAttachmentToolFunctions.test.ts
PASS apps/agents-server/src/app/admin/messages/send-email/actions.test.ts
PASS src/book-components/Chat/SourceChip/SourceChip.test.tsx
PASS src/commands/URL/urlCommand.test.ts
PASS src/commitments/_common/getGroupedCommitmentDefinitions.writing.test.ts
PASS src/utils/normalization/orderJson.test.ts
PASS apps/agents-server/src/utils/vpsSelfUpdate/vpsSelfUpdateJobHistory.test.ts
PASS src/commands/X_INSTRUMENT/instrumentCommand.test.ts
PASS apps/agents-server/src/tools/spawn_agent.test.ts
  ● Console

    console.info
      [spawn_agent] Created agent { agentId: 'child123', actor: 'anonymous', sourceLength: 36 }

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:106:17)

    console.warn
      [spawn_agent] Failed to create agent {
        errorName: 'ParseError',
        errorMessage: 'Invalid create-agent payload.\n\nUnknown field(s): `unknownField`.'
      }

    [0m [90m 120 |[39m         }[33m;[39m
     [90m 121 |[39m
    [31m[1m>[22m[39m[90m 122 |[39m         console[33m.[39mwarn([32m'[spawn_agent] Failed to create agent'[39m[33m,[39m {
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 123 |[39m             errorName[33m:[39m error[33m.[39mname[33m,[39m
     [90m 124 |[39m             errorMessage[33m:[39m error[33m.[39mmessage[33m,[39m
     [90m 125 |[39m         })[33m;[39m[0m

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:122:17)
      at Object.<anonymous> (apps/agents-server/src/tools/spawn_agent.test.ts:106:27)

    console.warn
      [spawn_agent] Failed to create agent {
        errorName: 'ParseError',
        errorMessage: 'Invalid create-agent payload.\n\nMissing required field `source`.'
      }

    [0m [90m 120 |[39m         }[33m;[39m
     [90m 121 |[39m
    [31m[1m>[22m[39m[90m 122 |[39m         console[33m.[39mwarn([32m'[spawn_agent] Failed to create agent'[39m[33m,[39m {
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 123 |[39m             errorName[33m:[39m error[33m.[39mname[33m,[39m
     [90m 124 |[39m             errorMessage[33m:[39m error[33m.[39mmessage[33m,[39m
     [90m 125 |[39m         })[33m;[39m[0m

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:122:17)
      at Object.<anonymous> (apps/agents-server/src/tools/spawn_agent.test.ts:118:27)

    console.warn
      [spawn_agent] Failed to create agent {
        errorName: 'NotAllowed',
        errorMessage: 'Spawning agents is not allowed.\n\nYou are not authorized to create agents.'
      }

    [0m [90m 120 |[39m         }[33m;[39m
     [90m 121 |[39m
    [31m[1m>[22m[39m[90m 122 |[39m         console[33m.[39mwarn([32m'[spawn_agent] Failed to create agent'[39m[33m,[39m {
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 123 |[39m             errorName[33m:[39m error[33m.[39mname[33m,[39m
     [90m 124 |[39m             errorMessage[33m:[39m error[33m.[39mmessage[33m,[39m
     [90m 125 |[39m         })[33m;[39m[0m

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:122:17)
      at Object.<anonymous> (apps/agents-server/src/tools/spawn_agent.test.ts:131:27)

    console.warn
      [spawn_agent] Failed to create agent {
        errorName: 'LimitReachedError',
        errorMessage: 'Create-agent payload is too large.\n' +
          '\n' +
          'Field `source` exceeds 120000 characters.'
      }

    [0m [90m 120 |[39m         }[33m;[39m
     [90m 121 |[39m
    [31m[1m>[22m[39m[90m 122 |[39m         console[33m.[39mwarn([32m'[spawn_agent] Failed to create agent'[39m[33m,[39m {
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 123 |[39m             errorName[33m:[39m error[33m.[39mname[33m,[39m
     [90m 124 |[39m             errorMessage[33m:[39m error[33m.[39mmessage[33m,[39m
     [90m 125 |[39m         })[33m;[39m[0m

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:122:17)
      at Object.<anonymous> (apps/agents-server/src/tools/spawn_agent.test.ts:142:27)

    console.info
      [spawn_agent] Created agent { agentId: 'child123', actor: 'user:42', sourceLength: 11 }

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:106:17)

    console.info
      [spawn_agent] Created agent { agentId: 'child123', actor: 'user:42', sourceLength: 11 }

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:106:17)

    console.info
      [spawn_agent] Created agent { agentId: 'child123', actor: 'user:42', sourceLength: 11 }

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:106:17)

    console.info
      [spawn_agent] Created agent { agentId: 'child123', actor: 'user:42', sourceLength: 11 }

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:106:17)

    console.info
      [spawn_agent] Created agent { agentId: 'child123', actor: 'user:42', sourceLength: 11 }

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:106:17)

    console.warn
      [spawn_agent] Failed to create agent {
        errorName: 'LimitReachedError',
        errorMessage: 'Spawn rate limit exceeded.\n' +
          '\n' +
          'You can create at most 5 agents per 10 minutes.'
      }

    [0m [90m 120 |[39m         }[33m;[39m
     [90m 121 |[39m
    [31m[1m>[22m[39m[90m 122 |[39m         console[33m.[39mwarn([32m'[spawn_agent] Failed to create agent'[39m[33m,[39m {
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 123 |[39m             errorName[33m:[39m error[33m.[39mname[33m,[39m
     [90m 124 |[39m             errorMessage[33m:[39m error[33m.[39mmessage[33m,[39m
     [90m 125 |[39m         })[33m;[39m[0m

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:122:17)
      at Object.<anonymous> (apps/agents-server/src/tools/spawn_agent.test.ts:170:34)

PASS book/scripts/import-markdown/placeImports.test.ts
PASS apps/agents-server/src/utils/vpsTask/runWithVpsServerSetupTask.test.ts
PASS src/cli/cli-commands/coder/generate-boilerplates.test.ts
  ● Console

    console.info
      🚀  Generate prompt boilerplate files

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:121:13)

    console.info
      [34mHighest existing number for 2026-09 found: 0000[39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:133:13)

    console.info
      [32mFound 1071 available fresh emojis[39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:144:13)

    console.info
      [32mSelected emojis: [✨🐹] [✨🤲] [✨👻] [✨❔] [✨🧡][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:145:13)

    console.info
      [33mCreating 5 files:[39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:183:13)

    console.info
      [32m✓ Created: 2026-09-0000-foo.md with [✨🐹][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:187:17)

    console.info
      [32m✓ Created: 2026-09-0010-bar.md with [✨🤲][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:187:17)

    console.info
      [32m✓ Created: 2026-09-0020-baz.md with [✨👻][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:187:17)

    console.info
      [32m✓ Created: 2026-09-0030-qux.md with [✨❔][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:187:17)

    console.info
      [32m✓ Created: 2026-09-0040-brr.md with [✨🧡][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:187:17)

    console.info
      [42m Successfully created 5 prompts in 5 prompt boilerplate files! [49m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:190:13)

    console.info
      🚀  Generate prompt boilerplate files

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:121:13)

    console.info
      [34mHighest existing number for 2026-09 found: 0000[39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:133:13)

    console.info
      [32mFound 1071 available fresh emojis[39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:144:13)

    console.info
      [32mSelected emojis: [✨🎶] [✨🧑] [✨🎄][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:145:13)

    console.info
      [33mCreating 3 files:[39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:183:13)

    console.info
      [32m✓ Created: 2026-09-0000-foo.md with [✨🎶][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:187:17)

    console.info
      [32m✓ Created: 2026-09-0010-bar.md with [✨🧑][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:187:17)

    console.info
      [32m✓ Created: 2026-09-0020-baz.md with [✨🎄][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:187:17)

    console.info
      [42m Successfully created 12 prompts in 3 prompt boilerplate files! [49m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:190:13)

PASS scripts/utils/emojiTags/scanEmojiTagUsage.test.ts
PASS src/commands/BOOK_VERSION/bookVersionCommand.test.ts
PASS apps/agents-server/src/components/Portal/Dialog.test.tsx
PASS apps/agents-server/src/utils/plannedMessageManager/filterPlannedMessages.test.ts
PASS apps/agents-server/src/utils/memoryRuntimeContext.test.ts
PASS apps/agents-server/src/utils/agentReferenceResolver/createAgentInheritanceDiagnostics.test.ts
PASS apps/agents-server/src/utils/agentProjects/resolveAgentProjectFaviconRelativePath.test.ts
PASS apps/agents-server/src/utils/email/sendEmailChatReply.test.ts
PASS src/book-components/Chat/utils/ChatPersistence.test.ts
PASS scripts/run-codex-prompts/cli/parseRunOptions.test.ts
PASS scripts/run-agent-messages/ui/loadAgentRunUiMetadata.test.ts
PASS scripts/run-codex-prompts/prompts/writePromptRunTrace.test.ts
PASS apps/agents-server/src/tools/BrowserConnectionProvider.test.ts
PASS apps/agents-server/src/utils/applyPagePreviewBrowserInput.test.ts
PASS src/book-2.0/agent-source/parseAgentSourceWithCommitments.blocks.test.ts
PASS apps/agents-server/src/components/Header/ControlPanel/createControlPanelContentState.test.ts
PASS src/book-components/Chat/hooks/useChatCompleteNotification.test.tsx
PASS apps/agents-server/src/utils/projects/extractProjectRepositoriesFromAgentSource.test.ts
PASS src/utils/linguistic-hash/linguisticHash.test.ts
PASS apps/agents-server/src/app/sitemap.xml/route.test.ts
PASS apps/agents-server/src/utils/agentReferenceResolver/createUnresolvedAgentReferenceDiagnostics.test.ts
PASS apps/agents-server/src/components/Homepage/useAgentsListSyncState.test.ts
PASS src/book-2.0/agent-source/extractOpenTeacherInstructions.test.ts
PASS src/utils/execCommand/execCommandNormalizeOptions.test.ts
PASS src/utils/execCommand/execCommand.test.ts
PASS src/executables/browsers/locateEdge.test.ts
PASS src/book-components/BookEditor/createDeprecatedCommitmentDiagnostics.test.ts
PASS scripts/run-codex-prompts/prompts/formatCoderRunSteps.test.ts
PASS src/utils/serialization/isSerializableAsJson.test.ts
PASS apps/agents-server/src/utils/vpsTask/vpsTaskHistory.test.ts
PASS src/book-2.0/book-language-documentation/renderGroupedCommitmentDocumentationMarkdown.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/generateAgentMetadata.test.ts
PASS src/book-components/BookEditor/createDuplicateFromCommitmentDiagnostics.test.ts
PASS scripts/run-codex-prompts/testing/runTestBefore.test.ts
  ● Console

    console.info
      [90mRunning initial tests before the agent coding starts: npm run test[39m

      at runTestBefore (scripts/run-codex-prompts/testing/runTestBefore.ts:38:13)

    console.info
      [32mPre-coding tests passed.[39m

      at runTestBefore (scripts/run-codex-prompts/testing/runTestBefore.ts:51:17)

    console.info
      [90mRunning initial tests before the agent coding starts: npm test[39m

      at runTestBefore (scripts/run-codex-prompts/testing/runTestBefore.ts:38:13)

PASS src/commitments/_common/getGroupedCommitmentDefinitions.order.test.ts
PASS src/commands/PARAMETER/parameterCommand.test.ts
PASS src/avatars/visuals/octopusAvatarVisualShared.test.ts
PASS src/book-2.0/agent-source/parseAgentSource.import.test.ts
PASS src/book-components/Chat/Chat/pagePreview/PagePreviewInputQueue.test.ts
PASS scripts/utils/emojiTags/validSinglePictogramEmojis.test.ts
PASS src/utils/normalization/normalizeMessageText.test.ts
PASS scripts/run-agent-messages/messages/resolveAgentProjectsUrlPath.test.ts
PASS apps/agents-server/src/utils/agentProjects/agentProjectRuntimePm2.test.ts
PASS apps/agents-server/src/utils/agentGoalChat/resolveEffectiveAgentGoal.test.ts
PASS src/utils/editable/utils/isFlatPipeline.test.ts
PASS src/commands/_common/getParserForCommand.test.ts
PASS scripts/run-agent-messages/messages/resolveAnsweredMessageTouches.test.ts
PASS apps/utils/src/app/avatars/avatarPlaygroundUrlState.test.ts
PASS apps/agents-server/src/utils/getControlPanelOptionAvailability.test.ts
PASS apps/agents-server/src/utils/speech-to-text/LongRunningSpeechRecognitionSession.test.ts
PASS apps/agents-server/src/components/NewAgentDialog/createNewAgentWizardSubmissionActions.test.ts
PASS src/commands/POSTPROCESS/postprocessCommand.test.ts
PASS src/book-components/Chat/hooks/useChatAutoScroll.test.tsx
PASS src/utils/files/mimeTypeToExtension.test.ts
PASS scripts/generate-packages/collectMainPackageDependencies.test.ts
PASS src/book-3.0/CliAgent.test.ts
PASS apps/agents-server/src/utils/vpsSelfUpdate/vpsSelfUpdateInstalledVersions.test.ts
PASS src/cli/cli-commands/coder/$ensureCoderHarnessGitignoreRules.test.ts
PASS apps/agents-server/src/utils/userPushNotificationSettings.test.ts
PASS src/llm-providers/openai/computeOpenAiUsage.test.ts
PASS apps/agents-server/src/message-providers/email/stalwart/StalwartMessageProvider.test.ts
PASS src/commands/FOREACH/foreachCommand.test.ts
PASS apps/agents-server/src/utils/agentProjects/listAgentProjectDirectoryEntries.test.ts
PASS apps/agents-server/src/components/NewAgentDialog/newAgentWizardPresets.test.ts
PASS apps/agents-server/src/utils/vpsSelfUpdate/readAgentsServerFooterVersion.test.ts
PASS apps/agents-server/src/components/Header/resolveHeaderSystemWarnings.test.ts
PASS src/commitments/CLOSED/CLOSED.test.ts
PASS scripts/run-agent-messages/main/runPersistentAgentWatch.test.ts
PASS src/utils/parameters/mapAvailableToExpectedParameters.test.ts
PASS scripts/run-codex-prompts/prompts/resolveInterruptedPrompt.test.ts
PASS apps/agents-server/src/utils/chat/createAttachmentAwareCitationLabelResolver.test.ts
PASS src/cli/cli-commands/common/promptbook-cli/$resolvePromptbookCliInstallations.test.ts
PASS apps/agents-server/src/utils/agentGoalChat/agentGoalChatIdentity.test.ts
PASS src/commitments/USE_PROJECT/USE_PROJECT.test.ts
PASS apps/agents-server/src/utils/vpsCertificateScheduler.test.ts
PASS src/commitments/_common/getGroupedCommitmentDefinitions.openClosed.test.ts
PASS scripts/run-codex-prompts/ui/coderRunUiRefresh.test.ts
PASS apps/agents-server/src/utils/agentOrganization/hiddenFolders.test.ts
PASS apps/agents-server/src/app/admin/messages/send-email/emailTesting.test.ts
PASS apps/agents-server/src/components/AdminTerminal/useAdminTerminalSession.test.ts
PASS apps/agents-server/src/utils/getAdminChatTasksResponse.test.ts
PASS apps/agents-server/src/utils/messages/parseUseEmailSmtpCredential.test.ts
PASS apps/agents-server/src/utils/localChatRunner/parseLocalTeamConversations.test.ts
PASS apps/agents-server/src/utils/agentProjects/agentProjectRuntimeDesiredState.test.ts
PASS src/cli/cli-commands/agents-server/init.test.ts
PASS src/commitments/_common/getGroupedCommitmentDefinitions.use.test.ts
PASS src/commands/PERSONA/personaCommand.test.ts
PASS src/commands/MODEL/modelCommand.test.ts
PASS src/commands/EXPECT/expectCommand.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/chat/useChatDocumentTitle.test.tsx
PASS scripts/run-codex-prompts/ui/buildCoderRunUiFrame.test.ts
PASS scripts/run-codex-prompts/server/buildCoderServerPromptResponse.test.ts
PASS src/utils/misc/computeHash.test.ts
PASS src/execution/execution-report/countWorkingDuration.test.ts
PASS apps/agents-server/src/app/admin/messages/messageAdminPresentation.test.ts
PASS src/utils/misc/arrayableToArray.test.ts
PASS apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts
  ● Console

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:106:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusHook (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:281:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:246:5)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 117 |[39m             localStorage[33m.[39mremoveItem(storageKey)[33m;[39m
     [90m 118 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 119 |[39m             console[33m.[39mwarn([32m'Failed to clear chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 120 |[39m         }
     [90m 121 |[39m     }
     [90m 122 |[39m[0m

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:106:25)

    console.warn
      Failed to save chat messages to localStorage: ReferenceError: localStorage is not defined
          at Function.saveMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:76:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:123:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 76 |[39m             localStorage[33m.[39msetItem(storageKey[33m,[39m serializedMessages)[33m;[39m
     [90m 77 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 78 |[39m             console[33m.[39mwarn([32m'Failed to save chat messages to localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 79 |[39m         }
     [90m 80 |[39m     }
     [90m 81 |[39m[0m

      at Function.saveMessages (src/book-components/Chat/utils/ChatPersistence.ts:78:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:123:25)

    console.warn
      Failed to load chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.loadSerializedMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:44:39)
          at Function.loadMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:88:33)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:124:40)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 48 |[39m             }
     [90m 49 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 50 |[39m             console[33m.[39mwarn([32m'Failed to load chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 51 |[39m         }
     [90m 52 |[39m
     [90m 53 |[39m         [36mreturn[39m [36mthis[39m[33m.[39m[33mMEMORY_STORAGE[39m[33m.[39m[36mget[39m(storageKey) [33m||[39m [36mnull[39m[33m;[39m[0m

      at Function.loadSerializedMessages (src/book-components/Chat/utils/ChatPersistence.ts:50:21)
      at Function.loadMessages (src/book-components/Chat/utils/ChatPersistence.ts:88:33)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:124:40)

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:110:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusHook (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:281:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:254:5)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 117 |[39m             localStorage[33m.[39mremoveItem(storageKey)[33m;[39m
     [90m 118 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 119 |[39m             console[33m.[39mwarn([32m'Failed to clear chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 120 |[39m         }
     [90m 121 |[39m     }
     [90m 122 |[39m[0m

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:110:25)

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:106:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusHook (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:281:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:246:5)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 117 |[39m             localStorage[33m.[39mremoveItem(storageKey)[33m;[39m
     [90m 118 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 119 |[39m             console[33m.[39mwarn([32m'Failed to clear chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 120 |[39m         }
     [90m 121 |[39m     }
     [90m 122 |[39m[0m

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:106:25)

    console.warn
      Failed to save chat messages to localStorage: ReferenceError: localStorage is not defined
          at Function.saveMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:76:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:140:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 76 |[39m             localStorage[33m.[39msetItem(storageKey[33m,[39m serializedMessages)[33m;[39m
     [90m 77 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 78 |[39m             console[33m.[39mwarn([32m'Failed to save chat messages to localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 79 |[39m         }
     [90m 80 |[39m     }
     [90m 81 |[39m[0m

      at Function.saveMessages (src/book-components/Chat/utils/ChatPersistence.ts:78:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:140:25)

    console.warn
      Failed to load chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.loadSerializedMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:44:39)
          at Function.loadMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:88:33)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:141:40)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 48 |[39m             }
     [90m 49 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 50 |[39m             console[33m.[39mwarn([32m'Failed to load chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 51 |[39m         }
     [90m 52 |[39m
     [90m 53 |[39m         [36mreturn[39m [36mthis[39m[33m.[39m[33mMEMORY_STORAGE[39m[33m.[39m[36mget[39m(storageKey) [33m||[39m [36mnull[39m[33m;[39m[0m

      at Function.loadSerializedMessages (src/book-components/Chat/utils/ChatPersistence.ts:50:21)
      at Function.loadMessages (src/book-components/Chat/utils/ChatPersistence.ts:88:33)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:141:40)

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:110:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusHook (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:281:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:254:5)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 117 |[39m             localStorage[33m.[39mremoveItem(storageKey)[33m;[39m
     [90m 118 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 119 |[39m             console[33m.[39mwarn([32m'Failed to clear chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 120 |[39m         }
     [90m 121 |[39m     }
     [90m 122 |[39m[0m

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:110:25)

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:106:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusHook (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:281:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:246:5)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 117 |[39m             localStorage[33m.[39mremoveItem(storageKey)[33m;[39m
     [90m 118 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 119 |[39m             console[33m.[39mwarn([32m'Failed to clear chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 120 |[39m         }
     [90m 121 |[39m     }
     [90m 122 |[39m[0m

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:106:25)

    console.warn
      Failed to save chat messages to localStorage: ReferenceError: localStorage is not defined
          at Function.saveMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:76:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:167:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 76 |[39m             localStorage[33m.[39msetItem(storageKey[33m,[39m serializedMessages)[33m;[39m
     [90m 77 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 78 |[39m             console[33m.[39mwarn([32m'Failed to save chat messages to localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 79 |[39m         }
     [90m 80 |[39m     }
     [90m 81 |[39m[0m

      at Function.saveMessages (src/book-components/Chat/utils/ChatPersistence.ts:78:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:167:25)

    console.warn
      Failed to load chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.loadSerializedMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:44:39)
          at Function.loadMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:88:33)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:170:40)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 48 |[39m             }
     [90m 49 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 50 |[39m             console[33m.[39mwarn([32m'Failed to load chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 51 |[39m         }
     [90m 52 |[39m
     [90m 53 |[39m         [36mreturn[39m [36mthis[39m[33m.[39m[33mMEMORY_STORAGE[39m[33m.[39m[36mget[39m(storageKey) [33m||[39m [36mnull[39m[33m;[39m[0m

      at Function.loadSerializedMessages (src/book-components/Chat/utils/ChatPersistence.ts:50:21)
      at Function.loadMessages (src/book-components/Chat/utils/ChatPersistence.ts:88:33)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:170:40)

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:177:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 117 |[39m             localStorage[33m.[39mremoveItem(storageKey)[33m;[39m
     [90m 118 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 119 |[39m             console[33m.[39mwarn([32m'Failed to clear chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 120 |[39m         }
     [90m 121 |[39m     }
     [90m 122 |[39m[0m

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:177:25)

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:110:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusHook (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:281:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:254:5)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 117 |[39m             localStorage[33m.[39mremoveItem(storageKey)[33m;[39m
     [90m 118 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 119 |[39m             console[33m.[39mwarn([32m'Failed to clear chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 120 |[39m         }
     [90m 121 |[39m     }
     [90m 122 |[39m[0m

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:110:25)

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:106:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusHook (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:281:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:246:5)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 117 |[39m             localStorage[33m.[39mremoveItem(storageKey)[33m;[39m
     [90m 118 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 119 |[39m             console[33m.[39mwarn([32m'Failed to clear chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 120 |[39m         }
     [90m 121 |[39m     }
     [90m 122 |[39m[0m

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:106:25)

    console.warn
      Failed to save chat messages to localStorage: ReferenceError: localStorage is not defined
          at Function.saveMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:76:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:185:29)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 76 |[39m             localStorage[33m.[39msetItem(storageKey[33m,[39m serializedMessages)[33m;[39m
     [90m 77 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 78 |[39m             console[33m.[39mwarn([32m'Failed to save chat messages to localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 79 |[39m         }
     [90m 80 |[39m     }
     [90m 81 |[39m[0m

      at Function.saveMessages (src/book-components/Chat/utils/ChatPersistence.ts:78:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:185:29)

    console.warn
      Failed to save chat messages to localStorage: ReferenceError: localStorage is not defined
          at Function.saveMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:76:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:185:29)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 76 |[39m             localStorage[33m.[39msetItem(storageKey[33m,[39m serializedMessages)[33m;[39m
     [90m 77 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 78 |[39m             console[33m.[39mwarn([32m'Failed to save chat messages to localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 79 |[39m         }
     [90m 80 |[39m     }
     [90m 81 |[39m[0m

      at Function.saveMessages (src/book-components/Chat/utils/ChatPersistence.ts:78:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:185:29)

    console.warn
      Failed to save chat messages to localStorage: ReferenceError: localStorage is not defined
          at Function.saveMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:76:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:185:29)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 76 |[39m             localStorage[33m.[39msetItem(storageKey[33m,[39m serializedMessages)[33m;[39m
     [90m 77 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 78 |[39m             console[33m.[39mwarn([32m'Failed to save chat messages to localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 79 |[39m         }
     [90m 80 |[39m     }
     [90m 81 |[39m[0m

      at Function.saveMessages (src/book-components/Chat/utils/ChatPersistence.ts:78:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:185:29)

    console.warn
      Failed to load chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.loadSerializedMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:44:39)
          at Function.loadMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:88:33)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:197:40)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 48 |[39m             }
     [90m 49 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 50 |[39m             console[33m.[39mwarn([32m'Failed to load chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 51 |[39m         }
     [90m 52 |[39m
     [90m 53 |[39m         [36mreturn[39m [36mthis[39m[33m.[39m[33mMEMORY_STORAGE[39m[33m.[39m[36mget[39m(storageKey) [33m||[39m [36mnull[39m[33m;[39m[0m

      at Function.loadSerializedMessages (src/book-components/Chat/utils/ChatPersistence.ts:50:21)
      at Function.loadMessages (src/book-components/Chat/utils/ChatPersistence.ts:88:33)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:197:40)

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:110:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusHook (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:281:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:254:5)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 117 |[39m             localStorage[33m.[39mremoveItem(storageKey)[33m;[39m
     [90m 118 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 119 |[39m             console[33m.[39mwarn([32m'Failed to clear chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 120 |[39m         }
     [90m 121 |[39m     }
     [90m 122 |[39m[0m

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:110:25)

PASS src/execution/utils/addUsage.test.ts
PASS src/commitments/_common/getAllCommitmentDefinitions.test.ts
PASS src/cli/cli-commands/agents-server/run.test.ts
PASS apps/agents-server/src/utils/upload/fileUploadAvailability.test.ts
PASS scripts/run-codex-prompts/main/listCoderPrompts.test.ts
PASS apps/agents-server/src/utils/serverSelection.test.ts
PASS scripts/run-codex-prompts/ping/pingCoderHarnessPeriodically.test.ts
PASS apps/agents-server/src/utils/externalChatRunner/GithubRepositoryClient.test.ts
PASS apps/agents-server/src/components/Footer/getCommitFooterEmoji.test.ts
PASS scripts/generate-packages/addDependenciesForGeneratedPackages.test.ts
PASS scripts/run-codex-prompts/ping/extractCoderPingAnswer.test.ts
PASS apps/agents-server/src/utils/agentBrowserProfile/resolveAgentBrowserProfileDirectory.test.ts
PASS src/book-components/Chat/save/markdown/mdSaveFormatDefinition.test.ts
PASS src/commitments/index.test.ts
PASS scripts/run-codex-prompts/runners/github-copilot/GitHubCopilotRunner.test.ts
PASS src/utils/normalization/normalizeTo_camelCase.test.ts
PASS src/book-components/Chat/MockedChat/MockedChat.test.tsx
PASS src/commands/X_ACTION/actionCommand.test.ts
PASS scripts/run-agent-messages/messages/buildAgentMessageScriptPath.test.ts
PASS src/cli/cli-commands/coder/server.test.ts
PASS apps/agents-server/src/utils/plannedMessageManager/resolvePlannedMessageLifecycle.test.ts
PASS apps/agents-server/src/utils/userChat/userChatMessageLifecycle.test.ts
PASS apps/agents-server/src/utils/agentReferenceResolver/createServerAgentReferenceResolver.test.ts
PASS apps/agents-server/src/utils/agentProjects/createAgentProjectInitials.test.ts
PASS scripts/run-codex-prompts/common/runGoScript/withTempScript.test.ts
PASS apps/agents-server/src/utils/localChatRunner/persistLocalTeamConversations.test.ts
PASS scripts/run-codex-prompts/isolation/runIsolatedPromptRound.test.ts
PASS src/commands/_common/stringifyCommand.test.ts
PASS src/commitments/DELETE/DELETE.test.ts
PASS apps/agents-server/src/database/runDatabaseMigrations.test.ts
PASS apps/agents-server/src/utils/getVpsAdminChatTasksResponse.test.ts
PASS apps/agents-server/src/components/Header/createMyChatsMobileMenuItem.test.ts
PASS apps/agents-server/src/components/Header/mapContextMenuItemsToSubMenuItems.test.ts
PASS src/commands/FORMAT/formatCommand.test.ts
PASS apps/agents-server/src/utils/vpsSelfUpdate.test.ts
PASS apps/agents-server/src/app/api/internal/user-chat-jobs/run/route.test.ts
PASS scripts/run-codex-prompts/runners/claude-code/ClaudeCodeRunner.test.ts
PASS apps/agents-server/src/app/api/agents/route.test.ts
PASS apps/agents-server/src/utils/dnsRecords/resolveDnsRecordBatchPlan.test.ts
PASS apps/agents-server/src/utils/agentProjects/listAgentProjectDnsDiagnostics.test.ts
PASS src/utils/random/$generateBookBoilerplate.test.ts
PASS src/pipeline/prompt-notation.test.ts
PASS src/book-components/Chat/utils/walletCredentialToolCall.test.ts
PASS src/utils/parameters/templateParameters.test.ts
PASS src/utils/markdown/parseMarkdownSection.test.ts
PASS src/pipeline/isValidPipelineString.test.ts
PASS src/commands/JOKER/jokerCommand.test.ts
PASS apps/agents-server/src/utils/agentReferenceResolver/$provideAgentReferenceResolver.test.ts
PASS src/book-2.0/agent-source/parseAgentSourceWithCommitments.use.test.ts
PASS apps/agents-server/src/utils/importAgentWithFallback.test.ts
PASS scripts/run-codex-prompts/runners/openai-codex/buildCodexScript.test.ts
PASS scripts/run-codex-prompts/common/runGoScript/withPromptRuntimeLog.test.ts
PASS apps/agents-server/src/database/loadAgentsServerEnvFile.test.ts
PASS scripts/run-codex-prompts/prompts/markPromptInProgress.test.ts
PASS scripts/run-codex-prompts/main/resolvePromptRunner.test.ts
PASS src/utils/markdown/splitMarkdownIntoSections.test.ts
PASS src/utils/misc/parseNumber.test.ts
PASS src/utils/markdown/removeMarkdownComments.test.ts
PASS apps/agents-server/src/database/metadataDefaults.test.ts
PASS apps/agents-server/src/components/Header/buildActiveAgentViewItems.test.ts
PASS src/cli/cli-commands/common/promptbook-cli/$ensurePromptbookCliInstallations.test.ts
PASS scripts/run-codex-prompts/runners/claude-code/ClaudeCodeSessionResurrection.test.ts
PASS src/commands/KNOWLEDGE/utils/knowledgeSourceContentToName.test.ts
PASS apps/agents-server/src/utils/userWallet/createUserWalletRecord.test.ts
PASS src/llm-providers/openai/openai-models.test.ts
PASS src/utils/editable/edit-pipeline-string/addPipelineCommand.test.ts
PASS src/commands/_BOILERPLATE/boilerplateCommand.test.ts
PASS apps/agents-server/src/utils/stalwart/resolveStalwartApiUrl.test.ts
PASS src/utils/files/listAllFiles.test.ts
PASS src/utils/markdown/extractAllBlocksFromMarkdown-real.test.ts
PASS src/utils/expectation-counters/countParagraphs.test.ts
PASS src/cli/cli-commands/coder/verify.test.ts
PASS scripts/run-agent-messages/ui/buildAgentRunUiFrame.test.ts
PASS apps/agents-server/src/utils/externalChatRunner/ExternalUserChatJobMetadata.test.ts
PASS apps/agents-server/src/utils/agentProjects/humanizeAgentProjectName.test.ts
PASS src/utils/expectation-counters/countWords.test.ts
PASS src/utils/agents/resolveAgentAvatarImageUrl.test.ts
PASS src/cli/cli-commands/coder/ping.test.ts
PASS src/book-components/Chat/Chat/pagePreview/normalizePagePreviewInputEvent.test.ts
PASS apps/agents-server/src/app/api/auth/login/route.test.ts
PASS apps/agents-server/src/utils/userChatTimeout/userChatTimeoutStore/repeatFiredUserChatTimeout.test.ts
PASS src/utils/validators/parameterName/validateParameterName.test.ts
PASS scripts/run-codex-prompts/testing/runPromptWithTestFeedback.test.ts
  ● Console

    console.info
      [90mRunning verification command after attempt #1: npm run test[39m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:106:17)

    console.warn
      [33mVerification failed for prompts/feature.md#1 on attempt #1. Sending feedback to github-copilot and retrying...[39m

    [0m [90m 135 |[39m         }
     [90m 136 |[39m
    [31m[1m>[22m[39m[90m 137 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 138 |[39m             colors[33m.[39myellow(
     [90m 139 |[39m                 [32m`Verification failed for ${options.promptLabel} on attempt #${attemptCount}. Sending feedback to ${options.runner.name} and retrying...`[39m[33m,[39m
     [90m 140 |[39m             )[33m,[39m[0m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:137:17)
      at Object.<anonymous> (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.test.ts:85:24)

    console.info
      [90mRunning verification command after attempt #2: npm run test[39m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:106:17)

    console.info
      [90mRunning verification command after attempt #1: npm run test[39m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:106:17)

    console.warn
      [33mVerification failed for prompts/feature.md#1 on attempt #1. Sending feedback to github-copilot and retrying...[39m

    [0m [90m 135 |[39m         }
     [90m 136 |[39m
    [31m[1m>[22m[39m[90m 137 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 138 |[39m             colors[33m.[39myellow(
     [90m 139 |[39m                 [32m`Verification failed for ${options.promptLabel} on attempt #${attemptCount}. Sending feedback to ${options.runner.name} and retrying...`[39m[33m,[39m
     [90m 140 |[39m             )[33m,[39m[0m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:137:17)
      at Object.<anonymous> (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.test.ts:123:9)

    console.info
      [90mRunning verification command after attempt #2: npm run test[39m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:106:17)

    console.info
      [90mRunning verification command after attempt #1: npm run test[39m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:106:17)

    console.warn
      [33mVerification failed for prompts/feature.md#1 on attempt #1. Sending feedback to github-copilot and retrying...[39m

    [0m [90m 135 |[39m         }
     [90m 136 |[39m
    [31m[1m>[22m[39m[90m 137 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 138 |[39m             colors[33m.[39myellow(
     [90m 139 |[39m                 [32m`Verification failed for ${options.promptLabel} on attempt #${attemptCount}. Sending feedback to ${options.runner.name} and retrying...`[39m[33m,[39m
     [90m 140 |[39m             )[33m,[39m[0m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:137:17)
      at Object.<anonymous> (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.test.ts:165:9)

    console.info
      [90mRunning verification command after attempt #2: npm run test[39m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:106:17)

    console.warn
      [33mVerification failed for prompts/feature.md#1 on attempt #2. Sending feedback to github-copilot and retrying...[39m

    [0m [90m 135 |[39m         }
     [90m 136 |[39m
    [31m[1m>[22m[39m[90m 137 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 138 |[39m             colors[33m.[39myellow(
     [90m 139 |[39m                 [32m`Verification failed for ${options.promptLabel} on attempt #${attemptCount}. Sending feedback to ${options.runner.name} and retrying...`[39m[33m,[39m
     [90m 140 |[39m             )[33m,[39m[0m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:137:17)
      at Object.<anonymous> (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.test.ts:165:9)

    console.info
      [90mRunning verification command after attempt #3: npm run test[39m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:106:17)

    console.error
      [31mVerification failed for prompts/feature.md#1 after 3 attempts.[39m

    [0m [90m 121 |[39m
     [90m 122 |[39m         [36mif[39m (attemptCount [33m>=[39m [33mMAX_PROMPT_TEST_ATTEMPTS[39m) {
    [31m[1m>[22m[39m[90m 123 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 124 |[39m                 colors[33m.[39mred([32m`Verification failed for ${options.promptLabel} after ${attemptCount} attempts.`[39m)[33m,[39m
     [90m 125 |[39m             )[33m;[39m
     [90m 126 |[39m[0m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:123:21)
      at Object.<anonymous> (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.test.ts:165:9)

PASS scripts/run-codex-prompts/common/resolveCoderContext.test.ts
PASS apps/agents-server/src/utils/messages/humanizeOutboundEmail.test.ts
PASS apps/agents-server/src/utils/messages/sendMessage.test.ts
PASS apps/agents-server/src/utils/agentReferenceResolver/resolveCoreAgentAwareMissingReferences.test.ts
PASS scripts/find-refactor-candidates/resolveRefactorCandidateProject.test.ts
PASS src/speech-recognition/OpenAiSpeechRecognition.test.ts
PASS apps/agents-server/src/components/NewAgentDialog/ManGoNewAgentWizard/services/bookService.test.ts
PASS apps/agents-server/src/utils/userChatTimeout/plannedMessageSchedule/parsePlannedMessageSchedule.test.ts
PASS apps/agents-server/src/utils/shibbolethAuthentication/resolveShibbolethPublicRequestUrl.test.ts
PASS src/utils/normalization/capitalize.test.ts
PASS src/utils/expectation-counters/countLines.test.ts
PASS src/utils/markdown/extractAllBlocksFromMarkdown.test.ts
PASS apps/agents-server/src/utils/resolveInternalServerOrigin.test.ts
PASS src/executables/browsers/locateFirefox.test.ts
PASS apps/agents-server/src/constants/defaultAgentAvatarVisual.test.ts
PASS apps/agents-server/src/app/admin/files/filesGalleryRows.test.ts
PASS src/utils/parameters/extractParameterNames.test.ts
PASS src/execution/utils/usageToWorktime.test.ts
PASS src/utils/validators/url/isValidAgentUrl.test.ts
PASS apps/agents-server/src/app/admin/messages/send-email/emailTestingAccess.test.ts
PASS apps/agents-server/src/utils/cloudflare/resolveCloudflareDnsRecordApplyAction.test.ts
PASS apps/agents-server/src/utils/userChat/getUserChatForJobRunner.test.ts
PASS src/book-components/BookEditor/BookEditorMonacoTokenization.test.ts
PASS apps/agents-server/src/components/NewAgentDialog/resolveWizardTeamReference.test.ts
PASS scripts/run-codex-prompts/runners/openai-codex/OpenAiCodexRunner.test.ts
  ● Console

    console.warn
      [33m[codex] Rate limit/quota detected (Error: rate limit reached). Retry #1 in 0s at 2026-09-17T00:46:44.376Z.[39m

    [0m [90m 173 |[39m
     [90m 174 |[39m                 [36mif[39m (options[33m.[39mshouldPrintLiveOutput [33m?[39m[33m?[39m [36mtrue[39m) {
    [31m[1m>[22m[39m[90m 175 |[39m                     console[33m.[39mwarn(
     [90m     |[39m                             [31m[1m^[22m[39m
     [90m 176 |[39m                         colors[33m.[39myellow(
     [90m 177 |[39m                             [32m`[codex] Rate limit/quota detected (${summary}). Retry #${retryIndex} in ${formatDelay([39m
     [90m 178 |[39m [32m                                delayMs,[39m[0m

      at OpenAiCodexRunner.runPrompt (scripts/run-codex-prompts/runners/openai-codex/OpenAiCodexRunner.ts:175:29)

PASS apps/agents-server/src/utils/chatHistoryMessage.test.ts
PASS apps/agents-server/src/utils/userChat/heartbeatUserChatJob.test.ts
PASS src/utils/files/isFileExisting.test.ts
PASS src/avatars/visuals/octopus3AvatarVisual.test.ts
PASS apps/agents-server/src/utils/serverRegistry.test.ts
PASS apps/agents-server/src/utils/agentChatInputPlaceholder.test.ts
PASS scripts/run-codex-prompts/prompts/loadPromptFiles.test.ts
PASS scripts/run-codex-prompts/prompts/buildScriptPath.test.ts
PASS apps/agents-server/src/utils/agentProjects/agentProjectHrefs.test.ts
PASS apps/agents-server/src/utils/shareTarget.test.ts
PASS apps/agents-server/src/utils/authenticationAttemptRateLimit.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/api/chat/resolveAgentChatRequestIdentities.test.ts
PASS scripts/run-codex-prompts/isolation/markPromptIsolationMergeFailed.test.ts
PASS scripts/run-codex-prompts/prompts/buildPromptRunTraceContent.test.ts
PASS apps/agents-server/src/utils/userChat/runDurableUserChatJobWorkerTick.test.ts
PASS apps/agents-server/src/utils/userChat/resolveUserChatWorkerInternalToken.test.ts
PASS apps/agents-server/src/utils/resourceMonitor/resolveServerResourceWarningStatus.test.ts
PASS apps/agents-server/src/components/Homepage/loadFederatedServerAgents.test.ts
PASS apps/agents-server/src/utils/getAdminChatTasksResponse/mapVpsSelfUpdateJobToAdminChatTask.test.ts
PASS scripts/run-codex-prompts/migrations/detectDestructiveSqlStatements.test.ts
PASS apps/agents-server/src/utils/agentProjects/createAgentProjectDnsRecords.test.ts
PASS src/execution/utils/checkExpectations.test.ts
PASS apps/agents-server/src/components/Header/buildHeaderMenuItems.test.ts
PASS src/executables/browsers/locateChrome.test.ts
PASS src/errors/utils/serializeError.test.ts
PASS apps/agents-server/src/database/selectPrefixesForMigration.test.ts
PASS apps/agents-server/src/utils/adminChatTaskLinks.test.ts
PASS src/cli/cli-commands/coder/getTypescriptModule.test.ts
PASS scripts/run-codex-prompts/common/formatUnknownErrorDetails.test.ts
PASS src/utils/color/utils/colorHueDistance.test.ts
PASS scripts/run-codex-prompts/runners/common/createAuthenticationAwarePromptRunner.test.ts
PASS src/commitments/FORMAT/FORMAT.test.ts
PASS apps/agents-server/src/app/api/admin/default-agents/reinstate/route.test.ts
PASS scripts/run-codex-prompts/ui/refreshCoderRunUiSubscriptionUsage.test.ts
PASS scripts/run-codex-prompts/git/gitLongPathsSupport.test.ts
PASS scripts/verify-prompts/$orderPromptFiles.test.ts
PASS apps/agents-server/src/utils/userChat/persistFrozenUserChat.test.ts
PASS src/book-3.0/Book.test.ts
PASS src/cli/cli-commands/agent-folder/run.test.ts
PASS src/cli/cli-commands/agent/run.test.ts
PASS src/book-components/Chat/utils/parseMessageButtons.test.ts
PASS apps/agents-server/src/utils/publicUser.test.ts
PASS src/executables/locateApp.test.ts
PASS src/execution/translation/automatic-translate/automatic-translators/utils/extractMultiplicatedOccurrence.test.ts
PASS src/cli/cli-commands/common/disk-space/$assertSufficientFreeDiskSpace.test.ts
PASS src/cli/cli-commands/common/questionsCliOptions.test.ts
PASS apps/agents-server/src/components/NewAgentDialog/ManGoNewAgentWizard/services/createManGoOpenEditorRequest.test.ts
PASS apps/agents-server/src/utils/chatExport/renderHtmlToPdfOnServer.test.ts
PASS src/utils/normalization/unwrapResult.test.ts
PASS src/utils/files/decodeAttachmentAsText.test.ts
PASS src/executables/apps/locatePandoc.test.ts
PASS src/executables/apps/locateVscode.test.ts
PASS apps/agents-server/src/utils/userThemeModeSettings.test.ts
PASS apps/agents-server/src/utils/chatEnterBehaviorSettings.test.ts
PASS src/cli/cli-commands/common/npm/extractNpmPackageVersionFromOutput.test.ts
PASS src/cli/cli-commands/coder/list.test.ts
PASS scripts/run-agent-messages/messages/buildAgentMessagePrompt.test.ts
PASS apps/agents-server/src/utils/vpsSelfUpdate/vpsSelfUpdateCron.test.ts
PASS src/utils/agent-message-runtime/resolveAgentMessageTouchedExternalSources.test.ts
PASS book/scripts/import-markdown/increaseHeadings.test.ts
PASS src/utils/normalization/parseKeywords.test.ts
PASS src/utils/expectation-counters/countCharacters.test.ts
PASS src/postprocessing/utils/extractJsonBlock.test.ts
PASS src/collection/agent-collection/CreateAgentInput.test.ts
PASS apps/agents-server/src/utils/localChatRunner/LocalUserChatJobMetadata.test.ts
PASS src/utils/editable/edit-pipeline-string/deflatePipeline.test.ts
PASS apps/agents-server/src/components/Header/resolveHeaderSystemActivities.test.ts
PASS src/commitments/USE_CALENDAR/USE_CALENDAR.test.ts
PASS apps/agents-server/src/utils/dnsRecords/createDnsZoneFileExport.test.ts
PASS src/llm-providers/anthropic-claude/computeAnthropicClaudeUsage.test.ts
PASS src/cli/cli-commands/coder/find-refactor-candidates.test.ts
PASS src/utils/validators/url/isUrlOnPrivateNetwork.test.ts
PASS apps/agents-server/src/utils/stalwart/stalwartBootstrap.test.ts
PASS scripts/repair-imports/utils/splitArrayIntoChunks.test.ts
PASS scripts/run-codex-prompts/runners/openai-codex/parseCodexLoginMethodFromOutput.test.ts
PASS src/utils/chat/chatAttachments.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/projectReferenceActions.test.ts
PASS scripts/run-codex-prompts/runners/openai-codex/buildCodexUsageFromOutput.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/AgentProfileChat.navigation.test.ts
PASS src/book-components/Chat/utils/toolCallParsing.test.ts
PASS scripts/run-agent-messages/main/listLocalAgentRunnerProjects.test.ts
PASS apps/agents-server/src/utils/agentProjects/readAgentProjectReadme.test.ts
PASS src/utils/normalization/parseKeywordsFromString.test.ts
PASS src/cli/cli-commands/common/$askForConfirmation.test.ts
PASS src/cli/cli-commands/coder/boilerplateCount.test.ts
PASS scripts/run-codex-prompts/common/createFreeDiskSpaceGuard.test.ts
PASS apps/agents-server/src/components/ViewportHeightController/resolveVisibleViewportHeight.test.ts
PASS src/utils/serialization/serializeToPromptbookJavascript.test.ts
PASS src/cli/cli-commands/common/npm/$resolveLatestNpmPackageVersion.test.ts
PASS src/cli/cli-commands/common/harness/HarnessDefinition.test.ts
PASS src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerChildEnvironment.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/chat/useCanonicalAgentChatPanelState.test.ts
PASS apps/agents-server/src/utils/normalization/normalizeUploadFilename.test.ts
PASS apps/agents-server/src/utils/agentRouting/agentRouteHrefs.test.ts
PASS src/utils/markdown/escapeMarkdownBlock.test.ts
PASS src/conversion/utils/extractParameterNamesFromTask.test.ts
PASS src/book-2.0/agent-source/computeAgentHash.test.ts
PASS apps/agents-server/src/utils/sendUserChatPushNotification.test.ts
PASS apps/agents-server/src/tools/$provideServer.test.ts
PASS src/cli/cli-commands/common/promptbook-cli/$updatePromptbookCliInstallation.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/chat/mergeCanonicalChatMessagesWithPendingOutboundMessages.test.ts
PASS scripts/run-codex-prompts/runners/openai-codex/CodexFailureHandling.test.ts
PASS scripts/run-codex-prompts/runners/github-copilot/buildGitHubCopilotScript.test.ts
PASS src/formats/json/utils/isValidJsonString.test.ts
PASS apps/agents-server/src/utils/normalization/filenameToPrompt.test.ts
PASS scripts/run-codex-prompts/common/waitForSkippableWorldTimeDeadline.test.ts
PASS apps/agents-server/src/utils/chat/createWordLikeDeltas.test.ts
PASS apps/agents-server/src/components/_utils/generateMetaTxt.test.ts
PASS scripts/find-refactor-candidates/selectMostImportantRefactorCandidates.test.ts
PASS src/utils/markdown/flattenMarkdown.test.ts
PASS apps/agents-server/src/utils/agentIdentifier.test.ts
PASS src/cli/cli-commands/common/disk-space/formatFreeDiskSpaceBytes.test.ts
PASS src/book-components/Chat/utils/parseImagePrompts.test.ts
PASS src/book-components/Chat/utils/decodeJsonUnicodeEscapesInMarkdownText.test.ts
PASS scripts/run-codex-prompts/prompts/promptRunnerAttribution.test.ts
PASS scripts/run-codex-prompts/git/agentGitIdentity.test.ts
PASS apps/agents-server/src/utils/shibbolethAuthentication/resolveShibbolethServiceProviderUrls.test.ts
PASS apps/agents-server/src/utils/speech-to-text/SpeechToTextFailoverRecognition.test.ts
PASS src/utils/markdown/extractOneBlockFromMarkdown.test.ts
PASS src/utils/markdown/extractAllListItemsFromMarkdown.test.ts
PASS src/cli/other/install.test.ts
PASS src/book-components/Chat/save/react/reactSaveFormatDefinition.test.ts
PASS src/book-components/Chat/utils/getToolCallChipletInfo.timeout.test.ts
PASS scripts/run-codex-prompts/common/applyCoderRunControlKey.test.ts
PASS apps/agents-server/src/utils/userChat/UserChatScopeError.test.ts
PASS src/utils/sets/difference.test.ts
PASS src/utils/markdown/trimCodeBlock.test.ts
PASS src/utils/color/Color.test.ts
PASS src/formats/xml/utils/isValidXmlString.test.ts
PASS src/file-security-checkers/virustotal/VirusTotalFileSecurityChecker.test.ts
PASS src/book-components/Chat/utils/timeoutToolCallPresentation.test.ts
PASS apps/agents-server/src/utils/userChat/createUserChatRunningActivity.test.ts
PASS src/avatars/visuals/orbAvatarVisual.test.ts
PASS apps/agents-server/src/utils/stalwart/stalwartJmapValues.test.ts
PASS src/book-3.0/AgentMessageRunReport.test.ts
PASS src/execution/resolveTaskTldr.test.ts
PASS src/utils/validators/url/normalizeDomainForMatching.test.ts
PASS book/scripts/import-markdown/removeComments.test.ts
PASS scripts/run-codex-prompts/prompts/priorityFilter.test.ts
PASS scripts/run-codex-prompts/prompts/buildPromptRunTracePath.test.ts
PASS scripts/run-codex-prompts/git/commitInitializedAgentBooks.test.ts
PASS apps/agents-server/src/utils/chat/validateChatMessageContent.test.ts
PASS src/utils/agent-message-runtime/resolveAgentMessageTouchedProjectNames.test.ts
PASS src/avatars/visuals/avatarVisualRegistry.test.ts
PASS apps/agents-server/src/utils/standaloneVpsDnsDiagnostics.test.ts
PASS src/avatars/avatarRenderingUtils.test.ts
PASS src/cli/cli-commands/common/disk-space/$readFreeDiskSpaceStatus.test.ts
PASS src/cli/common/$deprecateCliCommand.test.ts
PASS scripts/run-codex-prompts/prompts/priorityFiltering.test.ts
PASS apps/agents-server/src/utils/userChatTimeout/plannedMessageSchedule/resolvePlannedMessageDueAt.test.ts
PASS scripts/run-codex-prompts/common/waitUntilWorldTimeDeadline.test.ts
PASS scripts/run-codex-prompts/isolation/coderIsolationNaming.test.ts
PASS apps/agents-server/src/utils/agentProjects/parseAgentProjectIndexHtml.test.ts
PASS src/utils/serialization/jsonStringsToJsons.test.ts
PASS src/utils/serialization/deepClone.test.ts
PASS src/utils/misc/debounce.test.ts
PASS src/utils/knowledge/inlineKnowledgeSource.test.ts
PASS src/avatars/avatarInteractionUtils.test.ts
PASS src/scrapers/_common/utils/getScraperIntermediateSource.test.ts
PASS scripts/run-codex-prompts/isolation/coderIsolationCheckoutFailureReport.test.ts
PASS scripts/run-codex-prompts/git/pullLatestChanges.test.ts
PASS scripts/run-codex-prompts/git/coderGitSync.test.ts
PASS apps/agents-server/src/utils/userChatTimeout/createUserChatTimeoutActivity.test.ts
PASS apps/agents-server/src/components/AgentProjects/resolveLatestCompletedAgentMessageKey.test.ts
PASS src/utils/markdown/removeMarkdownFormatting.test.ts
PASS src/utils/normalization/titleToName.test.ts
PASS src/utils/expectation-counters/countPages.test.ts
PASS src/utils/markdown/promptbookifyAiText.test.ts
PASS src/utils/validators/filePath/isValidFilePath.test.ts
PASS src/book-components/Chat/Chat/insertDictationChunk.test.ts
PASS src/utils/normalization/searchKeywords.test.ts
PASS src/utils/markdown/createMarkdownChart.test.ts
PASS src/utils/expectation-counters/countSentences.test.ts
PASS src/commitments/ACTION/ACTION.test.ts
PASS src/config.test.ts
PASS src/utils/parameters/valueToString.test.ts
PASS src/commitments/USE_PROJECT/projectReference.test.ts
PASS apps/agents-server/src/utils/userLocationPromptParameter.test.ts
PASS apps/agents-server/src/utils/parsePositiveUserId.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/chat/formatChatTimeoutRemainingTime.test.ts
PASS src/book-components/Chat/utils/getToolCallChipletInfo.test.ts
PASS src/book-components/Chat/utils/splitMessageContentIntoSegments.test.ts
PASS scripts/run-codex-prompts/prompts/printStats.test.ts
PASS apps/agents-server/src/utils/localization/formatServerLanguageHumanReadableDate.test.ts
PASS apps/agents-server/src/utils/createChatStreamHandler.test.ts
PASS apps/agents-server/src/constants/newAgentWizard.test.ts
PASS src/cli/cli-commands/common/disk-space/resolveFreeDiskSpaceLevel.test.ts
PASS src/cli/cli-commands/common/harness/resolveHarnessInstallationMethodFromPaths.test.ts
PASS src/utils/validators/email/isValidEmail.test.ts
PASS src/book-components/Chat/utils/parseCitationsFromContent.test.ts
PASS scripts/run-codex-prompts/ui/CoderRunUiState.test.ts
PASS scripts/run-codex-prompts/ui/buildCoderRunUiTerminalFrameUpdate.test.ts
PASS src/utils/parameters/numberToString.test.ts
PASS src/executables/platforms/locateAppOnWindows.test.ts
PASS src/conversion/pipelineJsonToString.test.ts
PASS src/cli/cli-commands/common/promptbook-cli/$checkPromptbookCliInstallations.test.ts
PASS src/cli/cli-commands/common/harness/resolveHarnessUpdatePlan.test.ts
PASS src/utils/validators/url/isValidUrl.test.ts
PASS src/book-components/Chat/utils/citationHelpers.test.ts
PASS apps/agents-server/src/utils/userChat/triggerUserChatJobWorker.test.ts
PASS src/utils/normalization/normalizeTo_PascalCase.test.ts
PASS src/utils/knowledge/simplifyKnowledgeLabel.test.ts
PASS src/commitments/TEMPLATE/TEMPLATE.test.ts
PASS src/cli/cli-commands/common/harness/$checkHarnessInstallation.test.ts
PASS src/cli/common/$requireCliSubcommand.test.ts
PASS scripts/run-codex-prompts/runners/gemini/gemini-pricing.test.ts
PASS scripts/run-codex-prompts/runners/gemini/buildGeminiScript.test.ts
PASS scripts/run-codex-prompts/runners/common/modelPricing.test.ts
PASS scripts/run-codex-prompts/runners/claude-code/buildClaudeScript.test.ts
PASS src/utils/normalization/normalizeTo_snake_case.test.ts
PASS src/utils/validators/url/isValidPipelineUrl.test.ts
PASS src/executables/browsers/locateDefaultSystemBrowser.test.ts
PASS apps/agents-server/src/utils/resolveCurrentOrInternalServerOrigin.test.ts
PASS apps/agents-server/src/utils/getAdminChatTasksResponse/getAdminChatTasks/compareAdminChatTasks.test.ts
PASS src/utils/validators/javascriptName/isValidJavascriptName.test.ts
PASS apps/agents-server/src/utils/cloudflare/createCloudflareDnsRecordRequestBody.test.ts
PASS src/utils/sets/union.test.ts
PASS src/commitments/MODEL/MODEL.test.ts
PASS src/book-2.0/agent-source/normalizeAgentName.test.ts
PASS src/cli/cli-commands/common/harness/formatHarnessManualUpdateInstruction.test.ts
PASS apps/agents-server/src/utils/userChat/userChatReplies.test.ts
PASS src/cli/cli-commands/common/coderGitSyncCliOptions.test.ts
PASS apps/agents-server/src/utils/email/agentEmailAddress.test.ts
PASS src/utils/normalization/nameToUriParts.test.ts
PASS src/execution/utils/formatUsagePrice.test.ts
PASS src/book-2.0/agent-source/deduplicateSystemMessage.test.ts
PASS src/book-2.0/agent-source/agentSourceVisibility.test.ts
PASS apps/agents-server/src/utils/session.test.ts
PASS scripts/run-codex-prompts/runners/qwen-code/buildQwenCodeScript.test.ts
PASS src/cli/common/$hideCliCommandFromHelp.test.ts
PASS apps/agents-server/src/utils/agentProjects/listAgentProjectChatReferences.test.ts
PASS scripts/run-codex-prompts/common/ProgressiveBackoff.test.ts
PASS apps/agents-server/src/components/NewAgentDialog/NewAgentWizardState.test.ts
PASS src/utils/normalization/removeDiacritics.test.ts
PASS src/utils/normalization/isValidKeyword.test.ts
PASS src/utils/markdown/addAutoGeneratedSection.test.ts
PASS src/llm-providers/_common/utils/removeUnsupportedModelRequirements.test.ts
PASS src/book-components/Chat/utils/resolveCitationUrl.test.ts
PASS src/utils/normalization/removeEmojis.test.ts
PASS src/utils/markdown/createMarkdownTable.test.ts
PASS src/conversion/validation/_importPipeline.test.ts
PASS src/cli/cli-commands/common/npm/buildNpmPackageInstallCommand.test.ts
PASS scripts/run-codex-prompts/runners/qwen-code/qwen-code-pricing.test.ts
PASS src/utils/editable/edit-pipeline-string/removePipelineCommand.test.ts
PASS scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
PASS scripts/run-codex-prompts/prompts/formatRunnerSignature.test.ts
PASS src/utils/filesystem/promptbookTemporaryPath.test.ts
PASS scripts/run-codex-prompts/common/buildCoderRunProgressSnapshot.test.ts
PASS scripts/find-refactor-candidates/RefactorCandidateLevel.test.ts
PASS src/book-3.0/AgentPlannedMessagesSidecar.test.ts
PASS src/cli/cli-commands/common/npm/isNpmPackageVersionOutdated.test.ts
PASS src/utils/validators/uuid/isValidUuid.test.ts
PASS apps/agents-server/src/components/UsersList/generateSecurePassword.test.ts
PASS src/book-components/Chat/utils/createCitationFootnoteRenderModel.test.ts
PASS src/utils/files/extensionToMimeType.test.ts
PASS src/utils/normalization/normalize-to-kebab-case.test.ts
PASS scripts/verify-prompts/VerifyPromptsOrder.test.ts
PASS src/transpilers/_common/formatUsedToolFunctions.test.ts
PASS apps/agents-server/src/tools/createServerChromiumLaunchOptions.test.ts
PASS scripts/run-codex-prompts/runners/claude-code/parseClaudeCodeSubscriptionUsage.test.ts
PASS apps/agents-server/src/app/admin/_components/adminTableSorting.test.ts
PASS src/utils/validators/filePath/isRootPath.test.ts
PASS src/formats/csv/utils/isValidCsvString.test.ts
PASS src/book-components/Chat/utils/sanitizeStreamingMessageContent.test.ts
PASS src/book-components/Chat/CodeBlock/resolveCodeBlockLanguage.test.ts
PASS src/book-components/Chat/Chat/learnDictationDictionary.test.ts
PASS src/utils/normalization/removeQuotes.test.ts
PASS src/utils/markdown/removeMarkdownLinks.test.ts
PASS src/utils/normalization/constructImageFilename.test.ts
PASS src/utils/files/isDirectoryExisting.test.ts
PASS src/commitments/META_DISCLAIMER/META_DISCLAIMER.test.ts
PASS apps/agents-server/src/utils/createAgentWithDefaultVisibility.test.ts
PASS src/book-components/Chat/utils/agentProjectToolCall.test.ts
PASS src/collection/agent-collection/constructors/agent-collection-in-supabase/buildAgentNameOrPermanentIdFilter.test.ts
PASS src/utils/validators/url/extractUrlsFromText.test.ts
PASS src/storage/file-cache-storage/utils/nameToSubfolderPath.test.ts
PASS src/utils/take/take.test.ts
PASS src/utils/validators/semanticVersion/isValidSemanticVersion.test.ts
PASS src/llm-providers/_common/utils/pricing.test.ts
PASS apps/agents-server/src/utils/agentBook/createAgentBookDownloadFilename.test.ts
PASS src/utils/normalization/nameToUriPart.test.ts
PASS src/utils/sets/intersection.test.ts
PASS src/utils/normalization/decapitalize.test.ts
PASS src/executables/apps/locateLibreoffice.test.ts
PASS apps/agents-server/src/utils/chatFeedbackMode.test.ts
PASS src/book-components/Chat/utils/thinkingMessageVariants.test.ts
PASS scripts/run-codex-prompts/common/appendCoderContext.test.ts
PASS apps/agents-server/src/utils/transpilers/getTranspiledCodeFileMetadata.test.ts
PASS apps/agents-server/src/utils/stalwart/createEmailDnsInstructions.test.ts
PASS src/utils/serialization/asSerializable.test.ts
PASS src/utils/markdown/trimEndOfCodeBlock.test.ts
PASS src/commitments/_common/getCommitmentNoticeMetadata.test.ts
PASS apps/agents-server/src/utils/standaloneVpsRawIpBootstrap.test.ts
PASS src/utils/files/getFileExtension.test.ts
PASS src/utils/validators/semanticVersion/isValidPromptbookVersion.test.ts
PASS scripts/run-codex-prompts/runners/common/extractHarnessAuthenticationFailureReason.test.ts
PASS src/utils/normalization/normalizeWhitespaces.test.ts
PASS src/utils/normalization/suffixUrl.test.ts
PASS src/commitments/MESSAGE_SUFFIX/MESSAGE_SUFFIX.test.ts
PASS src/utils/normalization/normalizeTo_SCREAMING_CASE.test.ts
PASS scripts/run-codex-prompts/runners/openai-codex/getCodexSubscriptionUsage.test.ts
PASS apps/agents-server/src/utils/pagePreviewBrowserSessions.test.ts
PASS apps/agents-server/src/constants/chatVisualMode.test.ts
PASS src/book-components/Chat/Chat/refineFinalDictationChunk.test.ts
A worker process has failed to exit gracefully and has been force exited. This is likely caused by tests leaking due to improper teardown. Try running with --detectOpenHandles to find leaks. Active timers can also cause this, ensure that .unref() was called on them.

Test Suites: 765 passed, 765 total
Tests:       7 todo, 3400 passed, 3407 total
Snapshots:   0 total
Time:        382.949 s
Ran all test suites.
Force exiting Jest: Have you considered using `--detectOpenHandles` to detect async operations that kept running after all tests finished?

> lint
> eslint src/ --max-warnings=0 --cache --cache-location ./node_modules/.cache/eslint/agents-server-src.json


> pretest-build
> node ./scripts/kill-port.js 4021 4440

No process is listening on port 4021.
No process is listening on port 4440.

> test-build
> npm run build -- --no-sentry-upload


> prebuild
> npm run generate-reserved-paths && node ./scripts/clearNextGeneratedTypes.js && node ./scripts/kill-port.js 4440


> generate-reserved-paths
> ts-node --transpile-only ./scripts/generate-reserved-paths/generate-reserved-paths.ts

Kept C:\Users\me\work\ai\promptbook\apps\agents-server\src\generated\reservedPaths.ts unchanged with 31 reserved paths.
No process is listening on port 4440.

> build
> node ./scripts/build-agents-server.js --no-sentry-upload

   ▲ Next.js 15.4.11
   - Environments: .env
   - Experiments (use with caution):
     ✓ externalDir
     ✓ webpackBuildWorker
     · clientTraceMetadata
     ✓ parallelServerBuildTraces

   Creating an optimized production build ...
 ⚠ Compiled with warnings in 7.8min

../../node_modules/@ai-sdk/google/node_modules/@ai-sdk/provider-utils/dist/index.js
Critical dependency: the request of a dependency is an expression

Import trace for requested module:
../../node_modules/@ai-sdk/google/node_modules/@ai-sdk/provider-utils/dist/index.js
../../node_modules/@ai-sdk/google/dist/index.js
../../src/llm-providers/google/createGoogleExecutionTools.ts
../../src/_packages/google.index.ts
./src/tools/$provideExecutionToolsForServer.ts
./src/app/agents/[agentName]/images/default-avatar.png/route.ts

<w> [webpack.cache.PackFileCacheStrategy] Serializing big strings (127kiB) impacts deserialization performance (consider using Buffer instead and decode when needed)
<w> [webpack.cache.PackFileCacheStrategy] Serializing big strings (139kiB) impacts deserialization performance (consider using Buffer instead and decode when needed)
<w> [webpack.cache.PackFileCacheStrategy] Serializing big strings (126kiB) impacts deserialization performance (consider using Buffer instead and decode when needed)
 ⚠ Compiled with warnings in 25.0s

./src/utils/isIpAllowed.ts
A Node.js module is loaded ('net' at line 1) which is not supported in the Edge Runtime.
Learn More: https://nextjs.org/docs/messages/node-module-in-edge-runtime

Import trace for requested module:
./src/utils/isIpAllowed.ts
./src/middleware/createMiddlewareRequestContext.ts

../../node_modules/@supabase/realtime-js/dist/module/lib/websocket-factory.js
A Node.js API is used (process.versions at line: 32) which is not supported in the Edge Runtime.
Learn more: https://nextjs.org/docs/api-reference/edge-runtime

Import trace for requested module:
../../node_modules/@supabase/realtime-js/dist/module/lib/websocket-factory.js
../../node_modules/@supabase/realtime-js/dist/module/index.js
../../node_modules/@supabase/supabase-js/dist/module/index.js
./src/middleware/createMiddlewareRequestContext/getMiddlewareSupabase.ts
./src/middleware/createMiddlewareRequestContext.ts

../../node_modules/@supabase/supabase-js/dist/module/index.js
A Node.js API is used (process.version at line: 24) which is not supported in the Edge Runtime.
Learn more: https://nextjs.org/docs/api-reference/edge-runtime

Import trace for requested module:
../../node_modules/@supabase/supabase-js/dist/module/index.js
./src/middleware/createMiddlewareRequestContext/getMiddlewareSupabase.ts
./src/middleware/createMiddlewareRequestContext.ts

../../src/utils/normalization/titleToName.ts
A Node.js module is loaded ('path' at line 1) which is not supported in the Edge Runtime.
Learn More: https://nextjs.org/docs/messages/node-module-in-edge-runtime

Import trace for requested module:
../../src/utils/normalization/titleToName.ts
../../src/book-2.0/agent-source/normalizeAgentName.ts
./src/utils/agentReferenceResolver/createServerAgentReferenceResolver.ts
./src/utils/customDomainRouting.ts
./src/middleware/createMiddlewareRequestContext/resolveMiddlewareServerRouting.ts
./src/middleware/createMiddlewareRequestContext.ts

Client network socket disconnected before secure TLS connection was established

Retrying 1/3...
read ECONNRESET

Retrying 1/3...
 ✓ Compiled successfully in 9.4min
   Linting and checking validity of types ...
   Collecting page data ...
 ⚠ Using edge runtime on a page currently disables static generation for that page
   Generating static pages (0/146) ...
   Generating static pages (36/146) 
   Generating static pages (72/146) 
   Generating static pages (109/146) 
 ✓ Generating static pages (146/146)
   Finalizing page optimization ...
   Collecting build traces ...

Route (app)                                                                     Size  First Load JS
┌ ƒ /                                                                        1.39 kB        1.53 MB
├ ƒ /_not-found                                                                601 B         183 kB
├ ƒ /[agentName]                                                               593 B         331 kB
├ ƒ /[agentName]/[...rest]                                                     601 B         183 kB
├ ƒ /admin/about                                                              1.7 kB        1.77 MB
├ ƒ /admin/api-tokens                                                        6.21 kB         250 kB
├ ƒ /admin/backup                                                            3.95 kB         228 kB
├ ƒ /admin/browser-test                                                      3.13 kB         237 kB
├ ƒ /admin/chat-feedback                                                     20.2 kB        1.66 MB
├ ƒ /admin/chat-history                                                      22.8 kB        1.67 MB
├ ƒ /admin/core-agents                                                       2.41 kB        1.77 MB
├ ƒ /admin/custom-css                                                        9.52 kB         260 kB
├ ƒ /admin/custom-js                                                         11.3 kB         261 kB
├ ƒ /admin/email-server                                                      2.44 kB         232 kB
├ ƒ /admin/error-simulation                                                  4.82 kB         229 kB
├ ƒ /admin/files                                                             7.47 kB         261 kB
├ ƒ /admin/image-generator-test                                              13.9 kB         267 kB
├ ƒ /admin/images                                                            8.14 kB         261 kB
├ ƒ /admin/internal-s3                                                       1.65 kB         226 kB
├ ƒ /admin/limits                                                            2.31 kB         230 kB
├ ƒ /admin/login-methods/shibboleth                                           5.3 kB         249 kB
├ ƒ /admin/messages                                                          9.38 kB         253 kB
├ ƒ /admin/messages/send-email                                               5.84 kB         234 kB
├ ƒ /admin/metadata                                                          18.6 kB         303 kB
├ ƒ /admin/models                                                            1.65 kB         226 kB
├ ƒ /admin/planned-messages                                                  13.2 kB         260 kB
├ ƒ /admin/projects                                                            142 B         229 kB
├ ƒ /admin/search-engine-test                                                3.25 kB         228 kB
├ ƒ /admin/task-manager                                                        152 B         265 kB
├ ƒ /admin/task-manager-vps                                                    601 B         183 kB
├ ƒ /admin/task-manager/[taskId]                                             2.34 kB         260 kB
├ ƒ /admin/tool-limits                                                       1.64 kB         226 kB
├ ƒ /admin/transcriptions                                                    6.52 kB         237 kB
├ ƒ /admin/usage                                                             9.87 kB         263 kB
├ ƒ /admin/users                                                             2.08 kB         252 kB
├ ƒ /admin/users/[userId]                                                    6.57 kB         250 kB
├ ƒ /admin/voice-input-test                                                  5.13 kB         230 kB
├ ƒ /agents                                                                  1.39 kB        1.53 MB
├ ƒ /agents/[agentName]                                                        251 B         331 kB
├ ƒ /agents/[agentName]/api/book                                               601 B         183 kB
├ ƒ /agents/[agentName]/api/book/download                                      601 B         183 kB
├ ƒ /agents/[agentName]/api/book/history                                       601 B         183 kB
├ ƒ /agents/[agentName]/api/book/missing-agent                                 601 B         183 kB
├ ƒ /agents/[agentName]/api/book/reference-diagnostics                         601 B         183 kB
├ ƒ /agents/[agentName]/api/calendar-connections                               601 B         183 kB
├ ƒ /agents/[agentName]/api/calendar-connections/[connectionId]/disconnect     601 B         183 kB
├ ƒ /agents/[agentName]/api/calendar-events                                    601 B         183 kB
├ ƒ /agents/[agentName]/api/chat                                               601 B         183 kB
├ ƒ /agents/[agentName]/api/feedback                                           601 B         183 kB
├ ƒ /agents/[agentName]/api/mcp                                                601 B         183 kB
├ ƒ /agents/[agentName]/api/meta-disclaimer                                    601 B         183 kB
├ ƒ /agents/[agentName]/api/model-requirements                                 601 B         183 kB
├ ƒ /agents/[agentName]/api/model-requirements/system-message                  601 B         183 kB
├ ƒ /agents/[agentName]/api/openai/chat/completions                            601 B         183 kB
├ ƒ /agents/[agentName]/api/openai/models                                      601 B         183 kB
├ ƒ /agents/[agentName]/api/openai/v1/chat/completions                         601 B         183 kB
├ ƒ /agents/[agentName]/api/openai/v1/models                                   601 B         183 kB
├ ƒ /agents/[agentName]/api/openrouter/chat/completions                        601 B         183 kB
├ ƒ /agents/[agentName]/api/profile                                            601 B         183 kB
├ ƒ /agents/[agentName]/api/share-target/[shareTargetId]/consume               601 B         183 kB
├ ƒ /agents/[agentName]/api/timeouts                                           601 B         183 kB
├ ƒ /agents/[agentName]/api/timeouts/[timeoutId]                               601 B         183 kB
├ ƒ /agents/[agentName]/api/user-chats                                         601 B         183 kB
├ ƒ /agents/[agentName]/api/user-chats/[chatId]                                601 B         183 kB
├ ƒ /agents/[agentName]/api/user-chats/[chatId]/draft                          601 B         183 kB
├ ƒ /agents/[agentName]/api/user-chats/[chatId]/jobs/[jobId]/cancel            601 B         183 kB
├ ƒ /agents/[agentName]/api/user-chats/[chatId]/messages                       601 B         183 kB
├ ƒ /agents/[agentName]/api/user-chats/[chatId]/stream                         601 B         183 kB
├ ƒ /agents/[agentName]/api/user-chats/[chatId]/timeouts/[timeoutId]/cancel    601 B         183 kB
├ ƒ /agents/[agentName]/api/voice                                              601 B         183 kB
├ ƒ /agents/[agentName]/book                                                 6.19 kB         417 kB
├ ƒ /agents/[agentName]/book+chat                                            4.78 kB        1.81 MB
├ ƒ /agents/[agentName]/chat                                                 34.4 kB        1.83 MB
├ ƒ /agents/[agentName]/export-as-transpiled-code                             7.2 kB         380 kB
├ ƒ /agents/[agentName]/export-as-transpiled-code/api                          601 B         183 kB
├ ƒ /agents/[agentName]/export-as-transpiled-code/api/download                 601 B         183 kB
├ ƒ /agents/[agentName]/goal                                                 1.64 kB         226 kB
├ ƒ /agents/[agentName]/history                                              2.52 kB         227 kB
├ ƒ /agents/[agentName]/iframe                                                 601 B         183 kB
├ ƒ /agents/[agentName]/images                                                 172 B         184 kB
├ ƒ /agents/[agentName]/images/default-avatar.png                              601 B         183 kB
├ ƒ /agents/[agentName]/images/icon-256.png                                    601 B         183 kB
├ ƒ /agents/[agentName]/images/screenshot-fullhd.png                           601 B         183 kB
├ ƒ /agents/[agentName]/images/screenshot-phone.png                            601 B         183 kB
├ ƒ /agents/[agentName]/integration                                          10.7 kB         268 kB
├ ƒ /agents/[agentName]/opengraph-image                                        601 B         183 kB
├ ƒ /agents/[agentName]/projects                                               142 B         229 kB
├ ƒ /agents/[agentName]/projects/[projectName]                               3.34 kB        1.77 MB
├ ƒ /agents/[agentName]/projects/[projectName]/files/[...filePath]             601 B         183 kB
├ ƒ /agents/[agentName]/projects/[projectName]/vscode                          601 B         183 kB
├ ƒ /agents/[agentName]/share-target                                           601 B         183 kB
├ ƒ /agents/[agentName]/system-message                                       2.77 kB         376 kB
├ ƒ /agents/[agentName]/textarea                                             9.86 kB         248 kB
├ ƒ /agents/[agentName]/website-integration                                  2.46 kB        1.77 MB
├ ƒ /api/admin-email                                                           601 B         183 kB
├ ƒ /api/admin/backups/books                                                   601 B         183 kB
├ ƒ /api/admin/backups/server                                                  601 B         183 kB
├ ƒ /api/admin/chat-tasks                                                      601 B         183 kB
├ ƒ /api/admin/chat-tasks/[taskId]                                             601 B         183 kB
├ ƒ /api/admin/chat-tasks/[taskId]/cancel                                      601 B         183 kB
├ ƒ /api/admin/chat-tasks/[taskId]/retry                                       601 B         183 kB
├ ƒ /api/admin/chat-tasks/[taskId]/terminal                                    601 B         183 kB
├ ƒ /api/admin/chat-tasks/cancel-all                                           601 B         183 kB
├ ƒ /api/admin/cli-access                                                      601 B         183 kB
├ ƒ /api/admin/database/studio                                                 601 B         183 kB
├ ƒ /api/admin/default-agents/reinstate                                        601 B         183 kB
├ ƒ /api/admin/dns-records/cloudflare                                          601 B         183 kB
├ ƒ /api/admin/environment                                                     601 B         183 kB
├ ƒ /api/admin/error-simulation                                                601 B         183 kB
├ ƒ /api/admin/harness-auth                                                    601 B         183 kB
├ ƒ /api/admin/harness-auth/authentication                                     601 B         183 kB
├ ƒ /api/admin/limits                                                          601 B         183 kB
├ ƒ /api/admin/logs                                                            601 B         183 kB
├ ƒ /api/admin/planned-messages                                                601 B         183 kB
├ ƒ /api/admin/planned-messages/[timeoutId]                                    601 B         183 kB
├ ƒ /api/admin/servers                                                         601 B         183 kB
├ ƒ /api/admin/servers/[serverId]                                              601 B         183 kB
├ ƒ /api/admin/servers/[serverId]/migrate                                      601 B         183 kB
├ ƒ /api/admin/tool-limits                                                     601 B         183 kB
├ ƒ /api/admin/update                                                          601 B         183 kB
├ ƒ /api/admin/update/commits                                                  601 B         183 kB
├ ƒ /api/admin/update/log                                                      601 B         183 kB
├ ƒ /api/admin/update/versions                                                 601 B         183 kB
├ ƒ /api/admin/vps/chat-tasks                                                  601 B         183 kB
├ ƒ /api/admin/vps/chat-tasks/cancel-all                                       601 B         183 kB
├ ƒ /api/agent-folders                                                         601 B         183 kB
├ ƒ /api/agent-folders/[folderId]                                              601 B         183 kB
├ ƒ /api/agent-folders/[folderId]/restore                                      601 B         183 kB
├ ƒ /api/agent-folders/[folderId]/visibility                                   601 B         183 kB
├ ƒ /api/agent-organization                                                    601 B         183 kB
├ ƒ /api/agent-project-runtime-auth                                            601 B         183 kB
├ ƒ /api/agent-project-vscode-auth/[sessionId]                                 601 B         183 kB
├ ƒ /api/agents                                                                601 B         183 kB
├ ƒ /api/agents/[agentName]                                                    601 B         183 kB
├ ƒ /api/agents/[agentName]/clone                                              601 B         183 kB
├ ƒ /api/agents/[agentName]/restore                                            601 B         183 kB
├ ƒ /api/agents/export                                                         601 B         183 kB
├ ƒ /api/agents/import                                                         601 B         183 kB
├ ƒ /api/api-tokens                                                            601 B         183 kB
├ ƒ /api/auth/change-password                                                  601 B         183 kB
├ ƒ /api/auth/login                                                            601 B         183 kB
├ ƒ /api/auth/logout                                                           601 B         183 kB
├ ƒ /api/auth/shibboleth/acs                                                   601 B         183 kB
├ ƒ /api/auth/shibboleth/login                                                 601 B         183 kB
├ ƒ /api/auth/shibboleth/metadata                                              601 B         183 kB
├ ƒ /api/auth/shibboleth/status                                                601 B         183 kB
├ ƒ /api/browser-artifacts/[artifactName]                                      601 B         183 kB
├ ƒ /api/browser-test/act                                                      601 B         183 kB
├ ƒ /api/browser-test/screenshot                                               601 B         183 kB
├ ƒ /api/browser-test/scroll-facebook                                          601 B         183 kB
├ ƒ /api/calendar-oauth/callback                                               601 B         183 kB
├ ƒ /api/calendar-oauth/connect                                                601 B         183 kB
├ ƒ /api/calendar-oauth/refresh                                                601 B         183 kB
├ ƒ /api/calendar-oauth/revoke                                                 601 B         183 kB
├ ƒ /api/calendar-oauth/status                                                 601 B         183 kB
├ ƒ /api/chat                                                                  601 B         183 kB
├ ƒ /api/chat-feedback                                                         601 B         183 kB
├ ƒ /api/chat-feedback/[id]                                                    601 B         183 kB
├ ƒ /api/chat-feedback/export                                                  601 B         183 kB
├ ƒ /api/chat-history                                                          601 B         183 kB
├ ƒ /api/chat-history/[id]                                                     601 B         183 kB
├ ƒ /api/chat-history/export                                                   601 B         183 kB
├ ƒ /api/chat-history/threads                                                  601 B         183 kB
├ ƒ /api/chat-streaming                                                        601 B         183 kB
├ ƒ /api/chat/citation-label                                                   601 B         183 kB
├ ƒ /api/chat/export/pdf                                                       601 B         183 kB
├ ƒ /api/custom-css                                                            601 B         183 kB
├ ƒ /api/custom-js                                                             601 B         183 kB
├ ƒ /api/docs/book-language.md                                                 601 B         183 kB
├ ƒ /api/docs/book-language.pdf                                                601 B         183 kB
├ ƒ /api/docs/book.md                                                          601 B         183 kB
├ ƒ /api/elevenlabs/tts                                                        601 B         183 kB
├ ƒ /api/emails/incoming/sendgrid                                              601 B         183 kB
├ ƒ /api/emails/incoming/stalwart                                              601 B         183 kB
├ ƒ /api/embed.js                                                              601 B         183 kB
├ ƒ /api/error-reports/application                                             601 B         183 kB
├ ƒ /api/federated-agents                                                      601 B         183 kB
├ ƒ /api/github-app/callback                                                   601 B         183 kB
├ ƒ /api/github-app/connect                                                    601 B         183 kB
├ ƒ /api/github-app/status                                                     601 B         183 kB
├ ƒ /api/health                                                                601 B         183 kB
├ ƒ /api/images/[filename]                                                     601 B         183 kB
├ ƒ /api/internal/agent-goal-chat-planned-messages                             601 B         183 kB
├ ƒ /api/internal/agent-project-runtimes                                       601 B         183 kB
├ ƒ /api/internal/agent-runner-limits                                          601 B         183 kB
├ ƒ /api/internal/user-chat-jobs/run                                           601 B         183 kB
├ ƒ /api/internal/user-chat-timeouts/run                                       601 B         183 kB
├ ƒ /api/long-running-task                                                     601 B         183 kB
├ ƒ /api/messages                                                              601 B         183 kB
├ ƒ /api/metadata                                                              601 B         183 kB
├ ƒ /api/metadata/export                                                       601 B         183 kB
├ ƒ /api/metadata/import                                                       601 B         183 kB
├ ƒ /api/onboarding/book                                                       601 B         183 kB
├ ƒ /api/onboarding/evaluate                                                   601 B         183 kB
├ ƒ /api/onboarding/test                                                       601 B         183 kB
├ ƒ /api/openai/v1/audio/transcriptions                                        601 B         183 kB
├ ƒ /api/openai/v1/chat/completions                                            601 B         183 kB
├ ƒ /api/openai/v1/models                                                      601 B         183 kB
├ ƒ /api/page-preview/check                                                    601 B         183 kB
├ ƒ /api/page-preview/input                                                    601 B         183 kB
├ ƒ /api/page-preview/screenshot                                               601 B         183 kB
├ ƒ /api/page-preview/state                                                    601 B         183 kB
├ ƒ /api/page-preview/stream                                                   601 B         183 kB
├ ƒ /api/profile                                                               601 B         183 kB
├ ƒ /api/push-subscriptions                                                    601 B         183 kB
├ ƒ /api/scrape                                                                601 B         183 kB
├ ƒ /api/search                                                                601 B         183 kB
├ ƒ /api/send-email                                                            601 B         183 kB
├ ƒ /api/settings/keybindings                                                  601 B         183 kB
├ ƒ /api/settings/notifications                                                601 B         183 kB
├ ƒ /api/settings/theme                                                        601 B         183 kB
├ ƒ /api/spawn-agent                                                           601 B         183 kB
├ ƒ /api/story/export                                                          601 B         183 kB
├ ƒ /api/system/mocked-chats                                                   601 B         183 kB
├ ƒ /api/team-agent-profile                                                    601 B         183 kB
├ ƒ /api/upload                                                                601 B         183 kB
├ ƒ /api/usage                                                                 601 B         183 kB
├ ƒ /api/user-memory                                                           601 B         183 kB
├ ƒ /api/user-memory/[memoryId]                                                601 B         183 kB
├ ƒ /api/user-wallet                                                           601 B         183 kB
├ ƒ /api/user-wallet/[walletId]                                                601 B         183 kB
├ ƒ /api/users                                                                 601 B         183 kB
├ ƒ /api/users/[username]                                                      601 B         183 kB
├ ƒ /api/v1/agents                                                             601 B         183 kB
├ ƒ /api/v1/agents/[agentId]                                                   601 B         183 kB
├ ƒ /api/v1/folders                                                            601 B         183 kB
├ ƒ /api/v1/folders/[folderId]                                                 601 B         183 kB
├ ƒ /api/v1/folders/[folderId]/agents/[agentId]                                601 B         183 kB
├ ƒ /api/v1/instance                                                           601 B         183 kB
├ ƒ /api/v1/me                                                                 601 B         183 kB
├ ƒ /dashboard                                                               1.88 kB        1.55 MB
├ ƒ /docs                                                                    3.02 kB        1.77 MB
├ ƒ /docs/[docId]                                                            3.01 kB        1.77 MB
├ ƒ /embed                                                                   6.99 kB        1.71 MB
├ ƒ /experiments/story                                                       5.05 kB         378 kB
├ ƒ /humans.txt                                                                601 B         183 kB
├ ƒ /manifest.webmanifest                                                      601 B         183 kB
├ ƒ /openapi.json                                                              601 B         183 kB
├ ƒ /recycle-bin                                                             3.63 kB         309 kB
├ ƒ /restricted                                                                172 B         184 kB
├ ƒ /robots.txt                                                                601 B         183 kB
├ ƒ /s3/[first]/[second]/[hash]/[filename]                                     601 B         183 kB
├ ƒ /search                                                                  6.46 kB         190 kB
├ ƒ /security.txt                                                              601 B         183 kB
├ ƒ /sitemap.xml                                                               601 B         183 kB
├ ƒ /story/[[...story]]                                                        601 B         183 kB
├ ƒ /superadmin/cli-access                                                   3.05 kB         231 kB
├ ƒ /superadmin/database                                                     2.74 kB         227 kB
├ ƒ /superadmin/email-server                                                 2.08 kB         227 kB
├ ƒ /superadmin/environment                                                  5.72 kB         230 kB
├ ƒ /superadmin/harness-auth                                                 6.04 kB         234 kB
├ ƒ /superadmin/internal-s3                                                    601 B         183 kB
├ ƒ /superadmin/logs                                                         3.73 kB         228 kB
├ ƒ /superadmin/resource-monitor                                             6.94 kB         232 kB
├ ƒ /superadmin/servers                                                      22.9 kB         301 kB
├ ƒ /superadmin/task-manager                                                   152 B         265 kB
├ ƒ /superadmin/update                                                       14.6 kB         263 kB
├ ƒ /swagger                                                                 4.28 kB         244 kB
├ ƒ /system/profile                                                          2.87 kB         227 kB
├ ƒ /system/settings                                                         5.71 kB         225 kB
├ ƒ /system/user-memory                                                      5.49 kB         249 kB
├ ƒ /system/user-wallet                                                      9.42 kB         260 kB
├ ƒ /system/utilities                                                        1.64 kB         226 kB
├ ƒ /system/utilities/mocked-chats                                           19.1 kB         263 kB
├ ƒ /system/utilities/mocked-chats/view                                      5.16 kB        1.64 MB
├ ƒ /test/og-image                                                             601 B         183 kB
└ ƒ /test/og-image/opengraph-image                                             601 B         183 kB
+ First Load JS shared by all                                                 182 kB
  ├ chunks/1458-4bc1ff63d5ce79cc.js                                           123 kB
  ├ chunks/87c73c54-3c195070c5cbb22b.js                                      54.1 kB
  └ other shared chunks (total)                                              5.55 kB

Route (pages)                                                                   Size  First Load JS
┌   /_app                                                                        0 B         142 kB
└ ○ /500 (4860 ms)                                                           4.77 kB         147 kB
+ First Load JS shared by all                                                 174 kB
  ├ chunks/framework-415b41e54b4434c6.js                                     57.7 kB
  ├ chunks/main-d4f9f380592ed649.js                                            81 kB
  ├ css/c4bd8b207325b3b3.css                                                 31.5 kB
  └ other shared chunks (total)                                              3.67 kB

ƒ Middleware                                                                  301 kB

○  (Static)   prerendered as static content
ƒ  (Dynamic)  server-rendered on demand

   ▲ Next.js 15.4.11
   - Local:        http://localhost:4440
   - Network:      http://192.168.192.1:4440

 ✓ Starting...
 ✓ Ready in 4.7s
🗂️ VPS server registry ready with 0 registered server(s) in standalone SQLite mode.
Prerendered home page and saved to C:\Users\me\work\ai\promptbook\apps\agents-server\.next\prerendered\home.html
🎉 All tests passed!

=== test shell finished at 2026-09-17T01:02:16.411Z ===
Status: succeeded
````
