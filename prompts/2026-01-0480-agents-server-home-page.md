[x] ~$0.68

[✨⛵️] Simplify home page of agent server.

-   Currently, the home page of the agents server is showing lot of technical info.
-   Remove everything except the agents.
-   Keep federated servers
-   Keep list / graph view
-   Remove Users, Models,...
-   Keep header and footer as is
-   Move the old `/` to `/dashboard` and link this dashboard from the "System" in menu
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   You are working with the [Agents Server](apps/agents-server) with homepage at `/`
-   Add the changes into the [changelog](changelog/_current-preversion.md)

---

[x] ~$0.61

[✨⛵️] Allows to add a custom message on the homepage of the agent's server.

-   This message should be shown as Markdown above the agents on the homepage.
-   This message should be shown for both locked and anonymous users.
-   Message should be configurable through the metadata.
-   By default, the message is empty.
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   You are working with the [Agents Server](apps/agents-server) with homepage at `/`
-   Add the changes into the [changelog](changelog/_current-preversion.md)

---

[-]

[✨⛵️] qux

-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   You are working with the [Agents Server](apps/agents-server)
-   Add the changes into the [changelog](changelog/_current-preversion.md)

---

[-]

[✨⛵️] qux

-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   You are working with the [Agents Server](apps/agents-server)
-   Add the changes into the [changelog](changelog/_current-preversion.md)


