[x]

[✨😁] When I am on the fresh browser it says "Logged in as Admin" but clearly I should not be logged in

-   I even can not log out because logout button does not any effect
-   The user system should handle fresh browsers correctly and not assume the user is logged in
-   When I am logged in, some cookie item should be set to indicate that
-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.

---

[ ]

[✨😁] User system should be scoped to domain

-   One app can run on multiple domains (e.g. foo.ptbk.io and bar.ptbk.io)
-   User login cookie and session should be scoped to domain
-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.

---

[ ]

[✨😁] Login status isn't visible on mobile

-   When I open Agents Server on mobile browser, It should be visible in opened hamburger menu as first item
-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.

---

[-]

[✨😁] qux

-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.

---

[-]

[✨😁] qux

-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.

---

[-]

[✨😁] qux

-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.

---

[-]

[✨😁] qux

-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.

---

[-]

[✨😁] qux

-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
