[ ] !!

[🔎🔐] Find security vurnabilities

-   _(@@@ After VPS server is working)_
-   Go through the codebase and fix any security vurnabilities you find, and make PRD to fix them
-   Look at folder `prompts` and for each found security vurnability, create a new prompt PRD there, look how PRDs there are structured and create a similar one for each security vurnability
-   Each PRD is in one markdown file `prompts/2026-05-0440-ptbk-security-xxx.md`
-   You are not fixing the security vurnabilities, you are just creating PRDs for them in the `prompts` folder, so other developers can work on fixing them by following the PRDs you created
-   Do a proper analysis of the current functionality before you work on creating the PRDs.
-   When there is same vurnability in multiple places, create one PRD for it and link to all places where it is found, do not create multiple PRDs for the same vurnability

**The contents of the markdown file looks like this, for example:**

```markdown
[-]

[🔐] Fix XSS vurnability on `/foo/bar` page of Utils app

-   It is possible to inject malicious JavaScript code through the `input` field on the `/foo/bar` page of the Utils app, which can lead to XSS attacks. The input is not properly sanitized before being rendered on the page, allowing attackers to execute arbitrary JavaScript code in the context of the user's browser.
-   The fix for this vurnability involves properly sanitizing the user input before rendering it on the page, using a library such as DOMPurify or similar, to ensure that any potentially malicious code is neutralized and cannot be executed in the user's browser.
-   The vurnability is found in the [`bar.tsx` file](src/apps/utils/pages/foo/bar.tsx), specifically in the `handleInputChange` function where the user input is processed and rendered on the page without proper sanitization.
-   Do a proper analysis of the current functionality before you start implementing.
-   You are working with the [Utils app](apps/utils)
```

_<- This is just an example, the actual content of the PRD should be based on the security vurnability you found, and should contain all necessary information about the vurnability_
