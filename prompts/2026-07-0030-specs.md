[x] By codex `gpt-5.5`, commited manually

[✨⚓️] Create specs of Agents Server

-   Create specs of Agents Server, so that the functionality is well documented
-   Purpose of the specs is to have a single source of truth for the functionality of the Agents Server, so that it can be used for development, testing, and documentation but also for 1:1 replication of the functionality without having the source code of the Agents Server
-   When I take just specs folder it should be possible to implement the same functionality of the Agents Server and the Agents without having the source code of the Agents Server
-   Specs will be created in the `specs/` folder in the root of the repository
-   Use hierarchy of folders to organize the specs, for example `specs/foo/bar.md`
-   One markdown file should contain one spec / reponsibility / aspect of the Agents Server, so that it can be easily referenced and linked to from other specs
-   The most important specs and most abstract and core concepts should be in the root of the `specs/` folder, and less important specs should be in subfolders
-   Interlink the specs via markdown hyperlinks
-   You can omit orphan code, there is a lot of code from this repository that is not used and is not relevant for the functionality of the Agents Server, so you can omit it from the specs
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Do a proper analysis of the current functionality before you start writing specs.
-   You are working with the [Agents Server](apps/agents-server)
-   Do not change any code in the repository, just write specs
