[ ] !

[✨💒] New agent wizard: Fix the `OPEN` / `CLOSED` commitment placement

-   `OPEN` and `CLOSED` commitments should be always at the end of the agent source book NEVER in the middle
-   You are working with the [Agents Server](apps/agents-server)
