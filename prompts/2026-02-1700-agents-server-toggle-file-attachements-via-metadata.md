[x] ~$0.31 14 minutes by OpenAI Codex `gpt-5.1-codex-mini`

[✨🧝] Allow to enable/disable file attachments via metadata

-   Create `IS_FILE_ATTACHEMENTS_ENABLED` _(by default `true`)_ metadata key which allows to enable or disable file attachments in the chat.
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   You are working with the [Agents Server](apps/agents-server) with the chat
-   Add the changes into the [changelog](changelog/_current-preversion.md)

### Metadata of Agents server

-   There is a table called `Metadata`
-   It has `key` and `value` fields
-   It is a similar concept to configuration, but this configuration can be changed by the administrators in the Agents server website.

---

[-]

[✨🧝] bar

-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Do a proper analysis of the current functionality before you start implementing.
-   You are working with the [Agents Server](apps/agents-server)
-   Add the changes into the [changelog](changelog/_current-preversion.md)

---

[-]

[✨🧝] bar

-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Do a proper analysis of the current functionality before you start implementing.
-   You are working with the [Agents Server](apps/agents-server)
-   Add the changes into the [changelog](changelog/_current-preversion.md)

---

[-]

[✨🧝] bar

-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Do a proper analysis of the current functionality before you start implementing.
-   You are working with the [Agents Server](apps/agents-server)
-   Add the changes into the [changelog](changelog/_current-preversion.md)

