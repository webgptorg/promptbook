[x]

[✨🐞] Link directly from agent page to agent history and feedback

-   It should be possible to go from an agent page to the agent's history and feedback page with filtered view just for that agent with single click
-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.

---

[x]

[✨🐞] Both chat history and chat feedback should have view that shows the conversation in the chat

-   Keep the table view as is, but add also a view that shows the full conversation in the chat style in a `<Chat ... />` component
-   For chat history leverage also `<MockedChat ... />` component
-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.

---

[x]

[✨🐞] Allow to download chat history and feedback as `csv`

-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.

---

[-]

[✨🐞] foo

-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
