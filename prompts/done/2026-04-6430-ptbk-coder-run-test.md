[x] ~$0.00 an hour by GitHub Copilot `gpt-5.4`

[✨🧫] Add optional flag `--test` to `ptbk coder run`

```bash
npm install ptbk

ptbk coder run --agent github-copilot --model gpt-5.4 --thinking-level xhigh --context AGENTS.md --test npm run test
```

-   When present, the `--test` flag will run the specified test command after the coding agent finishes each prompt. This allows you to automatically verify that the code generated by the coding agent is correct and does not break any tests.
-   When the `--test` flag is used, the `ptbk coder` will execute the specified test command after each prompt is processed. If the test command fails (i.e., returns a non-zero exit code), the `ptbk coder` will send the test output back to the coding agent as feedback, allowing it to learn from its mistakes and improve its code generation in subsequent prompts. This creates a feedback loop where the coding agent can iteratively refine its code based on test results, ultimately leading to higher quality code generation.
-   There should be a constant for max retries, for example 3. If the coding agent fails the test after 3 attempts for the same prompt, the `ptbk coder` should log the failure.
-   When marking done prompt by `[ ]` -> `[x]` add there number of attempts, for example `[x] (2 attempts)` if it took 2 attempts to pass the test, or `[x] (failed after 3 attempts)` if it failed after 3 attempts.
-   For single attempt do not write the number of attempts, just mark it as before
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Do a proper analysis of the current functionality of `ptbk coder` and related functionality before you start implementing.
-   You are working with [`ptbk coder`](src/cli/cli-commands/coder/run.ts)
-   In `terminals.json` add the testing flag for Promptbook project itself
-   Add the changes into the [changelog](changelog/_current-preversion.md)

---

[-]

[✨🧫] bar

```bash
@@@

npm install ptbk

ptbk coder init

ptbk coder run --agent github-copilot --model gpt-5.4 --thinking-level xhigh --context AGENTS.md
```

-   @@@
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Do a proper analysis of the current functionality of `ptbk coder` and related functionality before you start implementing.
-   You are working with [`ptbk coder`](src/cli/cli-commands/coder/run.ts)
-   Add the changes into the [changelog](changelog/_current-preversion.md)

---

[-]

[✨🧫] bar

```bash
@@@

npm install ptbk

ptbk coder init

ptbk coder run --agent github-copilot --model gpt-5.4 --thinking-level xhigh --context AGENTS.md
```

-   @@@
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Do a proper analysis of the current functionality of `ptbk coder` and related functionality before you start implementing.
-   You are working with [`ptbk coder`](src/cli/cli-commands/coder/run.ts)
-   Add the changes into the [changelog](changelog/_current-preversion.md)

---

[-]

[✨🧫] bar

```bash
@@@

npm install ptbk

ptbk coder init

ptbk coder run --agent github-copilot --model gpt-5.4 --thinking-level xhigh --context AGENTS.md
```

-   @@@
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Do a proper analysis of the current functionality of `ptbk coder` and related functionality before you start implementing.
-   You are working with [`ptbk coder`](src/cli/cli-commands/coder/run.ts)
-   Add the changes into the [changelog](changelog/_current-preversion.md)
