[x] (2 attempts) ~$0.3770 an hour by OpenAI Codex `gpt-5.5`

[✨🦷] `ptbk agent-folder` should put the generated system message from generated model requirements into the prompt NOT the raw agent book

```bash
ptbk agent-folder run-multiple --agent openai-codex --model gpt-5.5 --thinking-level xhigh --auto-pull --auto-push --auto-clone
```

**This is how prompt going to coding agent look like now:**

```
Answer 1 user question

-   Read `messages/queued/2026-05-19-JeDaxhLVq3wCp8.book` and answer the most recent `MESSAGE @User`
-   Use `agent.book` as the source of truth for the local agent behavior
-   If `docs/book-language-manual.md` exists, use it as the local Book Language reference instead of guessing syntax
-   Use files in the `knowledge` folder when they are relevant
-   Only change the queued message file by appending one new `MESSAGE @Agent` block
-   Do not modify any other file
-   If the repository does not contain enough information, say what is missing instead of inventing facts
```

**This is how prompt going to coding agent should look like:**

```
Answer 1 user question

-   Read `messages/queued/2026-05-19-JeDaxhLVq3wCp8.book` and answer the most recent `MESSAGE @User`
-   Use files in the `knowledge` folder when they are relevant
-   Only change the queued message file by appending one new `MESSAGE @Agent` block
-   Do not modify any other file
-   If the repository does not contain enough information, say what is missing instead of inventing facts

**This is how you should behave:**

You are Generic Chatter
You are a helpful, honest, and intelligent AI assistant. Your goal is to provide accurate, clear, and concise responses while being friendly and engaging. Think step-by-step before answering complex questions.
You are a helpful and supportive, analytical, curious and inquisitive, strategic assistant

## Rules

...
```

-   The things after "This is how you should behave" is generated by [`createAgentModelRequirements` function](src/book-2.0/agent-source/createAgentModelRequirements.ts)
-   Keep the `agent.book` in the generated repository
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Do a proper analysis of the current functionality of `ptbk agent-folder` and related functionality before you start implementing.
-   You are working with [`ptbk agent-folder`](src/cli/cli-commands/agent-folder/run.ts)
-   Add the changes into the [changelog](changelog/_current-preversion.md)

---

[-]

[✨🦷] qux

```bash
@@@

npm install ptbk

ptbk agent-folder init

ptbk agent-folder run --agent github-copilot --model gpt-5.4 --thinking-level xhigh
```

-   @@@
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Do a proper analysis of the current functionality of `ptbk agent-folder` and related functionality before you start implementing.
-   You are working with [`ptbk agent-folder`](src/cli/cli-commands/agent-folder/run.ts)
-   Add the changes into the [changelog](changelog/_current-preversion.md)

---

[-]

[✨🦷] qux

```bash
@@@

npm install ptbk

ptbk agent-folder init

ptbk agent-folder run --agent github-copilot --model gpt-5.4 --thinking-level xhigh
```

-   @@@
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Do a proper analysis of the current functionality of `ptbk agent-folder` and related functionality before you start implementing.
-   You are working with [`ptbk agent-folder`](src/cli/cli-commands/agent-folder/run.ts)
-   Add the changes into the [changelog](changelog/_current-preversion.md)

---

[-]

[✨🦷] qux

```bash
@@@

npm install ptbk

ptbk agent-folder init

ptbk agent-folder run --agent github-copilot --model gpt-5.4 --thinking-level xhigh
```

-   @@@
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Do a proper analysis of the current functionality of `ptbk agent-folder` and related functionality before you start implementing.
-   You are working with [`ptbk agent-folder`](src/cli/cli-commands/agent-folder/run.ts)
-   Add the changes into the [changelog](changelog/_current-preversion.md)

