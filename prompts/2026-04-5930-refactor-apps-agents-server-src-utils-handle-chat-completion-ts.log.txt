=== runner shell started at 2026-04-18T07:43:37.759Z ===
Script path: /c/Users/me/work/ai/promptbook/prompts/2026-04-5930-refactor-apps-agents-server-src-utils-handle-chat-completion-ts.sh

--- raw input ---
if [ -f .env ]; then
set -a
source .env
set +a
fi

unset OPENAI_API_KEY
unset OPENAI_BASE_URL

codex \
      -c forced_login_method=chatgpt \
      -c model_reasoning_effort="xhigh" \
    --ask-for-approval never \
    exec --model gpt-5.4 \
    --local-provider none \
    --sandbox danger-full-access \
    -C /c/Users/me/work/ai/promptbook \
    <<'CODEX_PROMPT'

Refactor [`handleChatCompletion.ts` file](apps/agents-server/src/utils/handleChatCompletion.ts)

- The file packs too many responsibilities into one place.
- The file contains too many functions (29/20)
    - Keep related responsibilities grouped behind small facades or focused modules.
    - Consider extracting private helpers or splitting independent concerns into dedicated files.
- Purpose of this refactoring is to improve code maintainability and readability.
- Look at the internal structure, the usage and also surrounding code to understand how to best refactor this file.
- Consider breaking down large functions into smaller, more manageable ones, removing any redundant code, and ensuring that the file adheres to the project coding standards.
- After the refactoring, ensure that (1) `npm run test-name-discrepancies` and (2) `npm run test-package-generation` are passing successfully.
    1. All the things you have moved to new files should correspond the thing in the file with the file name, for example `MyComponent.tsx` should export `MyComponent`.
    2. All the things you have moved to new files but are private things to the outside world should have `@private function of TheMainThing` JSDoc comment.
- Keep in mind DRY *(Do not repeat yourself)* and SOLID principles while refactoring.
- **Do not change the external behavior** of the code. Focus solely on improving the internal structure and organization of the code.
- Before you start refactoring, make sure to read the code carefully and understand its current structure and functionality. Do a analysis of the current functionality before you start.

## How to Contribute

-   **Add changes in [Changelog](/changelog/_current-preversion.md)**
-   **Test your changes** to ensure they work as expected, use `npm test`

## Code Style

-   Follow the existing code style and structure.
-   Use comments to explain complex logic.

## Emoji in `[brackets]`

-   `[any emoji]` Connects multiple places that are related to each other across the repository
-   `[number]` Connects multiple places that are related to each other across the file
-   `[🧠]` Marks a place where there is something to decide and think about.
-   `[🕕]` List of models _(that should be progresively updated)_
    -   Prompt: Update available models and their prices, search online
-   `[🔼]` Marks an entity (function, class, type,...) in other project (like Promptbook.studio) which should be moved to this repository
-   `[🚉]` Marks an types / interfaces / structures fully serializable as JSON, not marking `string_` and `number_` prefixed aliases
-   `[🧹]` Need to implement garbage collection
-   `[🐣]` Easter eggs
-   `[💩]` Shitty code that needs refactoring
-   `$` When entity (function, class) starts by `$`, it means it is not pure and can have side effects.
-   3x `!` Marks a place that needs to be fixed before releasing a pre-release version.
-   4x (and more) `!` Marks a place that needs to be fixed as soon as possible.
-   `@@@` Marks a place where text / documentation / ... must be written.
-   [⚫] Code in this file should never be published in any package
-   [🟢] Code in this file should never be published into packages that could be imported into browser environment
-   [🔵] Code in this file should never be published outside of `@promptbook/browser`
-   [🟡] Code in this file should never be published outside of `@promptbook/cli`
-   [💞] Ignore a discrepancy between file name and entity name

## Generated code

Across the repository, there are several places where code has been generated automatically.
Do not edit these places manually, as they will be overwritten by the code generation process. Edit the source files instead.

> ⚠️ WARNING: This code has been generated so that any manual changes will be overwritten

## Dictionary

-   **Promptbook Engine vs. Agent Server** sre two distinct parts you should know the difference:
    -   **Promptbook Engine** is the core engine that powers AI agents, it is located in `/src` folder. It is framework-agnostic and can be used in different applications. It can run as standalone javascript/typescript library, CLI environment, or be integrated into different applications. It can be also runned in Agent Server:
    -   **Agent Server** is a specific application that uses the Promptbook Engine to provide a web and API interface for creating and interacting with AI agents. It is located in `/apps/agents-server` folder.
-   **Commitments** are basic syntax elements that add specific functionalities to AI agents
    -   They are used in `agentSource`, there are commitments like `PERSONA`, `RULE`, `KNOWLEDGE`, `USE BROWSER`, `USE SEARCH ENGINE`, `META IMAGE`, etc.
    -   Commitments are in the folder `/src/commitments`
    -   Each commitment starts with a keyword, e.g., `KNOWLEDGE`, `USE BROWSER`, etc. on a begining of the line and end by new co
    -   Agent source with commitments is parsed by two functions:
        -   `parseAgentSource` which is a lightweight parser for agent source, it parses basic information and its purpose is to be quick and synchronous. The commitments there are hardcoded.
        -   `createAgentModelRequirements` which is an asynchronous function that creates model requirements it applies each commitment one by one and works asynchronously.

## Common rules

-   Always analyze the context and requirements before generating any code.
-   Write clear, maintainable, and well-documented code.
-   Write JSDoc comments for all entities - functions, classes, types, top-level constants, etc.
    -   When this entity is exported from the file and it is under `src` folder *(not for example in the `apps` folder)*, it must be marked either as `@public` or `@private` at the end of the JSDoc comment.
    -   For example: "@private internal utility of <Chat/>" / "@public exported from `@promptbook/browser`"
    -   If you don't know, prefer to mark it as private, we can always change it to public later, but changing from public to private may cause breaking changes.
-   After code change, run the following tests to ensure everything works as expected:
    1) `npm run test-name-discrepancies` - Tests that file names match the exported names.
    2) `npm run test-spellcheck` - When using some new word, add it into the [dictionary](other/cspell-dictionaries).
    3) `npm run test-lint` - Linting.
    4) `npm run test-types` - Checks TypeScript types.
    5) `npm run test-package-generation` - Tests build/package generation and public/private JSDoc coverage.
    6) `npm run test-unit` - Unit tests.
    7) `npm run test-app-agents-server` - Tests that the Agents Server app is working correctly.
-   You don't need to run every test, run them only when you make changes which may cause them to fail.
-   You (the AI coding agent) are running inside a Node process, so do not kill all Node processes such as `taskkill /F /IM node.exe`. If you need to stop something you spawned, kill only that specific process, for example by PID or by port.

## Additional context

-   Attached images (if any) are relative to the root of the project.

### The Agents Server menu *(as additional context)*

The menu of the agent server looks like this:

1. The navigation hierarchy
    - Icon and Server name _(for example Promptbook Agents Server)_
    - arrow ">" and Agents or picked agent name (organized in folders)
    - arrow ">" and the view Profile / Chat / Book of the agent or nothing if no agent is picked
2. The menu items
    - Documentation
    - System
3. Control panel and user menu
    - Control panel
    - User menu with the avatar and the name of the user

### Database migrations for Agents server *(as additional context)*

-   Migrations are located in `apps/agents-server/src/database/migrations`
-   Be aware that table names in migrations have prefix `prefix_` _(look at existing migrations for reference)_
-   Migrations should be backwards compatible:
    -   Meaning that earlier versions of the server should be able to run with the database after migration without any issues.
    -   This is important because we want to have the same database for production and preview environments.
    -   This database will be migrated to the latest version of the preview environment, but the production environment should be able to run with it without any issues.
    -   The only thing that can happen is that older versions of the server will not be able to use new features.
    -   Adding new columns, tables, etc. is fine, but do not remove or rename existing ones, and do not change the meaning of existing columns or tables.
    -   When in doubt, prefer to add new things instead of changing existing ones.

### Metadata of Agents server *(as additional context)*

-   There is a table called `Metadata`.
-   It has `key` and `value` fields.
-   It is a similar concept to configuration, but this configuration can be changed by administrators in the Agents Server website.

### Book Language blueprint *(as additional context)*

Book language is a domain-specific language used for defining AI agents in the Promptbook Engine and Agents server.
It has lightweight syntax and keywords (the commitments) that allow you to define the "soul" of the agent.
The book language is designed to be human-readable and easy to write, while also being powerful enough to express complex agent behaviors.

Every agent has its source defined in the book language, which is called "agent source". The agent source is parsed and processed by the Promptbook Engine to create the actual AI agent that can interact with users and perform tasks.
This agent source is internally converted to a structured format called "agent model requirements" which are the actual raw technical instructions for the AI model to run the agent.

There is a standalone book language documentation on each agent server on `/api/docs/book.md` route, for example `https://pavol-hejny.ptbk.io/api/docs/book.md`.
Use it as a reference for the syntax and semantics of the book language, and modify `apps/agents-server/src/utils/bookLanguageDocumentation/createStandaloneBookLanguageMarkdown.ts` if it is relevant to the change you are doing.

#### Commitments *(as additional context and part of Book Language)*

Commitments are basic syntax elements that add specific functionalities to AI agents written in `book` language.

-   They are used in `agentSource`, there are commitments like `PERSONA`, `RULE`, `KNOWLEDGE`, `USE BROWSER`, `USE SEARCH ENGINE`, `META IMAGE`, `CLOSED`, etc.
-   They are in the folder `src/commitments`.
-   Each commitment starts with a keyword, e.g. `PERSONA`, `KNOWLEDGE`, `USE SEARCH ENGINE`, etc. on a beginning of the line and ends by a new commitment or the end of the book.
-   There is a general pattern that the commitment keyword is followed by a space and then by the content of the commitment, for example:
    -   `PERSONA You are a helpful assistant that helps with cooking recipes.`
    -   `USE SEARCH ENGINE Search only in French.`
-   In the commitment context, you can reference external agents, for example:
    -   `TEAM You can talk to {Criminal lawyer} and {Financial advisor}`

## Coding rules

-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Keep in mind the SOLID principles.
-   Do a proper analysis of the current functionality before you start implementing.
-   Keep small responsibilities of functions and classes, avoid creating big functions or classes that do many things.
-   When throwing errors, throw [branded errors](src/errors) and use `spaceTrim` utility to write clear and well-formatted multiline detailed error messages.
    -   Format errors as markdown, for example `variables` should be in backticks and important notes can be in bold.
-   Constants should always be `UPPER_SNAKE_CASE`.
-   Boolean variables should always be prefixed with `is`, for example `isUserChatJobLeaseExpired` or `IS_DEBUG_MODE`.
-   Do not use abbreviations, for example use `isExpired` instead of `isExp`, `translateMessage` instead of `t`, etc.
    -   It is fine to use well-known abbreviations, for example `id`, `url`, `html`, etc.
-   When writing multiline strings, use `spaceTrim` utility.
-   Do only the change described in the prompt. Do not add any additional features or make any additional changes that are not described there.
    -   If you find some critical issue that is not described in the prompt, report it to `./AGENT_REPORT.md` in the root of the project.

CODEX_PROMPT

--- raw output ---

Reading prompt from stdin...
OpenAI Codex v0.106.0 (research preview)
--------
workdir: C:/Users/me/work/ai/promptbook
model: gpt-5.4
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: xhigh
reasoning summaries: none
session id: 019d9f8b-b255-7ed3-8639-88730925c49d
--------
user

Refactor [`handleChatCompletion.ts` file](apps/agents-server/src/utils/handleChatCompletion.ts)

- The file packs too many responsibilities into one place.
- The file contains too many functions (29/20)
    - Keep related responsibilities grouped behind small facades or focused modules.
    - Consider extracting private helpers or splitting independent concerns into dedicated files.
- Purpose of this refactoring is to improve code maintainability and readability.
- Look at the internal structure, the usage and also surrounding code to understand how to best refactor this file.
- Consider breaking down large functions into smaller, more manageable ones, removing any redundant code, and ensuring that the file adheres to the project coding standards.
- After the refactoring, ensure that (1) `npm run test-name-discrepancies` and (2) `npm run test-package-generation` are passing successfully.
    1. All the things you have moved to new files should correspond the thing in the file with the file name, for example `MyComponent.tsx` should export `MyComponent`.
    2. All the things you have moved to new files but are private things to the outside world should have `@private function of TheMainThing` JSDoc comment.
- Keep in mind DRY *(Do not repeat yourself)* and SOLID principles while refactoring.
- **Do not change the external behavior** of the code. Focus solely on improving the internal structure and organization of the code.
- Before you start refactoring, make sure to read the code carefully and understand its current structure and functionality. Do a analysis of the current functionality before you start.

## How to Contribute

-   **Add changes in [Changelog](/changelog/_current-preversion.md)**
-   **Test your changes** to ensure they work as expected, use `npm test`

## Code Style

-   Follow the existing code style and structure.
-   Use comments to explain complex logic.

## Emoji in `[brackets]`

-   `[any emoji]` Connects multiple places that are related to each other across the repository
-   `[number]` Connects multiple places that are related to each other across the file
-   `[🧠]` Marks a place where there is something to decide and think about.
-   `[🕕]` List of models _(that should be progresively updated)_
    -   Prompt: Update available models and their prices, search online
-   `[🔼]` Marks an entity (function, class, type,...) in other project (like Promptbook.studio) which should be moved to this repository
-   `[🚉]` Marks an types / interfaces / structures fully serializable as JSON, not marking `string_` and `number_` prefixed aliases
-   `[🧹]` Need to implement garbage collection
-   `[🐣]` Easter eggs
-   `[💩]` Shitty code that needs refactoring
-   `$` When entity (function, class) starts by `$`, it means it is not pure and can have side effects.
-   3x `!` Marks a place that needs to be fixed before releasing a pre-release version.
-   4x (and more) `!` Marks a place that needs to be fixed as soon as possible.
-   `@@@` Marks a place where text / documentation / ... must be written.
-   [⚫] Code in this file should never be published in any package
-   [🟢] Code in this file should never be published into packages that could be imported into browser environment
-   [🔵] Code in this file should never be published outside of `@promptbook/browser`
-   [🟡] Code in this file should never be published outside of `@promptbook/cli`
-   [💞] Ignore a discrepancy between file name and entity name

## Generated code

Across the repository, there are several places where code has been generated automatically.
Do not edit these places manually, as they will be overwritten by the code generation process. Edit the source files instead.

> ⚠️ WARNING: This code has been generated so that any manual changes will be overwritten

## Dictionary

-   **Promptbook Engine vs. Agent Server** sre two distinct parts you should know the difference:
    -   **Promptbook Engine** is the core engine that powers AI agents, it is located in `/src` folder. It is framework-agnostic and can be used in different applications. It can run as standalone javascript/typescript library, CLI environment, or be integrated into different applications. It can be also runned in Agent Server:
    -   **Agent Server** is a specific application that uses the Promptbook Engine to provide a web and API interface for creating and interacting with AI agents. It is located in `/apps/agents-server` folder.
-   **Commitments** are basic syntax elements that add specific functionalities to AI agents
    -   They are used in `agentSource`, there are commitments like `PERSONA`, `RULE`, `KNOWLEDGE`, `USE BROWSER`, `USE SEARCH ENGINE`, `META IMAGE`, etc.
    -   Commitments are in the folder `/src/commitments`
    -   Each commitment starts with a keyword, e.g., `KNOWLEDGE`, `USE BROWSER`, etc. on a begining of the line and end by new co
    -   Agent source with commitments is parsed by two functions:
        -   `parseAgentSource` which is a lightweight parser for agent source, it parses basic information and its purpose is to be quick and synchronous. The commitments there are hardcoded.
        -   `createAgentModelRequirements` which is an asynchronous function that creates model requirements it applies each commitment one by one and works asynchronously.

## Common rules

-   Always analyze the context and requirements before generating any code.
-   Write clear, maintainable, and well-documented code.
-   Write JSDoc comments for all entities - functions, classes, types, top-level constants, etc.
    -   When this entity is exported from the file and it is under `src` folder *(not for example in the `apps` folder)*, it must be marked either as `@public` or `@private` at the end of the JSDoc comment.
    -   For example: "@private internal utility of <Chat/>" / "@public exported from `@promptbook/browser`"
    -   If you don't know, prefer to mark it as private, we can always change it to public later, but changing from public to private may cause breaking changes.
-   After code change, run the following tests to ensure everything works as expected:
    1) `npm run test-name-discrepancies` - Tests that file names match the exported names.
    2) `npm run test-spellcheck` - When using some new word, add it into the [dictionary](other/cspell-dictionaries).
    3) `npm run test-lint` - Linting.
    4) `npm run test-types` - Checks TypeScript types.
    5) `npm run test-package-generation` - Tests build/package generation and public/private JSDoc coverage.
    6) `npm run test-unit` - Unit tests.
    7) `npm run test-app-agents-server` - Tests that the Agents Server app is working correctly.
-   You don't need to run every test, run them only when you make changes which may cause them to fail.
-   You (the AI coding agent) are running inside a Node process, so do not kill all Node processes such as `taskkill /F /IM node.exe`. If you need to stop something you spawned, kill only that specific process, for example by PID or by port.

## Additional context

-   Attached images (if any) are relative to the root of the project.

### The Agents Server menu *(as additional context)*

The menu of the agent server looks like this:

1. The navigation hierarchy
    - Icon and Server name _(for example Promptbook Agents Server)_
    - arrow ">" and Agents or picked agent name (organized in folders)
    - arrow ">" and the view Profile / Chat / Book of the agent or nothing if no agent is picked
2. The menu items
    - Documentation
    - System
3. Control panel and user menu
    - Control panel
    - User menu with the avatar and the name of the user

### Database migrations for Agents server *(as additional context)*

-   Migrations are located in `apps/agents-server/src/database/migrations`
-   Be aware that table names in migrations have prefix `prefix_` _(look at existing migrations for reference)_
-   Migrations should be backwards compatible:
    -   Meaning that earlier versions of the server should be able to run with the database after migration without any issues.
    -   This is important because we want to have the same database for production and preview environments.
    -   This database will be migrated to the latest version of the preview environment, but the production environment should be able to run with it without any issues.
    -   The only thing that can happen is that older versions of the server will not be able to use new features.
    -   Adding new columns, tables, etc. is fine, but do not remove or rename existing ones, and do not change the meaning of existing columns or tables.
    -   When in doubt, prefer to add new things instead of changing existing ones.

### Metadata of Agents server *(as additional context)*

-   There is a table called `Metadata`.
-   It has `key` and `value` fields.
-   It is a similar concept to configuration, but this configuration can be changed by administrators in the Agents Server website.

### Book Language blueprint *(as additional context)*

Book language is a domain-specific language used for defining AI agents in the Promptbook Engine and Agents server.
It has lightweight syntax and keywords (the commitments) that allow you to define the "soul" of the agent.
The book language is designed to be human-readable and easy to write, while also being powerful enough to express complex agent behaviors.

Every agent has its source defined in the book language, which is called "agent source". The agent source is parsed and processed by the Promptbook Engine to create the actual AI agent that can interact with users and perform tasks.
This agent source is internally converted to a structured format called "agent model requirements" which are the actual raw technical instructions for the AI model to run the agent.

There is a standalone book language documentation on each agent server on `/api/docs/book.md` route, for example `https://pavol-hejny.ptbk.io/api/docs/book.md`.
Use it as a reference for the syntax and semantics of the book language, and modify `apps/agents-server/src/utils/bookLanguageDocumentation/createStandaloneBookLanguageMarkdown.ts` if it is relevant to the change you are doing.

#### Commitments *(as additional context and part of Book Language)*

Commitments are basic syntax elements that add specific functionalities to AI agents written in `book` language.

-   They are used in `agentSource`, there are commitments like `PERSONA`, `RULE`, `KNOWLEDGE`, `USE BROWSER`, `USE SEARCH ENGINE`, `META IMAGE`, `CLOSED`, etc.
-   They are in the folder `src/commitments`.
-   Each commitment starts with a keyword, e.g. `PERSONA`, `KNOWLEDGE`, `USE SEARCH ENGINE`, etc. on a beginning of the line and ends by a new commitment or the end of the book.
-   There is a general pattern that the commitment keyword is followed by a space and then by the content of the commitment, for example:
    -   `PERSONA You are a helpful assistant that helps with cooking recipes.`
    -   `USE SEARCH ENGINE Search only in French.`
-   In the commitment context, you can reference external agents, for example:
    -   `TEAM You can talk to {Criminal lawyer} and {Financial advisor}`

## Coding rules

-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Keep in mind the SOLID principles.
-   Do a proper analysis of the current functionality before you start implementing.
-   Keep small responsibilities of functions and classes, avoid creating big functions or classes that do many things.
-   When throwing errors, throw [branded errors](src/errors) and use `spaceTrim` utility to write clear and well-formatted multiline detailed error messages.
    -   Format errors as markdown, for example `variables` should be in backticks and important notes can be in bold.
-   Constants should always be `UPPER_SNAKE_CASE`.
-   Boolean variables should always be prefixed with `is`, for example `isUserChatJobLeaseExpired` or `IS_DEBUG_MODE`.
-   Do not use abbreviations, for example use `isExpired` instead of `isExp`, `translateMessage` instead of `t`, etc.
    -   It is fine to use well-known abbreviations, for example `id`, `url`, `html`, etc.
-   When writing multiline strings, use `spaceTrim` utility.
-   Do only the change described in the prompt. Do not add any additional features or make any additional changes that are not described there.
    -   If you find some critical issue that is not described in the prompt, report it to `./AGENT_REPORT.md` in the root of the project.


mcp startup: no servers

thinking
**Modifying code inspection**

I need to make some modifications to the code. First, I should add a commentary update to acknowledge the changes and outline the first step. I might use an update plan once I have enough context. It’s important to inspect the file and its surrounding code. I’ll probably use `rg` or something similar, and I might not need to browse the internet since this is about local code. Let’s start with the commentary.
codex
I’m analyzing `apps/agents-server/src/utils/handleChatCompletion.ts` and the surrounding call sites first, so I can split responsibilities without changing behavior. After that I’ll refactor into smaller focused modules, update the changelog, and run the required tests.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Item apps/agents-server/src/utils/handleChatCompletion.ts | Select-Object FullName,Length | Format-List' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg -n "handleChatCompletion" apps/agents-server/src -S' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content apps/agents-server/src/utils/handleChatCompletion.ts -TotalCount 260' in C:/Users/me/work/ai/promptbook succeeded in 953ms:
import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
import {
    createBookScopedAgentReferenceResolver,
    parseBookScopedAgentIdentifier,
} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
import {
    appendMessageSuffix,
    createMessageSuffixAppendix,
    emulateMessageSuffixStreaming,
    resolveMessageSuffixFromAgentSource,
} from '@/src/utils/chat/messageSuffix';
import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
import { prepareToolCallsForStreaming } from './toolCallStreaming';
import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
 succeeded in 1.15s:

[32;1mFullName : [0mC:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\handleChatCompletion.ts
[32;1mLength   : [0m42946

 succeeded in 1.55s:
apps/agents-server/src\utils\handleChatCompletion.ts:140: * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
apps/agents-server/src\utils\handleChatCompletion.ts:390:export async function handleChatCompletion(
apps/agents-server/src\app\api\openai\v1\chat\completions\route.ts:1:import { handleChatCompletion } from '@/src/utils/handleChatCompletion';
apps/agents-server/src\app\api\openai\v1\chat\completions\route.ts:8:    return handleChatCompletion(request, {}, 'OpenAI API Chat Completion (Global)');
apps/agents-server/src\app\agents\[agentName]\api\openrouter\chat\completions\route.ts:1:import { handleChatCompletion } from '@/src/utils/handleChatCompletion';
apps/agents-server/src\app\agents\[agentName]\api\openrouter\chat\completions\route.ts:12:    return handleChatCompletion(request, { agentName }, 'OpenRouter API Chat Completion');
apps/agents-server/src\app\agents\[agentName]\api\openai\v1\chat\completions\route.ts:1:import { handleChatCompletion } from '@/src/utils/handleChatCompletion';
apps/agents-server/src\app\agents\[agentName]\api\openai\v1\chat\completions\route.ts:12:    return handleChatCompletion(request, { agentName }, 'OpenAI API Chat Completion');
apps/agents-server/src\app\agents\[agentName]\api\openai\chat\completions\route.ts:1:import { handleChatCompletion } from '@/src/utils/handleChatCompletion';
apps/agents-server/src\app\agents\[agentName]\api\openai\chat\completions\route.ts:12:    return handleChatCompletion(request, { agentName }, 'OpenAI API Chat Completion');
codex
I’ve confirmed `handleChatCompletion.ts` is large enough to justify extraction, and I’m mapping its internal seams now: request parsing, runtime resolution, prompt/history setup, tool execution/streaming, and response shaping. Next I’m reading the rest of the file plus nearby utilities so the split follows existing patterns instead of inventing a new structure.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content apps/agents-server/src/utils/handleChatCompletion.ts | Select-Object -Skip 260 -First 260' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content apps/agents-server/src/utils/handleChatCompletion.ts | Select-Object -Skip 780 -First 260' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content apps/agents-server/src/utils/handleChatCompletion.ts | Select-Object -Skip 520 -First 260' in C:/Users/me/work/ai/promptbook succeeded in 851ms:

                const messageSuffixAppendix = createMessageSuffixAppendix(
                    normalizedResponse.content,
                    runtime.messageSuffix,
                );
                if (messageSuffixAppendix) {
                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
                        emitDeltaChunk(delta);
                    });
                }

                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
                await finalizeHandleChatCompletionResult({
                    responseContentWithSuffix,
                    usage: result.usage,
                    toolCalls: result.toolCalls,
                    parsedRequest,
                    runtime,
                    promptContext,
                    // Preserve historical behavior: streamed OpenAI-compatible calls still persist learning in private mode.
 succeeded in 953ms:
        incomingParameters:
            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
                ? (rawParameters as Record<string, unknown>)
                : {},
        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
    };
}

/**
 * Resolves agent/runtime dependencies needed before prompt execution.
 */
async function resolveHandleChatCompletionRuntime(options: {
    request: NextRequest;
    agentName: string;
    messages: ReadonlyArray<TODO_any>;
    isPrivateModeEnabled: boolean;
}): Promise<HandleChatCompletionRuntime | NextResponse> {
    const { request, agentName, messages, isPrivateModeEnabled } = options;
    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
 succeeded in 1.01s:
 * Parses open Ai tool choice.
 */
function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
    if (value === undefined || value === null) {
        return undefined;
    }

    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
}

/**
 * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
 */
function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
    if (typeof value === 'string') {
        return value;
    }

    if (Array.isArray(value)) {
        return value
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content apps/agents-server/src/utils/handleChatCompletion.ts | Select-Object -Skip 1040 -First 220' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg -n "resolveHandleChatCompletion|createHandleChatCompletion|persistOpenAiCompatibilityFrozenChat|appendSystemMessagesToAgentSource|convertOpenAiTool|createCompatibilityUsage" apps/agents-server/src/utils -S' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content changelog/_current-preversion.md -TotalCount 200' in C:/Users/me/work/ai/promptbook succeeded in 944ms:
apps/agents-server/src/utils\handleChatCompletion.ts:89:function createCompatibilityUsage(
apps/agents-server/src/utils\handleChatCompletion.ts:190:function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
apps/agents-server/src/utils\handleChatCompletion.ts:196:        .map((tool) => convertOpenAiTool(tool))
apps/agents-server/src/utils\handleChatCompletion.ts:205:function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
apps/agents-server/src/utils\handleChatCompletion.ts:427:        const runtime = await resolveHandleChatCompletionRuntime({
apps/agents-server/src/utils\handleChatCompletion.ts:437:        const promptContext = await createHandleChatCompletionPromptContext({
apps/agents-server/src/utils\handleChatCompletion.ts:446:            ? createHandleChatCompletionStreamResponse({
apps/agents-server/src/utils\handleChatCompletion.ts:453:            : createHandleChatCompletionJsonResponse({
apps/agents-server/src/utils\handleChatCompletion.ts:488:        return createHandleChatCompletionErrorResponse(
apps/agents-server/src/utils\handleChatCompletion.ts:496:        return createHandleChatCompletionErrorResponse(
apps/agents-server/src/utils\handleChatCompletion.ts:505:        return createHandleChatCompletionErrorResponse(
apps/agents-server/src/utils\handleChatCompletion.ts:519:        runtimeTools: convertOpenAiTools(rawTools),
apps/agents-server/src/utils\handleChatCompletion.ts:532:async function resolveHandleChatCompletionRuntime(options: {
apps/agents-server/src/utils\handleChatCompletion.ts:543:        return createHandleChatCompletionErrorResponse(
apps/agents-server/src/utils\handleChatCompletion.ts:563:        return createHandleChatCompletionErrorResponse(
apps/agents-server/src/utils\handleChatCompletion.ts:576:    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
apps/agents-server/src/utils\handleChatCompletion.ts:596:        await resolveHandleChatCompletionCredentials({
apps/agents-server/src/utils\handleChatCompletion.ts:602:    const agent = await createHandleChatCompletionAgent({
apps/agents-server/src/utils\handleChatCompletion.ts:636:async function createHandleChatCompletionPromptContext(options: {
apps/agents-server/src/utils\handleChatCompletion.ts:646:    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
 succeeded in 987ms:
        });
    }
}

/**
 * Resolves request-scoped project, calendar, and email credentials for the current agent.
 */
async function resolveHandleChatCompletionCredentials(options: {
    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
    agentId: string;
    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
}): Promise<{
    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
}> {
    const userId = options.currentUserIdentity?.userId;
    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
        resolveUseProjectGithubToken({
 succeeded in 1.11s:
-   Refactored Agents Server `errorMessages.ts` into smaller focused helpers without changing external behavior:

    -   Split the branching-heavy `categorizeError` flow into dedicated API-type, native-error-message, and status-code classification helpers while preserving the existing precedence.
    -   Consolidated repeated unknown-error object parsing and friendly-message creation into small shared utilities so the file now reads top-down more clearly.

-   Refactored Agents Server `AgentKitCacheManager.ts` into smaller focused private cache modules without changing external behavior:

    -   Extracted the short-lived prepared-agent cache, model-requirement resolution, citation-policy injection, knowledge-source hashing, and vector-store persistence into dedicated private modules behind the existing `AgentKitCacheManager` facade.
    -   Kept the existing AgentKit cache keys, vector-store reuse behavior, knowledge-source hash persistence, and verbose logging flow intact while making the public manager easier to read and maintain.

-   Fixed Agents Server homepage navigation in the responsive header so the server branding now reliably routes back to `/` on both desktop and mobile:

    -   Extracted the shared header homepage link into one reusable component and reused it in both the desktop breadcrumb bar and the mobile drawer to keep the homepage affordance consistent without duplicating branding markup.
    -   Added the missing mobile drawer homepage link, including the server logo/name branding and close-on-navigate behavior.
    -   Hardened the mobile menu trigger and drawer navigation landmark accessibility so the responsive header interactions are easier to target and verify across touch/mobile regression tests.
    -   Added e2e regression coverage for tapping the header branding and the mobile drawer server link from a mobile viewport.

-   Added a new deterministic `fractal` avatar visual to the shared canvas avatar system and the Utils miniapp:

    -   Added `src/avatars/visuals/fractalAvatarVisual.ts` and registered it in the shared avatar registry, so the existing `/avatars` playground and gallery can render a recursive fractal-style badge derived from agent name, hash, and colors.

thinking
**Inspecting project structure**

I'm looking to understand how to split private modules for utilities. I might need to check out existing refactors first. It makes sense to inspect the file structure of similar projects and search for private module patterns around utils. Maybe there's a recently refactored manager that can guide me. I’m considering creating a folder that shares the same basename and checking other refactored examples in the apps/agents-server/src/utils directory to see if a subfolder structure is used. Let's search!
codex
The file breaks cleanly into a few concerns, so I’m checking how this repo names and documents extracted private modules before editing. I want the split to pass `test-name-discrepancies` and package-generation the first time, especially around exported private helpers and folder layout.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg --files apps/agents-server/src | rg "useHeaderDropdownState|useControlPanelContentState|useBrowserPushNotificationsState|AgentKitCacheManager|errorMessages"' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content apps/agents-server/src/utils/cache/AgentKitCacheManager.ts -TotalCount 220' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content apps/agents-server/src/utils/errorMessages.ts -TotalCount 220' in C:/Users/me/work/ai/promptbook succeeded in 753ms:
apps/agents-server/src\utils\cache\AgentKitCacheManager.ts
apps/agents-server/src\utils\cache\AgentKitCacheManager\withAgentKitSourceCitationPolicy.ts
apps/agents-server/src\utils\cache\AgentKitCacheManager\resolveAgentKitModelRequirements.ts
apps/agents-server/src\utils\cache\AgentKitCacheManager\createAgentKitVectorStoreNote.ts
apps/agents-server/src\utils\cache\AgentKitCacheManager\AgentKitVectorStoreCache.ts
apps/agents-server/src\utils\cache\AgentKitCacheManager\AgentKitPreparedCache.ts
apps/agents-server/src\utils\cache\AgentKitCacheManager\AgentKitKnowledgeSourceHasher.ts
apps/agents-server/src\utils\errorMessages.ts
apps/agents-server/src\components\PushNotifications\useBrowserPushNotificationsState.ts
apps/agents-server/src\components\Header\useHeaderDropdownState.ts
apps/agents-server/src\components\Header\ControlPanel\useControlPanelContentState.ts
 succeeded in 1.07s:
import { resolveWebsiteKnowledgeSourcesForServer } from '@/src/utils/knowledge/resolveWebsiteKnowledgeSourcesForServer';
import type { AgentModelRequirements, string_agent_permanent_id, string_book } from '@promptbook-local/types';
import { OpenAiAgentKitExecutionTools } from '../../../../../src/llm-providers/openai/OpenAiAgentKitExecutionTools';
import type { AgentReferenceResolver } from '../../../../../src/book-2.0/agent-source/AgentReferenceResolver';
import {
    AssistantConfiguration,
    computeAssistantCacheKey,
    extractAssistantConfiguration,
    formatAssistantNameWithHash,
} from './computeAssistantCacheKey';
import { AgentKitKnowledgeSourceHasher } from './AgentKitCacheManager/AgentKitKnowledgeSourceHasher';
import {
    AgentKitPreparedCache,
    type AgentKitPreparedCacheEntry,
} from './AgentKitCacheManager/AgentKitPreparedCache';
import { AgentKitVectorStoreCache } from './AgentKitCacheManager/AgentKitVectorStoreCache';
import { resolveAgentKitModelRequirements } from './AgentKitCacheManager/resolveAgentKitModelRequirements';
import { withAgentKitSourceCitationPolicy } from './AgentKitCacheManager/withAgentKitSourceCitationPolicy';

/**
 succeeded in 1.00s:
/**
 * Centralized error message mapping utility for user-friendly error messages
 *
 * This utility provides:
 * - User-friendly error messages for common error scenarios
 * - Console logging of raw errors for debugging
 * - DRY principle by centralizing error message logic
 */

export type ErrorCategory =
    | 'NETWORK_ERROR'
    | 'AUTHENTICATION_ERROR'
    | 'AGENT_NOT_FOUND'
    | 'AGENT_DELETED'
    | 'VALIDATION_ERROR'
    | 'RATE_LIMIT_ERROR'
    | 'SERVER_ERROR'
    | 'TIMEOUT_ERROR'
    | 'UNKNOWN_ERROR';


thinking
**Inspecting folder patterns**

I need to check the current folder patterns for handleChatCompletion. I'm wondering if there’s a subfolder with the same name. It might help to search for the main utils file within that subfolder. I’ll probably use a command like `rg --files` to get a list of all the files. Taking a closer look will help clarify the structure I’m working with, and I want to be thorough with my inspection!
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-ChildItem apps/agents-server/src/utils | Select-Object Name,PSIsContainer | Format-Table -AutoSize' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg --files apps/agents-server/src/utils | rg \"handleChatCompletion/|handleChatCompletion\\.\"" in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg --files apps/agents-server/src/utils | rg "''^[''^'"\\n]*[A-Za-z0-9]+/[A-Za-z0-9]+\\.ts"'$"' in C:/Users/me/work/ai/promptbook succeeded in 664ms:
apps/agents-server/src/utils\handleChatCompletion.ts
 succeeded in 835ms:

[32;1mName                                            [0m[32;1m PSIsContainer[0m
[32;1m----                                            [0m [32;1m-------------[0m
agentOrganization                                         True
agentPreparation                                          True
agentReferenceResolver                                    True
agentRouting                                              True
analytics                                                 True
backup                                                    True
bookLanguageDocumentation                                 True
cache                                                     True
calendars                                                 True
cdn                                                       True
chat                                                      True
content                                                   True
defaultFederatedAgents                                    True
emails                                                    True
errorReporting                                            True
getAdminChatTasksResponse                                 True
githubApp                                                 True
 exited 1 in 647ms:


thinking
**Considering folder structure**

I need to think about what types I might want from the main file. It seems like a good idea to create a folder named `handleChatCompletion`. In there, I could include `handleChatCompletionRequest.ts`, which would handle parsing requests and converting tools, and possibly include error responses as well. I want to make sure everything is organized and functions smoothly for better clarity and performance.
codex
The structure is clear enough now that I’m checking the repo’s naming expectations for extracted helpers before I patch. I’m looking for how this codebase handles “types” files and private exported helpers so the new modules don’t fail the discrepancy checks.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg -n "test-name-discrepancies|name discrepancies|file names match" -S .' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg -n "@private" apps/agents-server/src/utils apps/agents-server/src/components -g "*.ts" -g "*.tsx" | Select-Object -First 80' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg -n "export type .*Types|export interface .*Types|export type .*State|export interface .*State" apps/agents-server/src -g "*.ts" -g "*.tsx" | Select-Object -First 80' in C:/Users/me/work/ai/promptbook succeeded in 1.06s:
apps/agents-server/src/components\WalletRecordDialog\WalletRecordDialogContent.tsx:14: * @private function of WalletRecordDialog
apps/agents-server/src/components\WalletRecordDialog\WalletRecordDialogContent.tsx:21: * @private function of WalletRecordDialog
apps/agents-server/src/components\WalletRecordDialog\WalletRecordDialogContent.tsx:29: * @private function of WalletRecordDialog
apps/agents-server/src/components\WalletRecordDialog\WalletRecordDialogContent.tsx:36: * @private function of WalletRecordDialog
apps/agents-server/src/components\WalletRecordDialog\WalletRecordDialogContent.tsx:52: * @private function of WalletRecordDialog
apps/agents-server/src/components\WalletRecordDialog\WalletRecordDialogContent.tsx:72: * @private function of WalletRecordDialog
apps/agents-server/src/components\WalletRecordDialog\WalletRecordDialogContent.tsx:92: * @private function of WalletRecordDialog
apps/agents-server/src/components\WalletRecordDialog\WalletRecordDialogContent.tsx:128: * @private function of WalletRecordDialog
apps/agents-server/src/components\WalletRecordDialog\WalletRecordDialogContent.tsx:144: * @private function of WalletRecordDialog
apps/agents-server/src/components\WalletRecordDialog\WalletRecordDialogContent.tsx:168: * @private function of WalletRecordDialog
apps/agents-server/src/components\WalletRecordDialog\WalletRecordDialogContent.tsx:202: * @private function of WalletRecordDialog
apps/agents-server/src/components\WalletRecordDialog\WalletRecordDialogContent.tsx:218: * @private function of WalletRecordDialog
apps/agents-server/src/components\WalletRecordDialog\WalletRecordDialogContent.tsx:247: * @private function of WalletRecordDialog
apps/agents-server/src/components\WalletRecordDialog\WalletRecordDialogContent.tsx:302: * @private function of WalletRecordDialog
apps/agents-server/src/components\WalletRecordDialog\WalletRecordDialogContent.tsx:321: * @private function of WalletRecordDialog
apps/agents-server/src/components\WalletRecordDialog\WalletRecordDialogContent.tsx:357: * @private function of WalletRecordDialog
apps/agents-server/src/components\WalletRecordDialog\WalletRecordDialogContent.tsx:421: * @private function of WalletRecordDialog
apps/agents-server/src/components\WalletRecordDialog\WalletRecordDialogContent.tsx:439: * @private function of WalletRecordDialog
apps/agents-server/src/components\WalletRecordDialog\WalletRecordDialogContent.tsx:478: * @private function of WalletRecordDialog
apps/agents-server/src/components\WalletRecordDialog\WalletRecordDialogContent.tsx:510: * @private function of WalletRecordDialog
 succeeded in 2.11s:
apps/agents-server/src\utils\userChat\userChatMessageLifecycle.ts:6:export type UserChatMessageLifecycleState = NonNullable<ChatMessage['lifecycleState']>;
apps/agents-server/src\utils\githubApp\GithubAppConnectionState.ts:17:export type GithubAppConnectionStatePayload = {
apps/agents-server/src\utils\githubApp\GithubAppConnectionState.ts:30:export type CreateGithubAppConnectionStateOptions = {
apps/agents-server/src\utils\githubApp\GithubAppConnectionState.ts:41:export type ParseGithubAppConnectionStateOptions = {
apps/agents-server/src\utils\googleCalendarOAuth\GoogleCalendarOAuthConnectionState.ts:20:export type GoogleCalendarOAuthConnectionStatePayload = {
apps/agents-server/src\utils\googleCalendarOAuth\GoogleCalendarOAuthConnectionState.ts:35:export type CreateGoogleCalendarOAuthConnectionStateOptions = {
apps/agents-server/src\utils\googleCalendarOAuth\GoogleCalendarOAuthConnectionState.ts:48:export type ParseGoogleCalendarOAuthConnectionStateOptions = {
apps/agents-server/src\components\NewAgentDialog\NewAgentWizardState.ts:57:export type NewAgentWizardState = {
apps/agents-server/src\utils\resolveStoredAgentState.ts:22:export type ResolvedStoredAgentState<TAgent extends StoredAgentSourceRecord> = TAgent & ResolvedAgentState & {
apps/agents-server/src\utils\resolveStoredAgentState.ts:31:export type ResolveStoredAgentStateOptions = Pick<
apps/agents-server/src\utils\resolveServerAgentContext.ts:11:export type ResolvedServerAgentContext = ResolvedBookScopedAgentContext & ResolvedAgentState;
apps/agents-server/src\utils\resolveAgentStateFromSource.ts:11:export type ResolveAgentStateFromSourceOptions = {
apps/agents-server/src\utils\resolveAgentStateFromSource.ts:41:export type ResolvedAgentState = {
apps/agents-server/src\components\Homepage\FolderEditDialogState.ts:10:export type FolderEditDialogState = {
apps/agents-server/src\components\Homepage\buildOfficeLayoutTypes.ts:9:export type OfficeAgentState = 'idle' | 'working' | 'meeting' | 'moving';
apps/agents-server/src\app\system\utilities\mocked-chats\useMockedChatsEditorState.ts:45:export type UseMockedChatsEditorState = {
apps/agents-server/src\components\Homepage\buildGraphData.ts:22:export type { GraphData, GraphDataInput } from './buildGraphDataTypes';
apps/agents-server/src\components\Header\ControlPanel\ControlPanelContentState.ts:13:export type ControlPanelContentStateProps = {
apps/agents-server/src\components\Header\ControlPanel\ControlPanelContentState.ts:79:export type ControlPanelPreferenceState = {
apps/agents-server/src\components\Header\ControlPanel\ControlPanelContentState.ts:92:export type ControlPanelNotificationState = ControlPanelPreferenceState & {
 exited 124 in 11.69s:
.\AGENTS.md:62:    1) `npm run test-name-discrepancies` - Tests that file names match the exported names.
.\scripts\find-refactor-candidates\buildPromptContent.ts:132:        '- After the refactoring, ensure that (1) `npm run test-name-discrepancies` and (2) `npm run test-package-generation` are passing successfully.',
.\scripts\find-name-discrepancies\find-name-discrepancies.ts:49: * Finds name discrepancies.
.\scripts\find-name-discrepancies\find-name-discrepancies.ts:52:    console.info(`🔎🩹 Find name discrepancies`);
.\scripts\find-name-discrepancies\find-name-discrepancies.ts:111:                        Found ${filenamesWithDiscrepanciesNotIgnored.length} files with name discrepancies
.\scripts\find-name-discrepancies\find-name-discrepancies.ts:119:                        Fix name discrepancies
.\scripts\find-name-discrepancies\find-name-discrepancies.ts:144:        console.info(colors.green(`No name discrepancies found`));
.\prompts\2026-01-0260-test-coding-agent-1-write-2.tmp.sh:27:    1) npm run test-name-discrepancies - tests that file names matches the exported names, for example if you export a class named "OpenAiAgent" from a file, the file should be named `OpenAiAgent.ts` *(not `ClaudeAgent.ts`)*. This helps to prevent typos and mismanagement of the project.
.\scripts\generate-packages\writePublishWorkflow.ts:181:            name: '🧪 Test | Name discrepancies',
.\scripts\generate-packages\writePublishWorkflow.ts:182:            run: `npm run test-name-discrepancies`,
.\prompts\2026-03-1560-agents-server-optimize.notes-1.md:156:- `npm run test-name-discrepancies` -> passed.
.\package.json:184:        "test": "echo \"Note: Running from most lightweight to heaviest tests\" && npm run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm run test-books && npm run test-book-components-build && npm run test-package-generation && npm run test-unit && npm run test-app-agents-server && echo \"🎉 All tests passed!\"",
.\package.json:185:        "test-without-package-generation": "echo \"Note: Running from most lightweight to heaviest tests\" && npm run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm run test-books && npm run test-book-components-build && npm run test-unit",
.\package.json:186:        "test-without-package-generation-and-unit": "echo \"Note: Running from most lightweight to heaviest tests\" && npm run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm run test-books && npm run test-book-components-build",
.\package.json:187:        "test-for-ptbk-coder": "echo \"Note: Running from most lightweight to heaviest tests\" && npm run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm run test-books && npm run test-book-components-build && npm run test-package-generation && npm run test-unit && (cd ./apps/agents-server/ && npm run lint && npm run test-build) && echo \"🎉 All tests passed!\"",
.\package.json:196:        "test-name-discrepancies": "ts-node ./scripts/find-name-discrepancies/find-name-discrepancies.ts",
.\prompts\2026-04-0010-agents-server-backup-restore-multi-entity.sh:84:    1) `npm run test-name-discrepancies` - Tests that file names match the exported names.
.\prompts\2026-04-4620-refactor-apps-agents-server-src-utils-server-management-ts.md:12:- After the refactoring, ensure that (1) `npm run test-name-discrepancies` and (2) `npm run test-package-generation` are passing successfully.
.\prompts\2026-04-4610-refactor-apps-agents-server-src-utils-mocked-chats-schema-ts.md:12:- After the refactoring, ensure that (1) `npm run test-name-discrepancies` and (2) `npm run test-package-generation` are passing successfully.
.\prompts\2026-04-4600-refactor-apps-agents-server-src-utils-get-admin-chat-tasks-response-ts.md:12:- After the refactoring, ensure that (1) `npm run test-name-discrepancies` and (2) `npm run test-package-generation` are passing successfully.

thinking
**Finding name discrepancies**

I need to inspect the script that deals with name discrepancies to understand the rules better. It sounds like I need to take a closer look to see what’s going on. There’s likely some important information that could help clarify any issues. I'll focus on understanding the specifics so I can address any potential discrepancies efficiently. Let's make sure I'm clear on the guidelines involved!
codex
I have enough context to shape the refactor. I’m now checking the actual discrepancy rule implementation, because that determines whether I can use a shared `types` file or need to keep type exports colocated with the helper whose name matches the file.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content apps/agents-server/src/utils/chatConfiguration.ts -TotalCount 180' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content scripts/find-name-discrepancies/find-name-discrepancies.ts -TotalCount 260' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content apps/agents-server/src/utils/userChat.ts -TotalCount 220' in C:/Users/me/work/ai/promptbook succeeded in 620ms:
export { appendQueuedUserChatTurn } from './userChat/appendQueuedUserChatTurn';
export { claimNextQueuedUserChatJob, USER_CHAT_JOB_LEASE_DURATION_MS } from './userChat/claimNextQueuedUserChatJob';
export { createUserChat } from './userChat/createUserChat';
export { createUserChatDetailPayload } from './userChat/createUserChatDetailPayload';
export { createUserChatJobFailureDetails } from './userChat/createUserChatJobFailureDetails';
export { createUserChatJob } from './userChat/createUserChatJob';
export { createUserChatSummary } from './userChat/createUserChatSummary';
export { createUserChatSummaryFromSeed } from './userChat/createUserChatSummary';
export { deleteUserChat } from './userChat/deleteUserChat';
export { deleteUserChatJob } from './userChat/deleteUserChatJob';
export { finalizeUserChatJob } from './userChat/finalizeUserChatJob';
export { getUserChat } from './userChat/getUserChat';
export { getUserChatJob } from './userChat/getUserChatJob';
export { getUserChatJobById } from './userChat/getUserChatJobById';
export { getUserChatJobByClientMessageId } from './userChat/getUserChatJobByClientMessageId';
export { heartbeatUserChatJob } from './userChat/heartbeatUserChatJob';
export { listExpiredRunningUserChatJobs } from './userChat/listExpiredRunningUserChatJobs';
export { listUserChats, listUserChatSummarySeeds } from './userChat/listUserChats';
export { listUserChatJobActivityCounts, listUserChatJobs } from './userChat/listUserChatJobs';
export { mutateUserChat } from './userChat/mutateUserChat';
 succeeded in 741ms:
import { getMetadataMap } from '../database/getMetadata';
import { parseChatFeedbackMode, type ChatFeedbackMode } from './chatFeedbackMode';

/**
 * Constant for chat fail message key.
 */
const CHAT_FAIL_MESSAGE_KEY = 'CHAT_FAIL_MESSAGE';
/**
 * Constant for is file attachements enabled key.
 */
const IS_FILE_ATTACHEMENTS_ENABLED_KEY = 'IS_FILE_ATTACHEMENTS_ENABLED';
/**
 * Constant for chat feedback mode key.
 */
const CHAT_FEEDBACK_MODE_KEY = 'CHAT_FEEDBACK_MODE';
/**
 * Constant for is feedback enabled key.
 */
const IS_FEEDBACK_ENABLED_KEY = 'IS_FEEDBACK_ENABLED';

 succeeded in 856ms:
#!/usr/bin/env ts-node
// repair-imports.ts

import colors from 'colors';
import commander from 'commander';
import { readFile } from 'fs/promises';
import { basename, join } from 'path';
import { spaceTrim } from 'spacetrim';
import { assertsError } from '../../src/errors/assertsError';
import { findAllProjectFilesWithEntities } from '../utils/findAllProjectFilesWithEntities';

if (process.cwd() !== join(__dirname, '../..')) {
    console.error(
        colors.red(
            spaceTrim(`
                CWD must be root of the project

                Script: find-name-discrepancies.ts
                Current CWD: ${process.cwd()}
                Expected CWD: ${join(__dirname, '../..')}
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content scripts/utils/findFileEntities.ts -TotalCount 260' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content scripts/utils/findProjectFilesWithEntities.ts -TotalCount 260' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content scripts/utils/findAllProjectFilesWithEntities.ts -TotalCount 260' in C:/Users/me/work/ai/promptbook succeeded in 664ms:
import { EntityMetadata, findAllProjectEntities } from './findAllProjectEntities';

/**
 * Groups all project entities by file, creating a mapping between source files
 * and the exported entities (i.e. functions, classes, interfaces, etc.) they contain
 */
export async function findAllProjectFilesWithEntities(): Promise<
    ReadonlyArray<{ filename: string; entities: ReadonlyArray<Omit<EntityMetadata, 'filename'>> }>
> {
    const allEntities = await findAllProjectEntities();

    const entitiesByFile: Record<string, Array<EntityMetadata>> = {};

    for (const entity of allEntities) {
        const { filename } = entity;

        if (!entitiesByFile[filename]) {
            entitiesByFile[filename] = [];
        }

 exited 1 in 760ms:
[31;1mGet-Content: [0m
[31;1m[36;1mLine |[0m
[31;1m[36;1m[36;1m   2 | [0m [36;1mGet-Content scripts/utils/findFileEntities.ts -TotalCount 260[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m     | [31;1m ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m[31;1m[31;1m[36;1m     | [31;1mCannot find path 'C:\Users\me\work\ai\promptbook\scripts\utils\findFileEntities.ts' because it does not exist.[0m
 exited 1 in 865ms:
[31;1mGet-Content: [0m
[31;1m[36;1mLine |[0m
[31;1m[36;1m[36;1m   2 | [0m [36;1mGet-Content scripts/utils/findProjectFilesWithEntities.ts -TotalCount[0m …[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m     | [31;1m ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m[31;1m[31;1m[36;1m     | [31;1mCannot find path 'C:\Users\me\work\ai\promptbook\scripts\utils\findProjectFilesWithEntities.ts' because it does not exist.[0m
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content scripts/utils/findEntities.ts -TotalCount 300' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content scripts/utils/findAllProjectEntities.ts -TotalCount 300' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg --files scripts | rg "findAllProjectEntities|find.*Entities"' in C:/Users/me/work/ai/promptbook succeeded in 640ms:
scripts\utils\findAllProjectFilesWithEntities.ts
scripts\utils\findAllProjectEntities.ts
 succeeded in 818ms:
import { readAllProjectFiles } from './readAllProjectFiles';

/**
 * All possible entity types in javascript and typescript
 */
type EntityType = 'const' | 'let' | 'class' | 'function' | 'interface' | 'type' /* <- TODO: More */;

/**
 * Metadata of entity in javascript and typescript
 */
export type EntityMetadata = {
    /**
     * Where is it
     */
    readonly filename: string;

    /**
     * What is it - type, class, function,...
     */
    readonly type: EntityType;
 exited 1 in 865ms:
[31;1mGet-Content: [0m
[31;1m[36;1mLine |[0m
[31;1m[36;1m[36;1m   2 | [0m [36;1mGet-Content scripts/utils/findEntities.ts -TotalCount 300[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m     | [31;1m ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m[31;1m[31;1m[36;1m     | [31;1mCannot find path 'C:\Users\me\work\ai\promptbook\scripts\utils\findEntities.ts' because it does not exist.[0m
codex
The refactor plan is set. I’m going to split the file into a thin `handleChatCompletion` facade plus focused private modules for request parsing, runtime resolution, prompt setup, frozen-chat persistence, and the two response branches, then update the changelog and verify with the required tests.
Plan update
Refactor `handleChatCompletion.ts` into smaller focused private modules while preserving behavior and satisfying naming/JSDoc requirements.
  ✓ Inspect current `handleChatCompletion` flow and surrounding patterns to identify clean extraction boundaries
  → Extract request parsing, runtime resolution, prompt setup, persistence, and response handling into private modules with matching filenames/JSDoc
  • Update changelog entry for the refactor
  • Run `npm run test-name-discrepancies` and `npm run test-package-generation` and fix any issues



thinking
**Considering code handling**

I'm thinking about using a `@private` type for `handleChatCompletion`, which seems okay. I wonder if I should include comments for top-level constants, but there may not be any. That's fine, though! I might need to apply a patch to add new files and replace contents. It’s possible I could patch the entire main file, but it might get quite large. Let’s weigh these options carefully!
thinking
**Planning code updates**

I'm thinking about resolving `resolveCachedServerAgentContext` in the same file. I might need to create a new folder with multiple files, use `apply_patch`, and update the changelog. It seems like I need to read the top of the changelog and probably append a bullet at the beginning, as it starts with bullets. 

After that, I'll need to run tests, possibly starting with `npm run test-name-discrepancies`. It might be quicker to run targeted tests like `npm run test-types` or `tsc` first to catch errors faster. Since the user only requested specific tests, that could save us time, but I’m not sure if it’s necessary. If `npm run test-name-discrepancies` fails or I encounter type errors, I can then run targeted commands. But running package generation might suffice. Perhaps I should run `npm run test-name-discrepancies` first and follow that up with `npm run test-package-generation`, and maybe `npm run test-types` if I don't catch any compile errors.
thinking
**Inspecting project structure**

I need to take a look for an existing `handleChatCompletion` subfolder, but if I find none, I should check the root file names. It seems like I might be doing something wrong if I'm attempting to import from the index file; maybe that’s not the best practice. Once I confirm everything, I guess I can go ahead and patch things accordingly. Let's make sure I’m on the right track before proceeding!
codex
I’ve finished the design pass and I’m editing now. The new layout keeps `handleChatCompletion.ts` as the public entrypoint and moves the internal branches into private files whose exported names match the filenames, so the discrepancy check should stay clean.
2026-04-18T07:50:56.219511Z ERROR codex_core::exec: exec error: The filename or extension is too long. (os error 206)
file update
A C:/Users/me/work/ai/promptbook\apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
import {
    appendMessageSuffix,
    createMessageSuffixAppendix,
    emulateMessageSuffixStreaming,
} from '@/src/utils/chat/messageSuffix';
import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
import { encodeChatStreamWhitespaceForTransport } from '../../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
import { prepareToolCallsForStreaming } from '../toolCallStreaming';
import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';

/**
 * Handles the streaming OpenAI-compatible response branch.
 *
 * @private function of `handleChatCompletion`
 */
export function createHandleChatCompletionStreamResponse(options: {
    request: Request;
    title: string;
    parsedRequest: HandleChatCompletionParsedRequest;
    runtime: HandleChatCompletionRuntime;
    promptContext: HandleChatCompletionPromptContext;
}): Response {
    const { request, title, parsedRequest, runtime, promptContext } = options;
    const encoder = new TextEncoder();
    const responseModel = parsedRequest.model || 'promptbook-agent';
    const readableStream = new ReadableStream({
        async start(controller) {
            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
            const created = Math.floor(Date.now() / 1000);
            const emitSsePayload = (payload: unknown) => {
                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
            };
            const emitDeltaChunk = (deltaContent: string) => {
                emitSsePayload(
                    createHandleChatCompletionChunk({
                        runId,
                        created,
                        model: responseModel,
                        delta: {
                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
                        },
                        finishReason: null,
                    }),
                );
            };

            emitSsePayload(
                createHandleChatCompletionChunk({
                    runId,
                    created,
                    model: responseModel,
                    delta: {
                        role: 'assistant',
                        content: '',
                    },
                    finishReason: null,
                }),
            );

            let hasMeaningfulDelta = false;

            try {
                const handleStreamChunk = createChatStreamHandler({
                    onDelta: (deltaContent) => {
                        if (deltaContent.trim().length > 0) {
                            hasMeaningfulDelta = true;
                        }
                        emitDeltaChunk(deltaContent);
                    },
                    onToolCalls: (toolCalls) => {
                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
                    },
                });
                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
                    signal: request.signal,
                });
                const normalizedResponse = ensureNonEmptyChatContent({
                    content: result.content,
                    context: title,
                });

                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
                    emitDeltaChunk(normalizedResponse.content);
                }

                const messageSuffixAppendix = createMessageSuffixAppendix(
                    normalizedResponse.content,
                    runtime.messageSuffix,
                );
                if (messageSuffixAppendix) {
                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
                        emitDeltaChunk(delta);
                    });
                }

                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
                await finalizeHandleChatCompletionResult({
                    responseContentWithSuffix,
                    usage: result.usage,
                    toolCalls: result.toolCalls,
                    parsedRequest,
                    runtime,
                    promptContext,
                    shouldPersistLearnedAgentSource: true,
                });

                emitSsePayload(
                    createHandleChatCompletionChunk({
                        runId,
                        created,
                        model: responseModel,
                        delta: {},
                        finishReason: 'stop',
                    }),
                );
                controller.enqueue(encoder.encode('[DONE]'));
            } catch (error) {
                console.error('Error during streaming:', error);
                controller.error(error);
            }
            controller.close();
        },
    });

    return new Response(readableStream, {
        headers: {
            'Content-Type': 'text/event-stream',
            'Cache-Control': 'no-cache',
            Connection: 'keep-alive',
        },
    });
}

/**
 * Creates one OpenAI-compatible streaming chunk payload.
 */
function createHandleChatCompletionChunk(options: {
    runId: string;
    created: number;
    model: HandleChatCompletionParsedRequest['model'];
    delta: Record<string, unknown>;
    finishReason: 'stop' | null;
}) {
    return {
        id: options.runId,
        object: 'chat.completion.chunk',
        created: options.created,
        model: options.model,
        choices: [
            {
                index: 0,
                delta: options.delta,
                finish_reason: options.finishReason,
            },
        ],
    };
}
A C:/Users/me/work/ai/promptbook\apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
import type { LlmToolDefinition, TODO_any } from '@promptbook-local/types';
import { NextRequest, NextResponse } from 'next/server';
import type OpenAI from 'openai';
import { HTTP_STATUS_CODES } from '../../constants';
import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';

/**
 * Definition of open AI chat tool.
 */
type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;

/**
 * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
 *
 * @private type of `handleChatCompletion`
 */
export type HandleChatCompletionParsedRequest = {
    agentName: string;
    messages: ReadonlyArray<TODO_any>;
    threadMessages: ReadonlyArray<TODO_any>;
    stream: TODO_any;
    model: TODO_any;
    responseFormat: TODO_any;
    runtimeTools?: Array<LlmToolDefinition>;
    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
    incomingParameters: Record<string, unknown>;
    isPrivateModeEnabled: boolean;
};

/**
 * Parses and validates the OpenAI-compatible request payload.
 *
 * @private function of `handleChatCompletion`
 */
export function parseHandleChatCompletionRequest(options: {
    body: TODO_any;
    agentNameFromParams?: string;
    request: NextRequest;
}): HandleChatCompletionParsedRequest | NextResponse {
    const { body, agentNameFromParams, request } = options;
    const {
        messages,
        stream,
        model,
        response_format: responseFormat,
        tools: rawTools,
        tool_choice: toolChoice,
        parameters: rawParameters = {},
    } = body;
    const agentName = (agentNameFromParams || model) as string | undefined;
    if (!agentName) {
        return createHandleChatCompletionErrorResponse(
            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
            'invalid_request_error',
            HTTP_STATUS_CODES.BAD_REQUEST,
        );
    }

    if (!messages || !Array.isArray(messages) || messages.length === 0) {
        return createHandleChatCompletionErrorResponse(
            'Messages array is required and cannot be empty.',
            'invalid_request_error',
            HTTP_STATUS_CODES.BAD_REQUEST,
        );
    }

    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
    if (threadMessages.length === 0) {
        return createHandleChatCompletionErrorResponse(
            'Messages array must contain at least one non-system message.',
            'invalid_request_error',
            HTTP_STATUS_CODES.BAD_REQUEST,
        );
    }

    return {
        agentName,
        messages,
        threadMessages,
        stream,
        model,
        responseFormat,
        runtimeTools: convertOpenAiTools(rawTools),
        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
        incomingParameters:
            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
                ? (rawParameters as Record<string, unknown>)
                : {},
        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
    };
}

/**
 * Handles convert open Ai tools.
 */
function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
    if (!Array.isArray(rawTools)) {
        return undefined;
    }

    const converted = rawTools
        .map((tool) => convertOpenAiTool(tool))
        .filter((tool): tool is LlmToolDefinition => tool !== null);

    return converted.length > 0 ? converted : undefined;
}

/**
 * Handles convert open Ai tool.
 */
function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
    const tool = rawTool as OpenAIChatToolDefinition;
    if (!tool || tool.type !== 'function') {
        return null;
    }

    const functionDefinition = tool.function;
    if (!functionDefinition) {
        return null;
    }

    const { name, description, parameters } = functionDefinition;
    if (typeof name !== 'string' || name.trim().length === 0) {
        return null;
    }

    const parameterSchema = parameters ?? {};
    if (parameterSchema.type !== 'object') {
        return null;
    }

    const properties = parameterSchema.properties ?? {};
    const normalizedProperties: Record<string, { type: string; description?: string }> = {};

    for (const [key, value] of Object.entries(properties)) {
        if (!value || typeof value !== 'object') {
            continue;
        }

        normalizedProperties[key] = {
            type: typeof value.type === 'string' ? value.type : 'string',
            description: typeof value.description === 'string' ? value.description : undefined,
        };
    }

    const required =
        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
            : undefined;

    return {
        name,
        description: typeof description === 'string' ? description : '',
        parameters: {
            type: 'object',
            properties: normalizedProperties,
            required,
            additionalProperties:
                typeof parameterSchema.additionalProperties === 'boolean'
                    ? parameterSchema.additionalProperties
                    : undefined,
        },
    };
}

/**
 * Parses open Ai tool choice.
 */
function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
    if (value === undefined || value === null) {
        return undefined;
    }

    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
}
A C:/Users/me/work/ai/promptbook\apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
import type { ChatMessage, ChatPrompt } from '@promptbook-local/types';
import { $getCurrentDate } from '@promptbook-local/utils';
import type { NextRequest } from 'next/server';
import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';

/**
 * Prompt, history recorder, and persistence context created for one chat call.
 *
 * @private type of `handleChatCompletion`
 */
export type HandleChatCompletionPromptContext = {
    prompt: ChatPrompt;
    userMessageHash: string;
    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
    persistedFrozenChatId?: string;
};

/**
 * Creates prompt and persistence helpers for one chat completion request.
 *
 * @private function of `handleChatCompletion`
 */
export async function createHandleChatCompletionPromptContext(options: {
    request: NextRequest;
    title: string;
    apiKey: string | null;
    parsedRequest: HandleChatCompletionParsedRequest;
    runtime: HandleChatCompletionRuntime;
}): Promise<HandleChatCompletionPromptContext> {
    const { request, title, apiKey, parsedRequest, runtime } = options;
    const timezone = request.headers.get('x-timezone') || 'UTC';
    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
    const recordChatHistoryMessage = await createChatHistoryRecorder({
        request,
        agentIdentifier: runtime.agentId,
        agentHash: runtime.agentHash,
        source: 'OPENAI_API_COMPATIBILITY',
        apiKey,
        userId: runtime.currentUserIdentity?.userId ?? null,
        isEnabled: !runtime.isPrivateModeEnabled,
    });
    const userMessageHash = await recordChatHistoryMessage({
        message: {
            role: 'USER',
            content: lastMessage.content,
        },
        previousMessageHash: null,
    });
    const promptParameters = composePromptParametersWithMemoryContext({
        baseParameters: {
            ...parsedRequest.incomingParameters,
            timezone,
        },
        currentUserIdentity: runtime.currentUserIdentity,
        agentPermanentId: runtime.agentId,
        agentName: runtime.resolvedAgentContext.resolvedAgentName,
        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
        projectRepositories: runtime.projectRepositories,
        projectGithubToken: runtime.projectGithubToken,
        emailSmtpCredential: runtime.emailSmtpCredential,
        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
        calendarConnections: runtime.calendarConnections,
    });
    const prompt: ChatPrompt = {
        title,
        content: lastMessage.content,
        modelRequirements: {
            modelVariant: 'CHAT',
            responseFormat: parsedRequest.responseFormat,
            toolChoice: parsedRequest.runtimeToolChoice,
        },
        parameters: promptParameters,
        thread,
        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
    };

    return {
        prompt,
        userMessageHash,
        recordChatHistoryMessage,
        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
            isEnabled: !runtime.isPrivateModeEnabled,
            userId: runtime.currentUserIdentity?.userId ?? null,
            agentId: runtime.agentId,
            messages: parsedRequest.messages,
            includeAssistantPlaceholder: true,
            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
        }),
    };
}

/**
 * Builds one OpenAI-compatible thread from the earlier request messages.
 */
function createHandleChatCompletionThread(
    previousMessages: HandleChatCompletionParsedRequest['threadMessages'],
): Array<ChatMessage> {
    return previousMessages.map((message, index) => ({
        id: `msg-${index}`,
        sender: message.role === 'assistant' ? 'agent' : 'user',
        content: message.content,
        isComplete: true,
        createdAt: $getCurrentDate(),
    }));
}
A C:/Users/me/work/ai/promptbook\apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
import { appendMessageSuffix } from '@/src/utils/chat/messageSuffix';
import type { UncertainNumber, Usage, UsageCounts } from '@promptbook-local/types';
import { NextResponse } from 'next/server';
import { computeUsageCounts } from '../../../../../src/execution/utils/computeUsageCounts';
import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';

/**
 * Handles the non-streaming OpenAI-compatible response branch.
 *
 * @private function of `handleChatCompletion`
 */
export async function createHandleChatCompletionJsonResponse(options: {
    title: string;
    parsedRequest: HandleChatCompletionParsedRequest;
    runtime: HandleChatCompletionRuntime;
    promptContext: HandleChatCompletionPromptContext;
}): Promise<NextResponse> {
    const { title, parsedRequest, runtime, promptContext } = options;
    const result = await runtime.agent.callChatModel(promptContext.prompt);
    const normalizedResponse = ensureNonEmptyChatContent({
        content: result.content,
        context: title,
    });
    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);

    await finalizeHandleChatCompletionResult({
        responseContentWithSuffix,
        usage: result.usage,
        toolCalls: result.toolCalls,
        parsedRequest,
        runtime,
        promptContext,
        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
    });

    return NextResponse.json({
        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
        object: 'chat.completion',
        created: Math.floor(Date.now() / 1000),
        model: parsedRequest.model || 'promptbook-agent',
        choices: [
            {
                index: 0,
                message: {
                    role: 'assistant',
                    content: responseContentWithSuffix,
                },
                finish_reason: 'stop',
            },
        ],
        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
    });
}

/**
 * Falls back to the estimated value when the original token count is unknown.
 *
 * @param tokenCount - Token count reported by the execution tools.
 * @param fallbackValue - Estimated token count based on text length.
 * @returns Token count to report in the OpenAI-compatible response.
 */
function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
    if (tokenCount.value === 0 && tokenCount.isUncertain) {
        return {
            value: fallbackValue,
            isUncertain: true,
        };
    }

    return tokenCount;
}

/**
 * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
 *
 * @param promptContent - Prompt content used for the request.
 * @param completionContent - Assistant response content.
 * @param usage - Native Promptbook usage details.
 * @returns Usage payload for compatibility responses.
 */
function createCompatibilityUsage(
    promptContent: string,
    completionContent: string,
    usage: Usage,
): {
    prompt_tokens: number;
    completion_tokens: number;
    total_tokens: number;
    details: Usage;
} {
    const promptCounts = computeUsageCounts(promptContent);
    const completionCounts = computeUsageCounts(completionContent);
    const inputUsage: UsageCounts = {
        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
        ...promptCounts,
    };
    const outputUsage: UsageCounts = {
        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
        ...completionCounts,
    };

    const promptTokens = inputUsage.tokensCount.value;
    const completionTokens = outputUsage.tokensCount.value;

    return {
        prompt_tokens: promptTokens,
        completion_tokens: completionTokens,
        total_tokens: promptTokens + completionTokens,
        details: {
            price: usage.price,
            duration: usage.duration,
            input: inputUsage,
            output: outputUsage,
        },
    };
}
A C:/Users/me/work/ai/promptbook\apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
import type { string_book, TODO_any } from '@promptbook-local/types';
import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
import {
    createBookScopedAgentReferenceResolver,
    parseBookScopedAgentIdentifier,
} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
import {
    appendMessageSuffix,
    resolveMessageSuffixFromAgentSource,
} from '@/src/utils/chat/messageSuffix';
import { NextRequest, NextResponse } from 'next/server';
import { isAgentDeleted } from '../../app/agents/[agentName]/_utils';
import {
    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
    resolveAgentCollectionTablePrefix,
    waitForRunningAgentPreparation,
} from '../agentPreparation';
import { resolveCachedServerAgentContext, resolveCachedServerAgentModelRequirements } from '../cachedServerAgentRuntime';
import { AgentKitCacheManager } from '../cache/AgentKitCacheManager';
import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';

/**
 * Runtime state resolved before the chat model is executed.
 *
 * @private type of `handleChatCompletion`
 */
export type HandleChatCompletionRuntime = {
    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
    agent: Agent;
    agentSource: string_book;
    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
    messageSuffix: string | null;
    agentId: string;
    agentHash: string;
    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
    isPrivateModeEnabled: boolean;
};

/**
 * Resolves agent/runtime dependencies needed before prompt execution.
 *
 * @private function of `handleChatCompletion`
 */
export async function resolveHandleChatCompletionRuntime(options: {
    request: NextRequest;
    agentName: string;
    messages: ReadonlyArray<TODO_any>;
    isPrivateModeEnabled: boolean;
}): Promise<HandleChatCompletionRuntime | NextResponse> {
    const { request, agentName, messages, isPrivateModeEnabled } = options;
    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;

    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
        return createHandleChatCompletionErrorResponse(
            'This agent has been deleted. You can restore it from the Recycle Bin.',
            'agent_deleted',
            410,
        );
    }

    const [collection, baseAgentReferenceResolver] = await Promise.all([
        $provideAgentCollectionForServer(),
        $provideAgentReferenceResolver(),
    ]);
    const localServerUrl = new URL(request.url).origin;
    const resolvedAgentContext = await resolveCachedServerAgentContext({
        collection,
        agentIdentifier: agentName,
        localServerUrl,
        fallbackResolver: baseAgentReferenceResolver,
    }).catch(() => null);

    if (!resolvedAgentContext?.resolvedAgentSource) {
        return createHandleChatCompletionErrorResponse(`Agent '${agentName}' not found.`, 'invalid_request_error', 404);
    }

    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
    agentSource = agentSourceWithContext;

    const preparedAgentModelRequirements = !hasDynamicContext
        ? await resolveCachedServerAgentModelRequirements({
              resolvedAgentContext,
              localServerUrl,
              fallbackResolver: baseAgentReferenceResolver,
          })
        : null;
    const agentId = resolvedAgentContext.parentAgentPermanentId;
    const agentHash = computeAgentHash(agentSource);
    await waitForHandleChatCompletionAgentPreparation({
        collection,
        agentId,
        agentHash,
    });

    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
        await resolveHandleChatCompletionCredentials({
            currentUserIdentity,
            agentId,
            calendarConnections,
            useEmailConfiguration,
        });
    const agent = await createHandleChatCompletionAgent({
        agentName,
        agentSource,
        agentId,
        resolvedAgentContext,
        localServerUrl,
        baseAgentReferenceResolver,
        hasDynamicContext,
        preparedAgentModelRequirements,
    });

    return {
        collection,
        resolvedAgentContext,
        agent,
        agentSource,
        unresolvedAgentSource,
        messageSuffix,
        agentId,
        agentHash,
        projectRepositories,
        calendarConnections,
        useEmailConfiguration,
        currentUserIdentity,
        projectGithubToken,
        calendarGoogleAccessToken,
        emailSmtpCredential,
        isPrivateModeEnabled,
    };
}

/**
 * Appends system messages to the agent source as dynamic `CONTEXT`.
 */
function appendSystemMessagesToAgentSource(
    agentSource: string_book,
    messages: ReadonlyArray<TODO_any>,
): {
    agentSourceWithContext: string_book;
    hasDynamicContext: boolean;
} {
    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
    if (systemMessages.length === 0) {
        return {
            agentSourceWithContext: agentSource,
            hasDynamicContext: false,
        };
    }

    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
    return {
        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
        hasDynamicContext: true,
    };
}

/**
 * Waits for any running preparation job of the current agent before executing the chat call.
 */
async function waitForHandleChatCompletionAgentPreparation(options: {
    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
    agentId: string;
    agentHash: string;
}): Promise<void> {
    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
    const preparationWaitResult = await waitForRunningAgentPreparation({
        tablePrefix,
        agentPermanentId: options.agentId,
        fingerprint: options.agentHash,
        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
    });

    if (preparationWaitResult !== 'not_running') {
        console.info('[pre-index]', 'openai_chat_wait_result', {
            tablePrefix,
            agentPermanentId: options.agentId,
            agentHash: options.agentHash,
            preparationWaitResult,
        });
    }
}

/**
 * Resolves request-scoped project, calendar, and email credentials for the current agent.
 */
async function resolveHandleChatCompletionCredentials(options: {
    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
    agentId: string;
    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
}): Promise<{
    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
}> {
    const userId = options.currentUserIdentity?.userId;
    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
        resolveUseProjectGithubToken({
            userId,
            agentPermanentId: options.agentId,
        }),
        options.calendarConnections.length > 0
            ? resolveUseCalendarGoogleToken({
                  userId,
                  agentPermanentId: options.agentId,
              })
            : Promise.resolve(undefined),
        options.useEmailConfiguration.isEnabled
            ? resolveUseEmailSmtpCredential({
                  userId,
                  agentPermanentId: options.agentId,
              })
            : Promise.resolve(undefined),
    ]);

    return {
        projectGithubToken,
        calendarGoogleAccessToken,
        emailSmtpCredential,
    };
}

/**
 * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
 */
async function createHandleChatCompletionAgent(options: {
    agentName: string;
    agentSource: string_book;
    agentId: string;
    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
    localServerUrl: string;
    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
    hasDynamicContext: boolean;
    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
}): Promise<Agent> {
    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
    const runtimeAgentReferenceResolver = options.hasDynamicContext
        ? createBookScopedAgentReferenceResolver({
              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
              localServerUrl: options.localServerUrl,
              fallbackResolver: options.baseAgentReferenceResolver,
          })
        : undefined;
    const agentModelRequirements =
        options.preparedAgentModelRequirements?.modelRequirements ||
        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
            agentReferenceResolver: runtimeAgentReferenceResolver,
            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
        }));

    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
        options.agentSource,
        options.resolvedAgentContext.resolvedAgentName,
        await baseOpenAiToolsPromise,
        {
            includeDynamicContext: true,
            agentId: options.agentId,
            modelRequirements: agentModelRequirements,
        },
    );

    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
        agentName: options.agentName,
        assistantCacheKey: agentKitResult.assistantCacheKey,
        vectorStoreHash: agentKitResult.vectorStoreHash,
        vectorStoreId: agentKitResult.vectorStoreId,
    });

    return new Agent({
        agentSource: options.agentSource,
        executionTools: {
            llm: agentKitResult.tools,
        },
        precomputedModelRequirements: agentModelRequirements,
        assistantPreparationMode: 'external',
        isVerbose: true,
        teacherAgent: null,
    });
}
A C:/Users/me/work/ai/promptbook\apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
import { resolveAppendOnlySelfLearningAgentSource } from '@/src/utils/resolveAppendOnlySelfLearningAgentSource';
import type { ToolCall, Usage } from '@promptbook-local/types';
import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';

/**
 * Runs the shared side effects after one successful model response.
 *
 * @private function of `handleChatCompletion`
 */
export async function finalizeHandleChatCompletionResult(options: {
    responseContentWithSuffix: string;
    usage: Usage;
    toolCalls?: ReadonlyArray<ToolCall>;
    parsedRequest: HandleChatCompletionParsedRequest;
    runtime: HandleChatCompletionRuntime;
    promptContext: HandleChatCompletionPromptContext;
    shouldPersistLearnedAgentSource: boolean;
}): Promise<void> {
    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;

    await promptContext.recordChatHistoryMessage({
        message: {
            role: 'MODEL',
            content: responseContentWithSuffix,
        },
        previousMessageHash: promptContext.userMessageHash,
        usage,
    });

    if (toolCalls && toolCalls.length > 0) {
        await logCalendarToolCallsActivity({
            userId: runtime.currentUserIdentity?.userId ?? null,
            agentPermanentId: runtime.agentId,
            toolCalls,
        });
    }

    await persistOpenAiCompatibilityFrozenChat({
        isEnabled: !runtime.isPrivateModeEnabled,
        userId: runtime.currentUserIdentity?.userId ?? null,
        agentId: runtime.agentId,
        chatId: promptContext.persistedFrozenChatId,
        messages: parsedRequest.messages,
        assistantContent: responseContentWithSuffix,
        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
    });
    await persistHandleChatCompletionLearnedAgentSource({
        runtime,
        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
    });
}

/**
 * Applies append-only self-learning updates when the current branch should persist them.
 */
async function persistHandleChatCompletionLearnedAgentSource(options: {
    runtime: HandleChatCompletionRuntime;
    shouldPersistLearnedAgentSource: boolean;
}): Promise<void> {
    const { runtime, shouldPersistLearnedAgentSource } = options;
    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
        return;
    }

    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
        resolvedAgentSourceBeforeLearning: runtime.agentSource,
        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
    });

    if (learnedAgentSource !== null) {
        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
    }
}
A C:/Users/me/work/ai/promptbook\apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
import type { ChatMessage } from '@promptbook-local/types';

/**
 * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
 */
type OpenAiCompatibilityMessageLike = {
    role?: unknown;
    content?: unknown;
};

/**
 * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
 *
 * @private function of `handleChatCompletion`
 */
export async function persistOpenAiCompatibilityFrozenChat(options: {
    isEnabled: boolean;
    userId: number | null | undefined;
    agentId: string;
    messages: ReadonlyArray<unknown>;
    chatId?: string;
    assistantContent?: string;
    includeAssistantPlaceholder?: boolean;
    failureMessage: string;
}): Promise<string | undefined> {
    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
        options;
    if (!isEnabled || !userId) {
        return undefined;
    }

    const persistedFrozenChat = await persistFrozenUserChat({
        userId,
        agentPermanentId: agentId,
        source: USER_CHAT_SOURCES.OPENAI_API,
        chatId,
        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
            includeAssistantPlaceholder,
        }),
    }).catch((error) => {
        console.error(failureMessage, error);
        return null;
    });

    return persistedFrozenChat?.id;
}

/**
 * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
 */
function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
    if (typeof value === 'string') {
        return value;
    }

    if (Array.isArray(value)) {
        return value
            .map((part) => {
                if (typeof part === 'string') {
                    return part;
                }

                if (!part || typeof part !== 'object') {
                    return '';
                }

                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
                if (typeof partLike.text === 'string') {
                    return partLike.text;
                }

                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
                    return '[Image input]';
                }

                if (partLike.image_url) {
                    return '[Image input]';
                }

                return '';
            })
            .filter(Boolean)
            .join('\n');
    }

    if (value === null || value === undefined) {
        return '';
    }

    try {
        return JSON.stringify(value, null, 2);
    } catch {
        return String(value);
    }
}

/**
 * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
 */
function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
    if (typeof role !== 'string') {
        return 'USER';
    }

    switch (role) {
        case 'assistant':
            return 'AGENT';
        case 'system':
            return 'SYSTEM';
        case 'tool':
            return 'TOOL';
        case 'developer':
            return 'SYSTEM';
        default:
            return 'USER';
    }
}

/**
 * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
 */
function createFrozenOpenAiCompatibilityMessages(
    rawMessages: ReadonlyArray<unknown>,
    assistantContent?: string,
    options: {
        includeAssistantPlaceholder?: boolean;
    } = {},
): Array<ChatMessage> {
    const startedAt = Date.now();
    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
        const message = rawMessage as OpenAiCompatibilityMessageLike;

        return {
            id: `openai-frozen-${index}`,
            sender: mapOpenAiCompatibilityRoleToSender(message.role),
            content: normalizeOpenAiCompatibilityMessageContent(message.content),
            isComplete: true,
            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
        } satisfies ChatMessage;
    });

    if (assistantContent !== undefined) {
        messages.push({
            id: `openai-frozen-${messages.length}`,
            sender: 'AGENT',
            content: assistantContent,
            isComplete: true,
            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
        } satisfies ChatMessage);
    } else if (options.includeAssistantPlaceholder) {
        messages.push({
            id: `openai-frozen-${messages.length}`,
            sender: 'AGENT',
            content: '',
            isComplete: false,
            lifecycleState: 'running',
            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
        } satisfies ChatMessage);
    }

    return messages;
}
A C:/Users/me/work/ai/promptbook\apps/agents-server/src/utils/handleChatCompletion.ts
import { NextRequest, NextResponse } from 'next/server';
import { HTTP_STATUS_CODES } from '../constants';
import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
import { validateApiKey } from './validateApiKey';

/**
 * Handles convert open Ai chat completion requests routed through the Agents Server.
 */
export async function handleChatCompletion(
    request: NextRequest,
    params: { agentName?: string },
    title: string = 'API Chat Completion',
) {
    const { agentName: agentNameFromParams } = params;

    const versionMismatchResponse = respondIfClientVersionIsOutdated(request, 'json', { mode: 'api' });
    if (versionMismatchResponse) {
        return versionMismatchResponse;
    }

    const apiKeyValidation = await validateApiKey(request);
    if (!apiKeyValidation.isValid) {
        return NextResponse.json(
            {
                error: {
                    message: apiKeyValidation.error || 'Invalid API key',
                    type: 'authentication_error',
                },
            },
            { status: HTTP_STATUS_CODES.UNAUTHORIZED },
        );
    }
    const apiKey = apiKeyValidation.token || null;

    try {
        const parsedRequest = parseHandleChatCompletionRequest({
            body: await request.json(),
            agentNameFromParams,
            request,
        });
        if (parsedRequest instanceof NextResponse) {
            return parsedRequest;
        }

        const runtime = await resolveHandleChatCompletionRuntime({
            request,
            agentName: parsedRequest.agentName,
            messages: parsedRequest.messages,
            isPrivateModeEnabled: parsedRequest.isPrivateModeEnabled,
        });
        if (runtime instanceof NextResponse) {
            return runtime;
        }

        const promptContext = await createHandleChatCompletionPromptContext({
            request,
            title,
            apiKey,
            parsedRequest,
            runtime,
        });

        return parsedRequest.stream
            ? createHandleChatCompletionStreamResponse({
                  request,
                  title,
                  parsedRequest,
                  runtime,
                  promptContext,
              })
            : createHandleChatCompletionJsonResponse({
                  title,
                  parsedRequest,
                  runtime,
                  promptContext,
              });
    } catch (error) {
        console.error(`Error in ${title} handler:`, error);
        return NextResponse.json(
            { error: { message: (error as Error).message || 'Internal Server Error', type: 'server_error' } },
            { status: HTTP_STATUS_CODES.INTERNAL_SERVER_ERROR },
        );
    }
}
A C:/Users/me/work/ai/promptbook\apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
import { NextResponse } from 'next/server';

/**
 * Creates one consistent OpenAI-compatible error response.
 *
 * @private function of `handleChatCompletion`
 */
export function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
    return NextResponse.json(
        {
            error: {
                message,
                type,
            },
        },
        { status },
    );
}
apply_patch(auto_approved=true) exited 1 in 6ms:
execution error: Io(Os { code: 206, kind: InvalidFilename, message: "The filename or extension is too long." })

thinking
**Considering patch issues**

I’m thinking that the patch might be too large or there could be issues with the Windows path. Maybe the command line for apply_patch is too long. I should split it into smaller parts. First, I could try deleting and adding the main file separately. I'll also need to inspect whether the patch has partially applied, which I doubt. Using smaller apply_patch calls might be the way to go. I should check if the old file exists too.
codex
The first patch was too large for the Windows tool invocation, so I’m applying the refactor in smaller chunks. I’m keeping the structure the same as planned; this is just a tooling limitation while writing the files.

thinking
**Planning file updates**

I think I need to create files through multiple apply_patch calls. First, I'll delete the old main file and add the new one. After that, I can add the modules one by one or in small groups. It's essential to ensure there's no parallel execution with apply_patch. Maybe I should use the shell to check that the current file remains unchanged during the process. This feels like a solid plan!
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content apps/agents-server/src/utils/handleChatCompletion.ts -TotalCount 20' in C:/Users/me/work/ai/promptbook succeeded in 441ms:
import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
import {
    createBookScopedAgentReferenceResolver,
    parseBookScopedAgentIdentifier,
} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
import {
    appendMessageSuffix,
    createMessageSuffixAppendix,
    emulateMessageSuffixStreaming,
    resolveMessageSuffixFromAgentSource,
} from '@/src/utils/chat/messageSuffix';
import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
import { prepareToolCallsForStreaming } from './toolCallStreaming';
import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';

thinking
**Considering file management**

I’m thinking about whether I need to create a new folder. I’m guessing that applying a patch to add files will indeed create directories, but I want to double-check that. I’ll probably start by replacing the main file first since it's smaller. In that case, I should use "Delete" and "Add" to make those changes effectively. Let's move forward with that plan!
codex
I’m replacing the facade file first so the rest of the extracted modules can slot in cleanly. After that I’ll add each private helper module in smaller patches.
file update
A C:/Users/me/work/ai/promptbook\apps/agents-server/src/utils/handleChatCompletion.ts
import { NextRequest, NextResponse } from 'next/server';
import { HTTP_STATUS_CODES } from '../constants';
import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
import { validateApiKey } from './validateApiKey';

/**
 * Handles convert open Ai chat completion requests routed through the Agents Server.
 */
export async function handleChatCompletion(
    request: NextRequest,
    params: { agentName?: string },
    title: string = 'API Chat Completion',
) {
    const { agentName: agentNameFromParams } = params;

    const versionMismatchResponse = respondIfClientVersionIsOutdated(request, 'json', { mode: 'api' });
    if (versionMismatchResponse) {
        return versionMismatchResponse;
    }

    const apiKeyValidation = await validateApiKey(request);
    if (!apiKeyValidation.isValid) {
        return NextResponse.json(
            {
                error: {
                    message: apiKeyValidation.error || 'Invalid API key',
                    type: 'authentication_error',
                },
            },
            { status: HTTP_STATUS_CODES.UNAUTHORIZED },
        );
    }
    const apiKey = apiKeyValidation.token || null;

    try {
        const parsedRequest = parseHandleChatCompletionRequest({
            body: await request.json(),
            agentNameFromParams,
            request,
        });
        if (parsedRequest instanceof NextResponse) {
            return parsedRequest;
        }

        const runtime = await resolveHandleChatCompletionRuntime({
            request,
            agentName: parsedRequest.agentName,
            messages: parsedRequest.messages,
            isPrivateModeEnabled: parsedRequest.isPrivateModeEnabled,
        });
        if (runtime instanceof NextResponse) {
            return runtime;
        }

        const promptContext = await createHandleChatCompletionPromptContext({
            request,
            title,
            apiKey,
            parsedRequest,
            runtime,
        });

        return parsedRequest.stream
            ? createHandleChatCompletionStreamResponse({
                  request,
                  title,
                  parsedRequest,
                  runtime,
                  promptContext,
              })
            : createHandleChatCompletionJsonResponse({
                  title,
                  parsedRequest,
                  runtime,
                  promptContext,
              });
    } catch (error) {
        console.error(`Error in ${title} handler:`, error);
        return NextResponse.json(
            { error: { message: (error as Error).message || 'Internal Server Error', type: 'server_error' } },
            { status: HTTP_STATUS_CODES.INTERNAL_SERVER_ERROR },
        );
    }
}
apply_patch(auto_approved=true) exited 0 in 54ms:
Success. Updated the following files:
A apps/agents-server/src/utils/handleChatCompletion.ts
D apps/agents-server/src/utils/handleChatCompletion.ts
file update:
diff --git a/apps/agents-server/src/utils/handleChatCompletion.ts b/apps/agents-server/src/utils/handleChatCompletion.ts
index 9c96d36fad9cf24bf26d89f57372332add4e4595..cff56fafd6cfa90bb4094f9f202c4f01f6f74af1
--- a/apps/agents-server/src/utils/handleChatCompletion.ts
+++ b/apps/agents-server/src/utils/handleChatCompletion.ts
@@ -1,392 +1,16 @@
-import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
-import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
-import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
-import {
-    createBookScopedAgentReferenceResolver,
-    parseBookScopedAgentIdentifier,
-} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
-import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
-import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
-import {
-    appendMessageSuffix,
-    createMessageSuffixAppendix,
-    emulateMessageSuffixStreaming,
-    resolveMessageSuffixFromAgentSource,
-} from '@/src/utils/chat/messageSuffix';
-import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
-import { prepareToolCallsForStreaming } from './toolCallStreaming';
-import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
-import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
-import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
-import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
-import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
-import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
-import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
-import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
-import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
-import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
-import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
-import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
-import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
-import type {
-    ChatMessage,
-    ChatPrompt,
-    LlmToolDefinition,
-    string_book,
-    ToolCall,
-    TODO_any,
-    UncertainNumber,
-    Usage,
-    UsageCounts,
-} from '@promptbook-local/types';
-import { $getCurrentDate } from '@promptbook-local/utils';
 import { NextRequest, NextResponse } from 'next/server';
-import type OpenAI from 'openai';
-import { computeUsageCounts } from '../../../../src/execution/utils/computeUsageCounts';
-import { encodeChatStreamWhitespaceForTransport } from '../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
-import { isAgentDeleted } from '../app/agents/[agentName]/_utils';
 import { HTTP_STATUS_CODES } from '../constants';
-import { AgentKitCacheManager } from './cache/AgentKitCacheManager';
 import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
+import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
+import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
+import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
+import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
+import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
 import { validateApiKey } from './validateApiKey';
-import {
-    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    resolveAgentCollectionTablePrefix,
-    waitForRunningAgentPreparation,
-} from './agentPreparation';
-import {
-    resolveCachedServerAgentContext,
-    resolveCachedServerAgentModelRequirements,
-} from './cachedServerAgentRuntime';
-import { resolveAppendOnlySelfLearningAgentSource } from './resolveAppendOnlySelfLearningAgentSource';
 
 /**
- * Falls back to the estimated value when the original token count is unknown.
- *
- * @param tokenCount - Token count reported by the execution tools.
- * @param fallbackValue - Estimated token count based on text length.
- * @returns Token count to report in the OpenAI-compatible response.
- */
-function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
-    if (tokenCount.value === 0 && tokenCount.isUncertain) {
-        return {
-            value: fallbackValue,
-            isUncertain: true,
-        };
-    }
-
-    return tokenCount;
-}
-
-/**
- * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
- *
- * @param promptContent - Prompt content used for the request.
- * @param completionContent - Assistant response content.
- * @param usage - Native Promptbook usage details.
- * @returns Usage payload for compatibility responses.
- */
-function createCompatibilityUsage(
-    promptContent: string,
-    completionContent: string,
-    usage: Usage,
-): {
-    prompt_tokens: number;
-    completion_tokens: number;
-    total_tokens: number;
-    details: Usage;
-} {
-    const promptCounts = computeUsageCounts(promptContent);
-    const completionCounts = computeUsageCounts(completionContent);
-    const inputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
-        ...promptCounts,
-    };
-    const outputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
-        ...completionCounts,
-    };
-
-    const promptTokens = inputUsage.tokensCount.value;
-    const completionTokens = outputUsage.tokensCount.value;
-
-    return {
-        prompt_tokens: promptTokens,
-        completion_tokens: completionTokens,
-        total_tokens: promptTokens + completionTokens,
-        details: {
-            price: usage.price,
-            duration: usage.duration,
-            input: inputUsage,
-            output: outputUsage,
-        },
-    };
-}
-
-/**
- * Definition of open AI chat tool.
- */
-type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
-
-/**
- * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
- */
-type OpenAiCompatibilityMessageLike = {
-    role?: unknown;
-    content?: unknown;
-};
-
-/**
- * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
- */
-type HandleChatCompletionParsedRequest = {
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    threadMessages: ReadonlyArray<TODO_any>;
-    stream: TODO_any;
-    model: TODO_any;
-    responseFormat: TODO_any;
-    runtimeTools?: Array<LlmToolDefinition>;
-    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-    incomingParameters: Record<string, unknown>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Runtime state resolved before the chat model is executed.
- */
-type HandleChatCompletionRuntime = {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    agent: Agent;
-    agentSource: string_book;
-    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
-    messageSuffix: string | null;
-    agentId: string;
-    agentHash: string;
-    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Prompt, history recorder, and persistence context created for one chat call.
- */
-type HandleChatCompletionPromptContext = {
-    prompt: ChatPrompt;
-    userMessageHash: string;
-    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
-    persistedFrozenChatId?: string;
-};
-
-/**
- * Handles convert open Ai tools.
- */
-function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
-    if (!Array.isArray(rawTools)) {
-        return undefined;
-    }
-
-    const converted = rawTools
-        .map((tool) => convertOpenAiTool(tool))
-        .filter((tool): tool is LlmToolDefinition => tool !== null);
-
-    return converted.length > 0 ? converted : undefined;
-}
-
-/**
- * Handles convert open Ai tool.
- */
-function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
-    const tool = rawTool as OpenAIChatToolDefinition;
-    if (!tool || tool.type !== 'function') {
-        return null;
-    }
-
-    const functionDefinition = tool.function;
-    if (!functionDefinition) {
-        return null;
-    }
-
-    const { name, description, parameters } = functionDefinition;
-    if (typeof name !== 'string' || name.trim().length === 0) {
-        return null;
-    }
-
-    const parameterSchema = parameters ?? {};
-    if (parameterSchema.type !== 'object') {
-        return null;
-    }
-
-    const properties = parameterSchema.properties ?? {};
-    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
-
-    for (const [key, value] of Object.entries(properties)) {
-        if (!value || typeof value !== 'object') {
-            continue;
-        }
-
-        normalizedProperties[key] = {
-            type: typeof value.type === 'string' ? value.type : 'string',
-            description: typeof value.description === 'string' ? value.description : undefined,
-        };
-    }
-
-    const required =
-        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
-            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
-            : undefined;
-
-    return {
-        name,
-        description: typeof description === 'string' ? description : '',
-        parameters: {
-            type: 'object',
-            properties: normalizedProperties,
-            required,
-            additionalProperties:
-                typeof parameterSchema.additionalProperties === 'boolean'
-                    ? parameterSchema.additionalProperties
-                    : undefined,
-        },
-    };
-}
-
-/**
- * Parses open Ai tool choice.
- */
-function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
-    if (value === undefined || value === null) {
-        return undefined;
-    }
-
-    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-}
-
-/**
- * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
- */
-function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
-    if (typeof value === 'string') {
-        return value;
-    }
-
-    if (Array.isArray(value)) {
-        return value
-            .map((part) => {
-                if (typeof part === 'string') {
-                    return part;
-                }
-
-                if (!part || typeof part !== 'object') {
-                    return '';
-                }
-
-                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
-                if (typeof partLike.text === 'string') {
-                    return partLike.text;
-                }
-
-                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
-                    return '[Image input]';
-                }
-
-                if (partLike.image_url) {
-                    return '[Image input]';
-                }
-
-                return '';
-            })
-            .filter(Boolean)
-            .join('\n');
-    }
-
-    if (value === null || value === undefined) {
-        return '';
-    }
-
-    try {
-        return JSON.stringify(value, null, 2);
-    } catch {
-        return String(value);
-    }
-}
-
-/**
- * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ * Handles convert open Ai chat completion requests routed through the Agents Server.
  */
-function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
-    if (typeof role !== 'string') {
-        return 'USER';
-    }
-
-    switch (role) {
-        case 'assistant':
-            return 'AGENT';
-        case 'system':
-            return 'SYSTEM';
-        case 'tool':
-            return 'TOOL';
-        case 'developer':
-            return 'SYSTEM';
-        default:
-            return 'USER';
-    }
-}
-
-/**
- * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
- */
-function createFrozenOpenAiCompatibilityMessages(
-    rawMessages: ReadonlyArray<unknown>,
-    assistantContent?: string,
-    options: {
-        includeAssistantPlaceholder?: boolean;
-    } = {},
-): Array<ChatMessage> {
-    const startedAt = Date.now();
-    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
-        const message = rawMessage as OpenAiCompatibilityMessageLike;
-
-        return {
-            id: `openai-frozen-${index}`,
-            sender: mapOpenAiCompatibilityRoleToSender(message.role),
-            content: normalizeOpenAiCompatibilityMessageContent(message.content),
-            isComplete: true,
-            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage;
-    });
-
-    if (assistantContent !== undefined) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: assistantContent,
-            isComplete: true,
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    } else if (options.includeAssistantPlaceholder) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: '',
-            isComplete: false,
-            lifecycleState: 'running',
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    }
-
-    return messages;
-}
-
-/**
- * Handles chat completion.
- */
 export async function handleChatCompletion(
     request: NextRequest,
     params: { agentName?: string },
@@ -399,7 +23,6 @@
         return versionMismatchResponse;
     }
 
-    // Validate API key explicitly (in addition to middleware)
     const apiKeyValidation = await validateApiKey(request);
     if (!apiKeyValidation.isValid) {
         return NextResponse.json(
@@ -462,724 +85,5 @@
             { error: { message: (error as Error).message || 'Internal Server Error', type: 'server_error' } },
             { status: HTTP_STATUS_CODES.INTERNAL_SERVER_ERROR },
         );
-    }
-}
-
-/**
- * Parses and validates the OpenAI-compatible request payload.
- */
-function parseHandleChatCompletionRequest(options: {
-    body: TODO_any;
-    agentNameFromParams?: string;
-    request: NextRequest;
-}): HandleChatCompletionParsedRequest | NextResponse {
-    const { body, agentNameFromParams, request } = options;
-    const {
-        messages,
-        stream,
-        model,
-        response_format: responseFormat,
-        tools: rawTools,
-        tool_choice: toolChoice,
-        parameters: rawParameters = {},
-    } = body;
-    const agentName = (agentNameFromParams || model) as string | undefined;
-    if (!agentName) {
-        return createHandleChatCompletionErrorResponse(
-            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    if (!messages || !Array.isArray(messages) || messages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array is required and cannot be empty.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
-    if (threadMessages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array must contain at least one non-system message.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    return {
-        agentName,
-        messages,
-        threadMessages,
-        stream,
-        model,
-        responseFormat,
-        runtimeTools: convertOpenAiTools(rawTools),
-        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
-        incomingParameters:
-            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
-                ? (rawParameters as Record<string, unknown>)
-                : {},
-        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
-    };
-}
-
-/**
- * Resolves agent/runtime dependencies needed before prompt execution.
- */
-async function resolveHandleChatCompletionRuntime(options: {
-    request: NextRequest;
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    isPrivateModeEnabled: boolean;
-}): Promise<HandleChatCompletionRuntime | NextResponse> {
-    const { request, agentName, messages, isPrivateModeEnabled } = options;
-    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
-    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
-
-    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
-        return createHandleChatCompletionErrorResponse(
-            'This agent has been deleted. You can restore it from the Recycle Bin.',
-            'agent_deleted',
-            410,
-        );
-    }
-
-    const [collection, baseAgentReferenceResolver] = await Promise.all([
-        $provideAgentCollectionForServer(),
-        $provideAgentReferenceResolver(),
-    ]);
-    const localServerUrl = new URL(request.url).origin;
-    const resolvedAgentContext = await resolveCachedServerAgentContext({
-        collection,
-        agentIdentifier: agentName,
-        localServerUrl,
-        fallbackResolver: baseAgentReferenceResolver,
-    }).catch(() => null);
-
-    if (!resolvedAgentContext?.resolvedAgentSource) {
-        return createHandleChatCompletionErrorResponse(
-            `Agent '${agentName}' not found.`,
-            'invalid_request_error',
-            HTTP_STATUS_CODES.NOT_FOUND,
-        );
-    }
-
-    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
-    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
-    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
-    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
-    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
-    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
-    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
-    agentSource = agentSourceWithContext;
-
-    const preparedAgentModelRequirements = !hasDynamicContext
-        ? await resolveCachedServerAgentModelRequirements({
-              resolvedAgentContext,
-              localServerUrl,
-              fallbackResolver: baseAgentReferenceResolver,
-          })
-        : null;
-    const agentId = resolvedAgentContext.parentAgentPermanentId;
-    const agentHash = computeAgentHash(agentSource);
-    await waitForHandleChatCompletionAgentPreparation({
-        collection,
-        agentId,
-        agentHash,
-    });
-
-    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
-    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
-        await resolveHandleChatCompletionCredentials({
-            currentUserIdentity,
-            agentId,
-            calendarConnections,
-            useEmailConfiguration,
-        });
-    const agent = await createHandleChatCompletionAgent({
-        agentName,
-        agentSource,
-        agentId,
-        resolvedAgentContext,
-        localServerUrl,
-        baseAgentReferenceResolver,
-        hasDynamicContext,
-        preparedAgentModelRequirements,
-    });
-
-    return {
-        collection,
-        resolvedAgentContext,
-        agent,
-        agentSource,
-        unresolvedAgentSource,
-        messageSuffix,
-        agentId,
-        agentHash,
-        projectRepositories,
-        calendarConnections,
-        useEmailConfiguration,
-        currentUserIdentity,
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-        isPrivateModeEnabled,
-    };
-}
-
-/**
- * Creates prompt and persistence helpers for one chat completion request.
- */
-async function createHandleChatCompletionPromptContext(options: {
-    request: NextRequest;
-    title: string;
-    apiKey: string | null;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-}): Promise<HandleChatCompletionPromptContext> {
-    const { request, title, apiKey, parsedRequest, runtime } = options;
-    const timezone = request.headers.get('x-timezone') || 'UTC';
-    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
-    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
-    const recordChatHistoryMessage = await createChatHistoryRecorder({
-        request,
-        agentIdentifier: runtime.agentId,
-        agentHash: runtime.agentHash,
-        source: 'OPENAI_API_COMPATIBILITY',
-        apiKey,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        isEnabled: !runtime.isPrivateModeEnabled,
-    });
-    const userMessageHash = await recordChatHistoryMessage({
-        message: {
-            role: 'USER',
-            content: lastMessage.content,
-        },
-        previousMessageHash: null,
-    });
-    const promptParameters = composePromptParametersWithMemoryContext({
-        baseParameters: {
-            ...parsedRequest.incomingParameters,
-            timezone,
-        },
-        currentUserIdentity: runtime.currentUserIdentity,
-        agentPermanentId: runtime.agentId,
-        agentName: runtime.resolvedAgentContext.resolvedAgentName,
-        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
-        projectRepositories: runtime.projectRepositories,
-        projectGithubToken: runtime.projectGithubToken,
-        emailSmtpCredential: runtime.emailSmtpCredential,
-        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
-        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
-        calendarConnections: runtime.calendarConnections,
-    });
-    const prompt: ChatPrompt = {
-        title,
-        content: lastMessage.content,
-        modelRequirements: {
-            modelVariant: 'CHAT',
-            responseFormat: parsedRequest.responseFormat,
-            toolChoice: parsedRequest.runtimeToolChoice,
-            // We could pass 'model' from body if we wanted to enforce it, but Agent usually has its own config
-        },
-        parameters: promptParameters,
-        thread,
-        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
-    };
-
-    return {
-        prompt,
-        userMessageHash,
-        recordChatHistoryMessage,
-        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
-            isEnabled: !runtime.isPrivateModeEnabled,
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentId: runtime.agentId,
-            messages: parsedRequest.messages,
-            includeAssistantPlaceholder: true,
-            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
-        }),
-    };
-}
-
-/**
- * Handles the streaming OpenAI-compatible response branch.
- */
-function createHandleChatCompletionStreamResponse(options: {
-    request: NextRequest;
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Response {
-    const { request, title, parsedRequest, runtime, promptContext } = options;
-    const encoder = new TextEncoder();
-    const responseModel = parsedRequest.model || 'promptbook-agent';
-    const readableStream = new ReadableStream({
-        async start(controller) {
-            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
-            const created = Math.floor(Date.now() / 1000);
-            const emitSsePayload = (payload: unknown) => {
-                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
-            };
-            const emitDeltaChunk = (deltaContent: string) => {
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {
-                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
-                        },
-                        finishReason: null,
-                    }),
-                );
-            };
-
-            emitSsePayload(
-                createHandleChatCompletionChunk({
-                    runId,
-                    created,
-                    model: responseModel,
-                    delta: {
-                        role: 'assistant',
-                        content: '',
-                    },
-                    finishReason: null,
-                }),
-            );
-
-            let hasMeaningfulDelta = false;
-
-            try {
-                const handleStreamChunk = createChatStreamHandler({
-                    onDelta: (deltaContent) => {
-                        if (deltaContent.trim().length > 0) {
-                            hasMeaningfulDelta = true;
-                        }
-                        emitDeltaChunk(deltaContent);
-                    },
-                    onToolCalls: (toolCalls) => {
-                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
-                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
-                    },
-                });
-                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
-                    signal: request.signal,
-                });
-                const normalizedResponse = ensureNonEmptyChatContent({
-                    content: result.content,
-                    context: title,
-                });
-
-                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
-                    emitDeltaChunk(normalizedResponse.content);
-                }
-
-                const messageSuffixAppendix = createMessageSuffixAppendix(
-                    normalizedResponse.content,
-                    runtime.messageSuffix,
-                );
-                if (messageSuffixAppendix) {
-                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
-                        emitDeltaChunk(delta);
-                    });
-                }
-
-                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-                await finalizeHandleChatCompletionResult({
-                    responseContentWithSuffix,
-                    usage: result.usage,
-                    toolCalls: result.toolCalls,
-                    parsedRequest,
-                    runtime,
-                    promptContext,
-                    // Preserve historical behavior: streamed OpenAI-compatible calls still persist learning in private mode.
-                    shouldPersistLearnedAgentSource: true,
-                });
-
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {},
-                        finishReason: 'stop',
-                    }),
-                );
-                controller.enqueue(encoder.encode('[DONE]'));
-            } catch (error) {
-                console.error('Error during streaming:', error);
-                controller.error(error);
-            }
-            controller.close();
-        },
-    });
-
-    return new Response(readableStream, {
-        headers: {
-            'Content-Type': 'text/event-stream',
-            'Cache-Control': 'no-cache',
-            Connection: 'keep-alive',
-        },
-    });
-}
-
-/**
- * Handles the non-streaming OpenAI-compatible response branch.
- */
-async function createHandleChatCompletionJsonResponse(options: {
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Promise<NextResponse> {
-    const { title, parsedRequest, runtime, promptContext } = options;
-    const result = await runtime.agent.callChatModel(promptContext.prompt);
-    const normalizedResponse = ensureNonEmptyChatContent({
-        content: result.content,
-        context: title,
-    });
-    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-
-    await finalizeHandleChatCompletionResult({
-        responseContentWithSuffix,
-        usage: result.usage,
-        toolCalls: result.toolCalls,
-        parsedRequest,
-        runtime,
-        promptContext,
-        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
-    });
-
-    return NextResponse.json({
-        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
-        object: 'chat.completion',
-        created: Math.floor(Date.now() / 1000),
-        model: parsedRequest.model || 'promptbook-agent',
-        choices: [
-            {
-                index: 0,
-                message: {
-                    role: 'assistant',
-                    content: responseContentWithSuffix,
-                },
-                finish_reason: 'stop',
-            },
-        ],
-        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
-    });
-}
-
-/**
- * Runs the shared side effects after one successful model response.
- */
-async function finalizeHandleChatCompletionResult(options: {
-    responseContentWithSuffix: string;
-    usage: Usage;
-    toolCalls?: ReadonlyArray<ToolCall>;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
-
-    await promptContext.recordChatHistoryMessage({
-        message: {
-            role: 'MODEL',
-            content: responseContentWithSuffix,
-        },
-        previousMessageHash: promptContext.userMessageHash,
-        usage,
-    });
-
-    if (toolCalls && toolCalls.length > 0) {
-        await logCalendarToolCallsActivity({
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentPermanentId: runtime.agentId,
-            toolCalls,
-        });
-    }
-
-    await persistOpenAiCompatibilityFrozenChat({
-        isEnabled: !runtime.isPrivateModeEnabled,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        agentId: runtime.agentId,
-        chatId: promptContext.persistedFrozenChatId,
-        messages: parsedRequest.messages,
-        assistantContent: responseContentWithSuffix,
-        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
-    });
-    await persistHandleChatCompletionLearnedAgentSource({
-        runtime,
-        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
-    });
-}
-
-/**
- * Applies append-only self-learning updates when the current branch should persist them.
- */
-async function persistHandleChatCompletionLearnedAgentSource(options: {
-    runtime: HandleChatCompletionRuntime;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { runtime, shouldPersistLearnedAgentSource } = options;
-    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
-        return;
-    }
-
-    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
-        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
-        resolvedAgentSourceBeforeLearning: runtime.agentSource,
-        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
-    });
-
-    if (learnedAgentSource !== null) {
-        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
     }
 }
-
-/**
- * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
- */
-async function persistOpenAiCompatibilityFrozenChat(options: {
-    isEnabled: boolean;
-    userId: number | null | undefined;
-    agentId: string;
-    messages: ReadonlyArray<unknown>;
-    chatId?: string;
-    assistantContent?: string;
-    includeAssistantPlaceholder?: boolean;
-    failureMessage: string;
-}): Promise<string | undefined> {
-    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
-        options;
-    if (!isEnabled || !userId) {
-        return undefined;
-    }
-
-    const persistedFrozenChat = await persistFrozenUserChat({
-        userId,
-        agentPermanentId: agentId,
-        source: USER_CHAT_SOURCES.OPENAI_API,
-        chatId,
-        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
-            includeAssistantPlaceholder,
-        }),
-    }).catch((error) => {
-        console.error(failureMessage, error);
-        return null;
-    });
-
-    return persistedFrozenChat?.id;
-}
-
-/**
- * Builds one OpenAI-compatible thread from the earlier request messages.
- */
-function createHandleChatCompletionThread(previousMessages: ReadonlyArray<TODO_any>): ChatMessage[] {
-    return previousMessages.map((message: TODO_any, index: number) => ({
-        id: `msg-${index}`,
-        sender: message.role === 'assistant' ? 'agent' : 'user',
-        content: message.content,
-        isComplete: true,
-        createdAt: $getCurrentDate(),
-    }));
-}
-
-/**
- * Appends system messages to the agent source as dynamic `CONTEXT`.
- */
-function appendSystemMessagesToAgentSource(
-    agentSource: string_book,
-    messages: ReadonlyArray<TODO_any>,
-): {
-    agentSourceWithContext: string_book;
-    hasDynamicContext: boolean;
-} {
-    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
-    if (systemMessages.length === 0) {
-        return {
-            agentSourceWithContext: agentSource,
-            hasDynamicContext: false,
-        };
-    }
-
-    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
-    return {
-        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
-        hasDynamicContext: true,
-    };
-}
-
-/**
- * Waits for any running preparation job of the current agent before executing the chat call.
- */
-async function waitForHandleChatCompletionAgentPreparation(options: {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    agentId: string;
-    agentHash: string;
-}): Promise<void> {
-    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
-    const preparationWaitResult = await waitForRunningAgentPreparation({
-        tablePrefix,
-        agentPermanentId: options.agentId,
-        fingerprint: options.agentHash,
-        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    });
-
-    if (preparationWaitResult !== 'not_running') {
-        console.info('[pre-index]', 'openai_chat_wait_result', {
-            tablePrefix,
-            agentPermanentId: options.agentId,
-            agentHash: options.agentHash,
-            preparationWaitResult,
-        });
-    }
-}
-
-/**
- * Resolves request-scoped project, calendar, and email credentials for the current agent.
- */
-async function resolveHandleChatCompletionCredentials(options: {
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    agentId: string;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-}): Promise<{
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-}> {
-    const userId = options.currentUserIdentity?.userId;
-    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
-        resolveUseProjectGithubToken({
-            userId,
-            agentPermanentId: options.agentId,
-        }),
-        options.calendarConnections.length > 0
-            ? resolveUseCalendarGoogleToken({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-        options.useEmailConfiguration.isEnabled
-            ? resolveUseEmailSmtpCredential({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-    ]);
-
-    return {
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-    };
-}
-
-/**
- * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
- */
-async function createHandleChatCompletionAgent(options: {
-    agentName: string;
-    agentSource: string_book;
-    agentId: string;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    localServerUrl: string;
-    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
-    hasDynamicContext: boolean;
-    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
-}): Promise<Agent> {
-    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
-    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
-    const runtimeAgentReferenceResolver = options.hasDynamicContext
-        ? createBookScopedAgentReferenceResolver({
-              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
-              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
-              localServerUrl: options.localServerUrl,
-              fallbackResolver: options.baseAgentReferenceResolver,
-          })
-        : undefined;
-    const agentModelRequirements =
-        options.preparedAgentModelRequirements?.modelRequirements ||
-        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
-            agentReferenceResolver: runtimeAgentReferenceResolver,
-            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
-        }));
-
-    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
-        options.agentSource,
-        options.resolvedAgentContext.resolvedAgentName,
-        await baseOpenAiToolsPromise,
-        {
-            includeDynamicContext: true,
-            agentId: options.agentId,
-            modelRequirements: agentModelRequirements,
-        },
-    );
-
-    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
-        agentName: options.agentName,
-        assistantCacheKey: agentKitResult.assistantCacheKey,
-        vectorStoreHash: agentKitResult.vectorStoreHash,
-        vectorStoreId: agentKitResult.vectorStoreId,
-    });
-
-    return new Agent({
-        agentSource: options.agentSource,
-        executionTools: {
-            llm: agentKitResult.tools,
-        },
-        precomputedModelRequirements: agentModelRequirements,
-        assistantPreparationMode: 'external',
-        isVerbose: true,
-        teacherAgent: null,
-    });
-}
-
-/**
- * Creates one OpenAI-compatible streaming chunk payload.
- */
-function createHandleChatCompletionChunk(options: {
-    runId: string;
-    created: number;
-    model: TODO_any;
-    delta: Record<string, unknown>;
-    finishReason: 'stop' | null;
-}) {
-    return {
-        id: options.runId,
-        object: 'chat.completion.chunk',
-        created: options.created,
-        model: options.model,
-        choices: [
-            {
-                index: 0,
-                delta: options.delta,
-                finish_reason: options.finishReason,
-            },
-        ],
-    };
-}
-
-/**
- * Creates one consistent OpenAI-compatible error response.
- */
-function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
-    return NextResponse.json(
-        {
-            error: {
-                message,
-                type,
-            },
-        },
-        { status },
-    );
-}
-
-// TODO: [🈹] Maybe move chat thread handling here

file update:
diff --git a/apps/agents-server/src/utils/handleChatCompletion.ts b/apps/agents-server/src/utils/handleChatCompletion.ts
index 9c96d36fad9cf24bf26d89f57372332add4e4595..cff56fafd6cfa90bb4094f9f202c4f01f6f74af1
--- a/apps/agents-server/src/utils/handleChatCompletion.ts
+++ b/apps/agents-server/src/utils/handleChatCompletion.ts
@@ -1,392 +1,16 @@
-import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
-import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
-import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
-import {
-    createBookScopedAgentReferenceResolver,
-    parseBookScopedAgentIdentifier,
-} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
-import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
-import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
-import {
-    appendMessageSuffix,
-    createMessageSuffixAppendix,
-    emulateMessageSuffixStreaming,
-    resolveMessageSuffixFromAgentSource,
-} from '@/src/utils/chat/messageSuffix';
-import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
-import { prepareToolCallsForStreaming } from './toolCallStreaming';
-import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
-import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
-import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
-import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
-import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
-import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
-import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
-import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
-import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
-import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
-import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
-import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
-import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
-import type {
-    ChatMessage,
-    ChatPrompt,
-    LlmToolDefinition,
-    string_book,
-    ToolCall,
-    TODO_any,
-    UncertainNumber,
-    Usage,
-    UsageCounts,
-} from '@promptbook-local/types';
-import { $getCurrentDate } from '@promptbook-local/utils';
 import { NextRequest, NextResponse } from 'next/server';
-import type OpenAI from 'openai';
-import { computeUsageCounts } from '../../../../src/execution/utils/computeUsageCounts';
-import { encodeChatStreamWhitespaceForTransport } from '../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
-import { isAgentDeleted } from '../app/agents/[agentName]/_utils';
 import { HTTP_STATUS_CODES } from '../constants';
-import { AgentKitCacheManager } from './cache/AgentKitCacheManager';
 import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
+import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
+import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
+import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
+import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
+import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
 import { validateApiKey } from './validateApiKey';
-import {
-    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    resolveAgentCollectionTablePrefix,
-    waitForRunningAgentPreparation,
-} from './agentPreparation';
-import {
-    resolveCachedServerAgentContext,
-    resolveCachedServerAgentModelRequirements,
-} from './cachedServerAgentRuntime';
-import { resolveAppendOnlySelfLearningAgentSource } from './resolveAppendOnlySelfLearningAgentSource';
 
 /**
- * Falls back to the estimated value when the original token count is unknown.
- *
- * @param tokenCount - Token count reported by the execution tools.
- * @param fallbackValue - Estimated token count based on text length.
- * @returns Token count to report in the OpenAI-compatible response.
- */
-function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
-    if (tokenCount.value === 0 && tokenCount.isUncertain) {
-        return {
-            value: fallbackValue,
-            isUncertain: true,
-        };
-    }
-
-    return tokenCount;
-}
-
-/**
- * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
- *
- * @param promptContent - Prompt content used for the request.
- * @param completionContent - Assistant response content.
- * @param usage - Native Promptbook usage details.
- * @returns Usage payload for compatibility responses.
- */
-function createCompatibilityUsage(
-    promptContent: string,
-    completionContent: string,
-    usage: Usage,
-): {
-    prompt_tokens: number;
-    completion_tokens: number;
-    total_tokens: number;
-    details: Usage;
-} {
-    const promptCounts = computeUsageCounts(promptContent);
-    const completionCounts = computeUsageCounts(completionContent);
-    const inputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
-        ...promptCounts,
-    };
-    const outputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
-        ...completionCounts,
-    };
-
-    const promptTokens = inputUsage.tokensCount.value;
-    const completionTokens = outputUsage.tokensCount.value;
-
-    return {
-        prompt_tokens: promptTokens,
-        completion_tokens: completionTokens,
-        total_tokens: promptTokens + completionTokens,
-        details: {
-            price: usage.price,
-            duration: usage.duration,
-            input: inputUsage,
-            output: outputUsage,
-        },
-    };
-}
-
-/**
- * Definition of open AI chat tool.
- */
-type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
-
-/**
- * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
- */
-type OpenAiCompatibilityMessageLike = {
-    role?: unknown;
-    content?: unknown;
-};
-
-/**
- * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
- */
-type HandleChatCompletionParsedRequest = {
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    threadMessages: ReadonlyArray<TODO_any>;
-    stream: TODO_any;
-    model: TODO_any;
-    responseFormat: TODO_any;
-    runtimeTools?: Array<LlmToolDefinition>;
-    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-    incomingParameters: Record<string, unknown>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Runtime state resolved before the chat model is executed.
- */
-type HandleChatCompletionRuntime = {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    agent: Agent;
-    agentSource: string_book;
-    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
-    messageSuffix: string | null;
-    agentId: string;
-    agentHash: string;
-    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Prompt, history recorder, and persistence context created for one chat call.
- */
-type HandleChatCompletionPromptContext = {
-    prompt: ChatPrompt;
-    userMessageHash: string;
-    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
-    persistedFrozenChatId?: string;
-};
-
-/**
- * Handles convert open Ai tools.
- */
-function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
-    if (!Array.isArray(rawTools)) {
-        return undefined;
-    }
-
-    const converted = rawTools
-        .map((tool) => convertOpenAiTool(tool))
-        .filter((tool): tool is LlmToolDefinition => tool !== null);
-
-    return converted.length > 0 ? converted : undefined;
-}
-
-/**
- * Handles convert open Ai tool.
- */
-function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
-    const tool = rawTool as OpenAIChatToolDefinition;
-    if (!tool || tool.type !== 'function') {
-        return null;
-    }
-
-    const functionDefinition = tool.function;
-    if (!functionDefinition) {
-        return null;
-    }
-
-    const { name, description, parameters } = functionDefinition;
-    if (typeof name !== 'string' || name.trim().length === 0) {
-        return null;
-    }
-
-    const parameterSchema = parameters ?? {};
-    if (parameterSchema.type !== 'object') {
-        return null;
-    }
-
-    const properties = parameterSchema.properties ?? {};
-    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
-
-    for (const [key, value] of Object.entries(properties)) {
-        if (!value || typeof value !== 'object') {
-            continue;
-        }
-
-        normalizedProperties[key] = {
-            type: typeof value.type === 'string' ? value.type : 'string',
-            description: typeof value.description === 'string' ? value.description : undefined,
-        };
-    }
-
-    const required =
-        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
-            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
-            : undefined;
-
-    return {
-        name,
-        description: typeof description === 'string' ? description : '',
-        parameters: {
-            type: 'object',
-            properties: normalizedProperties,
-            required,
-            additionalProperties:
-                typeof parameterSchema.additionalProperties === 'boolean'
-                    ? parameterSchema.additionalProperties
-                    : undefined,
-        },
-    };
-}
-
-/**
- * Parses open Ai tool choice.
- */
-function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
-    if (value === undefined || value === null) {
-        return undefined;
-    }
-
-    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-}
-
-/**
- * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
- */
-function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
-    if (typeof value === 'string') {
-        return value;
-    }
-
-    if (Array.isArray(value)) {
-        return value
-            .map((part) => {
-                if (typeof part === 'string') {
-                    return part;
-                }
-
-                if (!part || typeof part !== 'object') {
-                    return '';
-                }
-
-                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
-                if (typeof partLike.text === 'string') {
-                    return partLike.text;
-                }
-
-                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
-                    return '[Image input]';
-                }
-
-                if (partLike.image_url) {
-                    return '[Image input]';
-                }
-
-                return '';
-            })
-            .filter(Boolean)
-            .join('\n');
-    }
-
-    if (value === null || value === undefined) {
-        return '';
-    }
-
-    try {
-        return JSON.stringify(value, null, 2);
-    } catch {
-        return String(value);
-    }
-}
-
-/**
- * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ * Handles convert open Ai chat completion requests routed through the Agents Server.
  */
-function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
-    if (typeof role !== 'string') {
-        return 'USER';
-    }
-
-    switch (role) {
-        case 'assistant':
-            return 'AGENT';
-        case 'system':
-            return 'SYSTEM';
-        case 'tool':
-            return 'TOOL';
-        case 'developer':
-            return 'SYSTEM';
-        default:
-            return 'USER';
-    }
-}
-
-/**
- * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
- */
-function createFrozenOpenAiCompatibilityMessages(
-    rawMessages: ReadonlyArray<unknown>,
-    assistantContent?: string,
-    options: {
-        includeAssistantPlaceholder?: boolean;
-    } = {},
-): Array<ChatMessage> {
-    const startedAt = Date.now();
-    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
-        const message = rawMessage as OpenAiCompatibilityMessageLike;
-
-        return {
-            id: `openai-frozen-${index}`,
-            sender: mapOpenAiCompatibilityRoleToSender(message.role),
-            content: normalizeOpenAiCompatibilityMessageContent(message.content),
-            isComplete: true,
-            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage;
-    });
-
-    if (assistantContent !== undefined) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: assistantContent,
-            isComplete: true,
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    } else if (options.includeAssistantPlaceholder) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: '',
-            isComplete: false,
-            lifecycleState: 'running',
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    }
-
-    return messages;
-}
-
-/**
- * Handles chat completion.
- */
 export async function handleChatCompletion(
     request: NextRequest,
     params: { agentName?: string },
@@ -399,7 +23,6 @@
         return versionMismatchResponse;
     }
 
-    // Validate API key explicitly (in addition to middleware)
     const apiKeyValidation = await validateApiKey(request);
     if (!apiKeyValidation.isValid) {
         return NextResponse.json(
@@ -462,724 +85,5 @@
             { error: { message: (error as Error).message || 'Internal Server Error', type: 'server_error' } },
             { status: HTTP_STATUS_CODES.INTERNAL_SERVER_ERROR },
         );
-    }
-}
-
-/**
- * Parses and validates the OpenAI-compatible request payload.
- */
-function parseHandleChatCompletionRequest(options: {
-    body: TODO_any;
-    agentNameFromParams?: string;
-    request: NextRequest;
-}): HandleChatCompletionParsedRequest | NextResponse {
-    const { body, agentNameFromParams, request } = options;
-    const {
-        messages,
-        stream,
-        model,
-        response_format: responseFormat,
-        tools: rawTools,
-        tool_choice: toolChoice,
-        parameters: rawParameters = {},
-    } = body;
-    const agentName = (agentNameFromParams || model) as string | undefined;
-    if (!agentName) {
-        return createHandleChatCompletionErrorResponse(
-            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    if (!messages || !Array.isArray(messages) || messages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array is required and cannot be empty.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
-    if (threadMessages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array must contain at least one non-system message.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    return {
-        agentName,
-        messages,
-        threadMessages,
-        stream,
-        model,
-        responseFormat,
-        runtimeTools: convertOpenAiTools(rawTools),
-        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
-        incomingParameters:
-            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
-                ? (rawParameters as Record<string, unknown>)
-                : {},
-        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
-    };
-}
-
-/**
- * Resolves agent/runtime dependencies needed before prompt execution.
- */
-async function resolveHandleChatCompletionRuntime(options: {
-    request: NextRequest;
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    isPrivateModeEnabled: boolean;
-}): Promise<HandleChatCompletionRuntime | NextResponse> {
-    const { request, agentName, messages, isPrivateModeEnabled } = options;
-    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
-    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
-
-    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
-        return createHandleChatCompletionErrorResponse(
-            'This agent has been deleted. You can restore it from the Recycle Bin.',
-            'agent_deleted',
-            410,
-        );
-    }
-
-    const [collection, baseAgentReferenceResolver] = await Promise.all([
-        $provideAgentCollectionForServer(),
-        $provideAgentReferenceResolver(),
-    ]);
-    const localServerUrl = new URL(request.url).origin;
-    const resolvedAgentContext = await resolveCachedServerAgentContext({
-        collection,
-        agentIdentifier: agentName,
-        localServerUrl,
-        fallbackResolver: baseAgentReferenceResolver,
-    }).catch(() => null);
-
-    if (!resolvedAgentContext?.resolvedAgentSource) {
-        return createHandleChatCompletionErrorResponse(
-            `Agent '${agentName}' not found.`,
-            'invalid_request_error',
-            HTTP_STATUS_CODES.NOT_FOUND,
-        );
-    }
-
-    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
-    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
-    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
-    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
-    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
-    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
-    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
-    agentSource = agentSourceWithContext;
-
-    const preparedAgentModelRequirements = !hasDynamicContext
-        ? await resolveCachedServerAgentModelRequirements({
-              resolvedAgentContext,
-              localServerUrl,
-              fallbackResolver: baseAgentReferenceResolver,
-          })
-        : null;
-    const agentId = resolvedAgentContext.parentAgentPermanentId;
-    const agentHash = computeAgentHash(agentSource);
-    await waitForHandleChatCompletionAgentPreparation({
-        collection,
-        agentId,
-        agentHash,
-    });
-
-    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
-    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
-        await resolveHandleChatCompletionCredentials({
-            currentUserIdentity,
-            agentId,
-            calendarConnections,
-            useEmailConfiguration,
-        });
-    const agent = await createHandleChatCompletionAgent({
-        agentName,
-        agentSource,
-        agentId,
-        resolvedAgentContext,
-        localServerUrl,
-        baseAgentReferenceResolver,
-        hasDynamicContext,
-        preparedAgentModelRequirements,
-    });
-
-    return {
-        collection,
-        resolvedAgentContext,
-        agent,
-        agentSource,
-        unresolvedAgentSource,
-        messageSuffix,
-        agentId,
-        agentHash,
-        projectRepositories,
-        calendarConnections,
-        useEmailConfiguration,
-        currentUserIdentity,
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-        isPrivateModeEnabled,
-    };
-}
-
-/**
- * Creates prompt and persistence helpers for one chat completion request.
- */
-async function createHandleChatCompletionPromptContext(options: {
-    request: NextRequest;
-    title: string;
-    apiKey: string | null;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-}): Promise<HandleChatCompletionPromptContext> {
-    const { request, title, apiKey, parsedRequest, runtime } = options;
-    const timezone = request.headers.get('x-timezone') || 'UTC';
-    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
-    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
-    const recordChatHistoryMessage = await createChatHistoryRecorder({
-        request,
-        agentIdentifier: runtime.agentId,
-        agentHash: runtime.agentHash,
-        source: 'OPENAI_API_COMPATIBILITY',
-        apiKey,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        isEnabled: !runtime.isPrivateModeEnabled,
-    });
-    const userMessageHash = await recordChatHistoryMessage({
-        message: {
-            role: 'USER',
-            content: lastMessage.content,
-        },
-        previousMessageHash: null,
-    });
-    const promptParameters = composePromptParametersWithMemoryContext({
-        baseParameters: {
-            ...parsedRequest.incomingParameters,
-            timezone,
-        },
-        currentUserIdentity: runtime.currentUserIdentity,
-        agentPermanentId: runtime.agentId,
-        agentName: runtime.resolvedAgentContext.resolvedAgentName,
-        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
-        projectRepositories: runtime.projectRepositories,
-        projectGithubToken: runtime.projectGithubToken,
-        emailSmtpCredential: runtime.emailSmtpCredential,
-        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
-        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
-        calendarConnections: runtime.calendarConnections,
-    });
-    const prompt: ChatPrompt = {
-        title,
-        content: lastMessage.content,
-        modelRequirements: {
-            modelVariant: 'CHAT',
-            responseFormat: parsedRequest.responseFormat,
-            toolChoice: parsedRequest.runtimeToolChoice,
-            // We could pass 'model' from body if we wanted to enforce it, but Agent usually has its own config
-        },
-        parameters: promptParameters,
-        thread,
-        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
-    };
-
-    return {
-        prompt,
-        userMessageHash,
-        recordChatHistoryMessage,
-        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
-            isEnabled: !runtime.isPrivateModeEnabled,
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentId: runtime.agentId,
-            messages: parsedRequest.messages,
-            includeAssistantPlaceholder: true,
-            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
-        }),
-    };
-}
-
-/**
- * Handles the streaming OpenAI-compatible response branch.
- */
-function createHandleChatCompletionStreamResponse(options: {
-    request: NextRequest;
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Response {
-    const { request, title, parsedRequest, runtime, promptContext } = options;
-    const encoder = new TextEncoder();
-    const responseModel = parsedRequest.model || 'promptbook-agent';
-    const readableStream = new ReadableStream({
-        async start(controller) {
-            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
-            const created = Math.floor(Date.now() / 1000);
-            const emitSsePayload = (payload: unknown) => {
-                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
-            };
-            const emitDeltaChunk = (deltaContent: string) => {
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {
-                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
-                        },
-                        finishReason: null,
-                    }),
-                );
-            };
-
-            emitSsePayload(
-                createHandleChatCompletionChunk({
-                    runId,
-                    created,
-                    model: responseModel,
-                    delta: {
-                        role: 'assistant',
-                        content: '',
-                    },
-                    finishReason: null,
-                }),
-            );
-
-            let hasMeaningfulDelta = false;
-
-            try {
-                const handleStreamChunk = createChatStreamHandler({
-                    onDelta: (deltaContent) => {
-                        if (deltaContent.trim().length > 0) {
-                            hasMeaningfulDelta = true;
-                        }
-                        emitDeltaChunk(deltaContent);
-                    },
-                    onToolCalls: (toolCalls) => {
-                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
-                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
-                    },
-                });
-                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
-                    signal: request.signal,
-                });
-                const normalizedResponse = ensureNonEmptyChatContent({
-                    content: result.content,
-                    context: title,
-                });
-
-                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
-                    emitDeltaChunk(normalizedResponse.content);
-                }
-
-                const messageSuffixAppendix = createMessageSuffixAppendix(
-                    normalizedResponse.content,
-                    runtime.messageSuffix,
-                );
-                if (messageSuffixAppendix) {
-                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
-                        emitDeltaChunk(delta);
-                    });
-                }
-
-                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-                await finalizeHandleChatCompletionResult({
-                    responseContentWithSuffix,
-                    usage: result.usage,
-                    toolCalls: result.toolCalls,
-                    parsedRequest,
-                    runtime,
-                    promptContext,
-                    // Preserve historical behavior: streamed OpenAI-compatible calls still persist learning in private mode.
-                    shouldPersistLearnedAgentSource: true,
-                });
-
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {},
-                        finishReason: 'stop',
-                    }),
-                );
-                controller.enqueue(encoder.encode('[DONE]'));
-            } catch (error) {
-                console.error('Error during streaming:', error);
-                controller.error(error);
-            }
-            controller.close();
-        },
-    });
-
-    return new Response(readableStream, {
-        headers: {
-            'Content-Type': 'text/event-stream',
-            'Cache-Control': 'no-cache',
-            Connection: 'keep-alive',
-        },
-    });
-}
-
-/**
- * Handles the non-streaming OpenAI-compatible response branch.
- */
-async function createHandleChatCompletionJsonResponse(options: {
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Promise<NextResponse> {
-    const { title, parsedRequest, runtime, promptContext } = options;
-    const result = await runtime.agent.callChatModel(promptContext.prompt);
-    const normalizedResponse = ensureNonEmptyChatContent({
-        content: result.content,
-        context: title,
-    });
-    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-
-    await finalizeHandleChatCompletionResult({
-        responseContentWithSuffix,
-        usage: result.usage,
-        toolCalls: result.toolCalls,
-        parsedRequest,
-        runtime,
-        promptContext,
-        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
-    });
-
-    return NextResponse.json({
-        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
-        object: 'chat.completion',
-        created: Math.floor(Date.now() / 1000),
-        model: parsedRequest.model || 'promptbook-agent',
-        choices: [
-            {
-                index: 0,
-                message: {
-                    role: 'assistant',
-                    content: responseContentWithSuffix,
-                },
-                finish_reason: 'stop',
-            },
-        ],
-        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
-    });
-}
-
-/**
- * Runs the shared side effects after one successful model response.
- */
-async function finalizeHandleChatCompletionResult(options: {
-    responseContentWithSuffix: string;
-    usage: Usage;
-    toolCalls?: ReadonlyArray<ToolCall>;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
-
-    await promptContext.recordChatHistoryMessage({
-        message: {
-            role: 'MODEL',
-            content: responseContentWithSuffix,
-        },
-        previousMessageHash: promptContext.userMessageHash,
-        usage,
-    });
-
-    if (toolCalls && toolCalls.length > 0) {
-        await logCalendarToolCallsActivity({
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentPermanentId: runtime.agentId,
-            toolCalls,
-        });
-    }
-
-    await persistOpenAiCompatibilityFrozenChat({
-        isEnabled: !runtime.isPrivateModeEnabled,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        agentId: runtime.agentId,
-        chatId: promptContext.persistedFrozenChatId,
-        messages: parsedRequest.messages,
-        assistantContent: responseContentWithSuffix,
-        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
-    });
-    await persistHandleChatCompletionLearnedAgentSource({
-        runtime,
-        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
-    });
-}
-
-/**
- * Applies append-only self-learning updates when the current branch should persist them.
- */
-async function persistHandleChatCompletionLearnedAgentSource(options: {
-    runtime: HandleChatCompletionRuntime;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { runtime, shouldPersistLearnedAgentSource } = options;
-    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
-        return;
-    }
-
-    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
-        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
-        resolvedAgentSourceBeforeLearning: runtime.agentSource,
-        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
-    });
-
-    if (learnedAgentSource !== null) {
-        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
     }
 }
-
-/**
- * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
- */
-async function persistOpenAiCompatibilityFrozenChat(options: {
-    isEnabled: boolean;
-    userId: number | null | undefined;
-    agentId: string;
-    messages: ReadonlyArray<unknown>;
-    chatId?: string;
-    assistantContent?: string;
-    includeAssistantPlaceholder?: boolean;
-    failureMessage: string;
-}): Promise<string | undefined> {
-    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
-        options;
-    if (!isEnabled || !userId) {
-        return undefined;
-    }
-
-    const persistedFrozenChat = await persistFrozenUserChat({
-        userId,
-        agentPermanentId: agentId,
-        source: USER_CHAT_SOURCES.OPENAI_API,
-        chatId,
-        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
-            includeAssistantPlaceholder,
-        }),
-    }).catch((error) => {
-        console.error(failureMessage, error);
-        return null;
-    });
-
-    return persistedFrozenChat?.id;
-}
-
-/**
- * Builds one OpenAI-compatible thread from the earlier request messages.
- */
-function createHandleChatCompletionThread(previousMessages: ReadonlyArray<TODO_any>): ChatMessage[] {
-    return previousMessages.map((message: TODO_any, index: number) => ({
-        id: `msg-${index}`,
-        sender: message.role === 'assistant' ? 'agent' : 'user',
-        content: message.content,
-        isComplete: true,
-        createdAt: $getCurrentDate(),
-    }));
-}
-
-/**
- * Appends system messages to the agent source as dynamic `CONTEXT`.
- */
-function appendSystemMessagesToAgentSource(
-    agentSource: string_book,
-    messages: ReadonlyArray<TODO_any>,
-): {
-    agentSourceWithContext: string_book;
-    hasDynamicContext: boolean;
-} {
-    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
-    if (systemMessages.length === 0) {
-        return {
-            agentSourceWithContext: agentSource,
-            hasDynamicContext: false,
-        };
-    }
-
-    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
-    return {
-        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
-        hasDynamicContext: true,
-    };
-}
-
-/**
- * Waits for any running preparation job of the current agent before executing the chat call.
- */
-async function waitForHandleChatCompletionAgentPreparation(options: {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    agentId: string;
-    agentHash: string;
-}): Promise<void> {
-    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
-    const preparationWaitResult = await waitForRunningAgentPreparation({
-        tablePrefix,
-        agentPermanentId: options.agentId,
-        fingerprint: options.agentHash,
-        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    });
-
-    if (preparationWaitResult !== 'not_running') {
-        console.info('[pre-index]', 'openai_chat_wait_result', {
-            tablePrefix,
-            agentPermanentId: options.agentId,
-            agentHash: options.agentHash,
-            preparationWaitResult,
-        });
-    }
-}
-
-/**
- * Resolves request-scoped project, calendar, and email credentials for the current agent.
- */
-async function resolveHandleChatCompletionCredentials(options: {
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    agentId: string;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-}): Promise<{
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-}> {
-    const userId = options.currentUserIdentity?.userId;
-    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
-        resolveUseProjectGithubToken({
-            userId,
-            agentPermanentId: options.agentId,
-        }),
-        options.calendarConnections.length > 0
-            ? resolveUseCalendarGoogleToken({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-        options.useEmailConfiguration.isEnabled
-            ? resolveUseEmailSmtpCredential({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-    ]);
-
-    return {
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-    };
-}
-
-/**
- * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
- */
-async function createHandleChatCompletionAgent(options: {
-    agentName: string;
-    agentSource: string_book;
-    agentId: string;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    localServerUrl: string;
-    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
-    hasDynamicContext: boolean;
-    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
-}): Promise<Agent> {
-    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
-    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
-    const runtimeAgentReferenceResolver = options.hasDynamicContext
-        ? createBookScopedAgentReferenceResolver({
-              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
-              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
-              localServerUrl: options.localServerUrl,
-              fallbackResolver: options.baseAgentReferenceResolver,
-          })
-        : undefined;
-    const agentModelRequirements =
-        options.preparedAgentModelRequirements?.modelRequirements ||
-        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
-            agentReferenceResolver: runtimeAgentReferenceResolver,
-            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
-        }));
-
-    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
-        options.agentSource,
-        options.resolvedAgentContext.resolvedAgentName,
-        await baseOpenAiToolsPromise,
-        {
-            includeDynamicContext: true,
-            agentId: options.agentId,
-            modelRequirements: agentModelRequirements,
-        },
-    );
-
-    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
-        agentName: options.agentName,
-        assistantCacheKey: agentKitResult.assistantCacheKey,
-        vectorStoreHash: agentKitResult.vectorStoreHash,
-        vectorStoreId: agentKitResult.vectorStoreId,
-    });
-
-    return new Agent({
-        agentSource: options.agentSource,
-        executionTools: {
-            llm: agentKitResult.tools,
-        },
-        precomputedModelRequirements: agentModelRequirements,
-        assistantPreparationMode: 'external',
-        isVerbose: true,
-        teacherAgent: null,
-    });
-}
-
-/**
- * Creates one OpenAI-compatible streaming chunk payload.
- */
-function createHandleChatCompletionChunk(options: {
-    runId: string;
-    created: number;
-    model: TODO_any;
-    delta: Record<string, unknown>;
-    finishReason: 'stop' | null;
-}) {
-    return {
-        id: options.runId,
-        object: 'chat.completion.chunk',
-        created: options.created,
-        model: options.model,
-        choices: [
-            {
-                index: 0,
-                delta: options.delta,
-                finish_reason: options.finishReason,
-            },
-        ],
-    };
-}
-
-/**
- * Creates one consistent OpenAI-compatible error response.
- */
-function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
-    return NextResponse.json(
-        {
-            error: {
-                message,
-                type,
-            },
-        },
-        { status },
-    );
-}
-
-// TODO: [🈹] Maybe move chat thread handling here

file update
A C:/Users/me/work/ai/promptbook\apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
import { NextResponse } from 'next/server';

/**
 * Creates one consistent OpenAI-compatible error response.
 *
 * @private function of `handleChatCompletion`
 */
export function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
    return NextResponse.json(
        {
            error: {
                message,
                type,
            },
        },
        { status },
    );
}
A C:/Users/me/work/ai/promptbook\apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
import type { LlmToolDefinition, TODO_any } from '@promptbook-local/types';
import { NextRequest, NextResponse } from 'next/server';
import type OpenAI from 'openai';
import { HTTP_STATUS_CODES } from '../../constants';
import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';

/**
 * Definition of open AI chat tool.
 */
type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;

/**
 * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
 *
 * @private type of `handleChatCompletion`
 */
export type HandleChatCompletionParsedRequest = {
    agentName: string;
    messages: ReadonlyArray<TODO_any>;
    threadMessages: ReadonlyArray<TODO_any>;
    stream: TODO_any;
    model: TODO_any;
    responseFormat: TODO_any;
    runtimeTools?: Array<LlmToolDefinition>;
    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
    incomingParameters: Record<string, unknown>;
    isPrivateModeEnabled: boolean;
};

/**
 * Parses and validates the OpenAI-compatible request payload.
 *
 * @private function of `handleChatCompletion`
 */
export function parseHandleChatCompletionRequest(options: {
    body: TODO_any;
    agentNameFromParams?: string;
    request: NextRequest;
}): HandleChatCompletionParsedRequest | NextResponse {
    const { body, agentNameFromParams, request } = options;
    const {
        messages,
        stream,
        model,
        response_format: responseFormat,
        tools: rawTools,
        tool_choice: toolChoice,
        parameters: rawParameters = {},
    } = body;
    const agentName = (agentNameFromParams || model) as string | undefined;
    if (!agentName) {
        return createHandleChatCompletionErrorResponse(
            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
            'invalid_request_error',
            HTTP_STATUS_CODES.BAD_REQUEST,
        );
    }

    if (!messages || !Array.isArray(messages) || messages.length === 0) {
        return createHandleChatCompletionErrorResponse(
            'Messages array is required and cannot be empty.',
            'invalid_request_error',
            HTTP_STATUS_CODES.BAD_REQUEST,
        );
    }

    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
    if (threadMessages.length === 0) {
        return createHandleChatCompletionErrorResponse(
            'Messages array must contain at least one non-system message.',
            'invalid_request_error',
            HTTP_STATUS_CODES.BAD_REQUEST,
        );
    }

    return {
        agentName,
        messages,
        threadMessages,
        stream,
        model,
        responseFormat,
        runtimeTools: convertOpenAiTools(rawTools),
        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
        incomingParameters:
            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
                ? (rawParameters as Record<string, unknown>)
                : {},
        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
    };
}

/**
 * Handles convert open Ai tools.
 */
function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
    if (!Array.isArray(rawTools)) {
        return undefined;
    }

    const converted = rawTools
        .map((tool) => convertOpenAiTool(tool))
        .filter((tool): tool is LlmToolDefinition => tool !== null);

    return converted.length > 0 ? converted : undefined;
}

/**
 * Handles convert open Ai tool.
 */
function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
    const tool = rawTool as OpenAIChatToolDefinition;
    if (!tool || tool.type !== 'function') {
        return null;
    }

    const functionDefinition = tool.function;
    if (!functionDefinition) {
        return null;
    }

    const { name, description, parameters } = functionDefinition;
    if (typeof name !== 'string' || name.trim().length === 0) {
        return null;
    }

    const parameterSchema = parameters ?? {};
    if (parameterSchema.type !== 'object') {
        return null;
    }

    const properties = parameterSchema.properties ?? {};
    const normalizedProperties: Record<string, { type: string; description?: string }> = {};

    for (const [key, value] of Object.entries(properties)) {
        if (!value || typeof value !== 'object') {
            continue;
        }

        normalizedProperties[key] = {
            type: typeof value.type === 'string' ? value.type : 'string',
            description: typeof value.description === 'string' ? value.description : undefined,
        };
    }

    const required =
        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
            : undefined;

    return {
        name,
        description: typeof description === 'string' ? description : '',
        parameters: {
            type: 'object',
            properties: normalizedProperties,
            required,
            additionalProperties:
                typeof parameterSchema.additionalProperties === 'boolean'
                    ? parameterSchema.additionalProperties
                    : undefined,
        },
    };
}

/**
 * Parses open Ai tool choice.
 */
function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
    if (value === undefined || value === null) {
        return undefined;
    }

    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
}
apply_patch(auto_approved=true) exited 0 in 112ms:
Success. Updated the following files:
A apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
A apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
file update:
diff --git a/apps/agents-server/src/utils/handleChatCompletion.ts b/apps/agents-server/src/utils/handleChatCompletion.ts
index 9c96d36fad9cf24bf26d89f57372332add4e4595..cff56fafd6cfa90bb4094f9f202c4f01f6f74af1
--- a/apps/agents-server/src/utils/handleChatCompletion.ts
+++ b/apps/agents-server/src/utils/handleChatCompletion.ts
@@ -1,392 +1,16 @@
-import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
-import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
-import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
-import {
-    createBookScopedAgentReferenceResolver,
-    parseBookScopedAgentIdentifier,
-} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
-import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
-import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
-import {
-    appendMessageSuffix,
-    createMessageSuffixAppendix,
-    emulateMessageSuffixStreaming,
-    resolveMessageSuffixFromAgentSource,
-} from '@/src/utils/chat/messageSuffix';
-import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
-import { prepareToolCallsForStreaming } from './toolCallStreaming';
-import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
-import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
-import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
-import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
-import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
-import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
-import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
-import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
-import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
-import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
-import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
-import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
-import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
-import type {
-    ChatMessage,
-    ChatPrompt,
-    LlmToolDefinition,
-    string_book,
-    ToolCall,
-    TODO_any,
-    UncertainNumber,
-    Usage,
-    UsageCounts,
-} from '@promptbook-local/types';
-import { $getCurrentDate } from '@promptbook-local/utils';
 import { NextRequest, NextResponse } from 'next/server';
-import type OpenAI from 'openai';
-import { computeUsageCounts } from '../../../../src/execution/utils/computeUsageCounts';
-import { encodeChatStreamWhitespaceForTransport } from '../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
-import { isAgentDeleted } from '../app/agents/[agentName]/_utils';
 import { HTTP_STATUS_CODES } from '../constants';
-import { AgentKitCacheManager } from './cache/AgentKitCacheManager';
 import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
+import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
+import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
+import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
+import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
+import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
 import { validateApiKey } from './validateApiKey';
-import {
-    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    resolveAgentCollectionTablePrefix,
-    waitForRunningAgentPreparation,
-} from './agentPreparation';
-import {
-    resolveCachedServerAgentContext,
-    resolveCachedServerAgentModelRequirements,
-} from './cachedServerAgentRuntime';
-import { resolveAppendOnlySelfLearningAgentSource } from './resolveAppendOnlySelfLearningAgentSource';
 
 /**
- * Falls back to the estimated value when the original token count is unknown.
- *
- * @param tokenCount - Token count reported by the execution tools.
- * @param fallbackValue - Estimated token count based on text length.
- * @returns Token count to report in the OpenAI-compatible response.
- */
-function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
-    if (tokenCount.value === 0 && tokenCount.isUncertain) {
-        return {
-            value: fallbackValue,
-            isUncertain: true,
-        };
-    }
-
-    return tokenCount;
-}
-
-/**
- * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
- *
- * @param promptContent - Prompt content used for the request.
- * @param completionContent - Assistant response content.
- * @param usage - Native Promptbook usage details.
- * @returns Usage payload for compatibility responses.
- */
-function createCompatibilityUsage(
-    promptContent: string,
-    completionContent: string,
-    usage: Usage,
-): {
-    prompt_tokens: number;
-    completion_tokens: number;
-    total_tokens: number;
-    details: Usage;
-} {
-    const promptCounts = computeUsageCounts(promptContent);
-    const completionCounts = computeUsageCounts(completionContent);
-    const inputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
-        ...promptCounts,
-    };
-    const outputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
-        ...completionCounts,
-    };
-
-    const promptTokens = inputUsage.tokensCount.value;
-    const completionTokens = outputUsage.tokensCount.value;
-
-    return {
-        prompt_tokens: promptTokens,
-        completion_tokens: completionTokens,
-        total_tokens: promptTokens + completionTokens,
-        details: {
-            price: usage.price,
-            duration: usage.duration,
-            input: inputUsage,
-            output: outputUsage,
-        },
-    };
-}
-
-/**
- * Definition of open AI chat tool.
- */
-type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
-
-/**
- * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
- */
-type OpenAiCompatibilityMessageLike = {
-    role?: unknown;
-    content?: unknown;
-};
-
-/**
- * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
- */
-type HandleChatCompletionParsedRequest = {
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    threadMessages: ReadonlyArray<TODO_any>;
-    stream: TODO_any;
-    model: TODO_any;
-    responseFormat: TODO_any;
-    runtimeTools?: Array<LlmToolDefinition>;
-    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-    incomingParameters: Record<string, unknown>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Runtime state resolved before the chat model is executed.
- */
-type HandleChatCompletionRuntime = {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    agent: Agent;
-    agentSource: string_book;
-    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
-    messageSuffix: string | null;
-    agentId: string;
-    agentHash: string;
-    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Prompt, history recorder, and persistence context created for one chat call.
- */
-type HandleChatCompletionPromptContext = {
-    prompt: ChatPrompt;
-    userMessageHash: string;
-    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
-    persistedFrozenChatId?: string;
-};
-
-/**
- * Handles convert open Ai tools.
- */
-function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
-    if (!Array.isArray(rawTools)) {
-        return undefined;
-    }
-
-    const converted = rawTools
-        .map((tool) => convertOpenAiTool(tool))
-        .filter((tool): tool is LlmToolDefinition => tool !== null);
-
-    return converted.length > 0 ? converted : undefined;
-}
-
-/**
- * Handles convert open Ai tool.
- */
-function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
-    const tool = rawTool as OpenAIChatToolDefinition;
-    if (!tool || tool.type !== 'function') {
-        return null;
-    }
-
-    const functionDefinition = tool.function;
-    if (!functionDefinition) {
-        return null;
-    }
-
-    const { name, description, parameters } = functionDefinition;
-    if (typeof name !== 'string' || name.trim().length === 0) {
-        return null;
-    }
-
-    const parameterSchema = parameters ?? {};
-    if (parameterSchema.type !== 'object') {
-        return null;
-    }
-
-    const properties = parameterSchema.properties ?? {};
-    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
-
-    for (const [key, value] of Object.entries(properties)) {
-        if (!value || typeof value !== 'object') {
-            continue;
-        }
-
-        normalizedProperties[key] = {
-            type: typeof value.type === 'string' ? value.type : 'string',
-            description: typeof value.description === 'string' ? value.description : undefined,
-        };
-    }
-
-    const required =
-        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
-            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
-            : undefined;
-
-    return {
-        name,
-        description: typeof description === 'string' ? description : '',
-        parameters: {
-            type: 'object',
-            properties: normalizedProperties,
-            required,
-            additionalProperties:
-                typeof parameterSchema.additionalProperties === 'boolean'
-                    ? parameterSchema.additionalProperties
-                    : undefined,
-        },
-    };
-}
-
-/**
- * Parses open Ai tool choice.
- */
-function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
-    if (value === undefined || value === null) {
-        return undefined;
-    }
-
-    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-}
-
-/**
- * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
- */
-function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
-    if (typeof value === 'string') {
-        return value;
-    }
-
-    if (Array.isArray(value)) {
-        return value
-            .map((part) => {
-                if (typeof part === 'string') {
-                    return part;
-                }
-
-                if (!part || typeof part !== 'object') {
-                    return '';
-                }
-
-                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
-                if (typeof partLike.text === 'string') {
-                    return partLike.text;
-                }
-
-                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
-                    return '[Image input]';
-                }
-
-                if (partLike.image_url) {
-                    return '[Image input]';
-                }
-
-                return '';
-            })
-            .filter(Boolean)
-            .join('\n');
-    }
-
-    if (value === null || value === undefined) {
-        return '';
-    }
-
-    try {
-        return JSON.stringify(value, null, 2);
-    } catch {
-        return String(value);
-    }
-}
-
-/**
- * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ * Handles convert open Ai chat completion requests routed through the Agents Server.
  */
-function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
-    if (typeof role !== 'string') {
-        return 'USER';
-    }
-
-    switch (role) {
-        case 'assistant':
-            return 'AGENT';
-        case 'system':
-            return 'SYSTEM';
-        case 'tool':
-            return 'TOOL';
-        case 'developer':
-            return 'SYSTEM';
-        default:
-            return 'USER';
-    }
-}
-
-/**
- * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
- */
-function createFrozenOpenAiCompatibilityMessages(
-    rawMessages: ReadonlyArray<unknown>,
-    assistantContent?: string,
-    options: {
-        includeAssistantPlaceholder?: boolean;
-    } = {},
-): Array<ChatMessage> {
-    const startedAt = Date.now();
-    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
-        const message = rawMessage as OpenAiCompatibilityMessageLike;
-
-        return {
-            id: `openai-frozen-${index}`,
-            sender: mapOpenAiCompatibilityRoleToSender(message.role),
-            content: normalizeOpenAiCompatibilityMessageContent(message.content),
-            isComplete: true,
-            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage;
-    });
-
-    if (assistantContent !== undefined) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: assistantContent,
-            isComplete: true,
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    } else if (options.includeAssistantPlaceholder) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: '',
-            isComplete: false,
-            lifecycleState: 'running',
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    }
-
-    return messages;
-}
-
-/**
- * Handles chat completion.
- */
 export async function handleChatCompletion(
     request: NextRequest,
     params: { agentName?: string },
@@ -399,7 +23,6 @@
         return versionMismatchResponse;
     }
 
-    // Validate API key explicitly (in addition to middleware)
     const apiKeyValidation = await validateApiKey(request);
     if (!apiKeyValidation.isValid) {
         return NextResponse.json(
@@ -462,724 +85,5 @@
             { error: { message: (error as Error).message || 'Internal Server Error', type: 'server_error' } },
             { status: HTTP_STATUS_CODES.INTERNAL_SERVER_ERROR },
         );
-    }
-}
-
-/**
- * Parses and validates the OpenAI-compatible request payload.
- */
-function parseHandleChatCompletionRequest(options: {
-    body: TODO_any;
-    agentNameFromParams?: string;
-    request: NextRequest;
-}): HandleChatCompletionParsedRequest | NextResponse {
-    const { body, agentNameFromParams, request } = options;
-    const {
-        messages,
-        stream,
-        model,
-        response_format: responseFormat,
-        tools: rawTools,
-        tool_choice: toolChoice,
-        parameters: rawParameters = {},
-    } = body;
-    const agentName = (agentNameFromParams || model) as string | undefined;
-    if (!agentName) {
-        return createHandleChatCompletionErrorResponse(
-            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    if (!messages || !Array.isArray(messages) || messages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array is required and cannot be empty.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
-    if (threadMessages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array must contain at least one non-system message.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    return {
-        agentName,
-        messages,
-        threadMessages,
-        stream,
-        model,
-        responseFormat,
-        runtimeTools: convertOpenAiTools(rawTools),
-        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
-        incomingParameters:
-            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
-                ? (rawParameters as Record<string, unknown>)
-                : {},
-        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
-    };
-}
-
-/**
- * Resolves agent/runtime dependencies needed before prompt execution.
- */
-async function resolveHandleChatCompletionRuntime(options: {
-    request: NextRequest;
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    isPrivateModeEnabled: boolean;
-}): Promise<HandleChatCompletionRuntime | NextResponse> {
-    const { request, agentName, messages, isPrivateModeEnabled } = options;
-    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
-    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
-
-    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
-        return createHandleChatCompletionErrorResponse(
-            'This agent has been deleted. You can restore it from the Recycle Bin.',
-            'agent_deleted',
-            410,
-        );
-    }
-
-    const [collection, baseAgentReferenceResolver] = await Promise.all([
-        $provideAgentCollectionForServer(),
-        $provideAgentReferenceResolver(),
-    ]);
-    const localServerUrl = new URL(request.url).origin;
-    const resolvedAgentContext = await resolveCachedServerAgentContext({
-        collection,
-        agentIdentifier: agentName,
-        localServerUrl,
-        fallbackResolver: baseAgentReferenceResolver,
-    }).catch(() => null);
-
-    if (!resolvedAgentContext?.resolvedAgentSource) {
-        return createHandleChatCompletionErrorResponse(
-            `Agent '${agentName}' not found.`,
-            'invalid_request_error',
-            HTTP_STATUS_CODES.NOT_FOUND,
-        );
-    }
-
-    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
-    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
-    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
-    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
-    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
-    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
-    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
-    agentSource = agentSourceWithContext;
-
-    const preparedAgentModelRequirements = !hasDynamicContext
-        ? await resolveCachedServerAgentModelRequirements({
-              resolvedAgentContext,
-              localServerUrl,
-              fallbackResolver: baseAgentReferenceResolver,
-          })
-        : null;
-    const agentId = resolvedAgentContext.parentAgentPermanentId;
-    const agentHash = computeAgentHash(agentSource);
-    await waitForHandleChatCompletionAgentPreparation({
-        collection,
-        agentId,
-        agentHash,
-    });
-
-    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
-    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
-        await resolveHandleChatCompletionCredentials({
-            currentUserIdentity,
-            agentId,
-            calendarConnections,
-            useEmailConfiguration,
-        });
-    const agent = await createHandleChatCompletionAgent({
-        agentName,
-        agentSource,
-        agentId,
-        resolvedAgentContext,
-        localServerUrl,
-        baseAgentReferenceResolver,
-        hasDynamicContext,
-        preparedAgentModelRequirements,
-    });
-
-    return {
-        collection,
-        resolvedAgentContext,
-        agent,
-        agentSource,
-        unresolvedAgentSource,
-        messageSuffix,
-        agentId,
-        agentHash,
-        projectRepositories,
-        calendarConnections,
-        useEmailConfiguration,
-        currentUserIdentity,
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-        isPrivateModeEnabled,
-    };
-}
-
-/**
- * Creates prompt and persistence helpers for one chat completion request.
- */
-async function createHandleChatCompletionPromptContext(options: {
-    request: NextRequest;
-    title: string;
-    apiKey: string | null;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-}): Promise<HandleChatCompletionPromptContext> {
-    const { request, title, apiKey, parsedRequest, runtime } = options;
-    const timezone = request.headers.get('x-timezone') || 'UTC';
-    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
-    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
-    const recordChatHistoryMessage = await createChatHistoryRecorder({
-        request,
-        agentIdentifier: runtime.agentId,
-        agentHash: runtime.agentHash,
-        source: 'OPENAI_API_COMPATIBILITY',
-        apiKey,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        isEnabled: !runtime.isPrivateModeEnabled,
-    });
-    const userMessageHash = await recordChatHistoryMessage({
-        message: {
-            role: 'USER',
-            content: lastMessage.content,
-        },
-        previousMessageHash: null,
-    });
-    const promptParameters = composePromptParametersWithMemoryContext({
-        baseParameters: {
-            ...parsedRequest.incomingParameters,
-            timezone,
-        },
-        currentUserIdentity: runtime.currentUserIdentity,
-        agentPermanentId: runtime.agentId,
-        agentName: runtime.resolvedAgentContext.resolvedAgentName,
-        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
-        projectRepositories: runtime.projectRepositories,
-        projectGithubToken: runtime.projectGithubToken,
-        emailSmtpCredential: runtime.emailSmtpCredential,
-        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
-        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
-        calendarConnections: runtime.calendarConnections,
-    });
-    const prompt: ChatPrompt = {
-        title,
-        content: lastMessage.content,
-        modelRequirements: {
-            modelVariant: 'CHAT',
-            responseFormat: parsedRequest.responseFormat,
-            toolChoice: parsedRequest.runtimeToolChoice,
-            // We could pass 'model' from body if we wanted to enforce it, but Agent usually has its own config
-        },
-        parameters: promptParameters,
-        thread,
-        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
-    };
-
-    return {
-        prompt,
-        userMessageHash,
-        recordChatHistoryMessage,
-        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
-            isEnabled: !runtime.isPrivateModeEnabled,
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentId: runtime.agentId,
-            messages: parsedRequest.messages,
-            includeAssistantPlaceholder: true,
-            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
-        }),
-    };
-}
-
-/**
- * Handles the streaming OpenAI-compatible response branch.
- */
-function createHandleChatCompletionStreamResponse(options: {
-    request: NextRequest;
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Response {
-    const { request, title, parsedRequest, runtime, promptContext } = options;
-    const encoder = new TextEncoder();
-    const responseModel = parsedRequest.model || 'promptbook-agent';
-    const readableStream = new ReadableStream({
-        async start(controller) {
-            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
-            const created = Math.floor(Date.now() / 1000);
-            const emitSsePayload = (payload: unknown) => {
-                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
-            };
-            const emitDeltaChunk = (deltaContent: string) => {
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {
-                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
-                        },
-                        finishReason: null,
-                    }),
-                );
-            };
-
-            emitSsePayload(
-                createHandleChatCompletionChunk({
-                    runId,
-                    created,
-                    model: responseModel,
-                    delta: {
-                        role: 'assistant',
-                        content: '',
-                    },
-                    finishReason: null,
-                }),
-            );
-
-            let hasMeaningfulDelta = false;
-
-            try {
-                const handleStreamChunk = createChatStreamHandler({
-                    onDelta: (deltaContent) => {
-                        if (deltaContent.trim().length > 0) {
-                            hasMeaningfulDelta = true;
-                        }
-                        emitDeltaChunk(deltaContent);
-                    },
-                    onToolCalls: (toolCalls) => {
-                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
-                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
-                    },
-                });
-                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
-                    signal: request.signal,
-                });
-                const normalizedResponse = ensureNonEmptyChatContent({
-                    content: result.content,
-                    context: title,
-                });
-
-                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
-                    emitDeltaChunk(normalizedResponse.content);
-                }
-
-                const messageSuffixAppendix = createMessageSuffixAppendix(
-                    normalizedResponse.content,
-                    runtime.messageSuffix,
-                );
-                if (messageSuffixAppendix) {
-                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
-                        emitDeltaChunk(delta);
-                    });
-                }
-
-                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-                await finalizeHandleChatCompletionResult({
-                    responseContentWithSuffix,
-                    usage: result.usage,
-                    toolCalls: result.toolCalls,
-                    parsedRequest,
-                    runtime,
-                    promptContext,
-                    // Preserve historical behavior: streamed OpenAI-compatible calls still persist learning in private mode.
-                    shouldPersistLearnedAgentSource: true,
-                });
-
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {},
-                        finishReason: 'stop',
-                    }),
-                );
-                controller.enqueue(encoder.encode('[DONE]'));
-            } catch (error) {
-                console.error('Error during streaming:', error);
-                controller.error(error);
-            }
-            controller.close();
-        },
-    });
-
-    return new Response(readableStream, {
-        headers: {
-            'Content-Type': 'text/event-stream',
-            'Cache-Control': 'no-cache',
-            Connection: 'keep-alive',
-        },
-    });
-}
-
-/**
- * Handles the non-streaming OpenAI-compatible response branch.
- */
-async function createHandleChatCompletionJsonResponse(options: {
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Promise<NextResponse> {
-    const { title, parsedRequest, runtime, promptContext } = options;
-    const result = await runtime.agent.callChatModel(promptContext.prompt);
-    const normalizedResponse = ensureNonEmptyChatContent({
-        content: result.content,
-        context: title,
-    });
-    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-
-    await finalizeHandleChatCompletionResult({
-        responseContentWithSuffix,
-        usage: result.usage,
-        toolCalls: result.toolCalls,
-        parsedRequest,
-        runtime,
-        promptContext,
-        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
-    });
-
-    return NextResponse.json({
-        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
-        object: 'chat.completion',
-        created: Math.floor(Date.now() / 1000),
-        model: parsedRequest.model || 'promptbook-agent',
-        choices: [
-            {
-                index: 0,
-                message: {
-                    role: 'assistant',
-                    content: responseContentWithSuffix,
-                },
-                finish_reason: 'stop',
-            },
-        ],
-        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
-    });
-}
-
-/**
- * Runs the shared side effects after one successful model response.
- */
-async function finalizeHandleChatCompletionResult(options: {
-    responseContentWithSuffix: string;
-    usage: Usage;
-    toolCalls?: ReadonlyArray<ToolCall>;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
-
-    await promptContext.recordChatHistoryMessage({
-        message: {
-            role: 'MODEL',
-            content: responseContentWithSuffix,
-        },
-        previousMessageHash: promptContext.userMessageHash,
-        usage,
-    });
-
-    if (toolCalls && toolCalls.length > 0) {
-        await logCalendarToolCallsActivity({
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentPermanentId: runtime.agentId,
-            toolCalls,
-        });
-    }
-
-    await persistOpenAiCompatibilityFrozenChat({
-        isEnabled: !runtime.isPrivateModeEnabled,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        agentId: runtime.agentId,
-        chatId: promptContext.persistedFrozenChatId,
-        messages: parsedRequest.messages,
-        assistantContent: responseContentWithSuffix,
-        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
-    });
-    await persistHandleChatCompletionLearnedAgentSource({
-        runtime,
-        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
-    });
-}
-
-/**
- * Applies append-only self-learning updates when the current branch should persist them.
- */
-async function persistHandleChatCompletionLearnedAgentSource(options: {
-    runtime: HandleChatCompletionRuntime;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { runtime, shouldPersistLearnedAgentSource } = options;
-    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
-        return;
-    }
-
-    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
-        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
-        resolvedAgentSourceBeforeLearning: runtime.agentSource,
-        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
-    });
-
-    if (learnedAgentSource !== null) {
-        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
     }
 }
-
-/**
- * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
- */
-async function persistOpenAiCompatibilityFrozenChat(options: {
-    isEnabled: boolean;
-    userId: number | null | undefined;
-    agentId: string;
-    messages: ReadonlyArray<unknown>;
-    chatId?: string;
-    assistantContent?: string;
-    includeAssistantPlaceholder?: boolean;
-    failureMessage: string;
-}): Promise<string | undefined> {
-    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
-        options;
-    if (!isEnabled || !userId) {
-        return undefined;
-    }
-
-    const persistedFrozenChat = await persistFrozenUserChat({
-        userId,
-        agentPermanentId: agentId,
-        source: USER_CHAT_SOURCES.OPENAI_API,
-        chatId,
-        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
-            includeAssistantPlaceholder,
-        }),
-    }).catch((error) => {
-        console.error(failureMessage, error);
-        return null;
-    });
-
-    return persistedFrozenChat?.id;
-}
-
-/**
- * Builds one OpenAI-compatible thread from the earlier request messages.
- */
-function createHandleChatCompletionThread(previousMessages: ReadonlyArray<TODO_any>): ChatMessage[] {
-    return previousMessages.map((message: TODO_any, index: number) => ({
-        id: `msg-${index}`,
-        sender: message.role === 'assistant' ? 'agent' : 'user',
-        content: message.content,
-        isComplete: true,
-        createdAt: $getCurrentDate(),
-    }));
-}
-
-/**
- * Appends system messages to the agent source as dynamic `CONTEXT`.
- */
-function appendSystemMessagesToAgentSource(
-    agentSource: string_book,
-    messages: ReadonlyArray<TODO_any>,
-): {
-    agentSourceWithContext: string_book;
-    hasDynamicContext: boolean;
-} {
-    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
-    if (systemMessages.length === 0) {
-        return {
-            agentSourceWithContext: agentSource,
-            hasDynamicContext: false,
-        };
-    }
-
-    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
-    return {
-        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
-        hasDynamicContext: true,
-    };
-}
-
-/**
- * Waits for any running preparation job of the current agent before executing the chat call.
- */
-async function waitForHandleChatCompletionAgentPreparation(options: {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    agentId: string;
-    agentHash: string;
-}): Promise<void> {
-    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
-    const preparationWaitResult = await waitForRunningAgentPreparation({
-        tablePrefix,
-        agentPermanentId: options.agentId,
-        fingerprint: options.agentHash,
-        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    });
-
-    if (preparationWaitResult !== 'not_running') {
-        console.info('[pre-index]', 'openai_chat_wait_result', {
-            tablePrefix,
-            agentPermanentId: options.agentId,
-            agentHash: options.agentHash,
-            preparationWaitResult,
-        });
-    }
-}
-
-/**
- * Resolves request-scoped project, calendar, and email credentials for the current agent.
- */
-async function resolveHandleChatCompletionCredentials(options: {
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    agentId: string;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-}): Promise<{
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-}> {
-    const userId = options.currentUserIdentity?.userId;
-    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
-        resolveUseProjectGithubToken({
-            userId,
-            agentPermanentId: options.agentId,
-        }),
-        options.calendarConnections.length > 0
-            ? resolveUseCalendarGoogleToken({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-        options.useEmailConfiguration.isEnabled
-            ? resolveUseEmailSmtpCredential({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-    ]);
-
-    return {
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-    };
-}
-
-/**
- * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
- */
-async function createHandleChatCompletionAgent(options: {
-    agentName: string;
-    agentSource: string_book;
-    agentId: string;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    localServerUrl: string;
-    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
-    hasDynamicContext: boolean;
-    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
-}): Promise<Agent> {
-    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
-    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
-    const runtimeAgentReferenceResolver = options.hasDynamicContext
-        ? createBookScopedAgentReferenceResolver({
-              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
-              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
-              localServerUrl: options.localServerUrl,
-              fallbackResolver: options.baseAgentReferenceResolver,
-          })
-        : undefined;
-    const agentModelRequirements =
-        options.preparedAgentModelRequirements?.modelRequirements ||
-        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
-            agentReferenceResolver: runtimeAgentReferenceResolver,
-            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
-        }));
-
-    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
-        options.agentSource,
-        options.resolvedAgentContext.resolvedAgentName,
-        await baseOpenAiToolsPromise,
-        {
-            includeDynamicContext: true,
-            agentId: options.agentId,
-            modelRequirements: agentModelRequirements,
-        },
-    );
-
-    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
-        agentName: options.agentName,
-        assistantCacheKey: agentKitResult.assistantCacheKey,
-        vectorStoreHash: agentKitResult.vectorStoreHash,
-        vectorStoreId: agentKitResult.vectorStoreId,
-    });
-
-    return new Agent({
-        agentSource: options.agentSource,
-        executionTools: {
-            llm: agentKitResult.tools,
-        },
-        precomputedModelRequirements: agentModelRequirements,
-        assistantPreparationMode: 'external',
-        isVerbose: true,
-        teacherAgent: null,
-    });
-}
-
-/**
- * Creates one OpenAI-compatible streaming chunk payload.
- */
-function createHandleChatCompletionChunk(options: {
-    runId: string;
-    created: number;
-    model: TODO_any;
-    delta: Record<string, unknown>;
-    finishReason: 'stop' | null;
-}) {
-    return {
-        id: options.runId,
-        object: 'chat.completion.chunk',
-        created: options.created,
-        model: options.model,
-        choices: [
-            {
-                index: 0,
-                delta: options.delta,
-                finish_reason: options.finishReason,
-            },
-        ],
-    };
-}
-
-/**
- * Creates one consistent OpenAI-compatible error response.
- */
-function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
-    return NextResponse.json(
-        {
-            error: {
-                message,
-                type,
-            },
-        },
-        { status },
-    );
-}
-
-// TODO: [🈹] Maybe move chat thread handling here
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f49a4fa80fa21f140e25de59596e5546d62e3f7
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
@@ -0,0 +1,18 @@
+import { NextResponse } from 'next/server';
+
+/**
+ * Creates one consistent OpenAI-compatible error response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
+    return NextResponse.json(
+        {
+            error: {
+                message,
+                type,
+            },
+        },
+        { status },
+    );
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
new file mode 100644
index 0000000000000000000000000000000000000000..58e1c26d7f428c1bdb39b36411b3f75256e3de51
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
@@ -0,0 +1,176 @@
+import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
+import type { LlmToolDefinition, TODO_any } from '@promptbook-local/types';
+import { NextRequest, NextResponse } from 'next/server';
+import type OpenAI from 'openai';
+import { HTTP_STATUS_CODES } from '../../constants';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Definition of open AI chat tool.
+ */
+type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
+
+/**
+ * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionParsedRequest = {
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    threadMessages: ReadonlyArray<TODO_any>;
+    stream: TODO_any;
+    model: TODO_any;
+    responseFormat: TODO_any;
+    runtimeTools?: Array<LlmToolDefinition>;
+    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+    incomingParameters: Record<string, unknown>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Parses and validates the OpenAI-compatible request payload.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function parseHandleChatCompletionRequest(options: {
+    body: TODO_any;
+    agentNameFromParams?: string;
+    request: NextRequest;
+}): HandleChatCompletionParsedRequest | NextResponse {
+    const { body, agentNameFromParams, request } = options;
+    const {
+        messages,
+        stream,
+        model,
+        response_format: responseFormat,
+        tools: rawTools,
+        tool_choice: toolChoice,
+        parameters: rawParameters = {},
+    } = body;
+    const agentName = (agentNameFromParams || model) as string | undefined;
+    if (!agentName) {
+        return createHandleChatCompletionErrorResponse(
+            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    if (!messages || !Array.isArray(messages) || messages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array is required and cannot be empty.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
+    if (threadMessages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array must contain at least one non-system message.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    return {
+        agentName,
+        messages,
+        threadMessages,
+        stream,
+        model,
+        responseFormat,
+        runtimeTools: convertOpenAiTools(rawTools),
+        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
+        incomingParameters:
+            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
+                ? (rawParameters as Record<string, unknown>)
+                : {},
+        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
+    };
+}
+
+/**
+ * Handles convert open Ai tools.
+ */
+function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
+    if (!Array.isArray(rawTools)) {
+        return undefined;
+    }
+
+    const converted = rawTools
+        .map((tool) => convertOpenAiTool(tool))
+        .filter((tool): tool is LlmToolDefinition => tool !== null);
+
+    return converted.length > 0 ? converted : undefined;
+}
+
+/**
+ * Handles convert open Ai tool.
+ */
+function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
+    const tool = rawTool as OpenAIChatToolDefinition;
+    if (!tool || tool.type !== 'function') {
+        return null;
+    }
+
+    const functionDefinition = tool.function;
+    if (!functionDefinition) {
+        return null;
+    }
+
+    const { name, description, parameters } = functionDefinition;
+    if (typeof name !== 'string' || name.trim().length === 0) {
+        return null;
+    }
+
+    const parameterSchema = parameters ?? {};
+    if (parameterSchema.type !== 'object') {
+        return null;
+    }
+
+    const properties = parameterSchema.properties ?? {};
+    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
+
+    for (const [key, value] of Object.entries(properties)) {
+        if (!value || typeof value !== 'object') {
+            continue;
+        }
+
+        normalizedProperties[key] = {
+            type: typeof value.type === 'string' ? value.type : 'string',
+            description: typeof value.description === 'string' ? value.description : undefined,
+        };
+    }
+
+    const required =
+        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
+            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
+            : undefined;
+
+    return {
+        name,
+        description: typeof description === 'string' ? description : '',
+        parameters: {
+            type: 'object',
+            properties: normalizedProperties,
+            required,
+            additionalProperties:
+                typeof parameterSchema.additionalProperties === 'boolean'
+                    ? parameterSchema.additionalProperties
+                    : undefined,
+        },
+    };
+}
+
+/**
+ * Parses open Ai tool choice.
+ */
+function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
+    if (value === undefined || value === null) {
+        return undefined;
+    }
+
+    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+}

file update:
diff --git a/apps/agents-server/src/utils/handleChatCompletion.ts b/apps/agents-server/src/utils/handleChatCompletion.ts
index 9c96d36fad9cf24bf26d89f57372332add4e4595..cff56fafd6cfa90bb4094f9f202c4f01f6f74af1
--- a/apps/agents-server/src/utils/handleChatCompletion.ts
+++ b/apps/agents-server/src/utils/handleChatCompletion.ts
@@ -1,392 +1,16 @@
-import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
-import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
-import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
-import {
-    createBookScopedAgentReferenceResolver,
-    parseBookScopedAgentIdentifier,
-} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
-import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
-import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
-import {
-    appendMessageSuffix,
-    createMessageSuffixAppendix,
-    emulateMessageSuffixStreaming,
-    resolveMessageSuffixFromAgentSource,
-} from '@/src/utils/chat/messageSuffix';
-import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
-import { prepareToolCallsForStreaming } from './toolCallStreaming';
-import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
-import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
-import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
-import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
-import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
-import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
-import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
-import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
-import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
-import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
-import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
-import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
-import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
-import type {
-    ChatMessage,
-    ChatPrompt,
-    LlmToolDefinition,
-    string_book,
-    ToolCall,
-    TODO_any,
-    UncertainNumber,
-    Usage,
-    UsageCounts,
-} from '@promptbook-local/types';
-import { $getCurrentDate } from '@promptbook-local/utils';
 import { NextRequest, NextResponse } from 'next/server';
-import type OpenAI from 'openai';
-import { computeUsageCounts } from '../../../../src/execution/utils/computeUsageCounts';
-import { encodeChatStreamWhitespaceForTransport } from '../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
-import { isAgentDeleted } from '../app/agents/[agentName]/_utils';
 import { HTTP_STATUS_CODES } from '../constants';
-import { AgentKitCacheManager } from './cache/AgentKitCacheManager';
 import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
+import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
+import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
+import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
+import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
+import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
 import { validateApiKey } from './validateApiKey';
-import {
-    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    resolveAgentCollectionTablePrefix,
-    waitForRunningAgentPreparation,
-} from './agentPreparation';
-import {
-    resolveCachedServerAgentContext,
-    resolveCachedServerAgentModelRequirements,
-} from './cachedServerAgentRuntime';
-import { resolveAppendOnlySelfLearningAgentSource } from './resolveAppendOnlySelfLearningAgentSource';
 
 /**
- * Falls back to the estimated value when the original token count is unknown.
- *
- * @param tokenCount - Token count reported by the execution tools.
- * @param fallbackValue - Estimated token count based on text length.
- * @returns Token count to report in the OpenAI-compatible response.
- */
-function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
-    if (tokenCount.value === 0 && tokenCount.isUncertain) {
-        return {
-            value: fallbackValue,
-            isUncertain: true,
-        };
-    }
-
-    return tokenCount;
-}
-
-/**
- * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
- *
- * @param promptContent - Prompt content used for the request.
- * @param completionContent - Assistant response content.
- * @param usage - Native Promptbook usage details.
- * @returns Usage payload for compatibility responses.
- */
-function createCompatibilityUsage(
-    promptContent: string,
-    completionContent: string,
-    usage: Usage,
-): {
-    prompt_tokens: number;
-    completion_tokens: number;
-    total_tokens: number;
-    details: Usage;
-} {
-    const promptCounts = computeUsageCounts(promptContent);
-    const completionCounts = computeUsageCounts(completionContent);
-    const inputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
-        ...promptCounts,
-    };
-    const outputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
-        ...completionCounts,
-    };
-
-    const promptTokens = inputUsage.tokensCount.value;
-    const completionTokens = outputUsage.tokensCount.value;
-
-    return {
-        prompt_tokens: promptTokens,
-        completion_tokens: completionTokens,
-        total_tokens: promptTokens + completionTokens,
-        details: {
-            price: usage.price,
-            duration: usage.duration,
-            input: inputUsage,
-            output: outputUsage,
-        },
-    };
-}
-
-/**
- * Definition of open AI chat tool.
- */
-type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
-
-/**
- * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
- */
-type OpenAiCompatibilityMessageLike = {
-    role?: unknown;
-    content?: unknown;
-};
-
-/**
- * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
- */
-type HandleChatCompletionParsedRequest = {
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    threadMessages: ReadonlyArray<TODO_any>;
-    stream: TODO_any;
-    model: TODO_any;
-    responseFormat: TODO_any;
-    runtimeTools?: Array<LlmToolDefinition>;
-    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-    incomingParameters: Record<string, unknown>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Runtime state resolved before the chat model is executed.
- */
-type HandleChatCompletionRuntime = {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    agent: Agent;
-    agentSource: string_book;
-    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
-    messageSuffix: string | null;
-    agentId: string;
-    agentHash: string;
-    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Prompt, history recorder, and persistence context created for one chat call.
- */
-type HandleChatCompletionPromptContext = {
-    prompt: ChatPrompt;
-    userMessageHash: string;
-    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
-    persistedFrozenChatId?: string;
-};
-
-/**
- * Handles convert open Ai tools.
- */
-function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
-    if (!Array.isArray(rawTools)) {
-        return undefined;
-    }
-
-    const converted = rawTools
-        .map((tool) => convertOpenAiTool(tool))
-        .filter((tool): tool is LlmToolDefinition => tool !== null);
-
-    return converted.length > 0 ? converted : undefined;
-}
-
-/**
- * Handles convert open Ai tool.
- */
-function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
-    const tool = rawTool as OpenAIChatToolDefinition;
-    if (!tool || tool.type !== 'function') {
-        return null;
-    }
-
-    const functionDefinition = tool.function;
-    if (!functionDefinition) {
-        return null;
-    }
-
-    const { name, description, parameters } = functionDefinition;
-    if (typeof name !== 'string' || name.trim().length === 0) {
-        return null;
-    }
-
-    const parameterSchema = parameters ?? {};
-    if (parameterSchema.type !== 'object') {
-        return null;
-    }
-
-    const properties = parameterSchema.properties ?? {};
-    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
-
-    for (const [key, value] of Object.entries(properties)) {
-        if (!value || typeof value !== 'object') {
-            continue;
-        }
-
-        normalizedProperties[key] = {
-            type: typeof value.type === 'string' ? value.type : 'string',
-            description: typeof value.description === 'string' ? value.description : undefined,
-        };
-    }
-
-    const required =
-        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
-            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
-            : undefined;
-
-    return {
-        name,
-        description: typeof description === 'string' ? description : '',
-        parameters: {
-            type: 'object',
-            properties: normalizedProperties,
-            required,
-            additionalProperties:
-                typeof parameterSchema.additionalProperties === 'boolean'
-                    ? parameterSchema.additionalProperties
-                    : undefined,
-        },
-    };
-}
-
-/**
- * Parses open Ai tool choice.
- */
-function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
-    if (value === undefined || value === null) {
-        return undefined;
-    }
-
-    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-}
-
-/**
- * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
- */
-function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
-    if (typeof value === 'string') {
-        return value;
-    }
-
-    if (Array.isArray(value)) {
-        return value
-            .map((part) => {
-                if (typeof part === 'string') {
-                    return part;
-                }
-
-                if (!part || typeof part !== 'object') {
-                    return '';
-                }
-
-                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
-                if (typeof partLike.text === 'string') {
-                    return partLike.text;
-                }
-
-                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
-                    return '[Image input]';
-                }
-
-                if (partLike.image_url) {
-                    return '[Image input]';
-                }
-
-                return '';
-            })
-            .filter(Boolean)
-            .join('\n');
-    }
-
-    if (value === null || value === undefined) {
-        return '';
-    }
-
-    try {
-        return JSON.stringify(value, null, 2);
-    } catch {
-        return String(value);
-    }
-}
-
-/**
- * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ * Handles convert open Ai chat completion requests routed through the Agents Server.
  */
-function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
-    if (typeof role !== 'string') {
-        return 'USER';
-    }
-
-    switch (role) {
-        case 'assistant':
-            return 'AGENT';
-        case 'system':
-            return 'SYSTEM';
-        case 'tool':
-            return 'TOOL';
-        case 'developer':
-            return 'SYSTEM';
-        default:
-            return 'USER';
-    }
-}
-
-/**
- * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
- */
-function createFrozenOpenAiCompatibilityMessages(
-    rawMessages: ReadonlyArray<unknown>,
-    assistantContent?: string,
-    options: {
-        includeAssistantPlaceholder?: boolean;
-    } = {},
-): Array<ChatMessage> {
-    const startedAt = Date.now();
-    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
-        const message = rawMessage as OpenAiCompatibilityMessageLike;
-
-        return {
-            id: `openai-frozen-${index}`,
-            sender: mapOpenAiCompatibilityRoleToSender(message.role),
-            content: normalizeOpenAiCompatibilityMessageContent(message.content),
-            isComplete: true,
-            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage;
-    });
-
-    if (assistantContent !== undefined) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: assistantContent,
-            isComplete: true,
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    } else if (options.includeAssistantPlaceholder) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: '',
-            isComplete: false,
-            lifecycleState: 'running',
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    }
-
-    return messages;
-}
-
-/**
- * Handles chat completion.
- */
 export async function handleChatCompletion(
     request: NextRequest,
     params: { agentName?: string },
@@ -399,7 +23,6 @@
         return versionMismatchResponse;
     }
 
-    // Validate API key explicitly (in addition to middleware)
     const apiKeyValidation = await validateApiKey(request);
     if (!apiKeyValidation.isValid) {
         return NextResponse.json(
@@ -462,724 +85,5 @@
             { error: { message: (error as Error).message || 'Internal Server Error', type: 'server_error' } },
             { status: HTTP_STATUS_CODES.INTERNAL_SERVER_ERROR },
         );
-    }
-}
-
-/**
- * Parses and validates the OpenAI-compatible request payload.
- */
-function parseHandleChatCompletionRequest(options: {
-    body: TODO_any;
-    agentNameFromParams?: string;
-    request: NextRequest;
-}): HandleChatCompletionParsedRequest | NextResponse {
-    const { body, agentNameFromParams, request } = options;
-    const {
-        messages,
-        stream,
-        model,
-        response_format: responseFormat,
-        tools: rawTools,
-        tool_choice: toolChoice,
-        parameters: rawParameters = {},
-    } = body;
-    const agentName = (agentNameFromParams || model) as string | undefined;
-    if (!agentName) {
-        return createHandleChatCompletionErrorResponse(
-            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    if (!messages || !Array.isArray(messages) || messages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array is required and cannot be empty.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
-    if (threadMessages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array must contain at least one non-system message.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    return {
-        agentName,
-        messages,
-        threadMessages,
-        stream,
-        model,
-        responseFormat,
-        runtimeTools: convertOpenAiTools(rawTools),
-        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
-        incomingParameters:
-            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
-                ? (rawParameters as Record<string, unknown>)
-                : {},
-        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
-    };
-}
-
-/**
- * Resolves agent/runtime dependencies needed before prompt execution.
- */
-async function resolveHandleChatCompletionRuntime(options: {
-    request: NextRequest;
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    isPrivateModeEnabled: boolean;
-}): Promise<HandleChatCompletionRuntime | NextResponse> {
-    const { request, agentName, messages, isPrivateModeEnabled } = options;
-    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
-    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
-
-    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
-        return createHandleChatCompletionErrorResponse(
-            'This agent has been deleted. You can restore it from the Recycle Bin.',
-            'agent_deleted',
-            410,
-        );
-    }
-
-    const [collection, baseAgentReferenceResolver] = await Promise.all([
-        $provideAgentCollectionForServer(),
-        $provideAgentReferenceResolver(),
-    ]);
-    const localServerUrl = new URL(request.url).origin;
-    const resolvedAgentContext = await resolveCachedServerAgentContext({
-        collection,
-        agentIdentifier: agentName,
-        localServerUrl,
-        fallbackResolver: baseAgentReferenceResolver,
-    }).catch(() => null);
-
-    if (!resolvedAgentContext?.resolvedAgentSource) {
-        return createHandleChatCompletionErrorResponse(
-            `Agent '${agentName}' not found.`,
-            'invalid_request_error',
-            HTTP_STATUS_CODES.NOT_FOUND,
-        );
-    }
-
-    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
-    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
-    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
-    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
-    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
-    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
-    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
-    agentSource = agentSourceWithContext;
-
-    const preparedAgentModelRequirements = !hasDynamicContext
-        ? await resolveCachedServerAgentModelRequirements({
-              resolvedAgentContext,
-              localServerUrl,
-              fallbackResolver: baseAgentReferenceResolver,
-          })
-        : null;
-    const agentId = resolvedAgentContext.parentAgentPermanentId;
-    const agentHash = computeAgentHash(agentSource);
-    await waitForHandleChatCompletionAgentPreparation({
-        collection,
-        agentId,
-        agentHash,
-    });
-
-    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
-    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
-        await resolveHandleChatCompletionCredentials({
-            currentUserIdentity,
-            agentId,
-            calendarConnections,
-            useEmailConfiguration,
-        });
-    const agent = await createHandleChatCompletionAgent({
-        agentName,
-        agentSource,
-        agentId,
-        resolvedAgentContext,
-        localServerUrl,
-        baseAgentReferenceResolver,
-        hasDynamicContext,
-        preparedAgentModelRequirements,
-    });
-
-    return {
-        collection,
-        resolvedAgentContext,
-        agent,
-        agentSource,
-        unresolvedAgentSource,
-        messageSuffix,
-        agentId,
-        agentHash,
-        projectRepositories,
-        calendarConnections,
-        useEmailConfiguration,
-        currentUserIdentity,
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-        isPrivateModeEnabled,
-    };
-}
-
-/**
- * Creates prompt and persistence helpers for one chat completion request.
- */
-async function createHandleChatCompletionPromptContext(options: {
-    request: NextRequest;
-    title: string;
-    apiKey: string | null;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-}): Promise<HandleChatCompletionPromptContext> {
-    const { request, title, apiKey, parsedRequest, runtime } = options;
-    const timezone = request.headers.get('x-timezone') || 'UTC';
-    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
-    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
-    const recordChatHistoryMessage = await createChatHistoryRecorder({
-        request,
-        agentIdentifier: runtime.agentId,
-        agentHash: runtime.agentHash,
-        source: 'OPENAI_API_COMPATIBILITY',
-        apiKey,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        isEnabled: !runtime.isPrivateModeEnabled,
-    });
-    const userMessageHash = await recordChatHistoryMessage({
-        message: {
-            role: 'USER',
-            content: lastMessage.content,
-        },
-        previousMessageHash: null,
-    });
-    const promptParameters = composePromptParametersWithMemoryContext({
-        baseParameters: {
-            ...parsedRequest.incomingParameters,
-            timezone,
-        },
-        currentUserIdentity: runtime.currentUserIdentity,
-        agentPermanentId: runtime.agentId,
-        agentName: runtime.resolvedAgentContext.resolvedAgentName,
-        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
-        projectRepositories: runtime.projectRepositories,
-        projectGithubToken: runtime.projectGithubToken,
-        emailSmtpCredential: runtime.emailSmtpCredential,
-        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
-        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
-        calendarConnections: runtime.calendarConnections,
-    });
-    const prompt: ChatPrompt = {
-        title,
-        content: lastMessage.content,
-        modelRequirements: {
-            modelVariant: 'CHAT',
-            responseFormat: parsedRequest.responseFormat,
-            toolChoice: parsedRequest.runtimeToolChoice,
-            // We could pass 'model' from body if we wanted to enforce it, but Agent usually has its own config
-        },
-        parameters: promptParameters,
-        thread,
-        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
-    };
-
-    return {
-        prompt,
-        userMessageHash,
-        recordChatHistoryMessage,
-        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
-            isEnabled: !runtime.isPrivateModeEnabled,
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentId: runtime.agentId,
-            messages: parsedRequest.messages,
-            includeAssistantPlaceholder: true,
-            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
-        }),
-    };
-}
-
-/**
- * Handles the streaming OpenAI-compatible response branch.
- */
-function createHandleChatCompletionStreamResponse(options: {
-    request: NextRequest;
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Response {
-    const { request, title, parsedRequest, runtime, promptContext } = options;
-    const encoder = new TextEncoder();
-    const responseModel = parsedRequest.model || 'promptbook-agent';
-    const readableStream = new ReadableStream({
-        async start(controller) {
-            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
-            const created = Math.floor(Date.now() / 1000);
-            const emitSsePayload = (payload: unknown) => {
-                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
-            };
-            const emitDeltaChunk = (deltaContent: string) => {
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {
-                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
-                        },
-                        finishReason: null,
-                    }),
-                );
-            };
-
-            emitSsePayload(
-                createHandleChatCompletionChunk({
-                    runId,
-                    created,
-                    model: responseModel,
-                    delta: {
-                        role: 'assistant',
-                        content: '',
-                    },
-                    finishReason: null,
-                }),
-            );
-
-            let hasMeaningfulDelta = false;
-
-            try {
-                const handleStreamChunk = createChatStreamHandler({
-                    onDelta: (deltaContent) => {
-                        if (deltaContent.trim().length > 0) {
-                            hasMeaningfulDelta = true;
-                        }
-                        emitDeltaChunk(deltaContent);
-                    },
-                    onToolCalls: (toolCalls) => {
-                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
-                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
-                    },
-                });
-                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
-                    signal: request.signal,
-                });
-                const normalizedResponse = ensureNonEmptyChatContent({
-                    content: result.content,
-                    context: title,
-                });
-
-                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
-                    emitDeltaChunk(normalizedResponse.content);
-                }
-
-                const messageSuffixAppendix = createMessageSuffixAppendix(
-                    normalizedResponse.content,
-                    runtime.messageSuffix,
-                );
-                if (messageSuffixAppendix) {
-                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
-                        emitDeltaChunk(delta);
-                    });
-                }
-
-                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-                await finalizeHandleChatCompletionResult({
-                    responseContentWithSuffix,
-                    usage: result.usage,
-                    toolCalls: result.toolCalls,
-                    parsedRequest,
-                    runtime,
-                    promptContext,
-                    // Preserve historical behavior: streamed OpenAI-compatible calls still persist learning in private mode.
-                    shouldPersistLearnedAgentSource: true,
-                });
-
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {},
-                        finishReason: 'stop',
-                    }),
-                );
-                controller.enqueue(encoder.encode('[DONE]'));
-            } catch (error) {
-                console.error('Error during streaming:', error);
-                controller.error(error);
-            }
-            controller.close();
-        },
-    });
-
-    return new Response(readableStream, {
-        headers: {
-            'Content-Type': 'text/event-stream',
-            'Cache-Control': 'no-cache',
-            Connection: 'keep-alive',
-        },
-    });
-}
-
-/**
- * Handles the non-streaming OpenAI-compatible response branch.
- */
-async function createHandleChatCompletionJsonResponse(options: {
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Promise<NextResponse> {
-    const { title, parsedRequest, runtime, promptContext } = options;
-    const result = await runtime.agent.callChatModel(promptContext.prompt);
-    const normalizedResponse = ensureNonEmptyChatContent({
-        content: result.content,
-        context: title,
-    });
-    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-
-    await finalizeHandleChatCompletionResult({
-        responseContentWithSuffix,
-        usage: result.usage,
-        toolCalls: result.toolCalls,
-        parsedRequest,
-        runtime,
-        promptContext,
-        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
-    });
-
-    return NextResponse.json({
-        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
-        object: 'chat.completion',
-        created: Math.floor(Date.now() / 1000),
-        model: parsedRequest.model || 'promptbook-agent',
-        choices: [
-            {
-                index: 0,
-                message: {
-                    role: 'assistant',
-                    content: responseContentWithSuffix,
-                },
-                finish_reason: 'stop',
-            },
-        ],
-        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
-    });
-}
-
-/**
- * Runs the shared side effects after one successful model response.
- */
-async function finalizeHandleChatCompletionResult(options: {
-    responseContentWithSuffix: string;
-    usage: Usage;
-    toolCalls?: ReadonlyArray<ToolCall>;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
-
-    await promptContext.recordChatHistoryMessage({
-        message: {
-            role: 'MODEL',
-            content: responseContentWithSuffix,
-        },
-        previousMessageHash: promptContext.userMessageHash,
-        usage,
-    });
-
-    if (toolCalls && toolCalls.length > 0) {
-        await logCalendarToolCallsActivity({
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentPermanentId: runtime.agentId,
-            toolCalls,
-        });
-    }
-
-    await persistOpenAiCompatibilityFrozenChat({
-        isEnabled: !runtime.isPrivateModeEnabled,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        agentId: runtime.agentId,
-        chatId: promptContext.persistedFrozenChatId,
-        messages: parsedRequest.messages,
-        assistantContent: responseContentWithSuffix,
-        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
-    });
-    await persistHandleChatCompletionLearnedAgentSource({
-        runtime,
-        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
-    });
-}
-
-/**
- * Applies append-only self-learning updates when the current branch should persist them.
- */
-async function persistHandleChatCompletionLearnedAgentSource(options: {
-    runtime: HandleChatCompletionRuntime;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { runtime, shouldPersistLearnedAgentSource } = options;
-    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
-        return;
-    }
-
-    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
-        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
-        resolvedAgentSourceBeforeLearning: runtime.agentSource,
-        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
-    });
-
-    if (learnedAgentSource !== null) {
-        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
     }
 }
-
-/**
- * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
- */
-async function persistOpenAiCompatibilityFrozenChat(options: {
-    isEnabled: boolean;
-    userId: number | null | undefined;
-    agentId: string;
-    messages: ReadonlyArray<unknown>;
-    chatId?: string;
-    assistantContent?: string;
-    includeAssistantPlaceholder?: boolean;
-    failureMessage: string;
-}): Promise<string | undefined> {
-    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
-        options;
-    if (!isEnabled || !userId) {
-        return undefined;
-    }
-
-    const persistedFrozenChat = await persistFrozenUserChat({
-        userId,
-        agentPermanentId: agentId,
-        source: USER_CHAT_SOURCES.OPENAI_API,
-        chatId,
-        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
-            includeAssistantPlaceholder,
-        }),
-    }).catch((error) => {
-        console.error(failureMessage, error);
-        return null;
-    });
-
-    return persistedFrozenChat?.id;
-}
-
-/**
- * Builds one OpenAI-compatible thread from the earlier request messages.
- */
-function createHandleChatCompletionThread(previousMessages: ReadonlyArray<TODO_any>): ChatMessage[] {
-    return previousMessages.map((message: TODO_any, index: number) => ({
-        id: `msg-${index}`,
-        sender: message.role === 'assistant' ? 'agent' : 'user',
-        content: message.content,
-        isComplete: true,
-        createdAt: $getCurrentDate(),
-    }));
-}
-
-/**
- * Appends system messages to the agent source as dynamic `CONTEXT`.
- */
-function appendSystemMessagesToAgentSource(
-    agentSource: string_book,
-    messages: ReadonlyArray<TODO_any>,
-): {
-    agentSourceWithContext: string_book;
-    hasDynamicContext: boolean;
-} {
-    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
-    if (systemMessages.length === 0) {
-        return {
-            agentSourceWithContext: agentSource,
-            hasDynamicContext: false,
-        };
-    }
-
-    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
-    return {
-        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
-        hasDynamicContext: true,
-    };
-}
-
-/**
- * Waits for any running preparation job of the current agent before executing the chat call.
- */
-async function waitForHandleChatCompletionAgentPreparation(options: {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    agentId: string;
-    agentHash: string;
-}): Promise<void> {
-    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
-    const preparationWaitResult = await waitForRunningAgentPreparation({
-        tablePrefix,
-        agentPermanentId: options.agentId,
-        fingerprint: options.agentHash,
-        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    });
-
-    if (preparationWaitResult !== 'not_running') {
-        console.info('[pre-index]', 'openai_chat_wait_result', {
-            tablePrefix,
-            agentPermanentId: options.agentId,
-            agentHash: options.agentHash,
-            preparationWaitResult,
-        });
-    }
-}
-
-/**
- * Resolves request-scoped project, calendar, and email credentials for the current agent.
- */
-async function resolveHandleChatCompletionCredentials(options: {
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    agentId: string;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-}): Promise<{
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-}> {
-    const userId = options.currentUserIdentity?.userId;
-    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
-        resolveUseProjectGithubToken({
-            userId,
-            agentPermanentId: options.agentId,
-        }),
-        options.calendarConnections.length > 0
-            ? resolveUseCalendarGoogleToken({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-        options.useEmailConfiguration.isEnabled
-            ? resolveUseEmailSmtpCredential({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-    ]);
-
-    return {
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-    };
-}
-
-/**
- * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
- */
-async function createHandleChatCompletionAgent(options: {
-    agentName: string;
-    agentSource: string_book;
-    agentId: string;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    localServerUrl: string;
-    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
-    hasDynamicContext: boolean;
-    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
-}): Promise<Agent> {
-    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
-    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
-    const runtimeAgentReferenceResolver = options.hasDynamicContext
-        ? createBookScopedAgentReferenceResolver({
-              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
-              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
-              localServerUrl: options.localServerUrl,
-              fallbackResolver: options.baseAgentReferenceResolver,
-          })
-        : undefined;
-    const agentModelRequirements =
-        options.preparedAgentModelRequirements?.modelRequirements ||
-        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
-            agentReferenceResolver: runtimeAgentReferenceResolver,
-            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
-        }));
-
-    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
-        options.agentSource,
-        options.resolvedAgentContext.resolvedAgentName,
-        await baseOpenAiToolsPromise,
-        {
-            includeDynamicContext: true,
-            agentId: options.agentId,
-            modelRequirements: agentModelRequirements,
-        },
-    );
-
-    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
-        agentName: options.agentName,
-        assistantCacheKey: agentKitResult.assistantCacheKey,
-        vectorStoreHash: agentKitResult.vectorStoreHash,
-        vectorStoreId: agentKitResult.vectorStoreId,
-    });
-
-    return new Agent({
-        agentSource: options.agentSource,
-        executionTools: {
-            llm: agentKitResult.tools,
-        },
-        precomputedModelRequirements: agentModelRequirements,
-        assistantPreparationMode: 'external',
-        isVerbose: true,
-        teacherAgent: null,
-    });
-}
-
-/**
- * Creates one OpenAI-compatible streaming chunk payload.
- */
-function createHandleChatCompletionChunk(options: {
-    runId: string;
-    created: number;
-    model: TODO_any;
-    delta: Record<string, unknown>;
-    finishReason: 'stop' | null;
-}) {
-    return {
-        id: options.runId,
-        object: 'chat.completion.chunk',
-        created: options.created,
-        model: options.model,
-        choices: [
-            {
-                index: 0,
-                delta: options.delta,
-                finish_reason: options.finishReason,
-            },
-        ],
-    };
-}
-
-/**
- * Creates one consistent OpenAI-compatible error response.
- */
-function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
-    return NextResponse.json(
-        {
-            error: {
-                message,
-                type,
-            },
-        },
-        { status },
-    );
-}
-
-// TODO: [🈹] Maybe move chat thread handling here
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f49a4fa80fa21f140e25de59596e5546d62e3f7
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
@@ -0,0 +1,18 @@
+import { NextResponse } from 'next/server';
+
+/**
+ * Creates one consistent OpenAI-compatible error response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
+    return NextResponse.json(
+        {
+            error: {
+                message,
+                type,
+            },
+        },
+        { status },
+    );
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
new file mode 100644
index 0000000000000000000000000000000000000000..58e1c26d7f428c1bdb39b36411b3f75256e3de51
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
@@ -0,0 +1,176 @@
+import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
+import type { LlmToolDefinition, TODO_any } from '@promptbook-local/types';
+import { NextRequest, NextResponse } from 'next/server';
+import type OpenAI from 'openai';
+import { HTTP_STATUS_CODES } from '../../constants';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Definition of open AI chat tool.
+ */
+type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
+
+/**
+ * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionParsedRequest = {
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    threadMessages: ReadonlyArray<TODO_any>;
+    stream: TODO_any;
+    model: TODO_any;
+    responseFormat: TODO_any;
+    runtimeTools?: Array<LlmToolDefinition>;
+    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+    incomingParameters: Record<string, unknown>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Parses and validates the OpenAI-compatible request payload.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function parseHandleChatCompletionRequest(options: {
+    body: TODO_any;
+    agentNameFromParams?: string;
+    request: NextRequest;
+}): HandleChatCompletionParsedRequest | NextResponse {
+    const { body, agentNameFromParams, request } = options;
+    const {
+        messages,
+        stream,
+        model,
+        response_format: responseFormat,
+        tools: rawTools,
+        tool_choice: toolChoice,
+        parameters: rawParameters = {},
+    } = body;
+    const agentName = (agentNameFromParams || model) as string | undefined;
+    if (!agentName) {
+        return createHandleChatCompletionErrorResponse(
+            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    if (!messages || !Array.isArray(messages) || messages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array is required and cannot be empty.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
+    if (threadMessages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array must contain at least one non-system message.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    return {
+        agentName,
+        messages,
+        threadMessages,
+        stream,
+        model,
+        responseFormat,
+        runtimeTools: convertOpenAiTools(rawTools),
+        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
+        incomingParameters:
+            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
+                ? (rawParameters as Record<string, unknown>)
+                : {},
+        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
+    };
+}
+
+/**
+ * Handles convert open Ai tools.
+ */
+function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
+    if (!Array.isArray(rawTools)) {
+        return undefined;
+    }
+
+    const converted = rawTools
+        .map((tool) => convertOpenAiTool(tool))
+        .filter((tool): tool is LlmToolDefinition => tool !== null);
+
+    return converted.length > 0 ? converted : undefined;
+}
+
+/**
+ * Handles convert open Ai tool.
+ */
+function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
+    const tool = rawTool as OpenAIChatToolDefinition;
+    if (!tool || tool.type !== 'function') {
+        return null;
+    }
+
+    const functionDefinition = tool.function;
+    if (!functionDefinition) {
+        return null;
+    }
+
+    const { name, description, parameters } = functionDefinition;
+    if (typeof name !== 'string' || name.trim().length === 0) {
+        return null;
+    }
+
+    const parameterSchema = parameters ?? {};
+    if (parameterSchema.type !== 'object') {
+        return null;
+    }
+
+    const properties = parameterSchema.properties ?? {};
+    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
+
+    for (const [key, value] of Object.entries(properties)) {
+        if (!value || typeof value !== 'object') {
+            continue;
+        }
+
+        normalizedProperties[key] = {
+            type: typeof value.type === 'string' ? value.type : 'string',
+            description: typeof value.description === 'string' ? value.description : undefined,
+        };
+    }
+
+    const required =
+        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
+            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
+            : undefined;
+
+    return {
+        name,
+        description: typeof description === 'string' ? description : '',
+        parameters: {
+            type: 'object',
+            properties: normalizedProperties,
+            required,
+            additionalProperties:
+                typeof parameterSchema.additionalProperties === 'boolean'
+                    ? parameterSchema.additionalProperties
+                    : undefined,
+        },
+    };
+}
+
+/**
+ * Parses open Ai tool choice.
+ */
+function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
+    if (value === undefined || value === null) {
+        return undefined;
+    }
+
+    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+}

file update
A C:/Users/me/work/ai/promptbook\apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
import type { string_book, TODO_any } from '@promptbook-local/types';
import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
import {
    createBookScopedAgentReferenceResolver,
    parseBookScopedAgentIdentifier,
} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
import { resolveMessageSuffixFromAgentSource } from '@/src/utils/chat/messageSuffix';
import { NextRequest, NextResponse } from 'next/server';
import { isAgentDeleted } from '../../app/agents/[agentName]/_utils';
import {
    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
    resolveAgentCollectionTablePrefix,
    waitForRunningAgentPreparation,
} from '../agentPreparation';
import { resolveCachedServerAgentContext, resolveCachedServerAgentModelRequirements } from '../cachedServerAgentRuntime';
import { AgentKitCacheManager } from '../cache/AgentKitCacheManager';
import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';

/**
 * Runtime state resolved before the chat model is executed.
 *
 * @private type of `handleChatCompletion`
 */
export type HandleChatCompletionRuntime = {
    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
    agent: Agent;
    agentSource: string_book;
    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
    messageSuffix: string | null;
    agentId: string;
    agentHash: string;
    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
    isPrivateModeEnabled: boolean;
};

/**
 * Resolves agent/runtime dependencies needed before prompt execution.
 *
 * @private function of `handleChatCompletion`
 */
export async function resolveHandleChatCompletionRuntime(options: {
    request: NextRequest;
    agentName: string;
    messages: ReadonlyArray<TODO_any>;
    isPrivateModeEnabled: boolean;
}): Promise<HandleChatCompletionRuntime | NextResponse> {
    const { request, agentName, messages, isPrivateModeEnabled } = options;
    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;

    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
        return createHandleChatCompletionErrorResponse(
            'This agent has been deleted. You can restore it from the Recycle Bin.',
            'agent_deleted',
            410,
        );
    }

    const [collection, baseAgentReferenceResolver] = await Promise.all([
        $provideAgentCollectionForServer(),
        $provideAgentReferenceResolver(),
    ]);
    const localServerUrl = new URL(request.url).origin;
    const resolvedAgentContext = await resolveCachedServerAgentContext({
        collection,
        agentIdentifier: agentName,
        localServerUrl,
        fallbackResolver: baseAgentReferenceResolver,
    }).catch(() => null);

    if (!resolvedAgentContext?.resolvedAgentSource) {
        return createHandleChatCompletionErrorResponse(`Agent '${agentName}' not found.`, 'invalid_request_error', 404);
    }

    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
    agentSource = agentSourceWithContext;

    const preparedAgentModelRequirements = !hasDynamicContext
        ? await resolveCachedServerAgentModelRequirements({
              resolvedAgentContext,
              localServerUrl,
              fallbackResolver: baseAgentReferenceResolver,
          })
        : null;
    const agentId = resolvedAgentContext.parentAgentPermanentId;
    const agentHash = computeAgentHash(agentSource);
    await waitForHandleChatCompletionAgentPreparation({
        collection,
        agentId,
        agentHash,
    });

    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
        await resolveHandleChatCompletionCredentials({
            currentUserIdentity,
            agentId,
            calendarConnections,
            useEmailConfiguration,
        });
    const agent = await createHandleChatCompletionAgent({
        agentName,
        agentSource,
        agentId,
        resolvedAgentContext,
        localServerUrl,
        baseAgentReferenceResolver,
        hasDynamicContext,
        preparedAgentModelRequirements,
    });

    return {
        collection,
        resolvedAgentContext,
        agent,
        agentSource,
        unresolvedAgentSource,
        messageSuffix,
        agentId,
        agentHash,
        projectRepositories,
        calendarConnections,
        useEmailConfiguration,
        currentUserIdentity,
        projectGithubToken,
        calendarGoogleAccessToken,
        emailSmtpCredential,
        isPrivateModeEnabled,
    };
}

/**
 * Appends system messages to the agent source as dynamic `CONTEXT`.
 */
function appendSystemMessagesToAgentSource(
    agentSource: string_book,
    messages: ReadonlyArray<TODO_any>,
): {
    agentSourceWithContext: string_book;
    hasDynamicContext: boolean;
} {
    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
    if (systemMessages.length === 0) {
        return {
            agentSourceWithContext: agentSource,
            hasDynamicContext: false,
        };
    }

    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
    return {
        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
        hasDynamicContext: true,
    };
}

/**
 * Waits for any running preparation job of the current agent before executing the chat call.
 */
async function waitForHandleChatCompletionAgentPreparation(options: {
    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
    agentId: string;
    agentHash: string;
}): Promise<void> {
    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
    const preparationWaitResult = await waitForRunningAgentPreparation({
        tablePrefix,
        agentPermanentId: options.agentId,
        fingerprint: options.agentHash,
        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
    });

    if (preparationWaitResult !== 'not_running') {
        console.info('[pre-index]', 'openai_chat_wait_result', {
            tablePrefix,
            agentPermanentId: options.agentId,
            agentHash: options.agentHash,
            preparationWaitResult,
        });
    }
}

/**
 * Resolves request-scoped project, calendar, and email credentials for the current agent.
 */
async function resolveHandleChatCompletionCredentials(options: {
    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
    agentId: string;
    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
}): Promise<{
    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
}> {
    const userId = options.currentUserIdentity?.userId;
    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
        resolveUseProjectGithubToken({
            userId,
            agentPermanentId: options.agentId,
        }),
        options.calendarConnections.length > 0
            ? resolveUseCalendarGoogleToken({
                  userId,
                  agentPermanentId: options.agentId,
              })
            : Promise.resolve(undefined),
        options.useEmailConfiguration.isEnabled
            ? resolveUseEmailSmtpCredential({
                  userId,
                  agentPermanentId: options.agentId,
              })
            : Promise.resolve(undefined),
    ]);

    return {
        projectGithubToken,
        calendarGoogleAccessToken,
        emailSmtpCredential,
    };
}

/**
 * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
 */
async function createHandleChatCompletionAgent(options: {
    agentName: string;
    agentSource: string_book;
    agentId: string;
    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
    localServerUrl: string;
    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
    hasDynamicContext: boolean;
    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
}): Promise<Agent> {
    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
    const runtimeAgentReferenceResolver = options.hasDynamicContext
        ? createBookScopedAgentReferenceResolver({
              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
              localServerUrl: options.localServerUrl,
              fallbackResolver: options.baseAgentReferenceResolver,
          })
        : undefined;
    const agentModelRequirements =
        options.preparedAgentModelRequirements?.modelRequirements ||
        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
            agentReferenceResolver: runtimeAgentReferenceResolver,
            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
        }));

    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
        options.agentSource,
        options.resolvedAgentContext.resolvedAgentName,
        await baseOpenAiToolsPromise,
        {
            includeDynamicContext: true,
            agentId: options.agentId,
            modelRequirements: agentModelRequirements,
        },
    );

    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
        agentName: options.agentName,
        assistantCacheKey: agentKitResult.assistantCacheKey,
        vectorStoreHash: agentKitResult.vectorStoreHash,
        vectorStoreId: agentKitResult.vectorStoreId,
    });

    return new Agent({
        agentSource: options.agentSource,
        executionTools: {
            llm: agentKitResult.tools,
        },
        precomputedModelRequirements: agentModelRequirements,
        assistantPreparationMode: 'external',
        isVerbose: true,
        teacherAgent: null,
    });
}
apply_patch(auto_approved=true) exited 0 in 138ms:
Success. Updated the following files:
A apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
file update:
diff --git a/apps/agents-server/src/utils/handleChatCompletion.ts b/apps/agents-server/src/utils/handleChatCompletion.ts
index 9c96d36fad9cf24bf26d89f57372332add4e4595..cff56fafd6cfa90bb4094f9f202c4f01f6f74af1
--- a/apps/agents-server/src/utils/handleChatCompletion.ts
+++ b/apps/agents-server/src/utils/handleChatCompletion.ts
@@ -1,392 +1,16 @@
-import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
-import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
-import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
-import {
-    createBookScopedAgentReferenceResolver,
-    parseBookScopedAgentIdentifier,
-} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
-import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
-import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
-import {
-    appendMessageSuffix,
-    createMessageSuffixAppendix,
-    emulateMessageSuffixStreaming,
-    resolveMessageSuffixFromAgentSource,
-} from '@/src/utils/chat/messageSuffix';
-import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
-import { prepareToolCallsForStreaming } from './toolCallStreaming';
-import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
-import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
-import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
-import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
-import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
-import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
-import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
-import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
-import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
-import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
-import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
-import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
-import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
-import type {
-    ChatMessage,
-    ChatPrompt,
-    LlmToolDefinition,
-    string_book,
-    ToolCall,
-    TODO_any,
-    UncertainNumber,
-    Usage,
-    UsageCounts,
-} from '@promptbook-local/types';
-import { $getCurrentDate } from '@promptbook-local/utils';
 import { NextRequest, NextResponse } from 'next/server';
-import type OpenAI from 'openai';
-import { computeUsageCounts } from '../../../../src/execution/utils/computeUsageCounts';
-import { encodeChatStreamWhitespaceForTransport } from '../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
-import { isAgentDeleted } from '../app/agents/[agentName]/_utils';
 import { HTTP_STATUS_CODES } from '../constants';
-import { AgentKitCacheManager } from './cache/AgentKitCacheManager';
 import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
+import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
+import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
+import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
+import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
+import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
 import { validateApiKey } from './validateApiKey';
-import {
-    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    resolveAgentCollectionTablePrefix,
-    waitForRunningAgentPreparation,
-} from './agentPreparation';
-import {
-    resolveCachedServerAgentContext,
-    resolveCachedServerAgentModelRequirements,
-} from './cachedServerAgentRuntime';
-import { resolveAppendOnlySelfLearningAgentSource } from './resolveAppendOnlySelfLearningAgentSource';
 
 /**
- * Falls back to the estimated value when the original token count is unknown.
- *
- * @param tokenCount - Token count reported by the execution tools.
- * @param fallbackValue - Estimated token count based on text length.
- * @returns Token count to report in the OpenAI-compatible response.
- */
-function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
-    if (tokenCount.value === 0 && tokenCount.isUncertain) {
-        return {
-            value: fallbackValue,
-            isUncertain: true,
-        };
-    }
-
-    return tokenCount;
-}
-
-/**
- * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
- *
- * @param promptContent - Prompt content used for the request.
- * @param completionContent - Assistant response content.
- * @param usage - Native Promptbook usage details.
- * @returns Usage payload for compatibility responses.
- */
-function createCompatibilityUsage(
-    promptContent: string,
-    completionContent: string,
-    usage: Usage,
-): {
-    prompt_tokens: number;
-    completion_tokens: number;
-    total_tokens: number;
-    details: Usage;
-} {
-    const promptCounts = computeUsageCounts(promptContent);
-    const completionCounts = computeUsageCounts(completionContent);
-    const inputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
-        ...promptCounts,
-    };
-    const outputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
-        ...completionCounts,
-    };
-
-    const promptTokens = inputUsage.tokensCount.value;
-    const completionTokens = outputUsage.tokensCount.value;
-
-    return {
-        prompt_tokens: promptTokens,
-        completion_tokens: completionTokens,
-        total_tokens: promptTokens + completionTokens,
-        details: {
-            price: usage.price,
-            duration: usage.duration,
-            input: inputUsage,
-            output: outputUsage,
-        },
-    };
-}
-
-/**
- * Definition of open AI chat tool.
- */
-type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
-
-/**
- * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
- */
-type OpenAiCompatibilityMessageLike = {
-    role?: unknown;
-    content?: unknown;
-};
-
-/**
- * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
- */
-type HandleChatCompletionParsedRequest = {
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    threadMessages: ReadonlyArray<TODO_any>;
-    stream: TODO_any;
-    model: TODO_any;
-    responseFormat: TODO_any;
-    runtimeTools?: Array<LlmToolDefinition>;
-    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-    incomingParameters: Record<string, unknown>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Runtime state resolved before the chat model is executed.
- */
-type HandleChatCompletionRuntime = {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    agent: Agent;
-    agentSource: string_book;
-    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
-    messageSuffix: string | null;
-    agentId: string;
-    agentHash: string;
-    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Prompt, history recorder, and persistence context created for one chat call.
- */
-type HandleChatCompletionPromptContext = {
-    prompt: ChatPrompt;
-    userMessageHash: string;
-    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
-    persistedFrozenChatId?: string;
-};
-
-/**
- * Handles convert open Ai tools.
- */
-function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
-    if (!Array.isArray(rawTools)) {
-        return undefined;
-    }
-
-    const converted = rawTools
-        .map((tool) => convertOpenAiTool(tool))
-        .filter((tool): tool is LlmToolDefinition => tool !== null);
-
-    return converted.length > 0 ? converted : undefined;
-}
-
-/**
- * Handles convert open Ai tool.
- */
-function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
-    const tool = rawTool as OpenAIChatToolDefinition;
-    if (!tool || tool.type !== 'function') {
-        return null;
-    }
-
-    const functionDefinition = tool.function;
-    if (!functionDefinition) {
-        return null;
-    }
-
-    const { name, description, parameters } = functionDefinition;
-    if (typeof name !== 'string' || name.trim().length === 0) {
-        return null;
-    }
-
-    const parameterSchema = parameters ?? {};
-    if (parameterSchema.type !== 'object') {
-        return null;
-    }
-
-    const properties = parameterSchema.properties ?? {};
-    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
-
-    for (const [key, value] of Object.entries(properties)) {
-        if (!value || typeof value !== 'object') {
-            continue;
-        }
-
-        normalizedProperties[key] = {
-            type: typeof value.type === 'string' ? value.type : 'string',
-            description: typeof value.description === 'string' ? value.description : undefined,
-        };
-    }
-
-    const required =
-        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
-            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
-            : undefined;
-
-    return {
-        name,
-        description: typeof description === 'string' ? description : '',
-        parameters: {
-            type: 'object',
-            properties: normalizedProperties,
-            required,
-            additionalProperties:
-                typeof parameterSchema.additionalProperties === 'boolean'
-                    ? parameterSchema.additionalProperties
-                    : undefined,
-        },
-    };
-}
-
-/**
- * Parses open Ai tool choice.
- */
-function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
-    if (value === undefined || value === null) {
-        return undefined;
-    }
-
-    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-}
-
-/**
- * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
- */
-function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
-    if (typeof value === 'string') {
-        return value;
-    }
-
-    if (Array.isArray(value)) {
-        return value
-            .map((part) => {
-                if (typeof part === 'string') {
-                    return part;
-                }
-
-                if (!part || typeof part !== 'object') {
-                    return '';
-                }
-
-                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
-                if (typeof partLike.text === 'string') {
-                    return partLike.text;
-                }
-
-                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
-                    return '[Image input]';
-                }
-
-                if (partLike.image_url) {
-                    return '[Image input]';
-                }
-
-                return '';
-            })
-            .filter(Boolean)
-            .join('\n');
-    }
-
-    if (value === null || value === undefined) {
-        return '';
-    }
-
-    try {
-        return JSON.stringify(value, null, 2);
-    } catch {
-        return String(value);
-    }
-}
-
-/**
- * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ * Handles convert open Ai chat completion requests routed through the Agents Server.
  */
-function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
-    if (typeof role !== 'string') {
-        return 'USER';
-    }
-
-    switch (role) {
-        case 'assistant':
-            return 'AGENT';
-        case 'system':
-            return 'SYSTEM';
-        case 'tool':
-            return 'TOOL';
-        case 'developer':
-            return 'SYSTEM';
-        default:
-            return 'USER';
-    }
-}
-
-/**
- * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
- */
-function createFrozenOpenAiCompatibilityMessages(
-    rawMessages: ReadonlyArray<unknown>,
-    assistantContent?: string,
-    options: {
-        includeAssistantPlaceholder?: boolean;
-    } = {},
-): Array<ChatMessage> {
-    const startedAt = Date.now();
-    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
-        const message = rawMessage as OpenAiCompatibilityMessageLike;
-
-        return {
-            id: `openai-frozen-${index}`,
-            sender: mapOpenAiCompatibilityRoleToSender(message.role),
-            content: normalizeOpenAiCompatibilityMessageContent(message.content),
-            isComplete: true,
-            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage;
-    });
-
-    if (assistantContent !== undefined) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: assistantContent,
-            isComplete: true,
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    } else if (options.includeAssistantPlaceholder) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: '',
-            isComplete: false,
-            lifecycleState: 'running',
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    }
-
-    return messages;
-}
-
-/**
- * Handles chat completion.
- */
 export async function handleChatCompletion(
     request: NextRequest,
     params: { agentName?: string },
@@ -399,7 +23,6 @@
         return versionMismatchResponse;
     }
 
-    // Validate API key explicitly (in addition to middleware)
     const apiKeyValidation = await validateApiKey(request);
     if (!apiKeyValidation.isValid) {
         return NextResponse.json(
@@ -462,724 +85,5 @@
             { error: { message: (error as Error).message || 'Internal Server Error', type: 'server_error' } },
             { status: HTTP_STATUS_CODES.INTERNAL_SERVER_ERROR },
         );
-    }
-}
-
-/**
- * Parses and validates the OpenAI-compatible request payload.
- */
-function parseHandleChatCompletionRequest(options: {
-    body: TODO_any;
-    agentNameFromParams?: string;
-    request: NextRequest;
-}): HandleChatCompletionParsedRequest | NextResponse {
-    const { body, agentNameFromParams, request } = options;
-    const {
-        messages,
-        stream,
-        model,
-        response_format: responseFormat,
-        tools: rawTools,
-        tool_choice: toolChoice,
-        parameters: rawParameters = {},
-    } = body;
-    const agentName = (agentNameFromParams || model) as string | undefined;
-    if (!agentName) {
-        return createHandleChatCompletionErrorResponse(
-            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    if (!messages || !Array.isArray(messages) || messages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array is required and cannot be empty.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
-    if (threadMessages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array must contain at least one non-system message.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    return {
-        agentName,
-        messages,
-        threadMessages,
-        stream,
-        model,
-        responseFormat,
-        runtimeTools: convertOpenAiTools(rawTools),
-        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
-        incomingParameters:
-            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
-                ? (rawParameters as Record<string, unknown>)
-                : {},
-        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
-    };
-}
-
-/**
- * Resolves agent/runtime dependencies needed before prompt execution.
- */
-async function resolveHandleChatCompletionRuntime(options: {
-    request: NextRequest;
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    isPrivateModeEnabled: boolean;
-}): Promise<HandleChatCompletionRuntime | NextResponse> {
-    const { request, agentName, messages, isPrivateModeEnabled } = options;
-    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
-    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
-
-    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
-        return createHandleChatCompletionErrorResponse(
-            'This agent has been deleted. You can restore it from the Recycle Bin.',
-            'agent_deleted',
-            410,
-        );
-    }
-
-    const [collection, baseAgentReferenceResolver] = await Promise.all([
-        $provideAgentCollectionForServer(),
-        $provideAgentReferenceResolver(),
-    ]);
-    const localServerUrl = new URL(request.url).origin;
-    const resolvedAgentContext = await resolveCachedServerAgentContext({
-        collection,
-        agentIdentifier: agentName,
-        localServerUrl,
-        fallbackResolver: baseAgentReferenceResolver,
-    }).catch(() => null);
-
-    if (!resolvedAgentContext?.resolvedAgentSource) {
-        return createHandleChatCompletionErrorResponse(
-            `Agent '${agentName}' not found.`,
-            'invalid_request_error',
-            HTTP_STATUS_CODES.NOT_FOUND,
-        );
-    }
-
-    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
-    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
-    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
-    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
-    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
-    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
-    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
-    agentSource = agentSourceWithContext;
-
-    const preparedAgentModelRequirements = !hasDynamicContext
-        ? await resolveCachedServerAgentModelRequirements({
-              resolvedAgentContext,
-              localServerUrl,
-              fallbackResolver: baseAgentReferenceResolver,
-          })
-        : null;
-    const agentId = resolvedAgentContext.parentAgentPermanentId;
-    const agentHash = computeAgentHash(agentSource);
-    await waitForHandleChatCompletionAgentPreparation({
-        collection,
-        agentId,
-        agentHash,
-    });
-
-    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
-    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
-        await resolveHandleChatCompletionCredentials({
-            currentUserIdentity,
-            agentId,
-            calendarConnections,
-            useEmailConfiguration,
-        });
-    const agent = await createHandleChatCompletionAgent({
-        agentName,
-        agentSource,
-        agentId,
-        resolvedAgentContext,
-        localServerUrl,
-        baseAgentReferenceResolver,
-        hasDynamicContext,
-        preparedAgentModelRequirements,
-    });
-
-    return {
-        collection,
-        resolvedAgentContext,
-        agent,
-        agentSource,
-        unresolvedAgentSource,
-        messageSuffix,
-        agentId,
-        agentHash,
-        projectRepositories,
-        calendarConnections,
-        useEmailConfiguration,
-        currentUserIdentity,
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-        isPrivateModeEnabled,
-    };
-}
-
-/**
- * Creates prompt and persistence helpers for one chat completion request.
- */
-async function createHandleChatCompletionPromptContext(options: {
-    request: NextRequest;
-    title: string;
-    apiKey: string | null;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-}): Promise<HandleChatCompletionPromptContext> {
-    const { request, title, apiKey, parsedRequest, runtime } = options;
-    const timezone = request.headers.get('x-timezone') || 'UTC';
-    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
-    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
-    const recordChatHistoryMessage = await createChatHistoryRecorder({
-        request,
-        agentIdentifier: runtime.agentId,
-        agentHash: runtime.agentHash,
-        source: 'OPENAI_API_COMPATIBILITY',
-        apiKey,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        isEnabled: !runtime.isPrivateModeEnabled,
-    });
-    const userMessageHash = await recordChatHistoryMessage({
-        message: {
-            role: 'USER',
-            content: lastMessage.content,
-        },
-        previousMessageHash: null,
-    });
-    const promptParameters = composePromptParametersWithMemoryContext({
-        baseParameters: {
-            ...parsedRequest.incomingParameters,
-            timezone,
-        },
-        currentUserIdentity: runtime.currentUserIdentity,
-        agentPermanentId: runtime.agentId,
-        agentName: runtime.resolvedAgentContext.resolvedAgentName,
-        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
-        projectRepositories: runtime.projectRepositories,
-        projectGithubToken: runtime.projectGithubToken,
-        emailSmtpCredential: runtime.emailSmtpCredential,
-        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
-        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
-        calendarConnections: runtime.calendarConnections,
-    });
-    const prompt: ChatPrompt = {
-        title,
-        content: lastMessage.content,
-        modelRequirements: {
-            modelVariant: 'CHAT',
-            responseFormat: parsedRequest.responseFormat,
-            toolChoice: parsedRequest.runtimeToolChoice,
-            // We could pass 'model' from body if we wanted to enforce it, but Agent usually has its own config
-        },
-        parameters: promptParameters,
-        thread,
-        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
-    };
-
-    return {
-        prompt,
-        userMessageHash,
-        recordChatHistoryMessage,
-        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
-            isEnabled: !runtime.isPrivateModeEnabled,
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentId: runtime.agentId,
-            messages: parsedRequest.messages,
-            includeAssistantPlaceholder: true,
-            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
-        }),
-    };
-}
-
-/**
- * Handles the streaming OpenAI-compatible response branch.
- */
-function createHandleChatCompletionStreamResponse(options: {
-    request: NextRequest;
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Response {
-    const { request, title, parsedRequest, runtime, promptContext } = options;
-    const encoder = new TextEncoder();
-    const responseModel = parsedRequest.model || 'promptbook-agent';
-    const readableStream = new ReadableStream({
-        async start(controller) {
-            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
-            const created = Math.floor(Date.now() / 1000);
-            const emitSsePayload = (payload: unknown) => {
-                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
-            };
-            const emitDeltaChunk = (deltaContent: string) => {
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {
-                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
-                        },
-                        finishReason: null,
-                    }),
-                );
-            };
-
-            emitSsePayload(
-                createHandleChatCompletionChunk({
-                    runId,
-                    created,
-                    model: responseModel,
-                    delta: {
-                        role: 'assistant',
-                        content: '',
-                    },
-                    finishReason: null,
-                }),
-            );
-
-            let hasMeaningfulDelta = false;
-
-            try {
-                const handleStreamChunk = createChatStreamHandler({
-                    onDelta: (deltaContent) => {
-                        if (deltaContent.trim().length > 0) {
-                            hasMeaningfulDelta = true;
-                        }
-                        emitDeltaChunk(deltaContent);
-                    },
-                    onToolCalls: (toolCalls) => {
-                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
-                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
-                    },
-                });
-                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
-                    signal: request.signal,
-                });
-                const normalizedResponse = ensureNonEmptyChatContent({
-                    content: result.content,
-                    context: title,
-                });
-
-                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
-                    emitDeltaChunk(normalizedResponse.content);
-                }
-
-                const messageSuffixAppendix = createMessageSuffixAppendix(
-                    normalizedResponse.content,
-                    runtime.messageSuffix,
-                );
-                if (messageSuffixAppendix) {
-                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
-                        emitDeltaChunk(delta);
-                    });
-                }
-
-                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-                await finalizeHandleChatCompletionResult({
-                    responseContentWithSuffix,
-                    usage: result.usage,
-                    toolCalls: result.toolCalls,
-                    parsedRequest,
-                    runtime,
-                    promptContext,
-                    // Preserve historical behavior: streamed OpenAI-compatible calls still persist learning in private mode.
-                    shouldPersistLearnedAgentSource: true,
-                });
-
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {},
-                        finishReason: 'stop',
-                    }),
-                );
-                controller.enqueue(encoder.encode('[DONE]'));
-            } catch (error) {
-                console.error('Error during streaming:', error);
-                controller.error(error);
-            }
-            controller.close();
-        },
-    });
-
-    return new Response(readableStream, {
-        headers: {
-            'Content-Type': 'text/event-stream',
-            'Cache-Control': 'no-cache',
-            Connection: 'keep-alive',
-        },
-    });
-}
-
-/**
- * Handles the non-streaming OpenAI-compatible response branch.
- */
-async function createHandleChatCompletionJsonResponse(options: {
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Promise<NextResponse> {
-    const { title, parsedRequest, runtime, promptContext } = options;
-    const result = await runtime.agent.callChatModel(promptContext.prompt);
-    const normalizedResponse = ensureNonEmptyChatContent({
-        content: result.content,
-        context: title,
-    });
-    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-
-    await finalizeHandleChatCompletionResult({
-        responseContentWithSuffix,
-        usage: result.usage,
-        toolCalls: result.toolCalls,
-        parsedRequest,
-        runtime,
-        promptContext,
-        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
-    });
-
-    return NextResponse.json({
-        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
-        object: 'chat.completion',
-        created: Math.floor(Date.now() / 1000),
-        model: parsedRequest.model || 'promptbook-agent',
-        choices: [
-            {
-                index: 0,
-                message: {
-                    role: 'assistant',
-                    content: responseContentWithSuffix,
-                },
-                finish_reason: 'stop',
-            },
-        ],
-        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
-    });
-}
-
-/**
- * Runs the shared side effects after one successful model response.
- */
-async function finalizeHandleChatCompletionResult(options: {
-    responseContentWithSuffix: string;
-    usage: Usage;
-    toolCalls?: ReadonlyArray<ToolCall>;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
-
-    await promptContext.recordChatHistoryMessage({
-        message: {
-            role: 'MODEL',
-            content: responseContentWithSuffix,
-        },
-        previousMessageHash: promptContext.userMessageHash,
-        usage,
-    });
-
-    if (toolCalls && toolCalls.length > 0) {
-        await logCalendarToolCallsActivity({
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentPermanentId: runtime.agentId,
-            toolCalls,
-        });
-    }
-
-    await persistOpenAiCompatibilityFrozenChat({
-        isEnabled: !runtime.isPrivateModeEnabled,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        agentId: runtime.agentId,
-        chatId: promptContext.persistedFrozenChatId,
-        messages: parsedRequest.messages,
-        assistantContent: responseContentWithSuffix,
-        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
-    });
-    await persistHandleChatCompletionLearnedAgentSource({
-        runtime,
-        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
-    });
-}
-
-/**
- * Applies append-only self-learning updates when the current branch should persist them.
- */
-async function persistHandleChatCompletionLearnedAgentSource(options: {
-    runtime: HandleChatCompletionRuntime;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { runtime, shouldPersistLearnedAgentSource } = options;
-    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
-        return;
-    }
-
-    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
-        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
-        resolvedAgentSourceBeforeLearning: runtime.agentSource,
-        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
-    });
-
-    if (learnedAgentSource !== null) {
-        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
     }
 }
-
-/**
- * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
- */
-async function persistOpenAiCompatibilityFrozenChat(options: {
-    isEnabled: boolean;
-    userId: number | null | undefined;
-    agentId: string;
-    messages: ReadonlyArray<unknown>;
-    chatId?: string;
-    assistantContent?: string;
-    includeAssistantPlaceholder?: boolean;
-    failureMessage: string;
-}): Promise<string | undefined> {
-    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
-        options;
-    if (!isEnabled || !userId) {
-        return undefined;
-    }
-
-    const persistedFrozenChat = await persistFrozenUserChat({
-        userId,
-        agentPermanentId: agentId,
-        source: USER_CHAT_SOURCES.OPENAI_API,
-        chatId,
-        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
-            includeAssistantPlaceholder,
-        }),
-    }).catch((error) => {
-        console.error(failureMessage, error);
-        return null;
-    });
-
-    return persistedFrozenChat?.id;
-}
-
-/**
- * Builds one OpenAI-compatible thread from the earlier request messages.
- */
-function createHandleChatCompletionThread(previousMessages: ReadonlyArray<TODO_any>): ChatMessage[] {
-    return previousMessages.map((message: TODO_any, index: number) => ({
-        id: `msg-${index}`,
-        sender: message.role === 'assistant' ? 'agent' : 'user',
-        content: message.content,
-        isComplete: true,
-        createdAt: $getCurrentDate(),
-    }));
-}
-
-/**
- * Appends system messages to the agent source as dynamic `CONTEXT`.
- */
-function appendSystemMessagesToAgentSource(
-    agentSource: string_book,
-    messages: ReadonlyArray<TODO_any>,
-): {
-    agentSourceWithContext: string_book;
-    hasDynamicContext: boolean;
-} {
-    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
-    if (systemMessages.length === 0) {
-        return {
-            agentSourceWithContext: agentSource,
-            hasDynamicContext: false,
-        };
-    }
-
-    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
-    return {
-        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
-        hasDynamicContext: true,
-    };
-}
-
-/**
- * Waits for any running preparation job of the current agent before executing the chat call.
- */
-async function waitForHandleChatCompletionAgentPreparation(options: {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    agentId: string;
-    agentHash: string;
-}): Promise<void> {
-    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
-    const preparationWaitResult = await waitForRunningAgentPreparation({
-        tablePrefix,
-        agentPermanentId: options.agentId,
-        fingerprint: options.agentHash,
-        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    });
-
-    if (preparationWaitResult !== 'not_running') {
-        console.info('[pre-index]', 'openai_chat_wait_result', {
-            tablePrefix,
-            agentPermanentId: options.agentId,
-            agentHash: options.agentHash,
-            preparationWaitResult,
-        });
-    }
-}
-
-/**
- * Resolves request-scoped project, calendar, and email credentials for the current agent.
- */
-async function resolveHandleChatCompletionCredentials(options: {
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    agentId: string;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-}): Promise<{
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-}> {
-    const userId = options.currentUserIdentity?.userId;
-    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
-        resolveUseProjectGithubToken({
-            userId,
-            agentPermanentId: options.agentId,
-        }),
-        options.calendarConnections.length > 0
-            ? resolveUseCalendarGoogleToken({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-        options.useEmailConfiguration.isEnabled
-            ? resolveUseEmailSmtpCredential({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-    ]);
-
-    return {
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-    };
-}
-
-/**
- * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
- */
-async function createHandleChatCompletionAgent(options: {
-    agentName: string;
-    agentSource: string_book;
-    agentId: string;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    localServerUrl: string;
-    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
-    hasDynamicContext: boolean;
-    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
-}): Promise<Agent> {
-    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
-    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
-    const runtimeAgentReferenceResolver = options.hasDynamicContext
-        ? createBookScopedAgentReferenceResolver({
-              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
-              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
-              localServerUrl: options.localServerUrl,
-              fallbackResolver: options.baseAgentReferenceResolver,
-          })
-        : undefined;
-    const agentModelRequirements =
-        options.preparedAgentModelRequirements?.modelRequirements ||
-        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
-            agentReferenceResolver: runtimeAgentReferenceResolver,
-            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
-        }));
-
-    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
-        options.agentSource,
-        options.resolvedAgentContext.resolvedAgentName,
-        await baseOpenAiToolsPromise,
-        {
-            includeDynamicContext: true,
-            agentId: options.agentId,
-            modelRequirements: agentModelRequirements,
-        },
-    );
-
-    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
-        agentName: options.agentName,
-        assistantCacheKey: agentKitResult.assistantCacheKey,
-        vectorStoreHash: agentKitResult.vectorStoreHash,
-        vectorStoreId: agentKitResult.vectorStoreId,
-    });
-
-    return new Agent({
-        agentSource: options.agentSource,
-        executionTools: {
-            llm: agentKitResult.tools,
-        },
-        precomputedModelRequirements: agentModelRequirements,
-        assistantPreparationMode: 'external',
-        isVerbose: true,
-        teacherAgent: null,
-    });
-}
-
-/**
- * Creates one OpenAI-compatible streaming chunk payload.
- */
-function createHandleChatCompletionChunk(options: {
-    runId: string;
-    created: number;
-    model: TODO_any;
-    delta: Record<string, unknown>;
-    finishReason: 'stop' | null;
-}) {
-    return {
-        id: options.runId,
-        object: 'chat.completion.chunk',
-        created: options.created,
-        model: options.model,
-        choices: [
-            {
-                index: 0,
-                delta: options.delta,
-                finish_reason: options.finishReason,
-            },
-        ],
-    };
-}
-
-/**
- * Creates one consistent OpenAI-compatible error response.
- */
-function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
-    return NextResponse.json(
-        {
-            error: {
-                message,
-                type,
-            },
-        },
-        { status },
-    );
-}
-
-// TODO: [🈹] Maybe move chat thread handling here
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f49a4fa80fa21f140e25de59596e5546d62e3f7
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
@@ -0,0 +1,18 @@
+import { NextResponse } from 'next/server';
+
+/**
+ * Creates one consistent OpenAI-compatible error response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
+    return NextResponse.json(
+        {
+            error: {
+                message,
+                type,
+            },
+        },
+        { status },
+    );
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
new file mode 100644
index 0000000000000000000000000000000000000000..58e1c26d7f428c1bdb39b36411b3f75256e3de51
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
@@ -0,0 +1,176 @@
+import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
+import type { LlmToolDefinition, TODO_any } from '@promptbook-local/types';
+import { NextRequest, NextResponse } from 'next/server';
+import type OpenAI from 'openai';
+import { HTTP_STATUS_CODES } from '../../constants';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Definition of open AI chat tool.
+ */
+type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
+
+/**
+ * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionParsedRequest = {
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    threadMessages: ReadonlyArray<TODO_any>;
+    stream: TODO_any;
+    model: TODO_any;
+    responseFormat: TODO_any;
+    runtimeTools?: Array<LlmToolDefinition>;
+    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+    incomingParameters: Record<string, unknown>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Parses and validates the OpenAI-compatible request payload.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function parseHandleChatCompletionRequest(options: {
+    body: TODO_any;
+    agentNameFromParams?: string;
+    request: NextRequest;
+}): HandleChatCompletionParsedRequest | NextResponse {
+    const { body, agentNameFromParams, request } = options;
+    const {
+        messages,
+        stream,
+        model,
+        response_format: responseFormat,
+        tools: rawTools,
+        tool_choice: toolChoice,
+        parameters: rawParameters = {},
+    } = body;
+    const agentName = (agentNameFromParams || model) as string | undefined;
+    if (!agentName) {
+        return createHandleChatCompletionErrorResponse(
+            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    if (!messages || !Array.isArray(messages) || messages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array is required and cannot be empty.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
+    if (threadMessages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array must contain at least one non-system message.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    return {
+        agentName,
+        messages,
+        threadMessages,
+        stream,
+        model,
+        responseFormat,
+        runtimeTools: convertOpenAiTools(rawTools),
+        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
+        incomingParameters:
+            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
+                ? (rawParameters as Record<string, unknown>)
+                : {},
+        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
+    };
+}
+
+/**
+ * Handles convert open Ai tools.
+ */
+function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
+    if (!Array.isArray(rawTools)) {
+        return undefined;
+    }
+
+    const converted = rawTools
+        .map((tool) => convertOpenAiTool(tool))
+        .filter((tool): tool is LlmToolDefinition => tool !== null);
+
+    return converted.length > 0 ? converted : undefined;
+}
+
+/**
+ * Handles convert open Ai tool.
+ */
+function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
+    const tool = rawTool as OpenAIChatToolDefinition;
+    if (!tool || tool.type !== 'function') {
+        return null;
+    }
+
+    const functionDefinition = tool.function;
+    if (!functionDefinition) {
+        return null;
+    }
+
+    const { name, description, parameters } = functionDefinition;
+    if (typeof name !== 'string' || name.trim().length === 0) {
+        return null;
+    }
+
+    const parameterSchema = parameters ?? {};
+    if (parameterSchema.type !== 'object') {
+        return null;
+    }
+
+    const properties = parameterSchema.properties ?? {};
+    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
+
+    for (const [key, value] of Object.entries(properties)) {
+        if (!value || typeof value !== 'object') {
+            continue;
+        }
+
+        normalizedProperties[key] = {
+            type: typeof value.type === 'string' ? value.type : 'string',
+            description: typeof value.description === 'string' ? value.description : undefined,
+        };
+    }
+
+    const required =
+        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
+            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
+            : undefined;
+
+    return {
+        name,
+        description: typeof description === 'string' ? description : '',
+        parameters: {
+            type: 'object',
+            properties: normalizedProperties,
+            required,
+            additionalProperties:
+                typeof parameterSchema.additionalProperties === 'boolean'
+                    ? parameterSchema.additionalProperties
+                    : undefined,
+        },
+    };
+}
+
+/**
+ * Parses open Ai tool choice.
+ */
+function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
+    if (value === undefined || value === null) {
+        return undefined;
+    }
+
+    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fd44b7ea5a87168725a9e99784038137c96537ab
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
@@ -0,0 +1,305 @@
+import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
+import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
+import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
+import type { string_book, TODO_any } from '@promptbook-local/types';
+import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
+import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
+import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
+import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
+import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
+import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
+import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
+import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
+import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
+import {
+    createBookScopedAgentReferenceResolver,
+    parseBookScopedAgentIdentifier,
+} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
+import { resolveMessageSuffixFromAgentSource } from '@/src/utils/chat/messageSuffix';
+import { NextRequest, NextResponse } from 'next/server';
+import { isAgentDeleted } from '../../app/agents/[agentName]/_utils';
+import {
+    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    resolveAgentCollectionTablePrefix,
+    waitForRunningAgentPreparation,
+} from '../agentPreparation';
+import { resolveCachedServerAgentContext, resolveCachedServerAgentModelRequirements } from '../cachedServerAgentRuntime';
+import { AgentKitCacheManager } from '../cache/AgentKitCacheManager';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Runtime state resolved before the chat model is executed.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionRuntime = {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    agent: Agent;
+    agentSource: string_book;
+    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
+    messageSuffix: string | null;
+    agentId: string;
+    agentHash: string;
+    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Resolves agent/runtime dependencies needed before prompt execution.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function resolveHandleChatCompletionRuntime(options: {
+    request: NextRequest;
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    isPrivateModeEnabled: boolean;
+}): Promise<HandleChatCompletionRuntime | NextResponse> {
+    const { request, agentName, messages, isPrivateModeEnabled } = options;
+    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
+    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
+
+    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
+        return createHandleChatCompletionErrorResponse(
+            'This agent has been deleted. You can restore it from the Recycle Bin.',
+            'agent_deleted',
+            410,
+        );
+    }
+
+    const [collection, baseAgentReferenceResolver] = await Promise.all([
+        $provideAgentCollectionForServer(),
+        $provideAgentReferenceResolver(),
+    ]);
+    const localServerUrl = new URL(request.url).origin;
+    const resolvedAgentContext = await resolveCachedServerAgentContext({
+        collection,
+        agentIdentifier: agentName,
+        localServerUrl,
+        fallbackResolver: baseAgentReferenceResolver,
+    }).catch(() => null);
+
+    if (!resolvedAgentContext?.resolvedAgentSource) {
+        return createHandleChatCompletionErrorResponse(`Agent '${agentName}' not found.`, 'invalid_request_error', 404);
+    }
+
+    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
+    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
+    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
+    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
+    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
+    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
+    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
+    agentSource = agentSourceWithContext;
+
+    const preparedAgentModelRequirements = !hasDynamicContext
+        ? await resolveCachedServerAgentModelRequirements({
+              resolvedAgentContext,
+              localServerUrl,
+              fallbackResolver: baseAgentReferenceResolver,
+          })
+        : null;
+    const agentId = resolvedAgentContext.parentAgentPermanentId;
+    const agentHash = computeAgentHash(agentSource);
+    await waitForHandleChatCompletionAgentPreparation({
+        collection,
+        agentId,
+        agentHash,
+    });
+
+    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
+    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
+        await resolveHandleChatCompletionCredentials({
+            currentUserIdentity,
+            agentId,
+            calendarConnections,
+            useEmailConfiguration,
+        });
+    const agent = await createHandleChatCompletionAgent({
+        agentName,
+        agentSource,
+        agentId,
+        resolvedAgentContext,
+        localServerUrl,
+        baseAgentReferenceResolver,
+        hasDynamicContext,
+        preparedAgentModelRequirements,
+    });
+
+    return {
+        collection,
+        resolvedAgentContext,
+        agent,
+        agentSource,
+        unresolvedAgentSource,
+        messageSuffix,
+        agentId,
+        agentHash,
+        projectRepositories,
+        calendarConnections,
+        useEmailConfiguration,
+        currentUserIdentity,
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+        isPrivateModeEnabled,
+    };
+}
+
+/**
+ * Appends system messages to the agent source as dynamic `CONTEXT`.
+ */
+function appendSystemMessagesToAgentSource(
+    agentSource: string_book,
+    messages: ReadonlyArray<TODO_any>,
+): {
+    agentSourceWithContext: string_book;
+    hasDynamicContext: boolean;
+} {
+    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
+    if (systemMessages.length === 0) {
+        return {
+            agentSourceWithContext: agentSource,
+            hasDynamicContext: false,
+        };
+    }
+
+    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
+    return {
+        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
+        hasDynamicContext: true,
+    };
+}
+
+/**
+ * Waits for any running preparation job of the current agent before executing the chat call.
+ */
+async function waitForHandleChatCompletionAgentPreparation(options: {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    agentId: string;
+    agentHash: string;
+}): Promise<void> {
+    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
+    const preparationWaitResult = await waitForRunningAgentPreparation({
+        tablePrefix,
+        agentPermanentId: options.agentId,
+        fingerprint: options.agentHash,
+        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    });
+
+    if (preparationWaitResult !== 'not_running') {
+        console.info('[pre-index]', 'openai_chat_wait_result', {
+            tablePrefix,
+            agentPermanentId: options.agentId,
+            agentHash: options.agentHash,
+            preparationWaitResult,
+        });
+    }
+}
+
+/**
+ * Resolves request-scoped project, calendar, and email credentials for the current agent.
+ */
+async function resolveHandleChatCompletionCredentials(options: {
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    agentId: string;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+}): Promise<{
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+}> {
+    const userId = options.currentUserIdentity?.userId;
+    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
+        resolveUseProjectGithubToken({
+            userId,
+            agentPermanentId: options.agentId,
+        }),
+        options.calendarConnections.length > 0
+            ? resolveUseCalendarGoogleToken({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+        options.useEmailConfiguration.isEnabled
+            ? resolveUseEmailSmtpCredential({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+    ]);
+
+    return {
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+    };
+}
+
+/**
+ * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
+ */
+async function createHandleChatCompletionAgent(options: {
+    agentName: string;
+    agentSource: string_book;
+    agentId: string;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    localServerUrl: string;
+    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
+    hasDynamicContext: boolean;
+    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
+}): Promise<Agent> {
+    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
+    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
+    const runtimeAgentReferenceResolver = options.hasDynamicContext
+        ? createBookScopedAgentReferenceResolver({
+              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
+              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
+              localServerUrl: options.localServerUrl,
+              fallbackResolver: options.baseAgentReferenceResolver,
+          })
+        : undefined;
+    const agentModelRequirements =
+        options.preparedAgentModelRequirements?.modelRequirements ||
+        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
+            agentReferenceResolver: runtimeAgentReferenceResolver,
+            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
+        }));
+
+    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
+        options.agentSource,
+        options.resolvedAgentContext.resolvedAgentName,
+        await baseOpenAiToolsPromise,
+        {
+            includeDynamicContext: true,
+            agentId: options.agentId,
+            modelRequirements: agentModelRequirements,
+        },
+    );
+
+    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
+        agentName: options.agentName,
+        assistantCacheKey: agentKitResult.assistantCacheKey,
+        vectorStoreHash: agentKitResult.vectorStoreHash,
+        vectorStoreId: agentKitResult.vectorStoreId,
+    });
+
+    return new Agent({
+        agentSource: options.agentSource,
+        executionTools: {
+            llm: agentKitResult.tools,
+        },
+        precomputedModelRequirements: agentModelRequirements,
+        assistantPreparationMode: 'external',
+        isVerbose: true,
+        teacherAgent: null,
+    });
+}

file update:
diff --git a/apps/agents-server/src/utils/handleChatCompletion.ts b/apps/agents-server/src/utils/handleChatCompletion.ts
index 9c96d36fad9cf24bf26d89f57372332add4e4595..cff56fafd6cfa90bb4094f9f202c4f01f6f74af1
--- a/apps/agents-server/src/utils/handleChatCompletion.ts
+++ b/apps/agents-server/src/utils/handleChatCompletion.ts
@@ -1,392 +1,16 @@
-import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
-import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
-import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
-import {
-    createBookScopedAgentReferenceResolver,
-    parseBookScopedAgentIdentifier,
-} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
-import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
-import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
-import {
-    appendMessageSuffix,
-    createMessageSuffixAppendix,
-    emulateMessageSuffixStreaming,
-    resolveMessageSuffixFromAgentSource,
-} from '@/src/utils/chat/messageSuffix';
-import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
-import { prepareToolCallsForStreaming } from './toolCallStreaming';
-import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
-import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
-import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
-import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
-import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
-import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
-import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
-import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
-import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
-import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
-import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
-import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
-import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
-import type {
-    ChatMessage,
-    ChatPrompt,
-    LlmToolDefinition,
-    string_book,
-    ToolCall,
-    TODO_any,
-    UncertainNumber,
-    Usage,
-    UsageCounts,
-} from '@promptbook-local/types';
-import { $getCurrentDate } from '@promptbook-local/utils';
 import { NextRequest, NextResponse } from 'next/server';
-import type OpenAI from 'openai';
-import { computeUsageCounts } from '../../../../src/execution/utils/computeUsageCounts';
-import { encodeChatStreamWhitespaceForTransport } from '../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
-import { isAgentDeleted } from '../app/agents/[agentName]/_utils';
 import { HTTP_STATUS_CODES } from '../constants';
-import { AgentKitCacheManager } from './cache/AgentKitCacheManager';
 import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
+import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
+import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
+import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
+import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
+import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
 import { validateApiKey } from './validateApiKey';
-import {
-    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    resolveAgentCollectionTablePrefix,
-    waitForRunningAgentPreparation,
-} from './agentPreparation';
-import {
-    resolveCachedServerAgentContext,
-    resolveCachedServerAgentModelRequirements,
-} from './cachedServerAgentRuntime';
-import { resolveAppendOnlySelfLearningAgentSource } from './resolveAppendOnlySelfLearningAgentSource';
 
 /**
- * Falls back to the estimated value when the original token count is unknown.
- *
- * @param tokenCount - Token count reported by the execution tools.
- * @param fallbackValue - Estimated token count based on text length.
- * @returns Token count to report in the OpenAI-compatible response.
- */
-function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
-    if (tokenCount.value === 0 && tokenCount.isUncertain) {
-        return {
-            value: fallbackValue,
-            isUncertain: true,
-        };
-    }
-
-    return tokenCount;
-}
-
-/**
- * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
- *
- * @param promptContent - Prompt content used for the request.
- * @param completionContent - Assistant response content.
- * @param usage - Native Promptbook usage details.
- * @returns Usage payload for compatibility responses.
- */
-function createCompatibilityUsage(
-    promptContent: string,
-    completionContent: string,
-    usage: Usage,
-): {
-    prompt_tokens: number;
-    completion_tokens: number;
-    total_tokens: number;
-    details: Usage;
-} {
-    const promptCounts = computeUsageCounts(promptContent);
-    const completionCounts = computeUsageCounts(completionContent);
-    const inputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
-        ...promptCounts,
-    };
-    const outputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
-        ...completionCounts,
-    };
-
-    const promptTokens = inputUsage.tokensCount.value;
-    const completionTokens = outputUsage.tokensCount.value;
-
-    return {
-        prompt_tokens: promptTokens,
-        completion_tokens: completionTokens,
-        total_tokens: promptTokens + completionTokens,
-        details: {
-            price: usage.price,
-            duration: usage.duration,
-            input: inputUsage,
-            output: outputUsage,
-        },
-    };
-}
-
-/**
- * Definition of open AI chat tool.
- */
-type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
-
-/**
- * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
- */
-type OpenAiCompatibilityMessageLike = {
-    role?: unknown;
-    content?: unknown;
-};
-
-/**
- * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
- */
-type HandleChatCompletionParsedRequest = {
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    threadMessages: ReadonlyArray<TODO_any>;
-    stream: TODO_any;
-    model: TODO_any;
-    responseFormat: TODO_any;
-    runtimeTools?: Array<LlmToolDefinition>;
-    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-    incomingParameters: Record<string, unknown>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Runtime state resolved before the chat model is executed.
- */
-type HandleChatCompletionRuntime = {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    agent: Agent;
-    agentSource: string_book;
-    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
-    messageSuffix: string | null;
-    agentId: string;
-    agentHash: string;
-    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Prompt, history recorder, and persistence context created for one chat call.
- */
-type HandleChatCompletionPromptContext = {
-    prompt: ChatPrompt;
-    userMessageHash: string;
-    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
-    persistedFrozenChatId?: string;
-};
-
-/**
- * Handles convert open Ai tools.
- */
-function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
-    if (!Array.isArray(rawTools)) {
-        return undefined;
-    }
-
-    const converted = rawTools
-        .map((tool) => convertOpenAiTool(tool))
-        .filter((tool): tool is LlmToolDefinition => tool !== null);
-
-    return converted.length > 0 ? converted : undefined;
-}
-
-/**
- * Handles convert open Ai tool.
- */
-function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
-    const tool = rawTool as OpenAIChatToolDefinition;
-    if (!tool || tool.type !== 'function') {
-        return null;
-    }
-
-    const functionDefinition = tool.function;
-    if (!functionDefinition) {
-        return null;
-    }
-
-    const { name, description, parameters } = functionDefinition;
-    if (typeof name !== 'string' || name.trim().length === 0) {
-        return null;
-    }
-
-    const parameterSchema = parameters ?? {};
-    if (parameterSchema.type !== 'object') {
-        return null;
-    }
-
-    const properties = parameterSchema.properties ?? {};
-    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
-
-    for (const [key, value] of Object.entries(properties)) {
-        if (!value || typeof value !== 'object') {
-            continue;
-        }
-
-        normalizedProperties[key] = {
-            type: typeof value.type === 'string' ? value.type : 'string',
-            description: typeof value.description === 'string' ? value.description : undefined,
-        };
-    }
-
-    const required =
-        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
-            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
-            : undefined;
-
-    return {
-        name,
-        description: typeof description === 'string' ? description : '',
-        parameters: {
-            type: 'object',
-            properties: normalizedProperties,
-            required,
-            additionalProperties:
-                typeof parameterSchema.additionalProperties === 'boolean'
-                    ? parameterSchema.additionalProperties
-                    : undefined,
-        },
-    };
-}
-
-/**
- * Parses open Ai tool choice.
- */
-function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
-    if (value === undefined || value === null) {
-        return undefined;
-    }
-
-    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-}
-
-/**
- * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
- */
-function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
-    if (typeof value === 'string') {
-        return value;
-    }
-
-    if (Array.isArray(value)) {
-        return value
-            .map((part) => {
-                if (typeof part === 'string') {
-                    return part;
-                }
-
-                if (!part || typeof part !== 'object') {
-                    return '';
-                }
-
-                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
-                if (typeof partLike.text === 'string') {
-                    return partLike.text;
-                }
-
-                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
-                    return '[Image input]';
-                }
-
-                if (partLike.image_url) {
-                    return '[Image input]';
-                }
-
-                return '';
-            })
-            .filter(Boolean)
-            .join('\n');
-    }
-
-    if (value === null || value === undefined) {
-        return '';
-    }
-
-    try {
-        return JSON.stringify(value, null, 2);
-    } catch {
-        return String(value);
-    }
-}
-
-/**
- * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ * Handles convert open Ai chat completion requests routed through the Agents Server.
  */
-function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
-    if (typeof role !== 'string') {
-        return 'USER';
-    }
-
-    switch (role) {
-        case 'assistant':
-            return 'AGENT';
-        case 'system':
-            return 'SYSTEM';
-        case 'tool':
-            return 'TOOL';
-        case 'developer':
-            return 'SYSTEM';
-        default:
-            return 'USER';
-    }
-}
-
-/**
- * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
- */
-function createFrozenOpenAiCompatibilityMessages(
-    rawMessages: ReadonlyArray<unknown>,
-    assistantContent?: string,
-    options: {
-        includeAssistantPlaceholder?: boolean;
-    } = {},
-): Array<ChatMessage> {
-    const startedAt = Date.now();
-    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
-        const message = rawMessage as OpenAiCompatibilityMessageLike;
-
-        return {
-            id: `openai-frozen-${index}`,
-            sender: mapOpenAiCompatibilityRoleToSender(message.role),
-            content: normalizeOpenAiCompatibilityMessageContent(message.content),
-            isComplete: true,
-            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage;
-    });
-
-    if (assistantContent !== undefined) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: assistantContent,
-            isComplete: true,
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    } else if (options.includeAssistantPlaceholder) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: '',
-            isComplete: false,
-            lifecycleState: 'running',
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    }
-
-    return messages;
-}
-
-/**
- * Handles chat completion.
- */
 export async function handleChatCompletion(
     request: NextRequest,
     params: { agentName?: string },
@@ -399,7 +23,6 @@
         return versionMismatchResponse;
     }
 
-    // Validate API key explicitly (in addition to middleware)
     const apiKeyValidation = await validateApiKey(request);
     if (!apiKeyValidation.isValid) {
         return NextResponse.json(
@@ -462,724 +85,5 @@
             { error: { message: (error as Error).message || 'Internal Server Error', type: 'server_error' } },
             { status: HTTP_STATUS_CODES.INTERNAL_SERVER_ERROR },
         );
-    }
-}
-
-/**
- * Parses and validates the OpenAI-compatible request payload.
- */
-function parseHandleChatCompletionRequest(options: {
-    body: TODO_any;
-    agentNameFromParams?: string;
-    request: NextRequest;
-}): HandleChatCompletionParsedRequest | NextResponse {
-    const { body, agentNameFromParams, request } = options;
-    const {
-        messages,
-        stream,
-        model,
-        response_format: responseFormat,
-        tools: rawTools,
-        tool_choice: toolChoice,
-        parameters: rawParameters = {},
-    } = body;
-    const agentName = (agentNameFromParams || model) as string | undefined;
-    if (!agentName) {
-        return createHandleChatCompletionErrorResponse(
-            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    if (!messages || !Array.isArray(messages) || messages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array is required and cannot be empty.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
-    if (threadMessages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array must contain at least one non-system message.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    return {
-        agentName,
-        messages,
-        threadMessages,
-        stream,
-        model,
-        responseFormat,
-        runtimeTools: convertOpenAiTools(rawTools),
-        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
-        incomingParameters:
-            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
-                ? (rawParameters as Record<string, unknown>)
-                : {},
-        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
-    };
-}
-
-/**
- * Resolves agent/runtime dependencies needed before prompt execution.
- */
-async function resolveHandleChatCompletionRuntime(options: {
-    request: NextRequest;
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    isPrivateModeEnabled: boolean;
-}): Promise<HandleChatCompletionRuntime | NextResponse> {
-    const { request, agentName, messages, isPrivateModeEnabled } = options;
-    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
-    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
-
-    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
-        return createHandleChatCompletionErrorResponse(
-            'This agent has been deleted. You can restore it from the Recycle Bin.',
-            'agent_deleted',
-            410,
-        );
-    }
-
-    const [collection, baseAgentReferenceResolver] = await Promise.all([
-        $provideAgentCollectionForServer(),
-        $provideAgentReferenceResolver(),
-    ]);
-    const localServerUrl = new URL(request.url).origin;
-    const resolvedAgentContext = await resolveCachedServerAgentContext({
-        collection,
-        agentIdentifier: agentName,
-        localServerUrl,
-        fallbackResolver: baseAgentReferenceResolver,
-    }).catch(() => null);
-
-    if (!resolvedAgentContext?.resolvedAgentSource) {
-        return createHandleChatCompletionErrorResponse(
-            `Agent '${agentName}' not found.`,
-            'invalid_request_error',
-            HTTP_STATUS_CODES.NOT_FOUND,
-        );
-    }
-
-    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
-    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
-    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
-    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
-    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
-    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
-    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
-    agentSource = agentSourceWithContext;
-
-    const preparedAgentModelRequirements = !hasDynamicContext
-        ? await resolveCachedServerAgentModelRequirements({
-              resolvedAgentContext,
-              localServerUrl,
-              fallbackResolver: baseAgentReferenceResolver,
-          })
-        : null;
-    const agentId = resolvedAgentContext.parentAgentPermanentId;
-    const agentHash = computeAgentHash(agentSource);
-    await waitForHandleChatCompletionAgentPreparation({
-        collection,
-        agentId,
-        agentHash,
-    });
-
-    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
-    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
-        await resolveHandleChatCompletionCredentials({
-            currentUserIdentity,
-            agentId,
-            calendarConnections,
-            useEmailConfiguration,
-        });
-    const agent = await createHandleChatCompletionAgent({
-        agentName,
-        agentSource,
-        agentId,
-        resolvedAgentContext,
-        localServerUrl,
-        baseAgentReferenceResolver,
-        hasDynamicContext,
-        preparedAgentModelRequirements,
-    });
-
-    return {
-        collection,
-        resolvedAgentContext,
-        agent,
-        agentSource,
-        unresolvedAgentSource,
-        messageSuffix,
-        agentId,
-        agentHash,
-        projectRepositories,
-        calendarConnections,
-        useEmailConfiguration,
-        currentUserIdentity,
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-        isPrivateModeEnabled,
-    };
-}
-
-/**
- * Creates prompt and persistence helpers for one chat completion request.
- */
-async function createHandleChatCompletionPromptContext(options: {
-    request: NextRequest;
-    title: string;
-    apiKey: string | null;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-}): Promise<HandleChatCompletionPromptContext> {
-    const { request, title, apiKey, parsedRequest, runtime } = options;
-    const timezone = request.headers.get('x-timezone') || 'UTC';
-    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
-    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
-    const recordChatHistoryMessage = await createChatHistoryRecorder({
-        request,
-        agentIdentifier: runtime.agentId,
-        agentHash: runtime.agentHash,
-        source: 'OPENAI_API_COMPATIBILITY',
-        apiKey,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        isEnabled: !runtime.isPrivateModeEnabled,
-    });
-    const userMessageHash = await recordChatHistoryMessage({
-        message: {
-            role: 'USER',
-            content: lastMessage.content,
-        },
-        previousMessageHash: null,
-    });
-    const promptParameters = composePromptParametersWithMemoryContext({
-        baseParameters: {
-            ...parsedRequest.incomingParameters,
-            timezone,
-        },
-        currentUserIdentity: runtime.currentUserIdentity,
-        agentPermanentId: runtime.agentId,
-        agentName: runtime.resolvedAgentContext.resolvedAgentName,
-        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
-        projectRepositories: runtime.projectRepositories,
-        projectGithubToken: runtime.projectGithubToken,
-        emailSmtpCredential: runtime.emailSmtpCredential,
-        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
-        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
-        calendarConnections: runtime.calendarConnections,
-    });
-    const prompt: ChatPrompt = {
-        title,
-        content: lastMessage.content,
-        modelRequirements: {
-            modelVariant: 'CHAT',
-            responseFormat: parsedRequest.responseFormat,
-            toolChoice: parsedRequest.runtimeToolChoice,
-            // We could pass 'model' from body if we wanted to enforce it, but Agent usually has its own config
-        },
-        parameters: promptParameters,
-        thread,
-        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
-    };
-
-    return {
-        prompt,
-        userMessageHash,
-        recordChatHistoryMessage,
-        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
-            isEnabled: !runtime.isPrivateModeEnabled,
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentId: runtime.agentId,
-            messages: parsedRequest.messages,
-            includeAssistantPlaceholder: true,
-            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
-        }),
-    };
-}
-
-/**
- * Handles the streaming OpenAI-compatible response branch.
- */
-function createHandleChatCompletionStreamResponse(options: {
-    request: NextRequest;
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Response {
-    const { request, title, parsedRequest, runtime, promptContext } = options;
-    const encoder = new TextEncoder();
-    const responseModel = parsedRequest.model || 'promptbook-agent';
-    const readableStream = new ReadableStream({
-        async start(controller) {
-            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
-            const created = Math.floor(Date.now() / 1000);
-            const emitSsePayload = (payload: unknown) => {
-                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
-            };
-            const emitDeltaChunk = (deltaContent: string) => {
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {
-                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
-                        },
-                        finishReason: null,
-                    }),
-                );
-            };
-
-            emitSsePayload(
-                createHandleChatCompletionChunk({
-                    runId,
-                    created,
-                    model: responseModel,
-                    delta: {
-                        role: 'assistant',
-                        content: '',
-                    },
-                    finishReason: null,
-                }),
-            );
-
-            let hasMeaningfulDelta = false;
-
-            try {
-                const handleStreamChunk = createChatStreamHandler({
-                    onDelta: (deltaContent) => {
-                        if (deltaContent.trim().length > 0) {
-                            hasMeaningfulDelta = true;
-                        }
-                        emitDeltaChunk(deltaContent);
-                    },
-                    onToolCalls: (toolCalls) => {
-                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
-                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
-                    },
-                });
-                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
-                    signal: request.signal,
-                });
-                const normalizedResponse = ensureNonEmptyChatContent({
-                    content: result.content,
-                    context: title,
-                });
-
-                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
-                    emitDeltaChunk(normalizedResponse.content);
-                }
-
-                const messageSuffixAppendix = createMessageSuffixAppendix(
-                    normalizedResponse.content,
-                    runtime.messageSuffix,
-                );
-                if (messageSuffixAppendix) {
-                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
-                        emitDeltaChunk(delta);
-                    });
-                }
-
-                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-                await finalizeHandleChatCompletionResult({
-                    responseContentWithSuffix,
-                    usage: result.usage,
-                    toolCalls: result.toolCalls,
-                    parsedRequest,
-                    runtime,
-                    promptContext,
-                    // Preserve historical behavior: streamed OpenAI-compatible calls still persist learning in private mode.
-                    shouldPersistLearnedAgentSource: true,
-                });
-
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {},
-                        finishReason: 'stop',
-                    }),
-                );
-                controller.enqueue(encoder.encode('[DONE]'));
-            } catch (error) {
-                console.error('Error during streaming:', error);
-                controller.error(error);
-            }
-            controller.close();
-        },
-    });
-
-    return new Response(readableStream, {
-        headers: {
-            'Content-Type': 'text/event-stream',
-            'Cache-Control': 'no-cache',
-            Connection: 'keep-alive',
-        },
-    });
-}
-
-/**
- * Handles the non-streaming OpenAI-compatible response branch.
- */
-async function createHandleChatCompletionJsonResponse(options: {
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Promise<NextResponse> {
-    const { title, parsedRequest, runtime, promptContext } = options;
-    const result = await runtime.agent.callChatModel(promptContext.prompt);
-    const normalizedResponse = ensureNonEmptyChatContent({
-        content: result.content,
-        context: title,
-    });
-    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-
-    await finalizeHandleChatCompletionResult({
-        responseContentWithSuffix,
-        usage: result.usage,
-        toolCalls: result.toolCalls,
-        parsedRequest,
-        runtime,
-        promptContext,
-        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
-    });
-
-    return NextResponse.json({
-        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
-        object: 'chat.completion',
-        created: Math.floor(Date.now() / 1000),
-        model: parsedRequest.model || 'promptbook-agent',
-        choices: [
-            {
-                index: 0,
-                message: {
-                    role: 'assistant',
-                    content: responseContentWithSuffix,
-                },
-                finish_reason: 'stop',
-            },
-        ],
-        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
-    });
-}
-
-/**
- * Runs the shared side effects after one successful model response.
- */
-async function finalizeHandleChatCompletionResult(options: {
-    responseContentWithSuffix: string;
-    usage: Usage;
-    toolCalls?: ReadonlyArray<ToolCall>;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
-
-    await promptContext.recordChatHistoryMessage({
-        message: {
-            role: 'MODEL',
-            content: responseContentWithSuffix,
-        },
-        previousMessageHash: promptContext.userMessageHash,
-        usage,
-    });
-
-    if (toolCalls && toolCalls.length > 0) {
-        await logCalendarToolCallsActivity({
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentPermanentId: runtime.agentId,
-            toolCalls,
-        });
-    }
-
-    await persistOpenAiCompatibilityFrozenChat({
-        isEnabled: !runtime.isPrivateModeEnabled,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        agentId: runtime.agentId,
-        chatId: promptContext.persistedFrozenChatId,
-        messages: parsedRequest.messages,
-        assistantContent: responseContentWithSuffix,
-        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
-    });
-    await persistHandleChatCompletionLearnedAgentSource({
-        runtime,
-        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
-    });
-}
-
-/**
- * Applies append-only self-learning updates when the current branch should persist them.
- */
-async function persistHandleChatCompletionLearnedAgentSource(options: {
-    runtime: HandleChatCompletionRuntime;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { runtime, shouldPersistLearnedAgentSource } = options;
-    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
-        return;
-    }
-
-    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
-        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
-        resolvedAgentSourceBeforeLearning: runtime.agentSource,
-        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
-    });
-
-    if (learnedAgentSource !== null) {
-        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
     }
 }
-
-/**
- * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
- */
-async function persistOpenAiCompatibilityFrozenChat(options: {
-    isEnabled: boolean;
-    userId: number | null | undefined;
-    agentId: string;
-    messages: ReadonlyArray<unknown>;
-    chatId?: string;
-    assistantContent?: string;
-    includeAssistantPlaceholder?: boolean;
-    failureMessage: string;
-}): Promise<string | undefined> {
-    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
-        options;
-    if (!isEnabled || !userId) {
-        return undefined;
-    }
-
-    const persistedFrozenChat = await persistFrozenUserChat({
-        userId,
-        agentPermanentId: agentId,
-        source: USER_CHAT_SOURCES.OPENAI_API,
-        chatId,
-        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
-            includeAssistantPlaceholder,
-        }),
-    }).catch((error) => {
-        console.error(failureMessage, error);
-        return null;
-    });
-
-    return persistedFrozenChat?.id;
-}
-
-/**
- * Builds one OpenAI-compatible thread from the earlier request messages.
- */
-function createHandleChatCompletionThread(previousMessages: ReadonlyArray<TODO_any>): ChatMessage[] {
-    return previousMessages.map((message: TODO_any, index: number) => ({
-        id: `msg-${index}`,
-        sender: message.role === 'assistant' ? 'agent' : 'user',
-        content: message.content,
-        isComplete: true,
-        createdAt: $getCurrentDate(),
-    }));
-}
-
-/**
- * Appends system messages to the agent source as dynamic `CONTEXT`.
- */
-function appendSystemMessagesToAgentSource(
-    agentSource: string_book,
-    messages: ReadonlyArray<TODO_any>,
-): {
-    agentSourceWithContext: string_book;
-    hasDynamicContext: boolean;
-} {
-    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
-    if (systemMessages.length === 0) {
-        return {
-            agentSourceWithContext: agentSource,
-            hasDynamicContext: false,
-        };
-    }
-
-    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
-    return {
-        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
-        hasDynamicContext: true,
-    };
-}
-
-/**
- * Waits for any running preparation job of the current agent before executing the chat call.
- */
-async function waitForHandleChatCompletionAgentPreparation(options: {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    agentId: string;
-    agentHash: string;
-}): Promise<void> {
-    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
-    const preparationWaitResult = await waitForRunningAgentPreparation({
-        tablePrefix,
-        agentPermanentId: options.agentId,
-        fingerprint: options.agentHash,
-        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    });
-
-    if (preparationWaitResult !== 'not_running') {
-        console.info('[pre-index]', 'openai_chat_wait_result', {
-            tablePrefix,
-            agentPermanentId: options.agentId,
-            agentHash: options.agentHash,
-            preparationWaitResult,
-        });
-    }
-}
-
-/**
- * Resolves request-scoped project, calendar, and email credentials for the current agent.
- */
-async function resolveHandleChatCompletionCredentials(options: {
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    agentId: string;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-}): Promise<{
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-}> {
-    const userId = options.currentUserIdentity?.userId;
-    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
-        resolveUseProjectGithubToken({
-            userId,
-            agentPermanentId: options.agentId,
-        }),
-        options.calendarConnections.length > 0
-            ? resolveUseCalendarGoogleToken({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-        options.useEmailConfiguration.isEnabled
-            ? resolveUseEmailSmtpCredential({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-    ]);
-
-    return {
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-    };
-}
-
-/**
- * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
- */
-async function createHandleChatCompletionAgent(options: {
-    agentName: string;
-    agentSource: string_book;
-    agentId: string;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    localServerUrl: string;
-    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
-    hasDynamicContext: boolean;
-    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
-}): Promise<Agent> {
-    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
-    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
-    const runtimeAgentReferenceResolver = options.hasDynamicContext
-        ? createBookScopedAgentReferenceResolver({
-              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
-              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
-              localServerUrl: options.localServerUrl,
-              fallbackResolver: options.baseAgentReferenceResolver,
-          })
-        : undefined;
-    const agentModelRequirements =
-        options.preparedAgentModelRequirements?.modelRequirements ||
-        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
-            agentReferenceResolver: runtimeAgentReferenceResolver,
-            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
-        }));
-
-    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
-        options.agentSource,
-        options.resolvedAgentContext.resolvedAgentName,
-        await baseOpenAiToolsPromise,
-        {
-            includeDynamicContext: true,
-            agentId: options.agentId,
-            modelRequirements: agentModelRequirements,
-        },
-    );
-
-    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
-        agentName: options.agentName,
-        assistantCacheKey: agentKitResult.assistantCacheKey,
-        vectorStoreHash: agentKitResult.vectorStoreHash,
-        vectorStoreId: agentKitResult.vectorStoreId,
-    });
-
-    return new Agent({
-        agentSource: options.agentSource,
-        executionTools: {
-            llm: agentKitResult.tools,
-        },
-        precomputedModelRequirements: agentModelRequirements,
-        assistantPreparationMode: 'external',
-        isVerbose: true,
-        teacherAgent: null,
-    });
-}
-
-/**
- * Creates one OpenAI-compatible streaming chunk payload.
- */
-function createHandleChatCompletionChunk(options: {
-    runId: string;
-    created: number;
-    model: TODO_any;
-    delta: Record<string, unknown>;
-    finishReason: 'stop' | null;
-}) {
-    return {
-        id: options.runId,
-        object: 'chat.completion.chunk',
-        created: options.created,
-        model: options.model,
-        choices: [
-            {
-                index: 0,
-                delta: options.delta,
-                finish_reason: options.finishReason,
-            },
-        ],
-    };
-}
-
-/**
- * Creates one consistent OpenAI-compatible error response.
- */
-function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
-    return NextResponse.json(
-        {
-            error: {
-                message,
-                type,
-            },
-        },
-        { status },
-    );
-}
-
-// TODO: [🈹] Maybe move chat thread handling here
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f49a4fa80fa21f140e25de59596e5546d62e3f7
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
@@ -0,0 +1,18 @@
+import { NextResponse } from 'next/server';
+
+/**
+ * Creates one consistent OpenAI-compatible error response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
+    return NextResponse.json(
+        {
+            error: {
+                message,
+                type,
+            },
+        },
+        { status },
+    );
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
new file mode 100644
index 0000000000000000000000000000000000000000..58e1c26d7f428c1bdb39b36411b3f75256e3de51
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
@@ -0,0 +1,176 @@
+import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
+import type { LlmToolDefinition, TODO_any } from '@promptbook-local/types';
+import { NextRequest, NextResponse } from 'next/server';
+import type OpenAI from 'openai';
+import { HTTP_STATUS_CODES } from '../../constants';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Definition of open AI chat tool.
+ */
+type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
+
+/**
+ * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionParsedRequest = {
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    threadMessages: ReadonlyArray<TODO_any>;
+    stream: TODO_any;
+    model: TODO_any;
+    responseFormat: TODO_any;
+    runtimeTools?: Array<LlmToolDefinition>;
+    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+    incomingParameters: Record<string, unknown>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Parses and validates the OpenAI-compatible request payload.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function parseHandleChatCompletionRequest(options: {
+    body: TODO_any;
+    agentNameFromParams?: string;
+    request: NextRequest;
+}): HandleChatCompletionParsedRequest | NextResponse {
+    const { body, agentNameFromParams, request } = options;
+    const {
+        messages,
+        stream,
+        model,
+        response_format: responseFormat,
+        tools: rawTools,
+        tool_choice: toolChoice,
+        parameters: rawParameters = {},
+    } = body;
+    const agentName = (agentNameFromParams || model) as string | undefined;
+    if (!agentName) {
+        return createHandleChatCompletionErrorResponse(
+            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    if (!messages || !Array.isArray(messages) || messages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array is required and cannot be empty.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
+    if (threadMessages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array must contain at least one non-system message.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    return {
+        agentName,
+        messages,
+        threadMessages,
+        stream,
+        model,
+        responseFormat,
+        runtimeTools: convertOpenAiTools(rawTools),
+        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
+        incomingParameters:
+            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
+                ? (rawParameters as Record<string, unknown>)
+                : {},
+        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
+    };
+}
+
+/**
+ * Handles convert open Ai tools.
+ */
+function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
+    if (!Array.isArray(rawTools)) {
+        return undefined;
+    }
+
+    const converted = rawTools
+        .map((tool) => convertOpenAiTool(tool))
+        .filter((tool): tool is LlmToolDefinition => tool !== null);
+
+    return converted.length > 0 ? converted : undefined;
+}
+
+/**
+ * Handles convert open Ai tool.
+ */
+function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
+    const tool = rawTool as OpenAIChatToolDefinition;
+    if (!tool || tool.type !== 'function') {
+        return null;
+    }
+
+    const functionDefinition = tool.function;
+    if (!functionDefinition) {
+        return null;
+    }
+
+    const { name, description, parameters } = functionDefinition;
+    if (typeof name !== 'string' || name.trim().length === 0) {
+        return null;
+    }
+
+    const parameterSchema = parameters ?? {};
+    if (parameterSchema.type !== 'object') {
+        return null;
+    }
+
+    const properties = parameterSchema.properties ?? {};
+    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
+
+    for (const [key, value] of Object.entries(properties)) {
+        if (!value || typeof value !== 'object') {
+            continue;
+        }
+
+        normalizedProperties[key] = {
+            type: typeof value.type === 'string' ? value.type : 'string',
+            description: typeof value.description === 'string' ? value.description : undefined,
+        };
+    }
+
+    const required =
+        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
+            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
+            : undefined;
+
+    return {
+        name,
+        description: typeof description === 'string' ? description : '',
+        parameters: {
+            type: 'object',
+            properties: normalizedProperties,
+            required,
+            additionalProperties:
+                typeof parameterSchema.additionalProperties === 'boolean'
+                    ? parameterSchema.additionalProperties
+                    : undefined,
+        },
+    };
+}
+
+/**
+ * Parses open Ai tool choice.
+ */
+function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
+    if (value === undefined || value === null) {
+        return undefined;
+    }
+
+    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fd44b7ea5a87168725a9e99784038137c96537ab
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
@@ -0,0 +1,305 @@
+import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
+import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
+import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
+import type { string_book, TODO_any } from '@promptbook-local/types';
+import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
+import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
+import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
+import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
+import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
+import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
+import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
+import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
+import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
+import {
+    createBookScopedAgentReferenceResolver,
+    parseBookScopedAgentIdentifier,
+} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
+import { resolveMessageSuffixFromAgentSource } from '@/src/utils/chat/messageSuffix';
+import { NextRequest, NextResponse } from 'next/server';
+import { isAgentDeleted } from '../../app/agents/[agentName]/_utils';
+import {
+    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    resolveAgentCollectionTablePrefix,
+    waitForRunningAgentPreparation,
+} from '../agentPreparation';
+import { resolveCachedServerAgentContext, resolveCachedServerAgentModelRequirements } from '../cachedServerAgentRuntime';
+import { AgentKitCacheManager } from '../cache/AgentKitCacheManager';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Runtime state resolved before the chat model is executed.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionRuntime = {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    agent: Agent;
+    agentSource: string_book;
+    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
+    messageSuffix: string | null;
+    agentId: string;
+    agentHash: string;
+    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Resolves agent/runtime dependencies needed before prompt execution.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function resolveHandleChatCompletionRuntime(options: {
+    request: NextRequest;
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    isPrivateModeEnabled: boolean;
+}): Promise<HandleChatCompletionRuntime | NextResponse> {
+    const { request, agentName, messages, isPrivateModeEnabled } = options;
+    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
+    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
+
+    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
+        return createHandleChatCompletionErrorResponse(
+            'This agent has been deleted. You can restore it from the Recycle Bin.',
+            'agent_deleted',
+            410,
+        );
+    }
+
+    const [collection, baseAgentReferenceResolver] = await Promise.all([
+        $provideAgentCollectionForServer(),
+        $provideAgentReferenceResolver(),
+    ]);
+    const localServerUrl = new URL(request.url).origin;
+    const resolvedAgentContext = await resolveCachedServerAgentContext({
+        collection,
+        agentIdentifier: agentName,
+        localServerUrl,
+        fallbackResolver: baseAgentReferenceResolver,
+    }).catch(() => null);
+
+    if (!resolvedAgentContext?.resolvedAgentSource) {
+        return createHandleChatCompletionErrorResponse(`Agent '${agentName}' not found.`, 'invalid_request_error', 404);
+    }
+
+    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
+    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
+    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
+    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
+    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
+    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
+    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
+    agentSource = agentSourceWithContext;
+
+    const preparedAgentModelRequirements = !hasDynamicContext
+        ? await resolveCachedServerAgentModelRequirements({
+              resolvedAgentContext,
+              localServerUrl,
+              fallbackResolver: baseAgentReferenceResolver,
+          })
+        : null;
+    const agentId = resolvedAgentContext.parentAgentPermanentId;
+    const agentHash = computeAgentHash(agentSource);
+    await waitForHandleChatCompletionAgentPreparation({
+        collection,
+        agentId,
+        agentHash,
+    });
+
+    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
+    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
+        await resolveHandleChatCompletionCredentials({
+            currentUserIdentity,
+            agentId,
+            calendarConnections,
+            useEmailConfiguration,
+        });
+    const agent = await createHandleChatCompletionAgent({
+        agentName,
+        agentSource,
+        agentId,
+        resolvedAgentContext,
+        localServerUrl,
+        baseAgentReferenceResolver,
+        hasDynamicContext,
+        preparedAgentModelRequirements,
+    });
+
+    return {
+        collection,
+        resolvedAgentContext,
+        agent,
+        agentSource,
+        unresolvedAgentSource,
+        messageSuffix,
+        agentId,
+        agentHash,
+        projectRepositories,
+        calendarConnections,
+        useEmailConfiguration,
+        currentUserIdentity,
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+        isPrivateModeEnabled,
+    };
+}
+
+/**
+ * Appends system messages to the agent source as dynamic `CONTEXT`.
+ */
+function appendSystemMessagesToAgentSource(
+    agentSource: string_book,
+    messages: ReadonlyArray<TODO_any>,
+): {
+    agentSourceWithContext: string_book;
+    hasDynamicContext: boolean;
+} {
+    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
+    if (systemMessages.length === 0) {
+        return {
+            agentSourceWithContext: agentSource,
+            hasDynamicContext: false,
+        };
+    }
+
+    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
+    return {
+        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
+        hasDynamicContext: true,
+    };
+}
+
+/**
+ * Waits for any running preparation job of the current agent before executing the chat call.
+ */
+async function waitForHandleChatCompletionAgentPreparation(options: {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    agentId: string;
+    agentHash: string;
+}): Promise<void> {
+    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
+    const preparationWaitResult = await waitForRunningAgentPreparation({
+        tablePrefix,
+        agentPermanentId: options.agentId,
+        fingerprint: options.agentHash,
+        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    });
+
+    if (preparationWaitResult !== 'not_running') {
+        console.info('[pre-index]', 'openai_chat_wait_result', {
+            tablePrefix,
+            agentPermanentId: options.agentId,
+            agentHash: options.agentHash,
+            preparationWaitResult,
+        });
+    }
+}
+
+/**
+ * Resolves request-scoped project, calendar, and email credentials for the current agent.
+ */
+async function resolveHandleChatCompletionCredentials(options: {
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    agentId: string;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+}): Promise<{
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+}> {
+    const userId = options.currentUserIdentity?.userId;
+    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
+        resolveUseProjectGithubToken({
+            userId,
+            agentPermanentId: options.agentId,
+        }),
+        options.calendarConnections.length > 0
+            ? resolveUseCalendarGoogleToken({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+        options.useEmailConfiguration.isEnabled
+            ? resolveUseEmailSmtpCredential({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+    ]);
+
+    return {
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+    };
+}
+
+/**
+ * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
+ */
+async function createHandleChatCompletionAgent(options: {
+    agentName: string;
+    agentSource: string_book;
+    agentId: string;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    localServerUrl: string;
+    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
+    hasDynamicContext: boolean;
+    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
+}): Promise<Agent> {
+    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
+    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
+    const runtimeAgentReferenceResolver = options.hasDynamicContext
+        ? createBookScopedAgentReferenceResolver({
+              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
+              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
+              localServerUrl: options.localServerUrl,
+              fallbackResolver: options.baseAgentReferenceResolver,
+          })
+        : undefined;
+    const agentModelRequirements =
+        options.preparedAgentModelRequirements?.modelRequirements ||
+        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
+            agentReferenceResolver: runtimeAgentReferenceResolver,
+            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
+        }));
+
+    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
+        options.agentSource,
+        options.resolvedAgentContext.resolvedAgentName,
+        await baseOpenAiToolsPromise,
+        {
+            includeDynamicContext: true,
+            agentId: options.agentId,
+            modelRequirements: agentModelRequirements,
+        },
+    );
+
+    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
+        agentName: options.agentName,
+        assistantCacheKey: agentKitResult.assistantCacheKey,
+        vectorStoreHash: agentKitResult.vectorStoreHash,
+        vectorStoreId: agentKitResult.vectorStoreId,
+    });
+
+    return new Agent({
+        agentSource: options.agentSource,
+        executionTools: {
+            llm: agentKitResult.tools,
+        },
+        precomputedModelRequirements: agentModelRequirements,
+        assistantPreparationMode: 'external',
+        isVerbose: true,
+        teacherAgent: null,
+    });
+}


thinking
**Evaluating file organization**

I’m considering whether the import ordering is the best it can be, but I think it's okay for now. The next steps involve adding prompts and ensuring everything is being persisted correctly. I need to finalize maybe three files in this process. It's a little chaotic in my head, but I’m sure I can figure it all out! Keeping track of these steps feels important to maintaining clarity as I move forward.
file update
A C:/Users/me/work/ai/promptbook\apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
import type { ChatMessage } from '@promptbook-local/types';

/**
 * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
 */
type OpenAiCompatibilityMessageLike = {
    role?: unknown;
    content?: unknown;
};

/**
 * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
 *
 * @private function of `handleChatCompletion`
 */
export async function persistOpenAiCompatibilityFrozenChat(options: {
    isEnabled: boolean;
    userId: number | null | undefined;
    agentId: string;
    messages: ReadonlyArray<unknown>;
    chatId?: string;
    assistantContent?: string;
    includeAssistantPlaceholder?: boolean;
    failureMessage: string;
}): Promise<string | undefined> {
    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
        options;
    if (!isEnabled || !userId) {
        return undefined;
    }

    const persistedFrozenChat = await persistFrozenUserChat({
        userId,
        agentPermanentId: agentId,
        source: USER_CHAT_SOURCES.OPENAI_API,
        chatId,
        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
            includeAssistantPlaceholder,
        }),
    }).catch((error) => {
        console.error(failureMessage, error);
        return null;
    });

    return persistedFrozenChat?.id;
}

/**
 * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
 */
function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
    if (typeof value === 'string') {
        return value;
    }

    if (Array.isArray(value)) {
        return value
            .map((part) => {
                if (typeof part === 'string') {
                    return part;
                }

                if (!part || typeof part !== 'object') {
                    return '';
                }

                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
                if (typeof partLike.text === 'string') {
                    return partLike.text;
                }

                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
                    return '[Image input]';
                }

                if (partLike.image_url) {
                    return '[Image input]';
                }

                return '';
            })
            .filter(Boolean)
            .join('\n');
    }

    if (value === null || value === undefined) {
        return '';
    }

    try {
        return JSON.stringify(value, null, 2);
    } catch {
        return String(value);
    }
}

/**
 * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
 */
function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
    if (typeof role !== 'string') {
        return 'USER';
    }

    switch (role) {
        case 'assistant':
            return 'AGENT';
        case 'system':
            return 'SYSTEM';
        case 'tool':
            return 'TOOL';
        case 'developer':
            return 'SYSTEM';
        default:
            return 'USER';
    }
}

/**
 * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
 */
function createFrozenOpenAiCompatibilityMessages(
    rawMessages: ReadonlyArray<unknown>,
    assistantContent?: string,
    options: {
        includeAssistantPlaceholder?: boolean;
    } = {},
): Array<ChatMessage> {
    const startedAt = Date.now();
    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
        const message = rawMessage as OpenAiCompatibilityMessageLike;

        return {
            id: `openai-frozen-${index}`,
            sender: mapOpenAiCompatibilityRoleToSender(message.role),
            content: normalizeOpenAiCompatibilityMessageContent(message.content),
            isComplete: true,
            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
        } satisfies ChatMessage;
    });

    if (assistantContent !== undefined) {
        messages.push({
            id: `openai-frozen-${messages.length}`,
            sender: 'AGENT',
            content: assistantContent,
            isComplete: true,
            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
        } satisfies ChatMessage);
    } else if (options.includeAssistantPlaceholder) {
        messages.push({
            id: `openai-frozen-${messages.length}`,
            sender: 'AGENT',
            content: '',
            isComplete: false,
            lifecycleState: 'running',
            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
        } satisfies ChatMessage);
    }

    return messages;
}
A C:/Users/me/work/ai/promptbook\apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
import { resolveAppendOnlySelfLearningAgentSource } from '@/src/utils/resolveAppendOnlySelfLearningAgentSource';
import type { ToolCall, Usage } from '@promptbook-local/types';
import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';

/**
 * Runs the shared side effects after one successful model response.
 *
 * @private function of `handleChatCompletion`
 */
export async function finalizeHandleChatCompletionResult(options: {
    responseContentWithSuffix: string;
    usage: Usage;
    toolCalls?: ReadonlyArray<ToolCall>;
    parsedRequest: HandleChatCompletionParsedRequest;
    runtime: HandleChatCompletionRuntime;
    promptContext: HandleChatCompletionPromptContext;
    shouldPersistLearnedAgentSource: boolean;
}): Promise<void> {
    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;

    await promptContext.recordChatHistoryMessage({
        message: {
            role: 'MODEL',
            content: responseContentWithSuffix,
        },
        previousMessageHash: promptContext.userMessageHash,
        usage,
    });

    if (toolCalls && toolCalls.length > 0) {
        await logCalendarToolCallsActivity({
            userId: runtime.currentUserIdentity?.userId ?? null,
            agentPermanentId: runtime.agentId,
            toolCalls,
        });
    }

    await persistOpenAiCompatibilityFrozenChat({
        isEnabled: !runtime.isPrivateModeEnabled,
        userId: runtime.currentUserIdentity?.userId ?? null,
        agentId: runtime.agentId,
        chatId: promptContext.persistedFrozenChatId,
        messages: parsedRequest.messages,
        assistantContent: responseContentWithSuffix,
        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
    });
    await persistHandleChatCompletionLearnedAgentSource({
        runtime,
        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
    });
}

/**
 * Applies append-only self-learning updates when the current branch should persist them.
 */
async function persistHandleChatCompletionLearnedAgentSource(options: {
    runtime: HandleChatCompletionRuntime;
    shouldPersistLearnedAgentSource: boolean;
}): Promise<void> {
    const { runtime, shouldPersistLearnedAgentSource } = options;
    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
        return;
    }

    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
        resolvedAgentSourceBeforeLearning: runtime.agentSource,
        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
    });

    if (learnedAgentSource !== null) {
        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
    }
}
A C:/Users/me/work/ai/promptbook\apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
import type { ChatMessage, ChatPrompt } from '@promptbook-local/types';
import { $getCurrentDate } from '@promptbook-local/utils';
import type { NextRequest } from 'next/server';
import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';

/**
 * Prompt, history recorder, and persistence context created for one chat call.
 *
 * @private type of `handleChatCompletion`
 */
export type HandleChatCompletionPromptContext = {
    prompt: ChatPrompt;
    userMessageHash: string;
    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
    persistedFrozenChatId?: string;
};

/**
 * Creates prompt and persistence helpers for one chat completion request.
 *
 * @private function of `handleChatCompletion`
 */
export async function createHandleChatCompletionPromptContext(options: {
    request: NextRequest;
    title: string;
    apiKey: string | null;
    parsedRequest: HandleChatCompletionParsedRequest;
    runtime: HandleChatCompletionRuntime;
}): Promise<HandleChatCompletionPromptContext> {
    const { request, title, apiKey, parsedRequest, runtime } = options;
    const timezone = request.headers.get('x-timezone') || 'UTC';
    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
    const recordChatHistoryMessage = await createChatHistoryRecorder({
        request,
        agentIdentifier: runtime.agentId,
        agentHash: runtime.agentHash,
        source: 'OPENAI_API_COMPATIBILITY',
        apiKey,
        userId: runtime.currentUserIdentity?.userId ?? null,
        isEnabled: !runtime.isPrivateModeEnabled,
    });
    const userMessageHash = await recordChatHistoryMessage({
        message: {
            role: 'USER',
            content: lastMessage.content,
        },
        previousMessageHash: null,
    });
    const promptParameters = composePromptParametersWithMemoryContext({
        baseParameters: {
            ...parsedRequest.incomingParameters,
            timezone,
        },
        currentUserIdentity: runtime.currentUserIdentity,
        agentPermanentId: runtime.agentId,
        agentName: runtime.resolvedAgentContext.resolvedAgentName,
        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
        projectRepositories: runtime.projectRepositories,
        projectGithubToken: runtime.projectGithubToken,
        emailSmtpCredential: runtime.emailSmtpCredential,
        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
        calendarConnections: runtime.calendarConnections,
    });
    const prompt: ChatPrompt = {
        title,
        content: lastMessage.content,
        modelRequirements: {
            modelVariant: 'CHAT',
            responseFormat: parsedRequest.responseFormat,
            toolChoice: parsedRequest.runtimeToolChoice,
        },
        parameters: promptParameters,
        thread,
        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
    };

    return {
        prompt,
        userMessageHash,
        recordChatHistoryMessage,
        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
            isEnabled: !runtime.isPrivateModeEnabled,
            userId: runtime.currentUserIdentity?.userId ?? null,
            agentId: runtime.agentId,
            messages: parsedRequest.messages,
            includeAssistantPlaceholder: true,
            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
        }),
    };
}

/**
 * Builds one OpenAI-compatible thread from the earlier request messages.
 */
function createHandleChatCompletionThread(
    previousMessages: HandleChatCompletionParsedRequest['threadMessages'],
): Array<ChatMessage> {
    return previousMessages.map((message, index) => ({
        id: `msg-${index}`,
        sender: message.role === 'assistant' ? 'agent' : 'user',
        content: message.content,
        isComplete: true,
        createdAt: $getCurrentDate(),
    }));
}
apply_patch(auto_approved=true) exited 0 in 322ms:
Success. Updated the following files:
A apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
A apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
A apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
file update:
diff --git a/apps/agents-server/src/utils/handleChatCompletion.ts b/apps/agents-server/src/utils/handleChatCompletion.ts
index 9c96d36fad9cf24bf26d89f57372332add4e4595..cff56fafd6cfa90bb4094f9f202c4f01f6f74af1
--- a/apps/agents-server/src/utils/handleChatCompletion.ts
+++ b/apps/agents-server/src/utils/handleChatCompletion.ts
@@ -1,392 +1,16 @@
-import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
-import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
-import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
-import {
-    createBookScopedAgentReferenceResolver,
-    parseBookScopedAgentIdentifier,
-} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
-import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
-import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
-import {
-    appendMessageSuffix,
-    createMessageSuffixAppendix,
-    emulateMessageSuffixStreaming,
-    resolveMessageSuffixFromAgentSource,
-} from '@/src/utils/chat/messageSuffix';
-import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
-import { prepareToolCallsForStreaming } from './toolCallStreaming';
-import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
-import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
-import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
-import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
-import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
-import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
-import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
-import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
-import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
-import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
-import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
-import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
-import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
-import type {
-    ChatMessage,
-    ChatPrompt,
-    LlmToolDefinition,
-    string_book,
-    ToolCall,
-    TODO_any,
-    UncertainNumber,
-    Usage,
-    UsageCounts,
-} from '@promptbook-local/types';
-import { $getCurrentDate } from '@promptbook-local/utils';
 import { NextRequest, NextResponse } from 'next/server';
-import type OpenAI from 'openai';
-import { computeUsageCounts } from '../../../../src/execution/utils/computeUsageCounts';
-import { encodeChatStreamWhitespaceForTransport } from '../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
-import { isAgentDeleted } from '../app/agents/[agentName]/_utils';
 import { HTTP_STATUS_CODES } from '../constants';
-import { AgentKitCacheManager } from './cache/AgentKitCacheManager';
 import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
+import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
+import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
+import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
+import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
+import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
 import { validateApiKey } from './validateApiKey';
-import {
-    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    resolveAgentCollectionTablePrefix,
-    waitForRunningAgentPreparation,
-} from './agentPreparation';
-import {
-    resolveCachedServerAgentContext,
-    resolveCachedServerAgentModelRequirements,
-} from './cachedServerAgentRuntime';
-import { resolveAppendOnlySelfLearningAgentSource } from './resolveAppendOnlySelfLearningAgentSource';
 
 /**
- * Falls back to the estimated value when the original token count is unknown.
- *
- * @param tokenCount - Token count reported by the execution tools.
- * @param fallbackValue - Estimated token count based on text length.
- * @returns Token count to report in the OpenAI-compatible response.
- */
-function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
-    if (tokenCount.value === 0 && tokenCount.isUncertain) {
-        return {
-            value: fallbackValue,
-            isUncertain: true,
-        };
-    }
-
-    return tokenCount;
-}
-
-/**
- * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
- *
- * @param promptContent - Prompt content used for the request.
- * @param completionContent - Assistant response content.
- * @param usage - Native Promptbook usage details.
- * @returns Usage payload for compatibility responses.
- */
-function createCompatibilityUsage(
-    promptContent: string,
-    completionContent: string,
-    usage: Usage,
-): {
-    prompt_tokens: number;
-    completion_tokens: number;
-    total_tokens: number;
-    details: Usage;
-} {
-    const promptCounts = computeUsageCounts(promptContent);
-    const completionCounts = computeUsageCounts(completionContent);
-    const inputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
-        ...promptCounts,
-    };
-    const outputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
-        ...completionCounts,
-    };
-
-    const promptTokens = inputUsage.tokensCount.value;
-    const completionTokens = outputUsage.tokensCount.value;
-
-    return {
-        prompt_tokens: promptTokens,
-        completion_tokens: completionTokens,
-        total_tokens: promptTokens + completionTokens,
-        details: {
-            price: usage.price,
-            duration: usage.duration,
-            input: inputUsage,
-            output: outputUsage,
-        },
-    };
-}
-
-/**
- * Definition of open AI chat tool.
- */
-type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
-
-/**
- * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
- */
-type OpenAiCompatibilityMessageLike = {
-    role?: unknown;
-    content?: unknown;
-};
-
-/**
- * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
- */
-type HandleChatCompletionParsedRequest = {
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    threadMessages: ReadonlyArray<TODO_any>;
-    stream: TODO_any;
-    model: TODO_any;
-    responseFormat: TODO_any;
-    runtimeTools?: Array<LlmToolDefinition>;
-    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-    incomingParameters: Record<string, unknown>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Runtime state resolved before the chat model is executed.
- */
-type HandleChatCompletionRuntime = {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    agent: Agent;
-    agentSource: string_book;
-    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
-    messageSuffix: string | null;
-    agentId: string;
-    agentHash: string;
-    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Prompt, history recorder, and persistence context created for one chat call.
- */
-type HandleChatCompletionPromptContext = {
-    prompt: ChatPrompt;
-    userMessageHash: string;
-    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
-    persistedFrozenChatId?: string;
-};
-
-/**
- * Handles convert open Ai tools.
- */
-function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
-    if (!Array.isArray(rawTools)) {
-        return undefined;
-    }
-
-    const converted = rawTools
-        .map((tool) => convertOpenAiTool(tool))
-        .filter((tool): tool is LlmToolDefinition => tool !== null);
-
-    return converted.length > 0 ? converted : undefined;
-}
-
-/**
- * Handles convert open Ai tool.
- */
-function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
-    const tool = rawTool as OpenAIChatToolDefinition;
-    if (!tool || tool.type !== 'function') {
-        return null;
-    }
-
-    const functionDefinition = tool.function;
-    if (!functionDefinition) {
-        return null;
-    }
-
-    const { name, description, parameters } = functionDefinition;
-    if (typeof name !== 'string' || name.trim().length === 0) {
-        return null;
-    }
-
-    const parameterSchema = parameters ?? {};
-    if (parameterSchema.type !== 'object') {
-        return null;
-    }
-
-    const properties = parameterSchema.properties ?? {};
-    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
-
-    for (const [key, value] of Object.entries(properties)) {
-        if (!value || typeof value !== 'object') {
-            continue;
-        }
-
-        normalizedProperties[key] = {
-            type: typeof value.type === 'string' ? value.type : 'string',
-            description: typeof value.description === 'string' ? value.description : undefined,
-        };
-    }
-
-    const required =
-        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
-            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
-            : undefined;
-
-    return {
-        name,
-        description: typeof description === 'string' ? description : '',
-        parameters: {
-            type: 'object',
-            properties: normalizedProperties,
-            required,
-            additionalProperties:
-                typeof parameterSchema.additionalProperties === 'boolean'
-                    ? parameterSchema.additionalProperties
-                    : undefined,
-        },
-    };
-}
-
-/**
- * Parses open Ai tool choice.
- */
-function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
-    if (value === undefined || value === null) {
-        return undefined;
-    }
-
-    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-}
-
-/**
- * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
- */
-function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
-    if (typeof value === 'string') {
-        return value;
-    }
-
-    if (Array.isArray(value)) {
-        return value
-            .map((part) => {
-                if (typeof part === 'string') {
-                    return part;
-                }
-
-                if (!part || typeof part !== 'object') {
-                    return '';
-                }
-
-                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
-                if (typeof partLike.text === 'string') {
-                    return partLike.text;
-                }
-
-                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
-                    return '[Image input]';
-                }
-
-                if (partLike.image_url) {
-                    return '[Image input]';
-                }
-
-                return '';
-            })
-            .filter(Boolean)
-            .join('\n');
-    }
-
-    if (value === null || value === undefined) {
-        return '';
-    }
-
-    try {
-        return JSON.stringify(value, null, 2);
-    } catch {
-        return String(value);
-    }
-}
-
-/**
- * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ * Handles convert open Ai chat completion requests routed through the Agents Server.
  */
-function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
-    if (typeof role !== 'string') {
-        return 'USER';
-    }
-
-    switch (role) {
-        case 'assistant':
-            return 'AGENT';
-        case 'system':
-            return 'SYSTEM';
-        case 'tool':
-            return 'TOOL';
-        case 'developer':
-            return 'SYSTEM';
-        default:
-            return 'USER';
-    }
-}
-
-/**
- * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
- */
-function createFrozenOpenAiCompatibilityMessages(
-    rawMessages: ReadonlyArray<unknown>,
-    assistantContent?: string,
-    options: {
-        includeAssistantPlaceholder?: boolean;
-    } = {},
-): Array<ChatMessage> {
-    const startedAt = Date.now();
-    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
-        const message = rawMessage as OpenAiCompatibilityMessageLike;
-
-        return {
-            id: `openai-frozen-${index}`,
-            sender: mapOpenAiCompatibilityRoleToSender(message.role),
-            content: normalizeOpenAiCompatibilityMessageContent(message.content),
-            isComplete: true,
-            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage;
-    });
-
-    if (assistantContent !== undefined) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: assistantContent,
-            isComplete: true,
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    } else if (options.includeAssistantPlaceholder) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: '',
-            isComplete: false,
-            lifecycleState: 'running',
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    }
-
-    return messages;
-}
-
-/**
- * Handles chat completion.
- */
 export async function handleChatCompletion(
     request: NextRequest,
     params: { agentName?: string },
@@ -399,7 +23,6 @@
         return versionMismatchResponse;
     }
 
-    // Validate API key explicitly (in addition to middleware)
     const apiKeyValidation = await validateApiKey(request);
     if (!apiKeyValidation.isValid) {
         return NextResponse.json(
@@ -462,724 +85,5 @@
             { error: { message: (error as Error).message || 'Internal Server Error', type: 'server_error' } },
             { status: HTTP_STATUS_CODES.INTERNAL_SERVER_ERROR },
         );
-    }
-}
-
-/**
- * Parses and validates the OpenAI-compatible request payload.
- */
-function parseHandleChatCompletionRequest(options: {
-    body: TODO_any;
-    agentNameFromParams?: string;
-    request: NextRequest;
-}): HandleChatCompletionParsedRequest | NextResponse {
-    const { body, agentNameFromParams, request } = options;
-    const {
-        messages,
-        stream,
-        model,
-        response_format: responseFormat,
-        tools: rawTools,
-        tool_choice: toolChoice,
-        parameters: rawParameters = {},
-    } = body;
-    const agentName = (agentNameFromParams || model) as string | undefined;
-    if (!agentName) {
-        return createHandleChatCompletionErrorResponse(
-            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    if (!messages || !Array.isArray(messages) || messages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array is required and cannot be empty.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
-    if (threadMessages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array must contain at least one non-system message.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    return {
-        agentName,
-        messages,
-        threadMessages,
-        stream,
-        model,
-        responseFormat,
-        runtimeTools: convertOpenAiTools(rawTools),
-        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
-        incomingParameters:
-            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
-                ? (rawParameters as Record<string, unknown>)
-                : {},
-        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
-    };
-}
-
-/**
- * Resolves agent/runtime dependencies needed before prompt execution.
- */
-async function resolveHandleChatCompletionRuntime(options: {
-    request: NextRequest;
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    isPrivateModeEnabled: boolean;
-}): Promise<HandleChatCompletionRuntime | NextResponse> {
-    const { request, agentName, messages, isPrivateModeEnabled } = options;
-    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
-    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
-
-    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
-        return createHandleChatCompletionErrorResponse(
-            'This agent has been deleted. You can restore it from the Recycle Bin.',
-            'agent_deleted',
-            410,
-        );
-    }
-
-    const [collection, baseAgentReferenceResolver] = await Promise.all([
-        $provideAgentCollectionForServer(),
-        $provideAgentReferenceResolver(),
-    ]);
-    const localServerUrl = new URL(request.url).origin;
-    const resolvedAgentContext = await resolveCachedServerAgentContext({
-        collection,
-        agentIdentifier: agentName,
-        localServerUrl,
-        fallbackResolver: baseAgentReferenceResolver,
-    }).catch(() => null);
-
-    if (!resolvedAgentContext?.resolvedAgentSource) {
-        return createHandleChatCompletionErrorResponse(
-            `Agent '${agentName}' not found.`,
-            'invalid_request_error',
-            HTTP_STATUS_CODES.NOT_FOUND,
-        );
-    }
-
-    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
-    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
-    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
-    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
-    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
-    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
-    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
-    agentSource = agentSourceWithContext;
-
-    const preparedAgentModelRequirements = !hasDynamicContext
-        ? await resolveCachedServerAgentModelRequirements({
-              resolvedAgentContext,
-              localServerUrl,
-              fallbackResolver: baseAgentReferenceResolver,
-          })
-        : null;
-    const agentId = resolvedAgentContext.parentAgentPermanentId;
-    const agentHash = computeAgentHash(agentSource);
-    await waitForHandleChatCompletionAgentPreparation({
-        collection,
-        agentId,
-        agentHash,
-    });
-
-    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
-    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
-        await resolveHandleChatCompletionCredentials({
-            currentUserIdentity,
-            agentId,
-            calendarConnections,
-            useEmailConfiguration,
-        });
-    const agent = await createHandleChatCompletionAgent({
-        agentName,
-        agentSource,
-        agentId,
-        resolvedAgentContext,
-        localServerUrl,
-        baseAgentReferenceResolver,
-        hasDynamicContext,
-        preparedAgentModelRequirements,
-    });
-
-    return {
-        collection,
-        resolvedAgentContext,
-        agent,
-        agentSource,
-        unresolvedAgentSource,
-        messageSuffix,
-        agentId,
-        agentHash,
-        projectRepositories,
-        calendarConnections,
-        useEmailConfiguration,
-        currentUserIdentity,
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-        isPrivateModeEnabled,
-    };
-}
-
-/**
- * Creates prompt and persistence helpers for one chat completion request.
- */
-async function createHandleChatCompletionPromptContext(options: {
-    request: NextRequest;
-    title: string;
-    apiKey: string | null;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-}): Promise<HandleChatCompletionPromptContext> {
-    const { request, title, apiKey, parsedRequest, runtime } = options;
-    const timezone = request.headers.get('x-timezone') || 'UTC';
-    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
-    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
-    const recordChatHistoryMessage = await createChatHistoryRecorder({
-        request,
-        agentIdentifier: runtime.agentId,
-        agentHash: runtime.agentHash,
-        source: 'OPENAI_API_COMPATIBILITY',
-        apiKey,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        isEnabled: !runtime.isPrivateModeEnabled,
-    });
-    const userMessageHash = await recordChatHistoryMessage({
-        message: {
-            role: 'USER',
-            content: lastMessage.content,
-        },
-        previousMessageHash: null,
-    });
-    const promptParameters = composePromptParametersWithMemoryContext({
-        baseParameters: {
-            ...parsedRequest.incomingParameters,
-            timezone,
-        },
-        currentUserIdentity: runtime.currentUserIdentity,
-        agentPermanentId: runtime.agentId,
-        agentName: runtime.resolvedAgentContext.resolvedAgentName,
-        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
-        projectRepositories: runtime.projectRepositories,
-        projectGithubToken: runtime.projectGithubToken,
-        emailSmtpCredential: runtime.emailSmtpCredential,
-        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
-        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
-        calendarConnections: runtime.calendarConnections,
-    });
-    const prompt: ChatPrompt = {
-        title,
-        content: lastMessage.content,
-        modelRequirements: {
-            modelVariant: 'CHAT',
-            responseFormat: parsedRequest.responseFormat,
-            toolChoice: parsedRequest.runtimeToolChoice,
-            // We could pass 'model' from body if we wanted to enforce it, but Agent usually has its own config
-        },
-        parameters: promptParameters,
-        thread,
-        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
-    };
-
-    return {
-        prompt,
-        userMessageHash,
-        recordChatHistoryMessage,
-        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
-            isEnabled: !runtime.isPrivateModeEnabled,
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentId: runtime.agentId,
-            messages: parsedRequest.messages,
-            includeAssistantPlaceholder: true,
-            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
-        }),
-    };
-}
-
-/**
- * Handles the streaming OpenAI-compatible response branch.
- */
-function createHandleChatCompletionStreamResponse(options: {
-    request: NextRequest;
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Response {
-    const { request, title, parsedRequest, runtime, promptContext } = options;
-    const encoder = new TextEncoder();
-    const responseModel = parsedRequest.model || 'promptbook-agent';
-    const readableStream = new ReadableStream({
-        async start(controller) {
-            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
-            const created = Math.floor(Date.now() / 1000);
-            const emitSsePayload = (payload: unknown) => {
-                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
-            };
-            const emitDeltaChunk = (deltaContent: string) => {
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {
-                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
-                        },
-                        finishReason: null,
-                    }),
-                );
-            };
-
-            emitSsePayload(
-                createHandleChatCompletionChunk({
-                    runId,
-                    created,
-                    model: responseModel,
-                    delta: {
-                        role: 'assistant',
-                        content: '',
-                    },
-                    finishReason: null,
-                }),
-            );
-
-            let hasMeaningfulDelta = false;
-
-            try {
-                const handleStreamChunk = createChatStreamHandler({
-                    onDelta: (deltaContent) => {
-                        if (deltaContent.trim().length > 0) {
-                            hasMeaningfulDelta = true;
-                        }
-                        emitDeltaChunk(deltaContent);
-                    },
-                    onToolCalls: (toolCalls) => {
-                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
-                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
-                    },
-                });
-                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
-                    signal: request.signal,
-                });
-                const normalizedResponse = ensureNonEmptyChatContent({
-                    content: result.content,
-                    context: title,
-                });
-
-                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
-                    emitDeltaChunk(normalizedResponse.content);
-                }
-
-                const messageSuffixAppendix = createMessageSuffixAppendix(
-                    normalizedResponse.content,
-                    runtime.messageSuffix,
-                );
-                if (messageSuffixAppendix) {
-                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
-                        emitDeltaChunk(delta);
-                    });
-                }
-
-                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-                await finalizeHandleChatCompletionResult({
-                    responseContentWithSuffix,
-                    usage: result.usage,
-                    toolCalls: result.toolCalls,
-                    parsedRequest,
-                    runtime,
-                    promptContext,
-                    // Preserve historical behavior: streamed OpenAI-compatible calls still persist learning in private mode.
-                    shouldPersistLearnedAgentSource: true,
-                });
-
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {},
-                        finishReason: 'stop',
-                    }),
-                );
-                controller.enqueue(encoder.encode('[DONE]'));
-            } catch (error) {
-                console.error('Error during streaming:', error);
-                controller.error(error);
-            }
-            controller.close();
-        },
-    });
-
-    return new Response(readableStream, {
-        headers: {
-            'Content-Type': 'text/event-stream',
-            'Cache-Control': 'no-cache',
-            Connection: 'keep-alive',
-        },
-    });
-}
-
-/**
- * Handles the non-streaming OpenAI-compatible response branch.
- */
-async function createHandleChatCompletionJsonResponse(options: {
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Promise<NextResponse> {
-    const { title, parsedRequest, runtime, promptContext } = options;
-    const result = await runtime.agent.callChatModel(promptContext.prompt);
-    const normalizedResponse = ensureNonEmptyChatContent({
-        content: result.content,
-        context: title,
-    });
-    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-
-    await finalizeHandleChatCompletionResult({
-        responseContentWithSuffix,
-        usage: result.usage,
-        toolCalls: result.toolCalls,
-        parsedRequest,
-        runtime,
-        promptContext,
-        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
-    });
-
-    return NextResponse.json({
-        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
-        object: 'chat.completion',
-        created: Math.floor(Date.now() / 1000),
-        model: parsedRequest.model || 'promptbook-agent',
-        choices: [
-            {
-                index: 0,
-                message: {
-                    role: 'assistant',
-                    content: responseContentWithSuffix,
-                },
-                finish_reason: 'stop',
-            },
-        ],
-        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
-    });
-}
-
-/**
- * Runs the shared side effects after one successful model response.
- */
-async function finalizeHandleChatCompletionResult(options: {
-    responseContentWithSuffix: string;
-    usage: Usage;
-    toolCalls?: ReadonlyArray<ToolCall>;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
-
-    await promptContext.recordChatHistoryMessage({
-        message: {
-            role: 'MODEL',
-            content: responseContentWithSuffix,
-        },
-        previousMessageHash: promptContext.userMessageHash,
-        usage,
-    });
-
-    if (toolCalls && toolCalls.length > 0) {
-        await logCalendarToolCallsActivity({
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentPermanentId: runtime.agentId,
-            toolCalls,
-        });
-    }
-
-    await persistOpenAiCompatibilityFrozenChat({
-        isEnabled: !runtime.isPrivateModeEnabled,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        agentId: runtime.agentId,
-        chatId: promptContext.persistedFrozenChatId,
-        messages: parsedRequest.messages,
-        assistantContent: responseContentWithSuffix,
-        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
-    });
-    await persistHandleChatCompletionLearnedAgentSource({
-        runtime,
-        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
-    });
-}
-
-/**
- * Applies append-only self-learning updates when the current branch should persist them.
- */
-async function persistHandleChatCompletionLearnedAgentSource(options: {
-    runtime: HandleChatCompletionRuntime;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { runtime, shouldPersistLearnedAgentSource } = options;
-    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
-        return;
-    }
-
-    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
-        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
-        resolvedAgentSourceBeforeLearning: runtime.agentSource,
-        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
-    });
-
-    if (learnedAgentSource !== null) {
-        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
     }
 }
-
-/**
- * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
- */
-async function persistOpenAiCompatibilityFrozenChat(options: {
-    isEnabled: boolean;
-    userId: number | null | undefined;
-    agentId: string;
-    messages: ReadonlyArray<unknown>;
-    chatId?: string;
-    assistantContent?: string;
-    includeAssistantPlaceholder?: boolean;
-    failureMessage: string;
-}): Promise<string | undefined> {
-    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
-        options;
-    if (!isEnabled || !userId) {
-        return undefined;
-    }
-
-    const persistedFrozenChat = await persistFrozenUserChat({
-        userId,
-        agentPermanentId: agentId,
-        source: USER_CHAT_SOURCES.OPENAI_API,
-        chatId,
-        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
-            includeAssistantPlaceholder,
-        }),
-    }).catch((error) => {
-        console.error(failureMessage, error);
-        return null;
-    });
-
-    return persistedFrozenChat?.id;
-}
-
-/**
- * Builds one OpenAI-compatible thread from the earlier request messages.
- */
-function createHandleChatCompletionThread(previousMessages: ReadonlyArray<TODO_any>): ChatMessage[] {
-    return previousMessages.map((message: TODO_any, index: number) => ({
-        id: `msg-${index}`,
-        sender: message.role === 'assistant' ? 'agent' : 'user',
-        content: message.content,
-        isComplete: true,
-        createdAt: $getCurrentDate(),
-    }));
-}
-
-/**
- * Appends system messages to the agent source as dynamic `CONTEXT`.
- */
-function appendSystemMessagesToAgentSource(
-    agentSource: string_book,
-    messages: ReadonlyArray<TODO_any>,
-): {
-    agentSourceWithContext: string_book;
-    hasDynamicContext: boolean;
-} {
-    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
-    if (systemMessages.length === 0) {
-        return {
-            agentSourceWithContext: agentSource,
-            hasDynamicContext: false,
-        };
-    }
-
-    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
-    return {
-        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
-        hasDynamicContext: true,
-    };
-}
-
-/**
- * Waits for any running preparation job of the current agent before executing the chat call.
- */
-async function waitForHandleChatCompletionAgentPreparation(options: {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    agentId: string;
-    agentHash: string;
-}): Promise<void> {
-    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
-    const preparationWaitResult = await waitForRunningAgentPreparation({
-        tablePrefix,
-        agentPermanentId: options.agentId,
-        fingerprint: options.agentHash,
-        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    });
-
-    if (preparationWaitResult !== 'not_running') {
-        console.info('[pre-index]', 'openai_chat_wait_result', {
-            tablePrefix,
-            agentPermanentId: options.agentId,
-            agentHash: options.agentHash,
-            preparationWaitResult,
-        });
-    }
-}
-
-/**
- * Resolves request-scoped project, calendar, and email credentials for the current agent.
- */
-async function resolveHandleChatCompletionCredentials(options: {
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    agentId: string;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-}): Promise<{
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-}> {
-    const userId = options.currentUserIdentity?.userId;
-    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
-        resolveUseProjectGithubToken({
-            userId,
-            agentPermanentId: options.agentId,
-        }),
-        options.calendarConnections.length > 0
-            ? resolveUseCalendarGoogleToken({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-        options.useEmailConfiguration.isEnabled
-            ? resolveUseEmailSmtpCredential({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-    ]);
-
-    return {
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-    };
-}
-
-/**
- * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
- */
-async function createHandleChatCompletionAgent(options: {
-    agentName: string;
-    agentSource: string_book;
-    agentId: string;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    localServerUrl: string;
-    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
-    hasDynamicContext: boolean;
-    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
-}): Promise<Agent> {
-    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
-    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
-    const runtimeAgentReferenceResolver = options.hasDynamicContext
-        ? createBookScopedAgentReferenceResolver({
-              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
-              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
-              localServerUrl: options.localServerUrl,
-              fallbackResolver: options.baseAgentReferenceResolver,
-          })
-        : undefined;
-    const agentModelRequirements =
-        options.preparedAgentModelRequirements?.modelRequirements ||
-        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
-            agentReferenceResolver: runtimeAgentReferenceResolver,
-            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
-        }));
-
-    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
-        options.agentSource,
-        options.resolvedAgentContext.resolvedAgentName,
-        await baseOpenAiToolsPromise,
-        {
-            includeDynamicContext: true,
-            agentId: options.agentId,
-            modelRequirements: agentModelRequirements,
-        },
-    );
-
-    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
-        agentName: options.agentName,
-        assistantCacheKey: agentKitResult.assistantCacheKey,
-        vectorStoreHash: agentKitResult.vectorStoreHash,
-        vectorStoreId: agentKitResult.vectorStoreId,
-    });
-
-    return new Agent({
-        agentSource: options.agentSource,
-        executionTools: {
-            llm: agentKitResult.tools,
-        },
-        precomputedModelRequirements: agentModelRequirements,
-        assistantPreparationMode: 'external',
-        isVerbose: true,
-        teacherAgent: null,
-    });
-}
-
-/**
- * Creates one OpenAI-compatible streaming chunk payload.
- */
-function createHandleChatCompletionChunk(options: {
-    runId: string;
-    created: number;
-    model: TODO_any;
-    delta: Record<string, unknown>;
-    finishReason: 'stop' | null;
-}) {
-    return {
-        id: options.runId,
-        object: 'chat.completion.chunk',
-        created: options.created,
-        model: options.model,
-        choices: [
-            {
-                index: 0,
-                delta: options.delta,
-                finish_reason: options.finishReason,
-            },
-        ],
-    };
-}
-
-/**
- * Creates one consistent OpenAI-compatible error response.
- */
-function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
-    return NextResponse.json(
-        {
-            error: {
-                message,
-                type,
-            },
-        },
-        { status },
-    );
-}
-
-// TODO: [🈹] Maybe move chat thread handling here
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f49a4fa80fa21f140e25de59596e5546d62e3f7
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
@@ -0,0 +1,18 @@
+import { NextResponse } from 'next/server';
+
+/**
+ * Creates one consistent OpenAI-compatible error response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
+    return NextResponse.json(
+        {
+            error: {
+                message,
+                type,
+            },
+        },
+        { status },
+    );
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..7aac8141a8b1237ce36430b4b4598a618f0383ac
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
@@ -0,0 +1,111 @@
+import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
+import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
+import type { ChatMessage, ChatPrompt } from '@promptbook-local/types';
+import { $getCurrentDate } from '@promptbook-local/utils';
+import type { NextRequest } from 'next/server';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Prompt, history recorder, and persistence context created for one chat call.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionPromptContext = {
+    prompt: ChatPrompt;
+    userMessageHash: string;
+    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
+    persistedFrozenChatId?: string;
+};
+
+/**
+ * Creates prompt and persistence helpers for one chat completion request.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionPromptContext(options: {
+    request: NextRequest;
+    title: string;
+    apiKey: string | null;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+}): Promise<HandleChatCompletionPromptContext> {
+    const { request, title, apiKey, parsedRequest, runtime } = options;
+    const timezone = request.headers.get('x-timezone') || 'UTC';
+    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
+    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
+    const recordChatHistoryMessage = await createChatHistoryRecorder({
+        request,
+        agentIdentifier: runtime.agentId,
+        agentHash: runtime.agentHash,
+        source: 'OPENAI_API_COMPATIBILITY',
+        apiKey,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        isEnabled: !runtime.isPrivateModeEnabled,
+    });
+    const userMessageHash = await recordChatHistoryMessage({
+        message: {
+            role: 'USER',
+            content: lastMessage.content,
+        },
+        previousMessageHash: null,
+    });
+    const promptParameters = composePromptParametersWithMemoryContext({
+        baseParameters: {
+            ...parsedRequest.incomingParameters,
+            timezone,
+        },
+        currentUserIdentity: runtime.currentUserIdentity,
+        agentPermanentId: runtime.agentId,
+        agentName: runtime.resolvedAgentContext.resolvedAgentName,
+        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
+        projectRepositories: runtime.projectRepositories,
+        projectGithubToken: runtime.projectGithubToken,
+        emailSmtpCredential: runtime.emailSmtpCredential,
+        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
+        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
+        calendarConnections: runtime.calendarConnections,
+    });
+    const prompt: ChatPrompt = {
+        title,
+        content: lastMessage.content,
+        modelRequirements: {
+            modelVariant: 'CHAT',
+            responseFormat: parsedRequest.responseFormat,
+            toolChoice: parsedRequest.runtimeToolChoice,
+        },
+        parameters: promptParameters,
+        thread,
+        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
+    };
+
+    return {
+        prompt,
+        userMessageHash,
+        recordChatHistoryMessage,
+        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
+            isEnabled: !runtime.isPrivateModeEnabled,
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentId: runtime.agentId,
+            messages: parsedRequest.messages,
+            includeAssistantPlaceholder: true,
+            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
+        }),
+    };
+}
+
+/**
+ * Builds one OpenAI-compatible thread from the earlier request messages.
+ */
+function createHandleChatCompletionThread(
+    previousMessages: HandleChatCompletionParsedRequest['threadMessages'],
+): Array<ChatMessage> {
+    return previousMessages.map((message, index) => ({
+        id: `msg-${index}`,
+        sender: message.role === 'assistant' ? 'agent' : 'user',
+        content: message.content,
+        isComplete: true,
+        createdAt: $getCurrentDate(),
+    }));
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
new file mode 100644
index 0000000000000000000000000000000000000000..dd6a7d87ea491df245dbf93d8bcb277a9a10b034
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
@@ -0,0 +1,78 @@
+import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
+import { resolveAppendOnlySelfLearningAgentSource } from '@/src/utils/resolveAppendOnlySelfLearningAgentSource';
+import type { ToolCall, Usage } from '@promptbook-local/types';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Runs the shared side effects after one successful model response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function finalizeHandleChatCompletionResult(options: {
+    responseContentWithSuffix: string;
+    usage: Usage;
+    toolCalls?: ReadonlyArray<ToolCall>;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
+
+    await promptContext.recordChatHistoryMessage({
+        message: {
+            role: 'MODEL',
+            content: responseContentWithSuffix,
+        },
+        previousMessageHash: promptContext.userMessageHash,
+        usage,
+    });
+
+    if (toolCalls && toolCalls.length > 0) {
+        await logCalendarToolCallsActivity({
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentPermanentId: runtime.agentId,
+            toolCalls,
+        });
+    }
+
+    await persistOpenAiCompatibilityFrozenChat({
+        isEnabled: !runtime.isPrivateModeEnabled,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        agentId: runtime.agentId,
+        chatId: promptContext.persistedFrozenChatId,
+        messages: parsedRequest.messages,
+        assistantContent: responseContentWithSuffix,
+        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
+    });
+    await persistHandleChatCompletionLearnedAgentSource({
+        runtime,
+        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
+    });
+}
+
+/**
+ * Applies append-only self-learning updates when the current branch should persist them.
+ */
+async function persistHandleChatCompletionLearnedAgentSource(options: {
+    runtime: HandleChatCompletionRuntime;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { runtime, shouldPersistLearnedAgentSource } = options;
+    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
+        return;
+    }
+
+    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
+        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
+        resolvedAgentSourceBeforeLearning: runtime.agentSource,
+        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
+    });
+
+    if (learnedAgentSource !== null) {
+        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
+    }
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
new file mode 100644
index 0000000000000000000000000000000000000000..58e1c26d7f428c1bdb39b36411b3f75256e3de51
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
@@ -0,0 +1,176 @@
+import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
+import type { LlmToolDefinition, TODO_any } from '@promptbook-local/types';
+import { NextRequest, NextResponse } from 'next/server';
+import type OpenAI from 'openai';
+import { HTTP_STATUS_CODES } from '../../constants';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Definition of open AI chat tool.
+ */
+type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
+
+/**
+ * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionParsedRequest = {
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    threadMessages: ReadonlyArray<TODO_any>;
+    stream: TODO_any;
+    model: TODO_any;
+    responseFormat: TODO_any;
+    runtimeTools?: Array<LlmToolDefinition>;
+    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+    incomingParameters: Record<string, unknown>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Parses and validates the OpenAI-compatible request payload.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function parseHandleChatCompletionRequest(options: {
+    body: TODO_any;
+    agentNameFromParams?: string;
+    request: NextRequest;
+}): HandleChatCompletionParsedRequest | NextResponse {
+    const { body, agentNameFromParams, request } = options;
+    const {
+        messages,
+        stream,
+        model,
+        response_format: responseFormat,
+        tools: rawTools,
+        tool_choice: toolChoice,
+        parameters: rawParameters = {},
+    } = body;
+    const agentName = (agentNameFromParams || model) as string | undefined;
+    if (!agentName) {
+        return createHandleChatCompletionErrorResponse(
+            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    if (!messages || !Array.isArray(messages) || messages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array is required and cannot be empty.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
+    if (threadMessages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array must contain at least one non-system message.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    return {
+        agentName,
+        messages,
+        threadMessages,
+        stream,
+        model,
+        responseFormat,
+        runtimeTools: convertOpenAiTools(rawTools),
+        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
+        incomingParameters:
+            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
+                ? (rawParameters as Record<string, unknown>)
+                : {},
+        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
+    };
+}
+
+/**
+ * Handles convert open Ai tools.
+ */
+function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
+    if (!Array.isArray(rawTools)) {
+        return undefined;
+    }
+
+    const converted = rawTools
+        .map((tool) => convertOpenAiTool(tool))
+        .filter((tool): tool is LlmToolDefinition => tool !== null);
+
+    return converted.length > 0 ? converted : undefined;
+}
+
+/**
+ * Handles convert open Ai tool.
+ */
+function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
+    const tool = rawTool as OpenAIChatToolDefinition;
+    if (!tool || tool.type !== 'function') {
+        return null;
+    }
+
+    const functionDefinition = tool.function;
+    if (!functionDefinition) {
+        return null;
+    }
+
+    const { name, description, parameters } = functionDefinition;
+    if (typeof name !== 'string' || name.trim().length === 0) {
+        return null;
+    }
+
+    const parameterSchema = parameters ?? {};
+    if (parameterSchema.type !== 'object') {
+        return null;
+    }
+
+    const properties = parameterSchema.properties ?? {};
+    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
+
+    for (const [key, value] of Object.entries(properties)) {
+        if (!value || typeof value !== 'object') {
+            continue;
+        }
+
+        normalizedProperties[key] = {
+            type: typeof value.type === 'string' ? value.type : 'string',
+            description: typeof value.description === 'string' ? value.description : undefined,
+        };
+    }
+
+    const required =
+        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
+            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
+            : undefined;
+
+    return {
+        name,
+        description: typeof description === 'string' ? description : '',
+        parameters: {
+            type: 'object',
+            properties: normalizedProperties,
+            required,
+            additionalProperties:
+                typeof parameterSchema.additionalProperties === 'boolean'
+                    ? parameterSchema.additionalProperties
+                    : undefined,
+        },
+    };
+}
+
+/**
+ * Parses open Ai tool choice.
+ */
+function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
+    if (value === undefined || value === null) {
+        return undefined;
+    }
+
+    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
new file mode 100644
index 0000000000000000000000000000000000000000..60e5f1b008bcf5a6a1836c06c4c3d9ebcb909a9f
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
@@ -0,0 +1,163 @@
+import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
+import type { ChatMessage } from '@promptbook-local/types';
+
+/**
+ * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
+ */
+type OpenAiCompatibilityMessageLike = {
+    role?: unknown;
+    content?: unknown;
+};
+
+/**
+ * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function persistOpenAiCompatibilityFrozenChat(options: {
+    isEnabled: boolean;
+    userId: number | null | undefined;
+    agentId: string;
+    messages: ReadonlyArray<unknown>;
+    chatId?: string;
+    assistantContent?: string;
+    includeAssistantPlaceholder?: boolean;
+    failureMessage: string;
+}): Promise<string | undefined> {
+    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
+        options;
+    if (!isEnabled || !userId) {
+        return undefined;
+    }
+
+    const persistedFrozenChat = await persistFrozenUserChat({
+        userId,
+        agentPermanentId: agentId,
+        source: USER_CHAT_SOURCES.OPENAI_API,
+        chatId,
+        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
+            includeAssistantPlaceholder,
+        }),
+    }).catch((error) => {
+        console.error(failureMessage, error);
+        return null;
+    });
+
+    return persistedFrozenChat?.id;
+}
+
+/**
+ * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
+ */
+function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
+    if (typeof value === 'string') {
+        return value;
+    }
+
+    if (Array.isArray(value)) {
+        return value
+            .map((part) => {
+                if (typeof part === 'string') {
+                    return part;
+                }
+
+                if (!part || typeof part !== 'object') {
+                    return '';
+                }
+
+                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
+                if (typeof partLike.text === 'string') {
+                    return partLike.text;
+                }
+
+                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
+                    return '[Image input]';
+                }
+
+                if (partLike.image_url) {
+                    return '[Image input]';
+                }
+
+                return '';
+            })
+            .filter(Boolean)
+            .join('\n');
+    }
+
+    if (value === null || value === undefined) {
+        return '';
+    }
+
+    try {
+        return JSON.stringify(value, null, 2);
+    } catch {
+        return String(value);
+    }
+}
+
+/**
+ * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ */
+function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
+    if (typeof role !== 'string') {
+        return 'USER';
+    }
+
+    switch (role) {
+        case 'assistant':
+            return 'AGENT';
+        case 'system':
+            return 'SYSTEM';
+        case 'tool':
+            return 'TOOL';
+        case 'developer':
+            return 'SYSTEM';
+        default:
+            return 'USER';
+    }
+}
+
+/**
+ * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
+ */
+function createFrozenOpenAiCompatibilityMessages(
+    rawMessages: ReadonlyArray<unknown>,
+    assistantContent?: string,
+    options: {
+        includeAssistantPlaceholder?: boolean;
+    } = {},
+): Array<ChatMessage> {
+    const startedAt = Date.now();
+    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
+        const message = rawMessage as OpenAiCompatibilityMessageLike;
+
+        return {
+            id: `openai-frozen-${index}`,
+            sender: mapOpenAiCompatibilityRoleToSender(message.role),
+            content: normalizeOpenAiCompatibilityMessageContent(message.content),
+            isComplete: true,
+            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage;
+    });
+
+    if (assistantContent !== undefined) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: assistantContent,
+            isComplete: true,
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    } else if (options.includeAssistantPlaceholder) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: '',
+            isComplete: false,
+            lifecycleState: 'running',
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    }
+
+    return messages;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fd44b7ea5a87168725a9e99784038137c96537ab
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
@@ -0,0 +1,305 @@
+import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
+import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
+import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
+import type { string_book, TODO_any } from '@promptbook-local/types';
+import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
+import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
+import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
+import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
+import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
+import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
+import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
+import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
+import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
+import {
+    createBookScopedAgentReferenceResolver,
+    parseBookScopedAgentIdentifier,
+} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
+import { resolveMessageSuffixFromAgentSource } from '@/src/utils/chat/messageSuffix';
+import { NextRequest, NextResponse } from 'next/server';
+import { isAgentDeleted } from '../../app/agents/[agentName]/_utils';
+import {
+    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    resolveAgentCollectionTablePrefix,
+    waitForRunningAgentPreparation,
+} from '../agentPreparation';
+import { resolveCachedServerAgentContext, resolveCachedServerAgentModelRequirements } from '../cachedServerAgentRuntime';
+import { AgentKitCacheManager } from '../cache/AgentKitCacheManager';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Runtime state resolved before the chat model is executed.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionRuntime = {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    agent: Agent;
+    agentSource: string_book;
+    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
+    messageSuffix: string | null;
+    agentId: string;
+    agentHash: string;
+    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Resolves agent/runtime dependencies needed before prompt execution.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function resolveHandleChatCompletionRuntime(options: {
+    request: NextRequest;
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    isPrivateModeEnabled: boolean;
+}): Promise<HandleChatCompletionRuntime | NextResponse> {
+    const { request, agentName, messages, isPrivateModeEnabled } = options;
+    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
+    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
+
+    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
+        return createHandleChatCompletionErrorResponse(
+            'This agent has been deleted. You can restore it from the Recycle Bin.',
+            'agent_deleted',
+            410,
+        );
+    }
+
+    const [collection, baseAgentReferenceResolver] = await Promise.all([
+        $provideAgentCollectionForServer(),
+        $provideAgentReferenceResolver(),
+    ]);
+    const localServerUrl = new URL(request.url).origin;
+    const resolvedAgentContext = await resolveCachedServerAgentContext({
+        collection,
+        agentIdentifier: agentName,
+        localServerUrl,
+        fallbackResolver: baseAgentReferenceResolver,
+    }).catch(() => null);
+
+    if (!resolvedAgentContext?.resolvedAgentSource) {
+        return createHandleChatCompletionErrorResponse(`Agent '${agentName}' not found.`, 'invalid_request_error', 404);
+    }
+
+    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
+    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
+    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
+    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
+    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
+    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
+    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
+    agentSource = agentSourceWithContext;
+
+    const preparedAgentModelRequirements = !hasDynamicContext
+        ? await resolveCachedServerAgentModelRequirements({
+              resolvedAgentContext,
+              localServerUrl,
+              fallbackResolver: baseAgentReferenceResolver,
+          })
+        : null;
+    const agentId = resolvedAgentContext.parentAgentPermanentId;
+    const agentHash = computeAgentHash(agentSource);
+    await waitForHandleChatCompletionAgentPreparation({
+        collection,
+        agentId,
+        agentHash,
+    });
+
+    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
+    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
+        await resolveHandleChatCompletionCredentials({
+            currentUserIdentity,
+            agentId,
+            calendarConnections,
+            useEmailConfiguration,
+        });
+    const agent = await createHandleChatCompletionAgent({
+        agentName,
+        agentSource,
+        agentId,
+        resolvedAgentContext,
+        localServerUrl,
+        baseAgentReferenceResolver,
+        hasDynamicContext,
+        preparedAgentModelRequirements,
+    });
+
+    return {
+        collection,
+        resolvedAgentContext,
+        agent,
+        agentSource,
+        unresolvedAgentSource,
+        messageSuffix,
+        agentId,
+        agentHash,
+        projectRepositories,
+        calendarConnections,
+        useEmailConfiguration,
+        currentUserIdentity,
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+        isPrivateModeEnabled,
+    };
+}
+
+/**
+ * Appends system messages to the agent source as dynamic `CONTEXT`.
+ */
+function appendSystemMessagesToAgentSource(
+    agentSource: string_book,
+    messages: ReadonlyArray<TODO_any>,
+): {
+    agentSourceWithContext: string_book;
+    hasDynamicContext: boolean;
+} {
+    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
+    if (systemMessages.length === 0) {
+        return {
+            agentSourceWithContext: agentSource,
+            hasDynamicContext: false,
+        };
+    }
+
+    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
+    return {
+        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
+        hasDynamicContext: true,
+    };
+}
+
+/**
+ * Waits for any running preparation job of the current agent before executing the chat call.
+ */
+async function waitForHandleChatCompletionAgentPreparation(options: {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    agentId: string;
+    agentHash: string;
+}): Promise<void> {
+    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
+    const preparationWaitResult = await waitForRunningAgentPreparation({
+        tablePrefix,
+        agentPermanentId: options.agentId,
+        fingerprint: options.agentHash,
+        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    });
+
+    if (preparationWaitResult !== 'not_running') {
+        console.info('[pre-index]', 'openai_chat_wait_result', {
+            tablePrefix,
+            agentPermanentId: options.agentId,
+            agentHash: options.agentHash,
+            preparationWaitResult,
+        });
+    }
+}
+
+/**
+ * Resolves request-scoped project, calendar, and email credentials for the current agent.
+ */
+async function resolveHandleChatCompletionCredentials(options: {
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    agentId: string;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+}): Promise<{
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+}> {
+    const userId = options.currentUserIdentity?.userId;
+    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
+        resolveUseProjectGithubToken({
+            userId,
+            agentPermanentId: options.agentId,
+        }),
+        options.calendarConnections.length > 0
+            ? resolveUseCalendarGoogleToken({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+        options.useEmailConfiguration.isEnabled
+            ? resolveUseEmailSmtpCredential({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+    ]);
+
+    return {
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+    };
+}
+
+/**
+ * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
+ */
+async function createHandleChatCompletionAgent(options: {
+    agentName: string;
+    agentSource: string_book;
+    agentId: string;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    localServerUrl: string;
+    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
+    hasDynamicContext: boolean;
+    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
+}): Promise<Agent> {
+    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
+    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
+    const runtimeAgentReferenceResolver = options.hasDynamicContext
+        ? createBookScopedAgentReferenceResolver({
+              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
+              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
+              localServerUrl: options.localServerUrl,
+              fallbackResolver: options.baseAgentReferenceResolver,
+          })
+        : undefined;
+    const agentModelRequirements =
+        options.preparedAgentModelRequirements?.modelRequirements ||
+        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
+            agentReferenceResolver: runtimeAgentReferenceResolver,
+            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
+        }));
+
+    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
+        options.agentSource,
+        options.resolvedAgentContext.resolvedAgentName,
+        await baseOpenAiToolsPromise,
+        {
+            includeDynamicContext: true,
+            agentId: options.agentId,
+            modelRequirements: agentModelRequirements,
+        },
+    );
+
+    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
+        agentName: options.agentName,
+        assistantCacheKey: agentKitResult.assistantCacheKey,
+        vectorStoreHash: agentKitResult.vectorStoreHash,
+        vectorStoreId: agentKitResult.vectorStoreId,
+    });
+
+    return new Agent({
+        agentSource: options.agentSource,
+        executionTools: {
+            llm: agentKitResult.tools,
+        },
+        precomputedModelRequirements: agentModelRequirements,
+        assistantPreparationMode: 'external',
+        isVerbose: true,
+        teacherAgent: null,
+    });
+}

file update:
diff --git a/apps/agents-server/src/utils/handleChatCompletion.ts b/apps/agents-server/src/utils/handleChatCompletion.ts
index 9c96d36fad9cf24bf26d89f57372332add4e4595..cff56fafd6cfa90bb4094f9f202c4f01f6f74af1
--- a/apps/agents-server/src/utils/handleChatCompletion.ts
+++ b/apps/agents-server/src/utils/handleChatCompletion.ts
@@ -1,392 +1,16 @@
-import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
-import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
-import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
-import {
-    createBookScopedAgentReferenceResolver,
-    parseBookScopedAgentIdentifier,
-} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
-import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
-import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
-import {
-    appendMessageSuffix,
-    createMessageSuffixAppendix,
-    emulateMessageSuffixStreaming,
-    resolveMessageSuffixFromAgentSource,
-} from '@/src/utils/chat/messageSuffix';
-import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
-import { prepareToolCallsForStreaming } from './toolCallStreaming';
-import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
-import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
-import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
-import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
-import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
-import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
-import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
-import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
-import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
-import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
-import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
-import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
-import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
-import type {
-    ChatMessage,
-    ChatPrompt,
-    LlmToolDefinition,
-    string_book,
-    ToolCall,
-    TODO_any,
-    UncertainNumber,
-    Usage,
-    UsageCounts,
-} from '@promptbook-local/types';
-import { $getCurrentDate } from '@promptbook-local/utils';
 import { NextRequest, NextResponse } from 'next/server';
-import type OpenAI from 'openai';
-import { computeUsageCounts } from '../../../../src/execution/utils/computeUsageCounts';
-import { encodeChatStreamWhitespaceForTransport } from '../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
-import { isAgentDeleted } from '../app/agents/[agentName]/_utils';
 import { HTTP_STATUS_CODES } from '../constants';
-import { AgentKitCacheManager } from './cache/AgentKitCacheManager';
 import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
+import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
+import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
+import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
+import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
+import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
 import { validateApiKey } from './validateApiKey';
-import {
-    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    resolveAgentCollectionTablePrefix,
-    waitForRunningAgentPreparation,
-} from './agentPreparation';
-import {
-    resolveCachedServerAgentContext,
-    resolveCachedServerAgentModelRequirements,
-} from './cachedServerAgentRuntime';
-import { resolveAppendOnlySelfLearningAgentSource } from './resolveAppendOnlySelfLearningAgentSource';
 
 /**
- * Falls back to the estimated value when the original token count is unknown.
- *
- * @param tokenCount - Token count reported by the execution tools.
- * @param fallbackValue - Estimated token count based on text length.
- * @returns Token count to report in the OpenAI-compatible response.
- */
-function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
-    if (tokenCount.value === 0 && tokenCount.isUncertain) {
-        return {
-            value: fallbackValue,
-            isUncertain: true,
-        };
-    }
-
-    return tokenCount;
-}
-
-/**
- * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
- *
- * @param promptContent - Prompt content used for the request.
- * @param completionContent - Assistant response content.
- * @param usage - Native Promptbook usage details.
- * @returns Usage payload for compatibility responses.
- */
-function createCompatibilityUsage(
-    promptContent: string,
-    completionContent: string,
-    usage: Usage,
-): {
-    prompt_tokens: number;
-    completion_tokens: number;
-    total_tokens: number;
-    details: Usage;
-} {
-    const promptCounts = computeUsageCounts(promptContent);
-    const completionCounts = computeUsageCounts(completionContent);
-    const inputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
-        ...promptCounts,
-    };
-    const outputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
-        ...completionCounts,
-    };
-
-    const promptTokens = inputUsage.tokensCount.value;
-    const completionTokens = outputUsage.tokensCount.value;
-
-    return {
-        prompt_tokens: promptTokens,
-        completion_tokens: completionTokens,
-        total_tokens: promptTokens + completionTokens,
-        details: {
-            price: usage.price,
-            duration: usage.duration,
-            input: inputUsage,
-            output: outputUsage,
-        },
-    };
-}
-
-/**
- * Definition of open AI chat tool.
- */
-type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
-
-/**
- * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
- */
-type OpenAiCompatibilityMessageLike = {
-    role?: unknown;
-    content?: unknown;
-};
-
-/**
- * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
- */
-type HandleChatCompletionParsedRequest = {
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    threadMessages: ReadonlyArray<TODO_any>;
-    stream: TODO_any;
-    model: TODO_any;
-    responseFormat: TODO_any;
-    runtimeTools?: Array<LlmToolDefinition>;
-    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-    incomingParameters: Record<string, unknown>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Runtime state resolved before the chat model is executed.
- */
-type HandleChatCompletionRuntime = {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    agent: Agent;
-    agentSource: string_book;
-    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
-    messageSuffix: string | null;
-    agentId: string;
-    agentHash: string;
-    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Prompt, history recorder, and persistence context created for one chat call.
- */
-type HandleChatCompletionPromptContext = {
-    prompt: ChatPrompt;
-    userMessageHash: string;
-    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
-    persistedFrozenChatId?: string;
-};
-
-/**
- * Handles convert open Ai tools.
- */
-function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
-    if (!Array.isArray(rawTools)) {
-        return undefined;
-    }
-
-    const converted = rawTools
-        .map((tool) => convertOpenAiTool(tool))
-        .filter((tool): tool is LlmToolDefinition => tool !== null);
-
-    return converted.length > 0 ? converted : undefined;
-}
-
-/**
- * Handles convert open Ai tool.
- */
-function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
-    const tool = rawTool as OpenAIChatToolDefinition;
-    if (!tool || tool.type !== 'function') {
-        return null;
-    }
-
-    const functionDefinition = tool.function;
-    if (!functionDefinition) {
-        return null;
-    }
-
-    const { name, description, parameters } = functionDefinition;
-    if (typeof name !== 'string' || name.trim().length === 0) {
-        return null;
-    }
-
-    const parameterSchema = parameters ?? {};
-    if (parameterSchema.type !== 'object') {
-        return null;
-    }
-
-    const properties = parameterSchema.properties ?? {};
-    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
-
-    for (const [key, value] of Object.entries(properties)) {
-        if (!value || typeof value !== 'object') {
-            continue;
-        }
-
-        normalizedProperties[key] = {
-            type: typeof value.type === 'string' ? value.type : 'string',
-            description: typeof value.description === 'string' ? value.description : undefined,
-        };
-    }
-
-    const required =
-        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
-            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
-            : undefined;
-
-    return {
-        name,
-        description: typeof description === 'string' ? description : '',
-        parameters: {
-            type: 'object',
-            properties: normalizedProperties,
-            required,
-            additionalProperties:
-                typeof parameterSchema.additionalProperties === 'boolean'
-                    ? parameterSchema.additionalProperties
-                    : undefined,
-        },
-    };
-}
-
-/**
- * Parses open Ai tool choice.
- */
-function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
-    if (value === undefined || value === null) {
-        return undefined;
-    }
-
-    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-}
-
-/**
- * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
- */
-function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
-    if (typeof value === 'string') {
-        return value;
-    }
-
-    if (Array.isArray(value)) {
-        return value
-            .map((part) => {
-                if (typeof part === 'string') {
-                    return part;
-                }
-
-                if (!part || typeof part !== 'object') {
-                    return '';
-                }
-
-                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
-                if (typeof partLike.text === 'string') {
-                    return partLike.text;
-                }
-
-                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
-                    return '[Image input]';
-                }
-
-                if (partLike.image_url) {
-                    return '[Image input]';
-                }
-
-                return '';
-            })
-            .filter(Boolean)
-            .join('\n');
-    }
-
-    if (value === null || value === undefined) {
-        return '';
-    }
-
-    try {
-        return JSON.stringify(value, null, 2);
-    } catch {
-        return String(value);
-    }
-}
-
-/**
- * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ * Handles convert open Ai chat completion requests routed through the Agents Server.
  */
-function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
-    if (typeof role !== 'string') {
-        return 'USER';
-    }
-
-    switch (role) {
-        case 'assistant':
-            return 'AGENT';
-        case 'system':
-            return 'SYSTEM';
-        case 'tool':
-            return 'TOOL';
-        case 'developer':
-            return 'SYSTEM';
-        default:
-            return 'USER';
-    }
-}
-
-/**
- * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
- */
-function createFrozenOpenAiCompatibilityMessages(
-    rawMessages: ReadonlyArray<unknown>,
-    assistantContent?: string,
-    options: {
-        includeAssistantPlaceholder?: boolean;
-    } = {},
-): Array<ChatMessage> {
-    const startedAt = Date.now();
-    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
-        const message = rawMessage as OpenAiCompatibilityMessageLike;
-
-        return {
-            id: `openai-frozen-${index}`,
-            sender: mapOpenAiCompatibilityRoleToSender(message.role),
-            content: normalizeOpenAiCompatibilityMessageContent(message.content),
-            isComplete: true,
-            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage;
-    });
-
-    if (assistantContent !== undefined) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: assistantContent,
-            isComplete: true,
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    } else if (options.includeAssistantPlaceholder) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: '',
-            isComplete: false,
-            lifecycleState: 'running',
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    }
-
-    return messages;
-}
-
-/**
- * Handles chat completion.
- */
 export async function handleChatCompletion(
     request: NextRequest,
     params: { agentName?: string },
@@ -399,7 +23,6 @@
         return versionMismatchResponse;
     }
 
-    // Validate API key explicitly (in addition to middleware)
     const apiKeyValidation = await validateApiKey(request);
     if (!apiKeyValidation.isValid) {
         return NextResponse.json(
@@ -462,724 +85,5 @@
             { error: { message: (error as Error).message || 'Internal Server Error', type: 'server_error' } },
             { status: HTTP_STATUS_CODES.INTERNAL_SERVER_ERROR },
         );
-    }
-}
-
-/**
- * Parses and validates the OpenAI-compatible request payload.
- */
-function parseHandleChatCompletionRequest(options: {
-    body: TODO_any;
-    agentNameFromParams?: string;
-    request: NextRequest;
-}): HandleChatCompletionParsedRequest | NextResponse {
-    const { body, agentNameFromParams, request } = options;
-    const {
-        messages,
-        stream,
-        model,
-        response_format: responseFormat,
-        tools: rawTools,
-        tool_choice: toolChoice,
-        parameters: rawParameters = {},
-    } = body;
-    const agentName = (agentNameFromParams || model) as string | undefined;
-    if (!agentName) {
-        return createHandleChatCompletionErrorResponse(
-            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    if (!messages || !Array.isArray(messages) || messages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array is required and cannot be empty.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
-    if (threadMessages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array must contain at least one non-system message.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    return {
-        agentName,
-        messages,
-        threadMessages,
-        stream,
-        model,
-        responseFormat,
-        runtimeTools: convertOpenAiTools(rawTools),
-        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
-        incomingParameters:
-            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
-                ? (rawParameters as Record<string, unknown>)
-                : {},
-        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
-    };
-}
-
-/**
- * Resolves agent/runtime dependencies needed before prompt execution.
- */
-async function resolveHandleChatCompletionRuntime(options: {
-    request: NextRequest;
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    isPrivateModeEnabled: boolean;
-}): Promise<HandleChatCompletionRuntime | NextResponse> {
-    const { request, agentName, messages, isPrivateModeEnabled } = options;
-    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
-    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
-
-    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
-        return createHandleChatCompletionErrorResponse(
-            'This agent has been deleted. You can restore it from the Recycle Bin.',
-            'agent_deleted',
-            410,
-        );
-    }
-
-    const [collection, baseAgentReferenceResolver] = await Promise.all([
-        $provideAgentCollectionForServer(),
-        $provideAgentReferenceResolver(),
-    ]);
-    const localServerUrl = new URL(request.url).origin;
-    const resolvedAgentContext = await resolveCachedServerAgentContext({
-        collection,
-        agentIdentifier: agentName,
-        localServerUrl,
-        fallbackResolver: baseAgentReferenceResolver,
-    }).catch(() => null);
-
-    if (!resolvedAgentContext?.resolvedAgentSource) {
-        return createHandleChatCompletionErrorResponse(
-            `Agent '${agentName}' not found.`,
-            'invalid_request_error',
-            HTTP_STATUS_CODES.NOT_FOUND,
-        );
-    }
-
-    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
-    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
-    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
-    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
-    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
-    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
-    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
-    agentSource = agentSourceWithContext;
-
-    const preparedAgentModelRequirements = !hasDynamicContext
-        ? await resolveCachedServerAgentModelRequirements({
-              resolvedAgentContext,
-              localServerUrl,
-              fallbackResolver: baseAgentReferenceResolver,
-          })
-        : null;
-    const agentId = resolvedAgentContext.parentAgentPermanentId;
-    const agentHash = computeAgentHash(agentSource);
-    await waitForHandleChatCompletionAgentPreparation({
-        collection,
-        agentId,
-        agentHash,
-    });
-
-    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
-    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
-        await resolveHandleChatCompletionCredentials({
-            currentUserIdentity,
-            agentId,
-            calendarConnections,
-            useEmailConfiguration,
-        });
-    const agent = await createHandleChatCompletionAgent({
-        agentName,
-        agentSource,
-        agentId,
-        resolvedAgentContext,
-        localServerUrl,
-        baseAgentReferenceResolver,
-        hasDynamicContext,
-        preparedAgentModelRequirements,
-    });
-
-    return {
-        collection,
-        resolvedAgentContext,
-        agent,
-        agentSource,
-        unresolvedAgentSource,
-        messageSuffix,
-        agentId,
-        agentHash,
-        projectRepositories,
-        calendarConnections,
-        useEmailConfiguration,
-        currentUserIdentity,
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-        isPrivateModeEnabled,
-    };
-}
-
-/**
- * Creates prompt and persistence helpers for one chat completion request.
- */
-async function createHandleChatCompletionPromptContext(options: {
-    request: NextRequest;
-    title: string;
-    apiKey: string | null;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-}): Promise<HandleChatCompletionPromptContext> {
-    const { request, title, apiKey, parsedRequest, runtime } = options;
-    const timezone = request.headers.get('x-timezone') || 'UTC';
-    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
-    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
-    const recordChatHistoryMessage = await createChatHistoryRecorder({
-        request,
-        agentIdentifier: runtime.agentId,
-        agentHash: runtime.agentHash,
-        source: 'OPENAI_API_COMPATIBILITY',
-        apiKey,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        isEnabled: !runtime.isPrivateModeEnabled,
-    });
-    const userMessageHash = await recordChatHistoryMessage({
-        message: {
-            role: 'USER',
-            content: lastMessage.content,
-        },
-        previousMessageHash: null,
-    });
-    const promptParameters = composePromptParametersWithMemoryContext({
-        baseParameters: {
-            ...parsedRequest.incomingParameters,
-            timezone,
-        },
-        currentUserIdentity: runtime.currentUserIdentity,
-        agentPermanentId: runtime.agentId,
-        agentName: runtime.resolvedAgentContext.resolvedAgentName,
-        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
-        projectRepositories: runtime.projectRepositories,
-        projectGithubToken: runtime.projectGithubToken,
-        emailSmtpCredential: runtime.emailSmtpCredential,
-        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
-        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
-        calendarConnections: runtime.calendarConnections,
-    });
-    const prompt: ChatPrompt = {
-        title,
-        content: lastMessage.content,
-        modelRequirements: {
-            modelVariant: 'CHAT',
-            responseFormat: parsedRequest.responseFormat,
-            toolChoice: parsedRequest.runtimeToolChoice,
-            // We could pass 'model' from body if we wanted to enforce it, but Agent usually has its own config
-        },
-        parameters: promptParameters,
-        thread,
-        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
-    };
-
-    return {
-        prompt,
-        userMessageHash,
-        recordChatHistoryMessage,
-        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
-            isEnabled: !runtime.isPrivateModeEnabled,
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentId: runtime.agentId,
-            messages: parsedRequest.messages,
-            includeAssistantPlaceholder: true,
-            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
-        }),
-    };
-}
-
-/**
- * Handles the streaming OpenAI-compatible response branch.
- */
-function createHandleChatCompletionStreamResponse(options: {
-    request: NextRequest;
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Response {
-    const { request, title, parsedRequest, runtime, promptContext } = options;
-    const encoder = new TextEncoder();
-    const responseModel = parsedRequest.model || 'promptbook-agent';
-    const readableStream = new ReadableStream({
-        async start(controller) {
-            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
-            const created = Math.floor(Date.now() / 1000);
-            const emitSsePayload = (payload: unknown) => {
-                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
-            };
-            const emitDeltaChunk = (deltaContent: string) => {
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {
-                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
-                        },
-                        finishReason: null,
-                    }),
-                );
-            };
-
-            emitSsePayload(
-                createHandleChatCompletionChunk({
-                    runId,
-                    created,
-                    model: responseModel,
-                    delta: {
-                        role: 'assistant',
-                        content: '',
-                    },
-                    finishReason: null,
-                }),
-            );
-
-            let hasMeaningfulDelta = false;
-
-            try {
-                const handleStreamChunk = createChatStreamHandler({
-                    onDelta: (deltaContent) => {
-                        if (deltaContent.trim().length > 0) {
-                            hasMeaningfulDelta = true;
-                        }
-                        emitDeltaChunk(deltaContent);
-                    },
-                    onToolCalls: (toolCalls) => {
-                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
-                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
-                    },
-                });
-                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
-                    signal: request.signal,
-                });
-                const normalizedResponse = ensureNonEmptyChatContent({
-                    content: result.content,
-                    context: title,
-                });
-
-                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
-                    emitDeltaChunk(normalizedResponse.content);
-                }
-
-                const messageSuffixAppendix = createMessageSuffixAppendix(
-                    normalizedResponse.content,
-                    runtime.messageSuffix,
-                );
-                if (messageSuffixAppendix) {
-                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
-                        emitDeltaChunk(delta);
-                    });
-                }
-
-                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-                await finalizeHandleChatCompletionResult({
-                    responseContentWithSuffix,
-                    usage: result.usage,
-                    toolCalls: result.toolCalls,
-                    parsedRequest,
-                    runtime,
-                    promptContext,
-                    // Preserve historical behavior: streamed OpenAI-compatible calls still persist learning in private mode.
-                    shouldPersistLearnedAgentSource: true,
-                });
-
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {},
-                        finishReason: 'stop',
-                    }),
-                );
-                controller.enqueue(encoder.encode('[DONE]'));
-            } catch (error) {
-                console.error('Error during streaming:', error);
-                controller.error(error);
-            }
-            controller.close();
-        },
-    });
-
-    return new Response(readableStream, {
-        headers: {
-            'Content-Type': 'text/event-stream',
-            'Cache-Control': 'no-cache',
-            Connection: 'keep-alive',
-        },
-    });
-}
-
-/**
- * Handles the non-streaming OpenAI-compatible response branch.
- */
-async function createHandleChatCompletionJsonResponse(options: {
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Promise<NextResponse> {
-    const { title, parsedRequest, runtime, promptContext } = options;
-    const result = await runtime.agent.callChatModel(promptContext.prompt);
-    const normalizedResponse = ensureNonEmptyChatContent({
-        content: result.content,
-        context: title,
-    });
-    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-
-    await finalizeHandleChatCompletionResult({
-        responseContentWithSuffix,
-        usage: result.usage,
-        toolCalls: result.toolCalls,
-        parsedRequest,
-        runtime,
-        promptContext,
-        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
-    });
-
-    return NextResponse.json({
-        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
-        object: 'chat.completion',
-        created: Math.floor(Date.now() / 1000),
-        model: parsedRequest.model || 'promptbook-agent',
-        choices: [
-            {
-                index: 0,
-                message: {
-                    role: 'assistant',
-                    content: responseContentWithSuffix,
-                },
-                finish_reason: 'stop',
-            },
-        ],
-        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
-    });
-}
-
-/**
- * Runs the shared side effects after one successful model response.
- */
-async function finalizeHandleChatCompletionResult(options: {
-    responseContentWithSuffix: string;
-    usage: Usage;
-    toolCalls?: ReadonlyArray<ToolCall>;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
-
-    await promptContext.recordChatHistoryMessage({
-        message: {
-            role: 'MODEL',
-            content: responseContentWithSuffix,
-        },
-        previousMessageHash: promptContext.userMessageHash,
-        usage,
-    });
-
-    if (toolCalls && toolCalls.length > 0) {
-        await logCalendarToolCallsActivity({
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentPermanentId: runtime.agentId,
-            toolCalls,
-        });
-    }
-
-    await persistOpenAiCompatibilityFrozenChat({
-        isEnabled: !runtime.isPrivateModeEnabled,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        agentId: runtime.agentId,
-        chatId: promptContext.persistedFrozenChatId,
-        messages: parsedRequest.messages,
-        assistantContent: responseContentWithSuffix,
-        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
-    });
-    await persistHandleChatCompletionLearnedAgentSource({
-        runtime,
-        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
-    });
-}
-
-/**
- * Applies append-only self-learning updates when the current branch should persist them.
- */
-async function persistHandleChatCompletionLearnedAgentSource(options: {
-    runtime: HandleChatCompletionRuntime;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { runtime, shouldPersistLearnedAgentSource } = options;
-    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
-        return;
-    }
-
-    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
-        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
-        resolvedAgentSourceBeforeLearning: runtime.agentSource,
-        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
-    });
-
-    if (learnedAgentSource !== null) {
-        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
     }
 }
-
-/**
- * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
- */
-async function persistOpenAiCompatibilityFrozenChat(options: {
-    isEnabled: boolean;
-    userId: number | null | undefined;
-    agentId: string;
-    messages: ReadonlyArray<unknown>;
-    chatId?: string;
-    assistantContent?: string;
-    includeAssistantPlaceholder?: boolean;
-    failureMessage: string;
-}): Promise<string | undefined> {
-    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
-        options;
-    if (!isEnabled || !userId) {
-        return undefined;
-    }
-
-    const persistedFrozenChat = await persistFrozenUserChat({
-        userId,
-        agentPermanentId: agentId,
-        source: USER_CHAT_SOURCES.OPENAI_API,
-        chatId,
-        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
-            includeAssistantPlaceholder,
-        }),
-    }).catch((error) => {
-        console.error(failureMessage, error);
-        return null;
-    });
-
-    return persistedFrozenChat?.id;
-}
-
-/**
- * Builds one OpenAI-compatible thread from the earlier request messages.
- */
-function createHandleChatCompletionThread(previousMessages: ReadonlyArray<TODO_any>): ChatMessage[] {
-    return previousMessages.map((message: TODO_any, index: number) => ({
-        id: `msg-${index}`,
-        sender: message.role === 'assistant' ? 'agent' : 'user',
-        content: message.content,
-        isComplete: true,
-        createdAt: $getCurrentDate(),
-    }));
-}
-
-/**
- * Appends system messages to the agent source as dynamic `CONTEXT`.
- */
-function appendSystemMessagesToAgentSource(
-    agentSource: string_book,
-    messages: ReadonlyArray<TODO_any>,
-): {
-    agentSourceWithContext: string_book;
-    hasDynamicContext: boolean;
-} {
-    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
-    if (systemMessages.length === 0) {
-        return {
-            agentSourceWithContext: agentSource,
-            hasDynamicContext: false,
-        };
-    }
-
-    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
-    return {
-        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
-        hasDynamicContext: true,
-    };
-}
-
-/**
- * Waits for any running preparation job of the current agent before executing the chat call.
- */
-async function waitForHandleChatCompletionAgentPreparation(options: {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    agentId: string;
-    agentHash: string;
-}): Promise<void> {
-    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
-    const preparationWaitResult = await waitForRunningAgentPreparation({
-        tablePrefix,
-        agentPermanentId: options.agentId,
-        fingerprint: options.agentHash,
-        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    });
-
-    if (preparationWaitResult !== 'not_running') {
-        console.info('[pre-index]', 'openai_chat_wait_result', {
-            tablePrefix,
-            agentPermanentId: options.agentId,
-            agentHash: options.agentHash,
-            preparationWaitResult,
-        });
-    }
-}
-
-/**
- * Resolves request-scoped project, calendar, and email credentials for the current agent.
- */
-async function resolveHandleChatCompletionCredentials(options: {
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    agentId: string;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-}): Promise<{
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-}> {
-    const userId = options.currentUserIdentity?.userId;
-    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
-        resolveUseProjectGithubToken({
-            userId,
-            agentPermanentId: options.agentId,
-        }),
-        options.calendarConnections.length > 0
-            ? resolveUseCalendarGoogleToken({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-        options.useEmailConfiguration.isEnabled
-            ? resolveUseEmailSmtpCredential({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-    ]);
-
-    return {
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-    };
-}
-
-/**
- * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
- */
-async function createHandleChatCompletionAgent(options: {
-    agentName: string;
-    agentSource: string_book;
-    agentId: string;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    localServerUrl: string;
-    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
-    hasDynamicContext: boolean;
-    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
-}): Promise<Agent> {
-    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
-    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
-    const runtimeAgentReferenceResolver = options.hasDynamicContext
-        ? createBookScopedAgentReferenceResolver({
-              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
-              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
-              localServerUrl: options.localServerUrl,
-              fallbackResolver: options.baseAgentReferenceResolver,
-          })
-        : undefined;
-    const agentModelRequirements =
-        options.preparedAgentModelRequirements?.modelRequirements ||
-        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
-            agentReferenceResolver: runtimeAgentReferenceResolver,
-            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
-        }));
-
-    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
-        options.agentSource,
-        options.resolvedAgentContext.resolvedAgentName,
-        await baseOpenAiToolsPromise,
-        {
-            includeDynamicContext: true,
-            agentId: options.agentId,
-            modelRequirements: agentModelRequirements,
-        },
-    );
-
-    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
-        agentName: options.agentName,
-        assistantCacheKey: agentKitResult.assistantCacheKey,
-        vectorStoreHash: agentKitResult.vectorStoreHash,
-        vectorStoreId: agentKitResult.vectorStoreId,
-    });
-
-    return new Agent({
-        agentSource: options.agentSource,
-        executionTools: {
-            llm: agentKitResult.tools,
-        },
-        precomputedModelRequirements: agentModelRequirements,
-        assistantPreparationMode: 'external',
-        isVerbose: true,
-        teacherAgent: null,
-    });
-}
-
-/**
- * Creates one OpenAI-compatible streaming chunk payload.
- */
-function createHandleChatCompletionChunk(options: {
-    runId: string;
-    created: number;
-    model: TODO_any;
-    delta: Record<string, unknown>;
-    finishReason: 'stop' | null;
-}) {
-    return {
-        id: options.runId,
-        object: 'chat.completion.chunk',
-        created: options.created,
-        model: options.model,
-        choices: [
-            {
-                index: 0,
-                delta: options.delta,
-                finish_reason: options.finishReason,
-            },
-        ],
-    };
-}
-
-/**
- * Creates one consistent OpenAI-compatible error response.
- */
-function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
-    return NextResponse.json(
-        {
-            error: {
-                message,
-                type,
-            },
-        },
-        { status },
-    );
-}
-
-// TODO: [🈹] Maybe move chat thread handling here
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f49a4fa80fa21f140e25de59596e5546d62e3f7
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
@@ -0,0 +1,18 @@
+import { NextResponse } from 'next/server';
+
+/**
+ * Creates one consistent OpenAI-compatible error response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
+    return NextResponse.json(
+        {
+            error: {
+                message,
+                type,
+            },
+        },
+        { status },
+    );
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..7aac8141a8b1237ce36430b4b4598a618f0383ac
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
@@ -0,0 +1,111 @@
+import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
+import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
+import type { ChatMessage, ChatPrompt } from '@promptbook-local/types';
+import { $getCurrentDate } from '@promptbook-local/utils';
+import type { NextRequest } from 'next/server';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Prompt, history recorder, and persistence context created for one chat call.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionPromptContext = {
+    prompt: ChatPrompt;
+    userMessageHash: string;
+    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
+    persistedFrozenChatId?: string;
+};
+
+/**
+ * Creates prompt and persistence helpers for one chat completion request.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionPromptContext(options: {
+    request: NextRequest;
+    title: string;
+    apiKey: string | null;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+}): Promise<HandleChatCompletionPromptContext> {
+    const { request, title, apiKey, parsedRequest, runtime } = options;
+    const timezone = request.headers.get('x-timezone') || 'UTC';
+    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
+    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
+    const recordChatHistoryMessage = await createChatHistoryRecorder({
+        request,
+        agentIdentifier: runtime.agentId,
+        agentHash: runtime.agentHash,
+        source: 'OPENAI_API_COMPATIBILITY',
+        apiKey,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        isEnabled: !runtime.isPrivateModeEnabled,
+    });
+    const userMessageHash = await recordChatHistoryMessage({
+        message: {
+            role: 'USER',
+            content: lastMessage.content,
+        },
+        previousMessageHash: null,
+    });
+    const promptParameters = composePromptParametersWithMemoryContext({
+        baseParameters: {
+            ...parsedRequest.incomingParameters,
+            timezone,
+        },
+        currentUserIdentity: runtime.currentUserIdentity,
+        agentPermanentId: runtime.agentId,
+        agentName: runtime.resolvedAgentContext.resolvedAgentName,
+        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
+        projectRepositories: runtime.projectRepositories,
+        projectGithubToken: runtime.projectGithubToken,
+        emailSmtpCredential: runtime.emailSmtpCredential,
+        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
+        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
+        calendarConnections: runtime.calendarConnections,
+    });
+    const prompt: ChatPrompt = {
+        title,
+        content: lastMessage.content,
+        modelRequirements: {
+            modelVariant: 'CHAT',
+            responseFormat: parsedRequest.responseFormat,
+            toolChoice: parsedRequest.runtimeToolChoice,
+        },
+        parameters: promptParameters,
+        thread,
+        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
+    };
+
+    return {
+        prompt,
+        userMessageHash,
+        recordChatHistoryMessage,
+        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
+            isEnabled: !runtime.isPrivateModeEnabled,
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentId: runtime.agentId,
+            messages: parsedRequest.messages,
+            includeAssistantPlaceholder: true,
+            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
+        }),
+    };
+}
+
+/**
+ * Builds one OpenAI-compatible thread from the earlier request messages.
+ */
+function createHandleChatCompletionThread(
+    previousMessages: HandleChatCompletionParsedRequest['threadMessages'],
+): Array<ChatMessage> {
+    return previousMessages.map((message, index) => ({
+        id: `msg-${index}`,
+        sender: message.role === 'assistant' ? 'agent' : 'user',
+        content: message.content,
+        isComplete: true,
+        createdAt: $getCurrentDate(),
+    }));
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
new file mode 100644
index 0000000000000000000000000000000000000000..dd6a7d87ea491df245dbf93d8bcb277a9a10b034
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
@@ -0,0 +1,78 @@
+import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
+import { resolveAppendOnlySelfLearningAgentSource } from '@/src/utils/resolveAppendOnlySelfLearningAgentSource';
+import type { ToolCall, Usage } from '@promptbook-local/types';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Runs the shared side effects after one successful model response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function finalizeHandleChatCompletionResult(options: {
+    responseContentWithSuffix: string;
+    usage: Usage;
+    toolCalls?: ReadonlyArray<ToolCall>;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
+
+    await promptContext.recordChatHistoryMessage({
+        message: {
+            role: 'MODEL',
+            content: responseContentWithSuffix,
+        },
+        previousMessageHash: promptContext.userMessageHash,
+        usage,
+    });
+
+    if (toolCalls && toolCalls.length > 0) {
+        await logCalendarToolCallsActivity({
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentPermanentId: runtime.agentId,
+            toolCalls,
+        });
+    }
+
+    await persistOpenAiCompatibilityFrozenChat({
+        isEnabled: !runtime.isPrivateModeEnabled,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        agentId: runtime.agentId,
+        chatId: promptContext.persistedFrozenChatId,
+        messages: parsedRequest.messages,
+        assistantContent: responseContentWithSuffix,
+        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
+    });
+    await persistHandleChatCompletionLearnedAgentSource({
+        runtime,
+        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
+    });
+}
+
+/**
+ * Applies append-only self-learning updates when the current branch should persist them.
+ */
+async function persistHandleChatCompletionLearnedAgentSource(options: {
+    runtime: HandleChatCompletionRuntime;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { runtime, shouldPersistLearnedAgentSource } = options;
+    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
+        return;
+    }
+
+    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
+        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
+        resolvedAgentSourceBeforeLearning: runtime.agentSource,
+        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
+    });
+
+    if (learnedAgentSource !== null) {
+        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
+    }
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
new file mode 100644
index 0000000000000000000000000000000000000000..58e1c26d7f428c1bdb39b36411b3f75256e3de51
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
@@ -0,0 +1,176 @@
+import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
+import type { LlmToolDefinition, TODO_any } from '@promptbook-local/types';
+import { NextRequest, NextResponse } from 'next/server';
+import type OpenAI from 'openai';
+import { HTTP_STATUS_CODES } from '../../constants';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Definition of open AI chat tool.
+ */
+type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
+
+/**
+ * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionParsedRequest = {
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    threadMessages: ReadonlyArray<TODO_any>;
+    stream: TODO_any;
+    model: TODO_any;
+    responseFormat: TODO_any;
+    runtimeTools?: Array<LlmToolDefinition>;
+    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+    incomingParameters: Record<string, unknown>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Parses and validates the OpenAI-compatible request payload.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function parseHandleChatCompletionRequest(options: {
+    body: TODO_any;
+    agentNameFromParams?: string;
+    request: NextRequest;
+}): HandleChatCompletionParsedRequest | NextResponse {
+    const { body, agentNameFromParams, request } = options;
+    const {
+        messages,
+        stream,
+        model,
+        response_format: responseFormat,
+        tools: rawTools,
+        tool_choice: toolChoice,
+        parameters: rawParameters = {},
+    } = body;
+    const agentName = (agentNameFromParams || model) as string | undefined;
+    if (!agentName) {
+        return createHandleChatCompletionErrorResponse(
+            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    if (!messages || !Array.isArray(messages) || messages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array is required and cannot be empty.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
+    if (threadMessages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array must contain at least one non-system message.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    return {
+        agentName,
+        messages,
+        threadMessages,
+        stream,
+        model,
+        responseFormat,
+        runtimeTools: convertOpenAiTools(rawTools),
+        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
+        incomingParameters:
+            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
+                ? (rawParameters as Record<string, unknown>)
+                : {},
+        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
+    };
+}
+
+/**
+ * Handles convert open Ai tools.
+ */
+function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
+    if (!Array.isArray(rawTools)) {
+        return undefined;
+    }
+
+    const converted = rawTools
+        .map((tool) => convertOpenAiTool(tool))
+        .filter((tool): tool is LlmToolDefinition => tool !== null);
+
+    return converted.length > 0 ? converted : undefined;
+}
+
+/**
+ * Handles convert open Ai tool.
+ */
+function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
+    const tool = rawTool as OpenAIChatToolDefinition;
+    if (!tool || tool.type !== 'function') {
+        return null;
+    }
+
+    const functionDefinition = tool.function;
+    if (!functionDefinition) {
+        return null;
+    }
+
+    const { name, description, parameters } = functionDefinition;
+    if (typeof name !== 'string' || name.trim().length === 0) {
+        return null;
+    }
+
+    const parameterSchema = parameters ?? {};
+    if (parameterSchema.type !== 'object') {
+        return null;
+    }
+
+    const properties = parameterSchema.properties ?? {};
+    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
+
+    for (const [key, value] of Object.entries(properties)) {
+        if (!value || typeof value !== 'object') {
+            continue;
+        }
+
+        normalizedProperties[key] = {
+            type: typeof value.type === 'string' ? value.type : 'string',
+            description: typeof value.description === 'string' ? value.description : undefined,
+        };
+    }
+
+    const required =
+        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
+            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
+            : undefined;
+
+    return {
+        name,
+        description: typeof description === 'string' ? description : '',
+        parameters: {
+            type: 'object',
+            properties: normalizedProperties,
+            required,
+            additionalProperties:
+                typeof parameterSchema.additionalProperties === 'boolean'
+                    ? parameterSchema.additionalProperties
+                    : undefined,
+        },
+    };
+}
+
+/**
+ * Parses open Ai tool choice.
+ */
+function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
+    if (value === undefined || value === null) {
+        return undefined;
+    }
+
+    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
new file mode 100644
index 0000000000000000000000000000000000000000..60e5f1b008bcf5a6a1836c06c4c3d9ebcb909a9f
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
@@ -0,0 +1,163 @@
+import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
+import type { ChatMessage } from '@promptbook-local/types';
+
+/**
+ * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
+ */
+type OpenAiCompatibilityMessageLike = {
+    role?: unknown;
+    content?: unknown;
+};
+
+/**
+ * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function persistOpenAiCompatibilityFrozenChat(options: {
+    isEnabled: boolean;
+    userId: number | null | undefined;
+    agentId: string;
+    messages: ReadonlyArray<unknown>;
+    chatId?: string;
+    assistantContent?: string;
+    includeAssistantPlaceholder?: boolean;
+    failureMessage: string;
+}): Promise<string | undefined> {
+    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
+        options;
+    if (!isEnabled || !userId) {
+        return undefined;
+    }
+
+    const persistedFrozenChat = await persistFrozenUserChat({
+        userId,
+        agentPermanentId: agentId,
+        source: USER_CHAT_SOURCES.OPENAI_API,
+        chatId,
+        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
+            includeAssistantPlaceholder,
+        }),
+    }).catch((error) => {
+        console.error(failureMessage, error);
+        return null;
+    });
+
+    return persistedFrozenChat?.id;
+}
+
+/**
+ * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
+ */
+function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
+    if (typeof value === 'string') {
+        return value;
+    }
+
+    if (Array.isArray(value)) {
+        return value
+            .map((part) => {
+                if (typeof part === 'string') {
+                    return part;
+                }
+
+                if (!part || typeof part !== 'object') {
+                    return '';
+                }
+
+                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
+                if (typeof partLike.text === 'string') {
+                    return partLike.text;
+                }
+
+                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
+                    return '[Image input]';
+                }
+
+                if (partLike.image_url) {
+                    return '[Image input]';
+                }
+
+                return '';
+            })
+            .filter(Boolean)
+            .join('\n');
+    }
+
+    if (value === null || value === undefined) {
+        return '';
+    }
+
+    try {
+        return JSON.stringify(value, null, 2);
+    } catch {
+        return String(value);
+    }
+}
+
+/**
+ * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ */
+function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
+    if (typeof role !== 'string') {
+        return 'USER';
+    }
+
+    switch (role) {
+        case 'assistant':
+            return 'AGENT';
+        case 'system':
+            return 'SYSTEM';
+        case 'tool':
+            return 'TOOL';
+        case 'developer':
+            return 'SYSTEM';
+        default:
+            return 'USER';
+    }
+}
+
+/**
+ * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
+ */
+function createFrozenOpenAiCompatibilityMessages(
+    rawMessages: ReadonlyArray<unknown>,
+    assistantContent?: string,
+    options: {
+        includeAssistantPlaceholder?: boolean;
+    } = {},
+): Array<ChatMessage> {
+    const startedAt = Date.now();
+    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
+        const message = rawMessage as OpenAiCompatibilityMessageLike;
+
+        return {
+            id: `openai-frozen-${index}`,
+            sender: mapOpenAiCompatibilityRoleToSender(message.role),
+            content: normalizeOpenAiCompatibilityMessageContent(message.content),
+            isComplete: true,
+            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage;
+    });
+
+    if (assistantContent !== undefined) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: assistantContent,
+            isComplete: true,
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    } else if (options.includeAssistantPlaceholder) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: '',
+            isComplete: false,
+            lifecycleState: 'running',
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    }
+
+    return messages;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fd44b7ea5a87168725a9e99784038137c96537ab
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
@@ -0,0 +1,305 @@
+import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
+import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
+import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
+import type { string_book, TODO_any } from '@promptbook-local/types';
+import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
+import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
+import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
+import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
+import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
+import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
+import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
+import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
+import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
+import {
+    createBookScopedAgentReferenceResolver,
+    parseBookScopedAgentIdentifier,
+} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
+import { resolveMessageSuffixFromAgentSource } from '@/src/utils/chat/messageSuffix';
+import { NextRequest, NextResponse } from 'next/server';
+import { isAgentDeleted } from '../../app/agents/[agentName]/_utils';
+import {
+    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    resolveAgentCollectionTablePrefix,
+    waitForRunningAgentPreparation,
+} from '../agentPreparation';
+import { resolveCachedServerAgentContext, resolveCachedServerAgentModelRequirements } from '../cachedServerAgentRuntime';
+import { AgentKitCacheManager } from '../cache/AgentKitCacheManager';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Runtime state resolved before the chat model is executed.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionRuntime = {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    agent: Agent;
+    agentSource: string_book;
+    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
+    messageSuffix: string | null;
+    agentId: string;
+    agentHash: string;
+    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Resolves agent/runtime dependencies needed before prompt execution.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function resolveHandleChatCompletionRuntime(options: {
+    request: NextRequest;
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    isPrivateModeEnabled: boolean;
+}): Promise<HandleChatCompletionRuntime | NextResponse> {
+    const { request, agentName, messages, isPrivateModeEnabled } = options;
+    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
+    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
+
+    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
+        return createHandleChatCompletionErrorResponse(
+            'This agent has been deleted. You can restore it from the Recycle Bin.',
+            'agent_deleted',
+            410,
+        );
+    }
+
+    const [collection, baseAgentReferenceResolver] = await Promise.all([
+        $provideAgentCollectionForServer(),
+        $provideAgentReferenceResolver(),
+    ]);
+    const localServerUrl = new URL(request.url).origin;
+    const resolvedAgentContext = await resolveCachedServerAgentContext({
+        collection,
+        agentIdentifier: agentName,
+        localServerUrl,
+        fallbackResolver: baseAgentReferenceResolver,
+    }).catch(() => null);
+
+    if (!resolvedAgentContext?.resolvedAgentSource) {
+        return createHandleChatCompletionErrorResponse(`Agent '${agentName}' not found.`, 'invalid_request_error', 404);
+    }
+
+    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
+    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
+    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
+    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
+    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
+    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
+    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
+    agentSource = agentSourceWithContext;
+
+    const preparedAgentModelRequirements = !hasDynamicContext
+        ? await resolveCachedServerAgentModelRequirements({
+              resolvedAgentContext,
+              localServerUrl,
+              fallbackResolver: baseAgentReferenceResolver,
+          })
+        : null;
+    const agentId = resolvedAgentContext.parentAgentPermanentId;
+    const agentHash = computeAgentHash(agentSource);
+    await waitForHandleChatCompletionAgentPreparation({
+        collection,
+        agentId,
+        agentHash,
+    });
+
+    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
+    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
+        await resolveHandleChatCompletionCredentials({
+            currentUserIdentity,
+            agentId,
+            calendarConnections,
+            useEmailConfiguration,
+        });
+    const agent = await createHandleChatCompletionAgent({
+        agentName,
+        agentSource,
+        agentId,
+        resolvedAgentContext,
+        localServerUrl,
+        baseAgentReferenceResolver,
+        hasDynamicContext,
+        preparedAgentModelRequirements,
+    });
+
+    return {
+        collection,
+        resolvedAgentContext,
+        agent,
+        agentSource,
+        unresolvedAgentSource,
+        messageSuffix,
+        agentId,
+        agentHash,
+        projectRepositories,
+        calendarConnections,
+        useEmailConfiguration,
+        currentUserIdentity,
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+        isPrivateModeEnabled,
+    };
+}
+
+/**
+ * Appends system messages to the agent source as dynamic `CONTEXT`.
+ */
+function appendSystemMessagesToAgentSource(
+    agentSource: string_book,
+    messages: ReadonlyArray<TODO_any>,
+): {
+    agentSourceWithContext: string_book;
+    hasDynamicContext: boolean;
+} {
+    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
+    if (systemMessages.length === 0) {
+        return {
+            agentSourceWithContext: agentSource,
+            hasDynamicContext: false,
+        };
+    }
+
+    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
+    return {
+        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
+        hasDynamicContext: true,
+    };
+}
+
+/**
+ * Waits for any running preparation job of the current agent before executing the chat call.
+ */
+async function waitForHandleChatCompletionAgentPreparation(options: {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    agentId: string;
+    agentHash: string;
+}): Promise<void> {
+    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
+    const preparationWaitResult = await waitForRunningAgentPreparation({
+        tablePrefix,
+        agentPermanentId: options.agentId,
+        fingerprint: options.agentHash,
+        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    });
+
+    if (preparationWaitResult !== 'not_running') {
+        console.info('[pre-index]', 'openai_chat_wait_result', {
+            tablePrefix,
+            agentPermanentId: options.agentId,
+            agentHash: options.agentHash,
+            preparationWaitResult,
+        });
+    }
+}
+
+/**
+ * Resolves request-scoped project, calendar, and email credentials for the current agent.
+ */
+async function resolveHandleChatCompletionCredentials(options: {
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    agentId: string;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+}): Promise<{
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+}> {
+    const userId = options.currentUserIdentity?.userId;
+    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
+        resolveUseProjectGithubToken({
+            userId,
+            agentPermanentId: options.agentId,
+        }),
+        options.calendarConnections.length > 0
+            ? resolveUseCalendarGoogleToken({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+        options.useEmailConfiguration.isEnabled
+            ? resolveUseEmailSmtpCredential({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+    ]);
+
+    return {
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+    };
+}
+
+/**
+ * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
+ */
+async function createHandleChatCompletionAgent(options: {
+    agentName: string;
+    agentSource: string_book;
+    agentId: string;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    localServerUrl: string;
+    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
+    hasDynamicContext: boolean;
+    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
+}): Promise<Agent> {
+    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
+    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
+    const runtimeAgentReferenceResolver = options.hasDynamicContext
+        ? createBookScopedAgentReferenceResolver({
+              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
+              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
+              localServerUrl: options.localServerUrl,
+              fallbackResolver: options.baseAgentReferenceResolver,
+          })
+        : undefined;
+    const agentModelRequirements =
+        options.preparedAgentModelRequirements?.modelRequirements ||
+        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
+            agentReferenceResolver: runtimeAgentReferenceResolver,
+            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
+        }));
+
+    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
+        options.agentSource,
+        options.resolvedAgentContext.resolvedAgentName,
+        await baseOpenAiToolsPromise,
+        {
+            includeDynamicContext: true,
+            agentId: options.agentId,
+            modelRequirements: agentModelRequirements,
+        },
+    );
+
+    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
+        agentName: options.agentName,
+        assistantCacheKey: agentKitResult.assistantCacheKey,
+        vectorStoreHash: agentKitResult.vectorStoreHash,
+        vectorStoreId: agentKitResult.vectorStoreId,
+    });
+
+    return new Agent({
+        agentSource: options.agentSource,
+        executionTools: {
+            llm: agentKitResult.tools,
+        },
+        precomputedModelRequirements: agentModelRequirements,
+        assistantPreparationMode: 'external',
+        isVerbose: true,
+        teacherAgent: null,
+    });
+}

file update
A C:/Users/me/work/ai/promptbook\apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
import {
    appendMessageSuffix,
    createMessageSuffixAppendix,
    emulateMessageSuffixStreaming,
} from '@/src/utils/chat/messageSuffix';
import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
import { encodeChatStreamWhitespaceForTransport } from '../../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
import { prepareToolCallsForStreaming } from '../toolCallStreaming';
import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';

/**
 * Handles the streaming OpenAI-compatible response branch.
 *
 * @private function of `handleChatCompletion`
 */
export function createHandleChatCompletionStreamResponse(options: {
    request: Request;
    title: string;
    parsedRequest: HandleChatCompletionParsedRequest;
    runtime: HandleChatCompletionRuntime;
    promptContext: HandleChatCompletionPromptContext;
}): Response {
    const { request, title, parsedRequest, runtime, promptContext } = options;
    const encoder = new TextEncoder();
    const responseModel = parsedRequest.model || 'promptbook-agent';
    const readableStream = new ReadableStream({
        async start(controller) {
            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
            const created = Math.floor(Date.now() / 1000);
            const emitSsePayload = (payload: unknown) => {
                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
            };
            const emitDeltaChunk = (deltaContent: string) => {
                emitSsePayload(
                    createHandleChatCompletionChunk({
                        runId,
                        created,
                        model: responseModel,
                        delta: {
                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
                        },
                        finishReason: null,
                    }),
                );
            };

            emitSsePayload(
                createHandleChatCompletionChunk({
                    runId,
                    created,
                    model: responseModel,
                    delta: {
                        role: 'assistant',
                        content: '',
                    },
                    finishReason: null,
                }),
            );

            let hasMeaningfulDelta = false;

            try {
                const handleStreamChunk = createChatStreamHandler({
                    onDelta: (deltaContent) => {
                        if (deltaContent.trim().length > 0) {
                            hasMeaningfulDelta = true;
                        }
                        emitDeltaChunk(deltaContent);
                    },
                    onToolCalls: (toolCalls) => {
                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
                    },
                });
                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
                    signal: request.signal,
                });
                const normalizedResponse = ensureNonEmptyChatContent({
                    content: result.content,
                    context: title,
                });

                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
                    emitDeltaChunk(normalizedResponse.content);
                }

                const messageSuffixAppendix = createMessageSuffixAppendix(
                    normalizedResponse.content,
                    runtime.messageSuffix,
                );
                if (messageSuffixAppendix) {
                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
                        emitDeltaChunk(delta);
                    });
                }

                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
                await finalizeHandleChatCompletionResult({
                    responseContentWithSuffix,
                    usage: result.usage,
                    toolCalls: result.toolCalls,
                    parsedRequest,
                    runtime,
                    promptContext,
                    shouldPersistLearnedAgentSource: true,
                });

                emitSsePayload(
                    createHandleChatCompletionChunk({
                        runId,
                        created,
                        model: responseModel,
                        delta: {},
                        finishReason: 'stop',
                    }),
                );
                controller.enqueue(encoder.encode('[DONE]'));
            } catch (error) {
                console.error('Error during streaming:', error);
                controller.error(error);
            }
            controller.close();
        },
    });

    return new Response(readableStream, {
        headers: {
            'Content-Type': 'text/event-stream',
            'Cache-Control': 'no-cache',
            Connection: 'keep-alive',
        },
    });
}

/**
 * Creates one OpenAI-compatible streaming chunk payload.
 */
function createHandleChatCompletionChunk(options: {
    runId: string;
    created: number;
    model: HandleChatCompletionParsedRequest['model'];
    delta: Record<string, unknown>;
    finishReason: 'stop' | null;
}) {
    return {
        id: options.runId,
        object: 'chat.completion.chunk',
        created: options.created,
        model: options.model,
        choices: [
            {
                index: 0,
                delta: options.delta,
                finish_reason: options.finishReason,
            },
        ],
    };
}
A C:/Users/me/work/ai/promptbook\apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
import { appendMessageSuffix } from '@/src/utils/chat/messageSuffix';
import type { UncertainNumber, Usage, UsageCounts } from '@promptbook-local/types';
import { NextResponse } from 'next/server';
import { computeUsageCounts } from '../../../../../src/execution/utils/computeUsageCounts';
import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';

/**
 * Handles the non-streaming OpenAI-compatible response branch.
 *
 * @private function of `handleChatCompletion`
 */
export async function createHandleChatCompletionJsonResponse(options: {
    title: string;
    parsedRequest: HandleChatCompletionParsedRequest;
    runtime: HandleChatCompletionRuntime;
    promptContext: HandleChatCompletionPromptContext;
}): Promise<NextResponse> {
    const { title, parsedRequest, runtime, promptContext } = options;
    const result = await runtime.agent.callChatModel(promptContext.prompt);
    const normalizedResponse = ensureNonEmptyChatContent({
        content: result.content,
        context: title,
    });
    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);

    await finalizeHandleChatCompletionResult({
        responseContentWithSuffix,
        usage: result.usage,
        toolCalls: result.toolCalls,
        parsedRequest,
        runtime,
        promptContext,
        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
    });

    return NextResponse.json({
        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
        object: 'chat.completion',
        created: Math.floor(Date.now() / 1000),
        model: parsedRequest.model || 'promptbook-agent',
        choices: [
            {
                index: 0,
                message: {
                    role: 'assistant',
                    content: responseContentWithSuffix,
                },
                finish_reason: 'stop',
            },
        ],
        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
    });
}

/**
 * Falls back to the estimated value when the original token count is unknown.
 *
 * @param tokenCount - Token count reported by the execution tools.
 * @param fallbackValue - Estimated token count based on text length.
 * @returns Token count to report in the OpenAI-compatible response.
 */
function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
    if (tokenCount.value === 0 && tokenCount.isUncertain) {
        return {
            value: fallbackValue,
            isUncertain: true,
        };
    }

    return tokenCount;
}

/**
 * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
 *
 * @param promptContent - Prompt content used for the request.
 * @param completionContent - Assistant response content.
 * @param usage - Native Promptbook usage details.
 * @returns Usage payload for compatibility responses.
 */
function createCompatibilityUsage(
    promptContent: string,
    completionContent: string,
    usage: Usage,
): {
    prompt_tokens: number;
    completion_tokens: number;
    total_tokens: number;
    details: Usage;
} {
    const promptCounts = computeUsageCounts(promptContent);
    const completionCounts = computeUsageCounts(completionContent);
    const inputUsage: UsageCounts = {
        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
        ...promptCounts,
    };
    const outputUsage: UsageCounts = {
        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
        ...completionCounts,
    };

    const promptTokens = inputUsage.tokensCount.value;
    const completionTokens = outputUsage.tokensCount.value;

    return {
        prompt_tokens: promptTokens,
        completion_tokens: completionTokens,
        total_tokens: promptTokens + completionTokens,
        details: {
            price: usage.price,
            duration: usage.duration,
            input: inputUsage,
            output: outputUsage,
        },
    };
}
apply_patch(auto_approved=true) exited 0 in 270ms:
Success. Updated the following files:
A apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
A apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
file update:
diff --git a/apps/agents-server/src/utils/handleChatCompletion.ts b/apps/agents-server/src/utils/handleChatCompletion.ts
index 9c96d36fad9cf24bf26d89f57372332add4e4595..cff56fafd6cfa90bb4094f9f202c4f01f6f74af1
--- a/apps/agents-server/src/utils/handleChatCompletion.ts
+++ b/apps/agents-server/src/utils/handleChatCompletion.ts
@@ -1,392 +1,16 @@
-import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
-import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
-import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
-import {
-    createBookScopedAgentReferenceResolver,
-    parseBookScopedAgentIdentifier,
-} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
-import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
-import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
-import {
-    appendMessageSuffix,
-    createMessageSuffixAppendix,
-    emulateMessageSuffixStreaming,
-    resolveMessageSuffixFromAgentSource,
-} from '@/src/utils/chat/messageSuffix';
-import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
-import { prepareToolCallsForStreaming } from './toolCallStreaming';
-import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
-import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
-import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
-import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
-import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
-import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
-import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
-import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
-import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
-import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
-import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
-import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
-import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
-import type {
-    ChatMessage,
-    ChatPrompt,
-    LlmToolDefinition,
-    string_book,
-    ToolCall,
-    TODO_any,
-    UncertainNumber,
-    Usage,
-    UsageCounts,
-} from '@promptbook-local/types';
-import { $getCurrentDate } from '@promptbook-local/utils';
 import { NextRequest, NextResponse } from 'next/server';
-import type OpenAI from 'openai';
-import { computeUsageCounts } from '../../../../src/execution/utils/computeUsageCounts';
-import { encodeChatStreamWhitespaceForTransport } from '../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
-import { isAgentDeleted } from '../app/agents/[agentName]/_utils';
 import { HTTP_STATUS_CODES } from '../constants';
-import { AgentKitCacheManager } from './cache/AgentKitCacheManager';
 import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
+import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
+import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
+import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
+import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
+import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
 import { validateApiKey } from './validateApiKey';
-import {
-    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    resolveAgentCollectionTablePrefix,
-    waitForRunningAgentPreparation,
-} from './agentPreparation';
-import {
-    resolveCachedServerAgentContext,
-    resolveCachedServerAgentModelRequirements,
-} from './cachedServerAgentRuntime';
-import { resolveAppendOnlySelfLearningAgentSource } from './resolveAppendOnlySelfLearningAgentSource';
 
 /**
- * Falls back to the estimated value when the original token count is unknown.
- *
- * @param tokenCount - Token count reported by the execution tools.
- * @param fallbackValue - Estimated token count based on text length.
- * @returns Token count to report in the OpenAI-compatible response.
- */
-function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
-    if (tokenCount.value === 0 && tokenCount.isUncertain) {
-        return {
-            value: fallbackValue,
-            isUncertain: true,
-        };
-    }
-
-    return tokenCount;
-}
-
-/**
- * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
- *
- * @param promptContent - Prompt content used for the request.
- * @param completionContent - Assistant response content.
- * @param usage - Native Promptbook usage details.
- * @returns Usage payload for compatibility responses.
- */
-function createCompatibilityUsage(
-    promptContent: string,
-    completionContent: string,
-    usage: Usage,
-): {
-    prompt_tokens: number;
-    completion_tokens: number;
-    total_tokens: number;
-    details: Usage;
-} {
-    const promptCounts = computeUsageCounts(promptContent);
-    const completionCounts = computeUsageCounts(completionContent);
-    const inputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
-        ...promptCounts,
-    };
-    const outputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
-        ...completionCounts,
-    };
-
-    const promptTokens = inputUsage.tokensCount.value;
-    const completionTokens = outputUsage.tokensCount.value;
-
-    return {
-        prompt_tokens: promptTokens,
-        completion_tokens: completionTokens,
-        total_tokens: promptTokens + completionTokens,
-        details: {
-            price: usage.price,
-            duration: usage.duration,
-            input: inputUsage,
-            output: outputUsage,
-        },
-    };
-}
-
-/**
- * Definition of open AI chat tool.
- */
-type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
-
-/**
- * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
- */
-type OpenAiCompatibilityMessageLike = {
-    role?: unknown;
-    content?: unknown;
-};
-
-/**
- * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
- */
-type HandleChatCompletionParsedRequest = {
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    threadMessages: ReadonlyArray<TODO_any>;
-    stream: TODO_any;
-    model: TODO_any;
-    responseFormat: TODO_any;
-    runtimeTools?: Array<LlmToolDefinition>;
-    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-    incomingParameters: Record<string, unknown>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Runtime state resolved before the chat model is executed.
- */
-type HandleChatCompletionRuntime = {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    agent: Agent;
-    agentSource: string_book;
-    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
-    messageSuffix: string | null;
-    agentId: string;
-    agentHash: string;
-    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Prompt, history recorder, and persistence context created for one chat call.
- */
-type HandleChatCompletionPromptContext = {
-    prompt: ChatPrompt;
-    userMessageHash: string;
-    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
-    persistedFrozenChatId?: string;
-};
-
-/**
- * Handles convert open Ai tools.
- */
-function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
-    if (!Array.isArray(rawTools)) {
-        return undefined;
-    }
-
-    const converted = rawTools
-        .map((tool) => convertOpenAiTool(tool))
-        .filter((tool): tool is LlmToolDefinition => tool !== null);
-
-    return converted.length > 0 ? converted : undefined;
-}
-
-/**
- * Handles convert open Ai tool.
- */
-function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
-    const tool = rawTool as OpenAIChatToolDefinition;
-    if (!tool || tool.type !== 'function') {
-        return null;
-    }
-
-    const functionDefinition = tool.function;
-    if (!functionDefinition) {
-        return null;
-    }
-
-    const { name, description, parameters } = functionDefinition;
-    if (typeof name !== 'string' || name.trim().length === 0) {
-        return null;
-    }
-
-    const parameterSchema = parameters ?? {};
-    if (parameterSchema.type !== 'object') {
-        return null;
-    }
-
-    const properties = parameterSchema.properties ?? {};
-    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
-
-    for (const [key, value] of Object.entries(properties)) {
-        if (!value || typeof value !== 'object') {
-            continue;
-        }
-
-        normalizedProperties[key] = {
-            type: typeof value.type === 'string' ? value.type : 'string',
-            description: typeof value.description === 'string' ? value.description : undefined,
-        };
-    }
-
-    const required =
-        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
-            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
-            : undefined;
-
-    return {
-        name,
-        description: typeof description === 'string' ? description : '',
-        parameters: {
-            type: 'object',
-            properties: normalizedProperties,
-            required,
-            additionalProperties:
-                typeof parameterSchema.additionalProperties === 'boolean'
-                    ? parameterSchema.additionalProperties
-                    : undefined,
-        },
-    };
-}
-
-/**
- * Parses open Ai tool choice.
- */
-function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
-    if (value === undefined || value === null) {
-        return undefined;
-    }
-
-    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-}
-
-/**
- * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
- */
-function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
-    if (typeof value === 'string') {
-        return value;
-    }
-
-    if (Array.isArray(value)) {
-        return value
-            .map((part) => {
-                if (typeof part === 'string') {
-                    return part;
-                }
-
-                if (!part || typeof part !== 'object') {
-                    return '';
-                }
-
-                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
-                if (typeof partLike.text === 'string') {
-                    return partLike.text;
-                }
-
-                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
-                    return '[Image input]';
-                }
-
-                if (partLike.image_url) {
-                    return '[Image input]';
-                }
-
-                return '';
-            })
-            .filter(Boolean)
-            .join('\n');
-    }
-
-    if (value === null || value === undefined) {
-        return '';
-    }
-
-    try {
-        return JSON.stringify(value, null, 2);
-    } catch {
-        return String(value);
-    }
-}
-
-/**
- * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ * Handles convert open Ai chat completion requests routed through the Agents Server.
  */
-function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
-    if (typeof role !== 'string') {
-        return 'USER';
-    }
-
-    switch (role) {
-        case 'assistant':
-            return 'AGENT';
-        case 'system':
-            return 'SYSTEM';
-        case 'tool':
-            return 'TOOL';
-        case 'developer':
-            return 'SYSTEM';
-        default:
-            return 'USER';
-    }
-}
-
-/**
- * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
- */
-function createFrozenOpenAiCompatibilityMessages(
-    rawMessages: ReadonlyArray<unknown>,
-    assistantContent?: string,
-    options: {
-        includeAssistantPlaceholder?: boolean;
-    } = {},
-): Array<ChatMessage> {
-    const startedAt = Date.now();
-    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
-        const message = rawMessage as OpenAiCompatibilityMessageLike;
-
-        return {
-            id: `openai-frozen-${index}`,
-            sender: mapOpenAiCompatibilityRoleToSender(message.role),
-            content: normalizeOpenAiCompatibilityMessageContent(message.content),
-            isComplete: true,
-            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage;
-    });
-
-    if (assistantContent !== undefined) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: assistantContent,
-            isComplete: true,
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    } else if (options.includeAssistantPlaceholder) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: '',
-            isComplete: false,
-            lifecycleState: 'running',
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    }
-
-    return messages;
-}
-
-/**
- * Handles chat completion.
- */
 export async function handleChatCompletion(
     request: NextRequest,
     params: { agentName?: string },
@@ -399,7 +23,6 @@
         return versionMismatchResponse;
     }
 
-    // Validate API key explicitly (in addition to middleware)
     const apiKeyValidation = await validateApiKey(request);
     if (!apiKeyValidation.isValid) {
         return NextResponse.json(
@@ -462,724 +85,5 @@
             { error: { message: (error as Error).message || 'Internal Server Error', type: 'server_error' } },
             { status: HTTP_STATUS_CODES.INTERNAL_SERVER_ERROR },
         );
-    }
-}
-
-/**
- * Parses and validates the OpenAI-compatible request payload.
- */
-function parseHandleChatCompletionRequest(options: {
-    body: TODO_any;
-    agentNameFromParams?: string;
-    request: NextRequest;
-}): HandleChatCompletionParsedRequest | NextResponse {
-    const { body, agentNameFromParams, request } = options;
-    const {
-        messages,
-        stream,
-        model,
-        response_format: responseFormat,
-        tools: rawTools,
-        tool_choice: toolChoice,
-        parameters: rawParameters = {},
-    } = body;
-    const agentName = (agentNameFromParams || model) as string | undefined;
-    if (!agentName) {
-        return createHandleChatCompletionErrorResponse(
-            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    if (!messages || !Array.isArray(messages) || messages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array is required and cannot be empty.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
-    if (threadMessages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array must contain at least one non-system message.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    return {
-        agentName,
-        messages,
-        threadMessages,
-        stream,
-        model,
-        responseFormat,
-        runtimeTools: convertOpenAiTools(rawTools),
-        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
-        incomingParameters:
-            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
-                ? (rawParameters as Record<string, unknown>)
-                : {},
-        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
-    };
-}
-
-/**
- * Resolves agent/runtime dependencies needed before prompt execution.
- */
-async function resolveHandleChatCompletionRuntime(options: {
-    request: NextRequest;
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    isPrivateModeEnabled: boolean;
-}): Promise<HandleChatCompletionRuntime | NextResponse> {
-    const { request, agentName, messages, isPrivateModeEnabled } = options;
-    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
-    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
-
-    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
-        return createHandleChatCompletionErrorResponse(
-            'This agent has been deleted. You can restore it from the Recycle Bin.',
-            'agent_deleted',
-            410,
-        );
-    }
-
-    const [collection, baseAgentReferenceResolver] = await Promise.all([
-        $provideAgentCollectionForServer(),
-        $provideAgentReferenceResolver(),
-    ]);
-    const localServerUrl = new URL(request.url).origin;
-    const resolvedAgentContext = await resolveCachedServerAgentContext({
-        collection,
-        agentIdentifier: agentName,
-        localServerUrl,
-        fallbackResolver: baseAgentReferenceResolver,
-    }).catch(() => null);
-
-    if (!resolvedAgentContext?.resolvedAgentSource) {
-        return createHandleChatCompletionErrorResponse(
-            `Agent '${agentName}' not found.`,
-            'invalid_request_error',
-            HTTP_STATUS_CODES.NOT_FOUND,
-        );
-    }
-
-    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
-    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
-    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
-    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
-    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
-    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
-    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
-    agentSource = agentSourceWithContext;
-
-    const preparedAgentModelRequirements = !hasDynamicContext
-        ? await resolveCachedServerAgentModelRequirements({
-              resolvedAgentContext,
-              localServerUrl,
-              fallbackResolver: baseAgentReferenceResolver,
-          })
-        : null;
-    const agentId = resolvedAgentContext.parentAgentPermanentId;
-    const agentHash = computeAgentHash(agentSource);
-    await waitForHandleChatCompletionAgentPreparation({
-        collection,
-        agentId,
-        agentHash,
-    });
-
-    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
-    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
-        await resolveHandleChatCompletionCredentials({
-            currentUserIdentity,
-            agentId,
-            calendarConnections,
-            useEmailConfiguration,
-        });
-    const agent = await createHandleChatCompletionAgent({
-        agentName,
-        agentSource,
-        agentId,
-        resolvedAgentContext,
-        localServerUrl,
-        baseAgentReferenceResolver,
-        hasDynamicContext,
-        preparedAgentModelRequirements,
-    });
-
-    return {
-        collection,
-        resolvedAgentContext,
-        agent,
-        agentSource,
-        unresolvedAgentSource,
-        messageSuffix,
-        agentId,
-        agentHash,
-        projectRepositories,
-        calendarConnections,
-        useEmailConfiguration,
-        currentUserIdentity,
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-        isPrivateModeEnabled,
-    };
-}
-
-/**
- * Creates prompt and persistence helpers for one chat completion request.
- */
-async function createHandleChatCompletionPromptContext(options: {
-    request: NextRequest;
-    title: string;
-    apiKey: string | null;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-}): Promise<HandleChatCompletionPromptContext> {
-    const { request, title, apiKey, parsedRequest, runtime } = options;
-    const timezone = request.headers.get('x-timezone') || 'UTC';
-    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
-    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
-    const recordChatHistoryMessage = await createChatHistoryRecorder({
-        request,
-        agentIdentifier: runtime.agentId,
-        agentHash: runtime.agentHash,
-        source: 'OPENAI_API_COMPATIBILITY',
-        apiKey,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        isEnabled: !runtime.isPrivateModeEnabled,
-    });
-    const userMessageHash = await recordChatHistoryMessage({
-        message: {
-            role: 'USER',
-            content: lastMessage.content,
-        },
-        previousMessageHash: null,
-    });
-    const promptParameters = composePromptParametersWithMemoryContext({
-        baseParameters: {
-            ...parsedRequest.incomingParameters,
-            timezone,
-        },
-        currentUserIdentity: runtime.currentUserIdentity,
-        agentPermanentId: runtime.agentId,
-        agentName: runtime.resolvedAgentContext.resolvedAgentName,
-        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
-        projectRepositories: runtime.projectRepositories,
-        projectGithubToken: runtime.projectGithubToken,
-        emailSmtpCredential: runtime.emailSmtpCredential,
-        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
-        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
-        calendarConnections: runtime.calendarConnections,
-    });
-    const prompt: ChatPrompt = {
-        title,
-        content: lastMessage.content,
-        modelRequirements: {
-            modelVariant: 'CHAT',
-            responseFormat: parsedRequest.responseFormat,
-            toolChoice: parsedRequest.runtimeToolChoice,
-            // We could pass 'model' from body if we wanted to enforce it, but Agent usually has its own config
-        },
-        parameters: promptParameters,
-        thread,
-        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
-    };
-
-    return {
-        prompt,
-        userMessageHash,
-        recordChatHistoryMessage,
-        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
-            isEnabled: !runtime.isPrivateModeEnabled,
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentId: runtime.agentId,
-            messages: parsedRequest.messages,
-            includeAssistantPlaceholder: true,
-            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
-        }),
-    };
-}
-
-/**
- * Handles the streaming OpenAI-compatible response branch.
- */
-function createHandleChatCompletionStreamResponse(options: {
-    request: NextRequest;
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Response {
-    const { request, title, parsedRequest, runtime, promptContext } = options;
-    const encoder = new TextEncoder();
-    const responseModel = parsedRequest.model || 'promptbook-agent';
-    const readableStream = new ReadableStream({
-        async start(controller) {
-            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
-            const created = Math.floor(Date.now() / 1000);
-            const emitSsePayload = (payload: unknown) => {
-                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
-            };
-            const emitDeltaChunk = (deltaContent: string) => {
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {
-                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
-                        },
-                        finishReason: null,
-                    }),
-                );
-            };
-
-            emitSsePayload(
-                createHandleChatCompletionChunk({
-                    runId,
-                    created,
-                    model: responseModel,
-                    delta: {
-                        role: 'assistant',
-                        content: '',
-                    },
-                    finishReason: null,
-                }),
-            );
-
-            let hasMeaningfulDelta = false;
-
-            try {
-                const handleStreamChunk = createChatStreamHandler({
-                    onDelta: (deltaContent) => {
-                        if (deltaContent.trim().length > 0) {
-                            hasMeaningfulDelta = true;
-                        }
-                        emitDeltaChunk(deltaContent);
-                    },
-                    onToolCalls: (toolCalls) => {
-                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
-                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
-                    },
-                });
-                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
-                    signal: request.signal,
-                });
-                const normalizedResponse = ensureNonEmptyChatContent({
-                    content: result.content,
-                    context: title,
-                });
-
-                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
-                    emitDeltaChunk(normalizedResponse.content);
-                }
-
-                const messageSuffixAppendix = createMessageSuffixAppendix(
-                    normalizedResponse.content,
-                    runtime.messageSuffix,
-                );
-                if (messageSuffixAppendix) {
-                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
-                        emitDeltaChunk(delta);
-                    });
-                }
-
-                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-                await finalizeHandleChatCompletionResult({
-                    responseContentWithSuffix,
-                    usage: result.usage,
-                    toolCalls: result.toolCalls,
-                    parsedRequest,
-                    runtime,
-                    promptContext,
-                    // Preserve historical behavior: streamed OpenAI-compatible calls still persist learning in private mode.
-                    shouldPersistLearnedAgentSource: true,
-                });
-
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {},
-                        finishReason: 'stop',
-                    }),
-                );
-                controller.enqueue(encoder.encode('[DONE]'));
-            } catch (error) {
-                console.error('Error during streaming:', error);
-                controller.error(error);
-            }
-            controller.close();
-        },
-    });
-
-    return new Response(readableStream, {
-        headers: {
-            'Content-Type': 'text/event-stream',
-            'Cache-Control': 'no-cache',
-            Connection: 'keep-alive',
-        },
-    });
-}
-
-/**
- * Handles the non-streaming OpenAI-compatible response branch.
- */
-async function createHandleChatCompletionJsonResponse(options: {
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Promise<NextResponse> {
-    const { title, parsedRequest, runtime, promptContext } = options;
-    const result = await runtime.agent.callChatModel(promptContext.prompt);
-    const normalizedResponse = ensureNonEmptyChatContent({
-        content: result.content,
-        context: title,
-    });
-    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-
-    await finalizeHandleChatCompletionResult({
-        responseContentWithSuffix,
-        usage: result.usage,
-        toolCalls: result.toolCalls,
-        parsedRequest,
-        runtime,
-        promptContext,
-        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
-    });
-
-    return NextResponse.json({
-        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
-        object: 'chat.completion',
-        created: Math.floor(Date.now() / 1000),
-        model: parsedRequest.model || 'promptbook-agent',
-        choices: [
-            {
-                index: 0,
-                message: {
-                    role: 'assistant',
-                    content: responseContentWithSuffix,
-                },
-                finish_reason: 'stop',
-            },
-        ],
-        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
-    });
-}
-
-/**
- * Runs the shared side effects after one successful model response.
- */
-async function finalizeHandleChatCompletionResult(options: {
-    responseContentWithSuffix: string;
-    usage: Usage;
-    toolCalls?: ReadonlyArray<ToolCall>;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
-
-    await promptContext.recordChatHistoryMessage({
-        message: {
-            role: 'MODEL',
-            content: responseContentWithSuffix,
-        },
-        previousMessageHash: promptContext.userMessageHash,
-        usage,
-    });
-
-    if (toolCalls && toolCalls.length > 0) {
-        await logCalendarToolCallsActivity({
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentPermanentId: runtime.agentId,
-            toolCalls,
-        });
-    }
-
-    await persistOpenAiCompatibilityFrozenChat({
-        isEnabled: !runtime.isPrivateModeEnabled,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        agentId: runtime.agentId,
-        chatId: promptContext.persistedFrozenChatId,
-        messages: parsedRequest.messages,
-        assistantContent: responseContentWithSuffix,
-        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
-    });
-    await persistHandleChatCompletionLearnedAgentSource({
-        runtime,
-        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
-    });
-}
-
-/**
- * Applies append-only self-learning updates when the current branch should persist them.
- */
-async function persistHandleChatCompletionLearnedAgentSource(options: {
-    runtime: HandleChatCompletionRuntime;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { runtime, shouldPersistLearnedAgentSource } = options;
-    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
-        return;
-    }
-
-    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
-        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
-        resolvedAgentSourceBeforeLearning: runtime.agentSource,
-        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
-    });
-
-    if (learnedAgentSource !== null) {
-        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
     }
 }
-
-/**
- * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
- */
-async function persistOpenAiCompatibilityFrozenChat(options: {
-    isEnabled: boolean;
-    userId: number | null | undefined;
-    agentId: string;
-    messages: ReadonlyArray<unknown>;
-    chatId?: string;
-    assistantContent?: string;
-    includeAssistantPlaceholder?: boolean;
-    failureMessage: string;
-}): Promise<string | undefined> {
-    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
-        options;
-    if (!isEnabled || !userId) {
-        return undefined;
-    }
-
-    const persistedFrozenChat = await persistFrozenUserChat({
-        userId,
-        agentPermanentId: agentId,
-        source: USER_CHAT_SOURCES.OPENAI_API,
-        chatId,
-        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
-            includeAssistantPlaceholder,
-        }),
-    }).catch((error) => {
-        console.error(failureMessage, error);
-        return null;
-    });
-
-    return persistedFrozenChat?.id;
-}
-
-/**
- * Builds one OpenAI-compatible thread from the earlier request messages.
- */
-function createHandleChatCompletionThread(previousMessages: ReadonlyArray<TODO_any>): ChatMessage[] {
-    return previousMessages.map((message: TODO_any, index: number) => ({
-        id: `msg-${index}`,
-        sender: message.role === 'assistant' ? 'agent' : 'user',
-        content: message.content,
-        isComplete: true,
-        createdAt: $getCurrentDate(),
-    }));
-}
-
-/**
- * Appends system messages to the agent source as dynamic `CONTEXT`.
- */
-function appendSystemMessagesToAgentSource(
-    agentSource: string_book,
-    messages: ReadonlyArray<TODO_any>,
-): {
-    agentSourceWithContext: string_book;
-    hasDynamicContext: boolean;
-} {
-    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
-    if (systemMessages.length === 0) {
-        return {
-            agentSourceWithContext: agentSource,
-            hasDynamicContext: false,
-        };
-    }
-
-    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
-    return {
-        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
-        hasDynamicContext: true,
-    };
-}
-
-/**
- * Waits for any running preparation job of the current agent before executing the chat call.
- */
-async function waitForHandleChatCompletionAgentPreparation(options: {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    agentId: string;
-    agentHash: string;
-}): Promise<void> {
-    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
-    const preparationWaitResult = await waitForRunningAgentPreparation({
-        tablePrefix,
-        agentPermanentId: options.agentId,
-        fingerprint: options.agentHash,
-        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    });
-
-    if (preparationWaitResult !== 'not_running') {
-        console.info('[pre-index]', 'openai_chat_wait_result', {
-            tablePrefix,
-            agentPermanentId: options.agentId,
-            agentHash: options.agentHash,
-            preparationWaitResult,
-        });
-    }
-}
-
-/**
- * Resolves request-scoped project, calendar, and email credentials for the current agent.
- */
-async function resolveHandleChatCompletionCredentials(options: {
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    agentId: string;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-}): Promise<{
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-}> {
-    const userId = options.currentUserIdentity?.userId;
-    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
-        resolveUseProjectGithubToken({
-            userId,
-            agentPermanentId: options.agentId,
-        }),
-        options.calendarConnections.length > 0
-            ? resolveUseCalendarGoogleToken({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-        options.useEmailConfiguration.isEnabled
-            ? resolveUseEmailSmtpCredential({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-    ]);
-
-    return {
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-    };
-}
-
-/**
- * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
- */
-async function createHandleChatCompletionAgent(options: {
-    agentName: string;
-    agentSource: string_book;
-    agentId: string;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    localServerUrl: string;
-    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
-    hasDynamicContext: boolean;
-    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
-}): Promise<Agent> {
-    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
-    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
-    const runtimeAgentReferenceResolver = options.hasDynamicContext
-        ? createBookScopedAgentReferenceResolver({
-              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
-              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
-              localServerUrl: options.localServerUrl,
-              fallbackResolver: options.baseAgentReferenceResolver,
-          })
-        : undefined;
-    const agentModelRequirements =
-        options.preparedAgentModelRequirements?.modelRequirements ||
-        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
-            agentReferenceResolver: runtimeAgentReferenceResolver,
-            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
-        }));
-
-    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
-        options.agentSource,
-        options.resolvedAgentContext.resolvedAgentName,
-        await baseOpenAiToolsPromise,
-        {
-            includeDynamicContext: true,
-            agentId: options.agentId,
-            modelRequirements: agentModelRequirements,
-        },
-    );
-
-    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
-        agentName: options.agentName,
-        assistantCacheKey: agentKitResult.assistantCacheKey,
-        vectorStoreHash: agentKitResult.vectorStoreHash,
-        vectorStoreId: agentKitResult.vectorStoreId,
-    });
-
-    return new Agent({
-        agentSource: options.agentSource,
-        executionTools: {
-            llm: agentKitResult.tools,
-        },
-        precomputedModelRequirements: agentModelRequirements,
-        assistantPreparationMode: 'external',
-        isVerbose: true,
-        teacherAgent: null,
-    });
-}
-
-/**
- * Creates one OpenAI-compatible streaming chunk payload.
- */
-function createHandleChatCompletionChunk(options: {
-    runId: string;
-    created: number;
-    model: TODO_any;
-    delta: Record<string, unknown>;
-    finishReason: 'stop' | null;
-}) {
-    return {
-        id: options.runId,
-        object: 'chat.completion.chunk',
-        created: options.created,
-        model: options.model,
-        choices: [
-            {
-                index: 0,
-                delta: options.delta,
-                finish_reason: options.finishReason,
-            },
-        ],
-    };
-}
-
-/**
- * Creates one consistent OpenAI-compatible error response.
- */
-function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
-    return NextResponse.json(
-        {
-            error: {
-                message,
-                type,
-            },
-        },
-        { status },
-    );
-}
-
-// TODO: [🈹] Maybe move chat thread handling here
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f49a4fa80fa21f140e25de59596e5546d62e3f7
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
@@ -0,0 +1,18 @@
+import { NextResponse } from 'next/server';
+
+/**
+ * Creates one consistent OpenAI-compatible error response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
+    return NextResponse.json(
+        {
+            error: {
+                message,
+                type,
+            },
+        },
+        { status },
+    );
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b1979f4621921bc664fed75a05851c6fcb8fba6d
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
@@ -0,0 +1,120 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import { appendMessageSuffix } from '@/src/utils/chat/messageSuffix';
+import type { UncertainNumber, Usage, UsageCounts } from '@promptbook-local/types';
+import { NextResponse } from 'next/server';
+import { computeUsageCounts } from '../../../../../src/execution/utils/computeUsageCounts';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the non-streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionJsonResponse(options: {
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Promise<NextResponse> {
+    const { title, parsedRequest, runtime, promptContext } = options;
+    const result = await runtime.agent.callChatModel(promptContext.prompt);
+    const normalizedResponse = ensureNonEmptyChatContent({
+        content: result.content,
+        context: title,
+    });
+    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+
+    await finalizeHandleChatCompletionResult({
+        responseContentWithSuffix,
+        usage: result.usage,
+        toolCalls: result.toolCalls,
+        parsedRequest,
+        runtime,
+        promptContext,
+        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
+    });
+
+    return NextResponse.json({
+        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
+        object: 'chat.completion',
+        created: Math.floor(Date.now() / 1000),
+        model: parsedRequest.model || 'promptbook-agent',
+        choices: [
+            {
+                index: 0,
+                message: {
+                    role: 'assistant',
+                    content: responseContentWithSuffix,
+                },
+                finish_reason: 'stop',
+            },
+        ],
+        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
+    });
+}
+
+/**
+ * Falls back to the estimated value when the original token count is unknown.
+ *
+ * @param tokenCount - Token count reported by the execution tools.
+ * @param fallbackValue - Estimated token count based on text length.
+ * @returns Token count to report in the OpenAI-compatible response.
+ */
+function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
+    if (tokenCount.value === 0 && tokenCount.isUncertain) {
+        return {
+            value: fallbackValue,
+            isUncertain: true,
+        };
+    }
+
+    return tokenCount;
+}
+
+/**
+ * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
+ *
+ * @param promptContent - Prompt content used for the request.
+ * @param completionContent - Assistant response content.
+ * @param usage - Native Promptbook usage details.
+ * @returns Usage payload for compatibility responses.
+ */
+function createCompatibilityUsage(
+    promptContent: string,
+    completionContent: string,
+    usage: Usage,
+): {
+    prompt_tokens: number;
+    completion_tokens: number;
+    total_tokens: number;
+    details: Usage;
+} {
+    const promptCounts = computeUsageCounts(promptContent);
+    const completionCounts = computeUsageCounts(completionContent);
+    const inputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
+        ...promptCounts,
+    };
+    const outputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
+        ...completionCounts,
+    };
+
+    const promptTokens = inputUsage.tokensCount.value;
+    const completionTokens = outputUsage.tokensCount.value;
+
+    return {
+        prompt_tokens: promptTokens,
+        completion_tokens: completionTokens,
+        total_tokens: promptTokens + completionTokens,
+        details: {
+            price: usage.price,
+            duration: usage.duration,
+            input: inputUsage,
+            output: outputUsage,
+        },
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..7aac8141a8b1237ce36430b4b4598a618f0383ac
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
@@ -0,0 +1,111 @@
+import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
+import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
+import type { ChatMessage, ChatPrompt } from '@promptbook-local/types';
+import { $getCurrentDate } from '@promptbook-local/utils';
+import type { NextRequest } from 'next/server';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Prompt, history recorder, and persistence context created for one chat call.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionPromptContext = {
+    prompt: ChatPrompt;
+    userMessageHash: string;
+    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
+    persistedFrozenChatId?: string;
+};
+
+/**
+ * Creates prompt and persistence helpers for one chat completion request.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionPromptContext(options: {
+    request: NextRequest;
+    title: string;
+    apiKey: string | null;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+}): Promise<HandleChatCompletionPromptContext> {
+    const { request, title, apiKey, parsedRequest, runtime } = options;
+    const timezone = request.headers.get('x-timezone') || 'UTC';
+    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
+    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
+    const recordChatHistoryMessage = await createChatHistoryRecorder({
+        request,
+        agentIdentifier: runtime.agentId,
+        agentHash: runtime.agentHash,
+        source: 'OPENAI_API_COMPATIBILITY',
+        apiKey,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        isEnabled: !runtime.isPrivateModeEnabled,
+    });
+    const userMessageHash = await recordChatHistoryMessage({
+        message: {
+            role: 'USER',
+            content: lastMessage.content,
+        },
+        previousMessageHash: null,
+    });
+    const promptParameters = composePromptParametersWithMemoryContext({
+        baseParameters: {
+            ...parsedRequest.incomingParameters,
+            timezone,
+        },
+        currentUserIdentity: runtime.currentUserIdentity,
+        agentPermanentId: runtime.agentId,
+        agentName: runtime.resolvedAgentContext.resolvedAgentName,
+        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
+        projectRepositories: runtime.projectRepositories,
+        projectGithubToken: runtime.projectGithubToken,
+        emailSmtpCredential: runtime.emailSmtpCredential,
+        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
+        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
+        calendarConnections: runtime.calendarConnections,
+    });
+    const prompt: ChatPrompt = {
+        title,
+        content: lastMessage.content,
+        modelRequirements: {
+            modelVariant: 'CHAT',
+            responseFormat: parsedRequest.responseFormat,
+            toolChoice: parsedRequest.runtimeToolChoice,
+        },
+        parameters: promptParameters,
+        thread,
+        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
+    };
+
+    return {
+        prompt,
+        userMessageHash,
+        recordChatHistoryMessage,
+        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
+            isEnabled: !runtime.isPrivateModeEnabled,
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentId: runtime.agentId,
+            messages: parsedRequest.messages,
+            includeAssistantPlaceholder: true,
+            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
+        }),
+    };
+}
+
+/**
+ * Builds one OpenAI-compatible thread from the earlier request messages.
+ */
+function createHandleChatCompletionThread(
+    previousMessages: HandleChatCompletionParsedRequest['threadMessages'],
+): Array<ChatMessage> {
+    return previousMessages.map((message, index) => ({
+        id: `msg-${index}`,
+        sender: message.role === 'assistant' ? 'agent' : 'user',
+        content: message.content,
+        isComplete: true,
+        createdAt: $getCurrentDate(),
+    }));
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..85e096027760bc45fb730e74f8ae3bbb721b1029
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
@@ -0,0 +1,162 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import {
+    appendMessageSuffix,
+    createMessageSuffixAppendix,
+    emulateMessageSuffixStreaming,
+} from '@/src/utils/chat/messageSuffix';
+import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
+import { encodeChatStreamWhitespaceForTransport } from '../../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
+import { prepareToolCallsForStreaming } from '../toolCallStreaming';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionStreamResponse(options: {
+    request: Request;
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Response {
+    const { request, title, parsedRequest, runtime, promptContext } = options;
+    const encoder = new TextEncoder();
+    const responseModel = parsedRequest.model || 'promptbook-agent';
+    const readableStream = new ReadableStream({
+        async start(controller) {
+            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
+            const created = Math.floor(Date.now() / 1000);
+            const emitSsePayload = (payload: unknown) => {
+                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
+            };
+            const emitDeltaChunk = (deltaContent: string) => {
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {
+                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
+                        },
+                        finishReason: null,
+                    }),
+                );
+            };
+
+            emitSsePayload(
+                createHandleChatCompletionChunk({
+                    runId,
+                    created,
+                    model: responseModel,
+                    delta: {
+                        role: 'assistant',
+                        content: '',
+                    },
+                    finishReason: null,
+                }),
+            );
+
+            let hasMeaningfulDelta = false;
+
+            try {
+                const handleStreamChunk = createChatStreamHandler({
+                    onDelta: (deltaContent) => {
+                        if (deltaContent.trim().length > 0) {
+                            hasMeaningfulDelta = true;
+                        }
+                        emitDeltaChunk(deltaContent);
+                    },
+                    onToolCalls: (toolCalls) => {
+                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
+                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
+                    },
+                });
+                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
+                    signal: request.signal,
+                });
+                const normalizedResponse = ensureNonEmptyChatContent({
+                    content: result.content,
+                    context: title,
+                });
+
+                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
+                    emitDeltaChunk(normalizedResponse.content);
+                }
+
+                const messageSuffixAppendix = createMessageSuffixAppendix(
+                    normalizedResponse.content,
+                    runtime.messageSuffix,
+                );
+                if (messageSuffixAppendix) {
+                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
+                        emitDeltaChunk(delta);
+                    });
+                }
+
+                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+                await finalizeHandleChatCompletionResult({
+                    responseContentWithSuffix,
+                    usage: result.usage,
+                    toolCalls: result.toolCalls,
+                    parsedRequest,
+                    runtime,
+                    promptContext,
+                    shouldPersistLearnedAgentSource: true,
+                });
+
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {},
+                        finishReason: 'stop',
+                    }),
+                );
+                controller.enqueue(encoder.encode('[DONE]'));
+            } catch (error) {
+                console.error('Error during streaming:', error);
+                controller.error(error);
+            }
+            controller.close();
+        },
+    });
+
+    return new Response(readableStream, {
+        headers: {
+            'Content-Type': 'text/event-stream',
+            'Cache-Control': 'no-cache',
+            Connection: 'keep-alive',
+        },
+    });
+}
+
+/**
+ * Creates one OpenAI-compatible streaming chunk payload.
+ */
+function createHandleChatCompletionChunk(options: {
+    runId: string;
+    created: number;
+    model: HandleChatCompletionParsedRequest['model'];
+    delta: Record<string, unknown>;
+    finishReason: 'stop' | null;
+}) {
+    return {
+        id: options.runId,
+        object: 'chat.completion.chunk',
+        created: options.created,
+        model: options.model,
+        choices: [
+            {
+                index: 0,
+                delta: options.delta,
+                finish_reason: options.finishReason,
+            },
+        ],
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
new file mode 100644
index 0000000000000000000000000000000000000000..dd6a7d87ea491df245dbf93d8bcb277a9a10b034
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
@@ -0,0 +1,78 @@
+import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
+import { resolveAppendOnlySelfLearningAgentSource } from '@/src/utils/resolveAppendOnlySelfLearningAgentSource';
+import type { ToolCall, Usage } from '@promptbook-local/types';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Runs the shared side effects after one successful model response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function finalizeHandleChatCompletionResult(options: {
+    responseContentWithSuffix: string;
+    usage: Usage;
+    toolCalls?: ReadonlyArray<ToolCall>;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
+
+    await promptContext.recordChatHistoryMessage({
+        message: {
+            role: 'MODEL',
+            content: responseContentWithSuffix,
+        },
+        previousMessageHash: promptContext.userMessageHash,
+        usage,
+    });
+
+    if (toolCalls && toolCalls.length > 0) {
+        await logCalendarToolCallsActivity({
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentPermanentId: runtime.agentId,
+            toolCalls,
+        });
+    }
+
+    await persistOpenAiCompatibilityFrozenChat({
+        isEnabled: !runtime.isPrivateModeEnabled,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        agentId: runtime.agentId,
+        chatId: promptContext.persistedFrozenChatId,
+        messages: parsedRequest.messages,
+        assistantContent: responseContentWithSuffix,
+        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
+    });
+    await persistHandleChatCompletionLearnedAgentSource({
+        runtime,
+        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
+    });
+}
+
+/**
+ * Applies append-only self-learning updates when the current branch should persist them.
+ */
+async function persistHandleChatCompletionLearnedAgentSource(options: {
+    runtime: HandleChatCompletionRuntime;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { runtime, shouldPersistLearnedAgentSource } = options;
+    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
+        return;
+    }
+
+    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
+        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
+        resolvedAgentSourceBeforeLearning: runtime.agentSource,
+        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
+    });
+
+    if (learnedAgentSource !== null) {
+        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
+    }
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
new file mode 100644
index 0000000000000000000000000000000000000000..58e1c26d7f428c1bdb39b36411b3f75256e3de51
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
@@ -0,0 +1,176 @@
+import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
+import type { LlmToolDefinition, TODO_any } from '@promptbook-local/types';
+import { NextRequest, NextResponse } from 'next/server';
+import type OpenAI from 'openai';
+import { HTTP_STATUS_CODES } from '../../constants';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Definition of open AI chat tool.
+ */
+type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
+
+/**
+ * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionParsedRequest = {
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    threadMessages: ReadonlyArray<TODO_any>;
+    stream: TODO_any;
+    model: TODO_any;
+    responseFormat: TODO_any;
+    runtimeTools?: Array<LlmToolDefinition>;
+    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+    incomingParameters: Record<string, unknown>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Parses and validates the OpenAI-compatible request payload.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function parseHandleChatCompletionRequest(options: {
+    body: TODO_any;
+    agentNameFromParams?: string;
+    request: NextRequest;
+}): HandleChatCompletionParsedRequest | NextResponse {
+    const { body, agentNameFromParams, request } = options;
+    const {
+        messages,
+        stream,
+        model,
+        response_format: responseFormat,
+        tools: rawTools,
+        tool_choice: toolChoice,
+        parameters: rawParameters = {},
+    } = body;
+    const agentName = (agentNameFromParams || model) as string | undefined;
+    if (!agentName) {
+        return createHandleChatCompletionErrorResponse(
+            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    if (!messages || !Array.isArray(messages) || messages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array is required and cannot be empty.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
+    if (threadMessages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array must contain at least one non-system message.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    return {
+        agentName,
+        messages,
+        threadMessages,
+        stream,
+        model,
+        responseFormat,
+        runtimeTools: convertOpenAiTools(rawTools),
+        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
+        incomingParameters:
+            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
+                ? (rawParameters as Record<string, unknown>)
+                : {},
+        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
+    };
+}
+
+/**
+ * Handles convert open Ai tools.
+ */
+function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
+    if (!Array.isArray(rawTools)) {
+        return undefined;
+    }
+
+    const converted = rawTools
+        .map((tool) => convertOpenAiTool(tool))
+        .filter((tool): tool is LlmToolDefinition => tool !== null);
+
+    return converted.length > 0 ? converted : undefined;
+}
+
+/**
+ * Handles convert open Ai tool.
+ */
+function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
+    const tool = rawTool as OpenAIChatToolDefinition;
+    if (!tool || tool.type !== 'function') {
+        return null;
+    }
+
+    const functionDefinition = tool.function;
+    if (!functionDefinition) {
+        return null;
+    }
+
+    const { name, description, parameters } = functionDefinition;
+    if (typeof name !== 'string' || name.trim().length === 0) {
+        return null;
+    }
+
+    const parameterSchema = parameters ?? {};
+    if (parameterSchema.type !== 'object') {
+        return null;
+    }
+
+    const properties = parameterSchema.properties ?? {};
+    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
+
+    for (const [key, value] of Object.entries(properties)) {
+        if (!value || typeof value !== 'object') {
+            continue;
+        }
+
+        normalizedProperties[key] = {
+            type: typeof value.type === 'string' ? value.type : 'string',
+            description: typeof value.description === 'string' ? value.description : undefined,
+        };
+    }
+
+    const required =
+        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
+            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
+            : undefined;
+
+    return {
+        name,
+        description: typeof description === 'string' ? description : '',
+        parameters: {
+            type: 'object',
+            properties: normalizedProperties,
+            required,
+            additionalProperties:
+                typeof parameterSchema.additionalProperties === 'boolean'
+                    ? parameterSchema.additionalProperties
+                    : undefined,
+        },
+    };
+}
+
+/**
+ * Parses open Ai tool choice.
+ */
+function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
+    if (value === undefined || value === null) {
+        return undefined;
+    }
+
+    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
new file mode 100644
index 0000000000000000000000000000000000000000..60e5f1b008bcf5a6a1836c06c4c3d9ebcb909a9f
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
@@ -0,0 +1,163 @@
+import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
+import type { ChatMessage } from '@promptbook-local/types';
+
+/**
+ * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
+ */
+type OpenAiCompatibilityMessageLike = {
+    role?: unknown;
+    content?: unknown;
+};
+
+/**
+ * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function persistOpenAiCompatibilityFrozenChat(options: {
+    isEnabled: boolean;
+    userId: number | null | undefined;
+    agentId: string;
+    messages: ReadonlyArray<unknown>;
+    chatId?: string;
+    assistantContent?: string;
+    includeAssistantPlaceholder?: boolean;
+    failureMessage: string;
+}): Promise<string | undefined> {
+    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
+        options;
+    if (!isEnabled || !userId) {
+        return undefined;
+    }
+
+    const persistedFrozenChat = await persistFrozenUserChat({
+        userId,
+        agentPermanentId: agentId,
+        source: USER_CHAT_SOURCES.OPENAI_API,
+        chatId,
+        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
+            includeAssistantPlaceholder,
+        }),
+    }).catch((error) => {
+        console.error(failureMessage, error);
+        return null;
+    });
+
+    return persistedFrozenChat?.id;
+}
+
+/**
+ * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
+ */
+function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
+    if (typeof value === 'string') {
+        return value;
+    }
+
+    if (Array.isArray(value)) {
+        return value
+            .map((part) => {
+                if (typeof part === 'string') {
+                    return part;
+                }
+
+                if (!part || typeof part !== 'object') {
+                    return '';
+                }
+
+                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
+                if (typeof partLike.text === 'string') {
+                    return partLike.text;
+                }
+
+                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
+                    return '[Image input]';
+                }
+
+                if (partLike.image_url) {
+                    return '[Image input]';
+                }
+
+                return '';
+            })
+            .filter(Boolean)
+            .join('\n');
+    }
+
+    if (value === null || value === undefined) {
+        return '';
+    }
+
+    try {
+        return JSON.stringify(value, null, 2);
+    } catch {
+        return String(value);
+    }
+}
+
+/**
+ * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ */
+function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
+    if (typeof role !== 'string') {
+        return 'USER';
+    }
+
+    switch (role) {
+        case 'assistant':
+            return 'AGENT';
+        case 'system':
+            return 'SYSTEM';
+        case 'tool':
+            return 'TOOL';
+        case 'developer':
+            return 'SYSTEM';
+        default:
+            return 'USER';
+    }
+}
+
+/**
+ * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
+ */
+function createFrozenOpenAiCompatibilityMessages(
+    rawMessages: ReadonlyArray<unknown>,
+    assistantContent?: string,
+    options: {
+        includeAssistantPlaceholder?: boolean;
+    } = {},
+): Array<ChatMessage> {
+    const startedAt = Date.now();
+    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
+        const message = rawMessage as OpenAiCompatibilityMessageLike;
+
+        return {
+            id: `openai-frozen-${index}`,
+            sender: mapOpenAiCompatibilityRoleToSender(message.role),
+            content: normalizeOpenAiCompatibilityMessageContent(message.content),
+            isComplete: true,
+            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage;
+    });
+
+    if (assistantContent !== undefined) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: assistantContent,
+            isComplete: true,
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    } else if (options.includeAssistantPlaceholder) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: '',
+            isComplete: false,
+            lifecycleState: 'running',
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    }
+
+    return messages;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fd44b7ea5a87168725a9e99784038137c96537ab
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
@@ -0,0 +1,305 @@
+import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
+import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
+import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
+import type { string_book, TODO_any } from '@promptbook-local/types';
+import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
+import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
+import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
+import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
+import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
+import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
+import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
+import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
+import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
+import {
+    createBookScopedAgentReferenceResolver,
+    parseBookScopedAgentIdentifier,
+} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
+import { resolveMessageSuffixFromAgentSource } from '@/src/utils/chat/messageSuffix';
+import { NextRequest, NextResponse } from 'next/server';
+import { isAgentDeleted } from '../../app/agents/[agentName]/_utils';
+import {
+    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    resolveAgentCollectionTablePrefix,
+    waitForRunningAgentPreparation,
+} from '../agentPreparation';
+import { resolveCachedServerAgentContext, resolveCachedServerAgentModelRequirements } from '../cachedServerAgentRuntime';
+import { AgentKitCacheManager } from '../cache/AgentKitCacheManager';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Runtime state resolved before the chat model is executed.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionRuntime = {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    agent: Agent;
+    agentSource: string_book;
+    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
+    messageSuffix: string | null;
+    agentId: string;
+    agentHash: string;
+    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Resolves agent/runtime dependencies needed before prompt execution.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function resolveHandleChatCompletionRuntime(options: {
+    request: NextRequest;
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    isPrivateModeEnabled: boolean;
+}): Promise<HandleChatCompletionRuntime | NextResponse> {
+    const { request, agentName, messages, isPrivateModeEnabled } = options;
+    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
+    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
+
+    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
+        return createHandleChatCompletionErrorResponse(
+            'This agent has been deleted. You can restore it from the Recycle Bin.',
+            'agent_deleted',
+            410,
+        );
+    }
+
+    const [collection, baseAgentReferenceResolver] = await Promise.all([
+        $provideAgentCollectionForServer(),
+        $provideAgentReferenceResolver(),
+    ]);
+    const localServerUrl = new URL(request.url).origin;
+    const resolvedAgentContext = await resolveCachedServerAgentContext({
+        collection,
+        agentIdentifier: agentName,
+        localServerUrl,
+        fallbackResolver: baseAgentReferenceResolver,
+    }).catch(() => null);
+
+    if (!resolvedAgentContext?.resolvedAgentSource) {
+        return createHandleChatCompletionErrorResponse(`Agent '${agentName}' not found.`, 'invalid_request_error', 404);
+    }
+
+    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
+    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
+    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
+    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
+    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
+    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
+    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
+    agentSource = agentSourceWithContext;
+
+    const preparedAgentModelRequirements = !hasDynamicContext
+        ? await resolveCachedServerAgentModelRequirements({
+              resolvedAgentContext,
+              localServerUrl,
+              fallbackResolver: baseAgentReferenceResolver,
+          })
+        : null;
+    const agentId = resolvedAgentContext.parentAgentPermanentId;
+    const agentHash = computeAgentHash(agentSource);
+    await waitForHandleChatCompletionAgentPreparation({
+        collection,
+        agentId,
+        agentHash,
+    });
+
+    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
+    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
+        await resolveHandleChatCompletionCredentials({
+            currentUserIdentity,
+            agentId,
+            calendarConnections,
+            useEmailConfiguration,
+        });
+    const agent = await createHandleChatCompletionAgent({
+        agentName,
+        agentSource,
+        agentId,
+        resolvedAgentContext,
+        localServerUrl,
+        baseAgentReferenceResolver,
+        hasDynamicContext,
+        preparedAgentModelRequirements,
+    });
+
+    return {
+        collection,
+        resolvedAgentContext,
+        agent,
+        agentSource,
+        unresolvedAgentSource,
+        messageSuffix,
+        agentId,
+        agentHash,
+        projectRepositories,
+        calendarConnections,
+        useEmailConfiguration,
+        currentUserIdentity,
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+        isPrivateModeEnabled,
+    };
+}
+
+/**
+ * Appends system messages to the agent source as dynamic `CONTEXT`.
+ */
+function appendSystemMessagesToAgentSource(
+    agentSource: string_book,
+    messages: ReadonlyArray<TODO_any>,
+): {
+    agentSourceWithContext: string_book;
+    hasDynamicContext: boolean;
+} {
+    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
+    if (systemMessages.length === 0) {
+        return {
+            agentSourceWithContext: agentSource,
+            hasDynamicContext: false,
+        };
+    }
+
+    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
+    return {
+        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
+        hasDynamicContext: true,
+    };
+}
+
+/**
+ * Waits for any running preparation job of the current agent before executing the chat call.
+ */
+async function waitForHandleChatCompletionAgentPreparation(options: {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    agentId: string;
+    agentHash: string;
+}): Promise<void> {
+    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
+    const preparationWaitResult = await waitForRunningAgentPreparation({
+        tablePrefix,
+        agentPermanentId: options.agentId,
+        fingerprint: options.agentHash,
+        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    });
+
+    if (preparationWaitResult !== 'not_running') {
+        console.info('[pre-index]', 'openai_chat_wait_result', {
+            tablePrefix,
+            agentPermanentId: options.agentId,
+            agentHash: options.agentHash,
+            preparationWaitResult,
+        });
+    }
+}
+
+/**
+ * Resolves request-scoped project, calendar, and email credentials for the current agent.
+ */
+async function resolveHandleChatCompletionCredentials(options: {
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    agentId: string;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+}): Promise<{
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+}> {
+    const userId = options.currentUserIdentity?.userId;
+    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
+        resolveUseProjectGithubToken({
+            userId,
+            agentPermanentId: options.agentId,
+        }),
+        options.calendarConnections.length > 0
+            ? resolveUseCalendarGoogleToken({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+        options.useEmailConfiguration.isEnabled
+            ? resolveUseEmailSmtpCredential({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+    ]);
+
+    return {
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+    };
+}
+
+/**
+ * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
+ */
+async function createHandleChatCompletionAgent(options: {
+    agentName: string;
+    agentSource: string_book;
+    agentId: string;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    localServerUrl: string;
+    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
+    hasDynamicContext: boolean;
+    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
+}): Promise<Agent> {
+    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
+    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
+    const runtimeAgentReferenceResolver = options.hasDynamicContext
+        ? createBookScopedAgentReferenceResolver({
+              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
+              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
+              localServerUrl: options.localServerUrl,
+              fallbackResolver: options.baseAgentReferenceResolver,
+          })
+        : undefined;
+    const agentModelRequirements =
+        options.preparedAgentModelRequirements?.modelRequirements ||
+        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
+            agentReferenceResolver: runtimeAgentReferenceResolver,
+            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
+        }));
+
+    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
+        options.agentSource,
+        options.resolvedAgentContext.resolvedAgentName,
+        await baseOpenAiToolsPromise,
+        {
+            includeDynamicContext: true,
+            agentId: options.agentId,
+            modelRequirements: agentModelRequirements,
+        },
+    );
+
+    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
+        agentName: options.agentName,
+        assistantCacheKey: agentKitResult.assistantCacheKey,
+        vectorStoreHash: agentKitResult.vectorStoreHash,
+        vectorStoreId: agentKitResult.vectorStoreId,
+    });
+
+    return new Agent({
+        agentSource: options.agentSource,
+        executionTools: {
+            llm: agentKitResult.tools,
+        },
+        precomputedModelRequirements: agentModelRequirements,
+        assistantPreparationMode: 'external',
+        isVerbose: true,
+        teacherAgent: null,
+    });
+}

file update:
diff --git a/apps/agents-server/src/utils/handleChatCompletion.ts b/apps/agents-server/src/utils/handleChatCompletion.ts
index 9c96d36fad9cf24bf26d89f57372332add4e4595..cff56fafd6cfa90bb4094f9f202c4f01f6f74af1
--- a/apps/agents-server/src/utils/handleChatCompletion.ts
+++ b/apps/agents-server/src/utils/handleChatCompletion.ts
@@ -1,392 +1,16 @@
-import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
-import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
-import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
-import {
-    createBookScopedAgentReferenceResolver,
-    parseBookScopedAgentIdentifier,
-} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
-import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
-import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
-import {
-    appendMessageSuffix,
-    createMessageSuffixAppendix,
-    emulateMessageSuffixStreaming,
-    resolveMessageSuffixFromAgentSource,
-} from '@/src/utils/chat/messageSuffix';
-import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
-import { prepareToolCallsForStreaming } from './toolCallStreaming';
-import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
-import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
-import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
-import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
-import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
-import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
-import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
-import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
-import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
-import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
-import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
-import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
-import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
-import type {
-    ChatMessage,
-    ChatPrompt,
-    LlmToolDefinition,
-    string_book,
-    ToolCall,
-    TODO_any,
-    UncertainNumber,
-    Usage,
-    UsageCounts,
-} from '@promptbook-local/types';
-import { $getCurrentDate } from '@promptbook-local/utils';
 import { NextRequest, NextResponse } from 'next/server';
-import type OpenAI from 'openai';
-import { computeUsageCounts } from '../../../../src/execution/utils/computeUsageCounts';
-import { encodeChatStreamWhitespaceForTransport } from '../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
-import { isAgentDeleted } from '../app/agents/[agentName]/_utils';
 import { HTTP_STATUS_CODES } from '../constants';
-import { AgentKitCacheManager } from './cache/AgentKitCacheManager';
 import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
+import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
+import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
+import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
+import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
+import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
 import { validateApiKey } from './validateApiKey';
-import {
-    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    resolveAgentCollectionTablePrefix,
-    waitForRunningAgentPreparation,
-} from './agentPreparation';
-import {
-    resolveCachedServerAgentContext,
-    resolveCachedServerAgentModelRequirements,
-} from './cachedServerAgentRuntime';
-import { resolveAppendOnlySelfLearningAgentSource } from './resolveAppendOnlySelfLearningAgentSource';
 
 /**
- * Falls back to the estimated value when the original token count is unknown.
- *
- * @param tokenCount - Token count reported by the execution tools.
- * @param fallbackValue - Estimated token count based on text length.
- * @returns Token count to report in the OpenAI-compatible response.
- */
-function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
-    if (tokenCount.value === 0 && tokenCount.isUncertain) {
-        return {
-            value: fallbackValue,
-            isUncertain: true,
-        };
-    }
-
-    return tokenCount;
-}
-
-/**
- * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
- *
- * @param promptContent - Prompt content used for the request.
- * @param completionContent - Assistant response content.
- * @param usage - Native Promptbook usage details.
- * @returns Usage payload for compatibility responses.
- */
-function createCompatibilityUsage(
-    promptContent: string,
-    completionContent: string,
-    usage: Usage,
-): {
-    prompt_tokens: number;
-    completion_tokens: number;
-    total_tokens: number;
-    details: Usage;
-} {
-    const promptCounts = computeUsageCounts(promptContent);
-    const completionCounts = computeUsageCounts(completionContent);
-    const inputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
-        ...promptCounts,
-    };
-    const outputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
-        ...completionCounts,
-    };
-
-    const promptTokens = inputUsage.tokensCount.value;
-    const completionTokens = outputUsage.tokensCount.value;
-
-    return {
-        prompt_tokens: promptTokens,
-        completion_tokens: completionTokens,
-        total_tokens: promptTokens + completionTokens,
-        details: {
-            price: usage.price,
-            duration: usage.duration,
-            input: inputUsage,
-            output: outputUsage,
-        },
-    };
-}
-
-/**
- * Definition of open AI chat tool.
- */
-type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
-
-/**
- * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
- */
-type OpenAiCompatibilityMessageLike = {
-    role?: unknown;
-    content?: unknown;
-};
-
-/**
- * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
- */
-type HandleChatCompletionParsedRequest = {
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    threadMessages: ReadonlyArray<TODO_any>;
-    stream: TODO_any;
-    model: TODO_any;
-    responseFormat: TODO_any;
-    runtimeTools?: Array<LlmToolDefinition>;
-    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-    incomingParameters: Record<string, unknown>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Runtime state resolved before the chat model is executed.
- */
-type HandleChatCompletionRuntime = {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    agent: Agent;
-    agentSource: string_book;
-    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
-    messageSuffix: string | null;
-    agentId: string;
-    agentHash: string;
-    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Prompt, history recorder, and persistence context created for one chat call.
- */
-type HandleChatCompletionPromptContext = {
-    prompt: ChatPrompt;
-    userMessageHash: string;
-    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
-    persistedFrozenChatId?: string;
-};
-
-/**
- * Handles convert open Ai tools.
- */
-function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
-    if (!Array.isArray(rawTools)) {
-        return undefined;
-    }
-
-    const converted = rawTools
-        .map((tool) => convertOpenAiTool(tool))
-        .filter((tool): tool is LlmToolDefinition => tool !== null);
-
-    return converted.length > 0 ? converted : undefined;
-}
-
-/**
- * Handles convert open Ai tool.
- */
-function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
-    const tool = rawTool as OpenAIChatToolDefinition;
-    if (!tool || tool.type !== 'function') {
-        return null;
-    }
-
-    const functionDefinition = tool.function;
-    if (!functionDefinition) {
-        return null;
-    }
-
-    const { name, description, parameters } = functionDefinition;
-    if (typeof name !== 'string' || name.trim().length === 0) {
-        return null;
-    }
-
-    const parameterSchema = parameters ?? {};
-    if (parameterSchema.type !== 'object') {
-        return null;
-    }
-
-    const properties = parameterSchema.properties ?? {};
-    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
-
-    for (const [key, value] of Object.entries(properties)) {
-        if (!value || typeof value !== 'object') {
-            continue;
-        }
-
-        normalizedProperties[key] = {
-            type: typeof value.type === 'string' ? value.type : 'string',
-            description: typeof value.description === 'string' ? value.description : undefined,
-        };
-    }
-
-    const required =
-        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
-            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
-            : undefined;
-
-    return {
-        name,
-        description: typeof description === 'string' ? description : '',
-        parameters: {
-            type: 'object',
-            properties: normalizedProperties,
-            required,
-            additionalProperties:
-                typeof parameterSchema.additionalProperties === 'boolean'
-                    ? parameterSchema.additionalProperties
-                    : undefined,
-        },
-    };
-}
-
-/**
- * Parses open Ai tool choice.
- */
-function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
-    if (value === undefined || value === null) {
-        return undefined;
-    }
-
-    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-}
-
-/**
- * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
- */
-function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
-    if (typeof value === 'string') {
-        return value;
-    }
-
-    if (Array.isArray(value)) {
-        return value
-            .map((part) => {
-                if (typeof part === 'string') {
-                    return part;
-                }
-
-                if (!part || typeof part !== 'object') {
-                    return '';
-                }
-
-                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
-                if (typeof partLike.text === 'string') {
-                    return partLike.text;
-                }
-
-                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
-                    return '[Image input]';
-                }
-
-                if (partLike.image_url) {
-                    return '[Image input]';
-                }
-
-                return '';
-            })
-            .filter(Boolean)
-            .join('\n');
-    }
-
-    if (value === null || value === undefined) {
-        return '';
-    }
-
-    try {
-        return JSON.stringify(value, null, 2);
-    } catch {
-        return String(value);
-    }
-}
-
-/**
- * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ * Handles convert open Ai chat completion requests routed through the Agents Server.
  */
-function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
-    if (typeof role !== 'string') {
-        return 'USER';
-    }
-
-    switch (role) {
-        case 'assistant':
-            return 'AGENT';
-        case 'system':
-            return 'SYSTEM';
-        case 'tool':
-            return 'TOOL';
-        case 'developer':
-            return 'SYSTEM';
-        default:
-            return 'USER';
-    }
-}
-
-/**
- * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
- */
-function createFrozenOpenAiCompatibilityMessages(
-    rawMessages: ReadonlyArray<unknown>,
-    assistantContent?: string,
-    options: {
-        includeAssistantPlaceholder?: boolean;
-    } = {},
-): Array<ChatMessage> {
-    const startedAt = Date.now();
-    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
-        const message = rawMessage as OpenAiCompatibilityMessageLike;
-
-        return {
-            id: `openai-frozen-${index}`,
-            sender: mapOpenAiCompatibilityRoleToSender(message.role),
-            content: normalizeOpenAiCompatibilityMessageContent(message.content),
-            isComplete: true,
-            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage;
-    });
-
-    if (assistantContent !== undefined) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: assistantContent,
-            isComplete: true,
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    } else if (options.includeAssistantPlaceholder) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: '',
-            isComplete: false,
-            lifecycleState: 'running',
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    }
-
-    return messages;
-}
-
-/**
- * Handles chat completion.
- */
 export async function handleChatCompletion(
     request: NextRequest,
     params: { agentName?: string },
@@ -399,7 +23,6 @@
         return versionMismatchResponse;
     }
 
-    // Validate API key explicitly (in addition to middleware)
     const apiKeyValidation = await validateApiKey(request);
     if (!apiKeyValidation.isValid) {
         return NextResponse.json(
@@ -462,724 +85,5 @@
             { error: { message: (error as Error).message || 'Internal Server Error', type: 'server_error' } },
             { status: HTTP_STATUS_CODES.INTERNAL_SERVER_ERROR },
         );
-    }
-}
-
-/**
- * Parses and validates the OpenAI-compatible request payload.
- */
-function parseHandleChatCompletionRequest(options: {
-    body: TODO_any;
-    agentNameFromParams?: string;
-    request: NextRequest;
-}): HandleChatCompletionParsedRequest | NextResponse {
-    const { body, agentNameFromParams, request } = options;
-    const {
-        messages,
-        stream,
-        model,
-        response_format: responseFormat,
-        tools: rawTools,
-        tool_choice: toolChoice,
-        parameters: rawParameters = {},
-    } = body;
-    const agentName = (agentNameFromParams || model) as string | undefined;
-    if (!agentName) {
-        return createHandleChatCompletionErrorResponse(
-            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    if (!messages || !Array.isArray(messages) || messages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array is required and cannot be empty.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
-    if (threadMessages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array must contain at least one non-system message.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    return {
-        agentName,
-        messages,
-        threadMessages,
-        stream,
-        model,
-        responseFormat,
-        runtimeTools: convertOpenAiTools(rawTools),
-        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
-        incomingParameters:
-            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
-                ? (rawParameters as Record<string, unknown>)
-                : {},
-        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
-    };
-}
-
-/**
- * Resolves agent/runtime dependencies needed before prompt execution.
- */
-async function resolveHandleChatCompletionRuntime(options: {
-    request: NextRequest;
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    isPrivateModeEnabled: boolean;
-}): Promise<HandleChatCompletionRuntime | NextResponse> {
-    const { request, agentName, messages, isPrivateModeEnabled } = options;
-    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
-    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
-
-    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
-        return createHandleChatCompletionErrorResponse(
-            'This agent has been deleted. You can restore it from the Recycle Bin.',
-            'agent_deleted',
-            410,
-        );
-    }
-
-    const [collection, baseAgentReferenceResolver] = await Promise.all([
-        $provideAgentCollectionForServer(),
-        $provideAgentReferenceResolver(),
-    ]);
-    const localServerUrl = new URL(request.url).origin;
-    const resolvedAgentContext = await resolveCachedServerAgentContext({
-        collection,
-        agentIdentifier: agentName,
-        localServerUrl,
-        fallbackResolver: baseAgentReferenceResolver,
-    }).catch(() => null);
-
-    if (!resolvedAgentContext?.resolvedAgentSource) {
-        return createHandleChatCompletionErrorResponse(
-            `Agent '${agentName}' not found.`,
-            'invalid_request_error',
-            HTTP_STATUS_CODES.NOT_FOUND,
-        );
-    }
-
-    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
-    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
-    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
-    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
-    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
-    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
-    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
-    agentSource = agentSourceWithContext;
-
-    const preparedAgentModelRequirements = !hasDynamicContext
-        ? await resolveCachedServerAgentModelRequirements({
-              resolvedAgentContext,
-              localServerUrl,
-              fallbackResolver: baseAgentReferenceResolver,
-          })
-        : null;
-    const agentId = resolvedAgentContext.parentAgentPermanentId;
-    const agentHash = computeAgentHash(agentSource);
-    await waitForHandleChatCompletionAgentPreparation({
-        collection,
-        agentId,
-        agentHash,
-    });
-
-    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
-    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
-        await resolveHandleChatCompletionCredentials({
-            currentUserIdentity,
-            agentId,
-            calendarConnections,
-            useEmailConfiguration,
-        });
-    const agent = await createHandleChatCompletionAgent({
-        agentName,
-        agentSource,
-        agentId,
-        resolvedAgentContext,
-        localServerUrl,
-        baseAgentReferenceResolver,
-        hasDynamicContext,
-        preparedAgentModelRequirements,
-    });
-
-    return {
-        collection,
-        resolvedAgentContext,
-        agent,
-        agentSource,
-        unresolvedAgentSource,
-        messageSuffix,
-        agentId,
-        agentHash,
-        projectRepositories,
-        calendarConnections,
-        useEmailConfiguration,
-        currentUserIdentity,
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-        isPrivateModeEnabled,
-    };
-}
-
-/**
- * Creates prompt and persistence helpers for one chat completion request.
- */
-async function createHandleChatCompletionPromptContext(options: {
-    request: NextRequest;
-    title: string;
-    apiKey: string | null;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-}): Promise<HandleChatCompletionPromptContext> {
-    const { request, title, apiKey, parsedRequest, runtime } = options;
-    const timezone = request.headers.get('x-timezone') || 'UTC';
-    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
-    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
-    const recordChatHistoryMessage = await createChatHistoryRecorder({
-        request,
-        agentIdentifier: runtime.agentId,
-        agentHash: runtime.agentHash,
-        source: 'OPENAI_API_COMPATIBILITY',
-        apiKey,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        isEnabled: !runtime.isPrivateModeEnabled,
-    });
-    const userMessageHash = await recordChatHistoryMessage({
-        message: {
-            role: 'USER',
-            content: lastMessage.content,
-        },
-        previousMessageHash: null,
-    });
-    const promptParameters = composePromptParametersWithMemoryContext({
-        baseParameters: {
-            ...parsedRequest.incomingParameters,
-            timezone,
-        },
-        currentUserIdentity: runtime.currentUserIdentity,
-        agentPermanentId: runtime.agentId,
-        agentName: runtime.resolvedAgentContext.resolvedAgentName,
-        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
-        projectRepositories: runtime.projectRepositories,
-        projectGithubToken: runtime.projectGithubToken,
-        emailSmtpCredential: runtime.emailSmtpCredential,
-        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
-        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
-        calendarConnections: runtime.calendarConnections,
-    });
-    const prompt: ChatPrompt = {
-        title,
-        content: lastMessage.content,
-        modelRequirements: {
-            modelVariant: 'CHAT',
-            responseFormat: parsedRequest.responseFormat,
-            toolChoice: parsedRequest.runtimeToolChoice,
-            // We could pass 'model' from body if we wanted to enforce it, but Agent usually has its own config
-        },
-        parameters: promptParameters,
-        thread,
-        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
-    };
-
-    return {
-        prompt,
-        userMessageHash,
-        recordChatHistoryMessage,
-        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
-            isEnabled: !runtime.isPrivateModeEnabled,
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentId: runtime.agentId,
-            messages: parsedRequest.messages,
-            includeAssistantPlaceholder: true,
-            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
-        }),
-    };
-}
-
-/**
- * Handles the streaming OpenAI-compatible response branch.
- */
-function createHandleChatCompletionStreamResponse(options: {
-    request: NextRequest;
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Response {
-    const { request, title, parsedRequest, runtime, promptContext } = options;
-    const encoder = new TextEncoder();
-    const responseModel = parsedRequest.model || 'promptbook-agent';
-    const readableStream = new ReadableStream({
-        async start(controller) {
-            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
-            const created = Math.floor(Date.now() / 1000);
-            const emitSsePayload = (payload: unknown) => {
-                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
-            };
-            const emitDeltaChunk = (deltaContent: string) => {
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {
-                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
-                        },
-                        finishReason: null,
-                    }),
-                );
-            };
-
-            emitSsePayload(
-                createHandleChatCompletionChunk({
-                    runId,
-                    created,
-                    model: responseModel,
-                    delta: {
-                        role: 'assistant',
-                        content: '',
-                    },
-                    finishReason: null,
-                }),
-            );
-
-            let hasMeaningfulDelta = false;
-
-            try {
-                const handleStreamChunk = createChatStreamHandler({
-                    onDelta: (deltaContent) => {
-                        if (deltaContent.trim().length > 0) {
-                            hasMeaningfulDelta = true;
-                        }
-                        emitDeltaChunk(deltaContent);
-                    },
-                    onToolCalls: (toolCalls) => {
-                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
-                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
-                    },
-                });
-                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
-                    signal: request.signal,
-                });
-                const normalizedResponse = ensureNonEmptyChatContent({
-                    content: result.content,
-                    context: title,
-                });
-
-                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
-                    emitDeltaChunk(normalizedResponse.content);
-                }
-
-                const messageSuffixAppendix = createMessageSuffixAppendix(
-                    normalizedResponse.content,
-                    runtime.messageSuffix,
-                );
-                if (messageSuffixAppendix) {
-                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
-                        emitDeltaChunk(delta);
-                    });
-                }
-
-                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-                await finalizeHandleChatCompletionResult({
-                    responseContentWithSuffix,
-                    usage: result.usage,
-                    toolCalls: result.toolCalls,
-                    parsedRequest,
-                    runtime,
-                    promptContext,
-                    // Preserve historical behavior: streamed OpenAI-compatible calls still persist learning in private mode.
-                    shouldPersistLearnedAgentSource: true,
-                });
-
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {},
-                        finishReason: 'stop',
-                    }),
-                );
-                controller.enqueue(encoder.encode('[DONE]'));
-            } catch (error) {
-                console.error('Error during streaming:', error);
-                controller.error(error);
-            }
-            controller.close();
-        },
-    });
-
-    return new Response(readableStream, {
-        headers: {
-            'Content-Type': 'text/event-stream',
-            'Cache-Control': 'no-cache',
-            Connection: 'keep-alive',
-        },
-    });
-}
-
-/**
- * Handles the non-streaming OpenAI-compatible response branch.
- */
-async function createHandleChatCompletionJsonResponse(options: {
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Promise<NextResponse> {
-    const { title, parsedRequest, runtime, promptContext } = options;
-    const result = await runtime.agent.callChatModel(promptContext.prompt);
-    const normalizedResponse = ensureNonEmptyChatContent({
-        content: result.content,
-        context: title,
-    });
-    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-
-    await finalizeHandleChatCompletionResult({
-        responseContentWithSuffix,
-        usage: result.usage,
-        toolCalls: result.toolCalls,
-        parsedRequest,
-        runtime,
-        promptContext,
-        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
-    });
-
-    return NextResponse.json({
-        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
-        object: 'chat.completion',
-        created: Math.floor(Date.now() / 1000),
-        model: parsedRequest.model || 'promptbook-agent',
-        choices: [
-            {
-                index: 0,
-                message: {
-                    role: 'assistant',
-                    content: responseContentWithSuffix,
-                },
-                finish_reason: 'stop',
-            },
-        ],
-        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
-    });
-}
-
-/**
- * Runs the shared side effects after one successful model response.
- */
-async function finalizeHandleChatCompletionResult(options: {
-    responseContentWithSuffix: string;
-    usage: Usage;
-    toolCalls?: ReadonlyArray<ToolCall>;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
-
-    await promptContext.recordChatHistoryMessage({
-        message: {
-            role: 'MODEL',
-            content: responseContentWithSuffix,
-        },
-        previousMessageHash: promptContext.userMessageHash,
-        usage,
-    });
-
-    if (toolCalls && toolCalls.length > 0) {
-        await logCalendarToolCallsActivity({
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentPermanentId: runtime.agentId,
-            toolCalls,
-        });
-    }
-
-    await persistOpenAiCompatibilityFrozenChat({
-        isEnabled: !runtime.isPrivateModeEnabled,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        agentId: runtime.agentId,
-        chatId: promptContext.persistedFrozenChatId,
-        messages: parsedRequest.messages,
-        assistantContent: responseContentWithSuffix,
-        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
-    });
-    await persistHandleChatCompletionLearnedAgentSource({
-        runtime,
-        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
-    });
-}
-
-/**
- * Applies append-only self-learning updates when the current branch should persist them.
- */
-async function persistHandleChatCompletionLearnedAgentSource(options: {
-    runtime: HandleChatCompletionRuntime;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { runtime, shouldPersistLearnedAgentSource } = options;
-    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
-        return;
-    }
-
-    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
-        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
-        resolvedAgentSourceBeforeLearning: runtime.agentSource,
-        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
-    });
-
-    if (learnedAgentSource !== null) {
-        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
     }
 }
-
-/**
- * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
- */
-async function persistOpenAiCompatibilityFrozenChat(options: {
-    isEnabled: boolean;
-    userId: number | null | undefined;
-    agentId: string;
-    messages: ReadonlyArray<unknown>;
-    chatId?: string;
-    assistantContent?: string;
-    includeAssistantPlaceholder?: boolean;
-    failureMessage: string;
-}): Promise<string | undefined> {
-    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
-        options;
-    if (!isEnabled || !userId) {
-        return undefined;
-    }
-
-    const persistedFrozenChat = await persistFrozenUserChat({
-        userId,
-        agentPermanentId: agentId,
-        source: USER_CHAT_SOURCES.OPENAI_API,
-        chatId,
-        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
-            includeAssistantPlaceholder,
-        }),
-    }).catch((error) => {
-        console.error(failureMessage, error);
-        return null;
-    });
-
-    return persistedFrozenChat?.id;
-}
-
-/**
- * Builds one OpenAI-compatible thread from the earlier request messages.
- */
-function createHandleChatCompletionThread(previousMessages: ReadonlyArray<TODO_any>): ChatMessage[] {
-    return previousMessages.map((message: TODO_any, index: number) => ({
-        id: `msg-${index}`,
-        sender: message.role === 'assistant' ? 'agent' : 'user',
-        content: message.content,
-        isComplete: true,
-        createdAt: $getCurrentDate(),
-    }));
-}
-
-/**
- * Appends system messages to the agent source as dynamic `CONTEXT`.
- */
-function appendSystemMessagesToAgentSource(
-    agentSource: string_book,
-    messages: ReadonlyArray<TODO_any>,
-): {
-    agentSourceWithContext: string_book;
-    hasDynamicContext: boolean;
-} {
-    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
-    if (systemMessages.length === 0) {
-        return {
-            agentSourceWithContext: agentSource,
-            hasDynamicContext: false,
-        };
-    }
-
-    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
-    return {
-        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
-        hasDynamicContext: true,
-    };
-}
-
-/**
- * Waits for any running preparation job of the current agent before executing the chat call.
- */
-async function waitForHandleChatCompletionAgentPreparation(options: {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    agentId: string;
-    agentHash: string;
-}): Promise<void> {
-    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
-    const preparationWaitResult = await waitForRunningAgentPreparation({
-        tablePrefix,
-        agentPermanentId: options.agentId,
-        fingerprint: options.agentHash,
-        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    });
-
-    if (preparationWaitResult !== 'not_running') {
-        console.info('[pre-index]', 'openai_chat_wait_result', {
-            tablePrefix,
-            agentPermanentId: options.agentId,
-            agentHash: options.agentHash,
-            preparationWaitResult,
-        });
-    }
-}
-
-/**
- * Resolves request-scoped project, calendar, and email credentials for the current agent.
- */
-async function resolveHandleChatCompletionCredentials(options: {
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    agentId: string;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-}): Promise<{
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-}> {
-    const userId = options.currentUserIdentity?.userId;
-    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
-        resolveUseProjectGithubToken({
-            userId,
-            agentPermanentId: options.agentId,
-        }),
-        options.calendarConnections.length > 0
-            ? resolveUseCalendarGoogleToken({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-        options.useEmailConfiguration.isEnabled
-            ? resolveUseEmailSmtpCredential({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-    ]);
-
-    return {
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-    };
-}
-
-/**
- * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
- */
-async function createHandleChatCompletionAgent(options: {
-    agentName: string;
-    agentSource: string_book;
-    agentId: string;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    localServerUrl: string;
-    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
-    hasDynamicContext: boolean;
-    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
-}): Promise<Agent> {
-    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
-    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
-    const runtimeAgentReferenceResolver = options.hasDynamicContext
-        ? createBookScopedAgentReferenceResolver({
-              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
-              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
-              localServerUrl: options.localServerUrl,
-              fallbackResolver: options.baseAgentReferenceResolver,
-          })
-        : undefined;
-    const agentModelRequirements =
-        options.preparedAgentModelRequirements?.modelRequirements ||
-        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
-            agentReferenceResolver: runtimeAgentReferenceResolver,
-            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
-        }));
-
-    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
-        options.agentSource,
-        options.resolvedAgentContext.resolvedAgentName,
-        await baseOpenAiToolsPromise,
-        {
-            includeDynamicContext: true,
-            agentId: options.agentId,
-            modelRequirements: agentModelRequirements,
-        },
-    );
-
-    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
-        agentName: options.agentName,
-        assistantCacheKey: agentKitResult.assistantCacheKey,
-        vectorStoreHash: agentKitResult.vectorStoreHash,
-        vectorStoreId: agentKitResult.vectorStoreId,
-    });
-
-    return new Agent({
-        agentSource: options.agentSource,
-        executionTools: {
-            llm: agentKitResult.tools,
-        },
-        precomputedModelRequirements: agentModelRequirements,
-        assistantPreparationMode: 'external',
-        isVerbose: true,
-        teacherAgent: null,
-    });
-}
-
-/**
- * Creates one OpenAI-compatible streaming chunk payload.
- */
-function createHandleChatCompletionChunk(options: {
-    runId: string;
-    created: number;
-    model: TODO_any;
-    delta: Record<string, unknown>;
-    finishReason: 'stop' | null;
-}) {
-    return {
-        id: options.runId,
-        object: 'chat.completion.chunk',
-        created: options.created,
-        model: options.model,
-        choices: [
-            {
-                index: 0,
-                delta: options.delta,
-                finish_reason: options.finishReason,
-            },
-        ],
-    };
-}
-
-/**
- * Creates one consistent OpenAI-compatible error response.
- */
-function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
-    return NextResponse.json(
-        {
-            error: {
-                message,
-                type,
-            },
-        },
-        { status },
-    );
-}
-
-// TODO: [🈹] Maybe move chat thread handling here
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f49a4fa80fa21f140e25de59596e5546d62e3f7
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
@@ -0,0 +1,18 @@
+import { NextResponse } from 'next/server';
+
+/**
+ * Creates one consistent OpenAI-compatible error response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
+    return NextResponse.json(
+        {
+            error: {
+                message,
+                type,
+            },
+        },
+        { status },
+    );
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b1979f4621921bc664fed75a05851c6fcb8fba6d
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
@@ -0,0 +1,120 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import { appendMessageSuffix } from '@/src/utils/chat/messageSuffix';
+import type { UncertainNumber, Usage, UsageCounts } from '@promptbook-local/types';
+import { NextResponse } from 'next/server';
+import { computeUsageCounts } from '../../../../../src/execution/utils/computeUsageCounts';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the non-streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionJsonResponse(options: {
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Promise<NextResponse> {
+    const { title, parsedRequest, runtime, promptContext } = options;
+    const result = await runtime.agent.callChatModel(promptContext.prompt);
+    const normalizedResponse = ensureNonEmptyChatContent({
+        content: result.content,
+        context: title,
+    });
+    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+
+    await finalizeHandleChatCompletionResult({
+        responseContentWithSuffix,
+        usage: result.usage,
+        toolCalls: result.toolCalls,
+        parsedRequest,
+        runtime,
+        promptContext,
+        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
+    });
+
+    return NextResponse.json({
+        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
+        object: 'chat.completion',
+        created: Math.floor(Date.now() / 1000),
+        model: parsedRequest.model || 'promptbook-agent',
+        choices: [
+            {
+                index: 0,
+                message: {
+                    role: 'assistant',
+                    content: responseContentWithSuffix,
+                },
+                finish_reason: 'stop',
+            },
+        ],
+        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
+    });
+}
+
+/**
+ * Falls back to the estimated value when the original token count is unknown.
+ *
+ * @param tokenCount - Token count reported by the execution tools.
+ * @param fallbackValue - Estimated token count based on text length.
+ * @returns Token count to report in the OpenAI-compatible response.
+ */
+function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
+    if (tokenCount.value === 0 && tokenCount.isUncertain) {
+        return {
+            value: fallbackValue,
+            isUncertain: true,
+        };
+    }
+
+    return tokenCount;
+}
+
+/**
+ * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
+ *
+ * @param promptContent - Prompt content used for the request.
+ * @param completionContent - Assistant response content.
+ * @param usage - Native Promptbook usage details.
+ * @returns Usage payload for compatibility responses.
+ */
+function createCompatibilityUsage(
+    promptContent: string,
+    completionContent: string,
+    usage: Usage,
+): {
+    prompt_tokens: number;
+    completion_tokens: number;
+    total_tokens: number;
+    details: Usage;
+} {
+    const promptCounts = computeUsageCounts(promptContent);
+    const completionCounts = computeUsageCounts(completionContent);
+    const inputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
+        ...promptCounts,
+    };
+    const outputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
+        ...completionCounts,
+    };
+
+    const promptTokens = inputUsage.tokensCount.value;
+    const completionTokens = outputUsage.tokensCount.value;
+
+    return {
+        prompt_tokens: promptTokens,
+        completion_tokens: completionTokens,
+        total_tokens: promptTokens + completionTokens,
+        details: {
+            price: usage.price,
+            duration: usage.duration,
+            input: inputUsage,
+            output: outputUsage,
+        },
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..7aac8141a8b1237ce36430b4b4598a618f0383ac
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
@@ -0,0 +1,111 @@
+import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
+import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
+import type { ChatMessage, ChatPrompt } from '@promptbook-local/types';
+import { $getCurrentDate } from '@promptbook-local/utils';
+import type { NextRequest } from 'next/server';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Prompt, history recorder, and persistence context created for one chat call.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionPromptContext = {
+    prompt: ChatPrompt;
+    userMessageHash: string;
+    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
+    persistedFrozenChatId?: string;
+};
+
+/**
+ * Creates prompt and persistence helpers for one chat completion request.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionPromptContext(options: {
+    request: NextRequest;
+    title: string;
+    apiKey: string | null;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+}): Promise<HandleChatCompletionPromptContext> {
+    const { request, title, apiKey, parsedRequest, runtime } = options;
+    const timezone = request.headers.get('x-timezone') || 'UTC';
+    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
+    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
+    const recordChatHistoryMessage = await createChatHistoryRecorder({
+        request,
+        agentIdentifier: runtime.agentId,
+        agentHash: runtime.agentHash,
+        source: 'OPENAI_API_COMPATIBILITY',
+        apiKey,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        isEnabled: !runtime.isPrivateModeEnabled,
+    });
+    const userMessageHash = await recordChatHistoryMessage({
+        message: {
+            role: 'USER',
+            content: lastMessage.content,
+        },
+        previousMessageHash: null,
+    });
+    const promptParameters = composePromptParametersWithMemoryContext({
+        baseParameters: {
+            ...parsedRequest.incomingParameters,
+            timezone,
+        },
+        currentUserIdentity: runtime.currentUserIdentity,
+        agentPermanentId: runtime.agentId,
+        agentName: runtime.resolvedAgentContext.resolvedAgentName,
+        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
+        projectRepositories: runtime.projectRepositories,
+        projectGithubToken: runtime.projectGithubToken,
+        emailSmtpCredential: runtime.emailSmtpCredential,
+        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
+        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
+        calendarConnections: runtime.calendarConnections,
+    });
+    const prompt: ChatPrompt = {
+        title,
+        content: lastMessage.content,
+        modelRequirements: {
+            modelVariant: 'CHAT',
+            responseFormat: parsedRequest.responseFormat,
+            toolChoice: parsedRequest.runtimeToolChoice,
+        },
+        parameters: promptParameters,
+        thread,
+        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
+    };
+
+    return {
+        prompt,
+        userMessageHash,
+        recordChatHistoryMessage,
+        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
+            isEnabled: !runtime.isPrivateModeEnabled,
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentId: runtime.agentId,
+            messages: parsedRequest.messages,
+            includeAssistantPlaceholder: true,
+            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
+        }),
+    };
+}
+
+/**
+ * Builds one OpenAI-compatible thread from the earlier request messages.
+ */
+function createHandleChatCompletionThread(
+    previousMessages: HandleChatCompletionParsedRequest['threadMessages'],
+): Array<ChatMessage> {
+    return previousMessages.map((message, index) => ({
+        id: `msg-${index}`,
+        sender: message.role === 'assistant' ? 'agent' : 'user',
+        content: message.content,
+        isComplete: true,
+        createdAt: $getCurrentDate(),
+    }));
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..85e096027760bc45fb730e74f8ae3bbb721b1029
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
@@ -0,0 +1,162 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import {
+    appendMessageSuffix,
+    createMessageSuffixAppendix,
+    emulateMessageSuffixStreaming,
+} from '@/src/utils/chat/messageSuffix';
+import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
+import { encodeChatStreamWhitespaceForTransport } from '../../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
+import { prepareToolCallsForStreaming } from '../toolCallStreaming';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionStreamResponse(options: {
+    request: Request;
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Response {
+    const { request, title, parsedRequest, runtime, promptContext } = options;
+    const encoder = new TextEncoder();
+    const responseModel = parsedRequest.model || 'promptbook-agent';
+    const readableStream = new ReadableStream({
+        async start(controller) {
+            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
+            const created = Math.floor(Date.now() / 1000);
+            const emitSsePayload = (payload: unknown) => {
+                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
+            };
+            const emitDeltaChunk = (deltaContent: string) => {
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {
+                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
+                        },
+                        finishReason: null,
+                    }),
+                );
+            };
+
+            emitSsePayload(
+                createHandleChatCompletionChunk({
+                    runId,
+                    created,
+                    model: responseModel,
+                    delta: {
+                        role: 'assistant',
+                        content: '',
+                    },
+                    finishReason: null,
+                }),
+            );
+
+            let hasMeaningfulDelta = false;
+
+            try {
+                const handleStreamChunk = createChatStreamHandler({
+                    onDelta: (deltaContent) => {
+                        if (deltaContent.trim().length > 0) {
+                            hasMeaningfulDelta = true;
+                        }
+                        emitDeltaChunk(deltaContent);
+                    },
+                    onToolCalls: (toolCalls) => {
+                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
+                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
+                    },
+                });
+                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
+                    signal: request.signal,
+                });
+                const normalizedResponse = ensureNonEmptyChatContent({
+                    content: result.content,
+                    context: title,
+                });
+
+                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
+                    emitDeltaChunk(normalizedResponse.content);
+                }
+
+                const messageSuffixAppendix = createMessageSuffixAppendix(
+                    normalizedResponse.content,
+                    runtime.messageSuffix,
+                );
+                if (messageSuffixAppendix) {
+                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
+                        emitDeltaChunk(delta);
+                    });
+                }
+
+                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+                await finalizeHandleChatCompletionResult({
+                    responseContentWithSuffix,
+                    usage: result.usage,
+                    toolCalls: result.toolCalls,
+                    parsedRequest,
+                    runtime,
+                    promptContext,
+                    shouldPersistLearnedAgentSource: true,
+                });
+
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {},
+                        finishReason: 'stop',
+                    }),
+                );
+                controller.enqueue(encoder.encode('[DONE]'));
+            } catch (error) {
+                console.error('Error during streaming:', error);
+                controller.error(error);
+            }
+            controller.close();
+        },
+    });
+
+    return new Response(readableStream, {
+        headers: {
+            'Content-Type': 'text/event-stream',
+            'Cache-Control': 'no-cache',
+            Connection: 'keep-alive',
+        },
+    });
+}
+
+/**
+ * Creates one OpenAI-compatible streaming chunk payload.
+ */
+function createHandleChatCompletionChunk(options: {
+    runId: string;
+    created: number;
+    model: HandleChatCompletionParsedRequest['model'];
+    delta: Record<string, unknown>;
+    finishReason: 'stop' | null;
+}) {
+    return {
+        id: options.runId,
+        object: 'chat.completion.chunk',
+        created: options.created,
+        model: options.model,
+        choices: [
+            {
+                index: 0,
+                delta: options.delta,
+                finish_reason: options.finishReason,
+            },
+        ],
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
new file mode 100644
index 0000000000000000000000000000000000000000..dd6a7d87ea491df245dbf93d8bcb277a9a10b034
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
@@ -0,0 +1,78 @@
+import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
+import { resolveAppendOnlySelfLearningAgentSource } from '@/src/utils/resolveAppendOnlySelfLearningAgentSource';
+import type { ToolCall, Usage } from '@promptbook-local/types';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Runs the shared side effects after one successful model response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function finalizeHandleChatCompletionResult(options: {
+    responseContentWithSuffix: string;
+    usage: Usage;
+    toolCalls?: ReadonlyArray<ToolCall>;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
+
+    await promptContext.recordChatHistoryMessage({
+        message: {
+            role: 'MODEL',
+            content: responseContentWithSuffix,
+        },
+        previousMessageHash: promptContext.userMessageHash,
+        usage,
+    });
+
+    if (toolCalls && toolCalls.length > 0) {
+        await logCalendarToolCallsActivity({
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentPermanentId: runtime.agentId,
+            toolCalls,
+        });
+    }
+
+    await persistOpenAiCompatibilityFrozenChat({
+        isEnabled: !runtime.isPrivateModeEnabled,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        agentId: runtime.agentId,
+        chatId: promptContext.persistedFrozenChatId,
+        messages: parsedRequest.messages,
+        assistantContent: responseContentWithSuffix,
+        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
+    });
+    await persistHandleChatCompletionLearnedAgentSource({
+        runtime,
+        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
+    });
+}
+
+/**
+ * Applies append-only self-learning updates when the current branch should persist them.
+ */
+async function persistHandleChatCompletionLearnedAgentSource(options: {
+    runtime: HandleChatCompletionRuntime;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { runtime, shouldPersistLearnedAgentSource } = options;
+    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
+        return;
+    }
+
+    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
+        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
+        resolvedAgentSourceBeforeLearning: runtime.agentSource,
+        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
+    });
+
+    if (learnedAgentSource !== null) {
+        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
+    }
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
new file mode 100644
index 0000000000000000000000000000000000000000..58e1c26d7f428c1bdb39b36411b3f75256e3de51
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
@@ -0,0 +1,176 @@
+import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
+import type { LlmToolDefinition, TODO_any } from '@promptbook-local/types';
+import { NextRequest, NextResponse } from 'next/server';
+import type OpenAI from 'openai';
+import { HTTP_STATUS_CODES } from '../../constants';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Definition of open AI chat tool.
+ */
+type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
+
+/**
+ * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionParsedRequest = {
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    threadMessages: ReadonlyArray<TODO_any>;
+    stream: TODO_any;
+    model: TODO_any;
+    responseFormat: TODO_any;
+    runtimeTools?: Array<LlmToolDefinition>;
+    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+    incomingParameters: Record<string, unknown>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Parses and validates the OpenAI-compatible request payload.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function parseHandleChatCompletionRequest(options: {
+    body: TODO_any;
+    agentNameFromParams?: string;
+    request: NextRequest;
+}): HandleChatCompletionParsedRequest | NextResponse {
+    const { body, agentNameFromParams, request } = options;
+    const {
+        messages,
+        stream,
+        model,
+        response_format: responseFormat,
+        tools: rawTools,
+        tool_choice: toolChoice,
+        parameters: rawParameters = {},
+    } = body;
+    const agentName = (agentNameFromParams || model) as string | undefined;
+    if (!agentName) {
+        return createHandleChatCompletionErrorResponse(
+            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    if (!messages || !Array.isArray(messages) || messages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array is required and cannot be empty.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
+    if (threadMessages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array must contain at least one non-system message.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    return {
+        agentName,
+        messages,
+        threadMessages,
+        stream,
+        model,
+        responseFormat,
+        runtimeTools: convertOpenAiTools(rawTools),
+        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
+        incomingParameters:
+            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
+                ? (rawParameters as Record<string, unknown>)
+                : {},
+        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
+    };
+}
+
+/**
+ * Handles convert open Ai tools.
+ */
+function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
+    if (!Array.isArray(rawTools)) {
+        return undefined;
+    }
+
+    const converted = rawTools
+        .map((tool) => convertOpenAiTool(tool))
+        .filter((tool): tool is LlmToolDefinition => tool !== null);
+
+    return converted.length > 0 ? converted : undefined;
+}
+
+/**
+ * Handles convert open Ai tool.
+ */
+function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
+    const tool = rawTool as OpenAIChatToolDefinition;
+    if (!tool || tool.type !== 'function') {
+        return null;
+    }
+
+    const functionDefinition = tool.function;
+    if (!functionDefinition) {
+        return null;
+    }
+
+    const { name, description, parameters } = functionDefinition;
+    if (typeof name !== 'string' || name.trim().length === 0) {
+        return null;
+    }
+
+    const parameterSchema = parameters ?? {};
+    if (parameterSchema.type !== 'object') {
+        return null;
+    }
+
+    const properties = parameterSchema.properties ?? {};
+    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
+
+    for (const [key, value] of Object.entries(properties)) {
+        if (!value || typeof value !== 'object') {
+            continue;
+        }
+
+        normalizedProperties[key] = {
+            type: typeof value.type === 'string' ? value.type : 'string',
+            description: typeof value.description === 'string' ? value.description : undefined,
+        };
+    }
+
+    const required =
+        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
+            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
+            : undefined;
+
+    return {
+        name,
+        description: typeof description === 'string' ? description : '',
+        parameters: {
+            type: 'object',
+            properties: normalizedProperties,
+            required,
+            additionalProperties:
+                typeof parameterSchema.additionalProperties === 'boolean'
+                    ? parameterSchema.additionalProperties
+                    : undefined,
+        },
+    };
+}
+
+/**
+ * Parses open Ai tool choice.
+ */
+function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
+    if (value === undefined || value === null) {
+        return undefined;
+    }
+
+    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
new file mode 100644
index 0000000000000000000000000000000000000000..60e5f1b008bcf5a6a1836c06c4c3d9ebcb909a9f
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
@@ -0,0 +1,163 @@
+import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
+import type { ChatMessage } from '@promptbook-local/types';
+
+/**
+ * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
+ */
+type OpenAiCompatibilityMessageLike = {
+    role?: unknown;
+    content?: unknown;
+};
+
+/**
+ * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function persistOpenAiCompatibilityFrozenChat(options: {
+    isEnabled: boolean;
+    userId: number | null | undefined;
+    agentId: string;
+    messages: ReadonlyArray<unknown>;
+    chatId?: string;
+    assistantContent?: string;
+    includeAssistantPlaceholder?: boolean;
+    failureMessage: string;
+}): Promise<string | undefined> {
+    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
+        options;
+    if (!isEnabled || !userId) {
+        return undefined;
+    }
+
+    const persistedFrozenChat = await persistFrozenUserChat({
+        userId,
+        agentPermanentId: agentId,
+        source: USER_CHAT_SOURCES.OPENAI_API,
+        chatId,
+        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
+            includeAssistantPlaceholder,
+        }),
+    }).catch((error) => {
+        console.error(failureMessage, error);
+        return null;
+    });
+
+    return persistedFrozenChat?.id;
+}
+
+/**
+ * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
+ */
+function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
+    if (typeof value === 'string') {
+        return value;
+    }
+
+    if (Array.isArray(value)) {
+        return value
+            .map((part) => {
+                if (typeof part === 'string') {
+                    return part;
+                }
+
+                if (!part || typeof part !== 'object') {
+                    return '';
+                }
+
+                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
+                if (typeof partLike.text === 'string') {
+                    return partLike.text;
+                }
+
+                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
+                    return '[Image input]';
+                }
+
+                if (partLike.image_url) {
+                    return '[Image input]';
+                }
+
+                return '';
+            })
+            .filter(Boolean)
+            .join('\n');
+    }
+
+    if (value === null || value === undefined) {
+        return '';
+    }
+
+    try {
+        return JSON.stringify(value, null, 2);
+    } catch {
+        return String(value);
+    }
+}
+
+/**
+ * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ */
+function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
+    if (typeof role !== 'string') {
+        return 'USER';
+    }
+
+    switch (role) {
+        case 'assistant':
+            return 'AGENT';
+        case 'system':
+            return 'SYSTEM';
+        case 'tool':
+            return 'TOOL';
+        case 'developer':
+            return 'SYSTEM';
+        default:
+            return 'USER';
+    }
+}
+
+/**
+ * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
+ */
+function createFrozenOpenAiCompatibilityMessages(
+    rawMessages: ReadonlyArray<unknown>,
+    assistantContent?: string,
+    options: {
+        includeAssistantPlaceholder?: boolean;
+    } = {},
+): Array<ChatMessage> {
+    const startedAt = Date.now();
+    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
+        const message = rawMessage as OpenAiCompatibilityMessageLike;
+
+        return {
+            id: `openai-frozen-${index}`,
+            sender: mapOpenAiCompatibilityRoleToSender(message.role),
+            content: normalizeOpenAiCompatibilityMessageContent(message.content),
+            isComplete: true,
+            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage;
+    });
+
+    if (assistantContent !== undefined) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: assistantContent,
+            isComplete: true,
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    } else if (options.includeAssistantPlaceholder) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: '',
+            isComplete: false,
+            lifecycleState: 'running',
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    }
+
+    return messages;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fd44b7ea5a87168725a9e99784038137c96537ab
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
@@ -0,0 +1,305 @@
+import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
+import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
+import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
+import type { string_book, TODO_any } from '@promptbook-local/types';
+import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
+import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
+import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
+import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
+import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
+import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
+import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
+import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
+import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
+import {
+    createBookScopedAgentReferenceResolver,
+    parseBookScopedAgentIdentifier,
+} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
+import { resolveMessageSuffixFromAgentSource } from '@/src/utils/chat/messageSuffix';
+import { NextRequest, NextResponse } from 'next/server';
+import { isAgentDeleted } from '../../app/agents/[agentName]/_utils';
+import {
+    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    resolveAgentCollectionTablePrefix,
+    waitForRunningAgentPreparation,
+} from '../agentPreparation';
+import { resolveCachedServerAgentContext, resolveCachedServerAgentModelRequirements } from '../cachedServerAgentRuntime';
+import { AgentKitCacheManager } from '../cache/AgentKitCacheManager';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Runtime state resolved before the chat model is executed.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionRuntime = {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    agent: Agent;
+    agentSource: string_book;
+    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
+    messageSuffix: string | null;
+    agentId: string;
+    agentHash: string;
+    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Resolves agent/runtime dependencies needed before prompt execution.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function resolveHandleChatCompletionRuntime(options: {
+    request: NextRequest;
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    isPrivateModeEnabled: boolean;
+}): Promise<HandleChatCompletionRuntime | NextResponse> {
+    const { request, agentName, messages, isPrivateModeEnabled } = options;
+    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
+    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
+
+    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
+        return createHandleChatCompletionErrorResponse(
+            'This agent has been deleted. You can restore it from the Recycle Bin.',
+            'agent_deleted',
+            410,
+        );
+    }
+
+    const [collection, baseAgentReferenceResolver] = await Promise.all([
+        $provideAgentCollectionForServer(),
+        $provideAgentReferenceResolver(),
+    ]);
+    const localServerUrl = new URL(request.url).origin;
+    const resolvedAgentContext = await resolveCachedServerAgentContext({
+        collection,
+        agentIdentifier: agentName,
+        localServerUrl,
+        fallbackResolver: baseAgentReferenceResolver,
+    }).catch(() => null);
+
+    if (!resolvedAgentContext?.resolvedAgentSource) {
+        return createHandleChatCompletionErrorResponse(`Agent '${agentName}' not found.`, 'invalid_request_error', 404);
+    }
+
+    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
+    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
+    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
+    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
+    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
+    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
+    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
+    agentSource = agentSourceWithContext;
+
+    const preparedAgentModelRequirements = !hasDynamicContext
+        ? await resolveCachedServerAgentModelRequirements({
+              resolvedAgentContext,
+              localServerUrl,
+              fallbackResolver: baseAgentReferenceResolver,
+          })
+        : null;
+    const agentId = resolvedAgentContext.parentAgentPermanentId;
+    const agentHash = computeAgentHash(agentSource);
+    await waitForHandleChatCompletionAgentPreparation({
+        collection,
+        agentId,
+        agentHash,
+    });
+
+    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
+    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
+        await resolveHandleChatCompletionCredentials({
+            currentUserIdentity,
+            agentId,
+            calendarConnections,
+            useEmailConfiguration,
+        });
+    const agent = await createHandleChatCompletionAgent({
+        agentName,
+        agentSource,
+        agentId,
+        resolvedAgentContext,
+        localServerUrl,
+        baseAgentReferenceResolver,
+        hasDynamicContext,
+        preparedAgentModelRequirements,
+    });
+
+    return {
+        collection,
+        resolvedAgentContext,
+        agent,
+        agentSource,
+        unresolvedAgentSource,
+        messageSuffix,
+        agentId,
+        agentHash,
+        projectRepositories,
+        calendarConnections,
+        useEmailConfiguration,
+        currentUserIdentity,
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+        isPrivateModeEnabled,
+    };
+}
+
+/**
+ * Appends system messages to the agent source as dynamic `CONTEXT`.
+ */
+function appendSystemMessagesToAgentSource(
+    agentSource: string_book,
+    messages: ReadonlyArray<TODO_any>,
+): {
+    agentSourceWithContext: string_book;
+    hasDynamicContext: boolean;
+} {
+    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
+    if (systemMessages.length === 0) {
+        return {
+            agentSourceWithContext: agentSource,
+            hasDynamicContext: false,
+        };
+    }
+
+    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
+    return {
+        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
+        hasDynamicContext: true,
+    };
+}
+
+/**
+ * Waits for any running preparation job of the current agent before executing the chat call.
+ */
+async function waitForHandleChatCompletionAgentPreparation(options: {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    agentId: string;
+    agentHash: string;
+}): Promise<void> {
+    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
+    const preparationWaitResult = await waitForRunningAgentPreparation({
+        tablePrefix,
+        agentPermanentId: options.agentId,
+        fingerprint: options.agentHash,
+        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    });
+
+    if (preparationWaitResult !== 'not_running') {
+        console.info('[pre-index]', 'openai_chat_wait_result', {
+            tablePrefix,
+            agentPermanentId: options.agentId,
+            agentHash: options.agentHash,
+            preparationWaitResult,
+        });
+    }
+}
+
+/**
+ * Resolves request-scoped project, calendar, and email credentials for the current agent.
+ */
+async function resolveHandleChatCompletionCredentials(options: {
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    agentId: string;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+}): Promise<{
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+}> {
+    const userId = options.currentUserIdentity?.userId;
+    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
+        resolveUseProjectGithubToken({
+            userId,
+            agentPermanentId: options.agentId,
+        }),
+        options.calendarConnections.length > 0
+            ? resolveUseCalendarGoogleToken({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+        options.useEmailConfiguration.isEnabled
+            ? resolveUseEmailSmtpCredential({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+    ]);
+
+    return {
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+    };
+}
+
+/**
+ * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
+ */
+async function createHandleChatCompletionAgent(options: {
+    agentName: string;
+    agentSource: string_book;
+    agentId: string;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    localServerUrl: string;
+    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
+    hasDynamicContext: boolean;
+    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
+}): Promise<Agent> {
+    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
+    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
+    const runtimeAgentReferenceResolver = options.hasDynamicContext
+        ? createBookScopedAgentReferenceResolver({
+              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
+              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
+              localServerUrl: options.localServerUrl,
+              fallbackResolver: options.baseAgentReferenceResolver,
+          })
+        : undefined;
+    const agentModelRequirements =
+        options.preparedAgentModelRequirements?.modelRequirements ||
+        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
+            agentReferenceResolver: runtimeAgentReferenceResolver,
+            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
+        }));
+
+    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
+        options.agentSource,
+        options.resolvedAgentContext.resolvedAgentName,
+        await baseOpenAiToolsPromise,
+        {
+            includeDynamicContext: true,
+            agentId: options.agentId,
+            modelRequirements: agentModelRequirements,
+        },
+    );
+
+    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
+        agentName: options.agentName,
+        assistantCacheKey: agentKitResult.assistantCacheKey,
+        vectorStoreHash: agentKitResult.vectorStoreHash,
+        vectorStoreId: agentKitResult.vectorStoreId,
+    });
+
+    return new Agent({
+        agentSource: options.agentSource,
+        executionTools: {
+            llm: agentKitResult.tools,
+        },
+        precomputedModelRequirements: agentModelRequirements,
+        assistantPreparationMode: 'external',
+        isVerbose: true,
+        teacherAgent: null,
+    });
+}


thinking
**Evaluating import issues**

I need to check if the `appendMessageSuffix` has been imported correctly in the runtime. Oh wait, I thought an earlier patch addressed this, but maybe it didn’t? I'll also consider running a command to inspect any compile errors that could be caused by unresolved relative paths or types. It might help to list the files involved to better understand the situation.
codex
The extraction is in place. I’m doing a quick pass over the new modules now to catch path/type issues before I touch the changelog and run the required tests.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg -n "handleChatCompletion" apps/agents-server/src/utils/handleChatCompletion.ts apps/agents-server/src/utils/handleChatCompletion -S' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-ChildItem apps/agents-server/src/utils/handleChatCompletion | Select-Object Name | Format-Table -AutoSize' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts -TotalCount 80' in C:/Users/me/work/ai/promptbook succeeded in 760ms:

[32;1mName[0m
[32;1m----[0m
createHandleChatCompletionErrorResponse.ts
createHandleChatCompletionJsonResponse.ts
createHandleChatCompletionPromptContext.ts
createHandleChatCompletionStreamResponse.ts
finalizeHandleChatCompletionResult.ts
parseHandleChatCompletionRequest.ts
persistOpenAiCompatibilityFrozenChat.ts
resolveHandleChatCompletionRuntime.ts

 succeeded in 704ms:
apps/agents-server/src/utils/handleChatCompletion.ts:4:import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
apps/agents-server/src/utils/handleChatCompletion.ts:5:import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
apps/agents-server/src/utils/handleChatCompletion.ts:6:import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
apps/agents-server/src/utils/handleChatCompletion.ts:7:import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
apps/agents-server/src/utils/handleChatCompletion.ts:8:import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
apps/agents-server/src/utils/handleChatCompletion.ts:14:export async function handleChatCompletion(
apps/agents-server/src/utils/handleChatCompletion\resolveHandleChatCompletionRuntime.ts:33: * @private type of `handleChatCompletion`
apps/agents-server/src/utils/handleChatCompletion\resolveHandleChatCompletionRuntime.ts:57: * @private function of `handleChatCompletion`
apps/agents-server/src/utils/handleChatCompletion\createHandleChatCompletionStreamResponse.ts:18: * @private function of `handleChatCompletion`
apps/agents-server/src/utils/handleChatCompletion\parseHandleChatCompletionRequest.ts:14: * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
apps/agents-server/src/utils/handleChatCompletion\parseHandleChatCompletionRequest.ts:16: * @private type of `handleChatCompletion`
apps/agents-server/src/utils/handleChatCompletion\parseHandleChatCompletionRequest.ts:34: * @private function of `handleChatCompletion`
apps/agents-server/src/utils/handleChatCompletion\createHandleChatCompletionPromptContext.ts:13: * @private type of `handleChatCompletion`
apps/agents-server/src/utils/handleChatCompletion\createHandleChatCompletionPromptContext.ts:25: * @private function of `handleChatCompletion`
apps/agents-server/src/utils/handleChatCompletion\persistOpenAiCompatibilityFrozenChat.ts:15: * @private function of `handleChatCompletion`
apps/agents-server/src/utils/handleChatCompletion\createHandleChatCompletionJsonResponse.ts:14: * @private function of `handleChatCompletion`
apps/agents-server/src/utils/handleChatCompletion\createHandleChatCompletionErrorResponse.ts:6: * @private function of `handleChatCompletion`
apps/agents-server/src/utils/handleChatCompletion\finalizeHandleChatCompletionResult.ts:12: * @private function of `handleChatCompletion`
 succeeded in 819ms:
import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
import type { string_book, TODO_any } from '@promptbook-local/types';
import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
import {
    createBookScopedAgentReferenceResolver,
    parseBookScopedAgentIdentifier,
} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
import { resolveMessageSuffixFromAgentSource } from '@/src/utils/chat/messageSuffix';
import { NextRequest, NextResponse } from 'next/server';
import { isAgentDeleted } from '../../app/agents/[agentName]/_utils';
file update:
diff --git a/apps/agents-server/src/utils/handleChatCompletion.ts b/apps/agents-server/src/utils/handleChatCompletion.ts
index 9c96d36fad9cf24bf26d89f57372332add4e4595..cff56fafd6cfa90bb4094f9f202c4f01f6f74af1
--- a/apps/agents-server/src/utils/handleChatCompletion.ts
+++ b/apps/agents-server/src/utils/handleChatCompletion.ts
@@ -1,392 +1,16 @@
-import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
-import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
-import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
-import {
-    createBookScopedAgentReferenceResolver,
-    parseBookScopedAgentIdentifier,
-} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
-import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
-import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
-import {
-    appendMessageSuffix,
-    createMessageSuffixAppendix,
-    emulateMessageSuffixStreaming,
-    resolveMessageSuffixFromAgentSource,
-} from '@/src/utils/chat/messageSuffix';
-import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
-import { prepareToolCallsForStreaming } from './toolCallStreaming';
-import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
-import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
-import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
-import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
-import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
-import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
-import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
-import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
-import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
-import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
-import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
-import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
-import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
-import type {
-    ChatMessage,
-    ChatPrompt,
-    LlmToolDefinition,
-    string_book,
-    ToolCall,
-    TODO_any,
-    UncertainNumber,
-    Usage,
-    UsageCounts,
-} from '@promptbook-local/types';
-import { $getCurrentDate } from '@promptbook-local/utils';
 import { NextRequest, NextResponse } from 'next/server';
-import type OpenAI from 'openai';
-import { computeUsageCounts } from '../../../../src/execution/utils/computeUsageCounts';
-import { encodeChatStreamWhitespaceForTransport } from '../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
-import { isAgentDeleted } from '../app/agents/[agentName]/_utils';
 import { HTTP_STATUS_CODES } from '../constants';
-import { AgentKitCacheManager } from './cache/AgentKitCacheManager';
 import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
+import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
+import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
+import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
+import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
+import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
 import { validateApiKey } from './validateApiKey';
-import {
-    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    resolveAgentCollectionTablePrefix,
-    waitForRunningAgentPreparation,
-} from './agentPreparation';
-import {
-    resolveCachedServerAgentContext,
-    resolveCachedServerAgentModelRequirements,
-} from './cachedServerAgentRuntime';
-import { resolveAppendOnlySelfLearningAgentSource } from './resolveAppendOnlySelfLearningAgentSource';
 
 /**
- * Falls back to the estimated value when the original token count is unknown.
- *
- * @param tokenCount - Token count reported by the execution tools.
- * @param fallbackValue - Estimated token count based on text length.
- * @returns Token count to report in the OpenAI-compatible response.
- */
-function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
-    if (tokenCount.value === 0 && tokenCount.isUncertain) {
-        return {
-            value: fallbackValue,
-            isUncertain: true,
-        };
-    }
-
-    return tokenCount;
-}
-
-/**
- * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
- *
- * @param promptContent - Prompt content used for the request.
- * @param completionContent - Assistant response content.
- * @param usage - Native Promptbook usage details.
- * @returns Usage payload for compatibility responses.
- */
-function createCompatibilityUsage(
-    promptContent: string,
-    completionContent: string,
-    usage: Usage,
-): {
-    prompt_tokens: number;
-    completion_tokens: number;
-    total_tokens: number;
-    details: Usage;
-} {
-    const promptCounts = computeUsageCounts(promptContent);
-    const completionCounts = computeUsageCounts(completionContent);
-    const inputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
-        ...promptCounts,
-    };
-    const outputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
-        ...completionCounts,
-    };
-
-    const promptTokens = inputUsage.tokensCount.value;
-    const completionTokens = outputUsage.tokensCount.value;
-
-    return {
-        prompt_tokens: promptTokens,
-        completion_tokens: completionTokens,
-        total_tokens: promptTokens + completionTokens,
-        details: {
-            price: usage.price,
-            duration: usage.duration,
-            input: inputUsage,
-            output: outputUsage,
-        },
-    };
-}
-
-/**
- * Definition of open AI chat tool.
- */
-type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
-
-/**
- * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
- */
-type OpenAiCompatibilityMessageLike = {
-    role?: unknown;
-    content?: unknown;
-};
-
-/**
- * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
- */
-type HandleChatCompletionParsedRequest = {
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    threadMessages: ReadonlyArray<TODO_any>;
-    stream: TODO_any;
-    model: TODO_any;
-    responseFormat: TODO_any;
-    runtimeTools?: Array<LlmToolDefinition>;
-    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-    incomingParameters: Record<string, unknown>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Runtime state resolved before the chat model is executed.
- */
-type HandleChatCompletionRuntime = {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    agent: Agent;
-    agentSource: string_book;
-    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
-    messageSuffix: string | null;
-    agentId: string;
-    agentHash: string;
-    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Prompt, history recorder, and persistence context created for one chat call.
- */
-type HandleChatCompletionPromptContext = {
-    prompt: ChatPrompt;
-    userMessageHash: string;
-    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
-    persistedFrozenChatId?: string;
-};
-
-/**
- * Handles convert open Ai tools.
- */
-function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
-    if (!Array.isArray(rawTools)) {
-        return undefined;
-    }
-
-    const converted = rawTools
-        .map((tool) => convertOpenAiTool(tool))
-        .filter((tool): tool is LlmToolDefinition => tool !== null);
-
-    return converted.length > 0 ? converted : undefined;
-}
-
-/**
- * Handles convert open Ai tool.
- */
-function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
-    const tool = rawTool as OpenAIChatToolDefinition;
-    if (!tool || tool.type !== 'function') {
-        return null;
-    }
-
-    const functionDefinition = tool.function;
-    if (!functionDefinition) {
-        return null;
-    }
-
-    const { name, description, parameters } = functionDefinition;
-    if (typeof name !== 'string' || name.trim().length === 0) {
-        return null;
-    }
-
-    const parameterSchema = parameters ?? {};
-    if (parameterSchema.type !== 'object') {
-        return null;
-    }
-
-    const properties = parameterSchema.properties ?? {};
-    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
-
-    for (const [key, value] of Object.entries(properties)) {
-        if (!value || typeof value !== 'object') {
-            continue;
-        }
-
-        normalizedProperties[key] = {
-            type: typeof value.type === 'string' ? value.type : 'string',
-            description: typeof value.description === 'string' ? value.description : undefined,
-        };
-    }
-
-    const required =
-        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
-            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
-            : undefined;
-
-    return {
-        name,
-        description: typeof description === 'string' ? description : '',
-        parameters: {
-            type: 'object',
-            properties: normalizedProperties,
-            required,
-            additionalProperties:
-                typeof parameterSchema.additionalProperties === 'boolean'
-                    ? parameterSchema.additionalProperties
-                    : undefined,
-        },
-    };
-}
-
-/**
- * Parses open Ai tool choice.
- */
-function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
-    if (value === undefined || value === null) {
-        return undefined;
-    }
-
-    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-}
-
-/**
- * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
- */
-function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
-    if (typeof value === 'string') {
-        return value;
-    }
-
-    if (Array.isArray(value)) {
-        return value
-            .map((part) => {
-                if (typeof part === 'string') {
-                    return part;
-                }
-
-                if (!part || typeof part !== 'object') {
-                    return '';
-                }
-
-                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
-                if (typeof partLike.text === 'string') {
-                    return partLike.text;
-                }
-
-                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
-                    return '[Image input]';
-                }
-
-                if (partLike.image_url) {
-                    return '[Image input]';
-                }
-
-                return '';
-            })
-            .filter(Boolean)
-            .join('\n');
-    }
-
-    if (value === null || value === undefined) {
-        return '';
-    }
-
-    try {
-        return JSON.stringify(value, null, 2);
-    } catch {
-        return String(value);
-    }
-}
-
-/**
- * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ * Handles convert open Ai chat completion requests routed through the Agents Server.
  */
-function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
-    if (typeof role !== 'string') {
-        return 'USER';
-    }
-
-    switch (role) {
-        case 'assistant':
-            return 'AGENT';
-        case 'system':
-            return 'SYSTEM';
-        case 'tool':
-            return 'TOOL';
-        case 'developer':
-            return 'SYSTEM';
-        default:
-            return 'USER';
-    }
-}
-
-/**
- * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
- */
-function createFrozenOpenAiCompatibilityMessages(
-    rawMessages: ReadonlyArray<unknown>,
-    assistantContent?: string,
-    options: {
-        includeAssistantPlaceholder?: boolean;
-    } = {},
-): Array<ChatMessage> {
-    const startedAt = Date.now();
-    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
-        const message = rawMessage as OpenAiCompatibilityMessageLike;
-
-        return {
-            id: `openai-frozen-${index}`,
-            sender: mapOpenAiCompatibilityRoleToSender(message.role),
-            content: normalizeOpenAiCompatibilityMessageContent(message.content),
-            isComplete: true,
-            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage;
-    });
-
-    if (assistantContent !== undefined) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: assistantContent,
-            isComplete: true,
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    } else if (options.includeAssistantPlaceholder) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: '',
-            isComplete: false,
-            lifecycleState: 'running',
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    }
-
-    return messages;
-}
-
-/**
- * Handles chat completion.
- */
 export async function handleChatCompletion(
     request: NextRequest,
     params: { agentName?: string },
@@ -399,7 +23,6 @@
         return versionMismatchResponse;
     }
 
-    // Validate API key explicitly (in addition to middleware)
     const apiKeyValidation = await validateApiKey(request);
     if (!apiKeyValidation.isValid) {
         return NextResponse.json(
@@ -462,724 +85,5 @@
             { error: { message: (error as Error).message || 'Internal Server Error', type: 'server_error' } },
             { status: HTTP_STATUS_CODES.INTERNAL_SERVER_ERROR },
         );
-    }
-}
-
-/**
- * Parses and validates the OpenAI-compatible request payload.
- */
-function parseHandleChatCompletionRequest(options: {
-    body: TODO_any;
-    agentNameFromParams?: string;
-    request: NextRequest;
-}): HandleChatCompletionParsedRequest | NextResponse {
-    const { body, agentNameFromParams, request } = options;
-    const {
-        messages,
-        stream,
-        model,
-        response_format: responseFormat,
-        tools: rawTools,
-        tool_choice: toolChoice,
-        parameters: rawParameters = {},
-    } = body;
-    const agentName = (agentNameFromParams || model) as string | undefined;
-    if (!agentName) {
-        return createHandleChatCompletionErrorResponse(
-            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    if (!messages || !Array.isArray(messages) || messages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array is required and cannot be empty.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
-    if (threadMessages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array must contain at least one non-system message.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    return {
-        agentName,
-        messages,
-        threadMessages,
-        stream,
-        model,
-        responseFormat,
-        runtimeTools: convertOpenAiTools(rawTools),
-        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
-        incomingParameters:
-            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
-                ? (rawParameters as Record<string, unknown>)
-                : {},
-        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
-    };
-}
-
-/**
- * Resolves agent/runtime dependencies needed before prompt execution.
- */
-async function resolveHandleChatCompletionRuntime(options: {
-    request: NextRequest;
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    isPrivateModeEnabled: boolean;
-}): Promise<HandleChatCompletionRuntime | NextResponse> {
-    const { request, agentName, messages, isPrivateModeEnabled } = options;
-    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
-    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
-
-    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
-        return createHandleChatCompletionErrorResponse(
-            'This agent has been deleted. You can restore it from the Recycle Bin.',
-            'agent_deleted',
-            410,
-        );
-    }
-
-    const [collection, baseAgentReferenceResolver] = await Promise.all([
-        $provideAgentCollectionForServer(),
-        $provideAgentReferenceResolver(),
-    ]);
-    const localServerUrl = new URL(request.url).origin;
-    const resolvedAgentContext = await resolveCachedServerAgentContext({
-        collection,
-        agentIdentifier: agentName,
-        localServerUrl,
-        fallbackResolver: baseAgentReferenceResolver,
-    }).catch(() => null);
-
-    if (!resolvedAgentContext?.resolvedAgentSource) {
-        return createHandleChatCompletionErrorResponse(
-            `Agent '${agentName}' not found.`,
-            'invalid_request_error',
-            HTTP_STATUS_CODES.NOT_FOUND,
-        );
-    }
-
-    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
-    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
-    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
-    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
-    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
-    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
-    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
-    agentSource = agentSourceWithContext;
-
-    const preparedAgentModelRequirements = !hasDynamicContext
-        ? await resolveCachedServerAgentModelRequirements({
-              resolvedAgentContext,
-              localServerUrl,
-              fallbackResolver: baseAgentReferenceResolver,
-          })
-        : null;
-    const agentId = resolvedAgentContext.parentAgentPermanentId;
-    const agentHash = computeAgentHash(agentSource);
-    await waitForHandleChatCompletionAgentPreparation({
-        collection,
-        agentId,
-        agentHash,
-    });
-
-    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
-    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
-        await resolveHandleChatCompletionCredentials({
-            currentUserIdentity,
-            agentId,
-            calendarConnections,
-            useEmailConfiguration,
-        });
-    const agent = await createHandleChatCompletionAgent({
-        agentName,
-        agentSource,
-        agentId,
-        resolvedAgentContext,
-        localServerUrl,
-        baseAgentReferenceResolver,
-        hasDynamicContext,
-        preparedAgentModelRequirements,
-    });
-
-    return {
-        collection,
-        resolvedAgentContext,
-        agent,
-        agentSource,
-        unresolvedAgentSource,
-        messageSuffix,
-        agentId,
-        agentHash,
-        projectRepositories,
-        calendarConnections,
-        useEmailConfiguration,
-        currentUserIdentity,
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-        isPrivateModeEnabled,
-    };
-}
-
-/**
- * Creates prompt and persistence helpers for one chat completion request.
- */
-async function createHandleChatCompletionPromptContext(options: {
-    request: NextRequest;
-    title: string;
-    apiKey: string | null;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-}): Promise<HandleChatCompletionPromptContext> {
-    const { request, title, apiKey, parsedRequest, runtime } = options;
-    const timezone = request.headers.get('x-timezone') || 'UTC';
-    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
-    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
-    const recordChatHistoryMessage = await createChatHistoryRecorder({
-        request,
-        agentIdentifier: runtime.agentId,
-        agentHash: runtime.agentHash,
-        source: 'OPENAI_API_COMPATIBILITY',
-        apiKey,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        isEnabled: !runtime.isPrivateModeEnabled,
-    });
-    const userMessageHash = await recordChatHistoryMessage({
-        message: {
-            role: 'USER',
-            content: lastMessage.content,
-        },
-        previousMessageHash: null,
-    });
-    const promptParameters = composePromptParametersWithMemoryContext({
-        baseParameters: {
-            ...parsedRequest.incomingParameters,
-            timezone,
-        },
-        currentUserIdentity: runtime.currentUserIdentity,
-        agentPermanentId: runtime.agentId,
-        agentName: runtime.resolvedAgentContext.resolvedAgentName,
-        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
-        projectRepositories: runtime.projectRepositories,
-        projectGithubToken: runtime.projectGithubToken,
-        emailSmtpCredential: runtime.emailSmtpCredential,
-        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
-        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
-        calendarConnections: runtime.calendarConnections,
-    });
-    const prompt: ChatPrompt = {
-        title,
-        content: lastMessage.content,
-        modelRequirements: {
-            modelVariant: 'CHAT',
-            responseFormat: parsedRequest.responseFormat,
-            toolChoice: parsedRequest.runtimeToolChoice,
-            // We could pass 'model' from body if we wanted to enforce it, but Agent usually has its own config
-        },
-        parameters: promptParameters,
-        thread,
-        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
-    };
-
-    return {
-        prompt,
-        userMessageHash,
-        recordChatHistoryMessage,
-        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
-            isEnabled: !runtime.isPrivateModeEnabled,
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentId: runtime.agentId,
-            messages: parsedRequest.messages,
-            includeAssistantPlaceholder: true,
-            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
-        }),
-    };
-}
-
-/**
- * Handles the streaming OpenAI-compatible response branch.
- */
-function createHandleChatCompletionStreamResponse(options: {
-    request: NextRequest;
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Response {
-    const { request, title, parsedRequest, runtime, promptContext } = options;
-    const encoder = new TextEncoder();
-    const responseModel = parsedRequest.model || 'promptbook-agent';
-    const readableStream = new ReadableStream({
-        async start(controller) {
-            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
-            const created = Math.floor(Date.now() / 1000);
-            const emitSsePayload = (payload: unknown) => {
-                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
-            };
-            const emitDeltaChunk = (deltaContent: string) => {
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {
-                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
-                        },
-                        finishReason: null,
-                    }),
-                );
-            };
-
-            emitSsePayload(
-                createHandleChatCompletionChunk({
-                    runId,
-                    created,
-                    model: responseModel,
-                    delta: {
-                        role: 'assistant',
-                        content: '',
-                    },
-                    finishReason: null,
-                }),
-            );
-
-            let hasMeaningfulDelta = false;
-
-            try {
-                const handleStreamChunk = createChatStreamHandler({
-                    onDelta: (deltaContent) => {
-                        if (deltaContent.trim().length > 0) {
-                            hasMeaningfulDelta = true;
-                        }
-                        emitDeltaChunk(deltaContent);
-                    },
-                    onToolCalls: (toolCalls) => {
-                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
-                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
-                    },
-                });
-                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
-                    signal: request.signal,
-                });
-                const normalizedResponse = ensureNonEmptyChatContent({
-                    content: result.content,
-                    context: title,
-                });
-
-                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
-                    emitDeltaChunk(normalizedResponse.content);
-                }
-
-                const messageSuffixAppendix = createMessageSuffixAppendix(
-                    normalizedResponse.content,
-                    runtime.messageSuffix,
-                );
-                if (messageSuffixAppendix) {
-                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
-                        emitDeltaChunk(delta);
-                    });
-                }
-
-                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-                await finalizeHandleChatCompletionResult({
-                    responseContentWithSuffix,
-                    usage: result.usage,
-                    toolCalls: result.toolCalls,
-                    parsedRequest,
-                    runtime,
-                    promptContext,
-                    // Preserve historical behavior: streamed OpenAI-compatible calls still persist learning in private mode.
-                    shouldPersistLearnedAgentSource: true,
-                });
-
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {},
-                        finishReason: 'stop',
-                    }),
-                );
-                controller.enqueue(encoder.encode('[DONE]'));
-            } catch (error) {
-                console.error('Error during streaming:', error);
-                controller.error(error);
-            }
-            controller.close();
-        },
-    });
-
-    return new Response(readableStream, {
-        headers: {
-            'Content-Type': 'text/event-stream',
-            'Cache-Control': 'no-cache',
-            Connection: 'keep-alive',
-        },
-    });
-}
-
-/**
- * Handles the non-streaming OpenAI-compatible response branch.
- */
-async function createHandleChatCompletionJsonResponse(options: {
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Promise<NextResponse> {
-    const { title, parsedRequest, runtime, promptContext } = options;
-    const result = await runtime.agent.callChatModel(promptContext.prompt);
-    const normalizedResponse = ensureNonEmptyChatContent({
-        content: result.content,
-        context: title,
-    });
-    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-
-    await finalizeHandleChatCompletionResult({
-        responseContentWithSuffix,
-        usage: result.usage,
-        toolCalls: result.toolCalls,
-        parsedRequest,
-        runtime,
-        promptContext,
-        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
-    });
-
-    return NextResponse.json({
-        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
-        object: 'chat.completion',
-        created: Math.floor(Date.now() / 1000),
-        model: parsedRequest.model || 'promptbook-agent',
-        choices: [
-            {
-                index: 0,
-                message: {
-                    role: 'assistant',
-                    content: responseContentWithSuffix,
-                },
-                finish_reason: 'stop',
-            },
-        ],
-        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
-    });
-}
-
-/**
- * Runs the shared side effects after one successful model response.
- */
-async function finalizeHandleChatCompletionResult(options: {
-    responseContentWithSuffix: string;
-    usage: Usage;
-    toolCalls?: ReadonlyArray<ToolCall>;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
-
-    await promptContext.recordChatHistoryMessage({
-        message: {
-            role: 'MODEL',
-            content: responseContentWithSuffix,
-        },
-        previousMessageHash: promptContext.userMessageHash,
-        usage,
-    });
-
-    if (toolCalls && toolCalls.length > 0) {
-        await logCalendarToolCallsActivity({
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentPermanentId: runtime.agentId,
-            toolCalls,
-        });
-    }
-
-    await persistOpenAiCompatibilityFrozenChat({
-        isEnabled: !runtime.isPrivateModeEnabled,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        agentId: runtime.agentId,
-        chatId: promptContext.persistedFrozenChatId,
-        messages: parsedRequest.messages,
-        assistantContent: responseContentWithSuffix,
-        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
-    });
-    await persistHandleChatCompletionLearnedAgentSource({
-        runtime,
-        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
-    });
-}
-
-/**
- * Applies append-only self-learning updates when the current branch should persist them.
- */
-async function persistHandleChatCompletionLearnedAgentSource(options: {
-    runtime: HandleChatCompletionRuntime;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { runtime, shouldPersistLearnedAgentSource } = options;
-    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
-        return;
-    }
-
-    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
-        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
-        resolvedAgentSourceBeforeLearning: runtime.agentSource,
-        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
-    });
-
-    if (learnedAgentSource !== null) {
-        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
     }
 }
-
-/**
- * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
- */
-async function persistOpenAiCompatibilityFrozenChat(options: {
-    isEnabled: boolean;
-    userId: number | null | undefined;
-    agentId: string;
-    messages: ReadonlyArray<unknown>;
-    chatId?: string;
-    assistantContent?: string;
-    includeAssistantPlaceholder?: boolean;
-    failureMessage: string;
-}): Promise<string | undefined> {
-    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
-        options;
-    if (!isEnabled || !userId) {
-        return undefined;
-    }
-
-    const persistedFrozenChat = await persistFrozenUserChat({
-        userId,
-        agentPermanentId: agentId,
-        source: USER_CHAT_SOURCES.OPENAI_API,
-        chatId,
-        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
-            includeAssistantPlaceholder,
-        }),
-    }).catch((error) => {
-        console.error(failureMessage, error);
-        return null;
-    });
-
-    return persistedFrozenChat?.id;
-}
-
-/**
- * Builds one OpenAI-compatible thread from the earlier request messages.
- */
-function createHandleChatCompletionThread(previousMessages: ReadonlyArray<TODO_any>): ChatMessage[] {
-    return previousMessages.map((message: TODO_any, index: number) => ({
-        id: `msg-${index}`,
-        sender: message.role === 'assistant' ? 'agent' : 'user',
-        content: message.content,
-        isComplete: true,
-        createdAt: $getCurrentDate(),
-    }));
-}
-
-/**
- * Appends system messages to the agent source as dynamic `CONTEXT`.
- */
-function appendSystemMessagesToAgentSource(
-    agentSource: string_book,
-    messages: ReadonlyArray<TODO_any>,
-): {
-    agentSourceWithContext: string_book;
-    hasDynamicContext: boolean;
-} {
-    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
-    if (systemMessages.length === 0) {
-        return {
-            agentSourceWithContext: agentSource,
-            hasDynamicContext: false,
-        };
-    }
-
-    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
-    return {
-        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
-        hasDynamicContext: true,
-    };
-}
-
-/**
- * Waits for any running preparation job of the current agent before executing the chat call.
- */
-async function waitForHandleChatCompletionAgentPreparation(options: {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    agentId: string;
-    agentHash: string;
-}): Promise<void> {
-    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
-    const preparationWaitResult = await waitForRunningAgentPreparation({
-        tablePrefix,
-        agentPermanentId: options.agentId,
-        fingerprint: options.agentHash,
-        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    });
-
-    if (preparationWaitResult !== 'not_running') {
-        console.info('[pre-index]', 'openai_chat_wait_result', {
-            tablePrefix,
-            agentPermanentId: options.agentId,
-            agentHash: options.agentHash,
-            preparationWaitResult,
-        });
-    }
-}
-
-/**
- * Resolves request-scoped project, calendar, and email credentials for the current agent.
- */
-async function resolveHandleChatCompletionCredentials(options: {
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    agentId: string;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-}): Promise<{
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-}> {
-    const userId = options.currentUserIdentity?.userId;
-    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
-        resolveUseProjectGithubToken({
-            userId,
-            agentPermanentId: options.agentId,
-        }),
-        options.calendarConnections.length > 0
-            ? resolveUseCalendarGoogleToken({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-        options.useEmailConfiguration.isEnabled
-            ? resolveUseEmailSmtpCredential({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-    ]);
-
-    return {
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-    };
-}
-
-/**
- * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
- */
-async function createHandleChatCompletionAgent(options: {
-    agentName: string;
-    agentSource: string_book;
-    agentId: string;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    localServerUrl: string;
-    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
-    hasDynamicContext: boolean;
-    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
-}): Promise<Agent> {
-    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
-    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
-    const runtimeAgentReferenceResolver = options.hasDynamicContext
-        ? createBookScopedAgentReferenceResolver({
-              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
-              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
-              localServerUrl: options.localServerUrl,
-              fallbackResolver: options.baseAgentReferenceResolver,
-          })
-        : undefined;
-    const agentModelRequirements =
-        options.preparedAgentModelRequirements?.modelRequirements ||
-        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
-            agentReferenceResolver: runtimeAgentReferenceResolver,
-            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
-        }));
-
-    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
-        options.agentSource,
-        options.resolvedAgentContext.resolvedAgentName,
-        await baseOpenAiToolsPromise,
-        {
-            includeDynamicContext: true,
-            agentId: options.agentId,
-            modelRequirements: agentModelRequirements,
-        },
-    );
-
-    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
-        agentName: options.agentName,
-        assistantCacheKey: agentKitResult.assistantCacheKey,
-        vectorStoreHash: agentKitResult.vectorStoreHash,
-        vectorStoreId: agentKitResult.vectorStoreId,
-    });
-
-    return new Agent({
-        agentSource: options.agentSource,
-        executionTools: {
-            llm: agentKitResult.tools,
-        },
-        precomputedModelRequirements: agentModelRequirements,
-        assistantPreparationMode: 'external',
-        isVerbose: true,
-        teacherAgent: null,
-    });
-}
-
-/**
- * Creates one OpenAI-compatible streaming chunk payload.
- */
-function createHandleChatCompletionChunk(options: {
-    runId: string;
-    created: number;
-    model: TODO_any;
-    delta: Record<string, unknown>;
-    finishReason: 'stop' | null;
-}) {
-    return {
-        id: options.runId,
-        object: 'chat.completion.chunk',
-        created: options.created,
-        model: options.model,
-        choices: [
-            {
-                index: 0,
-                delta: options.delta,
-                finish_reason: options.finishReason,
-            },
-        ],
-    };
-}
-
-/**
- * Creates one consistent OpenAI-compatible error response.
- */
-function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
-    return NextResponse.json(
-        {
-            error: {
-                message,
-                type,
-            },
-        },
-        { status },
-    );
-}
-
-// TODO: [🈹] Maybe move chat thread handling here
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f49a4fa80fa21f140e25de59596e5546d62e3f7
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
@@ -0,0 +1,18 @@
+import { NextResponse } from 'next/server';
+
+/**
+ * Creates one consistent OpenAI-compatible error response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
+    return NextResponse.json(
+        {
+            error: {
+                message,
+                type,
+            },
+        },
+        { status },
+    );
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b1979f4621921bc664fed75a05851c6fcb8fba6d
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
@@ -0,0 +1,120 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import { appendMessageSuffix } from '@/src/utils/chat/messageSuffix';
+import type { UncertainNumber, Usage, UsageCounts } from '@promptbook-local/types';
+import { NextResponse } from 'next/server';
+import { computeUsageCounts } from '../../../../../src/execution/utils/computeUsageCounts';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the non-streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionJsonResponse(options: {
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Promise<NextResponse> {
+    const { title, parsedRequest, runtime, promptContext } = options;
+    const result = await runtime.agent.callChatModel(promptContext.prompt);
+    const normalizedResponse = ensureNonEmptyChatContent({
+        content: result.content,
+        context: title,
+    });
+    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+
+    await finalizeHandleChatCompletionResult({
+        responseContentWithSuffix,
+        usage: result.usage,
+        toolCalls: result.toolCalls,
+        parsedRequest,
+        runtime,
+        promptContext,
+        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
+    });
+
+    return NextResponse.json({
+        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
+        object: 'chat.completion',
+        created: Math.floor(Date.now() / 1000),
+        model: parsedRequest.model || 'promptbook-agent',
+        choices: [
+            {
+                index: 0,
+                message: {
+                    role: 'assistant',
+                    content: responseContentWithSuffix,
+                },
+                finish_reason: 'stop',
+            },
+        ],
+        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
+    });
+}
+
+/**
+ * Falls back to the estimated value when the original token count is unknown.
+ *
+ * @param tokenCount - Token count reported by the execution tools.
+ * @param fallbackValue - Estimated token count based on text length.
+ * @returns Token count to report in the OpenAI-compatible response.
+ */
+function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
+    if (tokenCount.value === 0 && tokenCount.isUncertain) {
+        return {
+            value: fallbackValue,
+            isUncertain: true,
+        };
+    }
+
+    return tokenCount;
+}
+
+/**
+ * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
+ *
+ * @param promptContent - Prompt content used for the request.
+ * @param completionContent - Assistant response content.
+ * @param usage - Native Promptbook usage details.
+ * @returns Usage payload for compatibility responses.
+ */
+function createCompatibilityUsage(
+    promptContent: string,
+    completionContent: string,
+    usage: Usage,
+): {
+    prompt_tokens: number;
+    completion_tokens: number;
+    total_tokens: number;
+    details: Usage;
+} {
+    const promptCounts = computeUsageCounts(promptContent);
+    const completionCounts = computeUsageCounts(completionContent);
+    const inputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
+        ...promptCounts,
+    };
+    const outputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
+        ...completionCounts,
+    };
+
+    const promptTokens = inputUsage.tokensCount.value;
+    const completionTokens = outputUsage.tokensCount.value;
+
+    return {
+        prompt_tokens: promptTokens,
+        completion_tokens: completionTokens,
+        total_tokens: promptTokens + completionTokens,
+        details: {
+            price: usage.price,
+            duration: usage.duration,
+            input: inputUsage,
+            output: outputUsage,
+        },
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..7aac8141a8b1237ce36430b4b4598a618f0383ac
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
@@ -0,0 +1,111 @@
+import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
+import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
+import type { ChatMessage, ChatPrompt } from '@promptbook-local/types';
+import { $getCurrentDate } from '@promptbook-local/utils';
+import type { NextRequest } from 'next/server';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Prompt, history recorder, and persistence context created for one chat call.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionPromptContext = {
+    prompt: ChatPrompt;
+    userMessageHash: string;
+    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
+    persistedFrozenChatId?: string;
+};
+
+/**
+ * Creates prompt and persistence helpers for one chat completion request.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionPromptContext(options: {
+    request: NextRequest;
+    title: string;
+    apiKey: string | null;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+}): Promise<HandleChatCompletionPromptContext> {
+    const { request, title, apiKey, parsedRequest, runtime } = options;
+    const timezone = request.headers.get('x-timezone') || 'UTC';
+    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
+    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
+    const recordChatHistoryMessage = await createChatHistoryRecorder({
+        request,
+        agentIdentifier: runtime.agentId,
+        agentHash: runtime.agentHash,
+        source: 'OPENAI_API_COMPATIBILITY',
+        apiKey,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        isEnabled: !runtime.isPrivateModeEnabled,
+    });
+    const userMessageHash = await recordChatHistoryMessage({
+        message: {
+            role: 'USER',
+            content: lastMessage.content,
+        },
+        previousMessageHash: null,
+    });
+    const promptParameters = composePromptParametersWithMemoryContext({
+        baseParameters: {
+            ...parsedRequest.incomingParameters,
+            timezone,
+        },
+        currentUserIdentity: runtime.currentUserIdentity,
+        agentPermanentId: runtime.agentId,
+        agentName: runtime.resolvedAgentContext.resolvedAgentName,
+        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
+        projectRepositories: runtime.projectRepositories,
+        projectGithubToken: runtime.projectGithubToken,
+        emailSmtpCredential: runtime.emailSmtpCredential,
+        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
+        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
+        calendarConnections: runtime.calendarConnections,
+    });
+    const prompt: ChatPrompt = {
+        title,
+        content: lastMessage.content,
+        modelRequirements: {
+            modelVariant: 'CHAT',
+            responseFormat: parsedRequest.responseFormat,
+            toolChoice: parsedRequest.runtimeToolChoice,
+        },
+        parameters: promptParameters,
+        thread,
+        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
+    };
+
+    return {
+        prompt,
+        userMessageHash,
+        recordChatHistoryMessage,
+        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
+            isEnabled: !runtime.isPrivateModeEnabled,
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentId: runtime.agentId,
+            messages: parsedRequest.messages,
+            includeAssistantPlaceholder: true,
+            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
+        }),
+    };
+}
+
+/**
+ * Builds one OpenAI-compatible thread from the earlier request messages.
+ */
+function createHandleChatCompletionThread(
+    previousMessages: HandleChatCompletionParsedRequest['threadMessages'],
+): Array<ChatMessage> {
+    return previousMessages.map((message, index) => ({
+        id: `msg-${index}`,
+        sender: message.role === 'assistant' ? 'agent' : 'user',
+        content: message.content,
+        isComplete: true,
+        createdAt: $getCurrentDate(),
+    }));
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..85e096027760bc45fb730e74f8ae3bbb721b1029
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
@@ -0,0 +1,162 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import {
+    appendMessageSuffix,
+    createMessageSuffixAppendix,
+    emulateMessageSuffixStreaming,
+} from '@/src/utils/chat/messageSuffix';
+import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
+import { encodeChatStreamWhitespaceForTransport } from '../../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
+import { prepareToolCallsForStreaming } from '../toolCallStreaming';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionStreamResponse(options: {
+    request: Request;
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Response {
+    const { request, title, parsedRequest, runtime, promptContext } = options;
+    const encoder = new TextEncoder();
+    const responseModel = parsedRequest.model || 'promptbook-agent';
+    const readableStream = new ReadableStream({
+        async start(controller) {
+            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
+            const created = Math.floor(Date.now() / 1000);
+            const emitSsePayload = (payload: unknown) => {
+                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
+            };
+            const emitDeltaChunk = (deltaContent: string) => {
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {
+                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
+                        },
+                        finishReason: null,
+                    }),
+                );
+            };
+
+            emitSsePayload(
+                createHandleChatCompletionChunk({
+                    runId,
+                    created,
+                    model: responseModel,
+                    delta: {
+                        role: 'assistant',
+                        content: '',
+                    },
+                    finishReason: null,
+                }),
+            );
+
+            let hasMeaningfulDelta = false;
+
+            try {
+                const handleStreamChunk = createChatStreamHandler({
+                    onDelta: (deltaContent) => {
+                        if (deltaContent.trim().length > 0) {
+                            hasMeaningfulDelta = true;
+                        }
+                        emitDeltaChunk(deltaContent);
+                    },
+                    onToolCalls: (toolCalls) => {
+                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
+                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
+                    },
+                });
+                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
+                    signal: request.signal,
+                });
+                const normalizedResponse = ensureNonEmptyChatContent({
+                    content: result.content,
+                    context: title,
+                });
+
+                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
+                    emitDeltaChunk(normalizedResponse.content);
+                }
+
+                const messageSuffixAppendix = createMessageSuffixAppendix(
+                    normalizedResponse.content,
+                    runtime.messageSuffix,
+                );
+                if (messageSuffixAppendix) {
+                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
+                        emitDeltaChunk(delta);
+                    });
+                }
+
+                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+                await finalizeHandleChatCompletionResult({
+                    responseContentWithSuffix,
+                    usage: result.usage,
+                    toolCalls: result.toolCalls,
+                    parsedRequest,
+                    runtime,
+                    promptContext,
+                    shouldPersistLearnedAgentSource: true,
+                });
+
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {},
+                        finishReason: 'stop',
+                    }),
+                );
+                controller.enqueue(encoder.encode('[DONE]'));
+            } catch (error) {
+                console.error('Error during streaming:', error);
+                controller.error(error);
+            }
+            controller.close();
+        },
+    });
+
+    return new Response(readableStream, {
+        headers: {
+            'Content-Type': 'text/event-stream',
+            'Cache-Control': 'no-cache',
+            Connection: 'keep-alive',
+        },
+    });
+}
+
+/**
+ * Creates one OpenAI-compatible streaming chunk payload.
+ */
+function createHandleChatCompletionChunk(options: {
+    runId: string;
+    created: number;
+    model: HandleChatCompletionParsedRequest['model'];
+    delta: Record<string, unknown>;
+    finishReason: 'stop' | null;
+}) {
+    return {
+        id: options.runId,
+        object: 'chat.completion.chunk',
+        created: options.created,
+        model: options.model,
+        choices: [
+            {
+                index: 0,
+                delta: options.delta,
+                finish_reason: options.finishReason,
+            },
+        ],
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
new file mode 100644
index 0000000000000000000000000000000000000000..dd6a7d87ea491df245dbf93d8bcb277a9a10b034
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
@@ -0,0 +1,78 @@
+import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
+import { resolveAppendOnlySelfLearningAgentSource } from '@/src/utils/resolveAppendOnlySelfLearningAgentSource';
+import type { ToolCall, Usage } from '@promptbook-local/types';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Runs the shared side effects after one successful model response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function finalizeHandleChatCompletionResult(options: {
+    responseContentWithSuffix: string;
+    usage: Usage;
+    toolCalls?: ReadonlyArray<ToolCall>;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
+
+    await promptContext.recordChatHistoryMessage({
+        message: {
+            role: 'MODEL',
+            content: responseContentWithSuffix,
+        },
+        previousMessageHash: promptContext.userMessageHash,
+        usage,
+    });
+
+    if (toolCalls && toolCalls.length > 0) {
+        await logCalendarToolCallsActivity({
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentPermanentId: runtime.agentId,
+            toolCalls,
+        });
+    }
+
+    await persistOpenAiCompatibilityFrozenChat({
+        isEnabled: !runtime.isPrivateModeEnabled,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        agentId: runtime.agentId,
+        chatId: promptContext.persistedFrozenChatId,
+        messages: parsedRequest.messages,
+        assistantContent: responseContentWithSuffix,
+        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
+    });
+    await persistHandleChatCompletionLearnedAgentSource({
+        runtime,
+        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
+    });
+}
+
+/**
+ * Applies append-only self-learning updates when the current branch should persist them.
+ */
+async function persistHandleChatCompletionLearnedAgentSource(options: {
+    runtime: HandleChatCompletionRuntime;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { runtime, shouldPersistLearnedAgentSource } = options;
+    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
+        return;
+    }
+
+    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
+        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
+        resolvedAgentSourceBeforeLearning: runtime.agentSource,
+        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
+    });
+
+    if (learnedAgentSource !== null) {
+        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
+    }
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
new file mode 100644
index 0000000000000000000000000000000000000000..58e1c26d7f428c1bdb39b36411b3f75256e3de51
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
@@ -0,0 +1,176 @@
+import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
+import type { LlmToolDefinition, TODO_any } from '@promptbook-local/types';
+import { NextRequest, NextResponse } from 'next/server';
+import type OpenAI from 'openai';
+import { HTTP_STATUS_CODES } from '../../constants';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Definition of open AI chat tool.
+ */
+type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
+
+/**
+ * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionParsedRequest = {
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    threadMessages: ReadonlyArray<TODO_any>;
+    stream: TODO_any;
+    model: TODO_any;
+    responseFormat: TODO_any;
+    runtimeTools?: Array<LlmToolDefinition>;
+    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+    incomingParameters: Record<string, unknown>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Parses and validates the OpenAI-compatible request payload.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function parseHandleChatCompletionRequest(options: {
+    body: TODO_any;
+    agentNameFromParams?: string;
+    request: NextRequest;
+}): HandleChatCompletionParsedRequest | NextResponse {
+    const { body, agentNameFromParams, request } = options;
+    const {
+        messages,
+        stream,
+        model,
+        response_format: responseFormat,
+        tools: rawTools,
+        tool_choice: toolChoice,
+        parameters: rawParameters = {},
+    } = body;
+    const agentName = (agentNameFromParams || model) as string | undefined;
+    if (!agentName) {
+        return createHandleChatCompletionErrorResponse(
+            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    if (!messages || !Array.isArray(messages) || messages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array is required and cannot be empty.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
+    if (threadMessages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array must contain at least one non-system message.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    return {
+        agentName,
+        messages,
+        threadMessages,
+        stream,
+        model,
+        responseFormat,
+        runtimeTools: convertOpenAiTools(rawTools),
+        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
+        incomingParameters:
+            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
+                ? (rawParameters as Record<string, unknown>)
+                : {},
+        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
+    };
+}
+
+/**
+ * Handles convert open Ai tools.
+ */
+function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
+    if (!Array.isArray(rawTools)) {
+        return undefined;
+    }
+
+    const converted = rawTools
+        .map((tool) => convertOpenAiTool(tool))
+        .filter((tool): tool is LlmToolDefinition => tool !== null);
+
+    return converted.length > 0 ? converted : undefined;
+}
+
+/**
+ * Handles convert open Ai tool.
+ */
+function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
+    const tool = rawTool as OpenAIChatToolDefinition;
+    if (!tool || tool.type !== 'function') {
+        return null;
+    }
+
+    const functionDefinition = tool.function;
+    if (!functionDefinition) {
+        return null;
+    }
+
+    const { name, description, parameters } = functionDefinition;
+    if (typeof name !== 'string' || name.trim().length === 0) {
+        return null;
+    }
+
+    const parameterSchema = parameters ?? {};
+    if (parameterSchema.type !== 'object') {
+        return null;
+    }
+
+    const properties = parameterSchema.properties ?? {};
+    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
+
+    for (const [key, value] of Object.entries(properties)) {
+        if (!value || typeof value !== 'object') {
+            continue;
+        }
+
+        normalizedProperties[key] = {
+            type: typeof value.type === 'string' ? value.type : 'string',
+            description: typeof value.description === 'string' ? value.description : undefined,
+        };
+    }
+
+    const required =
+        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
+            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
+            : undefined;
+
+    return {
+        name,
+        description: typeof description === 'string' ? description : '',
+        parameters: {
+            type: 'object',
+            properties: normalizedProperties,
+            required,
+            additionalProperties:
+                typeof parameterSchema.additionalProperties === 'boolean'
+                    ? parameterSchema.additionalProperties
+                    : undefined,
+        },
+    };
+}
+
+/**
+ * Parses open Ai tool choice.
+ */
+function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
+    if (value === undefined || value === null) {
+        return undefined;
+    }
+
+    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
new file mode 100644
index 0000000000000000000000000000000000000000..60e5f1b008bcf5a6a1836c06c4c3d9ebcb909a9f
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
@@ -0,0 +1,163 @@
+import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
+import type { ChatMessage } from '@promptbook-local/types';
+
+/**
+ * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
+ */
+type OpenAiCompatibilityMessageLike = {
+    role?: unknown;
+    content?: unknown;
+};
+
+/**
+ * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function persistOpenAiCompatibilityFrozenChat(options: {
+    isEnabled: boolean;
+    userId: number | null | undefined;
+    agentId: string;
+    messages: ReadonlyArray<unknown>;
+    chatId?: string;
+    assistantContent?: string;
+    includeAssistantPlaceholder?: boolean;
+    failureMessage: string;
+}): Promise<string | undefined> {
+    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
+        options;
+    if (!isEnabled || !userId) {
+        return undefined;
+    }
+
+    const persistedFrozenChat = await persistFrozenUserChat({
+        userId,
+        agentPermanentId: agentId,
+        source: USER_CHAT_SOURCES.OPENAI_API,
+        chatId,
+        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
+            includeAssistantPlaceholder,
+        }),
+    }).catch((error) => {
+        console.error(failureMessage, error);
+        return null;
+    });
+
+    return persistedFrozenChat?.id;
+}
+
+/**
+ * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
+ */
+function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
+    if (typeof value === 'string') {
+        return value;
+    }
+
+    if (Array.isArray(value)) {
+        return value
+            .map((part) => {
+                if (typeof part === 'string') {
+                    return part;
+                }
+
+                if (!part || typeof part !== 'object') {
+                    return '';
+                }
+
+                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
+                if (typeof partLike.text === 'string') {
+                    return partLike.text;
+                }
+
+                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
+                    return '[Image input]';
+                }
+
+                if (partLike.image_url) {
+                    return '[Image input]';
+                }
+
+                return '';
+            })
+            .filter(Boolean)
+            .join('\n');
+    }
+
+    if (value === null || value === undefined) {
+        return '';
+    }
+
+    try {
+        return JSON.stringify(value, null, 2);
+    } catch {
+        return String(value);
+    }
+}
+
+/**
+ * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ */
+function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
+    if (typeof role !== 'string') {
+        return 'USER';
+    }
+
+    switch (role) {
+        case 'assistant':
+            return 'AGENT';
+        case 'system':
+            return 'SYSTEM';
+        case 'tool':
+            return 'TOOL';
+        case 'developer':
+            return 'SYSTEM';
+        default:
+            return 'USER';
+    }
+}
+
+/**
+ * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
+ */
+function createFrozenOpenAiCompatibilityMessages(
+    rawMessages: ReadonlyArray<unknown>,
+    assistantContent?: string,
+    options: {
+        includeAssistantPlaceholder?: boolean;
+    } = {},
+): Array<ChatMessage> {
+    const startedAt = Date.now();
+    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
+        const message = rawMessage as OpenAiCompatibilityMessageLike;
+
+        return {
+            id: `openai-frozen-${index}`,
+            sender: mapOpenAiCompatibilityRoleToSender(message.role),
+            content: normalizeOpenAiCompatibilityMessageContent(message.content),
+            isComplete: true,
+            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage;
+    });
+
+    if (assistantContent !== undefined) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: assistantContent,
+            isComplete: true,
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    } else if (options.includeAssistantPlaceholder) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: '',
+            isComplete: false,
+            lifecycleState: 'running',
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    }
+
+    return messages;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fd44b7ea5a87168725a9e99784038137c96537ab
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
@@ -0,0 +1,305 @@
+import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
+import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
+import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
+import type { string_book, TODO_any } from '@promptbook-local/types';
+import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
+import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
+import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
+import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
+import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
+import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
+import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
+import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
+import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
+import {
+    createBookScopedAgentReferenceResolver,
+    parseBookScopedAgentIdentifier,
+} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
+import { resolveMessageSuffixFromAgentSource } from '@/src/utils/chat/messageSuffix';
+import { NextRequest, NextResponse } from 'next/server';
+import { isAgentDeleted } from '../../app/agents/[agentName]/_utils';
+import {
+    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    resolveAgentCollectionTablePrefix,
+    waitForRunningAgentPreparation,
+} from '../agentPreparation';
+import { resolveCachedServerAgentContext, resolveCachedServerAgentModelRequirements } from '../cachedServerAgentRuntime';
+import { AgentKitCacheManager } from '../cache/AgentKitCacheManager';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Runtime state resolved before the chat model is executed.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionRuntime = {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    agent: Agent;
+    agentSource: string_book;
+    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
+    messageSuffix: string | null;
+    agentId: string;
+    agentHash: string;
+    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Resolves agent/runtime dependencies needed before prompt execution.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function resolveHandleChatCompletionRuntime(options: {
+    request: NextRequest;
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    isPrivateModeEnabled: boolean;
+}): Promise<HandleChatCompletionRuntime | NextResponse> {
+    const { request, agentName, messages, isPrivateModeEnabled } = options;
+    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
+    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
+
+    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
+        return createHandleChatCompletionErrorResponse(
+            'This agent has been deleted. You can restore it from the Recycle Bin.',
+            'agent_deleted',
+            410,
+        );
+    }
+
+    const [collection, baseAgentReferenceResolver] = await Promise.all([
+        $provideAgentCollectionForServer(),
+        $provideAgentReferenceResolver(),
+    ]);
+    const localServerUrl = new URL(request.url).origin;
+    const resolvedAgentContext = await resolveCachedServerAgentContext({
+        collection,
+        agentIdentifier: agentName,
+        localServerUrl,
+        fallbackResolver: baseAgentReferenceResolver,
+    }).catch(() => null);
+
+    if (!resolvedAgentContext?.resolvedAgentSource) {
+        return createHandleChatCompletionErrorResponse(`Agent '${agentName}' not found.`, 'invalid_request_error', 404);
+    }
+
+    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
+    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
+    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
+    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
+    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
+    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
+    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
+    agentSource = agentSourceWithContext;
+
+    const preparedAgentModelRequirements = !hasDynamicContext
+        ? await resolveCachedServerAgentModelRequirements({
+              resolvedAgentContext,
+              localServerUrl,
+              fallbackResolver: baseAgentReferenceResolver,
+          })
+        : null;
+    const agentId = resolvedAgentContext.parentAgentPermanentId;
+    const agentHash = computeAgentHash(agentSource);
+    await waitForHandleChatCompletionAgentPreparation({
+        collection,
+        agentId,
+        agentHash,
+    });
+
+    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
+    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
+        await resolveHandleChatCompletionCredentials({
+            currentUserIdentity,
+            agentId,
+            calendarConnections,
+            useEmailConfiguration,
+        });
+    const agent = await createHandleChatCompletionAgent({
+        agentName,
+        agentSource,
+        agentId,
+        resolvedAgentContext,
+        localServerUrl,
+        baseAgentReferenceResolver,
+        hasDynamicContext,
+        preparedAgentModelRequirements,
+    });
+
+    return {
+        collection,
+        resolvedAgentContext,
+        agent,
+        agentSource,
+        unresolvedAgentSource,
+        messageSuffix,
+        agentId,
+        agentHash,
+        projectRepositories,
+        calendarConnections,
+        useEmailConfiguration,
+        currentUserIdentity,
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+        isPrivateModeEnabled,
+    };
+}
+
+/**
+ * Appends system messages to the agent source as dynamic `CONTEXT`.
+ */
+function appendSystemMessagesToAgentSource(
+    agentSource: string_book,
+    messages: ReadonlyArray<TODO_any>,
+): {
+    agentSourceWithContext: string_book;
+    hasDynamicContext: boolean;
+} {
+    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
+    if (systemMessages.length === 0) {
+        return {
+            agentSourceWithContext: agentSource,
+            hasDynamicContext: false,
+        };
+    }
+
+    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
+    return {
+        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
+        hasDynamicContext: true,
+    };
+}
+
+/**
+ * Waits for any running preparation job of the current agent before executing the chat call.
+ */
+async function waitForHandleChatCompletionAgentPreparation(options: {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    agentId: string;
+    agentHash: string;
+}): Promise<void> {
+    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
+    const preparationWaitResult = await waitForRunningAgentPreparation({
+        tablePrefix,
+        agentPermanentId: options.agentId,
+        fingerprint: options.agentHash,
+        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    });
+
+    if (preparationWaitResult !== 'not_running') {
+        console.info('[pre-index]', 'openai_chat_wait_result', {
+            tablePrefix,
+            agentPermanentId: options.agentId,
+            agentHash: options.agentHash,
+            preparationWaitResult,
+        });
+    }
+}
+
+/**
+ * Resolves request-scoped project, calendar, and email credentials for the current agent.
+ */
+async function resolveHandleChatCompletionCredentials(options: {
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    agentId: string;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+}): Promise<{
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+}> {
+    const userId = options.currentUserIdentity?.userId;
+    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
+        resolveUseProjectGithubToken({
+            userId,
+            agentPermanentId: options.agentId,
+        }),
+        options.calendarConnections.length > 0
+            ? resolveUseCalendarGoogleToken({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+        options.useEmailConfiguration.isEnabled
+            ? resolveUseEmailSmtpCredential({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+    ]);
+
+    return {
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+    };
+}
+
+/**
+ * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
+ */
+async function createHandleChatCompletionAgent(options: {
+    agentName: string;
+    agentSource: string_book;
+    agentId: string;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    localServerUrl: string;
+    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
+    hasDynamicContext: boolean;
+    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
+}): Promise<Agent> {
+    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
+    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
+    const runtimeAgentReferenceResolver = options.hasDynamicContext
+        ? createBookScopedAgentReferenceResolver({
+              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
+              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
+              localServerUrl: options.localServerUrl,
+              fallbackResolver: options.baseAgentReferenceResolver,
+          })
+        : undefined;
+    const agentModelRequirements =
+        options.preparedAgentModelRequirements?.modelRequirements ||
+        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
+            agentReferenceResolver: runtimeAgentReferenceResolver,
+            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
+        }));
+
+    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
+        options.agentSource,
+        options.resolvedAgentContext.resolvedAgentName,
+        await baseOpenAiToolsPromise,
+        {
+            includeDynamicContext: true,
+            agentId: options.agentId,
+            modelRequirements: agentModelRequirements,
+        },
+    );
+
+    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
+        agentName: options.agentName,
+        assistantCacheKey: agentKitResult.assistantCacheKey,
+        vectorStoreHash: agentKitResult.vectorStoreHash,
+        vectorStoreId: agentKitResult.vectorStoreId,
+    });
+
+    return new Agent({
+        agentSource: options.agentSource,
+        executionTools: {
+            llm: agentKitResult.tools,
+        },
+        precomputedModelRequirements: agentModelRequirements,
+        assistantPreparationMode: 'external',
+        isVerbose: true,
+        teacherAgent: null,
+    });
+}

codex
The extracted modules look structurally right. I’m validating the couple of deep relative imports and then I’ll add the changelog entry and move to the required test runs.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Item src/utils/chat/encodeChatStreamWhitespaceForTransport.ts | Select-Object FullName' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts -TotalCount 40' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Item src/execution/utils/computeUsageCounts.ts | Select-Object FullName' in C:/Users/me/work/ai/promptbook succeeded in 861ms:

[32;1mFullName[0m
[32;1m--------[0m
C:\Users\me\work\ai\promptbook\src\execution\utils\computeUsageCounts.ts

 succeeded in 911ms:

[32;1mFullName[0m
[32;1m--------[0m
C:\Users\me\work\ai\promptbook\src\utils\chat\encodeChatStreamWhitespaceForTransport.ts

 succeeded in 1.17s:
import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
import {
    appendMessageSuffix,
    createMessageSuffixAppendix,
    emulateMessageSuffixStreaming,
} from '@/src/utils/chat/messageSuffix';
import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
import { encodeChatStreamWhitespaceForTransport } from '../../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
import { prepareToolCallsForStreaming } from '../toolCallStreaming';
import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';

/**
 * Handles the streaming OpenAI-compatible response branch.
 *
 * @private function of `handleChatCompletion`
 */
export function createHandleChatCompletionStreamResponse(options: {
file update:
diff --git a/apps/agents-server/src/utils/handleChatCompletion.ts b/apps/agents-server/src/utils/handleChatCompletion.ts
index 9c96d36fad9cf24bf26d89f57372332add4e4595..cff56fafd6cfa90bb4094f9f202c4f01f6f74af1
--- a/apps/agents-server/src/utils/handleChatCompletion.ts
+++ b/apps/agents-server/src/utils/handleChatCompletion.ts
@@ -1,392 +1,16 @@
-import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
-import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
-import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
-import {
-    createBookScopedAgentReferenceResolver,
-    parseBookScopedAgentIdentifier,
-} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
-import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
-import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
-import {
-    appendMessageSuffix,
-    createMessageSuffixAppendix,
-    emulateMessageSuffixStreaming,
-    resolveMessageSuffixFromAgentSource,
-} from '@/src/utils/chat/messageSuffix';
-import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
-import { prepareToolCallsForStreaming } from './toolCallStreaming';
-import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
-import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
-import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
-import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
-import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
-import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
-import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
-import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
-import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
-import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
-import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
-import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
-import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
-import type {
-    ChatMessage,
-    ChatPrompt,
-    LlmToolDefinition,
-    string_book,
-    ToolCall,
-    TODO_any,
-    UncertainNumber,
-    Usage,
-    UsageCounts,
-} from '@promptbook-local/types';
-import { $getCurrentDate } from '@promptbook-local/utils';
 import { NextRequest, NextResponse } from 'next/server';
-import type OpenAI from 'openai';
-import { computeUsageCounts } from '../../../../src/execution/utils/computeUsageCounts';
-import { encodeChatStreamWhitespaceForTransport } from '../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
-import { isAgentDeleted } from '../app/agents/[agentName]/_utils';
 import { HTTP_STATUS_CODES } from '../constants';
-import { AgentKitCacheManager } from './cache/AgentKitCacheManager';
 import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
+import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
+import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
+import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
+import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
+import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
 import { validateApiKey } from './validateApiKey';
-import {
-    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    resolveAgentCollectionTablePrefix,
-    waitForRunningAgentPreparation,
-} from './agentPreparation';
-import {
-    resolveCachedServerAgentContext,
-    resolveCachedServerAgentModelRequirements,
-} from './cachedServerAgentRuntime';
-import { resolveAppendOnlySelfLearningAgentSource } from './resolveAppendOnlySelfLearningAgentSource';
 
 /**
- * Falls back to the estimated value when the original token count is unknown.
- *
- * @param tokenCount - Token count reported by the execution tools.
- * @param fallbackValue - Estimated token count based on text length.
- * @returns Token count to report in the OpenAI-compatible response.
- */
-function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
-    if (tokenCount.value === 0 && tokenCount.isUncertain) {
-        return {
-            value: fallbackValue,
-            isUncertain: true,
-        };
-    }
-
-    return tokenCount;
-}
-
-/**
- * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
- *
- * @param promptContent - Prompt content used for the request.
- * @param completionContent - Assistant response content.
- * @param usage - Native Promptbook usage details.
- * @returns Usage payload for compatibility responses.
- */
-function createCompatibilityUsage(
-    promptContent: string,
-    completionContent: string,
-    usage: Usage,
-): {
-    prompt_tokens: number;
-    completion_tokens: number;
-    total_tokens: number;
-    details: Usage;
-} {
-    const promptCounts = computeUsageCounts(promptContent);
-    const completionCounts = computeUsageCounts(completionContent);
-    const inputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
-        ...promptCounts,
-    };
-    const outputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
-        ...completionCounts,
-    };
-
-    const promptTokens = inputUsage.tokensCount.value;
-    const completionTokens = outputUsage.tokensCount.value;
-
-    return {
-        prompt_tokens: promptTokens,
-        completion_tokens: completionTokens,
-        total_tokens: promptTokens + completionTokens,
-        details: {
-            price: usage.price,
-            duration: usage.duration,
-            input: inputUsage,
-            output: outputUsage,
-        },
-    };
-}
-
-/**
- * Definition of open AI chat tool.
- */
-type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
-
-/**
- * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
- */
-type OpenAiCompatibilityMessageLike = {
-    role?: unknown;
-    content?: unknown;
-};
-
-/**
- * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
- */
-type HandleChatCompletionParsedRequest = {
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    threadMessages: ReadonlyArray<TODO_any>;
-    stream: TODO_any;
-    model: TODO_any;
-    responseFormat: TODO_any;
-    runtimeTools?: Array<LlmToolDefinition>;
-    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-    incomingParameters: Record<string, unknown>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Runtime state resolved before the chat model is executed.
- */
-type HandleChatCompletionRuntime = {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    agent: Agent;
-    agentSource: string_book;
-    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
-    messageSuffix: string | null;
-    agentId: string;
-    agentHash: string;
-    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Prompt, history recorder, and persistence context created for one chat call.
- */
-type HandleChatCompletionPromptContext = {
-    prompt: ChatPrompt;
-    userMessageHash: string;
-    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
-    persistedFrozenChatId?: string;
-};
-
-/**
- * Handles convert open Ai tools.
- */
-function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
-    if (!Array.isArray(rawTools)) {
-        return undefined;
-    }
-
-    const converted = rawTools
-        .map((tool) => convertOpenAiTool(tool))
-        .filter((tool): tool is LlmToolDefinition => tool !== null);
-
-    return converted.length > 0 ? converted : undefined;
-}
-
-/**
- * Handles convert open Ai tool.
- */
-function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
-    const tool = rawTool as OpenAIChatToolDefinition;
-    if (!tool || tool.type !== 'function') {
-        return null;
-    }
-
-    const functionDefinition = tool.function;
-    if (!functionDefinition) {
-        return null;
-    }
-
-    const { name, description, parameters } = functionDefinition;
-    if (typeof name !== 'string' || name.trim().length === 0) {
-        return null;
-    }
-
-    const parameterSchema = parameters ?? {};
-    if (parameterSchema.type !== 'object') {
-        return null;
-    }
-
-    const properties = parameterSchema.properties ?? {};
-    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
-
-    for (const [key, value] of Object.entries(properties)) {
-        if (!value || typeof value !== 'object') {
-            continue;
-        }
-
-        normalizedProperties[key] = {
-            type: typeof value.type === 'string' ? value.type : 'string',
-            description: typeof value.description === 'string' ? value.description : undefined,
-        };
-    }
-
-    const required =
-        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
-            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
-            : undefined;
-
-    return {
-        name,
-        description: typeof description === 'string' ? description : '',
-        parameters: {
-            type: 'object',
-            properties: normalizedProperties,
-            required,
-            additionalProperties:
-                typeof parameterSchema.additionalProperties === 'boolean'
-                    ? parameterSchema.additionalProperties
-                    : undefined,
-        },
-    };
-}
-
-/**
- * Parses open Ai tool choice.
- */
-function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
-    if (value === undefined || value === null) {
-        return undefined;
-    }
-
-    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-}
-
-/**
- * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
- */
-function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
-    if (typeof value === 'string') {
-        return value;
-    }
-
-    if (Array.isArray(value)) {
-        return value
-            .map((part) => {
-                if (typeof part === 'string') {
-                    return part;
-                }
-
-                if (!part || typeof part !== 'object') {
-                    return '';
-                }
-
-                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
-                if (typeof partLike.text === 'string') {
-                    return partLike.text;
-                }
-
-                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
-                    return '[Image input]';
-                }
-
-                if (partLike.image_url) {
-                    return '[Image input]';
-                }
-
-                return '';
-            })
-            .filter(Boolean)
-            .join('\n');
-    }
-
-    if (value === null || value === undefined) {
-        return '';
-    }
-
-    try {
-        return JSON.stringify(value, null, 2);
-    } catch {
-        return String(value);
-    }
-}
-
-/**
- * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ * Handles convert open Ai chat completion requests routed through the Agents Server.
  */
-function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
-    if (typeof role !== 'string') {
-        return 'USER';
-    }
-
-    switch (role) {
-        case 'assistant':
-            return 'AGENT';
-        case 'system':
-            return 'SYSTEM';
-        case 'tool':
-            return 'TOOL';
-        case 'developer':
-            return 'SYSTEM';
-        default:
-            return 'USER';
-    }
-}
-
-/**
- * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
- */
-function createFrozenOpenAiCompatibilityMessages(
-    rawMessages: ReadonlyArray<unknown>,
-    assistantContent?: string,
-    options: {
-        includeAssistantPlaceholder?: boolean;
-    } = {},
-): Array<ChatMessage> {
-    const startedAt = Date.now();
-    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
-        const message = rawMessage as OpenAiCompatibilityMessageLike;
-
-        return {
-            id: `openai-frozen-${index}`,
-            sender: mapOpenAiCompatibilityRoleToSender(message.role),
-            content: normalizeOpenAiCompatibilityMessageContent(message.content),
-            isComplete: true,
-            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage;
-    });
-
-    if (assistantContent !== undefined) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: assistantContent,
-            isComplete: true,
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    } else if (options.includeAssistantPlaceholder) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: '',
-            isComplete: false,
-            lifecycleState: 'running',
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    }
-
-    return messages;
-}
-
-/**
- * Handles chat completion.
- */
 export async function handleChatCompletion(
     request: NextRequest,
     params: { agentName?: string },
@@ -399,7 +23,6 @@
         return versionMismatchResponse;
     }
 
-    // Validate API key explicitly (in addition to middleware)
     const apiKeyValidation = await validateApiKey(request);
     if (!apiKeyValidation.isValid) {
         return NextResponse.json(
@@ -462,724 +85,5 @@
             { error: { message: (error as Error).message || 'Internal Server Error', type: 'server_error' } },
             { status: HTTP_STATUS_CODES.INTERNAL_SERVER_ERROR },
         );
-    }
-}
-
-/**
- * Parses and validates the OpenAI-compatible request payload.
- */
-function parseHandleChatCompletionRequest(options: {
-    body: TODO_any;
-    agentNameFromParams?: string;
-    request: NextRequest;
-}): HandleChatCompletionParsedRequest | NextResponse {
-    const { body, agentNameFromParams, request } = options;
-    const {
-        messages,
-        stream,
-        model,
-        response_format: responseFormat,
-        tools: rawTools,
-        tool_choice: toolChoice,
-        parameters: rawParameters = {},
-    } = body;
-    const agentName = (agentNameFromParams || model) as string | undefined;
-    if (!agentName) {
-        return createHandleChatCompletionErrorResponse(
-            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    if (!messages || !Array.isArray(messages) || messages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array is required and cannot be empty.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
-    if (threadMessages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array must contain at least one non-system message.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    return {
-        agentName,
-        messages,
-        threadMessages,
-        stream,
-        model,
-        responseFormat,
-        runtimeTools: convertOpenAiTools(rawTools),
-        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
-        incomingParameters:
-            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
-                ? (rawParameters as Record<string, unknown>)
-                : {},
-        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
-    };
-}
-
-/**
- * Resolves agent/runtime dependencies needed before prompt execution.
- */
-async function resolveHandleChatCompletionRuntime(options: {
-    request: NextRequest;
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    isPrivateModeEnabled: boolean;
-}): Promise<HandleChatCompletionRuntime | NextResponse> {
-    const { request, agentName, messages, isPrivateModeEnabled } = options;
-    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
-    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
-
-    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
-        return createHandleChatCompletionErrorResponse(
-            'This agent has been deleted. You can restore it from the Recycle Bin.',
-            'agent_deleted',
-            410,
-        );
-    }
-
-    const [collection, baseAgentReferenceResolver] = await Promise.all([
-        $provideAgentCollectionForServer(),
-        $provideAgentReferenceResolver(),
-    ]);
-    const localServerUrl = new URL(request.url).origin;
-    const resolvedAgentContext = await resolveCachedServerAgentContext({
-        collection,
-        agentIdentifier: agentName,
-        localServerUrl,
-        fallbackResolver: baseAgentReferenceResolver,
-    }).catch(() => null);
-
-    if (!resolvedAgentContext?.resolvedAgentSource) {
-        return createHandleChatCompletionErrorResponse(
-            `Agent '${agentName}' not found.`,
-            'invalid_request_error',
-            HTTP_STATUS_CODES.NOT_FOUND,
-        );
-    }
-
-    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
-    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
-    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
-    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
-    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
-    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
-    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
-    agentSource = agentSourceWithContext;
-
-    const preparedAgentModelRequirements = !hasDynamicContext
-        ? await resolveCachedServerAgentModelRequirements({
-              resolvedAgentContext,
-              localServerUrl,
-              fallbackResolver: baseAgentReferenceResolver,
-          })
-        : null;
-    const agentId = resolvedAgentContext.parentAgentPermanentId;
-    const agentHash = computeAgentHash(agentSource);
-    await waitForHandleChatCompletionAgentPreparation({
-        collection,
-        agentId,
-        agentHash,
-    });
-
-    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
-    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
-        await resolveHandleChatCompletionCredentials({
-            currentUserIdentity,
-            agentId,
-            calendarConnections,
-            useEmailConfiguration,
-        });
-    const agent = await createHandleChatCompletionAgent({
-        agentName,
-        agentSource,
-        agentId,
-        resolvedAgentContext,
-        localServerUrl,
-        baseAgentReferenceResolver,
-        hasDynamicContext,
-        preparedAgentModelRequirements,
-    });
-
-    return {
-        collection,
-        resolvedAgentContext,
-        agent,
-        agentSource,
-        unresolvedAgentSource,
-        messageSuffix,
-        agentId,
-        agentHash,
-        projectRepositories,
-        calendarConnections,
-        useEmailConfiguration,
-        currentUserIdentity,
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-        isPrivateModeEnabled,
-    };
-}
-
-/**
- * Creates prompt and persistence helpers for one chat completion request.
- */
-async function createHandleChatCompletionPromptContext(options: {
-    request: NextRequest;
-    title: string;
-    apiKey: string | null;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-}): Promise<HandleChatCompletionPromptContext> {
-    const { request, title, apiKey, parsedRequest, runtime } = options;
-    const timezone = request.headers.get('x-timezone') || 'UTC';
-    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
-    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
-    const recordChatHistoryMessage = await createChatHistoryRecorder({
-        request,
-        agentIdentifier: runtime.agentId,
-        agentHash: runtime.agentHash,
-        source: 'OPENAI_API_COMPATIBILITY',
-        apiKey,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        isEnabled: !runtime.isPrivateModeEnabled,
-    });
-    const userMessageHash = await recordChatHistoryMessage({
-        message: {
-            role: 'USER',
-            content: lastMessage.content,
-        },
-        previousMessageHash: null,
-    });
-    const promptParameters = composePromptParametersWithMemoryContext({
-        baseParameters: {
-            ...parsedRequest.incomingParameters,
-            timezone,
-        },
-        currentUserIdentity: runtime.currentUserIdentity,
-        agentPermanentId: runtime.agentId,
-        agentName: runtime.resolvedAgentContext.resolvedAgentName,
-        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
-        projectRepositories: runtime.projectRepositories,
-        projectGithubToken: runtime.projectGithubToken,
-        emailSmtpCredential: runtime.emailSmtpCredential,
-        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
-        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
-        calendarConnections: runtime.calendarConnections,
-    });
-    const prompt: ChatPrompt = {
-        title,
-        content: lastMessage.content,
-        modelRequirements: {
-            modelVariant: 'CHAT',
-            responseFormat: parsedRequest.responseFormat,
-            toolChoice: parsedRequest.runtimeToolChoice,
-            // We could pass 'model' from body if we wanted to enforce it, but Agent usually has its own config
-        },
-        parameters: promptParameters,
-        thread,
-        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
-    };
-
-    return {
-        prompt,
-        userMessageHash,
-        recordChatHistoryMessage,
-        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
-            isEnabled: !runtime.isPrivateModeEnabled,
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentId: runtime.agentId,
-            messages: parsedRequest.messages,
-            includeAssistantPlaceholder: true,
-            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
-        }),
-    };
-}
-
-/**
- * Handles the streaming OpenAI-compatible response branch.
- */
-function createHandleChatCompletionStreamResponse(options: {
-    request: NextRequest;
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Response {
-    const { request, title, parsedRequest, runtime, promptContext } = options;
-    const encoder = new TextEncoder();
-    const responseModel = parsedRequest.model || 'promptbook-agent';
-    const readableStream = new ReadableStream({
-        async start(controller) {
-            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
-            const created = Math.floor(Date.now() / 1000);
-            const emitSsePayload = (payload: unknown) => {
-                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
-            };
-            const emitDeltaChunk = (deltaContent: string) => {
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {
-                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
-                        },
-                        finishReason: null,
-                    }),
-                );
-            };
-
-            emitSsePayload(
-                createHandleChatCompletionChunk({
-                    runId,
-                    created,
-                    model: responseModel,
-                    delta: {
-                        role: 'assistant',
-                        content: '',
-                    },
-                    finishReason: null,
-                }),
-            );
-
-            let hasMeaningfulDelta = false;
-
-            try {
-                const handleStreamChunk = createChatStreamHandler({
-                    onDelta: (deltaContent) => {
-                        if (deltaContent.trim().length > 0) {
-                            hasMeaningfulDelta = true;
-                        }
-                        emitDeltaChunk(deltaContent);
-                    },
-                    onToolCalls: (toolCalls) => {
-                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
-                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
-                    },
-                });
-                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
-                    signal: request.signal,
-                });
-                const normalizedResponse = ensureNonEmptyChatContent({
-                    content: result.content,
-                    context: title,
-                });
-
-                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
-                    emitDeltaChunk(normalizedResponse.content);
-                }
-
-                const messageSuffixAppendix = createMessageSuffixAppendix(
-                    normalizedResponse.content,
-                    runtime.messageSuffix,
-                );
-                if (messageSuffixAppendix) {
-                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
-                        emitDeltaChunk(delta);
-                    });
-                }
-
-                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-                await finalizeHandleChatCompletionResult({
-                    responseContentWithSuffix,
-                    usage: result.usage,
-                    toolCalls: result.toolCalls,
-                    parsedRequest,
-                    runtime,
-                    promptContext,
-                    // Preserve historical behavior: streamed OpenAI-compatible calls still persist learning in private mode.
-                    shouldPersistLearnedAgentSource: true,
-                });
-
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {},
-                        finishReason: 'stop',
-                    }),
-                );
-                controller.enqueue(encoder.encode('[DONE]'));
-            } catch (error) {
-                console.error('Error during streaming:', error);
-                controller.error(error);
-            }
-            controller.close();
-        },
-    });
-
-    return new Response(readableStream, {
-        headers: {
-            'Content-Type': 'text/event-stream',
-            'Cache-Control': 'no-cache',
-            Connection: 'keep-alive',
-        },
-    });
-}
-
-/**
- * Handles the non-streaming OpenAI-compatible response branch.
- */
-async function createHandleChatCompletionJsonResponse(options: {
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Promise<NextResponse> {
-    const { title, parsedRequest, runtime, promptContext } = options;
-    const result = await runtime.agent.callChatModel(promptContext.prompt);
-    const normalizedResponse = ensureNonEmptyChatContent({
-        content: result.content,
-        context: title,
-    });
-    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-
-    await finalizeHandleChatCompletionResult({
-        responseContentWithSuffix,
-        usage: result.usage,
-        toolCalls: result.toolCalls,
-        parsedRequest,
-        runtime,
-        promptContext,
-        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
-    });
-
-    return NextResponse.json({
-        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
-        object: 'chat.completion',
-        created: Math.floor(Date.now() / 1000),
-        model: parsedRequest.model || 'promptbook-agent',
-        choices: [
-            {
-                index: 0,
-                message: {
-                    role: 'assistant',
-                    content: responseContentWithSuffix,
-                },
-                finish_reason: 'stop',
-            },
-        ],
-        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
-    });
-}
-
-/**
- * Runs the shared side effects after one successful model response.
- */
-async function finalizeHandleChatCompletionResult(options: {
-    responseContentWithSuffix: string;
-    usage: Usage;
-    toolCalls?: ReadonlyArray<ToolCall>;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
-
-    await promptContext.recordChatHistoryMessage({
-        message: {
-            role: 'MODEL',
-            content: responseContentWithSuffix,
-        },
-        previousMessageHash: promptContext.userMessageHash,
-        usage,
-    });
-
-    if (toolCalls && toolCalls.length > 0) {
-        await logCalendarToolCallsActivity({
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentPermanentId: runtime.agentId,
-            toolCalls,
-        });
-    }
-
-    await persistOpenAiCompatibilityFrozenChat({
-        isEnabled: !runtime.isPrivateModeEnabled,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        agentId: runtime.agentId,
-        chatId: promptContext.persistedFrozenChatId,
-        messages: parsedRequest.messages,
-        assistantContent: responseContentWithSuffix,
-        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
-    });
-    await persistHandleChatCompletionLearnedAgentSource({
-        runtime,
-        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
-    });
-}
-
-/**
- * Applies append-only self-learning updates when the current branch should persist them.
- */
-async function persistHandleChatCompletionLearnedAgentSource(options: {
-    runtime: HandleChatCompletionRuntime;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { runtime, shouldPersistLearnedAgentSource } = options;
-    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
-        return;
-    }
-
-    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
-        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
-        resolvedAgentSourceBeforeLearning: runtime.agentSource,
-        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
-    });
-
-    if (learnedAgentSource !== null) {
-        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
     }
 }
-
-/**
- * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
- */
-async function persistOpenAiCompatibilityFrozenChat(options: {
-    isEnabled: boolean;
-    userId: number | null | undefined;
-    agentId: string;
-    messages: ReadonlyArray<unknown>;
-    chatId?: string;
-    assistantContent?: string;
-    includeAssistantPlaceholder?: boolean;
-    failureMessage: string;
-}): Promise<string | undefined> {
-    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
-        options;
-    if (!isEnabled || !userId) {
-        return undefined;
-    }
-
-    const persistedFrozenChat = await persistFrozenUserChat({
-        userId,
-        agentPermanentId: agentId,
-        source: USER_CHAT_SOURCES.OPENAI_API,
-        chatId,
-        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
-            includeAssistantPlaceholder,
-        }),
-    }).catch((error) => {
-        console.error(failureMessage, error);
-        return null;
-    });
-
-    return persistedFrozenChat?.id;
-}
-
-/**
- * Builds one OpenAI-compatible thread from the earlier request messages.
- */
-function createHandleChatCompletionThread(previousMessages: ReadonlyArray<TODO_any>): ChatMessage[] {
-    return previousMessages.map((message: TODO_any, index: number) => ({
-        id: `msg-${index}`,
-        sender: message.role === 'assistant' ? 'agent' : 'user',
-        content: message.content,
-        isComplete: true,
-        createdAt: $getCurrentDate(),
-    }));
-}
-
-/**
- * Appends system messages to the agent source as dynamic `CONTEXT`.
- */
-function appendSystemMessagesToAgentSource(
-    agentSource: string_book,
-    messages: ReadonlyArray<TODO_any>,
-): {
-    agentSourceWithContext: string_book;
-    hasDynamicContext: boolean;
-} {
-    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
-    if (systemMessages.length === 0) {
-        return {
-            agentSourceWithContext: agentSource,
-            hasDynamicContext: false,
-        };
-    }
-
-    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
-    return {
-        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
-        hasDynamicContext: true,
-    };
-}
-
-/**
- * Waits for any running preparation job of the current agent before executing the chat call.
- */
-async function waitForHandleChatCompletionAgentPreparation(options: {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    agentId: string;
-    agentHash: string;
-}): Promise<void> {
-    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
-    const preparationWaitResult = await waitForRunningAgentPreparation({
-        tablePrefix,
-        agentPermanentId: options.agentId,
-        fingerprint: options.agentHash,
-        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    });
-
-    if (preparationWaitResult !== 'not_running') {
-        console.info('[pre-index]', 'openai_chat_wait_result', {
-            tablePrefix,
-            agentPermanentId: options.agentId,
-            agentHash: options.agentHash,
-            preparationWaitResult,
-        });
-    }
-}
-
-/**
- * Resolves request-scoped project, calendar, and email credentials for the current agent.
- */
-async function resolveHandleChatCompletionCredentials(options: {
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    agentId: string;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-}): Promise<{
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-}> {
-    const userId = options.currentUserIdentity?.userId;
-    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
-        resolveUseProjectGithubToken({
-            userId,
-            agentPermanentId: options.agentId,
-        }),
-        options.calendarConnections.length > 0
-            ? resolveUseCalendarGoogleToken({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-        options.useEmailConfiguration.isEnabled
-            ? resolveUseEmailSmtpCredential({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-    ]);
-
-    return {
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-    };
-}
-
-/**
- * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
- */
-async function createHandleChatCompletionAgent(options: {
-    agentName: string;
-    agentSource: string_book;
-    agentId: string;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    localServerUrl: string;
-    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
-    hasDynamicContext: boolean;
-    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
-}): Promise<Agent> {
-    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
-    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
-    const runtimeAgentReferenceResolver = options.hasDynamicContext
-        ? createBookScopedAgentReferenceResolver({
-              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
-              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
-              localServerUrl: options.localServerUrl,
-              fallbackResolver: options.baseAgentReferenceResolver,
-          })
-        : undefined;
-    const agentModelRequirements =
-        options.preparedAgentModelRequirements?.modelRequirements ||
-        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
-            agentReferenceResolver: runtimeAgentReferenceResolver,
-            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
-        }));
-
-    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
-        options.agentSource,
-        options.resolvedAgentContext.resolvedAgentName,
-        await baseOpenAiToolsPromise,
-        {
-            includeDynamicContext: true,
-            agentId: options.agentId,
-            modelRequirements: agentModelRequirements,
-        },
-    );
-
-    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
-        agentName: options.agentName,
-        assistantCacheKey: agentKitResult.assistantCacheKey,
-        vectorStoreHash: agentKitResult.vectorStoreHash,
-        vectorStoreId: agentKitResult.vectorStoreId,
-    });
-
-    return new Agent({
-        agentSource: options.agentSource,
-        executionTools: {
-            llm: agentKitResult.tools,
-        },
-        precomputedModelRequirements: agentModelRequirements,
-        assistantPreparationMode: 'external',
-        isVerbose: true,
-        teacherAgent: null,
-    });
-}
-
-/**
- * Creates one OpenAI-compatible streaming chunk payload.
- */
-function createHandleChatCompletionChunk(options: {
-    runId: string;
-    created: number;
-    model: TODO_any;
-    delta: Record<string, unknown>;
-    finishReason: 'stop' | null;
-}) {
-    return {
-        id: options.runId,
-        object: 'chat.completion.chunk',
-        created: options.created,
-        model: options.model,
-        choices: [
-            {
-                index: 0,
-                delta: options.delta,
-                finish_reason: options.finishReason,
-            },
-        ],
-    };
-}
-
-/**
- * Creates one consistent OpenAI-compatible error response.
- */
-function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
-    return NextResponse.json(
-        {
-            error: {
-                message,
-                type,
-            },
-        },
-        { status },
-    );
-}
-
-// TODO: [🈹] Maybe move chat thread handling here
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f49a4fa80fa21f140e25de59596e5546d62e3f7
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
@@ -0,0 +1,18 @@
+import { NextResponse } from 'next/server';
+
+/**
+ * Creates one consistent OpenAI-compatible error response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
+    return NextResponse.json(
+        {
+            error: {
+                message,
+                type,
+            },
+        },
+        { status },
+    );
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b1979f4621921bc664fed75a05851c6fcb8fba6d
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
@@ -0,0 +1,120 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import { appendMessageSuffix } from '@/src/utils/chat/messageSuffix';
+import type { UncertainNumber, Usage, UsageCounts } from '@promptbook-local/types';
+import { NextResponse } from 'next/server';
+import { computeUsageCounts } from '../../../../../src/execution/utils/computeUsageCounts';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the non-streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionJsonResponse(options: {
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Promise<NextResponse> {
+    const { title, parsedRequest, runtime, promptContext } = options;
+    const result = await runtime.agent.callChatModel(promptContext.prompt);
+    const normalizedResponse = ensureNonEmptyChatContent({
+        content: result.content,
+        context: title,
+    });
+    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+
+    await finalizeHandleChatCompletionResult({
+        responseContentWithSuffix,
+        usage: result.usage,
+        toolCalls: result.toolCalls,
+        parsedRequest,
+        runtime,
+        promptContext,
+        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
+    });
+
+    return NextResponse.json({
+        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
+        object: 'chat.completion',
+        created: Math.floor(Date.now() / 1000),
+        model: parsedRequest.model || 'promptbook-agent',
+        choices: [
+            {
+                index: 0,
+                message: {
+                    role: 'assistant',
+                    content: responseContentWithSuffix,
+                },
+                finish_reason: 'stop',
+            },
+        ],
+        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
+    });
+}
+
+/**
+ * Falls back to the estimated value when the original token count is unknown.
+ *
+ * @param tokenCount - Token count reported by the execution tools.
+ * @param fallbackValue - Estimated token count based on text length.
+ * @returns Token count to report in the OpenAI-compatible response.
+ */
+function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
+    if (tokenCount.value === 0 && tokenCount.isUncertain) {
+        return {
+            value: fallbackValue,
+            isUncertain: true,
+        };
+    }
+
+    return tokenCount;
+}
+
+/**
+ * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
+ *
+ * @param promptContent - Prompt content used for the request.
+ * @param completionContent - Assistant response content.
+ * @param usage - Native Promptbook usage details.
+ * @returns Usage payload for compatibility responses.
+ */
+function createCompatibilityUsage(
+    promptContent: string,
+    completionContent: string,
+    usage: Usage,
+): {
+    prompt_tokens: number;
+    completion_tokens: number;
+    total_tokens: number;
+    details: Usage;
+} {
+    const promptCounts = computeUsageCounts(promptContent);
+    const completionCounts = computeUsageCounts(completionContent);
+    const inputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
+        ...promptCounts,
+    };
+    const outputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
+        ...completionCounts,
+    };
+
+    const promptTokens = inputUsage.tokensCount.value;
+    const completionTokens = outputUsage.tokensCount.value;
+
+    return {
+        prompt_tokens: promptTokens,
+        completion_tokens: completionTokens,
+        total_tokens: promptTokens + completionTokens,
+        details: {
+            price: usage.price,
+            duration: usage.duration,
+            input: inputUsage,
+            output: outputUsage,
+        },
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..7aac8141a8b1237ce36430b4b4598a618f0383ac
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
@@ -0,0 +1,111 @@
+import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
+import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
+import type { ChatMessage, ChatPrompt } from '@promptbook-local/types';
+import { $getCurrentDate } from '@promptbook-local/utils';
+import type { NextRequest } from 'next/server';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Prompt, history recorder, and persistence context created for one chat call.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionPromptContext = {
+    prompt: ChatPrompt;
+    userMessageHash: string;
+    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
+    persistedFrozenChatId?: string;
+};
+
+/**
+ * Creates prompt and persistence helpers for one chat completion request.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionPromptContext(options: {
+    request: NextRequest;
+    title: string;
+    apiKey: string | null;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+}): Promise<HandleChatCompletionPromptContext> {
+    const { request, title, apiKey, parsedRequest, runtime } = options;
+    const timezone = request.headers.get('x-timezone') || 'UTC';
+    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
+    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
+    const recordChatHistoryMessage = await createChatHistoryRecorder({
+        request,
+        agentIdentifier: runtime.agentId,
+        agentHash: runtime.agentHash,
+        source: 'OPENAI_API_COMPATIBILITY',
+        apiKey,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        isEnabled: !runtime.isPrivateModeEnabled,
+    });
+    const userMessageHash = await recordChatHistoryMessage({
+        message: {
+            role: 'USER',
+            content: lastMessage.content,
+        },
+        previousMessageHash: null,
+    });
+    const promptParameters = composePromptParametersWithMemoryContext({
+        baseParameters: {
+            ...parsedRequest.incomingParameters,
+            timezone,
+        },
+        currentUserIdentity: runtime.currentUserIdentity,
+        agentPermanentId: runtime.agentId,
+        agentName: runtime.resolvedAgentContext.resolvedAgentName,
+        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
+        projectRepositories: runtime.projectRepositories,
+        projectGithubToken: runtime.projectGithubToken,
+        emailSmtpCredential: runtime.emailSmtpCredential,
+        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
+        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
+        calendarConnections: runtime.calendarConnections,
+    });
+    const prompt: ChatPrompt = {
+        title,
+        content: lastMessage.content,
+        modelRequirements: {
+            modelVariant: 'CHAT',
+            responseFormat: parsedRequest.responseFormat,
+            toolChoice: parsedRequest.runtimeToolChoice,
+        },
+        parameters: promptParameters,
+        thread,
+        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
+    };
+
+    return {
+        prompt,
+        userMessageHash,
+        recordChatHistoryMessage,
+        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
+            isEnabled: !runtime.isPrivateModeEnabled,
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentId: runtime.agentId,
+            messages: parsedRequest.messages,
+            includeAssistantPlaceholder: true,
+            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
+        }),
+    };
+}
+
+/**
+ * Builds one OpenAI-compatible thread from the earlier request messages.
+ */
+function createHandleChatCompletionThread(
+    previousMessages: HandleChatCompletionParsedRequest['threadMessages'],
+): Array<ChatMessage> {
+    return previousMessages.map((message, index) => ({
+        id: `msg-${index}`,
+        sender: message.role === 'assistant' ? 'agent' : 'user',
+        content: message.content,
+        isComplete: true,
+        createdAt: $getCurrentDate(),
+    }));
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..85e096027760bc45fb730e74f8ae3bbb721b1029
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
@@ -0,0 +1,162 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import {
+    appendMessageSuffix,
+    createMessageSuffixAppendix,
+    emulateMessageSuffixStreaming,
+} from '@/src/utils/chat/messageSuffix';
+import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
+import { encodeChatStreamWhitespaceForTransport } from '../../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
+import { prepareToolCallsForStreaming } from '../toolCallStreaming';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionStreamResponse(options: {
+    request: Request;
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Response {
+    const { request, title, parsedRequest, runtime, promptContext } = options;
+    const encoder = new TextEncoder();
+    const responseModel = parsedRequest.model || 'promptbook-agent';
+    const readableStream = new ReadableStream({
+        async start(controller) {
+            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
+            const created = Math.floor(Date.now() / 1000);
+            const emitSsePayload = (payload: unknown) => {
+                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
+            };
+            const emitDeltaChunk = (deltaContent: string) => {
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {
+                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
+                        },
+                        finishReason: null,
+                    }),
+                );
+            };
+
+            emitSsePayload(
+                createHandleChatCompletionChunk({
+                    runId,
+                    created,
+                    model: responseModel,
+                    delta: {
+                        role: 'assistant',
+                        content: '',
+                    },
+                    finishReason: null,
+                }),
+            );
+
+            let hasMeaningfulDelta = false;
+
+            try {
+                const handleStreamChunk = createChatStreamHandler({
+                    onDelta: (deltaContent) => {
+                        if (deltaContent.trim().length > 0) {
+                            hasMeaningfulDelta = true;
+                        }
+                        emitDeltaChunk(deltaContent);
+                    },
+                    onToolCalls: (toolCalls) => {
+                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
+                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
+                    },
+                });
+                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
+                    signal: request.signal,
+                });
+                const normalizedResponse = ensureNonEmptyChatContent({
+                    content: result.content,
+                    context: title,
+                });
+
+                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
+                    emitDeltaChunk(normalizedResponse.content);
+                }
+
+                const messageSuffixAppendix = createMessageSuffixAppendix(
+                    normalizedResponse.content,
+                    runtime.messageSuffix,
+                );
+                if (messageSuffixAppendix) {
+                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
+                        emitDeltaChunk(delta);
+                    });
+                }
+
+                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+                await finalizeHandleChatCompletionResult({
+                    responseContentWithSuffix,
+                    usage: result.usage,
+                    toolCalls: result.toolCalls,
+                    parsedRequest,
+                    runtime,
+                    promptContext,
+                    shouldPersistLearnedAgentSource: true,
+                });
+
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {},
+                        finishReason: 'stop',
+                    }),
+                );
+                controller.enqueue(encoder.encode('[DONE]'));
+            } catch (error) {
+                console.error('Error during streaming:', error);
+                controller.error(error);
+            }
+            controller.close();
+        },
+    });
+
+    return new Response(readableStream, {
+        headers: {
+            'Content-Type': 'text/event-stream',
+            'Cache-Control': 'no-cache',
+            Connection: 'keep-alive',
+        },
+    });
+}
+
+/**
+ * Creates one OpenAI-compatible streaming chunk payload.
+ */
+function createHandleChatCompletionChunk(options: {
+    runId: string;
+    created: number;
+    model: HandleChatCompletionParsedRequest['model'];
+    delta: Record<string, unknown>;
+    finishReason: 'stop' | null;
+}) {
+    return {
+        id: options.runId,
+        object: 'chat.completion.chunk',
+        created: options.created,
+        model: options.model,
+        choices: [
+            {
+                index: 0,
+                delta: options.delta,
+                finish_reason: options.finishReason,
+            },
+        ],
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
new file mode 100644
index 0000000000000000000000000000000000000000..dd6a7d87ea491df245dbf93d8bcb277a9a10b034
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
@@ -0,0 +1,78 @@
+import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
+import { resolveAppendOnlySelfLearningAgentSource } from '@/src/utils/resolveAppendOnlySelfLearningAgentSource';
+import type { ToolCall, Usage } from '@promptbook-local/types';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Runs the shared side effects after one successful model response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function finalizeHandleChatCompletionResult(options: {
+    responseContentWithSuffix: string;
+    usage: Usage;
+    toolCalls?: ReadonlyArray<ToolCall>;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
+
+    await promptContext.recordChatHistoryMessage({
+        message: {
+            role: 'MODEL',
+            content: responseContentWithSuffix,
+        },
+        previousMessageHash: promptContext.userMessageHash,
+        usage,
+    });
+
+    if (toolCalls && toolCalls.length > 0) {
+        await logCalendarToolCallsActivity({
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentPermanentId: runtime.agentId,
+            toolCalls,
+        });
+    }
+
+    await persistOpenAiCompatibilityFrozenChat({
+        isEnabled: !runtime.isPrivateModeEnabled,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        agentId: runtime.agentId,
+        chatId: promptContext.persistedFrozenChatId,
+        messages: parsedRequest.messages,
+        assistantContent: responseContentWithSuffix,
+        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
+    });
+    await persistHandleChatCompletionLearnedAgentSource({
+        runtime,
+        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
+    });
+}
+
+/**
+ * Applies append-only self-learning updates when the current branch should persist them.
+ */
+async function persistHandleChatCompletionLearnedAgentSource(options: {
+    runtime: HandleChatCompletionRuntime;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { runtime, shouldPersistLearnedAgentSource } = options;
+    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
+        return;
+    }
+
+    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
+        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
+        resolvedAgentSourceBeforeLearning: runtime.agentSource,
+        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
+    });
+
+    if (learnedAgentSource !== null) {
+        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
+    }
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
new file mode 100644
index 0000000000000000000000000000000000000000..58e1c26d7f428c1bdb39b36411b3f75256e3de51
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
@@ -0,0 +1,176 @@
+import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
+import type { LlmToolDefinition, TODO_any } from '@promptbook-local/types';
+import { NextRequest, NextResponse } from 'next/server';
+import type OpenAI from 'openai';
+import { HTTP_STATUS_CODES } from '../../constants';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Definition of open AI chat tool.
+ */
+type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
+
+/**
+ * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionParsedRequest = {
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    threadMessages: ReadonlyArray<TODO_any>;
+    stream: TODO_any;
+    model: TODO_any;
+    responseFormat: TODO_any;
+    runtimeTools?: Array<LlmToolDefinition>;
+    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+    incomingParameters: Record<string, unknown>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Parses and validates the OpenAI-compatible request payload.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function parseHandleChatCompletionRequest(options: {
+    body: TODO_any;
+    agentNameFromParams?: string;
+    request: NextRequest;
+}): HandleChatCompletionParsedRequest | NextResponse {
+    const { body, agentNameFromParams, request } = options;
+    const {
+        messages,
+        stream,
+        model,
+        response_format: responseFormat,
+        tools: rawTools,
+        tool_choice: toolChoice,
+        parameters: rawParameters = {},
+    } = body;
+    const agentName = (agentNameFromParams || model) as string | undefined;
+    if (!agentName) {
+        return createHandleChatCompletionErrorResponse(
+            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    if (!messages || !Array.isArray(messages) || messages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array is required and cannot be empty.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
+    if (threadMessages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array must contain at least one non-system message.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    return {
+        agentName,
+        messages,
+        threadMessages,
+        stream,
+        model,
+        responseFormat,
+        runtimeTools: convertOpenAiTools(rawTools),
+        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
+        incomingParameters:
+            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
+                ? (rawParameters as Record<string, unknown>)
+                : {},
+        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
+    };
+}
+
+/**
+ * Handles convert open Ai tools.
+ */
+function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
+    if (!Array.isArray(rawTools)) {
+        return undefined;
+    }
+
+    const converted = rawTools
+        .map((tool) => convertOpenAiTool(tool))
+        .filter((tool): tool is LlmToolDefinition => tool !== null);
+
+    return converted.length > 0 ? converted : undefined;
+}
+
+/**
+ * Handles convert open Ai tool.
+ */
+function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
+    const tool = rawTool as OpenAIChatToolDefinition;
+    if (!tool || tool.type !== 'function') {
+        return null;
+    }
+
+    const functionDefinition = tool.function;
+    if (!functionDefinition) {
+        return null;
+    }
+
+    const { name, description, parameters } = functionDefinition;
+    if (typeof name !== 'string' || name.trim().length === 0) {
+        return null;
+    }
+
+    const parameterSchema = parameters ?? {};
+    if (parameterSchema.type !== 'object') {
+        return null;
+    }
+
+    const properties = parameterSchema.properties ?? {};
+    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
+
+    for (const [key, value] of Object.entries(properties)) {
+        if (!value || typeof value !== 'object') {
+            continue;
+        }
+
+        normalizedProperties[key] = {
+            type: typeof value.type === 'string' ? value.type : 'string',
+            description: typeof value.description === 'string' ? value.description : undefined,
+        };
+    }
+
+    const required =
+        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
+            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
+            : undefined;
+
+    return {
+        name,
+        description: typeof description === 'string' ? description : '',
+        parameters: {
+            type: 'object',
+            properties: normalizedProperties,
+            required,
+            additionalProperties:
+                typeof parameterSchema.additionalProperties === 'boolean'
+                    ? parameterSchema.additionalProperties
+                    : undefined,
+        },
+    };
+}
+
+/**
+ * Parses open Ai tool choice.
+ */
+function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
+    if (value === undefined || value === null) {
+        return undefined;
+    }
+
+    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
new file mode 100644
index 0000000000000000000000000000000000000000..60e5f1b008bcf5a6a1836c06c4c3d9ebcb909a9f
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
@@ -0,0 +1,163 @@
+import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
+import type { ChatMessage } from '@promptbook-local/types';
+
+/**
+ * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
+ */
+type OpenAiCompatibilityMessageLike = {
+    role?: unknown;
+    content?: unknown;
+};
+
+/**
+ * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function persistOpenAiCompatibilityFrozenChat(options: {
+    isEnabled: boolean;
+    userId: number | null | undefined;
+    agentId: string;
+    messages: ReadonlyArray<unknown>;
+    chatId?: string;
+    assistantContent?: string;
+    includeAssistantPlaceholder?: boolean;
+    failureMessage: string;
+}): Promise<string | undefined> {
+    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
+        options;
+    if (!isEnabled || !userId) {
+        return undefined;
+    }
+
+    const persistedFrozenChat = await persistFrozenUserChat({
+        userId,
+        agentPermanentId: agentId,
+        source: USER_CHAT_SOURCES.OPENAI_API,
+        chatId,
+        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
+            includeAssistantPlaceholder,
+        }),
+    }).catch((error) => {
+        console.error(failureMessage, error);
+        return null;
+    });
+
+    return persistedFrozenChat?.id;
+}
+
+/**
+ * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
+ */
+function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
+    if (typeof value === 'string') {
+        return value;
+    }
+
+    if (Array.isArray(value)) {
+        return value
+            .map((part) => {
+                if (typeof part === 'string') {
+                    return part;
+                }
+
+                if (!part || typeof part !== 'object') {
+                    return '';
+                }
+
+                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
+                if (typeof partLike.text === 'string') {
+                    return partLike.text;
+                }
+
+                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
+                    return '[Image input]';
+                }
+
+                if (partLike.image_url) {
+                    return '[Image input]';
+                }
+
+                return '';
+            })
+            .filter(Boolean)
+            .join('\n');
+    }
+
+    if (value === null || value === undefined) {
+        return '';
+    }
+
+    try {
+        return JSON.stringify(value, null, 2);
+    } catch {
+        return String(value);
+    }
+}
+
+/**
+ * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ */
+function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
+    if (typeof role !== 'string') {
+        return 'USER';
+    }
+
+    switch (role) {
+        case 'assistant':
+            return 'AGENT';
+        case 'system':
+            return 'SYSTEM';
+        case 'tool':
+            return 'TOOL';
+        case 'developer':
+            return 'SYSTEM';
+        default:
+            return 'USER';
+    }
+}
+
+/**
+ * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
+ */
+function createFrozenOpenAiCompatibilityMessages(
+    rawMessages: ReadonlyArray<unknown>,
+    assistantContent?: string,
+    options: {
+        includeAssistantPlaceholder?: boolean;
+    } = {},
+): Array<ChatMessage> {
+    const startedAt = Date.now();
+    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
+        const message = rawMessage as OpenAiCompatibilityMessageLike;
+
+        return {
+            id: `openai-frozen-${index}`,
+            sender: mapOpenAiCompatibilityRoleToSender(message.role),
+            content: normalizeOpenAiCompatibilityMessageContent(message.content),
+            isComplete: true,
+            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage;
+    });
+
+    if (assistantContent !== undefined) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: assistantContent,
+            isComplete: true,
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    } else if (options.includeAssistantPlaceholder) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: '',
+            isComplete: false,
+            lifecycleState: 'running',
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    }
+
+    return messages;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fd44b7ea5a87168725a9e99784038137c96537ab
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
@@ -0,0 +1,305 @@
+import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
+import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
+import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
+import type { string_book, TODO_any } from '@promptbook-local/types';
+import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
+import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
+import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
+import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
+import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
+import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
+import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
+import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
+import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
+import {
+    createBookScopedAgentReferenceResolver,
+    parseBookScopedAgentIdentifier,
+} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
+import { resolveMessageSuffixFromAgentSource } from '@/src/utils/chat/messageSuffix';
+import { NextRequest, NextResponse } from 'next/server';
+import { isAgentDeleted } from '../../app/agents/[agentName]/_utils';
+import {
+    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    resolveAgentCollectionTablePrefix,
+    waitForRunningAgentPreparation,
+} from '../agentPreparation';
+import { resolveCachedServerAgentContext, resolveCachedServerAgentModelRequirements } from '../cachedServerAgentRuntime';
+import { AgentKitCacheManager } from '../cache/AgentKitCacheManager';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Runtime state resolved before the chat model is executed.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionRuntime = {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    agent: Agent;
+    agentSource: string_book;
+    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
+    messageSuffix: string | null;
+    agentId: string;
+    agentHash: string;
+    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Resolves agent/runtime dependencies needed before prompt execution.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function resolveHandleChatCompletionRuntime(options: {
+    request: NextRequest;
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    isPrivateModeEnabled: boolean;
+}): Promise<HandleChatCompletionRuntime | NextResponse> {
+    const { request, agentName, messages, isPrivateModeEnabled } = options;
+    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
+    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
+
+    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
+        return createHandleChatCompletionErrorResponse(
+            'This agent has been deleted. You can restore it from the Recycle Bin.',
+            'agent_deleted',
+            410,
+        );
+    }
+
+    const [collection, baseAgentReferenceResolver] = await Promise.all([
+        $provideAgentCollectionForServer(),
+        $provideAgentReferenceResolver(),
+    ]);
+    const localServerUrl = new URL(request.url).origin;
+    const resolvedAgentContext = await resolveCachedServerAgentContext({
+        collection,
+        agentIdentifier: agentName,
+        localServerUrl,
+        fallbackResolver: baseAgentReferenceResolver,
+    }).catch(() => null);
+
+    if (!resolvedAgentContext?.resolvedAgentSource) {
+        return createHandleChatCompletionErrorResponse(`Agent '${agentName}' not found.`, 'invalid_request_error', 404);
+    }
+
+    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
+    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
+    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
+    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
+    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
+    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
+    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
+    agentSource = agentSourceWithContext;
+
+    const preparedAgentModelRequirements = !hasDynamicContext
+        ? await resolveCachedServerAgentModelRequirements({
+              resolvedAgentContext,
+              localServerUrl,
+              fallbackResolver: baseAgentReferenceResolver,
+          })
+        : null;
+    const agentId = resolvedAgentContext.parentAgentPermanentId;
+    const agentHash = computeAgentHash(agentSource);
+    await waitForHandleChatCompletionAgentPreparation({
+        collection,
+        agentId,
+        agentHash,
+    });
+
+    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
+    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
+        await resolveHandleChatCompletionCredentials({
+            currentUserIdentity,
+            agentId,
+            calendarConnections,
+            useEmailConfiguration,
+        });
+    const agent = await createHandleChatCompletionAgent({
+        agentName,
+        agentSource,
+        agentId,
+        resolvedAgentContext,
+        localServerUrl,
+        baseAgentReferenceResolver,
+        hasDynamicContext,
+        preparedAgentModelRequirements,
+    });
+
+    return {
+        collection,
+        resolvedAgentContext,
+        agent,
+        agentSource,
+        unresolvedAgentSource,
+        messageSuffix,
+        agentId,
+        agentHash,
+        projectRepositories,
+        calendarConnections,
+        useEmailConfiguration,
+        currentUserIdentity,
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+        isPrivateModeEnabled,
+    };
+}
+
+/**
+ * Appends system messages to the agent source as dynamic `CONTEXT`.
+ */
+function appendSystemMessagesToAgentSource(
+    agentSource: string_book,
+    messages: ReadonlyArray<TODO_any>,
+): {
+    agentSourceWithContext: string_book;
+    hasDynamicContext: boolean;
+} {
+    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
+    if (systemMessages.length === 0) {
+        return {
+            agentSourceWithContext: agentSource,
+            hasDynamicContext: false,
+        };
+    }
+
+    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
+    return {
+        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
+        hasDynamicContext: true,
+    };
+}
+
+/**
+ * Waits for any running preparation job of the current agent before executing the chat call.
+ */
+async function waitForHandleChatCompletionAgentPreparation(options: {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    agentId: string;
+    agentHash: string;
+}): Promise<void> {
+    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
+    const preparationWaitResult = await waitForRunningAgentPreparation({
+        tablePrefix,
+        agentPermanentId: options.agentId,
+        fingerprint: options.agentHash,
+        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    });
+
+    if (preparationWaitResult !== 'not_running') {
+        console.info('[pre-index]', 'openai_chat_wait_result', {
+            tablePrefix,
+            agentPermanentId: options.agentId,
+            agentHash: options.agentHash,
+            preparationWaitResult,
+        });
+    }
+}
+
+/**
+ * Resolves request-scoped project, calendar, and email credentials for the current agent.
+ */
+async function resolveHandleChatCompletionCredentials(options: {
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    agentId: string;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+}): Promise<{
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+}> {
+    const userId = options.currentUserIdentity?.userId;
+    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
+        resolveUseProjectGithubToken({
+            userId,
+            agentPermanentId: options.agentId,
+        }),
+        options.calendarConnections.length > 0
+            ? resolveUseCalendarGoogleToken({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+        options.useEmailConfiguration.isEnabled
+            ? resolveUseEmailSmtpCredential({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+    ]);
+
+    return {
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+    };
+}
+
+/**
+ * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
+ */
+async function createHandleChatCompletionAgent(options: {
+    agentName: string;
+    agentSource: string_book;
+    agentId: string;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    localServerUrl: string;
+    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
+    hasDynamicContext: boolean;
+    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
+}): Promise<Agent> {
+    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
+    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
+    const runtimeAgentReferenceResolver = options.hasDynamicContext
+        ? createBookScopedAgentReferenceResolver({
+              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
+              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
+              localServerUrl: options.localServerUrl,
+              fallbackResolver: options.baseAgentReferenceResolver,
+          })
+        : undefined;
+    const agentModelRequirements =
+        options.preparedAgentModelRequirements?.modelRequirements ||
+        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
+            agentReferenceResolver: runtimeAgentReferenceResolver,
+            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
+        }));
+
+    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
+        options.agentSource,
+        options.resolvedAgentContext.resolvedAgentName,
+        await baseOpenAiToolsPromise,
+        {
+            includeDynamicContext: true,
+            agentId: options.agentId,
+            modelRequirements: agentModelRequirements,
+        },
+    );
+
+    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
+        agentName: options.agentName,
+        assistantCacheKey: agentKitResult.assistantCacheKey,
+        vectorStoreHash: agentKitResult.vectorStoreHash,
+        vectorStoreId: agentKitResult.vectorStoreId,
+    });
+
+    return new Agent({
+        agentSource: options.agentSource,
+        executionTools: {
+            llm: agentKitResult.tools,
+        },
+        precomputedModelRequirements: agentModelRequirements,
+        assistantPreparationMode: 'external',
+        isVerbose: true,
+        teacherAgent: null,
+    });
+}


thinking
**Considering grammar adjustments**

I need to comment on the main public function "Handles convert open Ai..." because the grammar seems a bit off. It could be clearer if I say, "Handles OpenAI-compatible chat completion requests." However, I’m thinking it may also be good to maintain the current style. I should also consider updating the changelog to reflect this change, possibly by adding a top bullet point to emphasize the update. Let’s keep things organized!
file update
M C:/Users/me/work/ai/promptbook\changelog/_current-preversion.md
@@ -1 +1,6 @@
+-   Refactored Agents Server `handleChatCompletion.ts` into a thinner chat-completion facade plus focused private helper modules without changing external behavior:
+
+    -   Extracted OpenAI-compatible request parsing, runtime/agent resolution, prompt-context creation, frozen-chat persistence, shared post-response side effects, and the streaming vs non-streaming response branches into dedicated private files behind the existing `handleChatCompletion` entrypoint.
+    -   Kept the existing API-key validation, deleted-agent handling, dynamic system-context injection, AgentKit cache usage, private-mode behavior, frozen audit snapshots, self-learning persistence, streaming chunk format, and OpenAI-compatible JSON response shape intact while reducing responsibility density in the main file.
+
 -   Refactored Agents Server `errorMessages.ts` into smaller focused helpers without changing external behavior:
apply_patch(auto_approved=true) exited 0 in 744ms:
Success. Updated the following files:
M changelog/_current-preversion.md
file update:
diff --git a/apps/agents-server/src/utils/handleChatCompletion.ts b/apps/agents-server/src/utils/handleChatCompletion.ts
index 9c96d36fad9cf24bf26d89f57372332add4e4595..cff56fafd6cfa90bb4094f9f202c4f01f6f74af1
--- a/apps/agents-server/src/utils/handleChatCompletion.ts
+++ b/apps/agents-server/src/utils/handleChatCompletion.ts
@@ -1,392 +1,16 @@
-import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
-import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
-import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
-import {
-    createBookScopedAgentReferenceResolver,
-    parseBookScopedAgentIdentifier,
-} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
-import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
-import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
-import {
-    appendMessageSuffix,
-    createMessageSuffixAppendix,
-    emulateMessageSuffixStreaming,
-    resolveMessageSuffixFromAgentSource,
-} from '@/src/utils/chat/messageSuffix';
-import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
-import { prepareToolCallsForStreaming } from './toolCallStreaming';
-import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
-import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
-import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
-import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
-import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
-import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
-import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
-import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
-import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
-import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
-import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
-import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
-import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
-import type {
-    ChatMessage,
-    ChatPrompt,
-    LlmToolDefinition,
-    string_book,
-    ToolCall,
-    TODO_any,
-    UncertainNumber,
-    Usage,
-    UsageCounts,
-} from '@promptbook-local/types';
-import { $getCurrentDate } from '@promptbook-local/utils';
 import { NextRequest, NextResponse } from 'next/server';
-import type OpenAI from 'openai';
-import { computeUsageCounts } from '../../../../src/execution/utils/computeUsageCounts';
-import { encodeChatStreamWhitespaceForTransport } from '../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
-import { isAgentDeleted } from '../app/agents/[agentName]/_utils';
 import { HTTP_STATUS_CODES } from '../constants';
-import { AgentKitCacheManager } from './cache/AgentKitCacheManager';
 import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
+import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
+import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
+import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
+import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
+import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
 import { validateApiKey } from './validateApiKey';
-import {
-    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    resolveAgentCollectionTablePrefix,
-    waitForRunningAgentPreparation,
-} from './agentPreparation';
-import {
-    resolveCachedServerAgentContext,
-    resolveCachedServerAgentModelRequirements,
-} from './cachedServerAgentRuntime';
-import { resolveAppendOnlySelfLearningAgentSource } from './resolveAppendOnlySelfLearningAgentSource';
 
 /**
- * Falls back to the estimated value when the original token count is unknown.
- *
- * @param tokenCount - Token count reported by the execution tools.
- * @param fallbackValue - Estimated token count based on text length.
- * @returns Token count to report in the OpenAI-compatible response.
- */
-function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
-    if (tokenCount.value === 0 && tokenCount.isUncertain) {
-        return {
-            value: fallbackValue,
-            isUncertain: true,
-        };
-    }
-
-    return tokenCount;
-}
-
-/**
- * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
- *
- * @param promptContent - Prompt content used for the request.
- * @param completionContent - Assistant response content.
- * @param usage - Native Promptbook usage details.
- * @returns Usage payload for compatibility responses.
- */
-function createCompatibilityUsage(
-    promptContent: string,
-    completionContent: string,
-    usage: Usage,
-): {
-    prompt_tokens: number;
-    completion_tokens: number;
-    total_tokens: number;
-    details: Usage;
-} {
-    const promptCounts = computeUsageCounts(promptContent);
-    const completionCounts = computeUsageCounts(completionContent);
-    const inputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
-        ...promptCounts,
-    };
-    const outputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
-        ...completionCounts,
-    };
-
-    const promptTokens = inputUsage.tokensCount.value;
-    const completionTokens = outputUsage.tokensCount.value;
-
-    return {
-        prompt_tokens: promptTokens,
-        completion_tokens: completionTokens,
-        total_tokens: promptTokens + completionTokens,
-        details: {
-            price: usage.price,
-            duration: usage.duration,
-            input: inputUsage,
-            output: outputUsage,
-        },
-    };
-}
-
-/**
- * Definition of open AI chat tool.
- */
-type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
-
-/**
- * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
- */
-type OpenAiCompatibilityMessageLike = {
-    role?: unknown;
-    content?: unknown;
-};
-
-/**
- * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
- */
-type HandleChatCompletionParsedRequest = {
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    threadMessages: ReadonlyArray<TODO_any>;
-    stream: TODO_any;
-    model: TODO_any;
-    responseFormat: TODO_any;
-    runtimeTools?: Array<LlmToolDefinition>;
-    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-    incomingParameters: Record<string, unknown>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Runtime state resolved before the chat model is executed.
- */
-type HandleChatCompletionRuntime = {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    agent: Agent;
-    agentSource: string_book;
-    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
-    messageSuffix: string | null;
-    agentId: string;
-    agentHash: string;
-    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Prompt, history recorder, and persistence context created for one chat call.
- */
-type HandleChatCompletionPromptContext = {
-    prompt: ChatPrompt;
-    userMessageHash: string;
-    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
-    persistedFrozenChatId?: string;
-};
-
-/**
- * Handles convert open Ai tools.
- */
-function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
-    if (!Array.isArray(rawTools)) {
-        return undefined;
-    }
-
-    const converted = rawTools
-        .map((tool) => convertOpenAiTool(tool))
-        .filter((tool): tool is LlmToolDefinition => tool !== null);
-
-    return converted.length > 0 ? converted : undefined;
-}
-
-/**
- * Handles convert open Ai tool.
- */
-function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
-    const tool = rawTool as OpenAIChatToolDefinition;
-    if (!tool || tool.type !== 'function') {
-        return null;
-    }
-
-    const functionDefinition = tool.function;
-    if (!functionDefinition) {
-        return null;
-    }
-
-    const { name, description, parameters } = functionDefinition;
-    if (typeof name !== 'string' || name.trim().length === 0) {
-        return null;
-    }
-
-    const parameterSchema = parameters ?? {};
-    if (parameterSchema.type !== 'object') {
-        return null;
-    }
-
-    const properties = parameterSchema.properties ?? {};
-    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
-
-    for (const [key, value] of Object.entries(properties)) {
-        if (!value || typeof value !== 'object') {
-            continue;
-        }
-
-        normalizedProperties[key] = {
-            type: typeof value.type === 'string' ? value.type : 'string',
-            description: typeof value.description === 'string' ? value.description : undefined,
-        };
-    }
-
-    const required =
-        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
-            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
-            : undefined;
-
-    return {
-        name,
-        description: typeof description === 'string' ? description : '',
-        parameters: {
-            type: 'object',
-            properties: normalizedProperties,
-            required,
-            additionalProperties:
-                typeof parameterSchema.additionalProperties === 'boolean'
-                    ? parameterSchema.additionalProperties
-                    : undefined,
-        },
-    };
-}
-
-/**
- * Parses open Ai tool choice.
- */
-function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
-    if (value === undefined || value === null) {
-        return undefined;
-    }
-
-    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-}
-
-/**
- * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
- */
-function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
-    if (typeof value === 'string') {
-        return value;
-    }
-
-    if (Array.isArray(value)) {
-        return value
-            .map((part) => {
-                if (typeof part === 'string') {
-                    return part;
-                }
-
-                if (!part || typeof part !== 'object') {
-                    return '';
-                }
-
-                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
-                if (typeof partLike.text === 'string') {
-                    return partLike.text;
-                }
-
-                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
-                    return '[Image input]';
-                }
-
-                if (partLike.image_url) {
-                    return '[Image input]';
-                }
-
-                return '';
-            })
-            .filter(Boolean)
-            .join('\n');
-    }
-
-    if (value === null || value === undefined) {
-        return '';
-    }
-
-    try {
-        return JSON.stringify(value, null, 2);
-    } catch {
-        return String(value);
-    }
-}
-
-/**
- * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ * Handles convert open Ai chat completion requests routed through the Agents Server.
  */
-function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
-    if (typeof role !== 'string') {
-        return 'USER';
-    }
-
-    switch (role) {
-        case 'assistant':
-            return 'AGENT';
-        case 'system':
-            return 'SYSTEM';
-        case 'tool':
-            return 'TOOL';
-        case 'developer':
-            return 'SYSTEM';
-        default:
-            return 'USER';
-    }
-}
-
-/**
- * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
- */
-function createFrozenOpenAiCompatibilityMessages(
-    rawMessages: ReadonlyArray<unknown>,
-    assistantContent?: string,
-    options: {
-        includeAssistantPlaceholder?: boolean;
-    } = {},
-): Array<ChatMessage> {
-    const startedAt = Date.now();
-    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
-        const message = rawMessage as OpenAiCompatibilityMessageLike;
-
-        return {
-            id: `openai-frozen-${index}`,
-            sender: mapOpenAiCompatibilityRoleToSender(message.role),
-            content: normalizeOpenAiCompatibilityMessageContent(message.content),
-            isComplete: true,
-            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage;
-    });
-
-    if (assistantContent !== undefined) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: assistantContent,
-            isComplete: true,
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    } else if (options.includeAssistantPlaceholder) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: '',
-            isComplete: false,
-            lifecycleState: 'running',
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    }
-
-    return messages;
-}
-
-/**
- * Handles chat completion.
- */
 export async function handleChatCompletion(
     request: NextRequest,
     params: { agentName?: string },
@@ -399,7 +23,6 @@
         return versionMismatchResponse;
     }
 
-    // Validate API key explicitly (in addition to middleware)
     const apiKeyValidation = await validateApiKey(request);
     if (!apiKeyValidation.isValid) {
         return NextResponse.json(
@@ -462,724 +85,5 @@
             { error: { message: (error as Error).message || 'Internal Server Error', type: 'server_error' } },
             { status: HTTP_STATUS_CODES.INTERNAL_SERVER_ERROR },
         );
-    }
-}
-
-/**
- * Parses and validates the OpenAI-compatible request payload.
- */
-function parseHandleChatCompletionRequest(options: {
-    body: TODO_any;
-    agentNameFromParams?: string;
-    request: NextRequest;
-}): HandleChatCompletionParsedRequest | NextResponse {
-    const { body, agentNameFromParams, request } = options;
-    const {
-        messages,
-        stream,
-        model,
-        response_format: responseFormat,
-        tools: rawTools,
-        tool_choice: toolChoice,
-        parameters: rawParameters = {},
-    } = body;
-    const agentName = (agentNameFromParams || model) as string | undefined;
-    if (!agentName) {
-        return createHandleChatCompletionErrorResponse(
-            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    if (!messages || !Array.isArray(messages) || messages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array is required and cannot be empty.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
-    if (threadMessages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array must contain at least one non-system message.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    return {
-        agentName,
-        messages,
-        threadMessages,
-        stream,
-        model,
-        responseFormat,
-        runtimeTools: convertOpenAiTools(rawTools),
-        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
-        incomingParameters:
-            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
-                ? (rawParameters as Record<string, unknown>)
-                : {},
-        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
-    };
-}
-
-/**
- * Resolves agent/runtime dependencies needed before prompt execution.
- */
-async function resolveHandleChatCompletionRuntime(options: {
-    request: NextRequest;
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    isPrivateModeEnabled: boolean;
-}): Promise<HandleChatCompletionRuntime | NextResponse> {
-    const { request, agentName, messages, isPrivateModeEnabled } = options;
-    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
-    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
-
-    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
-        return createHandleChatCompletionErrorResponse(
-            'This agent has been deleted. You can restore it from the Recycle Bin.',
-            'agent_deleted',
-            410,
-        );
-    }
-
-    const [collection, baseAgentReferenceResolver] = await Promise.all([
-        $provideAgentCollectionForServer(),
-        $provideAgentReferenceResolver(),
-    ]);
-    const localServerUrl = new URL(request.url).origin;
-    const resolvedAgentContext = await resolveCachedServerAgentContext({
-        collection,
-        agentIdentifier: agentName,
-        localServerUrl,
-        fallbackResolver: baseAgentReferenceResolver,
-    }).catch(() => null);
-
-    if (!resolvedAgentContext?.resolvedAgentSource) {
-        return createHandleChatCompletionErrorResponse(
-            `Agent '${agentName}' not found.`,
-            'invalid_request_error',
-            HTTP_STATUS_CODES.NOT_FOUND,
-        );
-    }
-
-    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
-    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
-    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
-    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
-    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
-    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
-    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
-    agentSource = agentSourceWithContext;
-
-    const preparedAgentModelRequirements = !hasDynamicContext
-        ? await resolveCachedServerAgentModelRequirements({
-              resolvedAgentContext,
-              localServerUrl,
-              fallbackResolver: baseAgentReferenceResolver,
-          })
-        : null;
-    const agentId = resolvedAgentContext.parentAgentPermanentId;
-    const agentHash = computeAgentHash(agentSource);
-    await waitForHandleChatCompletionAgentPreparation({
-        collection,
-        agentId,
-        agentHash,
-    });
-
-    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
-    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
-        await resolveHandleChatCompletionCredentials({
-            currentUserIdentity,
-            agentId,
-            calendarConnections,
-            useEmailConfiguration,
-        });
-    const agent = await createHandleChatCompletionAgent({
-        agentName,
-        agentSource,
-        agentId,
-        resolvedAgentContext,
-        localServerUrl,
-        baseAgentReferenceResolver,
-        hasDynamicContext,
-        preparedAgentModelRequirements,
-    });
-
-    return {
-        collection,
-        resolvedAgentContext,
-        agent,
-        agentSource,
-        unresolvedAgentSource,
-        messageSuffix,
-        agentId,
-        agentHash,
-        projectRepositories,
-        calendarConnections,
-        useEmailConfiguration,
-        currentUserIdentity,
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-        isPrivateModeEnabled,
-    };
-}
-
-/**
- * Creates prompt and persistence helpers for one chat completion request.
- */
-async function createHandleChatCompletionPromptContext(options: {
-    request: NextRequest;
-    title: string;
-    apiKey: string | null;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-}): Promise<HandleChatCompletionPromptContext> {
-    const { request, title, apiKey, parsedRequest, runtime } = options;
-    const timezone = request.headers.get('x-timezone') || 'UTC';
-    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
-    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
-    const recordChatHistoryMessage = await createChatHistoryRecorder({
-        request,
-        agentIdentifier: runtime.agentId,
-        agentHash: runtime.agentHash,
-        source: 'OPENAI_API_COMPATIBILITY',
-        apiKey,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        isEnabled: !runtime.isPrivateModeEnabled,
-    });
-    const userMessageHash = await recordChatHistoryMessage({
-        message: {
-            role: 'USER',
-            content: lastMessage.content,
-        },
-        previousMessageHash: null,
-    });
-    const promptParameters = composePromptParametersWithMemoryContext({
-        baseParameters: {
-            ...parsedRequest.incomingParameters,
-            timezone,
-        },
-        currentUserIdentity: runtime.currentUserIdentity,
-        agentPermanentId: runtime.agentId,
-        agentName: runtime.resolvedAgentContext.resolvedAgentName,
-        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
-        projectRepositories: runtime.projectRepositories,
-        projectGithubToken: runtime.projectGithubToken,
-        emailSmtpCredential: runtime.emailSmtpCredential,
-        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
-        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
-        calendarConnections: runtime.calendarConnections,
-    });
-    const prompt: ChatPrompt = {
-        title,
-        content: lastMessage.content,
-        modelRequirements: {
-            modelVariant: 'CHAT',
-            responseFormat: parsedRequest.responseFormat,
-            toolChoice: parsedRequest.runtimeToolChoice,
-            // We could pass 'model' from body if we wanted to enforce it, but Agent usually has its own config
-        },
-        parameters: promptParameters,
-        thread,
-        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
-    };
-
-    return {
-        prompt,
-        userMessageHash,
-        recordChatHistoryMessage,
-        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
-            isEnabled: !runtime.isPrivateModeEnabled,
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentId: runtime.agentId,
-            messages: parsedRequest.messages,
-            includeAssistantPlaceholder: true,
-            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
-        }),
-    };
-}
-
-/**
- * Handles the streaming OpenAI-compatible response branch.
- */
-function createHandleChatCompletionStreamResponse(options: {
-    request: NextRequest;
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Response {
-    const { request, title, parsedRequest, runtime, promptContext } = options;
-    const encoder = new TextEncoder();
-    const responseModel = parsedRequest.model || 'promptbook-agent';
-    const readableStream = new ReadableStream({
-        async start(controller) {
-            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
-            const created = Math.floor(Date.now() / 1000);
-            const emitSsePayload = (payload: unknown) => {
-                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
-            };
-            const emitDeltaChunk = (deltaContent: string) => {
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {
-                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
-                        },
-                        finishReason: null,
-                    }),
-                );
-            };
-
-            emitSsePayload(
-                createHandleChatCompletionChunk({
-                    runId,
-                    created,
-                    model: responseModel,
-                    delta: {
-                        role: 'assistant',
-                        content: '',
-                    },
-                    finishReason: null,
-                }),
-            );
-
-            let hasMeaningfulDelta = false;
-
-            try {
-                const handleStreamChunk = createChatStreamHandler({
-                    onDelta: (deltaContent) => {
-                        if (deltaContent.trim().length > 0) {
-                            hasMeaningfulDelta = true;
-                        }
-                        emitDeltaChunk(deltaContent);
-                    },
-                    onToolCalls: (toolCalls) => {
-                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
-                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
-                    },
-                });
-                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
-                    signal: request.signal,
-                });
-                const normalizedResponse = ensureNonEmptyChatContent({
-                    content: result.content,
-                    context: title,
-                });
-
-                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
-                    emitDeltaChunk(normalizedResponse.content);
-                }
-
-                const messageSuffixAppendix = createMessageSuffixAppendix(
-                    normalizedResponse.content,
-                    runtime.messageSuffix,
-                );
-                if (messageSuffixAppendix) {
-                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
-                        emitDeltaChunk(delta);
-                    });
-                }
-
-                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-                await finalizeHandleChatCompletionResult({
-                    responseContentWithSuffix,
-                    usage: result.usage,
-                    toolCalls: result.toolCalls,
-                    parsedRequest,
-                    runtime,
-                    promptContext,
-                    // Preserve historical behavior: streamed OpenAI-compatible calls still persist learning in private mode.
-                    shouldPersistLearnedAgentSource: true,
-                });
-
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {},
-                        finishReason: 'stop',
-                    }),
-                );
-                controller.enqueue(encoder.encode('[DONE]'));
-            } catch (error) {
-                console.error('Error during streaming:', error);
-                controller.error(error);
-            }
-            controller.close();
-        },
-    });
-
-    return new Response(readableStream, {
-        headers: {
-            'Content-Type': 'text/event-stream',
-            'Cache-Control': 'no-cache',
-            Connection: 'keep-alive',
-        },
-    });
-}
-
-/**
- * Handles the non-streaming OpenAI-compatible response branch.
- */
-async function createHandleChatCompletionJsonResponse(options: {
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Promise<NextResponse> {
-    const { title, parsedRequest, runtime, promptContext } = options;
-    const result = await runtime.agent.callChatModel(promptContext.prompt);
-    const normalizedResponse = ensureNonEmptyChatContent({
-        content: result.content,
-        context: title,
-    });
-    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-
-    await finalizeHandleChatCompletionResult({
-        responseContentWithSuffix,
-        usage: result.usage,
-        toolCalls: result.toolCalls,
-        parsedRequest,
-        runtime,
-        promptContext,
-        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
-    });
-
-    return NextResponse.json({
-        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
-        object: 'chat.completion',
-        created: Math.floor(Date.now() / 1000),
-        model: parsedRequest.model || 'promptbook-agent',
-        choices: [
-            {
-                index: 0,
-                message: {
-                    role: 'assistant',
-                    content: responseContentWithSuffix,
-                },
-                finish_reason: 'stop',
-            },
-        ],
-        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
-    });
-}
-
-/**
- * Runs the shared side effects after one successful model response.
- */
-async function finalizeHandleChatCompletionResult(options: {
-    responseContentWithSuffix: string;
-    usage: Usage;
-    toolCalls?: ReadonlyArray<ToolCall>;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
-
-    await promptContext.recordChatHistoryMessage({
-        message: {
-            role: 'MODEL',
-            content: responseContentWithSuffix,
-        },
-        previousMessageHash: promptContext.userMessageHash,
-        usage,
-    });
-
-    if (toolCalls && toolCalls.length > 0) {
-        await logCalendarToolCallsActivity({
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentPermanentId: runtime.agentId,
-            toolCalls,
-        });
-    }
-
-    await persistOpenAiCompatibilityFrozenChat({
-        isEnabled: !runtime.isPrivateModeEnabled,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        agentId: runtime.agentId,
-        chatId: promptContext.persistedFrozenChatId,
-        messages: parsedRequest.messages,
-        assistantContent: responseContentWithSuffix,
-        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
-    });
-    await persistHandleChatCompletionLearnedAgentSource({
-        runtime,
-        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
-    });
-}
-
-/**
- * Applies append-only self-learning updates when the current branch should persist them.
- */
-async function persistHandleChatCompletionLearnedAgentSource(options: {
-    runtime: HandleChatCompletionRuntime;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { runtime, shouldPersistLearnedAgentSource } = options;
-    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
-        return;
-    }
-
-    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
-        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
-        resolvedAgentSourceBeforeLearning: runtime.agentSource,
-        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
-    });
-
-    if (learnedAgentSource !== null) {
-        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
     }
 }
-
-/**
- * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
- */
-async function persistOpenAiCompatibilityFrozenChat(options: {
-    isEnabled: boolean;
-    userId: number | null | undefined;
-    agentId: string;
-    messages: ReadonlyArray<unknown>;
-    chatId?: string;
-    assistantContent?: string;
-    includeAssistantPlaceholder?: boolean;
-    failureMessage: string;
-}): Promise<string | undefined> {
-    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
-        options;
-    if (!isEnabled || !userId) {
-        return undefined;
-    }
-
-    const persistedFrozenChat = await persistFrozenUserChat({
-        userId,
-        agentPermanentId: agentId,
-        source: USER_CHAT_SOURCES.OPENAI_API,
-        chatId,
-        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
-            includeAssistantPlaceholder,
-        }),
-    }).catch((error) => {
-        console.error(failureMessage, error);
-        return null;
-    });
-
-    return persistedFrozenChat?.id;
-}
-
-/**
- * Builds one OpenAI-compatible thread from the earlier request messages.
- */
-function createHandleChatCompletionThread(previousMessages: ReadonlyArray<TODO_any>): ChatMessage[] {
-    return previousMessages.map((message: TODO_any, index: number) => ({
-        id: `msg-${index}`,
-        sender: message.role === 'assistant' ? 'agent' : 'user',
-        content: message.content,
-        isComplete: true,
-        createdAt: $getCurrentDate(),
-    }));
-}
-
-/**
- * Appends system messages to the agent source as dynamic `CONTEXT`.
- */
-function appendSystemMessagesToAgentSource(
-    agentSource: string_book,
-    messages: ReadonlyArray<TODO_any>,
-): {
-    agentSourceWithContext: string_book;
-    hasDynamicContext: boolean;
-} {
-    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
-    if (systemMessages.length === 0) {
-        return {
-            agentSourceWithContext: agentSource,
-            hasDynamicContext: false,
-        };
-    }
-
-    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
-    return {
-        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
-        hasDynamicContext: true,
-    };
-}
-
-/**
- * Waits for any running preparation job of the current agent before executing the chat call.
- */
-async function waitForHandleChatCompletionAgentPreparation(options: {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    agentId: string;
-    agentHash: string;
-}): Promise<void> {
-    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
-    const preparationWaitResult = await waitForRunningAgentPreparation({
-        tablePrefix,
-        agentPermanentId: options.agentId,
-        fingerprint: options.agentHash,
-        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    });
-
-    if (preparationWaitResult !== 'not_running') {
-        console.info('[pre-index]', 'openai_chat_wait_result', {
-            tablePrefix,
-            agentPermanentId: options.agentId,
-            agentHash: options.agentHash,
-            preparationWaitResult,
-        });
-    }
-}
-
-/**
- * Resolves request-scoped project, calendar, and email credentials for the current agent.
- */
-async function resolveHandleChatCompletionCredentials(options: {
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    agentId: string;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-}): Promise<{
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-}> {
-    const userId = options.currentUserIdentity?.userId;
-    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
-        resolveUseProjectGithubToken({
-            userId,
-            agentPermanentId: options.agentId,
-        }),
-        options.calendarConnections.length > 0
-            ? resolveUseCalendarGoogleToken({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-        options.useEmailConfiguration.isEnabled
-            ? resolveUseEmailSmtpCredential({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-    ]);
-
-    return {
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-    };
-}
-
-/**
- * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
- */
-async function createHandleChatCompletionAgent(options: {
-    agentName: string;
-    agentSource: string_book;
-    agentId: string;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    localServerUrl: string;
-    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
-    hasDynamicContext: boolean;
-    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
-}): Promise<Agent> {
-    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
-    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
-    const runtimeAgentReferenceResolver = options.hasDynamicContext
-        ? createBookScopedAgentReferenceResolver({
-              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
-              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
-              localServerUrl: options.localServerUrl,
-              fallbackResolver: options.baseAgentReferenceResolver,
-          })
-        : undefined;
-    const agentModelRequirements =
-        options.preparedAgentModelRequirements?.modelRequirements ||
-        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
-            agentReferenceResolver: runtimeAgentReferenceResolver,
-            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
-        }));
-
-    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
-        options.agentSource,
-        options.resolvedAgentContext.resolvedAgentName,
-        await baseOpenAiToolsPromise,
-        {
-            includeDynamicContext: true,
-            agentId: options.agentId,
-            modelRequirements: agentModelRequirements,
-        },
-    );
-
-    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
-        agentName: options.agentName,
-        assistantCacheKey: agentKitResult.assistantCacheKey,
-        vectorStoreHash: agentKitResult.vectorStoreHash,
-        vectorStoreId: agentKitResult.vectorStoreId,
-    });
-
-    return new Agent({
-        agentSource: options.agentSource,
-        executionTools: {
-            llm: agentKitResult.tools,
-        },
-        precomputedModelRequirements: agentModelRequirements,
-        assistantPreparationMode: 'external',
-        isVerbose: true,
-        teacherAgent: null,
-    });
-}
-
-/**
- * Creates one OpenAI-compatible streaming chunk payload.
- */
-function createHandleChatCompletionChunk(options: {
-    runId: string;
-    created: number;
-    model: TODO_any;
-    delta: Record<string, unknown>;
-    finishReason: 'stop' | null;
-}) {
-    return {
-        id: options.runId,
-        object: 'chat.completion.chunk',
-        created: options.created,
-        model: options.model,
-        choices: [
-            {
-                index: 0,
-                delta: options.delta,
-                finish_reason: options.finishReason,
-            },
-        ],
-    };
-}
-
-/**
- * Creates one consistent OpenAI-compatible error response.
- */
-function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
-    return NextResponse.json(
-        {
-            error: {
-                message,
-                type,
-            },
-        },
-        { status },
-    );
-}
-
-// TODO: [🈹] Maybe move chat thread handling here
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f49a4fa80fa21f140e25de59596e5546d62e3f7
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
@@ -0,0 +1,18 @@
+import { NextResponse } from 'next/server';
+
+/**
+ * Creates one consistent OpenAI-compatible error response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
+    return NextResponse.json(
+        {
+            error: {
+                message,
+                type,
+            },
+        },
+        { status },
+    );
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b1979f4621921bc664fed75a05851c6fcb8fba6d
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
@@ -0,0 +1,120 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import { appendMessageSuffix } from '@/src/utils/chat/messageSuffix';
+import type { UncertainNumber, Usage, UsageCounts } from '@promptbook-local/types';
+import { NextResponse } from 'next/server';
+import { computeUsageCounts } from '../../../../../src/execution/utils/computeUsageCounts';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the non-streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionJsonResponse(options: {
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Promise<NextResponse> {
+    const { title, parsedRequest, runtime, promptContext } = options;
+    const result = await runtime.agent.callChatModel(promptContext.prompt);
+    const normalizedResponse = ensureNonEmptyChatContent({
+        content: result.content,
+        context: title,
+    });
+    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+
+    await finalizeHandleChatCompletionResult({
+        responseContentWithSuffix,
+        usage: result.usage,
+        toolCalls: result.toolCalls,
+        parsedRequest,
+        runtime,
+        promptContext,
+        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
+    });
+
+    return NextResponse.json({
+        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
+        object: 'chat.completion',
+        created: Math.floor(Date.now() / 1000),
+        model: parsedRequest.model || 'promptbook-agent',
+        choices: [
+            {
+                index: 0,
+                message: {
+                    role: 'assistant',
+                    content: responseContentWithSuffix,
+                },
+                finish_reason: 'stop',
+            },
+        ],
+        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
+    });
+}
+
+/**
+ * Falls back to the estimated value when the original token count is unknown.
+ *
+ * @param tokenCount - Token count reported by the execution tools.
+ * @param fallbackValue - Estimated token count based on text length.
+ * @returns Token count to report in the OpenAI-compatible response.
+ */
+function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
+    if (tokenCount.value === 0 && tokenCount.isUncertain) {
+        return {
+            value: fallbackValue,
+            isUncertain: true,
+        };
+    }
+
+    return tokenCount;
+}
+
+/**
+ * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
+ *
+ * @param promptContent - Prompt content used for the request.
+ * @param completionContent - Assistant response content.
+ * @param usage - Native Promptbook usage details.
+ * @returns Usage payload for compatibility responses.
+ */
+function createCompatibilityUsage(
+    promptContent: string,
+    completionContent: string,
+    usage: Usage,
+): {
+    prompt_tokens: number;
+    completion_tokens: number;
+    total_tokens: number;
+    details: Usage;
+} {
+    const promptCounts = computeUsageCounts(promptContent);
+    const completionCounts = computeUsageCounts(completionContent);
+    const inputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
+        ...promptCounts,
+    };
+    const outputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
+        ...completionCounts,
+    };
+
+    const promptTokens = inputUsage.tokensCount.value;
+    const completionTokens = outputUsage.tokensCount.value;
+
+    return {
+        prompt_tokens: promptTokens,
+        completion_tokens: completionTokens,
+        total_tokens: promptTokens + completionTokens,
+        details: {
+            price: usage.price,
+            duration: usage.duration,
+            input: inputUsage,
+            output: outputUsage,
+        },
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..7aac8141a8b1237ce36430b4b4598a618f0383ac
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
@@ -0,0 +1,111 @@
+import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
+import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
+import type { ChatMessage, ChatPrompt } from '@promptbook-local/types';
+import { $getCurrentDate } from '@promptbook-local/utils';
+import type { NextRequest } from 'next/server';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Prompt, history recorder, and persistence context created for one chat call.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionPromptContext = {
+    prompt: ChatPrompt;
+    userMessageHash: string;
+    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
+    persistedFrozenChatId?: string;
+};
+
+/**
+ * Creates prompt and persistence helpers for one chat completion request.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionPromptContext(options: {
+    request: NextRequest;
+    title: string;
+    apiKey: string | null;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+}): Promise<HandleChatCompletionPromptContext> {
+    const { request, title, apiKey, parsedRequest, runtime } = options;
+    const timezone = request.headers.get('x-timezone') || 'UTC';
+    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
+    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
+    const recordChatHistoryMessage = await createChatHistoryRecorder({
+        request,
+        agentIdentifier: runtime.agentId,
+        agentHash: runtime.agentHash,
+        source: 'OPENAI_API_COMPATIBILITY',
+        apiKey,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        isEnabled: !runtime.isPrivateModeEnabled,
+    });
+    const userMessageHash = await recordChatHistoryMessage({
+        message: {
+            role: 'USER',
+            content: lastMessage.content,
+        },
+        previousMessageHash: null,
+    });
+    const promptParameters = composePromptParametersWithMemoryContext({
+        baseParameters: {
+            ...parsedRequest.incomingParameters,
+            timezone,
+        },
+        currentUserIdentity: runtime.currentUserIdentity,
+        agentPermanentId: runtime.agentId,
+        agentName: runtime.resolvedAgentContext.resolvedAgentName,
+        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
+        projectRepositories: runtime.projectRepositories,
+        projectGithubToken: runtime.projectGithubToken,
+        emailSmtpCredential: runtime.emailSmtpCredential,
+        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
+        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
+        calendarConnections: runtime.calendarConnections,
+    });
+    const prompt: ChatPrompt = {
+        title,
+        content: lastMessage.content,
+        modelRequirements: {
+            modelVariant: 'CHAT',
+            responseFormat: parsedRequest.responseFormat,
+            toolChoice: parsedRequest.runtimeToolChoice,
+        },
+        parameters: promptParameters,
+        thread,
+        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
+    };
+
+    return {
+        prompt,
+        userMessageHash,
+        recordChatHistoryMessage,
+        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
+            isEnabled: !runtime.isPrivateModeEnabled,
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentId: runtime.agentId,
+            messages: parsedRequest.messages,
+            includeAssistantPlaceholder: true,
+            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
+        }),
+    };
+}
+
+/**
+ * Builds one OpenAI-compatible thread from the earlier request messages.
+ */
+function createHandleChatCompletionThread(
+    previousMessages: HandleChatCompletionParsedRequest['threadMessages'],
+): Array<ChatMessage> {
+    return previousMessages.map((message, index) => ({
+        id: `msg-${index}`,
+        sender: message.role === 'assistant' ? 'agent' : 'user',
+        content: message.content,
+        isComplete: true,
+        createdAt: $getCurrentDate(),
+    }));
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..85e096027760bc45fb730e74f8ae3bbb721b1029
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
@@ -0,0 +1,162 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import {
+    appendMessageSuffix,
+    createMessageSuffixAppendix,
+    emulateMessageSuffixStreaming,
+} from '@/src/utils/chat/messageSuffix';
+import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
+import { encodeChatStreamWhitespaceForTransport } from '../../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
+import { prepareToolCallsForStreaming } from '../toolCallStreaming';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionStreamResponse(options: {
+    request: Request;
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Response {
+    const { request, title, parsedRequest, runtime, promptContext } = options;
+    const encoder = new TextEncoder();
+    const responseModel = parsedRequest.model || 'promptbook-agent';
+    const readableStream = new ReadableStream({
+        async start(controller) {
+            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
+            const created = Math.floor(Date.now() / 1000);
+            const emitSsePayload = (payload: unknown) => {
+                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
+            };
+            const emitDeltaChunk = (deltaContent: string) => {
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {
+                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
+                        },
+                        finishReason: null,
+                    }),
+                );
+            };
+
+            emitSsePayload(
+                createHandleChatCompletionChunk({
+                    runId,
+                    created,
+                    model: responseModel,
+                    delta: {
+                        role: 'assistant',
+                        content: '',
+                    },
+                    finishReason: null,
+                }),
+            );
+
+            let hasMeaningfulDelta = false;
+
+            try {
+                const handleStreamChunk = createChatStreamHandler({
+                    onDelta: (deltaContent) => {
+                        if (deltaContent.trim().length > 0) {
+                            hasMeaningfulDelta = true;
+                        }
+                        emitDeltaChunk(deltaContent);
+                    },
+                    onToolCalls: (toolCalls) => {
+                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
+                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
+                    },
+                });
+                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
+                    signal: request.signal,
+                });
+                const normalizedResponse = ensureNonEmptyChatContent({
+                    content: result.content,
+                    context: title,
+                });
+
+                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
+                    emitDeltaChunk(normalizedResponse.content);
+                }
+
+                const messageSuffixAppendix = createMessageSuffixAppendix(
+                    normalizedResponse.content,
+                    runtime.messageSuffix,
+                );
+                if (messageSuffixAppendix) {
+                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
+                        emitDeltaChunk(delta);
+                    });
+                }
+
+                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+                await finalizeHandleChatCompletionResult({
+                    responseContentWithSuffix,
+                    usage: result.usage,
+                    toolCalls: result.toolCalls,
+                    parsedRequest,
+                    runtime,
+                    promptContext,
+                    shouldPersistLearnedAgentSource: true,
+                });
+
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {},
+                        finishReason: 'stop',
+                    }),
+                );
+                controller.enqueue(encoder.encode('[DONE]'));
+            } catch (error) {
+                console.error('Error during streaming:', error);
+                controller.error(error);
+            }
+            controller.close();
+        },
+    });
+
+    return new Response(readableStream, {
+        headers: {
+            'Content-Type': 'text/event-stream',
+            'Cache-Control': 'no-cache',
+            Connection: 'keep-alive',
+        },
+    });
+}
+
+/**
+ * Creates one OpenAI-compatible streaming chunk payload.
+ */
+function createHandleChatCompletionChunk(options: {
+    runId: string;
+    created: number;
+    model: HandleChatCompletionParsedRequest['model'];
+    delta: Record<string, unknown>;
+    finishReason: 'stop' | null;
+}) {
+    return {
+        id: options.runId,
+        object: 'chat.completion.chunk',
+        created: options.created,
+        model: options.model,
+        choices: [
+            {
+                index: 0,
+                delta: options.delta,
+                finish_reason: options.finishReason,
+            },
+        ],
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
new file mode 100644
index 0000000000000000000000000000000000000000..dd6a7d87ea491df245dbf93d8bcb277a9a10b034
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
@@ -0,0 +1,78 @@
+import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
+import { resolveAppendOnlySelfLearningAgentSource } from '@/src/utils/resolveAppendOnlySelfLearningAgentSource';
+import type { ToolCall, Usage } from '@promptbook-local/types';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Runs the shared side effects after one successful model response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function finalizeHandleChatCompletionResult(options: {
+    responseContentWithSuffix: string;
+    usage: Usage;
+    toolCalls?: ReadonlyArray<ToolCall>;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
+
+    await promptContext.recordChatHistoryMessage({
+        message: {
+            role: 'MODEL',
+            content: responseContentWithSuffix,
+        },
+        previousMessageHash: promptContext.userMessageHash,
+        usage,
+    });
+
+    if (toolCalls && toolCalls.length > 0) {
+        await logCalendarToolCallsActivity({
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentPermanentId: runtime.agentId,
+            toolCalls,
+        });
+    }
+
+    await persistOpenAiCompatibilityFrozenChat({
+        isEnabled: !runtime.isPrivateModeEnabled,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        agentId: runtime.agentId,
+        chatId: promptContext.persistedFrozenChatId,
+        messages: parsedRequest.messages,
+        assistantContent: responseContentWithSuffix,
+        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
+    });
+    await persistHandleChatCompletionLearnedAgentSource({
+        runtime,
+        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
+    });
+}
+
+/**
+ * Applies append-only self-learning updates when the current branch should persist them.
+ */
+async function persistHandleChatCompletionLearnedAgentSource(options: {
+    runtime: HandleChatCompletionRuntime;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { runtime, shouldPersistLearnedAgentSource } = options;
+    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
+        return;
+    }
+
+    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
+        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
+        resolvedAgentSourceBeforeLearning: runtime.agentSource,
+        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
+    });
+
+    if (learnedAgentSource !== null) {
+        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
+    }
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
new file mode 100644
index 0000000000000000000000000000000000000000..58e1c26d7f428c1bdb39b36411b3f75256e3de51
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
@@ -0,0 +1,176 @@
+import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
+import type { LlmToolDefinition, TODO_any } from '@promptbook-local/types';
+import { NextRequest, NextResponse } from 'next/server';
+import type OpenAI from 'openai';
+import { HTTP_STATUS_CODES } from '../../constants';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Definition of open AI chat tool.
+ */
+type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
+
+/**
+ * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionParsedRequest = {
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    threadMessages: ReadonlyArray<TODO_any>;
+    stream: TODO_any;
+    model: TODO_any;
+    responseFormat: TODO_any;
+    runtimeTools?: Array<LlmToolDefinition>;
+    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+    incomingParameters: Record<string, unknown>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Parses and validates the OpenAI-compatible request payload.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function parseHandleChatCompletionRequest(options: {
+    body: TODO_any;
+    agentNameFromParams?: string;
+    request: NextRequest;
+}): HandleChatCompletionParsedRequest | NextResponse {
+    const { body, agentNameFromParams, request } = options;
+    const {
+        messages,
+        stream,
+        model,
+        response_format: responseFormat,
+        tools: rawTools,
+        tool_choice: toolChoice,
+        parameters: rawParameters = {},
+    } = body;
+    const agentName = (agentNameFromParams || model) as string | undefined;
+    if (!agentName) {
+        return createHandleChatCompletionErrorResponse(
+            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    if (!messages || !Array.isArray(messages) || messages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array is required and cannot be empty.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
+    if (threadMessages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array must contain at least one non-system message.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    return {
+        agentName,
+        messages,
+        threadMessages,
+        stream,
+        model,
+        responseFormat,
+        runtimeTools: convertOpenAiTools(rawTools),
+        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
+        incomingParameters:
+            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
+                ? (rawParameters as Record<string, unknown>)
+                : {},
+        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
+    };
+}
+
+/**
+ * Handles convert open Ai tools.
+ */
+function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
+    if (!Array.isArray(rawTools)) {
+        return undefined;
+    }
+
+    const converted = rawTools
+        .map((tool) => convertOpenAiTool(tool))
+        .filter((tool): tool is LlmToolDefinition => tool !== null);
+
+    return converted.length > 0 ? converted : undefined;
+}
+
+/**
+ * Handles convert open Ai tool.
+ */
+function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
+    const tool = rawTool as OpenAIChatToolDefinition;
+    if (!tool || tool.type !== 'function') {
+        return null;
+    }
+
+    const functionDefinition = tool.function;
+    if (!functionDefinition) {
+        return null;
+    }
+
+    const { name, description, parameters } = functionDefinition;
+    if (typeof name !== 'string' || name.trim().length === 0) {
+        return null;
+    }
+
+    const parameterSchema = parameters ?? {};
+    if (parameterSchema.type !== 'object') {
+        return null;
+    }
+
+    const properties = parameterSchema.properties ?? {};
+    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
+
+    for (const [key, value] of Object.entries(properties)) {
+        if (!value || typeof value !== 'object') {
+            continue;
+        }
+
+        normalizedProperties[key] = {
+            type: typeof value.type === 'string' ? value.type : 'string',
+            description: typeof value.description === 'string' ? value.description : undefined,
+        };
+    }
+
+    const required =
+        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
+            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
+            : undefined;
+
+    return {
+        name,
+        description: typeof description === 'string' ? description : '',
+        parameters: {
+            type: 'object',
+            properties: normalizedProperties,
+            required,
+            additionalProperties:
+                typeof parameterSchema.additionalProperties === 'boolean'
+                    ? parameterSchema.additionalProperties
+                    : undefined,
+        },
+    };
+}
+
+/**
+ * Parses open Ai tool choice.
+ */
+function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
+    if (value === undefined || value === null) {
+        return undefined;
+    }
+
+    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
new file mode 100644
index 0000000000000000000000000000000000000000..60e5f1b008bcf5a6a1836c06c4c3d9ebcb909a9f
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
@@ -0,0 +1,163 @@
+import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
+import type { ChatMessage } from '@promptbook-local/types';
+
+/**
+ * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
+ */
+type OpenAiCompatibilityMessageLike = {
+    role?: unknown;
+    content?: unknown;
+};
+
+/**
+ * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function persistOpenAiCompatibilityFrozenChat(options: {
+    isEnabled: boolean;
+    userId: number | null | undefined;
+    agentId: string;
+    messages: ReadonlyArray<unknown>;
+    chatId?: string;
+    assistantContent?: string;
+    includeAssistantPlaceholder?: boolean;
+    failureMessage: string;
+}): Promise<string | undefined> {
+    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
+        options;
+    if (!isEnabled || !userId) {
+        return undefined;
+    }
+
+    const persistedFrozenChat = await persistFrozenUserChat({
+        userId,
+        agentPermanentId: agentId,
+        source: USER_CHAT_SOURCES.OPENAI_API,
+        chatId,
+        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
+            includeAssistantPlaceholder,
+        }),
+    }).catch((error) => {
+        console.error(failureMessage, error);
+        return null;
+    });
+
+    return persistedFrozenChat?.id;
+}
+
+/**
+ * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
+ */
+function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
+    if (typeof value === 'string') {
+        return value;
+    }
+
+    if (Array.isArray(value)) {
+        return value
+            .map((part) => {
+                if (typeof part === 'string') {
+                    return part;
+                }
+
+                if (!part || typeof part !== 'object') {
+                    return '';
+                }
+
+                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
+                if (typeof partLike.text === 'string') {
+                    return partLike.text;
+                }
+
+                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
+                    return '[Image input]';
+                }
+
+                if (partLike.image_url) {
+                    return '[Image input]';
+                }
+
+                return '';
+            })
+            .filter(Boolean)
+            .join('\n');
+    }
+
+    if (value === null || value === undefined) {
+        return '';
+    }
+
+    try {
+        return JSON.stringify(value, null, 2);
+    } catch {
+        return String(value);
+    }
+}
+
+/**
+ * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ */
+function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
+    if (typeof role !== 'string') {
+        return 'USER';
+    }
+
+    switch (role) {
+        case 'assistant':
+            return 'AGENT';
+        case 'system':
+            return 'SYSTEM';
+        case 'tool':
+            return 'TOOL';
+        case 'developer':
+            return 'SYSTEM';
+        default:
+            return 'USER';
+    }
+}
+
+/**
+ * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
+ */
+function createFrozenOpenAiCompatibilityMessages(
+    rawMessages: ReadonlyArray<unknown>,
+    assistantContent?: string,
+    options: {
+        includeAssistantPlaceholder?: boolean;
+    } = {},
+): Array<ChatMessage> {
+    const startedAt = Date.now();
+    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
+        const message = rawMessage as OpenAiCompatibilityMessageLike;
+
+        return {
+            id: `openai-frozen-${index}`,
+            sender: mapOpenAiCompatibilityRoleToSender(message.role),
+            content: normalizeOpenAiCompatibilityMessageContent(message.content),
+            isComplete: true,
+            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage;
+    });
+
+    if (assistantContent !== undefined) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: assistantContent,
+            isComplete: true,
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    } else if (options.includeAssistantPlaceholder) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: '',
+            isComplete: false,
+            lifecycleState: 'running',
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    }
+
+    return messages;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fd44b7ea5a87168725a9e99784038137c96537ab
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
@@ -0,0 +1,305 @@
+import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
+import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
+import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
+import type { string_book, TODO_any } from '@promptbook-local/types';
+import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
+import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
+import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
+import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
+import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
+import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
+import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
+import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
+import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
+import {
+    createBookScopedAgentReferenceResolver,
+    parseBookScopedAgentIdentifier,
+} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
+import { resolveMessageSuffixFromAgentSource } from '@/src/utils/chat/messageSuffix';
+import { NextRequest, NextResponse } from 'next/server';
+import { isAgentDeleted } from '../../app/agents/[agentName]/_utils';
+import {
+    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    resolveAgentCollectionTablePrefix,
+    waitForRunningAgentPreparation,
+} from '../agentPreparation';
+import { resolveCachedServerAgentContext, resolveCachedServerAgentModelRequirements } from '../cachedServerAgentRuntime';
+import { AgentKitCacheManager } from '../cache/AgentKitCacheManager';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Runtime state resolved before the chat model is executed.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionRuntime = {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    agent: Agent;
+    agentSource: string_book;
+    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
+    messageSuffix: string | null;
+    agentId: string;
+    agentHash: string;
+    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Resolves agent/runtime dependencies needed before prompt execution.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function resolveHandleChatCompletionRuntime(options: {
+    request: NextRequest;
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    isPrivateModeEnabled: boolean;
+}): Promise<HandleChatCompletionRuntime | NextResponse> {
+    const { request, agentName, messages, isPrivateModeEnabled } = options;
+    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
+    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
+
+    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
+        return createHandleChatCompletionErrorResponse(
+            'This agent has been deleted. You can restore it from the Recycle Bin.',
+            'agent_deleted',
+            410,
+        );
+    }
+
+    const [collection, baseAgentReferenceResolver] = await Promise.all([
+        $provideAgentCollectionForServer(),
+        $provideAgentReferenceResolver(),
+    ]);
+    const localServerUrl = new URL(request.url).origin;
+    const resolvedAgentContext = await resolveCachedServerAgentContext({
+        collection,
+        agentIdentifier: agentName,
+        localServerUrl,
+        fallbackResolver: baseAgentReferenceResolver,
+    }).catch(() => null);
+
+    if (!resolvedAgentContext?.resolvedAgentSource) {
+        return createHandleChatCompletionErrorResponse(`Agent '${agentName}' not found.`, 'invalid_request_error', 404);
+    }
+
+    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
+    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
+    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
+    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
+    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
+    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
+    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
+    agentSource = agentSourceWithContext;
+
+    const preparedAgentModelRequirements = !hasDynamicContext
+        ? await resolveCachedServerAgentModelRequirements({
+              resolvedAgentContext,
+              localServerUrl,
+              fallbackResolver: baseAgentReferenceResolver,
+          })
+        : null;
+    const agentId = resolvedAgentContext.parentAgentPermanentId;
+    const agentHash = computeAgentHash(agentSource);
+    await waitForHandleChatCompletionAgentPreparation({
+        collection,
+        agentId,
+        agentHash,
+    });
+
+    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
+    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
+        await resolveHandleChatCompletionCredentials({
+            currentUserIdentity,
+            agentId,
+            calendarConnections,
+            useEmailConfiguration,
+        });
+    const agent = await createHandleChatCompletionAgent({
+        agentName,
+        agentSource,
+        agentId,
+        resolvedAgentContext,
+        localServerUrl,
+        baseAgentReferenceResolver,
+        hasDynamicContext,
+        preparedAgentModelRequirements,
+    });
+
+    return {
+        collection,
+        resolvedAgentContext,
+        agent,
+        agentSource,
+        unresolvedAgentSource,
+        messageSuffix,
+        agentId,
+        agentHash,
+        projectRepositories,
+        calendarConnections,
+        useEmailConfiguration,
+        currentUserIdentity,
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+        isPrivateModeEnabled,
+    };
+}
+
+/**
+ * Appends system messages to the agent source as dynamic `CONTEXT`.
+ */
+function appendSystemMessagesToAgentSource(
+    agentSource: string_book,
+    messages: ReadonlyArray<TODO_any>,
+): {
+    agentSourceWithContext: string_book;
+    hasDynamicContext: boolean;
+} {
+    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
+    if (systemMessages.length === 0) {
+        return {
+            agentSourceWithContext: agentSource,
+            hasDynamicContext: false,
+        };
+    }
+
+    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
+    return {
+        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
+        hasDynamicContext: true,
+    };
+}
+
+/**
+ * Waits for any running preparation job of the current agent before executing the chat call.
+ */
+async function waitForHandleChatCompletionAgentPreparation(options: {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    agentId: string;
+    agentHash: string;
+}): Promise<void> {
+    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
+    const preparationWaitResult = await waitForRunningAgentPreparation({
+        tablePrefix,
+        agentPermanentId: options.agentId,
+        fingerprint: options.agentHash,
+        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    });
+
+    if (preparationWaitResult !== 'not_running') {
+        console.info('[pre-index]', 'openai_chat_wait_result', {
+            tablePrefix,
+            agentPermanentId: options.agentId,
+            agentHash: options.agentHash,
+            preparationWaitResult,
+        });
+    }
+}
+
+/**
+ * Resolves request-scoped project, calendar, and email credentials for the current agent.
+ */
+async function resolveHandleChatCompletionCredentials(options: {
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    agentId: string;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+}): Promise<{
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+}> {
+    const userId = options.currentUserIdentity?.userId;
+    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
+        resolveUseProjectGithubToken({
+            userId,
+            agentPermanentId: options.agentId,
+        }),
+        options.calendarConnections.length > 0
+            ? resolveUseCalendarGoogleToken({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+        options.useEmailConfiguration.isEnabled
+            ? resolveUseEmailSmtpCredential({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+    ]);
+
+    return {
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+    };
+}
+
+/**
+ * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
+ */
+async function createHandleChatCompletionAgent(options: {
+    agentName: string;
+    agentSource: string_book;
+    agentId: string;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    localServerUrl: string;
+    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
+    hasDynamicContext: boolean;
+    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
+}): Promise<Agent> {
+    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
+    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
+    const runtimeAgentReferenceResolver = options.hasDynamicContext
+        ? createBookScopedAgentReferenceResolver({
+              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
+              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
+              localServerUrl: options.localServerUrl,
+              fallbackResolver: options.baseAgentReferenceResolver,
+          })
+        : undefined;
+    const agentModelRequirements =
+        options.preparedAgentModelRequirements?.modelRequirements ||
+        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
+            agentReferenceResolver: runtimeAgentReferenceResolver,
+            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
+        }));
+
+    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
+        options.agentSource,
+        options.resolvedAgentContext.resolvedAgentName,
+        await baseOpenAiToolsPromise,
+        {
+            includeDynamicContext: true,
+            agentId: options.agentId,
+            modelRequirements: agentModelRequirements,
+        },
+    );
+
+    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
+        agentName: options.agentName,
+        assistantCacheKey: agentKitResult.assistantCacheKey,
+        vectorStoreHash: agentKitResult.vectorStoreHash,
+        vectorStoreId: agentKitResult.vectorStoreId,
+    });
+
+    return new Agent({
+        agentSource: options.agentSource,
+        executionTools: {
+            llm: agentKitResult.tools,
+        },
+        precomputedModelRequirements: agentModelRequirements,
+        assistantPreparationMode: 'external',
+        isVerbose: true,
+        teacherAgent: null,
+    });
+}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 474fd839d14b2bdab324e5f59fce0b15ddf95afd..bc9cfde73332af5d107ad35780a31963bc4e68e9
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,8 @@
+-   Refactored Agents Server `handleChatCompletion.ts` into a thinner chat-completion facade plus focused private helper modules without changing external behavior:
+
+    -   Extracted OpenAI-compatible request parsing, runtime/agent resolution, prompt-context creation, frozen-chat persistence, shared post-response side effects, and the streaming vs non-streaming response branches into dedicated private files behind the existing `handleChatCompletion` entrypoint.
+    -   Kept the existing API-key validation, deleted-agent handling, dynamic system-context injection, AgentKit cache usage, private-mode behavior, frozen audit snapshots, self-learning persistence, streaming chunk format, and OpenAI-compatible JSON response shape intact while reducing responsibility density in the main file.
+
 -   Refactored Agents Server `errorMessages.ts` into smaller focused helpers without changing external behavior:
 
     -   Split the branching-heavy `categorizeError` flow into dedicated API-type, native-error-message, and status-code classification helpers while preserving the existing precedence.

file update:
diff --git a/apps/agents-server/src/utils/handleChatCompletion.ts b/apps/agents-server/src/utils/handleChatCompletion.ts
index 9c96d36fad9cf24bf26d89f57372332add4e4595..cff56fafd6cfa90bb4094f9f202c4f01f6f74af1
--- a/apps/agents-server/src/utils/handleChatCompletion.ts
+++ b/apps/agents-server/src/utils/handleChatCompletion.ts
@@ -1,392 +1,16 @@
-import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
-import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
-import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
-import {
-    createBookScopedAgentReferenceResolver,
-    parseBookScopedAgentIdentifier,
-} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
-import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
-import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
-import {
-    appendMessageSuffix,
-    createMessageSuffixAppendix,
-    emulateMessageSuffixStreaming,
-    resolveMessageSuffixFromAgentSource,
-} from '@/src/utils/chat/messageSuffix';
-import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
-import { prepareToolCallsForStreaming } from './toolCallStreaming';
-import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
-import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
-import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
-import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
-import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
-import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
-import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
-import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
-import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
-import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
-import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
-import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
-import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
-import type {
-    ChatMessage,
-    ChatPrompt,
-    LlmToolDefinition,
-    string_book,
-    ToolCall,
-    TODO_any,
-    UncertainNumber,
-    Usage,
-    UsageCounts,
-} from '@promptbook-local/types';
-import { $getCurrentDate } from '@promptbook-local/utils';
 import { NextRequest, NextResponse } from 'next/server';
-import type OpenAI from 'openai';
-import { computeUsageCounts } from '../../../../src/execution/utils/computeUsageCounts';
-import { encodeChatStreamWhitespaceForTransport } from '../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
-import { isAgentDeleted } from '../app/agents/[agentName]/_utils';
 import { HTTP_STATUS_CODES } from '../constants';
-import { AgentKitCacheManager } from './cache/AgentKitCacheManager';
 import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
+import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
+import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
+import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
+import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
+import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
 import { validateApiKey } from './validateApiKey';
-import {
-    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    resolveAgentCollectionTablePrefix,
-    waitForRunningAgentPreparation,
-} from './agentPreparation';
-import {
-    resolveCachedServerAgentContext,
-    resolveCachedServerAgentModelRequirements,
-} from './cachedServerAgentRuntime';
-import { resolveAppendOnlySelfLearningAgentSource } from './resolveAppendOnlySelfLearningAgentSource';
 
 /**
- * Falls back to the estimated value when the original token count is unknown.
- *
- * @param tokenCount - Token count reported by the execution tools.
- * @param fallbackValue - Estimated token count based on text length.
- * @returns Token count to report in the OpenAI-compatible response.
- */
-function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
-    if (tokenCount.value === 0 && tokenCount.isUncertain) {
-        return {
-            value: fallbackValue,
-            isUncertain: true,
-        };
-    }
-
-    return tokenCount;
-}
-
-/**
- * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
- *
- * @param promptContent - Prompt content used for the request.
- * @param completionContent - Assistant response content.
- * @param usage - Native Promptbook usage details.
- * @returns Usage payload for compatibility responses.
- */
-function createCompatibilityUsage(
-    promptContent: string,
-    completionContent: string,
-    usage: Usage,
-): {
-    prompt_tokens: number;
-    completion_tokens: number;
-    total_tokens: number;
-    details: Usage;
-} {
-    const promptCounts = computeUsageCounts(promptContent);
-    const completionCounts = computeUsageCounts(completionContent);
-    const inputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
-        ...promptCounts,
-    };
-    const outputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
-        ...completionCounts,
-    };
-
-    const promptTokens = inputUsage.tokensCount.value;
-    const completionTokens = outputUsage.tokensCount.value;
-
-    return {
-        prompt_tokens: promptTokens,
-        completion_tokens: completionTokens,
-        total_tokens: promptTokens + completionTokens,
-        details: {
-            price: usage.price,
-            duration: usage.duration,
-            input: inputUsage,
-            output: outputUsage,
-        },
-    };
-}
-
-/**
- * Definition of open AI chat tool.
- */
-type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
-
-/**
- * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
- */
-type OpenAiCompatibilityMessageLike = {
-    role?: unknown;
-    content?: unknown;
-};
-
-/**
- * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
- */
-type HandleChatCompletionParsedRequest = {
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    threadMessages: ReadonlyArray<TODO_any>;
-    stream: TODO_any;
-    model: TODO_any;
-    responseFormat: TODO_any;
-    runtimeTools?: Array<LlmToolDefinition>;
-    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-    incomingParameters: Record<string, unknown>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Runtime state resolved before the chat model is executed.
- */
-type HandleChatCompletionRuntime = {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    agent: Agent;
-    agentSource: string_book;
-    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
-    messageSuffix: string | null;
-    agentId: string;
-    agentHash: string;
-    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Prompt, history recorder, and persistence context created for one chat call.
- */
-type HandleChatCompletionPromptContext = {
-    prompt: ChatPrompt;
-    userMessageHash: string;
-    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
-    persistedFrozenChatId?: string;
-};
-
-/**
- * Handles convert open Ai tools.
- */
-function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
-    if (!Array.isArray(rawTools)) {
-        return undefined;
-    }
-
-    const converted = rawTools
-        .map((tool) => convertOpenAiTool(tool))
-        .filter((tool): tool is LlmToolDefinition => tool !== null);
-
-    return converted.length > 0 ? converted : undefined;
-}
-
-/**
- * Handles convert open Ai tool.
- */
-function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
-    const tool = rawTool as OpenAIChatToolDefinition;
-    if (!tool || tool.type !== 'function') {
-        return null;
-    }
-
-    const functionDefinition = tool.function;
-    if (!functionDefinition) {
-        return null;
-    }
-
-    const { name, description, parameters } = functionDefinition;
-    if (typeof name !== 'string' || name.trim().length === 0) {
-        return null;
-    }
-
-    const parameterSchema = parameters ?? {};
-    if (parameterSchema.type !== 'object') {
-        return null;
-    }
-
-    const properties = parameterSchema.properties ?? {};
-    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
-
-    for (const [key, value] of Object.entries(properties)) {
-        if (!value || typeof value !== 'object') {
-            continue;
-        }
-
-        normalizedProperties[key] = {
-            type: typeof value.type === 'string' ? value.type : 'string',
-            description: typeof value.description === 'string' ? value.description : undefined,
-        };
-    }
-
-    const required =
-        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
-            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
-            : undefined;
-
-    return {
-        name,
-        description: typeof description === 'string' ? description : '',
-        parameters: {
-            type: 'object',
-            properties: normalizedProperties,
-            required,
-            additionalProperties:
-                typeof parameterSchema.additionalProperties === 'boolean'
-                    ? parameterSchema.additionalProperties
-                    : undefined,
-        },
-    };
-}
-
-/**
- * Parses open Ai tool choice.
- */
-function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
-    if (value === undefined || value === null) {
-        return undefined;
-    }
-
-    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-}
-
-/**
- * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
- */
-function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
-    if (typeof value === 'string') {
-        return value;
-    }
-
-    if (Array.isArray(value)) {
-        return value
-            .map((part) => {
-                if (typeof part === 'string') {
-                    return part;
-                }
-
-                if (!part || typeof part !== 'object') {
-                    return '';
-                }
-
-                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
-                if (typeof partLike.text === 'string') {
-                    return partLike.text;
-                }
-
-                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
-                    return '[Image input]';
-                }
-
-                if (partLike.image_url) {
-                    return '[Image input]';
-                }
-
-                return '';
-            })
-            .filter(Boolean)
-            .join('\n');
-    }
-
-    if (value === null || value === undefined) {
-        return '';
-    }
-
-    try {
-        return JSON.stringify(value, null, 2);
-    } catch {
-        return String(value);
-    }
-}
-
-/**
- * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ * Handles convert open Ai chat completion requests routed through the Agents Server.
  */
-function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
-    if (typeof role !== 'string') {
-        return 'USER';
-    }
-
-    switch (role) {
-        case 'assistant':
-            return 'AGENT';
-        case 'system':
-            return 'SYSTEM';
-        case 'tool':
-            return 'TOOL';
-        case 'developer':
-            return 'SYSTEM';
-        default:
-            return 'USER';
-    }
-}
-
-/**
- * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
- */
-function createFrozenOpenAiCompatibilityMessages(
-    rawMessages: ReadonlyArray<unknown>,
-    assistantContent?: string,
-    options: {
-        includeAssistantPlaceholder?: boolean;
-    } = {},
-): Array<ChatMessage> {
-    const startedAt = Date.now();
-    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
-        const message = rawMessage as OpenAiCompatibilityMessageLike;
-
-        return {
-            id: `openai-frozen-${index}`,
-            sender: mapOpenAiCompatibilityRoleToSender(message.role),
-            content: normalizeOpenAiCompatibilityMessageContent(message.content),
-            isComplete: true,
-            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage;
-    });
-
-    if (assistantContent !== undefined) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: assistantContent,
-            isComplete: true,
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    } else if (options.includeAssistantPlaceholder) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: '',
-            isComplete: false,
-            lifecycleState: 'running',
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    }
-
-    return messages;
-}
-
-/**
- * Handles chat completion.
- */
 export async function handleChatCompletion(
     request: NextRequest,
     params: { agentName?: string },
@@ -399,7 +23,6 @@
         return versionMismatchResponse;
     }
 
-    // Validate API key explicitly (in addition to middleware)
     const apiKeyValidation = await validateApiKey(request);
     if (!apiKeyValidation.isValid) {
         return NextResponse.json(
@@ -462,724 +85,5 @@
             { error: { message: (error as Error).message || 'Internal Server Error', type: 'server_error' } },
             { status: HTTP_STATUS_CODES.INTERNAL_SERVER_ERROR },
         );
-    }
-}
-
-/**
- * Parses and validates the OpenAI-compatible request payload.
- */
-function parseHandleChatCompletionRequest(options: {
-    body: TODO_any;
-    agentNameFromParams?: string;
-    request: NextRequest;
-}): HandleChatCompletionParsedRequest | NextResponse {
-    const { body, agentNameFromParams, request } = options;
-    const {
-        messages,
-        stream,
-        model,
-        response_format: responseFormat,
-        tools: rawTools,
-        tool_choice: toolChoice,
-        parameters: rawParameters = {},
-    } = body;
-    const agentName = (agentNameFromParams || model) as string | undefined;
-    if (!agentName) {
-        return createHandleChatCompletionErrorResponse(
-            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    if (!messages || !Array.isArray(messages) || messages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array is required and cannot be empty.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
-    if (threadMessages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array must contain at least one non-system message.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    return {
-        agentName,
-        messages,
-        threadMessages,
-        stream,
-        model,
-        responseFormat,
-        runtimeTools: convertOpenAiTools(rawTools),
-        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
-        incomingParameters:
-            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
-                ? (rawParameters as Record<string, unknown>)
-                : {},
-        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
-    };
-}
-
-/**
- * Resolves agent/runtime dependencies needed before prompt execution.
- */
-async function resolveHandleChatCompletionRuntime(options: {
-    request: NextRequest;
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    isPrivateModeEnabled: boolean;
-}): Promise<HandleChatCompletionRuntime | NextResponse> {
-    const { request, agentName, messages, isPrivateModeEnabled } = options;
-    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
-    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
-
-    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
-        return createHandleChatCompletionErrorResponse(
-            'This agent has been deleted. You can restore it from the Recycle Bin.',
-            'agent_deleted',
-            410,
-        );
-    }
-
-    const [collection, baseAgentReferenceResolver] = await Promise.all([
-        $provideAgentCollectionForServer(),
-        $provideAgentReferenceResolver(),
-    ]);
-    const localServerUrl = new URL(request.url).origin;
-    const resolvedAgentContext = await resolveCachedServerAgentContext({
-        collection,
-        agentIdentifier: agentName,
-        localServerUrl,
-        fallbackResolver: baseAgentReferenceResolver,
-    }).catch(() => null);
-
-    if (!resolvedAgentContext?.resolvedAgentSource) {
-        return createHandleChatCompletionErrorResponse(
-            `Agent '${agentName}' not found.`,
-            'invalid_request_error',
-            HTTP_STATUS_CODES.NOT_FOUND,
-        );
-    }
-
-    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
-    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
-    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
-    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
-    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
-    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
-    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
-    agentSource = agentSourceWithContext;
-
-    const preparedAgentModelRequirements = !hasDynamicContext
-        ? await resolveCachedServerAgentModelRequirements({
-              resolvedAgentContext,
-              localServerUrl,
-              fallbackResolver: baseAgentReferenceResolver,
-          })
-        : null;
-    const agentId = resolvedAgentContext.parentAgentPermanentId;
-    const agentHash = computeAgentHash(agentSource);
-    await waitForHandleChatCompletionAgentPreparation({
-        collection,
-        agentId,
-        agentHash,
-    });
-
-    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
-    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
-        await resolveHandleChatCompletionCredentials({
-            currentUserIdentity,
-            agentId,
-            calendarConnections,
-            useEmailConfiguration,
-        });
-    const agent = await createHandleChatCompletionAgent({
-        agentName,
-        agentSource,
-        agentId,
-        resolvedAgentContext,
-        localServerUrl,
-        baseAgentReferenceResolver,
-        hasDynamicContext,
-        preparedAgentModelRequirements,
-    });
-
-    return {
-        collection,
-        resolvedAgentContext,
-        agent,
-        agentSource,
-        unresolvedAgentSource,
-        messageSuffix,
-        agentId,
-        agentHash,
-        projectRepositories,
-        calendarConnections,
-        useEmailConfiguration,
-        currentUserIdentity,
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-        isPrivateModeEnabled,
-    };
-}
-
-/**
- * Creates prompt and persistence helpers for one chat completion request.
- */
-async function createHandleChatCompletionPromptContext(options: {
-    request: NextRequest;
-    title: string;
-    apiKey: string | null;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-}): Promise<HandleChatCompletionPromptContext> {
-    const { request, title, apiKey, parsedRequest, runtime } = options;
-    const timezone = request.headers.get('x-timezone') || 'UTC';
-    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
-    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
-    const recordChatHistoryMessage = await createChatHistoryRecorder({
-        request,
-        agentIdentifier: runtime.agentId,
-        agentHash: runtime.agentHash,
-        source: 'OPENAI_API_COMPATIBILITY',
-        apiKey,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        isEnabled: !runtime.isPrivateModeEnabled,
-    });
-    const userMessageHash = await recordChatHistoryMessage({
-        message: {
-            role: 'USER',
-            content: lastMessage.content,
-        },
-        previousMessageHash: null,
-    });
-    const promptParameters = composePromptParametersWithMemoryContext({
-        baseParameters: {
-            ...parsedRequest.incomingParameters,
-            timezone,
-        },
-        currentUserIdentity: runtime.currentUserIdentity,
-        agentPermanentId: runtime.agentId,
-        agentName: runtime.resolvedAgentContext.resolvedAgentName,
-        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
-        projectRepositories: runtime.projectRepositories,
-        projectGithubToken: runtime.projectGithubToken,
-        emailSmtpCredential: runtime.emailSmtpCredential,
-        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
-        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
-        calendarConnections: runtime.calendarConnections,
-    });
-    const prompt: ChatPrompt = {
-        title,
-        content: lastMessage.content,
-        modelRequirements: {
-            modelVariant: 'CHAT',
-            responseFormat: parsedRequest.responseFormat,
-            toolChoice: parsedRequest.runtimeToolChoice,
-            // We could pass 'model' from body if we wanted to enforce it, but Agent usually has its own config
-        },
-        parameters: promptParameters,
-        thread,
-        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
-    };
-
-    return {
-        prompt,
-        userMessageHash,
-        recordChatHistoryMessage,
-        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
-            isEnabled: !runtime.isPrivateModeEnabled,
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentId: runtime.agentId,
-            messages: parsedRequest.messages,
-            includeAssistantPlaceholder: true,
-            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
-        }),
-    };
-}
-
-/**
- * Handles the streaming OpenAI-compatible response branch.
- */
-function createHandleChatCompletionStreamResponse(options: {
-    request: NextRequest;
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Response {
-    const { request, title, parsedRequest, runtime, promptContext } = options;
-    const encoder = new TextEncoder();
-    const responseModel = parsedRequest.model || 'promptbook-agent';
-    const readableStream = new ReadableStream({
-        async start(controller) {
-            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
-            const created = Math.floor(Date.now() / 1000);
-            const emitSsePayload = (payload: unknown) => {
-                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
-            };
-            const emitDeltaChunk = (deltaContent: string) => {
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {
-                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
-                        },
-                        finishReason: null,
-                    }),
-                );
-            };
-
-            emitSsePayload(
-                createHandleChatCompletionChunk({
-                    runId,
-                    created,
-                    model: responseModel,
-                    delta: {
-                        role: 'assistant',
-                        content: '',
-                    },
-                    finishReason: null,
-                }),
-            );
-
-            let hasMeaningfulDelta = false;
-
-            try {
-                const handleStreamChunk = createChatStreamHandler({
-                    onDelta: (deltaContent) => {
-                        if (deltaContent.trim().length > 0) {
-                            hasMeaningfulDelta = true;
-                        }
-                        emitDeltaChunk(deltaContent);
-                    },
-                    onToolCalls: (toolCalls) => {
-                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
-                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
-                    },
-                });
-                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
-                    signal: request.signal,
-                });
-                const normalizedResponse = ensureNonEmptyChatContent({
-                    content: result.content,
-                    context: title,
-                });
-
-                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
-                    emitDeltaChunk(normalizedResponse.content);
-                }
-
-                const messageSuffixAppendix = createMessageSuffixAppendix(
-                    normalizedResponse.content,
-                    runtime.messageSuffix,
-                );
-                if (messageSuffixAppendix) {
-                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
-                        emitDeltaChunk(delta);
-                    });
-                }
-
-                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-                await finalizeHandleChatCompletionResult({
-                    responseContentWithSuffix,
-                    usage: result.usage,
-                    toolCalls: result.toolCalls,
-                    parsedRequest,
-                    runtime,
-                    promptContext,
-                    // Preserve historical behavior: streamed OpenAI-compatible calls still persist learning in private mode.
-                    shouldPersistLearnedAgentSource: true,
-                });
-
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {},
-                        finishReason: 'stop',
-                    }),
-                );
-                controller.enqueue(encoder.encode('[DONE]'));
-            } catch (error) {
-                console.error('Error during streaming:', error);
-                controller.error(error);
-            }
-            controller.close();
-        },
-    });
-
-    return new Response(readableStream, {
-        headers: {
-            'Content-Type': 'text/event-stream',
-            'Cache-Control': 'no-cache',
-            Connection: 'keep-alive',
-        },
-    });
-}
-
-/**
- * Handles the non-streaming OpenAI-compatible response branch.
- */
-async function createHandleChatCompletionJsonResponse(options: {
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Promise<NextResponse> {
-    const { title, parsedRequest, runtime, promptContext } = options;
-    const result = await runtime.agent.callChatModel(promptContext.prompt);
-    const normalizedResponse = ensureNonEmptyChatContent({
-        content: result.content,
-        context: title,
-    });
-    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-
-    await finalizeHandleChatCompletionResult({
-        responseContentWithSuffix,
-        usage: result.usage,
-        toolCalls: result.toolCalls,
-        parsedRequest,
-        runtime,
-        promptContext,
-        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
-    });
-
-    return NextResponse.json({
-        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
-        object: 'chat.completion',
-        created: Math.floor(Date.now() / 1000),
-        model: parsedRequest.model || 'promptbook-agent',
-        choices: [
-            {
-                index: 0,
-                message: {
-                    role: 'assistant',
-                    content: responseContentWithSuffix,
-                },
-                finish_reason: 'stop',
-            },
-        ],
-        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
-    });
-}
-
-/**
- * Runs the shared side effects after one successful model response.
- */
-async function finalizeHandleChatCompletionResult(options: {
-    responseContentWithSuffix: string;
-    usage: Usage;
-    toolCalls?: ReadonlyArray<ToolCall>;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
-
-    await promptContext.recordChatHistoryMessage({
-        message: {
-            role: 'MODEL',
-            content: responseContentWithSuffix,
-        },
-        previousMessageHash: promptContext.userMessageHash,
-        usage,
-    });
-
-    if (toolCalls && toolCalls.length > 0) {
-        await logCalendarToolCallsActivity({
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentPermanentId: runtime.agentId,
-            toolCalls,
-        });
-    }
-
-    await persistOpenAiCompatibilityFrozenChat({
-        isEnabled: !runtime.isPrivateModeEnabled,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        agentId: runtime.agentId,
-        chatId: promptContext.persistedFrozenChatId,
-        messages: parsedRequest.messages,
-        assistantContent: responseContentWithSuffix,
-        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
-    });
-    await persistHandleChatCompletionLearnedAgentSource({
-        runtime,
-        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
-    });
-}
-
-/**
- * Applies append-only self-learning updates when the current branch should persist them.
- */
-async function persistHandleChatCompletionLearnedAgentSource(options: {
-    runtime: HandleChatCompletionRuntime;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { runtime, shouldPersistLearnedAgentSource } = options;
-    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
-        return;
-    }
-
-    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
-        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
-        resolvedAgentSourceBeforeLearning: runtime.agentSource,
-        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
-    });
-
-    if (learnedAgentSource !== null) {
-        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
     }
 }
-
-/**
- * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
- */
-async function persistOpenAiCompatibilityFrozenChat(options: {
-    isEnabled: boolean;
-    userId: number | null | undefined;
-    agentId: string;
-    messages: ReadonlyArray<unknown>;
-    chatId?: string;
-    assistantContent?: string;
-    includeAssistantPlaceholder?: boolean;
-    failureMessage: string;
-}): Promise<string | undefined> {
-    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
-        options;
-    if (!isEnabled || !userId) {
-        return undefined;
-    }
-
-    const persistedFrozenChat = await persistFrozenUserChat({
-        userId,
-        agentPermanentId: agentId,
-        source: USER_CHAT_SOURCES.OPENAI_API,
-        chatId,
-        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
-            includeAssistantPlaceholder,
-        }),
-    }).catch((error) => {
-        console.error(failureMessage, error);
-        return null;
-    });
-
-    return persistedFrozenChat?.id;
-}
-
-/**
- * Builds one OpenAI-compatible thread from the earlier request messages.
- */
-function createHandleChatCompletionThread(previousMessages: ReadonlyArray<TODO_any>): ChatMessage[] {
-    return previousMessages.map((message: TODO_any, index: number) => ({
-        id: `msg-${index}`,
-        sender: message.role === 'assistant' ? 'agent' : 'user',
-        content: message.content,
-        isComplete: true,
-        createdAt: $getCurrentDate(),
-    }));
-}
-
-/**
- * Appends system messages to the agent source as dynamic `CONTEXT`.
- */
-function appendSystemMessagesToAgentSource(
-    agentSource: string_book,
-    messages: ReadonlyArray<TODO_any>,
-): {
-    agentSourceWithContext: string_book;
-    hasDynamicContext: boolean;
-} {
-    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
-    if (systemMessages.length === 0) {
-        return {
-            agentSourceWithContext: agentSource,
-            hasDynamicContext: false,
-        };
-    }
-
-    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
-    return {
-        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
-        hasDynamicContext: true,
-    };
-}
-
-/**
- * Waits for any running preparation job of the current agent before executing the chat call.
- */
-async function waitForHandleChatCompletionAgentPreparation(options: {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    agentId: string;
-    agentHash: string;
-}): Promise<void> {
-    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
-    const preparationWaitResult = await waitForRunningAgentPreparation({
-        tablePrefix,
-        agentPermanentId: options.agentId,
-        fingerprint: options.agentHash,
-        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    });
-
-    if (preparationWaitResult !== 'not_running') {
-        console.info('[pre-index]', 'openai_chat_wait_result', {
-            tablePrefix,
-            agentPermanentId: options.agentId,
-            agentHash: options.agentHash,
-            preparationWaitResult,
-        });
-    }
-}
-
-/**
- * Resolves request-scoped project, calendar, and email credentials for the current agent.
- */
-async function resolveHandleChatCompletionCredentials(options: {
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    agentId: string;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-}): Promise<{
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-}> {
-    const userId = options.currentUserIdentity?.userId;
-    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
-        resolveUseProjectGithubToken({
-            userId,
-            agentPermanentId: options.agentId,
-        }),
-        options.calendarConnections.length > 0
-            ? resolveUseCalendarGoogleToken({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-        options.useEmailConfiguration.isEnabled
-            ? resolveUseEmailSmtpCredential({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-    ]);
-
-    return {
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-    };
-}
-
-/**
- * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
- */
-async function createHandleChatCompletionAgent(options: {
-    agentName: string;
-    agentSource: string_book;
-    agentId: string;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    localServerUrl: string;
-    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
-    hasDynamicContext: boolean;
-    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
-}): Promise<Agent> {
-    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
-    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
-    const runtimeAgentReferenceResolver = options.hasDynamicContext
-        ? createBookScopedAgentReferenceResolver({
-              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
-              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
-              localServerUrl: options.localServerUrl,
-              fallbackResolver: options.baseAgentReferenceResolver,
-          })
-        : undefined;
-    const agentModelRequirements =
-        options.preparedAgentModelRequirements?.modelRequirements ||
-        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
-            agentReferenceResolver: runtimeAgentReferenceResolver,
-            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
-        }));
-
-    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
-        options.agentSource,
-        options.resolvedAgentContext.resolvedAgentName,
-        await baseOpenAiToolsPromise,
-        {
-            includeDynamicContext: true,
-            agentId: options.agentId,
-            modelRequirements: agentModelRequirements,
-        },
-    );
-
-    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
-        agentName: options.agentName,
-        assistantCacheKey: agentKitResult.assistantCacheKey,
-        vectorStoreHash: agentKitResult.vectorStoreHash,
-        vectorStoreId: agentKitResult.vectorStoreId,
-    });
-
-    return new Agent({
-        agentSource: options.agentSource,
-        executionTools: {
-            llm: agentKitResult.tools,
-        },
-        precomputedModelRequirements: agentModelRequirements,
-        assistantPreparationMode: 'external',
-        isVerbose: true,
-        teacherAgent: null,
-    });
-}
-
-/**
- * Creates one OpenAI-compatible streaming chunk payload.
- */
-function createHandleChatCompletionChunk(options: {
-    runId: string;
-    created: number;
-    model: TODO_any;
-    delta: Record<string, unknown>;
-    finishReason: 'stop' | null;
-}) {
-    return {
-        id: options.runId,
-        object: 'chat.completion.chunk',
-        created: options.created,
-        model: options.model,
-        choices: [
-            {
-                index: 0,
-                delta: options.delta,
-                finish_reason: options.finishReason,
-            },
-        ],
-    };
-}
-
-/**
- * Creates one consistent OpenAI-compatible error response.
- */
-function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
-    return NextResponse.json(
-        {
-            error: {
-                message,
-                type,
-            },
-        },
-        { status },
-    );
-}
-
-// TODO: [🈹] Maybe move chat thread handling here
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f49a4fa80fa21f140e25de59596e5546d62e3f7
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
@@ -0,0 +1,18 @@
+import { NextResponse } from 'next/server';
+
+/**
+ * Creates one consistent OpenAI-compatible error response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
+    return NextResponse.json(
+        {
+            error: {
+                message,
+                type,
+            },
+        },
+        { status },
+    );
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b1979f4621921bc664fed75a05851c6fcb8fba6d
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
@@ -0,0 +1,120 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import { appendMessageSuffix } from '@/src/utils/chat/messageSuffix';
+import type { UncertainNumber, Usage, UsageCounts } from '@promptbook-local/types';
+import { NextResponse } from 'next/server';
+import { computeUsageCounts } from '../../../../../src/execution/utils/computeUsageCounts';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the non-streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionJsonResponse(options: {
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Promise<NextResponse> {
+    const { title, parsedRequest, runtime, promptContext } = options;
+    const result = await runtime.agent.callChatModel(promptContext.prompt);
+    const normalizedResponse = ensureNonEmptyChatContent({
+        content: result.content,
+        context: title,
+    });
+    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+
+    await finalizeHandleChatCompletionResult({
+        responseContentWithSuffix,
+        usage: result.usage,
+        toolCalls: result.toolCalls,
+        parsedRequest,
+        runtime,
+        promptContext,
+        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
+    });
+
+    return NextResponse.json({
+        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
+        object: 'chat.completion',
+        created: Math.floor(Date.now() / 1000),
+        model: parsedRequest.model || 'promptbook-agent',
+        choices: [
+            {
+                index: 0,
+                message: {
+                    role: 'assistant',
+                    content: responseContentWithSuffix,
+                },
+                finish_reason: 'stop',
+            },
+        ],
+        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
+    });
+}
+
+/**
+ * Falls back to the estimated value when the original token count is unknown.
+ *
+ * @param tokenCount - Token count reported by the execution tools.
+ * @param fallbackValue - Estimated token count based on text length.
+ * @returns Token count to report in the OpenAI-compatible response.
+ */
+function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
+    if (tokenCount.value === 0 && tokenCount.isUncertain) {
+        return {
+            value: fallbackValue,
+            isUncertain: true,
+        };
+    }
+
+    return tokenCount;
+}
+
+/**
+ * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
+ *
+ * @param promptContent - Prompt content used for the request.
+ * @param completionContent - Assistant response content.
+ * @param usage - Native Promptbook usage details.
+ * @returns Usage payload for compatibility responses.
+ */
+function createCompatibilityUsage(
+    promptContent: string,
+    completionContent: string,
+    usage: Usage,
+): {
+    prompt_tokens: number;
+    completion_tokens: number;
+    total_tokens: number;
+    details: Usage;
+} {
+    const promptCounts = computeUsageCounts(promptContent);
+    const completionCounts = computeUsageCounts(completionContent);
+    const inputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
+        ...promptCounts,
+    };
+    const outputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
+        ...completionCounts,
+    };
+
+    const promptTokens = inputUsage.tokensCount.value;
+    const completionTokens = outputUsage.tokensCount.value;
+
+    return {
+        prompt_tokens: promptTokens,
+        completion_tokens: completionTokens,
+        total_tokens: promptTokens + completionTokens,
+        details: {
+            price: usage.price,
+            duration: usage.duration,
+            input: inputUsage,
+            output: outputUsage,
+        },
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..7aac8141a8b1237ce36430b4b4598a618f0383ac
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
@@ -0,0 +1,111 @@
+import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
+import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
+import type { ChatMessage, ChatPrompt } from '@promptbook-local/types';
+import { $getCurrentDate } from '@promptbook-local/utils';
+import type { NextRequest } from 'next/server';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Prompt, history recorder, and persistence context created for one chat call.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionPromptContext = {
+    prompt: ChatPrompt;
+    userMessageHash: string;
+    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
+    persistedFrozenChatId?: string;
+};
+
+/**
+ * Creates prompt and persistence helpers for one chat completion request.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionPromptContext(options: {
+    request: NextRequest;
+    title: string;
+    apiKey: string | null;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+}): Promise<HandleChatCompletionPromptContext> {
+    const { request, title, apiKey, parsedRequest, runtime } = options;
+    const timezone = request.headers.get('x-timezone') || 'UTC';
+    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
+    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
+    const recordChatHistoryMessage = await createChatHistoryRecorder({
+        request,
+        agentIdentifier: runtime.agentId,
+        agentHash: runtime.agentHash,
+        source: 'OPENAI_API_COMPATIBILITY',
+        apiKey,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        isEnabled: !runtime.isPrivateModeEnabled,
+    });
+    const userMessageHash = await recordChatHistoryMessage({
+        message: {
+            role: 'USER',
+            content: lastMessage.content,
+        },
+        previousMessageHash: null,
+    });
+    const promptParameters = composePromptParametersWithMemoryContext({
+        baseParameters: {
+            ...parsedRequest.incomingParameters,
+            timezone,
+        },
+        currentUserIdentity: runtime.currentUserIdentity,
+        agentPermanentId: runtime.agentId,
+        agentName: runtime.resolvedAgentContext.resolvedAgentName,
+        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
+        projectRepositories: runtime.projectRepositories,
+        projectGithubToken: runtime.projectGithubToken,
+        emailSmtpCredential: runtime.emailSmtpCredential,
+        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
+        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
+        calendarConnections: runtime.calendarConnections,
+    });
+    const prompt: ChatPrompt = {
+        title,
+        content: lastMessage.content,
+        modelRequirements: {
+            modelVariant: 'CHAT',
+            responseFormat: parsedRequest.responseFormat,
+            toolChoice: parsedRequest.runtimeToolChoice,
+        },
+        parameters: promptParameters,
+        thread,
+        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
+    };
+
+    return {
+        prompt,
+        userMessageHash,
+        recordChatHistoryMessage,
+        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
+            isEnabled: !runtime.isPrivateModeEnabled,
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentId: runtime.agentId,
+            messages: parsedRequest.messages,
+            includeAssistantPlaceholder: true,
+            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
+        }),
+    };
+}
+
+/**
+ * Builds one OpenAI-compatible thread from the earlier request messages.
+ */
+function createHandleChatCompletionThread(
+    previousMessages: HandleChatCompletionParsedRequest['threadMessages'],
+): Array<ChatMessage> {
+    return previousMessages.map((message, index) => ({
+        id: `msg-${index}`,
+        sender: message.role === 'assistant' ? 'agent' : 'user',
+        content: message.content,
+        isComplete: true,
+        createdAt: $getCurrentDate(),
+    }));
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..85e096027760bc45fb730e74f8ae3bbb721b1029
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
@@ -0,0 +1,162 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import {
+    appendMessageSuffix,
+    createMessageSuffixAppendix,
+    emulateMessageSuffixStreaming,
+} from '@/src/utils/chat/messageSuffix';
+import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
+import { encodeChatStreamWhitespaceForTransport } from '../../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
+import { prepareToolCallsForStreaming } from '../toolCallStreaming';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionStreamResponse(options: {
+    request: Request;
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Response {
+    const { request, title, parsedRequest, runtime, promptContext } = options;
+    const encoder = new TextEncoder();
+    const responseModel = parsedRequest.model || 'promptbook-agent';
+    const readableStream = new ReadableStream({
+        async start(controller) {
+            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
+            const created = Math.floor(Date.now() / 1000);
+            const emitSsePayload = (payload: unknown) => {
+                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
+            };
+            const emitDeltaChunk = (deltaContent: string) => {
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {
+                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
+                        },
+                        finishReason: null,
+                    }),
+                );
+            };
+
+            emitSsePayload(
+                createHandleChatCompletionChunk({
+                    runId,
+                    created,
+                    model: responseModel,
+                    delta: {
+                        role: 'assistant',
+                        content: '',
+                    },
+                    finishReason: null,
+                }),
+            );
+
+            let hasMeaningfulDelta = false;
+
+            try {
+                const handleStreamChunk = createChatStreamHandler({
+                    onDelta: (deltaContent) => {
+                        if (deltaContent.trim().length > 0) {
+                            hasMeaningfulDelta = true;
+                        }
+                        emitDeltaChunk(deltaContent);
+                    },
+                    onToolCalls: (toolCalls) => {
+                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
+                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
+                    },
+                });
+                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
+                    signal: request.signal,
+                });
+                const normalizedResponse = ensureNonEmptyChatContent({
+                    content: result.content,
+                    context: title,
+                });
+
+                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
+                    emitDeltaChunk(normalizedResponse.content);
+                }
+
+                const messageSuffixAppendix = createMessageSuffixAppendix(
+                    normalizedResponse.content,
+                    runtime.messageSuffix,
+                );
+                if (messageSuffixAppendix) {
+                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
+                        emitDeltaChunk(delta);
+                    });
+                }
+
+                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+                await finalizeHandleChatCompletionResult({
+                    responseContentWithSuffix,
+                    usage: result.usage,
+                    toolCalls: result.toolCalls,
+                    parsedRequest,
+                    runtime,
+                    promptContext,
+                    shouldPersistLearnedAgentSource: true,
+                });
+
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {},
+                        finishReason: 'stop',
+                    }),
+                );
+                controller.enqueue(encoder.encode('[DONE]'));
+            } catch (error) {
+                console.error('Error during streaming:', error);
+                controller.error(error);
+            }
+            controller.close();
+        },
+    });
+
+    return new Response(readableStream, {
+        headers: {
+            'Content-Type': 'text/event-stream',
+            'Cache-Control': 'no-cache',
+            Connection: 'keep-alive',
+        },
+    });
+}
+
+/**
+ * Creates one OpenAI-compatible streaming chunk payload.
+ */
+function createHandleChatCompletionChunk(options: {
+    runId: string;
+    created: number;
+    model: HandleChatCompletionParsedRequest['model'];
+    delta: Record<string, unknown>;
+    finishReason: 'stop' | null;
+}) {
+    return {
+        id: options.runId,
+        object: 'chat.completion.chunk',
+        created: options.created,
+        model: options.model,
+        choices: [
+            {
+                index: 0,
+                delta: options.delta,
+                finish_reason: options.finishReason,
+            },
+        ],
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
new file mode 100644
index 0000000000000000000000000000000000000000..dd6a7d87ea491df245dbf93d8bcb277a9a10b034
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
@@ -0,0 +1,78 @@
+import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
+import { resolveAppendOnlySelfLearningAgentSource } from '@/src/utils/resolveAppendOnlySelfLearningAgentSource';
+import type { ToolCall, Usage } from '@promptbook-local/types';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Runs the shared side effects after one successful model response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function finalizeHandleChatCompletionResult(options: {
+    responseContentWithSuffix: string;
+    usage: Usage;
+    toolCalls?: ReadonlyArray<ToolCall>;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
+
+    await promptContext.recordChatHistoryMessage({
+        message: {
+            role: 'MODEL',
+            content: responseContentWithSuffix,
+        },
+        previousMessageHash: promptContext.userMessageHash,
+        usage,
+    });
+
+    if (toolCalls && toolCalls.length > 0) {
+        await logCalendarToolCallsActivity({
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentPermanentId: runtime.agentId,
+            toolCalls,
+        });
+    }
+
+    await persistOpenAiCompatibilityFrozenChat({
+        isEnabled: !runtime.isPrivateModeEnabled,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        agentId: runtime.agentId,
+        chatId: promptContext.persistedFrozenChatId,
+        messages: parsedRequest.messages,
+        assistantContent: responseContentWithSuffix,
+        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
+    });
+    await persistHandleChatCompletionLearnedAgentSource({
+        runtime,
+        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
+    });
+}
+
+/**
+ * Applies append-only self-learning updates when the current branch should persist them.
+ */
+async function persistHandleChatCompletionLearnedAgentSource(options: {
+    runtime: HandleChatCompletionRuntime;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { runtime, shouldPersistLearnedAgentSource } = options;
+    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
+        return;
+    }
+
+    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
+        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
+        resolvedAgentSourceBeforeLearning: runtime.agentSource,
+        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
+    });
+
+    if (learnedAgentSource !== null) {
+        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
+    }
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
new file mode 100644
index 0000000000000000000000000000000000000000..58e1c26d7f428c1bdb39b36411b3f75256e3de51
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
@@ -0,0 +1,176 @@
+import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
+import type { LlmToolDefinition, TODO_any } from '@promptbook-local/types';
+import { NextRequest, NextResponse } from 'next/server';
+import type OpenAI from 'openai';
+import { HTTP_STATUS_CODES } from '../../constants';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Definition of open AI chat tool.
+ */
+type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
+
+/**
+ * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionParsedRequest = {
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    threadMessages: ReadonlyArray<TODO_any>;
+    stream: TODO_any;
+    model: TODO_any;
+    responseFormat: TODO_any;
+    runtimeTools?: Array<LlmToolDefinition>;
+    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+    incomingParameters: Record<string, unknown>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Parses and validates the OpenAI-compatible request payload.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function parseHandleChatCompletionRequest(options: {
+    body: TODO_any;
+    agentNameFromParams?: string;
+    request: NextRequest;
+}): HandleChatCompletionParsedRequest | NextResponse {
+    const { body, agentNameFromParams, request } = options;
+    const {
+        messages,
+        stream,
+        model,
+        response_format: responseFormat,
+        tools: rawTools,
+        tool_choice: toolChoice,
+        parameters: rawParameters = {},
+    } = body;
+    const agentName = (agentNameFromParams || model) as string | undefined;
+    if (!agentName) {
+        return createHandleChatCompletionErrorResponse(
+            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    if (!messages || !Array.isArray(messages) || messages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array is required and cannot be empty.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
+    if (threadMessages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array must contain at least one non-system message.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    return {
+        agentName,
+        messages,
+        threadMessages,
+        stream,
+        model,
+        responseFormat,
+        runtimeTools: convertOpenAiTools(rawTools),
+        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
+        incomingParameters:
+            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
+                ? (rawParameters as Record<string, unknown>)
+                : {},
+        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
+    };
+}
+
+/**
+ * Handles convert open Ai tools.
+ */
+function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
+    if (!Array.isArray(rawTools)) {
+        return undefined;
+    }
+
+    const converted = rawTools
+        .map((tool) => convertOpenAiTool(tool))
+        .filter((tool): tool is LlmToolDefinition => tool !== null);
+
+    return converted.length > 0 ? converted : undefined;
+}
+
+/**
+ * Handles convert open Ai tool.
+ */
+function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
+    const tool = rawTool as OpenAIChatToolDefinition;
+    if (!tool || tool.type !== 'function') {
+        return null;
+    }
+
+    const functionDefinition = tool.function;
+    if (!functionDefinition) {
+        return null;
+    }
+
+    const { name, description, parameters } = functionDefinition;
+    if (typeof name !== 'string' || name.trim().length === 0) {
+        return null;
+    }
+
+    const parameterSchema = parameters ?? {};
+    if (parameterSchema.type !== 'object') {
+        return null;
+    }
+
+    const properties = parameterSchema.properties ?? {};
+    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
+
+    for (const [key, value] of Object.entries(properties)) {
+        if (!value || typeof value !== 'object') {
+            continue;
+        }
+
+        normalizedProperties[key] = {
+            type: typeof value.type === 'string' ? value.type : 'string',
+            description: typeof value.description === 'string' ? value.description : undefined,
+        };
+    }
+
+    const required =
+        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
+            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
+            : undefined;
+
+    return {
+        name,
+        description: typeof description === 'string' ? description : '',
+        parameters: {
+            type: 'object',
+            properties: normalizedProperties,
+            required,
+            additionalProperties:
+                typeof parameterSchema.additionalProperties === 'boolean'
+                    ? parameterSchema.additionalProperties
+                    : undefined,
+        },
+    };
+}
+
+/**
+ * Parses open Ai tool choice.
+ */
+function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
+    if (value === undefined || value === null) {
+        return undefined;
+    }
+
+    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
new file mode 100644
index 0000000000000000000000000000000000000000..60e5f1b008bcf5a6a1836c06c4c3d9ebcb909a9f
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
@@ -0,0 +1,163 @@
+import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
+import type { ChatMessage } from '@promptbook-local/types';
+
+/**
+ * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
+ */
+type OpenAiCompatibilityMessageLike = {
+    role?: unknown;
+    content?: unknown;
+};
+
+/**
+ * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function persistOpenAiCompatibilityFrozenChat(options: {
+    isEnabled: boolean;
+    userId: number | null | undefined;
+    agentId: string;
+    messages: ReadonlyArray<unknown>;
+    chatId?: string;
+    assistantContent?: string;
+    includeAssistantPlaceholder?: boolean;
+    failureMessage: string;
+}): Promise<string | undefined> {
+    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
+        options;
+    if (!isEnabled || !userId) {
+        return undefined;
+    }
+
+    const persistedFrozenChat = await persistFrozenUserChat({
+        userId,
+        agentPermanentId: agentId,
+        source: USER_CHAT_SOURCES.OPENAI_API,
+        chatId,
+        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
+            includeAssistantPlaceholder,
+        }),
+    }).catch((error) => {
+        console.error(failureMessage, error);
+        return null;
+    });
+
+    return persistedFrozenChat?.id;
+}
+
+/**
+ * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
+ */
+function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
+    if (typeof value === 'string') {
+        return value;
+    }
+
+    if (Array.isArray(value)) {
+        return value
+            .map((part) => {
+                if (typeof part === 'string') {
+                    return part;
+                }
+
+                if (!part || typeof part !== 'object') {
+                    return '';
+                }
+
+                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
+                if (typeof partLike.text === 'string') {
+                    return partLike.text;
+                }
+
+                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
+                    return '[Image input]';
+                }
+
+                if (partLike.image_url) {
+                    return '[Image input]';
+                }
+
+                return '';
+            })
+            .filter(Boolean)
+            .join('\n');
+    }
+
+    if (value === null || value === undefined) {
+        return '';
+    }
+
+    try {
+        return JSON.stringify(value, null, 2);
+    } catch {
+        return String(value);
+    }
+}
+
+/**
+ * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ */
+function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
+    if (typeof role !== 'string') {
+        return 'USER';
+    }
+
+    switch (role) {
+        case 'assistant':
+            return 'AGENT';
+        case 'system':
+            return 'SYSTEM';
+        case 'tool':
+            return 'TOOL';
+        case 'developer':
+            return 'SYSTEM';
+        default:
+            return 'USER';
+    }
+}
+
+/**
+ * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
+ */
+function createFrozenOpenAiCompatibilityMessages(
+    rawMessages: ReadonlyArray<unknown>,
+    assistantContent?: string,
+    options: {
+        includeAssistantPlaceholder?: boolean;
+    } = {},
+): Array<ChatMessage> {
+    const startedAt = Date.now();
+    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
+        const message = rawMessage as OpenAiCompatibilityMessageLike;
+
+        return {
+            id: `openai-frozen-${index}`,
+            sender: mapOpenAiCompatibilityRoleToSender(message.role),
+            content: normalizeOpenAiCompatibilityMessageContent(message.content),
+            isComplete: true,
+            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage;
+    });
+
+    if (assistantContent !== undefined) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: assistantContent,
+            isComplete: true,
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    } else if (options.includeAssistantPlaceholder) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: '',
+            isComplete: false,
+            lifecycleState: 'running',
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    }
+
+    return messages;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fd44b7ea5a87168725a9e99784038137c96537ab
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
@@ -0,0 +1,305 @@
+import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
+import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
+import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
+import type { string_book, TODO_any } from '@promptbook-local/types';
+import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
+import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
+import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
+import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
+import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
+import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
+import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
+import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
+import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
+import {
+    createBookScopedAgentReferenceResolver,
+    parseBookScopedAgentIdentifier,
+} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
+import { resolveMessageSuffixFromAgentSource } from '@/src/utils/chat/messageSuffix';
+import { NextRequest, NextResponse } from 'next/server';
+import { isAgentDeleted } from '../../app/agents/[agentName]/_utils';
+import {
+    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    resolveAgentCollectionTablePrefix,
+    waitForRunningAgentPreparation,
+} from '../agentPreparation';
+import { resolveCachedServerAgentContext, resolveCachedServerAgentModelRequirements } from '../cachedServerAgentRuntime';
+import { AgentKitCacheManager } from '../cache/AgentKitCacheManager';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Runtime state resolved before the chat model is executed.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionRuntime = {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    agent: Agent;
+    agentSource: string_book;
+    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
+    messageSuffix: string | null;
+    agentId: string;
+    agentHash: string;
+    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Resolves agent/runtime dependencies needed before prompt execution.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function resolveHandleChatCompletionRuntime(options: {
+    request: NextRequest;
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    isPrivateModeEnabled: boolean;
+}): Promise<HandleChatCompletionRuntime | NextResponse> {
+    const { request, agentName, messages, isPrivateModeEnabled } = options;
+    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
+    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
+
+    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
+        return createHandleChatCompletionErrorResponse(
+            'This agent has been deleted. You can restore it from the Recycle Bin.',
+            'agent_deleted',
+            410,
+        );
+    }
+
+    const [collection, baseAgentReferenceResolver] = await Promise.all([
+        $provideAgentCollectionForServer(),
+        $provideAgentReferenceResolver(),
+    ]);
+    const localServerUrl = new URL(request.url).origin;
+    const resolvedAgentContext = await resolveCachedServerAgentContext({
+        collection,
+        agentIdentifier: agentName,
+        localServerUrl,
+        fallbackResolver: baseAgentReferenceResolver,
+    }).catch(() => null);
+
+    if (!resolvedAgentContext?.resolvedAgentSource) {
+        return createHandleChatCompletionErrorResponse(`Agent '${agentName}' not found.`, 'invalid_request_error', 404);
+    }
+
+    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
+    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
+    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
+    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
+    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
+    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
+    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
+    agentSource = agentSourceWithContext;
+
+    const preparedAgentModelRequirements = !hasDynamicContext
+        ? await resolveCachedServerAgentModelRequirements({
+              resolvedAgentContext,
+              localServerUrl,
+              fallbackResolver: baseAgentReferenceResolver,
+          })
+        : null;
+    const agentId = resolvedAgentContext.parentAgentPermanentId;
+    const agentHash = computeAgentHash(agentSource);
+    await waitForHandleChatCompletionAgentPreparation({
+        collection,
+        agentId,
+        agentHash,
+    });
+
+    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
+    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
+        await resolveHandleChatCompletionCredentials({
+            currentUserIdentity,
+            agentId,
+            calendarConnections,
+            useEmailConfiguration,
+        });
+    const agent = await createHandleChatCompletionAgent({
+        agentName,
+        agentSource,
+        agentId,
+        resolvedAgentContext,
+        localServerUrl,
+        baseAgentReferenceResolver,
+        hasDynamicContext,
+        preparedAgentModelRequirements,
+    });
+
+    return {
+        collection,
+        resolvedAgentContext,
+        agent,
+        agentSource,
+        unresolvedAgentSource,
+        messageSuffix,
+        agentId,
+        agentHash,
+        projectRepositories,
+        calendarConnections,
+        useEmailConfiguration,
+        currentUserIdentity,
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+        isPrivateModeEnabled,
+    };
+}
+
+/**
+ * Appends system messages to the agent source as dynamic `CONTEXT`.
+ */
+function appendSystemMessagesToAgentSource(
+    agentSource: string_book,
+    messages: ReadonlyArray<TODO_any>,
+): {
+    agentSourceWithContext: string_book;
+    hasDynamicContext: boolean;
+} {
+    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
+    if (systemMessages.length === 0) {
+        return {
+            agentSourceWithContext: agentSource,
+            hasDynamicContext: false,
+        };
+    }
+
+    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
+    return {
+        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
+        hasDynamicContext: true,
+    };
+}
+
+/**
+ * Waits for any running preparation job of the current agent before executing the chat call.
+ */
+async function waitForHandleChatCompletionAgentPreparation(options: {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    agentId: string;
+    agentHash: string;
+}): Promise<void> {
+    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
+    const preparationWaitResult = await waitForRunningAgentPreparation({
+        tablePrefix,
+        agentPermanentId: options.agentId,
+        fingerprint: options.agentHash,
+        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    });
+
+    if (preparationWaitResult !== 'not_running') {
+        console.info('[pre-index]', 'openai_chat_wait_result', {
+            tablePrefix,
+            agentPermanentId: options.agentId,
+            agentHash: options.agentHash,
+            preparationWaitResult,
+        });
+    }
+}
+
+/**
+ * Resolves request-scoped project, calendar, and email credentials for the current agent.
+ */
+async function resolveHandleChatCompletionCredentials(options: {
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    agentId: string;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+}): Promise<{
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+}> {
+    const userId = options.currentUserIdentity?.userId;
+    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
+        resolveUseProjectGithubToken({
+            userId,
+            agentPermanentId: options.agentId,
+        }),
+        options.calendarConnections.length > 0
+            ? resolveUseCalendarGoogleToken({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+        options.useEmailConfiguration.isEnabled
+            ? resolveUseEmailSmtpCredential({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+    ]);
+
+    return {
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+    };
+}
+
+/**
+ * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
+ */
+async function createHandleChatCompletionAgent(options: {
+    agentName: string;
+    agentSource: string_book;
+    agentId: string;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    localServerUrl: string;
+    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
+    hasDynamicContext: boolean;
+    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
+}): Promise<Agent> {
+    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
+    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
+    const runtimeAgentReferenceResolver = options.hasDynamicContext
+        ? createBookScopedAgentReferenceResolver({
+              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
+              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
+              localServerUrl: options.localServerUrl,
+              fallbackResolver: options.baseAgentReferenceResolver,
+          })
+        : undefined;
+    const agentModelRequirements =
+        options.preparedAgentModelRequirements?.modelRequirements ||
+        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
+            agentReferenceResolver: runtimeAgentReferenceResolver,
+            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
+        }));
+
+    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
+        options.agentSource,
+        options.resolvedAgentContext.resolvedAgentName,
+        await baseOpenAiToolsPromise,
+        {
+            includeDynamicContext: true,
+            agentId: options.agentId,
+            modelRequirements: agentModelRequirements,
+        },
+    );
+
+    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
+        agentName: options.agentName,
+        assistantCacheKey: agentKitResult.assistantCacheKey,
+        vectorStoreHash: agentKitResult.vectorStoreHash,
+        vectorStoreId: agentKitResult.vectorStoreId,
+    });
+
+    return new Agent({
+        agentSource: options.agentSource,
+        executionTools: {
+            llm: agentKitResult.tools,
+        },
+        precomputedModelRequirements: agentModelRequirements,
+        assistantPreparationMode: 'external',
+        isVerbose: true,
+        teacherAgent: null,
+    });
+}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 474fd839d14b2bdab324e5f59fce0b15ddf95afd..bc9cfde73332af5d107ad35780a31963bc4e68e9
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,8 @@
+-   Refactored Agents Server `handleChatCompletion.ts` into a thinner chat-completion facade plus focused private helper modules without changing external behavior:
+
+    -   Extracted OpenAI-compatible request parsing, runtime/agent resolution, prompt-context creation, frozen-chat persistence, shared post-response side effects, and the streaming vs non-streaming response branches into dedicated private files behind the existing `handleChatCompletion` entrypoint.
+    -   Kept the existing API-key validation, deleted-agent handling, dynamic system-context injection, AgentKit cache usage, private-mode behavior, frozen audit snapshots, self-learning persistence, streaming chunk format, and OpenAI-compatible JSON response shape intact while reducing responsibility density in the main file.
+
 -   Refactored Agents Server `errorMessages.ts` into smaller focused helpers without changing external behavior:
 
     -   Split the branching-heavy `categorizeError` flow into dedicated API-type, native-error-message, and status-code classification helpers while preserving the existing precedence.



thinking
**Inspecting code issues**

I need to check the files for obvious problems, like seeing if the imported NextResponse is actually necessary. I could run a git diff using the shell and maybe utilize rg to find any entries marked with `TODO: [🈹]`. Counting functions in the main file shows I have one, so that's okay. I also need to make sure there are no name discrepancies due to new folder or filenames, but they likely match. Exported types should be reviewed, including ensuring JSDoc is in place where necessary.
thinking
**Investigating import issues**

I want to check for potential issues with unused imports like `NextRequest` or `NextResponse`, particularly considering the `importsNotUsedAsValues` setting. Maybe I should compile with `verbatimModuleSyntax` set to true, which would require type-only imports to be marked correctly. Some imports might be used as values but are treated as types. So, I should also take a look at the `tsconfig` to see how it's configured and search for `verbatimModuleSyntax`.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg -n "verbatimModuleSyntax|importsNotUsedAsValues|preserveValueImports|isolatedModules|noUnusedLocals|noUnusedParameters" tsconfig*.json apps/agents-server -S' in C:/Users/me/work/ai/promptbook exited 1 in 482ms:
apps/agents-server\tsconfig.json:13:        "isolatedModules": true,
apps/agents-server\scripts\generate-reserved-paths\tsconfig.json:14:        "noUnusedLocals": false,
rg: tsconfig*.json: The filename, directory name, or volume label syntax is incorrect. (os error 123)
file update:
diff --git a/apps/agents-server/src/utils/handleChatCompletion.ts b/apps/agents-server/src/utils/handleChatCompletion.ts
index 9c96d36fad9cf24bf26d89f57372332add4e4595..cff56fafd6cfa90bb4094f9f202c4f01f6f74af1
--- a/apps/agents-server/src/utils/handleChatCompletion.ts
+++ b/apps/agents-server/src/utils/handleChatCompletion.ts
@@ -1,392 +1,16 @@
-import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
-import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
-import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
-import {
-    createBookScopedAgentReferenceResolver,
-    parseBookScopedAgentIdentifier,
-} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
-import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
-import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
-import {
-    appendMessageSuffix,
-    createMessageSuffixAppendix,
-    emulateMessageSuffixStreaming,
-    resolveMessageSuffixFromAgentSource,
-} from '@/src/utils/chat/messageSuffix';
-import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
-import { prepareToolCallsForStreaming } from './toolCallStreaming';
-import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
-import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
-import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
-import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
-import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
-import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
-import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
-import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
-import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
-import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
-import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
-import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
-import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
-import type {
-    ChatMessage,
-    ChatPrompt,
-    LlmToolDefinition,
-    string_book,
-    ToolCall,
-    TODO_any,
-    UncertainNumber,
-    Usage,
-    UsageCounts,
-} from '@promptbook-local/types';
-import { $getCurrentDate } from '@promptbook-local/utils';
 import { NextRequest, NextResponse } from 'next/server';
-import type OpenAI from 'openai';
-import { computeUsageCounts } from '../../../../src/execution/utils/computeUsageCounts';
-import { encodeChatStreamWhitespaceForTransport } from '../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
-import { isAgentDeleted } from '../app/agents/[agentName]/_utils';
 import { HTTP_STATUS_CODES } from '../constants';
-import { AgentKitCacheManager } from './cache/AgentKitCacheManager';
 import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
+import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
+import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
+import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
+import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
+import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
 import { validateApiKey } from './validateApiKey';
-import {
-    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    resolveAgentCollectionTablePrefix,
-    waitForRunningAgentPreparation,
-} from './agentPreparation';
-import {
-    resolveCachedServerAgentContext,
-    resolveCachedServerAgentModelRequirements,
-} from './cachedServerAgentRuntime';
-import { resolveAppendOnlySelfLearningAgentSource } from './resolveAppendOnlySelfLearningAgentSource';
 
 /**
- * Falls back to the estimated value when the original token count is unknown.
- *
- * @param tokenCount - Token count reported by the execution tools.
- * @param fallbackValue - Estimated token count based on text length.
- * @returns Token count to report in the OpenAI-compatible response.
- */
-function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
-    if (tokenCount.value === 0 && tokenCount.isUncertain) {
-        return {
-            value: fallbackValue,
-            isUncertain: true,
-        };
-    }
-
-    return tokenCount;
-}
-
-/**
- * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
- *
- * @param promptContent - Prompt content used for the request.
- * @param completionContent - Assistant response content.
- * @param usage - Native Promptbook usage details.
- * @returns Usage payload for compatibility responses.
- */
-function createCompatibilityUsage(
-    promptContent: string,
-    completionContent: string,
-    usage: Usage,
-): {
-    prompt_tokens: number;
-    completion_tokens: number;
-    total_tokens: number;
-    details: Usage;
-} {
-    const promptCounts = computeUsageCounts(promptContent);
-    const completionCounts = computeUsageCounts(completionContent);
-    const inputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
-        ...promptCounts,
-    };
-    const outputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
-        ...completionCounts,
-    };
-
-    const promptTokens = inputUsage.tokensCount.value;
-    const completionTokens = outputUsage.tokensCount.value;
-
-    return {
-        prompt_tokens: promptTokens,
-        completion_tokens: completionTokens,
-        total_tokens: promptTokens + completionTokens,
-        details: {
-            price: usage.price,
-            duration: usage.duration,
-            input: inputUsage,
-            output: outputUsage,
-        },
-    };
-}
-
-/**
- * Definition of open AI chat tool.
- */
-type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
-
-/**
- * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
- */
-type OpenAiCompatibilityMessageLike = {
-    role?: unknown;
-    content?: unknown;
-};
-
-/**
- * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
- */
-type HandleChatCompletionParsedRequest = {
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    threadMessages: ReadonlyArray<TODO_any>;
-    stream: TODO_any;
-    model: TODO_any;
-    responseFormat: TODO_any;
-    runtimeTools?: Array<LlmToolDefinition>;
-    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-    incomingParameters: Record<string, unknown>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Runtime state resolved before the chat model is executed.
- */
-type HandleChatCompletionRuntime = {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    agent: Agent;
-    agentSource: string_book;
-    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
-    messageSuffix: string | null;
-    agentId: string;
-    agentHash: string;
-    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Prompt, history recorder, and persistence context created for one chat call.
- */
-type HandleChatCompletionPromptContext = {
-    prompt: ChatPrompt;
-    userMessageHash: string;
-    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
-    persistedFrozenChatId?: string;
-};
-
-/**
- * Handles convert open Ai tools.
- */
-function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
-    if (!Array.isArray(rawTools)) {
-        return undefined;
-    }
-
-    const converted = rawTools
-        .map((tool) => convertOpenAiTool(tool))
-        .filter((tool): tool is LlmToolDefinition => tool !== null);
-
-    return converted.length > 0 ? converted : undefined;
-}
-
-/**
- * Handles convert open Ai tool.
- */
-function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
-    const tool = rawTool as OpenAIChatToolDefinition;
-    if (!tool || tool.type !== 'function') {
-        return null;
-    }
-
-    const functionDefinition = tool.function;
-    if (!functionDefinition) {
-        return null;
-    }
-
-    const { name, description, parameters } = functionDefinition;
-    if (typeof name !== 'string' || name.trim().length === 0) {
-        return null;
-    }
-
-    const parameterSchema = parameters ?? {};
-    if (parameterSchema.type !== 'object') {
-        return null;
-    }
-
-    const properties = parameterSchema.properties ?? {};
-    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
-
-    for (const [key, value] of Object.entries(properties)) {
-        if (!value || typeof value !== 'object') {
-            continue;
-        }
-
-        normalizedProperties[key] = {
-            type: typeof value.type === 'string' ? value.type : 'string',
-            description: typeof value.description === 'string' ? value.description : undefined,
-        };
-    }
-
-    const required =
-        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
-            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
-            : undefined;
-
-    return {
-        name,
-        description: typeof description === 'string' ? description : '',
-        parameters: {
-            type: 'object',
-            properties: normalizedProperties,
-            required,
-            additionalProperties:
-                typeof parameterSchema.additionalProperties === 'boolean'
-                    ? parameterSchema.additionalProperties
-                    : undefined,
-        },
-    };
-}
-
-/**
- * Parses open Ai tool choice.
- */
-function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
-    if (value === undefined || value === null) {
-        return undefined;
-    }
-
-    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-}
-
-/**
- * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
- */
-function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
-    if (typeof value === 'string') {
-        return value;
-    }
-
-    if (Array.isArray(value)) {
-        return value
-            .map((part) => {
-                if (typeof part === 'string') {
-                    return part;
-                }
-
-                if (!part || typeof part !== 'object') {
-                    return '';
-                }
-
-                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
-                if (typeof partLike.text === 'string') {
-                    return partLike.text;
-                }
-
-                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
-                    return '[Image input]';
-                }
-
-                if (partLike.image_url) {
-                    return '[Image input]';
-                }
-
-                return '';
-            })
-            .filter(Boolean)
-            .join('\n');
-    }
-
-    if (value === null || value === undefined) {
-        return '';
-    }
-
-    try {
-        return JSON.stringify(value, null, 2);
-    } catch {
-        return String(value);
-    }
-}
-
-/**
- * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ * Handles convert open Ai chat completion requests routed through the Agents Server.
  */
-function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
-    if (typeof role !== 'string') {
-        return 'USER';
-    }
-
-    switch (role) {
-        case 'assistant':
-            return 'AGENT';
-        case 'system':
-            return 'SYSTEM';
-        case 'tool':
-            return 'TOOL';
-        case 'developer':
-            return 'SYSTEM';
-        default:
-            return 'USER';
-    }
-}
-
-/**
- * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
- */
-function createFrozenOpenAiCompatibilityMessages(
-    rawMessages: ReadonlyArray<unknown>,
-    assistantContent?: string,
-    options: {
-        includeAssistantPlaceholder?: boolean;
-    } = {},
-): Array<ChatMessage> {
-    const startedAt = Date.now();
-    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
-        const message = rawMessage as OpenAiCompatibilityMessageLike;
-
-        return {
-            id: `openai-frozen-${index}`,
-            sender: mapOpenAiCompatibilityRoleToSender(message.role),
-            content: normalizeOpenAiCompatibilityMessageContent(message.content),
-            isComplete: true,
-            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage;
-    });
-
-    if (assistantContent !== undefined) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: assistantContent,
-            isComplete: true,
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    } else if (options.includeAssistantPlaceholder) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: '',
-            isComplete: false,
-            lifecycleState: 'running',
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    }
-
-    return messages;
-}
-
-/**
- * Handles chat completion.
- */
 export async function handleChatCompletion(
     request: NextRequest,
     params: { agentName?: string },
@@ -399,7 +23,6 @@
         return versionMismatchResponse;
     }
 
-    // Validate API key explicitly (in addition to middleware)
     const apiKeyValidation = await validateApiKey(request);
     if (!apiKeyValidation.isValid) {
         return NextResponse.json(
@@ -462,724 +85,5 @@
             { error: { message: (error as Error).message || 'Internal Server Error', type: 'server_error' } },
             { status: HTTP_STATUS_CODES.INTERNAL_SERVER_ERROR },
         );
-    }
-}
-
-/**
- * Parses and validates the OpenAI-compatible request payload.
- */
-function parseHandleChatCompletionRequest(options: {
-    body: TODO_any;
-    agentNameFromParams?: string;
-    request: NextRequest;
-}): HandleChatCompletionParsedRequest | NextResponse {
-    const { body, agentNameFromParams, request } = options;
-    const {
-        messages,
-        stream,
-        model,
-        response_format: responseFormat,
-        tools: rawTools,
-        tool_choice: toolChoice,
-        parameters: rawParameters = {},
-    } = body;
-    const agentName = (agentNameFromParams || model) as string | undefined;
-    if (!agentName) {
-        return createHandleChatCompletionErrorResponse(
-            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    if (!messages || !Array.isArray(messages) || messages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array is required and cannot be empty.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
-    if (threadMessages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array must contain at least one non-system message.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    return {
-        agentName,
-        messages,
-        threadMessages,
-        stream,
-        model,
-        responseFormat,
-        runtimeTools: convertOpenAiTools(rawTools),
-        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
-        incomingParameters:
-            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
-                ? (rawParameters as Record<string, unknown>)
-                : {},
-        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
-    };
-}
-
-/**
- * Resolves agent/runtime dependencies needed before prompt execution.
- */
-async function resolveHandleChatCompletionRuntime(options: {
-    request: NextRequest;
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    isPrivateModeEnabled: boolean;
-}): Promise<HandleChatCompletionRuntime | NextResponse> {
-    const { request, agentName, messages, isPrivateModeEnabled } = options;
-    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
-    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
-
-    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
-        return createHandleChatCompletionErrorResponse(
-            'This agent has been deleted. You can restore it from the Recycle Bin.',
-            'agent_deleted',
-            410,
-        );
-    }
-
-    const [collection, baseAgentReferenceResolver] = await Promise.all([
-        $provideAgentCollectionForServer(),
-        $provideAgentReferenceResolver(),
-    ]);
-    const localServerUrl = new URL(request.url).origin;
-    const resolvedAgentContext = await resolveCachedServerAgentContext({
-        collection,
-        agentIdentifier: agentName,
-        localServerUrl,
-        fallbackResolver: baseAgentReferenceResolver,
-    }).catch(() => null);
-
-    if (!resolvedAgentContext?.resolvedAgentSource) {
-        return createHandleChatCompletionErrorResponse(
-            `Agent '${agentName}' not found.`,
-            'invalid_request_error',
-            HTTP_STATUS_CODES.NOT_FOUND,
-        );
-    }
-
-    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
-    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
-    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
-    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
-    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
-    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
-    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
-    agentSource = agentSourceWithContext;
-
-    const preparedAgentModelRequirements = !hasDynamicContext
-        ? await resolveCachedServerAgentModelRequirements({
-              resolvedAgentContext,
-              localServerUrl,
-              fallbackResolver: baseAgentReferenceResolver,
-          })
-        : null;
-    const agentId = resolvedAgentContext.parentAgentPermanentId;
-    const agentHash = computeAgentHash(agentSource);
-    await waitForHandleChatCompletionAgentPreparation({
-        collection,
-        agentId,
-        agentHash,
-    });
-
-    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
-    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
-        await resolveHandleChatCompletionCredentials({
-            currentUserIdentity,
-            agentId,
-            calendarConnections,
-            useEmailConfiguration,
-        });
-    const agent = await createHandleChatCompletionAgent({
-        agentName,
-        agentSource,
-        agentId,
-        resolvedAgentContext,
-        localServerUrl,
-        baseAgentReferenceResolver,
-        hasDynamicContext,
-        preparedAgentModelRequirements,
-    });
-
-    return {
-        collection,
-        resolvedAgentContext,
-        agent,
-        agentSource,
-        unresolvedAgentSource,
-        messageSuffix,
-        agentId,
-        agentHash,
-        projectRepositories,
-        calendarConnections,
-        useEmailConfiguration,
-        currentUserIdentity,
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-        isPrivateModeEnabled,
-    };
-}
-
-/**
- * Creates prompt and persistence helpers for one chat completion request.
- */
-async function createHandleChatCompletionPromptContext(options: {
-    request: NextRequest;
-    title: string;
-    apiKey: string | null;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-}): Promise<HandleChatCompletionPromptContext> {
-    const { request, title, apiKey, parsedRequest, runtime } = options;
-    const timezone = request.headers.get('x-timezone') || 'UTC';
-    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
-    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
-    const recordChatHistoryMessage = await createChatHistoryRecorder({
-        request,
-        agentIdentifier: runtime.agentId,
-        agentHash: runtime.agentHash,
-        source: 'OPENAI_API_COMPATIBILITY',
-        apiKey,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        isEnabled: !runtime.isPrivateModeEnabled,
-    });
-    const userMessageHash = await recordChatHistoryMessage({
-        message: {
-            role: 'USER',
-            content: lastMessage.content,
-        },
-        previousMessageHash: null,
-    });
-    const promptParameters = composePromptParametersWithMemoryContext({
-        baseParameters: {
-            ...parsedRequest.incomingParameters,
-            timezone,
-        },
-        currentUserIdentity: runtime.currentUserIdentity,
-        agentPermanentId: runtime.agentId,
-        agentName: runtime.resolvedAgentContext.resolvedAgentName,
-        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
-        projectRepositories: runtime.projectRepositories,
-        projectGithubToken: runtime.projectGithubToken,
-        emailSmtpCredential: runtime.emailSmtpCredential,
-        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
-        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
-        calendarConnections: runtime.calendarConnections,
-    });
-    const prompt: ChatPrompt = {
-        title,
-        content: lastMessage.content,
-        modelRequirements: {
-            modelVariant: 'CHAT',
-            responseFormat: parsedRequest.responseFormat,
-            toolChoice: parsedRequest.runtimeToolChoice,
-            // We could pass 'model' from body if we wanted to enforce it, but Agent usually has its own config
-        },
-        parameters: promptParameters,
-        thread,
-        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
-    };
-
-    return {
-        prompt,
-        userMessageHash,
-        recordChatHistoryMessage,
-        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
-            isEnabled: !runtime.isPrivateModeEnabled,
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentId: runtime.agentId,
-            messages: parsedRequest.messages,
-            includeAssistantPlaceholder: true,
-            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
-        }),
-    };
-}
-
-/**
- * Handles the streaming OpenAI-compatible response branch.
- */
-function createHandleChatCompletionStreamResponse(options: {
-    request: NextRequest;
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Response {
-    const { request, title, parsedRequest, runtime, promptContext } = options;
-    const encoder = new TextEncoder();
-    const responseModel = parsedRequest.model || 'promptbook-agent';
-    const readableStream = new ReadableStream({
-        async start(controller) {
-            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
-            const created = Math.floor(Date.now() / 1000);
-            const emitSsePayload = (payload: unknown) => {
-                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
-            };
-            const emitDeltaChunk = (deltaContent: string) => {
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {
-                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
-                        },
-                        finishReason: null,
-                    }),
-                );
-            };
-
-            emitSsePayload(
-                createHandleChatCompletionChunk({
-                    runId,
-                    created,
-                    model: responseModel,
-                    delta: {
-                        role: 'assistant',
-                        content: '',
-                    },
-                    finishReason: null,
-                }),
-            );
-
-            let hasMeaningfulDelta = false;
-
-            try {
-                const handleStreamChunk = createChatStreamHandler({
-                    onDelta: (deltaContent) => {
-                        if (deltaContent.trim().length > 0) {
-                            hasMeaningfulDelta = true;
-                        }
-                        emitDeltaChunk(deltaContent);
-                    },
-                    onToolCalls: (toolCalls) => {
-                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
-                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
-                    },
-                });
-                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
-                    signal: request.signal,
-                });
-                const normalizedResponse = ensureNonEmptyChatContent({
-                    content: result.content,
-                    context: title,
-                });
-
-                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
-                    emitDeltaChunk(normalizedResponse.content);
-                }
-
-                const messageSuffixAppendix = createMessageSuffixAppendix(
-                    normalizedResponse.content,
-                    runtime.messageSuffix,
-                );
-                if (messageSuffixAppendix) {
-                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
-                        emitDeltaChunk(delta);
-                    });
-                }
-
-                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-                await finalizeHandleChatCompletionResult({
-                    responseContentWithSuffix,
-                    usage: result.usage,
-                    toolCalls: result.toolCalls,
-                    parsedRequest,
-                    runtime,
-                    promptContext,
-                    // Preserve historical behavior: streamed OpenAI-compatible calls still persist learning in private mode.
-                    shouldPersistLearnedAgentSource: true,
-                });
-
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {},
-                        finishReason: 'stop',
-                    }),
-                );
-                controller.enqueue(encoder.encode('[DONE]'));
-            } catch (error) {
-                console.error('Error during streaming:', error);
-                controller.error(error);
-            }
-            controller.close();
-        },
-    });
-
-    return new Response(readableStream, {
-        headers: {
-            'Content-Type': 'text/event-stream',
-            'Cache-Control': 'no-cache',
-            Connection: 'keep-alive',
-        },
-    });
-}
-
-/**
- * Handles the non-streaming OpenAI-compatible response branch.
- */
-async function createHandleChatCompletionJsonResponse(options: {
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Promise<NextResponse> {
-    const { title, parsedRequest, runtime, promptContext } = options;
-    const result = await runtime.agent.callChatModel(promptContext.prompt);
-    const normalizedResponse = ensureNonEmptyChatContent({
-        content: result.content,
-        context: title,
-    });
-    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-
-    await finalizeHandleChatCompletionResult({
-        responseContentWithSuffix,
-        usage: result.usage,
-        toolCalls: result.toolCalls,
-        parsedRequest,
-        runtime,
-        promptContext,
-        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
-    });
-
-    return NextResponse.json({
-        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
-        object: 'chat.completion',
-        created: Math.floor(Date.now() / 1000),
-        model: parsedRequest.model || 'promptbook-agent',
-        choices: [
-            {
-                index: 0,
-                message: {
-                    role: 'assistant',
-                    content: responseContentWithSuffix,
-                },
-                finish_reason: 'stop',
-            },
-        ],
-        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
-    });
-}
-
-/**
- * Runs the shared side effects after one successful model response.
- */
-async function finalizeHandleChatCompletionResult(options: {
-    responseContentWithSuffix: string;
-    usage: Usage;
-    toolCalls?: ReadonlyArray<ToolCall>;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
-
-    await promptContext.recordChatHistoryMessage({
-        message: {
-            role: 'MODEL',
-            content: responseContentWithSuffix,
-        },
-        previousMessageHash: promptContext.userMessageHash,
-        usage,
-    });
-
-    if (toolCalls && toolCalls.length > 0) {
-        await logCalendarToolCallsActivity({
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentPermanentId: runtime.agentId,
-            toolCalls,
-        });
-    }
-
-    await persistOpenAiCompatibilityFrozenChat({
-        isEnabled: !runtime.isPrivateModeEnabled,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        agentId: runtime.agentId,
-        chatId: promptContext.persistedFrozenChatId,
-        messages: parsedRequest.messages,
-        assistantContent: responseContentWithSuffix,
-        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
-    });
-    await persistHandleChatCompletionLearnedAgentSource({
-        runtime,
-        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
-    });
-}
-
-/**
- * Applies append-only self-learning updates when the current branch should persist them.
- */
-async function persistHandleChatCompletionLearnedAgentSource(options: {
-    runtime: HandleChatCompletionRuntime;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { runtime, shouldPersistLearnedAgentSource } = options;
-    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
-        return;
-    }
-
-    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
-        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
-        resolvedAgentSourceBeforeLearning: runtime.agentSource,
-        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
-    });
-
-    if (learnedAgentSource !== null) {
-        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
     }
 }
-
-/**
- * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
- */
-async function persistOpenAiCompatibilityFrozenChat(options: {
-    isEnabled: boolean;
-    userId: number | null | undefined;
-    agentId: string;
-    messages: ReadonlyArray<unknown>;
-    chatId?: string;
-    assistantContent?: string;
-    includeAssistantPlaceholder?: boolean;
-    failureMessage: string;
-}): Promise<string | undefined> {
-    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
-        options;
-    if (!isEnabled || !userId) {
-        return undefined;
-    }
-
-    const persistedFrozenChat = await persistFrozenUserChat({
-        userId,
-        agentPermanentId: agentId,
-        source: USER_CHAT_SOURCES.OPENAI_API,
-        chatId,
-        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
-            includeAssistantPlaceholder,
-        }),
-    }).catch((error) => {
-        console.error(failureMessage, error);
-        return null;
-    });
-
-    return persistedFrozenChat?.id;
-}
-
-/**
- * Builds one OpenAI-compatible thread from the earlier request messages.
- */
-function createHandleChatCompletionThread(previousMessages: ReadonlyArray<TODO_any>): ChatMessage[] {
-    return previousMessages.map((message: TODO_any, index: number) => ({
-        id: `msg-${index}`,
-        sender: message.role === 'assistant' ? 'agent' : 'user',
-        content: message.content,
-        isComplete: true,
-        createdAt: $getCurrentDate(),
-    }));
-}
-
-/**
- * Appends system messages to the agent source as dynamic `CONTEXT`.
- */
-function appendSystemMessagesToAgentSource(
-    agentSource: string_book,
-    messages: ReadonlyArray<TODO_any>,
-): {
-    agentSourceWithContext: string_book;
-    hasDynamicContext: boolean;
-} {
-    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
-    if (systemMessages.length === 0) {
-        return {
-            agentSourceWithContext: agentSource,
-            hasDynamicContext: false,
-        };
-    }
-
-    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
-    return {
-        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
-        hasDynamicContext: true,
-    };
-}
-
-/**
- * Waits for any running preparation job of the current agent before executing the chat call.
- */
-async function waitForHandleChatCompletionAgentPreparation(options: {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    agentId: string;
-    agentHash: string;
-}): Promise<void> {
-    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
-    const preparationWaitResult = await waitForRunningAgentPreparation({
-        tablePrefix,
-        agentPermanentId: options.agentId,
-        fingerprint: options.agentHash,
-        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    });
-
-    if (preparationWaitResult !== 'not_running') {
-        console.info('[pre-index]', 'openai_chat_wait_result', {
-            tablePrefix,
-            agentPermanentId: options.agentId,
-            agentHash: options.agentHash,
-            preparationWaitResult,
-        });
-    }
-}
-
-/**
- * Resolves request-scoped project, calendar, and email credentials for the current agent.
- */
-async function resolveHandleChatCompletionCredentials(options: {
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    agentId: string;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-}): Promise<{
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-}> {
-    const userId = options.currentUserIdentity?.userId;
-    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
-        resolveUseProjectGithubToken({
-            userId,
-            agentPermanentId: options.agentId,
-        }),
-        options.calendarConnections.length > 0
-            ? resolveUseCalendarGoogleToken({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-        options.useEmailConfiguration.isEnabled
-            ? resolveUseEmailSmtpCredential({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-    ]);
-
-    return {
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-    };
-}
-
-/**
- * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
- */
-async function createHandleChatCompletionAgent(options: {
-    agentName: string;
-    agentSource: string_book;
-    agentId: string;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    localServerUrl: string;
-    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
-    hasDynamicContext: boolean;
-    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
-}): Promise<Agent> {
-    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
-    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
-    const runtimeAgentReferenceResolver = options.hasDynamicContext
-        ? createBookScopedAgentReferenceResolver({
-              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
-              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
-              localServerUrl: options.localServerUrl,
-              fallbackResolver: options.baseAgentReferenceResolver,
-          })
-        : undefined;
-    const agentModelRequirements =
-        options.preparedAgentModelRequirements?.modelRequirements ||
-        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
-            agentReferenceResolver: runtimeAgentReferenceResolver,
-            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
-        }));
-
-    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
-        options.agentSource,
-        options.resolvedAgentContext.resolvedAgentName,
-        await baseOpenAiToolsPromise,
-        {
-            includeDynamicContext: true,
-            agentId: options.agentId,
-            modelRequirements: agentModelRequirements,
-        },
-    );
-
-    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
-        agentName: options.agentName,
-        assistantCacheKey: agentKitResult.assistantCacheKey,
-        vectorStoreHash: agentKitResult.vectorStoreHash,
-        vectorStoreId: agentKitResult.vectorStoreId,
-    });
-
-    return new Agent({
-        agentSource: options.agentSource,
-        executionTools: {
-            llm: agentKitResult.tools,
-        },
-        precomputedModelRequirements: agentModelRequirements,
-        assistantPreparationMode: 'external',
-        isVerbose: true,
-        teacherAgent: null,
-    });
-}
-
-/**
- * Creates one OpenAI-compatible streaming chunk payload.
- */
-function createHandleChatCompletionChunk(options: {
-    runId: string;
-    created: number;
-    model: TODO_any;
-    delta: Record<string, unknown>;
-    finishReason: 'stop' | null;
-}) {
-    return {
-        id: options.runId,
-        object: 'chat.completion.chunk',
-        created: options.created,
-        model: options.model,
-        choices: [
-            {
-                index: 0,
-                delta: options.delta,
-                finish_reason: options.finishReason,
-            },
-        ],
-    };
-}
-
-/**
- * Creates one consistent OpenAI-compatible error response.
- */
-function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
-    return NextResponse.json(
-        {
-            error: {
-                message,
-                type,
-            },
-        },
-        { status },
-    );
-}
-
-// TODO: [🈹] Maybe move chat thread handling here
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f49a4fa80fa21f140e25de59596e5546d62e3f7
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
@@ -0,0 +1,18 @@
+import { NextResponse } from 'next/server';
+
+/**
+ * Creates one consistent OpenAI-compatible error response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
+    return NextResponse.json(
+        {
+            error: {
+                message,
+                type,
+            },
+        },
+        { status },
+    );
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b1979f4621921bc664fed75a05851c6fcb8fba6d
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
@@ -0,0 +1,120 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import { appendMessageSuffix } from '@/src/utils/chat/messageSuffix';
+import type { UncertainNumber, Usage, UsageCounts } from '@promptbook-local/types';
+import { NextResponse } from 'next/server';
+import { computeUsageCounts } from '../../../../../src/execution/utils/computeUsageCounts';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the non-streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionJsonResponse(options: {
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Promise<NextResponse> {
+    const { title, parsedRequest, runtime, promptContext } = options;
+    const result = await runtime.agent.callChatModel(promptContext.prompt);
+    const normalizedResponse = ensureNonEmptyChatContent({
+        content: result.content,
+        context: title,
+    });
+    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+
+    await finalizeHandleChatCompletionResult({
+        responseContentWithSuffix,
+        usage: result.usage,
+        toolCalls: result.toolCalls,
+        parsedRequest,
+        runtime,
+        promptContext,
+        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
+    });
+
+    return NextResponse.json({
+        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
+        object: 'chat.completion',
+        created: Math.floor(Date.now() / 1000),
+        model: parsedRequest.model || 'promptbook-agent',
+        choices: [
+            {
+                index: 0,
+                message: {
+                    role: 'assistant',
+                    content: responseContentWithSuffix,
+                },
+                finish_reason: 'stop',
+            },
+        ],
+        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
+    });
+}
+
+/**
+ * Falls back to the estimated value when the original token count is unknown.
+ *
+ * @param tokenCount - Token count reported by the execution tools.
+ * @param fallbackValue - Estimated token count based on text length.
+ * @returns Token count to report in the OpenAI-compatible response.
+ */
+function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
+    if (tokenCount.value === 0 && tokenCount.isUncertain) {
+        return {
+            value: fallbackValue,
+            isUncertain: true,
+        };
+    }
+
+    return tokenCount;
+}
+
+/**
+ * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
+ *
+ * @param promptContent - Prompt content used for the request.
+ * @param completionContent - Assistant response content.
+ * @param usage - Native Promptbook usage details.
+ * @returns Usage payload for compatibility responses.
+ */
+function createCompatibilityUsage(
+    promptContent: string,
+    completionContent: string,
+    usage: Usage,
+): {
+    prompt_tokens: number;
+    completion_tokens: number;
+    total_tokens: number;
+    details: Usage;
+} {
+    const promptCounts = computeUsageCounts(promptContent);
+    const completionCounts = computeUsageCounts(completionContent);
+    const inputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
+        ...promptCounts,
+    };
+    const outputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
+        ...completionCounts,
+    };
+
+    const promptTokens = inputUsage.tokensCount.value;
+    const completionTokens = outputUsage.tokensCount.value;
+
+    return {
+        prompt_tokens: promptTokens,
+        completion_tokens: completionTokens,
+        total_tokens: promptTokens + completionTokens,
+        details: {
+            price: usage.price,
+            duration: usage.duration,
+            input: inputUsage,
+            output: outputUsage,
+        },
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..7aac8141a8b1237ce36430b4b4598a618f0383ac
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
@@ -0,0 +1,111 @@
+import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
+import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
+import type { ChatMessage, ChatPrompt } from '@promptbook-local/types';
+import { $getCurrentDate } from '@promptbook-local/utils';
+import type { NextRequest } from 'next/server';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Prompt, history recorder, and persistence context created for one chat call.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionPromptContext = {
+    prompt: ChatPrompt;
+    userMessageHash: string;
+    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
+    persistedFrozenChatId?: string;
+};
+
+/**
+ * Creates prompt and persistence helpers for one chat completion request.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionPromptContext(options: {
+    request: NextRequest;
+    title: string;
+    apiKey: string | null;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+}): Promise<HandleChatCompletionPromptContext> {
+    const { request, title, apiKey, parsedRequest, runtime } = options;
+    const timezone = request.headers.get('x-timezone') || 'UTC';
+    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
+    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
+    const recordChatHistoryMessage = await createChatHistoryRecorder({
+        request,
+        agentIdentifier: runtime.agentId,
+        agentHash: runtime.agentHash,
+        source: 'OPENAI_API_COMPATIBILITY',
+        apiKey,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        isEnabled: !runtime.isPrivateModeEnabled,
+    });
+    const userMessageHash = await recordChatHistoryMessage({
+        message: {
+            role: 'USER',
+            content: lastMessage.content,
+        },
+        previousMessageHash: null,
+    });
+    const promptParameters = composePromptParametersWithMemoryContext({
+        baseParameters: {
+            ...parsedRequest.incomingParameters,
+            timezone,
+        },
+        currentUserIdentity: runtime.currentUserIdentity,
+        agentPermanentId: runtime.agentId,
+        agentName: runtime.resolvedAgentContext.resolvedAgentName,
+        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
+        projectRepositories: runtime.projectRepositories,
+        projectGithubToken: runtime.projectGithubToken,
+        emailSmtpCredential: runtime.emailSmtpCredential,
+        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
+        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
+        calendarConnections: runtime.calendarConnections,
+    });
+    const prompt: ChatPrompt = {
+        title,
+        content: lastMessage.content,
+        modelRequirements: {
+            modelVariant: 'CHAT',
+            responseFormat: parsedRequest.responseFormat,
+            toolChoice: parsedRequest.runtimeToolChoice,
+        },
+        parameters: promptParameters,
+        thread,
+        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
+    };
+
+    return {
+        prompt,
+        userMessageHash,
+        recordChatHistoryMessage,
+        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
+            isEnabled: !runtime.isPrivateModeEnabled,
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentId: runtime.agentId,
+            messages: parsedRequest.messages,
+            includeAssistantPlaceholder: true,
+            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
+        }),
+    };
+}
+
+/**
+ * Builds one OpenAI-compatible thread from the earlier request messages.
+ */
+function createHandleChatCompletionThread(
+    previousMessages: HandleChatCompletionParsedRequest['threadMessages'],
+): Array<ChatMessage> {
+    return previousMessages.map((message, index) => ({
+        id: `msg-${index}`,
+        sender: message.role === 'assistant' ? 'agent' : 'user',
+        content: message.content,
+        isComplete: true,
+        createdAt: $getCurrentDate(),
+    }));
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..85e096027760bc45fb730e74f8ae3bbb721b1029
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
@@ -0,0 +1,162 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import {
+    appendMessageSuffix,
+    createMessageSuffixAppendix,
+    emulateMessageSuffixStreaming,
+} from '@/src/utils/chat/messageSuffix';
+import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
+import { encodeChatStreamWhitespaceForTransport } from '../../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
+import { prepareToolCallsForStreaming } from '../toolCallStreaming';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionStreamResponse(options: {
+    request: Request;
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Response {
+    const { request, title, parsedRequest, runtime, promptContext } = options;
+    const encoder = new TextEncoder();
+    const responseModel = parsedRequest.model || 'promptbook-agent';
+    const readableStream = new ReadableStream({
+        async start(controller) {
+            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
+            const created = Math.floor(Date.now() / 1000);
+            const emitSsePayload = (payload: unknown) => {
+                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
+            };
+            const emitDeltaChunk = (deltaContent: string) => {
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {
+                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
+                        },
+                        finishReason: null,
+                    }),
+                );
+            };
+
+            emitSsePayload(
+                createHandleChatCompletionChunk({
+                    runId,
+                    created,
+                    model: responseModel,
+                    delta: {
+                        role: 'assistant',
+                        content: '',
+                    },
+                    finishReason: null,
+                }),
+            );
+
+            let hasMeaningfulDelta = false;
+
+            try {
+                const handleStreamChunk = createChatStreamHandler({
+                    onDelta: (deltaContent) => {
+                        if (deltaContent.trim().length > 0) {
+                            hasMeaningfulDelta = true;
+                        }
+                        emitDeltaChunk(deltaContent);
+                    },
+                    onToolCalls: (toolCalls) => {
+                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
+                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
+                    },
+                });
+                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
+                    signal: request.signal,
+                });
+                const normalizedResponse = ensureNonEmptyChatContent({
+                    content: result.content,
+                    context: title,
+                });
+
+                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
+                    emitDeltaChunk(normalizedResponse.content);
+                }
+
+                const messageSuffixAppendix = createMessageSuffixAppendix(
+                    normalizedResponse.content,
+                    runtime.messageSuffix,
+                );
+                if (messageSuffixAppendix) {
+                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
+                        emitDeltaChunk(delta);
+                    });
+                }
+
+                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+                await finalizeHandleChatCompletionResult({
+                    responseContentWithSuffix,
+                    usage: result.usage,
+                    toolCalls: result.toolCalls,
+                    parsedRequest,
+                    runtime,
+                    promptContext,
+                    shouldPersistLearnedAgentSource: true,
+                });
+
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {},
+                        finishReason: 'stop',
+                    }),
+                );
+                controller.enqueue(encoder.encode('[DONE]'));
+            } catch (error) {
+                console.error('Error during streaming:', error);
+                controller.error(error);
+            }
+            controller.close();
+        },
+    });
+
+    return new Response(readableStream, {
+        headers: {
+            'Content-Type': 'text/event-stream',
+            'Cache-Control': 'no-cache',
+            Connection: 'keep-alive',
+        },
+    });
+}
+
+/**
+ * Creates one OpenAI-compatible streaming chunk payload.
+ */
+function createHandleChatCompletionChunk(options: {
+    runId: string;
+    created: number;
+    model: HandleChatCompletionParsedRequest['model'];
+    delta: Record<string, unknown>;
+    finishReason: 'stop' | null;
+}) {
+    return {
+        id: options.runId,
+        object: 'chat.completion.chunk',
+        created: options.created,
+        model: options.model,
+        choices: [
+            {
+                index: 0,
+                delta: options.delta,
+                finish_reason: options.finishReason,
+            },
+        ],
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
new file mode 100644
index 0000000000000000000000000000000000000000..dd6a7d87ea491df245dbf93d8bcb277a9a10b034
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
@@ -0,0 +1,78 @@
+import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
+import { resolveAppendOnlySelfLearningAgentSource } from '@/src/utils/resolveAppendOnlySelfLearningAgentSource';
+import type { ToolCall, Usage } from '@promptbook-local/types';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Runs the shared side effects after one successful model response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function finalizeHandleChatCompletionResult(options: {
+    responseContentWithSuffix: string;
+    usage: Usage;
+    toolCalls?: ReadonlyArray<ToolCall>;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
+
+    await promptContext.recordChatHistoryMessage({
+        message: {
+            role: 'MODEL',
+            content: responseContentWithSuffix,
+        },
+        previousMessageHash: promptContext.userMessageHash,
+        usage,
+    });
+
+    if (toolCalls && toolCalls.length > 0) {
+        await logCalendarToolCallsActivity({
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentPermanentId: runtime.agentId,
+            toolCalls,
+        });
+    }
+
+    await persistOpenAiCompatibilityFrozenChat({
+        isEnabled: !runtime.isPrivateModeEnabled,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        agentId: runtime.agentId,
+        chatId: promptContext.persistedFrozenChatId,
+        messages: parsedRequest.messages,
+        assistantContent: responseContentWithSuffix,
+        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
+    });
+    await persistHandleChatCompletionLearnedAgentSource({
+        runtime,
+        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
+    });
+}
+
+/**
+ * Applies append-only self-learning updates when the current branch should persist them.
+ */
+async function persistHandleChatCompletionLearnedAgentSource(options: {
+    runtime: HandleChatCompletionRuntime;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { runtime, shouldPersistLearnedAgentSource } = options;
+    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
+        return;
+    }
+
+    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
+        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
+        resolvedAgentSourceBeforeLearning: runtime.agentSource,
+        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
+    });
+
+    if (learnedAgentSource !== null) {
+        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
+    }
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
new file mode 100644
index 0000000000000000000000000000000000000000..58e1c26d7f428c1bdb39b36411b3f75256e3de51
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
@@ -0,0 +1,176 @@
+import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
+import type { LlmToolDefinition, TODO_any } from '@promptbook-local/types';
+import { NextRequest, NextResponse } from 'next/server';
+import type OpenAI from 'openai';
+import { HTTP_STATUS_CODES } from '../../constants';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Definition of open AI chat tool.
+ */
+type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
+
+/**
+ * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionParsedRequest = {
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    threadMessages: ReadonlyArray<TODO_any>;
+    stream: TODO_any;
+    model: TODO_any;
+    responseFormat: TODO_any;
+    runtimeTools?: Array<LlmToolDefinition>;
+    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+    incomingParameters: Record<string, unknown>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Parses and validates the OpenAI-compatible request payload.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function parseHandleChatCompletionRequest(options: {
+    body: TODO_any;
+    agentNameFromParams?: string;
+    request: NextRequest;
+}): HandleChatCompletionParsedRequest | NextResponse {
+    const { body, agentNameFromParams, request } = options;
+    const {
+        messages,
+        stream,
+        model,
+        response_format: responseFormat,
+        tools: rawTools,
+        tool_choice: toolChoice,
+        parameters: rawParameters = {},
+    } = body;
+    const agentName = (agentNameFromParams || model) as string | undefined;
+    if (!agentName) {
+        return createHandleChatCompletionErrorResponse(
+            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    if (!messages || !Array.isArray(messages) || messages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array is required and cannot be empty.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
+    if (threadMessages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array must contain at least one non-system message.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    return {
+        agentName,
+        messages,
+        threadMessages,
+        stream,
+        model,
+        responseFormat,
+        runtimeTools: convertOpenAiTools(rawTools),
+        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
+        incomingParameters:
+            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
+                ? (rawParameters as Record<string, unknown>)
+                : {},
+        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
+    };
+}
+
+/**
+ * Handles convert open Ai tools.
+ */
+function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
+    if (!Array.isArray(rawTools)) {
+        return undefined;
+    }
+
+    const converted = rawTools
+        .map((tool) => convertOpenAiTool(tool))
+        .filter((tool): tool is LlmToolDefinition => tool !== null);
+
+    return converted.length > 0 ? converted : undefined;
+}
+
+/**
+ * Handles convert open Ai tool.
+ */
+function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
+    const tool = rawTool as OpenAIChatToolDefinition;
+    if (!tool || tool.type !== 'function') {
+        return null;
+    }
+
+    const functionDefinition = tool.function;
+    if (!functionDefinition) {
+        return null;
+    }
+
+    const { name, description, parameters } = functionDefinition;
+    if (typeof name !== 'string' || name.trim().length === 0) {
+        return null;
+    }
+
+    const parameterSchema = parameters ?? {};
+    if (parameterSchema.type !== 'object') {
+        return null;
+    }
+
+    const properties = parameterSchema.properties ?? {};
+    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
+
+    for (const [key, value] of Object.entries(properties)) {
+        if (!value || typeof value !== 'object') {
+            continue;
+        }
+
+        normalizedProperties[key] = {
+            type: typeof value.type === 'string' ? value.type : 'string',
+            description: typeof value.description === 'string' ? value.description : undefined,
+        };
+    }
+
+    const required =
+        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
+            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
+            : undefined;
+
+    return {
+        name,
+        description: typeof description === 'string' ? description : '',
+        parameters: {
+            type: 'object',
+            properties: normalizedProperties,
+            required,
+            additionalProperties:
+                typeof parameterSchema.additionalProperties === 'boolean'
+                    ? parameterSchema.additionalProperties
+                    : undefined,
+        },
+    };
+}
+
+/**
+ * Parses open Ai tool choice.
+ */
+function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
+    if (value === undefined || value === null) {
+        return undefined;
+    }
+
+    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
new file mode 100644
index 0000000000000000000000000000000000000000..60e5f1b008bcf5a6a1836c06c4c3d9ebcb909a9f
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
@@ -0,0 +1,163 @@
+import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
+import type { ChatMessage } from '@promptbook-local/types';
+
+/**
+ * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
+ */
+type OpenAiCompatibilityMessageLike = {
+    role?: unknown;
+    content?: unknown;
+};
+
+/**
+ * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function persistOpenAiCompatibilityFrozenChat(options: {
+    isEnabled: boolean;
+    userId: number | null | undefined;
+    agentId: string;
+    messages: ReadonlyArray<unknown>;
+    chatId?: string;
+    assistantContent?: string;
+    includeAssistantPlaceholder?: boolean;
+    failureMessage: string;
+}): Promise<string | undefined> {
+    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
+        options;
+    if (!isEnabled || !userId) {
+        return undefined;
+    }
+
+    const persistedFrozenChat = await persistFrozenUserChat({
+        userId,
+        agentPermanentId: agentId,
+        source: USER_CHAT_SOURCES.OPENAI_API,
+        chatId,
+        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
+            includeAssistantPlaceholder,
+        }),
+    }).catch((error) => {
+        console.error(failureMessage, error);
+        return null;
+    });
+
+    return persistedFrozenChat?.id;
+}
+
+/**
+ * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
+ */
+function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
+    if (typeof value === 'string') {
+        return value;
+    }
+
+    if (Array.isArray(value)) {
+        return value
+            .map((part) => {
+                if (typeof part === 'string') {
+                    return part;
+                }
+
+                if (!part || typeof part !== 'object') {
+                    return '';
+                }
+
+                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
+                if (typeof partLike.text === 'string') {
+                    return partLike.text;
+                }
+
+                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
+                    return '[Image input]';
+                }
+
+                if (partLike.image_url) {
+                    return '[Image input]';
+                }
+
+                return '';
+            })
+            .filter(Boolean)
+            .join('\n');
+    }
+
+    if (value === null || value === undefined) {
+        return '';
+    }
+
+    try {
+        return JSON.stringify(value, null, 2);
+    } catch {
+        return String(value);
+    }
+}
+
+/**
+ * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ */
+function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
+    if (typeof role !== 'string') {
+        return 'USER';
+    }
+
+    switch (role) {
+        case 'assistant':
+            return 'AGENT';
+        case 'system':
+            return 'SYSTEM';
+        case 'tool':
+            return 'TOOL';
+        case 'developer':
+            return 'SYSTEM';
+        default:
+            return 'USER';
+    }
+}
+
+/**
+ * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
+ */
+function createFrozenOpenAiCompatibilityMessages(
+    rawMessages: ReadonlyArray<unknown>,
+    assistantContent?: string,
+    options: {
+        includeAssistantPlaceholder?: boolean;
+    } = {},
+): Array<ChatMessage> {
+    const startedAt = Date.now();
+    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
+        const message = rawMessage as OpenAiCompatibilityMessageLike;
+
+        return {
+            id: `openai-frozen-${index}`,
+            sender: mapOpenAiCompatibilityRoleToSender(message.role),
+            content: normalizeOpenAiCompatibilityMessageContent(message.content),
+            isComplete: true,
+            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage;
+    });
+
+    if (assistantContent !== undefined) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: assistantContent,
+            isComplete: true,
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    } else if (options.includeAssistantPlaceholder) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: '',
+            isComplete: false,
+            lifecycleState: 'running',
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    }
+
+    return messages;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fd44b7ea5a87168725a9e99784038137c96537ab
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
@@ -0,0 +1,305 @@
+import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
+import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
+import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
+import type { string_book, TODO_any } from '@promptbook-local/types';
+import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
+import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
+import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
+import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
+import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
+import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
+import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
+import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
+import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
+import {
+    createBookScopedAgentReferenceResolver,
+    parseBookScopedAgentIdentifier,
+} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
+import { resolveMessageSuffixFromAgentSource } from '@/src/utils/chat/messageSuffix';
+import { NextRequest, NextResponse } from 'next/server';
+import { isAgentDeleted } from '../../app/agents/[agentName]/_utils';
+import {
+    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    resolveAgentCollectionTablePrefix,
+    waitForRunningAgentPreparation,
+} from '../agentPreparation';
+import { resolveCachedServerAgentContext, resolveCachedServerAgentModelRequirements } from '../cachedServerAgentRuntime';
+import { AgentKitCacheManager } from '../cache/AgentKitCacheManager';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Runtime state resolved before the chat model is executed.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionRuntime = {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    agent: Agent;
+    agentSource: string_book;
+    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
+    messageSuffix: string | null;
+    agentId: string;
+    agentHash: string;
+    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Resolves agent/runtime dependencies needed before prompt execution.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function resolveHandleChatCompletionRuntime(options: {
+    request: NextRequest;
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    isPrivateModeEnabled: boolean;
+}): Promise<HandleChatCompletionRuntime | NextResponse> {
+    const { request, agentName, messages, isPrivateModeEnabled } = options;
+    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
+    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
+
+    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
+        return createHandleChatCompletionErrorResponse(
+            'This agent has been deleted. You can restore it from the Recycle Bin.',
+            'agent_deleted',
+            410,
+        );
+    }
+
+    const [collection, baseAgentReferenceResolver] = await Promise.all([
+        $provideAgentCollectionForServer(),
+        $provideAgentReferenceResolver(),
+    ]);
+    const localServerUrl = new URL(request.url).origin;
+    const resolvedAgentContext = await resolveCachedServerAgentContext({
+        collection,
+        agentIdentifier: agentName,
+        localServerUrl,
+        fallbackResolver: baseAgentReferenceResolver,
+    }).catch(() => null);
+
+    if (!resolvedAgentContext?.resolvedAgentSource) {
+        return createHandleChatCompletionErrorResponse(`Agent '${agentName}' not found.`, 'invalid_request_error', 404);
+    }
+
+    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
+    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
+    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
+    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
+    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
+    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
+    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
+    agentSource = agentSourceWithContext;
+
+    const preparedAgentModelRequirements = !hasDynamicContext
+        ? await resolveCachedServerAgentModelRequirements({
+              resolvedAgentContext,
+              localServerUrl,
+              fallbackResolver: baseAgentReferenceResolver,
+          })
+        : null;
+    const agentId = resolvedAgentContext.parentAgentPermanentId;
+    const agentHash = computeAgentHash(agentSource);
+    await waitForHandleChatCompletionAgentPreparation({
+        collection,
+        agentId,
+        agentHash,
+    });
+
+    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
+    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
+        await resolveHandleChatCompletionCredentials({
+            currentUserIdentity,
+            agentId,
+            calendarConnections,
+            useEmailConfiguration,
+        });
+    const agent = await createHandleChatCompletionAgent({
+        agentName,
+        agentSource,
+        agentId,
+        resolvedAgentContext,
+        localServerUrl,
+        baseAgentReferenceResolver,
+        hasDynamicContext,
+        preparedAgentModelRequirements,
+    });
+
+    return {
+        collection,
+        resolvedAgentContext,
+        agent,
+        agentSource,
+        unresolvedAgentSource,
+        messageSuffix,
+        agentId,
+        agentHash,
+        projectRepositories,
+        calendarConnections,
+        useEmailConfiguration,
+        currentUserIdentity,
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+        isPrivateModeEnabled,
+    };
+}
+
+/**
+ * Appends system messages to the agent source as dynamic `CONTEXT`.
+ */
+function appendSystemMessagesToAgentSource(
+    agentSource: string_book,
+    messages: ReadonlyArray<TODO_any>,
+): {
+    agentSourceWithContext: string_book;
+    hasDynamicContext: boolean;
+} {
+    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
+    if (systemMessages.length === 0) {
+        return {
+            agentSourceWithContext: agentSource,
+            hasDynamicContext: false,
+        };
+    }
+
+    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
+    return {
+        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
+        hasDynamicContext: true,
+    };
+}
+
+/**
+ * Waits for any running preparation job of the current agent before executing the chat call.
+ */
+async function waitForHandleChatCompletionAgentPreparation(options: {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    agentId: string;
+    agentHash: string;
+}): Promise<void> {
+    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
+    const preparationWaitResult = await waitForRunningAgentPreparation({
+        tablePrefix,
+        agentPermanentId: options.agentId,
+        fingerprint: options.agentHash,
+        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    });
+
+    if (preparationWaitResult !== 'not_running') {
+        console.info('[pre-index]', 'openai_chat_wait_result', {
+            tablePrefix,
+            agentPermanentId: options.agentId,
+            agentHash: options.agentHash,
+            preparationWaitResult,
+        });
+    }
+}
+
+/**
+ * Resolves request-scoped project, calendar, and email credentials for the current agent.
+ */
+async function resolveHandleChatCompletionCredentials(options: {
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    agentId: string;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+}): Promise<{
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+}> {
+    const userId = options.currentUserIdentity?.userId;
+    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
+        resolveUseProjectGithubToken({
+            userId,
+            agentPermanentId: options.agentId,
+        }),
+        options.calendarConnections.length > 0
+            ? resolveUseCalendarGoogleToken({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+        options.useEmailConfiguration.isEnabled
+            ? resolveUseEmailSmtpCredential({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+    ]);
+
+    return {
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+    };
+}
+
+/**
+ * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
+ */
+async function createHandleChatCompletionAgent(options: {
+    agentName: string;
+    agentSource: string_book;
+    agentId: string;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    localServerUrl: string;
+    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
+    hasDynamicContext: boolean;
+    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
+}): Promise<Agent> {
+    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
+    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
+    const runtimeAgentReferenceResolver = options.hasDynamicContext
+        ? createBookScopedAgentReferenceResolver({
+              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
+              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
+              localServerUrl: options.localServerUrl,
+              fallbackResolver: options.baseAgentReferenceResolver,
+          })
+        : undefined;
+    const agentModelRequirements =
+        options.preparedAgentModelRequirements?.modelRequirements ||
+        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
+            agentReferenceResolver: runtimeAgentReferenceResolver,
+            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
+        }));
+
+    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
+        options.agentSource,
+        options.resolvedAgentContext.resolvedAgentName,
+        await baseOpenAiToolsPromise,
+        {
+            includeDynamicContext: true,
+            agentId: options.agentId,
+            modelRequirements: agentModelRequirements,
+        },
+    );
+
+    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
+        agentName: options.agentName,
+        assistantCacheKey: agentKitResult.assistantCacheKey,
+        vectorStoreHash: agentKitResult.vectorStoreHash,
+        vectorStoreId: agentKitResult.vectorStoreId,
+    });
+
+    return new Agent({
+        agentSource: options.agentSource,
+        executionTools: {
+            llm: agentKitResult.tools,
+        },
+        precomputedModelRequirements: agentModelRequirements,
+        assistantPreparationMode: 'external',
+        isVerbose: true,
+        teacherAgent: null,
+    });
+}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 474fd839d14b2bdab324e5f59fce0b15ddf95afd..bc9cfde73332af5d107ad35780a31963bc4e68e9
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,8 @@
+-   Refactored Agents Server `handleChatCompletion.ts` into a thinner chat-completion facade plus focused private helper modules without changing external behavior:
+
+    -   Extracted OpenAI-compatible request parsing, runtime/agent resolution, prompt-context creation, frozen-chat persistence, shared post-response side effects, and the streaming vs non-streaming response branches into dedicated private files behind the existing `handleChatCompletion` entrypoint.
+    -   Kept the existing API-key validation, deleted-agent handling, dynamic system-context injection, AgentKit cache usage, private-mode behavior, frozen audit snapshots, self-learning persistence, streaming chunk format, and OpenAI-compatible JSON response shape intact while reducing responsibility density in the main file.
+
 -   Refactored Agents Server `errorMessages.ts` into smaller focused helpers without changing external behavior:
 
     -   Split the branching-heavy `categorizeError` flow into dedicated API-type, native-error-message, and status-code classification helpers while preserving the existing precedence.


thinking
**Inspecting tsconfig**

I think it's important to take a closer look at the tsconfig file. I'll want to check its settings to ensure everything is configured correctly for TypeScript. I'm curious about what options are included, such as compiler options and paths. I'll dive into specific details, maybe look for any tweaks that could optimize the setup. Getting this right can make a big difference in the overall development process!
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content apps/agents-server/tsconfig.json -TotalCount 80' in C:/Users/me/work/ai/promptbook succeeded in 470ms:
{
    "compilerOptions": {
        "target": "esnext",
        "lib": ["dom", "dom.iterable", "esnext"],
        "allowJs": true,
        "skipLibCheck": true,
        "strict": true,
        "noEmit": true,
        "esModuleInterop": true,
        "module": "esnext",
        "moduleResolution": "bundler",
        "resolveJsonModule": true,
        "isolatedModules": true,
        "jsx": "preserve",
        "incremental": true,
        "plugins": [
            {
                "name": "next"
            }
        ],
file update:
diff --git a/apps/agents-server/src/utils/handleChatCompletion.ts b/apps/agents-server/src/utils/handleChatCompletion.ts
index 9c96d36fad9cf24bf26d89f57372332add4e4595..cff56fafd6cfa90bb4094f9f202c4f01f6f74af1
--- a/apps/agents-server/src/utils/handleChatCompletion.ts
+++ b/apps/agents-server/src/utils/handleChatCompletion.ts
@@ -1,392 +1,16 @@
-import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
-import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
-import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
-import {
-    createBookScopedAgentReferenceResolver,
-    parseBookScopedAgentIdentifier,
-} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
-import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
-import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
-import {
-    appendMessageSuffix,
-    createMessageSuffixAppendix,
-    emulateMessageSuffixStreaming,
-    resolveMessageSuffixFromAgentSource,
-} from '@/src/utils/chat/messageSuffix';
-import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
-import { prepareToolCallsForStreaming } from './toolCallStreaming';
-import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
-import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
-import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
-import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
-import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
-import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
-import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
-import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
-import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
-import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
-import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
-import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
-import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
-import type {
-    ChatMessage,
-    ChatPrompt,
-    LlmToolDefinition,
-    string_book,
-    ToolCall,
-    TODO_any,
-    UncertainNumber,
-    Usage,
-    UsageCounts,
-} from '@promptbook-local/types';
-import { $getCurrentDate } from '@promptbook-local/utils';
 import { NextRequest, NextResponse } from 'next/server';
-import type OpenAI from 'openai';
-import { computeUsageCounts } from '../../../../src/execution/utils/computeUsageCounts';
-import { encodeChatStreamWhitespaceForTransport } from '../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
-import { isAgentDeleted } from '../app/agents/[agentName]/_utils';
 import { HTTP_STATUS_CODES } from '../constants';
-import { AgentKitCacheManager } from './cache/AgentKitCacheManager';
 import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
+import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
+import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
+import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
+import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
+import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
 import { validateApiKey } from './validateApiKey';
-import {
-    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    resolveAgentCollectionTablePrefix,
-    waitForRunningAgentPreparation,
-} from './agentPreparation';
-import {
-    resolveCachedServerAgentContext,
-    resolveCachedServerAgentModelRequirements,
-} from './cachedServerAgentRuntime';
-import { resolveAppendOnlySelfLearningAgentSource } from './resolveAppendOnlySelfLearningAgentSource';
 
 /**
- * Falls back to the estimated value when the original token count is unknown.
- *
- * @param tokenCount - Token count reported by the execution tools.
- * @param fallbackValue - Estimated token count based on text length.
- * @returns Token count to report in the OpenAI-compatible response.
- */
-function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
-    if (tokenCount.value === 0 && tokenCount.isUncertain) {
-        return {
-            value: fallbackValue,
-            isUncertain: true,
-        };
-    }
-
-    return tokenCount;
-}
-
-/**
- * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
- *
- * @param promptContent - Prompt content used for the request.
- * @param completionContent - Assistant response content.
- * @param usage - Native Promptbook usage details.
- * @returns Usage payload for compatibility responses.
- */
-function createCompatibilityUsage(
-    promptContent: string,
-    completionContent: string,
-    usage: Usage,
-): {
-    prompt_tokens: number;
-    completion_tokens: number;
-    total_tokens: number;
-    details: Usage;
-} {
-    const promptCounts = computeUsageCounts(promptContent);
-    const completionCounts = computeUsageCounts(completionContent);
-    const inputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
-        ...promptCounts,
-    };
-    const outputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
-        ...completionCounts,
-    };
-
-    const promptTokens = inputUsage.tokensCount.value;
-    const completionTokens = outputUsage.tokensCount.value;
-
-    return {
-        prompt_tokens: promptTokens,
-        completion_tokens: completionTokens,
-        total_tokens: promptTokens + completionTokens,
-        details: {
-            price: usage.price,
-            duration: usage.duration,
-            input: inputUsage,
-            output: outputUsage,
-        },
-    };
-}
-
-/**
- * Definition of open AI chat tool.
- */
-type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
-
-/**
- * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
- */
-type OpenAiCompatibilityMessageLike = {
-    role?: unknown;
-    content?: unknown;
-};
-
-/**
- * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
- */
-type HandleChatCompletionParsedRequest = {
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    threadMessages: ReadonlyArray<TODO_any>;
-    stream: TODO_any;
-    model: TODO_any;
-    responseFormat: TODO_any;
-    runtimeTools?: Array<LlmToolDefinition>;
-    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-    incomingParameters: Record<string, unknown>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Runtime state resolved before the chat model is executed.
- */
-type HandleChatCompletionRuntime = {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    agent: Agent;
-    agentSource: string_book;
-    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
-    messageSuffix: string | null;
-    agentId: string;
-    agentHash: string;
-    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Prompt, history recorder, and persistence context created for one chat call.
- */
-type HandleChatCompletionPromptContext = {
-    prompt: ChatPrompt;
-    userMessageHash: string;
-    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
-    persistedFrozenChatId?: string;
-};
-
-/**
- * Handles convert open Ai tools.
- */
-function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
-    if (!Array.isArray(rawTools)) {
-        return undefined;
-    }
-
-    const converted = rawTools
-        .map((tool) => convertOpenAiTool(tool))
-        .filter((tool): tool is LlmToolDefinition => tool !== null);
-
-    return converted.length > 0 ? converted : undefined;
-}
-
-/**
- * Handles convert open Ai tool.
- */
-function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
-    const tool = rawTool as OpenAIChatToolDefinition;
-    if (!tool || tool.type !== 'function') {
-        return null;
-    }
-
-    const functionDefinition = tool.function;
-    if (!functionDefinition) {
-        return null;
-    }
-
-    const { name, description, parameters } = functionDefinition;
-    if (typeof name !== 'string' || name.trim().length === 0) {
-        return null;
-    }
-
-    const parameterSchema = parameters ?? {};
-    if (parameterSchema.type !== 'object') {
-        return null;
-    }
-
-    const properties = parameterSchema.properties ?? {};
-    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
-
-    for (const [key, value] of Object.entries(properties)) {
-        if (!value || typeof value !== 'object') {
-            continue;
-        }
-
-        normalizedProperties[key] = {
-            type: typeof value.type === 'string' ? value.type : 'string',
-            description: typeof value.description === 'string' ? value.description : undefined,
-        };
-    }
-
-    const required =
-        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
-            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
-            : undefined;
-
-    return {
-        name,
-        description: typeof description === 'string' ? description : '',
-        parameters: {
-            type: 'object',
-            properties: normalizedProperties,
-            required,
-            additionalProperties:
-                typeof parameterSchema.additionalProperties === 'boolean'
-                    ? parameterSchema.additionalProperties
-                    : undefined,
-        },
-    };
-}
-
-/**
- * Parses open Ai tool choice.
- */
-function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
-    if (value === undefined || value === null) {
-        return undefined;
-    }
-
-    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-}
-
-/**
- * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
- */
-function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
-    if (typeof value === 'string') {
-        return value;
-    }
-
-    if (Array.isArray(value)) {
-        return value
-            .map((part) => {
-                if (typeof part === 'string') {
-                    return part;
-                }
-
-                if (!part || typeof part !== 'object') {
-                    return '';
-                }
-
-                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
-                if (typeof partLike.text === 'string') {
-                    return partLike.text;
-                }
-
-                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
-                    return '[Image input]';
-                }
-
-                if (partLike.image_url) {
-                    return '[Image input]';
-                }
-
-                return '';
-            })
-            .filter(Boolean)
-            .join('\n');
-    }
-
-    if (value === null || value === undefined) {
-        return '';
-    }
-
-    try {
-        return JSON.stringify(value, null, 2);
-    } catch {
-        return String(value);
-    }
-}
-
-/**
- * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ * Handles convert open Ai chat completion requests routed through the Agents Server.
  */
-function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
-    if (typeof role !== 'string') {
-        return 'USER';
-    }
-
-    switch (role) {
-        case 'assistant':
-            return 'AGENT';
-        case 'system':
-            return 'SYSTEM';
-        case 'tool':
-            return 'TOOL';
-        case 'developer':
-            return 'SYSTEM';
-        default:
-            return 'USER';
-    }
-}
-
-/**
- * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
- */
-function createFrozenOpenAiCompatibilityMessages(
-    rawMessages: ReadonlyArray<unknown>,
-    assistantContent?: string,
-    options: {
-        includeAssistantPlaceholder?: boolean;
-    } = {},
-): Array<ChatMessage> {
-    const startedAt = Date.now();
-    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
-        const message = rawMessage as OpenAiCompatibilityMessageLike;
-
-        return {
-            id: `openai-frozen-${index}`,
-            sender: mapOpenAiCompatibilityRoleToSender(message.role),
-            content: normalizeOpenAiCompatibilityMessageContent(message.content),
-            isComplete: true,
-            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage;
-    });
-
-    if (assistantContent !== undefined) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: assistantContent,
-            isComplete: true,
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    } else if (options.includeAssistantPlaceholder) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: '',
-            isComplete: false,
-            lifecycleState: 'running',
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    }
-
-    return messages;
-}
-
-/**
- * Handles chat completion.
- */
 export async function handleChatCompletion(
     request: NextRequest,
     params: { agentName?: string },
@@ -399,7 +23,6 @@
         return versionMismatchResponse;
     }
 
-    // Validate API key explicitly (in addition to middleware)
     const apiKeyValidation = await validateApiKey(request);
     if (!apiKeyValidation.isValid) {
         return NextResponse.json(
@@ -462,724 +85,5 @@
             { error: { message: (error as Error).message || 'Internal Server Error', type: 'server_error' } },
             { status: HTTP_STATUS_CODES.INTERNAL_SERVER_ERROR },
         );
-    }
-}
-
-/**
- * Parses and validates the OpenAI-compatible request payload.
- */
-function parseHandleChatCompletionRequest(options: {
-    body: TODO_any;
-    agentNameFromParams?: string;
-    request: NextRequest;
-}): HandleChatCompletionParsedRequest | NextResponse {
-    const { body, agentNameFromParams, request } = options;
-    const {
-        messages,
-        stream,
-        model,
-        response_format: responseFormat,
-        tools: rawTools,
-        tool_choice: toolChoice,
-        parameters: rawParameters = {},
-    } = body;
-    const agentName = (agentNameFromParams || model) as string | undefined;
-    if (!agentName) {
-        return createHandleChatCompletionErrorResponse(
-            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    if (!messages || !Array.isArray(messages) || messages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array is required and cannot be empty.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
-    if (threadMessages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array must contain at least one non-system message.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    return {
-        agentName,
-        messages,
-        threadMessages,
-        stream,
-        model,
-        responseFormat,
-        runtimeTools: convertOpenAiTools(rawTools),
-        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
-        incomingParameters:
-            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
-                ? (rawParameters as Record<string, unknown>)
-                : {},
-        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
-    };
-}
-
-/**
- * Resolves agent/runtime dependencies needed before prompt execution.
- */
-async function resolveHandleChatCompletionRuntime(options: {
-    request: NextRequest;
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    isPrivateModeEnabled: boolean;
-}): Promise<HandleChatCompletionRuntime | NextResponse> {
-    const { request, agentName, messages, isPrivateModeEnabled } = options;
-    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
-    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
-
-    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
-        return createHandleChatCompletionErrorResponse(
-            'This agent has been deleted. You can restore it from the Recycle Bin.',
-            'agent_deleted',
-            410,
-        );
-    }
-
-    const [collection, baseAgentReferenceResolver] = await Promise.all([
-        $provideAgentCollectionForServer(),
-        $provideAgentReferenceResolver(),
-    ]);
-    const localServerUrl = new URL(request.url).origin;
-    const resolvedAgentContext = await resolveCachedServerAgentContext({
-        collection,
-        agentIdentifier: agentName,
-        localServerUrl,
-        fallbackResolver: baseAgentReferenceResolver,
-    }).catch(() => null);
-
-    if (!resolvedAgentContext?.resolvedAgentSource) {
-        return createHandleChatCompletionErrorResponse(
-            `Agent '${agentName}' not found.`,
-            'invalid_request_error',
-            HTTP_STATUS_CODES.NOT_FOUND,
-        );
-    }
-
-    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
-    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
-    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
-    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
-    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
-    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
-    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
-    agentSource = agentSourceWithContext;
-
-    const preparedAgentModelRequirements = !hasDynamicContext
-        ? await resolveCachedServerAgentModelRequirements({
-              resolvedAgentContext,
-              localServerUrl,
-              fallbackResolver: baseAgentReferenceResolver,
-          })
-        : null;
-    const agentId = resolvedAgentContext.parentAgentPermanentId;
-    const agentHash = computeAgentHash(agentSource);
-    await waitForHandleChatCompletionAgentPreparation({
-        collection,
-        agentId,
-        agentHash,
-    });
-
-    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
-    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
-        await resolveHandleChatCompletionCredentials({
-            currentUserIdentity,
-            agentId,
-            calendarConnections,
-            useEmailConfiguration,
-        });
-    const agent = await createHandleChatCompletionAgent({
-        agentName,
-        agentSource,
-        agentId,
-        resolvedAgentContext,
-        localServerUrl,
-        baseAgentReferenceResolver,
-        hasDynamicContext,
-        preparedAgentModelRequirements,
-    });
-
-    return {
-        collection,
-        resolvedAgentContext,
-        agent,
-        agentSource,
-        unresolvedAgentSource,
-        messageSuffix,
-        agentId,
-        agentHash,
-        projectRepositories,
-        calendarConnections,
-        useEmailConfiguration,
-        currentUserIdentity,
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-        isPrivateModeEnabled,
-    };
-}
-
-/**
- * Creates prompt and persistence helpers for one chat completion request.
- */
-async function createHandleChatCompletionPromptContext(options: {
-    request: NextRequest;
-    title: string;
-    apiKey: string | null;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-}): Promise<HandleChatCompletionPromptContext> {
-    const { request, title, apiKey, parsedRequest, runtime } = options;
-    const timezone = request.headers.get('x-timezone') || 'UTC';
-    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
-    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
-    const recordChatHistoryMessage = await createChatHistoryRecorder({
-        request,
-        agentIdentifier: runtime.agentId,
-        agentHash: runtime.agentHash,
-        source: 'OPENAI_API_COMPATIBILITY',
-        apiKey,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        isEnabled: !runtime.isPrivateModeEnabled,
-    });
-    const userMessageHash = await recordChatHistoryMessage({
-        message: {
-            role: 'USER',
-            content: lastMessage.content,
-        },
-        previousMessageHash: null,
-    });
-    const promptParameters = composePromptParametersWithMemoryContext({
-        baseParameters: {
-            ...parsedRequest.incomingParameters,
-            timezone,
-        },
-        currentUserIdentity: runtime.currentUserIdentity,
-        agentPermanentId: runtime.agentId,
-        agentName: runtime.resolvedAgentContext.resolvedAgentName,
-        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
-        projectRepositories: runtime.projectRepositories,
-        projectGithubToken: runtime.projectGithubToken,
-        emailSmtpCredential: runtime.emailSmtpCredential,
-        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
-        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
-        calendarConnections: runtime.calendarConnections,
-    });
-    const prompt: ChatPrompt = {
-        title,
-        content: lastMessage.content,
-        modelRequirements: {
-            modelVariant: 'CHAT',
-            responseFormat: parsedRequest.responseFormat,
-            toolChoice: parsedRequest.runtimeToolChoice,
-            // We could pass 'model' from body if we wanted to enforce it, but Agent usually has its own config
-        },
-        parameters: promptParameters,
-        thread,
-        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
-    };
-
-    return {
-        prompt,
-        userMessageHash,
-        recordChatHistoryMessage,
-        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
-            isEnabled: !runtime.isPrivateModeEnabled,
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentId: runtime.agentId,
-            messages: parsedRequest.messages,
-            includeAssistantPlaceholder: true,
-            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
-        }),
-    };
-}
-
-/**
- * Handles the streaming OpenAI-compatible response branch.
- */
-function createHandleChatCompletionStreamResponse(options: {
-    request: NextRequest;
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Response {
-    const { request, title, parsedRequest, runtime, promptContext } = options;
-    const encoder = new TextEncoder();
-    const responseModel = parsedRequest.model || 'promptbook-agent';
-    const readableStream = new ReadableStream({
-        async start(controller) {
-            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
-            const created = Math.floor(Date.now() / 1000);
-            const emitSsePayload = (payload: unknown) => {
-                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
-            };
-            const emitDeltaChunk = (deltaContent: string) => {
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {
-                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
-                        },
-                        finishReason: null,
-                    }),
-                );
-            };
-
-            emitSsePayload(
-                createHandleChatCompletionChunk({
-                    runId,
-                    created,
-                    model: responseModel,
-                    delta: {
-                        role: 'assistant',
-                        content: '',
-                    },
-                    finishReason: null,
-                }),
-            );
-
-            let hasMeaningfulDelta = false;
-
-            try {
-                const handleStreamChunk = createChatStreamHandler({
-                    onDelta: (deltaContent) => {
-                        if (deltaContent.trim().length > 0) {
-                            hasMeaningfulDelta = true;
-                        }
-                        emitDeltaChunk(deltaContent);
-                    },
-                    onToolCalls: (toolCalls) => {
-                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
-                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
-                    },
-                });
-                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
-                    signal: request.signal,
-                });
-                const normalizedResponse = ensureNonEmptyChatContent({
-                    content: result.content,
-                    context: title,
-                });
-
-                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
-                    emitDeltaChunk(normalizedResponse.content);
-                }
-
-                const messageSuffixAppendix = createMessageSuffixAppendix(
-                    normalizedResponse.content,
-                    runtime.messageSuffix,
-                );
-                if (messageSuffixAppendix) {
-                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
-                        emitDeltaChunk(delta);
-                    });
-                }
-
-                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-                await finalizeHandleChatCompletionResult({
-                    responseContentWithSuffix,
-                    usage: result.usage,
-                    toolCalls: result.toolCalls,
-                    parsedRequest,
-                    runtime,
-                    promptContext,
-                    // Preserve historical behavior: streamed OpenAI-compatible calls still persist learning in private mode.
-                    shouldPersistLearnedAgentSource: true,
-                });
-
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {},
-                        finishReason: 'stop',
-                    }),
-                );
-                controller.enqueue(encoder.encode('[DONE]'));
-            } catch (error) {
-                console.error('Error during streaming:', error);
-                controller.error(error);
-            }
-            controller.close();
-        },
-    });
-
-    return new Response(readableStream, {
-        headers: {
-            'Content-Type': 'text/event-stream',
-            'Cache-Control': 'no-cache',
-            Connection: 'keep-alive',
-        },
-    });
-}
-
-/**
- * Handles the non-streaming OpenAI-compatible response branch.
- */
-async function createHandleChatCompletionJsonResponse(options: {
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Promise<NextResponse> {
-    const { title, parsedRequest, runtime, promptContext } = options;
-    const result = await runtime.agent.callChatModel(promptContext.prompt);
-    const normalizedResponse = ensureNonEmptyChatContent({
-        content: result.content,
-        context: title,
-    });
-    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-
-    await finalizeHandleChatCompletionResult({
-        responseContentWithSuffix,
-        usage: result.usage,
-        toolCalls: result.toolCalls,
-        parsedRequest,
-        runtime,
-        promptContext,
-        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
-    });
-
-    return NextResponse.json({
-        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
-        object: 'chat.completion',
-        created: Math.floor(Date.now() / 1000),
-        model: parsedRequest.model || 'promptbook-agent',
-        choices: [
-            {
-                index: 0,
-                message: {
-                    role: 'assistant',
-                    content: responseContentWithSuffix,
-                },
-                finish_reason: 'stop',
-            },
-        ],
-        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
-    });
-}
-
-/**
- * Runs the shared side effects after one successful model response.
- */
-async function finalizeHandleChatCompletionResult(options: {
-    responseContentWithSuffix: string;
-    usage: Usage;
-    toolCalls?: ReadonlyArray<ToolCall>;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
-
-    await promptContext.recordChatHistoryMessage({
-        message: {
-            role: 'MODEL',
-            content: responseContentWithSuffix,
-        },
-        previousMessageHash: promptContext.userMessageHash,
-        usage,
-    });
-
-    if (toolCalls && toolCalls.length > 0) {
-        await logCalendarToolCallsActivity({
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentPermanentId: runtime.agentId,
-            toolCalls,
-        });
-    }
-
-    await persistOpenAiCompatibilityFrozenChat({
-        isEnabled: !runtime.isPrivateModeEnabled,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        agentId: runtime.agentId,
-        chatId: promptContext.persistedFrozenChatId,
-        messages: parsedRequest.messages,
-        assistantContent: responseContentWithSuffix,
-        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
-    });
-    await persistHandleChatCompletionLearnedAgentSource({
-        runtime,
-        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
-    });
-}
-
-/**
- * Applies append-only self-learning updates when the current branch should persist them.
- */
-async function persistHandleChatCompletionLearnedAgentSource(options: {
-    runtime: HandleChatCompletionRuntime;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { runtime, shouldPersistLearnedAgentSource } = options;
-    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
-        return;
-    }
-
-    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
-        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
-        resolvedAgentSourceBeforeLearning: runtime.agentSource,
-        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
-    });
-
-    if (learnedAgentSource !== null) {
-        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
     }
 }
-
-/**
- * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
- */
-async function persistOpenAiCompatibilityFrozenChat(options: {
-    isEnabled: boolean;
-    userId: number | null | undefined;
-    agentId: string;
-    messages: ReadonlyArray<unknown>;
-    chatId?: string;
-    assistantContent?: string;
-    includeAssistantPlaceholder?: boolean;
-    failureMessage: string;
-}): Promise<string | undefined> {
-    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
-        options;
-    if (!isEnabled || !userId) {
-        return undefined;
-    }
-
-    const persistedFrozenChat = await persistFrozenUserChat({
-        userId,
-        agentPermanentId: agentId,
-        source: USER_CHAT_SOURCES.OPENAI_API,
-        chatId,
-        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
-            includeAssistantPlaceholder,
-        }),
-    }).catch((error) => {
-        console.error(failureMessage, error);
-        return null;
-    });
-
-    return persistedFrozenChat?.id;
-}
-
-/**
- * Builds one OpenAI-compatible thread from the earlier request messages.
- */
-function createHandleChatCompletionThread(previousMessages: ReadonlyArray<TODO_any>): ChatMessage[] {
-    return previousMessages.map((message: TODO_any, index: number) => ({
-        id: `msg-${index}`,
-        sender: message.role === 'assistant' ? 'agent' : 'user',
-        content: message.content,
-        isComplete: true,
-        createdAt: $getCurrentDate(),
-    }));
-}
-
-/**
- * Appends system messages to the agent source as dynamic `CONTEXT`.
- */
-function appendSystemMessagesToAgentSource(
-    agentSource: string_book,
-    messages: ReadonlyArray<TODO_any>,
-): {
-    agentSourceWithContext: string_book;
-    hasDynamicContext: boolean;
-} {
-    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
-    if (systemMessages.length === 0) {
-        return {
-            agentSourceWithContext: agentSource,
-            hasDynamicContext: false,
-        };
-    }
-
-    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
-    return {
-        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
-        hasDynamicContext: true,
-    };
-}
-
-/**
- * Waits for any running preparation job of the current agent before executing the chat call.
- */
-async function waitForHandleChatCompletionAgentPreparation(options: {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    agentId: string;
-    agentHash: string;
-}): Promise<void> {
-    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
-    const preparationWaitResult = await waitForRunningAgentPreparation({
-        tablePrefix,
-        agentPermanentId: options.agentId,
-        fingerprint: options.agentHash,
-        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    });
-
-    if (preparationWaitResult !== 'not_running') {
-        console.info('[pre-index]', 'openai_chat_wait_result', {
-            tablePrefix,
-            agentPermanentId: options.agentId,
-            agentHash: options.agentHash,
-            preparationWaitResult,
-        });
-    }
-}
-
-/**
- * Resolves request-scoped project, calendar, and email credentials for the current agent.
- */
-async function resolveHandleChatCompletionCredentials(options: {
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    agentId: string;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-}): Promise<{
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-}> {
-    const userId = options.currentUserIdentity?.userId;
-    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
-        resolveUseProjectGithubToken({
-            userId,
-            agentPermanentId: options.agentId,
-        }),
-        options.calendarConnections.length > 0
-            ? resolveUseCalendarGoogleToken({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-        options.useEmailConfiguration.isEnabled
-            ? resolveUseEmailSmtpCredential({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-    ]);
-
-    return {
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-    };
-}
-
-/**
- * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
- */
-async function createHandleChatCompletionAgent(options: {
-    agentName: string;
-    agentSource: string_book;
-    agentId: string;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    localServerUrl: string;
-    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
-    hasDynamicContext: boolean;
-    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
-}): Promise<Agent> {
-    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
-    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
-    const runtimeAgentReferenceResolver = options.hasDynamicContext
-        ? createBookScopedAgentReferenceResolver({
-              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
-              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
-              localServerUrl: options.localServerUrl,
-              fallbackResolver: options.baseAgentReferenceResolver,
-          })
-        : undefined;
-    const agentModelRequirements =
-        options.preparedAgentModelRequirements?.modelRequirements ||
-        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
-            agentReferenceResolver: runtimeAgentReferenceResolver,
-            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
-        }));
-
-    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
-        options.agentSource,
-        options.resolvedAgentContext.resolvedAgentName,
-        await baseOpenAiToolsPromise,
-        {
-            includeDynamicContext: true,
-            agentId: options.agentId,
-            modelRequirements: agentModelRequirements,
-        },
-    );
-
-    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
-        agentName: options.agentName,
-        assistantCacheKey: agentKitResult.assistantCacheKey,
-        vectorStoreHash: agentKitResult.vectorStoreHash,
-        vectorStoreId: agentKitResult.vectorStoreId,
-    });
-
-    return new Agent({
-        agentSource: options.agentSource,
-        executionTools: {
-            llm: agentKitResult.tools,
-        },
-        precomputedModelRequirements: agentModelRequirements,
-        assistantPreparationMode: 'external',
-        isVerbose: true,
-        teacherAgent: null,
-    });
-}
-
-/**
- * Creates one OpenAI-compatible streaming chunk payload.
- */
-function createHandleChatCompletionChunk(options: {
-    runId: string;
-    created: number;
-    model: TODO_any;
-    delta: Record<string, unknown>;
-    finishReason: 'stop' | null;
-}) {
-    return {
-        id: options.runId,
-        object: 'chat.completion.chunk',
-        created: options.created,
-        model: options.model,
-        choices: [
-            {
-                index: 0,
-                delta: options.delta,
-                finish_reason: options.finishReason,
-            },
-        ],
-    };
-}
-
-/**
- * Creates one consistent OpenAI-compatible error response.
- */
-function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
-    return NextResponse.json(
-        {
-            error: {
-                message,
-                type,
-            },
-        },
-        { status },
-    );
-}
-
-// TODO: [🈹] Maybe move chat thread handling here
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f49a4fa80fa21f140e25de59596e5546d62e3f7
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
@@ -0,0 +1,18 @@
+import { NextResponse } from 'next/server';
+
+/**
+ * Creates one consistent OpenAI-compatible error response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
+    return NextResponse.json(
+        {
+            error: {
+                message,
+                type,
+            },
+        },
+        { status },
+    );
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b1979f4621921bc664fed75a05851c6fcb8fba6d
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
@@ -0,0 +1,120 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import { appendMessageSuffix } from '@/src/utils/chat/messageSuffix';
+import type { UncertainNumber, Usage, UsageCounts } from '@promptbook-local/types';
+import { NextResponse } from 'next/server';
+import { computeUsageCounts } from '../../../../../src/execution/utils/computeUsageCounts';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the non-streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionJsonResponse(options: {
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Promise<NextResponse> {
+    const { title, parsedRequest, runtime, promptContext } = options;
+    const result = await runtime.agent.callChatModel(promptContext.prompt);
+    const normalizedResponse = ensureNonEmptyChatContent({
+        content: result.content,
+        context: title,
+    });
+    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+
+    await finalizeHandleChatCompletionResult({
+        responseContentWithSuffix,
+        usage: result.usage,
+        toolCalls: result.toolCalls,
+        parsedRequest,
+        runtime,
+        promptContext,
+        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
+    });
+
+    return NextResponse.json({
+        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
+        object: 'chat.completion',
+        created: Math.floor(Date.now() / 1000),
+        model: parsedRequest.model || 'promptbook-agent',
+        choices: [
+            {
+                index: 0,
+                message: {
+                    role: 'assistant',
+                    content: responseContentWithSuffix,
+                },
+                finish_reason: 'stop',
+            },
+        ],
+        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
+    });
+}
+
+/**
+ * Falls back to the estimated value when the original token count is unknown.
+ *
+ * @param tokenCount - Token count reported by the execution tools.
+ * @param fallbackValue - Estimated token count based on text length.
+ * @returns Token count to report in the OpenAI-compatible response.
+ */
+function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
+    if (tokenCount.value === 0 && tokenCount.isUncertain) {
+        return {
+            value: fallbackValue,
+            isUncertain: true,
+        };
+    }
+
+    return tokenCount;
+}
+
+/**
+ * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
+ *
+ * @param promptContent - Prompt content used for the request.
+ * @param completionContent - Assistant response content.
+ * @param usage - Native Promptbook usage details.
+ * @returns Usage payload for compatibility responses.
+ */
+function createCompatibilityUsage(
+    promptContent: string,
+    completionContent: string,
+    usage: Usage,
+): {
+    prompt_tokens: number;
+    completion_tokens: number;
+    total_tokens: number;
+    details: Usage;
+} {
+    const promptCounts = computeUsageCounts(promptContent);
+    const completionCounts = computeUsageCounts(completionContent);
+    const inputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
+        ...promptCounts,
+    };
+    const outputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
+        ...completionCounts,
+    };
+
+    const promptTokens = inputUsage.tokensCount.value;
+    const completionTokens = outputUsage.tokensCount.value;
+
+    return {
+        prompt_tokens: promptTokens,
+        completion_tokens: completionTokens,
+        total_tokens: promptTokens + completionTokens,
+        details: {
+            price: usage.price,
+            duration: usage.duration,
+            input: inputUsage,
+            output: outputUsage,
+        },
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..7aac8141a8b1237ce36430b4b4598a618f0383ac
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
@@ -0,0 +1,111 @@
+import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
+import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
+import type { ChatMessage, ChatPrompt } from '@promptbook-local/types';
+import { $getCurrentDate } from '@promptbook-local/utils';
+import type { NextRequest } from 'next/server';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Prompt, history recorder, and persistence context created for one chat call.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionPromptContext = {
+    prompt: ChatPrompt;
+    userMessageHash: string;
+    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
+    persistedFrozenChatId?: string;
+};
+
+/**
+ * Creates prompt and persistence helpers for one chat completion request.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionPromptContext(options: {
+    request: NextRequest;
+    title: string;
+    apiKey: string | null;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+}): Promise<HandleChatCompletionPromptContext> {
+    const { request, title, apiKey, parsedRequest, runtime } = options;
+    const timezone = request.headers.get('x-timezone') || 'UTC';
+    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
+    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
+    const recordChatHistoryMessage = await createChatHistoryRecorder({
+        request,
+        agentIdentifier: runtime.agentId,
+        agentHash: runtime.agentHash,
+        source: 'OPENAI_API_COMPATIBILITY',
+        apiKey,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        isEnabled: !runtime.isPrivateModeEnabled,
+    });
+    const userMessageHash = await recordChatHistoryMessage({
+        message: {
+            role: 'USER',
+            content: lastMessage.content,
+        },
+        previousMessageHash: null,
+    });
+    const promptParameters = composePromptParametersWithMemoryContext({
+        baseParameters: {
+            ...parsedRequest.incomingParameters,
+            timezone,
+        },
+        currentUserIdentity: runtime.currentUserIdentity,
+        agentPermanentId: runtime.agentId,
+        agentName: runtime.resolvedAgentContext.resolvedAgentName,
+        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
+        projectRepositories: runtime.projectRepositories,
+        projectGithubToken: runtime.projectGithubToken,
+        emailSmtpCredential: runtime.emailSmtpCredential,
+        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
+        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
+        calendarConnections: runtime.calendarConnections,
+    });
+    const prompt: ChatPrompt = {
+        title,
+        content: lastMessage.content,
+        modelRequirements: {
+            modelVariant: 'CHAT',
+            responseFormat: parsedRequest.responseFormat,
+            toolChoice: parsedRequest.runtimeToolChoice,
+        },
+        parameters: promptParameters,
+        thread,
+        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
+    };
+
+    return {
+        prompt,
+        userMessageHash,
+        recordChatHistoryMessage,
+        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
+            isEnabled: !runtime.isPrivateModeEnabled,
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentId: runtime.agentId,
+            messages: parsedRequest.messages,
+            includeAssistantPlaceholder: true,
+            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
+        }),
+    };
+}
+
+/**
+ * Builds one OpenAI-compatible thread from the earlier request messages.
+ */
+function createHandleChatCompletionThread(
+    previousMessages: HandleChatCompletionParsedRequest['threadMessages'],
+): Array<ChatMessage> {
+    return previousMessages.map((message, index) => ({
+        id: `msg-${index}`,
+        sender: message.role === 'assistant' ? 'agent' : 'user',
+        content: message.content,
+        isComplete: true,
+        createdAt: $getCurrentDate(),
+    }));
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..85e096027760bc45fb730e74f8ae3bbb721b1029
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
@@ -0,0 +1,162 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import {
+    appendMessageSuffix,
+    createMessageSuffixAppendix,
+    emulateMessageSuffixStreaming,
+} from '@/src/utils/chat/messageSuffix';
+import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
+import { encodeChatStreamWhitespaceForTransport } from '../../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
+import { prepareToolCallsForStreaming } from '../toolCallStreaming';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionStreamResponse(options: {
+    request: Request;
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Response {
+    const { request, title, parsedRequest, runtime, promptContext } = options;
+    const encoder = new TextEncoder();
+    const responseModel = parsedRequest.model || 'promptbook-agent';
+    const readableStream = new ReadableStream({
+        async start(controller) {
+            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
+            const created = Math.floor(Date.now() / 1000);
+            const emitSsePayload = (payload: unknown) => {
+                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
+            };
+            const emitDeltaChunk = (deltaContent: string) => {
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {
+                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
+                        },
+                        finishReason: null,
+                    }),
+                );
+            };
+
+            emitSsePayload(
+                createHandleChatCompletionChunk({
+                    runId,
+                    created,
+                    model: responseModel,
+                    delta: {
+                        role: 'assistant',
+                        content: '',
+                    },
+                    finishReason: null,
+                }),
+            );
+
+            let hasMeaningfulDelta = false;
+
+            try {
+                const handleStreamChunk = createChatStreamHandler({
+                    onDelta: (deltaContent) => {
+                        if (deltaContent.trim().length > 0) {
+                            hasMeaningfulDelta = true;
+                        }
+                        emitDeltaChunk(deltaContent);
+                    },
+                    onToolCalls: (toolCalls) => {
+                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
+                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
+                    },
+                });
+                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
+                    signal: request.signal,
+                });
+                const normalizedResponse = ensureNonEmptyChatContent({
+                    content: result.content,
+                    context: title,
+                });
+
+                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
+                    emitDeltaChunk(normalizedResponse.content);
+                }
+
+                const messageSuffixAppendix = createMessageSuffixAppendix(
+                    normalizedResponse.content,
+                    runtime.messageSuffix,
+                );
+                if (messageSuffixAppendix) {
+                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
+                        emitDeltaChunk(delta);
+                    });
+                }
+
+                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+                await finalizeHandleChatCompletionResult({
+                    responseContentWithSuffix,
+                    usage: result.usage,
+                    toolCalls: result.toolCalls,
+                    parsedRequest,
+                    runtime,
+                    promptContext,
+                    shouldPersistLearnedAgentSource: true,
+                });
+
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {},
+                        finishReason: 'stop',
+                    }),
+                );
+                controller.enqueue(encoder.encode('[DONE]'));
+            } catch (error) {
+                console.error('Error during streaming:', error);
+                controller.error(error);
+            }
+            controller.close();
+        },
+    });
+
+    return new Response(readableStream, {
+        headers: {
+            'Content-Type': 'text/event-stream',
+            'Cache-Control': 'no-cache',
+            Connection: 'keep-alive',
+        },
+    });
+}
+
+/**
+ * Creates one OpenAI-compatible streaming chunk payload.
+ */
+function createHandleChatCompletionChunk(options: {
+    runId: string;
+    created: number;
+    model: HandleChatCompletionParsedRequest['model'];
+    delta: Record<string, unknown>;
+    finishReason: 'stop' | null;
+}) {
+    return {
+        id: options.runId,
+        object: 'chat.completion.chunk',
+        created: options.created,
+        model: options.model,
+        choices: [
+            {
+                index: 0,
+                delta: options.delta,
+                finish_reason: options.finishReason,
+            },
+        ],
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
new file mode 100644
index 0000000000000000000000000000000000000000..dd6a7d87ea491df245dbf93d8bcb277a9a10b034
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
@@ -0,0 +1,78 @@
+import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
+import { resolveAppendOnlySelfLearningAgentSource } from '@/src/utils/resolveAppendOnlySelfLearningAgentSource';
+import type { ToolCall, Usage } from '@promptbook-local/types';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Runs the shared side effects after one successful model response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function finalizeHandleChatCompletionResult(options: {
+    responseContentWithSuffix: string;
+    usage: Usage;
+    toolCalls?: ReadonlyArray<ToolCall>;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
+
+    await promptContext.recordChatHistoryMessage({
+        message: {
+            role: 'MODEL',
+            content: responseContentWithSuffix,
+        },
+        previousMessageHash: promptContext.userMessageHash,
+        usage,
+    });
+
+    if (toolCalls && toolCalls.length > 0) {
+        await logCalendarToolCallsActivity({
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentPermanentId: runtime.agentId,
+            toolCalls,
+        });
+    }
+
+    await persistOpenAiCompatibilityFrozenChat({
+        isEnabled: !runtime.isPrivateModeEnabled,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        agentId: runtime.agentId,
+        chatId: promptContext.persistedFrozenChatId,
+        messages: parsedRequest.messages,
+        assistantContent: responseContentWithSuffix,
+        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
+    });
+    await persistHandleChatCompletionLearnedAgentSource({
+        runtime,
+        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
+    });
+}
+
+/**
+ * Applies append-only self-learning updates when the current branch should persist them.
+ */
+async function persistHandleChatCompletionLearnedAgentSource(options: {
+    runtime: HandleChatCompletionRuntime;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { runtime, shouldPersistLearnedAgentSource } = options;
+    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
+        return;
+    }
+
+    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
+        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
+        resolvedAgentSourceBeforeLearning: runtime.agentSource,
+        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
+    });
+
+    if (learnedAgentSource !== null) {
+        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
+    }
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
new file mode 100644
index 0000000000000000000000000000000000000000..58e1c26d7f428c1bdb39b36411b3f75256e3de51
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
@@ -0,0 +1,176 @@
+import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
+import type { LlmToolDefinition, TODO_any } from '@promptbook-local/types';
+import { NextRequest, NextResponse } from 'next/server';
+import type OpenAI from 'openai';
+import { HTTP_STATUS_CODES } from '../../constants';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Definition of open AI chat tool.
+ */
+type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
+
+/**
+ * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionParsedRequest = {
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    threadMessages: ReadonlyArray<TODO_any>;
+    stream: TODO_any;
+    model: TODO_any;
+    responseFormat: TODO_any;
+    runtimeTools?: Array<LlmToolDefinition>;
+    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+    incomingParameters: Record<string, unknown>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Parses and validates the OpenAI-compatible request payload.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function parseHandleChatCompletionRequest(options: {
+    body: TODO_any;
+    agentNameFromParams?: string;
+    request: NextRequest;
+}): HandleChatCompletionParsedRequest | NextResponse {
+    const { body, agentNameFromParams, request } = options;
+    const {
+        messages,
+        stream,
+        model,
+        response_format: responseFormat,
+        tools: rawTools,
+        tool_choice: toolChoice,
+        parameters: rawParameters = {},
+    } = body;
+    const agentName = (agentNameFromParams || model) as string | undefined;
+    if (!agentName) {
+        return createHandleChatCompletionErrorResponse(
+            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    if (!messages || !Array.isArray(messages) || messages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array is required and cannot be empty.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
+    if (threadMessages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array must contain at least one non-system message.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    return {
+        agentName,
+        messages,
+        threadMessages,
+        stream,
+        model,
+        responseFormat,
+        runtimeTools: convertOpenAiTools(rawTools),
+        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
+        incomingParameters:
+            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
+                ? (rawParameters as Record<string, unknown>)
+                : {},
+        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
+    };
+}
+
+/**
+ * Handles convert open Ai tools.
+ */
+function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
+    if (!Array.isArray(rawTools)) {
+        return undefined;
+    }
+
+    const converted = rawTools
+        .map((tool) => convertOpenAiTool(tool))
+        .filter((tool): tool is LlmToolDefinition => tool !== null);
+
+    return converted.length > 0 ? converted : undefined;
+}
+
+/**
+ * Handles convert open Ai tool.
+ */
+function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
+    const tool = rawTool as OpenAIChatToolDefinition;
+    if (!tool || tool.type !== 'function') {
+        return null;
+    }
+
+    const functionDefinition = tool.function;
+    if (!functionDefinition) {
+        return null;
+    }
+
+    const { name, description, parameters } = functionDefinition;
+    if (typeof name !== 'string' || name.trim().length === 0) {
+        return null;
+    }
+
+    const parameterSchema = parameters ?? {};
+    if (parameterSchema.type !== 'object') {
+        return null;
+    }
+
+    const properties = parameterSchema.properties ?? {};
+    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
+
+    for (const [key, value] of Object.entries(properties)) {
+        if (!value || typeof value !== 'object') {
+            continue;
+        }
+
+        normalizedProperties[key] = {
+            type: typeof value.type === 'string' ? value.type : 'string',
+            description: typeof value.description === 'string' ? value.description : undefined,
+        };
+    }
+
+    const required =
+        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
+            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
+            : undefined;
+
+    return {
+        name,
+        description: typeof description === 'string' ? description : '',
+        parameters: {
+            type: 'object',
+            properties: normalizedProperties,
+            required,
+            additionalProperties:
+                typeof parameterSchema.additionalProperties === 'boolean'
+                    ? parameterSchema.additionalProperties
+                    : undefined,
+        },
+    };
+}
+
+/**
+ * Parses open Ai tool choice.
+ */
+function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
+    if (value === undefined || value === null) {
+        return undefined;
+    }
+
+    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
new file mode 100644
index 0000000000000000000000000000000000000000..60e5f1b008bcf5a6a1836c06c4c3d9ebcb909a9f
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
@@ -0,0 +1,163 @@
+import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
+import type { ChatMessage } from '@promptbook-local/types';
+
+/**
+ * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
+ */
+type OpenAiCompatibilityMessageLike = {
+    role?: unknown;
+    content?: unknown;
+};
+
+/**
+ * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function persistOpenAiCompatibilityFrozenChat(options: {
+    isEnabled: boolean;
+    userId: number | null | undefined;
+    agentId: string;
+    messages: ReadonlyArray<unknown>;
+    chatId?: string;
+    assistantContent?: string;
+    includeAssistantPlaceholder?: boolean;
+    failureMessage: string;
+}): Promise<string | undefined> {
+    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
+        options;
+    if (!isEnabled || !userId) {
+        return undefined;
+    }
+
+    const persistedFrozenChat = await persistFrozenUserChat({
+        userId,
+        agentPermanentId: agentId,
+        source: USER_CHAT_SOURCES.OPENAI_API,
+        chatId,
+        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
+            includeAssistantPlaceholder,
+        }),
+    }).catch((error) => {
+        console.error(failureMessage, error);
+        return null;
+    });
+
+    return persistedFrozenChat?.id;
+}
+
+/**
+ * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
+ */
+function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
+    if (typeof value === 'string') {
+        return value;
+    }
+
+    if (Array.isArray(value)) {
+        return value
+            .map((part) => {
+                if (typeof part === 'string') {
+                    return part;
+                }
+
+                if (!part || typeof part !== 'object') {
+                    return '';
+                }
+
+                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
+                if (typeof partLike.text === 'string') {
+                    return partLike.text;
+                }
+
+                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
+                    return '[Image input]';
+                }
+
+                if (partLike.image_url) {
+                    return '[Image input]';
+                }
+
+                return '';
+            })
+            .filter(Boolean)
+            .join('\n');
+    }
+
+    if (value === null || value === undefined) {
+        return '';
+    }
+
+    try {
+        return JSON.stringify(value, null, 2);
+    } catch {
+        return String(value);
+    }
+}
+
+/**
+ * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ */
+function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
+    if (typeof role !== 'string') {
+        return 'USER';
+    }
+
+    switch (role) {
+        case 'assistant':
+            return 'AGENT';
+        case 'system':
+            return 'SYSTEM';
+        case 'tool':
+            return 'TOOL';
+        case 'developer':
+            return 'SYSTEM';
+        default:
+            return 'USER';
+    }
+}
+
+/**
+ * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
+ */
+function createFrozenOpenAiCompatibilityMessages(
+    rawMessages: ReadonlyArray<unknown>,
+    assistantContent?: string,
+    options: {
+        includeAssistantPlaceholder?: boolean;
+    } = {},
+): Array<ChatMessage> {
+    const startedAt = Date.now();
+    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
+        const message = rawMessage as OpenAiCompatibilityMessageLike;
+
+        return {
+            id: `openai-frozen-${index}`,
+            sender: mapOpenAiCompatibilityRoleToSender(message.role),
+            content: normalizeOpenAiCompatibilityMessageContent(message.content),
+            isComplete: true,
+            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage;
+    });
+
+    if (assistantContent !== undefined) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: assistantContent,
+            isComplete: true,
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    } else if (options.includeAssistantPlaceholder) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: '',
+            isComplete: false,
+            lifecycleState: 'running',
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    }
+
+    return messages;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fd44b7ea5a87168725a9e99784038137c96537ab
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
@@ -0,0 +1,305 @@
+import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
+import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
+import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
+import type { string_book, TODO_any } from '@promptbook-local/types';
+import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
+import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
+import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
+import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
+import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
+import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
+import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
+import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
+import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
+import {
+    createBookScopedAgentReferenceResolver,
+    parseBookScopedAgentIdentifier,
+} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
+import { resolveMessageSuffixFromAgentSource } from '@/src/utils/chat/messageSuffix';
+import { NextRequest, NextResponse } from 'next/server';
+import { isAgentDeleted } from '../../app/agents/[agentName]/_utils';
+import {
+    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    resolveAgentCollectionTablePrefix,
+    waitForRunningAgentPreparation,
+} from '../agentPreparation';
+import { resolveCachedServerAgentContext, resolveCachedServerAgentModelRequirements } from '../cachedServerAgentRuntime';
+import { AgentKitCacheManager } from '../cache/AgentKitCacheManager';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Runtime state resolved before the chat model is executed.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionRuntime = {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    agent: Agent;
+    agentSource: string_book;
+    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
+    messageSuffix: string | null;
+    agentId: string;
+    agentHash: string;
+    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Resolves agent/runtime dependencies needed before prompt execution.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function resolveHandleChatCompletionRuntime(options: {
+    request: NextRequest;
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    isPrivateModeEnabled: boolean;
+}): Promise<HandleChatCompletionRuntime | NextResponse> {
+    const { request, agentName, messages, isPrivateModeEnabled } = options;
+    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
+    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
+
+    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
+        return createHandleChatCompletionErrorResponse(
+            'This agent has been deleted. You can restore it from the Recycle Bin.',
+            'agent_deleted',
+            410,
+        );
+    }
+
+    const [collection, baseAgentReferenceResolver] = await Promise.all([
+        $provideAgentCollectionForServer(),
+        $provideAgentReferenceResolver(),
+    ]);
+    const localServerUrl = new URL(request.url).origin;
+    const resolvedAgentContext = await resolveCachedServerAgentContext({
+        collection,
+        agentIdentifier: agentName,
+        localServerUrl,
+        fallbackResolver: baseAgentReferenceResolver,
+    }).catch(() => null);
+
+    if (!resolvedAgentContext?.resolvedAgentSource) {
+        return createHandleChatCompletionErrorResponse(`Agent '${agentName}' not found.`, 'invalid_request_error', 404);
+    }
+
+    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
+    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
+    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
+    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
+    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
+    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
+    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
+    agentSource = agentSourceWithContext;
+
+    const preparedAgentModelRequirements = !hasDynamicContext
+        ? await resolveCachedServerAgentModelRequirements({
+              resolvedAgentContext,
+              localServerUrl,
+              fallbackResolver: baseAgentReferenceResolver,
+          })
+        : null;
+    const agentId = resolvedAgentContext.parentAgentPermanentId;
+    const agentHash = computeAgentHash(agentSource);
+    await waitForHandleChatCompletionAgentPreparation({
+        collection,
+        agentId,
+        agentHash,
+    });
+
+    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
+    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
+        await resolveHandleChatCompletionCredentials({
+            currentUserIdentity,
+            agentId,
+            calendarConnections,
+            useEmailConfiguration,
+        });
+    const agent = await createHandleChatCompletionAgent({
+        agentName,
+        agentSource,
+        agentId,
+        resolvedAgentContext,
+        localServerUrl,
+        baseAgentReferenceResolver,
+        hasDynamicContext,
+        preparedAgentModelRequirements,
+    });
+
+    return {
+        collection,
+        resolvedAgentContext,
+        agent,
+        agentSource,
+        unresolvedAgentSource,
+        messageSuffix,
+        agentId,
+        agentHash,
+        projectRepositories,
+        calendarConnections,
+        useEmailConfiguration,
+        currentUserIdentity,
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+        isPrivateModeEnabled,
+    };
+}
+
+/**
+ * Appends system messages to the agent source as dynamic `CONTEXT`.
+ */
+function appendSystemMessagesToAgentSource(
+    agentSource: string_book,
+    messages: ReadonlyArray<TODO_any>,
+): {
+    agentSourceWithContext: string_book;
+    hasDynamicContext: boolean;
+} {
+    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
+    if (systemMessages.length === 0) {
+        return {
+            agentSourceWithContext: agentSource,
+            hasDynamicContext: false,
+        };
+    }
+
+    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
+    return {
+        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
+        hasDynamicContext: true,
+    };
+}
+
+/**
+ * Waits for any running preparation job of the current agent before executing the chat call.
+ */
+async function waitForHandleChatCompletionAgentPreparation(options: {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    agentId: string;
+    agentHash: string;
+}): Promise<void> {
+    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
+    const preparationWaitResult = await waitForRunningAgentPreparation({
+        tablePrefix,
+        agentPermanentId: options.agentId,
+        fingerprint: options.agentHash,
+        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    });
+
+    if (preparationWaitResult !== 'not_running') {
+        console.info('[pre-index]', 'openai_chat_wait_result', {
+            tablePrefix,
+            agentPermanentId: options.agentId,
+            agentHash: options.agentHash,
+            preparationWaitResult,
+        });
+    }
+}
+
+/**
+ * Resolves request-scoped project, calendar, and email credentials for the current agent.
+ */
+async function resolveHandleChatCompletionCredentials(options: {
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    agentId: string;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+}): Promise<{
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+}> {
+    const userId = options.currentUserIdentity?.userId;
+    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
+        resolveUseProjectGithubToken({
+            userId,
+            agentPermanentId: options.agentId,
+        }),
+        options.calendarConnections.length > 0
+            ? resolveUseCalendarGoogleToken({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+        options.useEmailConfiguration.isEnabled
+            ? resolveUseEmailSmtpCredential({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+    ]);
+
+    return {
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+    };
+}
+
+/**
+ * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
+ */
+async function createHandleChatCompletionAgent(options: {
+    agentName: string;
+    agentSource: string_book;
+    agentId: string;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    localServerUrl: string;
+    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
+    hasDynamicContext: boolean;
+    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
+}): Promise<Agent> {
+    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
+    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
+    const runtimeAgentReferenceResolver = options.hasDynamicContext
+        ? createBookScopedAgentReferenceResolver({
+              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
+              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
+              localServerUrl: options.localServerUrl,
+              fallbackResolver: options.baseAgentReferenceResolver,
+          })
+        : undefined;
+    const agentModelRequirements =
+        options.preparedAgentModelRequirements?.modelRequirements ||
+        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
+            agentReferenceResolver: runtimeAgentReferenceResolver,
+            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
+        }));
+
+    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
+        options.agentSource,
+        options.resolvedAgentContext.resolvedAgentName,
+        await baseOpenAiToolsPromise,
+        {
+            includeDynamicContext: true,
+            agentId: options.agentId,
+            modelRequirements: agentModelRequirements,
+        },
+    );
+
+    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
+        agentName: options.agentName,
+        assistantCacheKey: agentKitResult.assistantCacheKey,
+        vectorStoreHash: agentKitResult.vectorStoreHash,
+        vectorStoreId: agentKitResult.vectorStoreId,
+    });
+
+    return new Agent({
+        agentSource: options.agentSource,
+        executionTools: {
+            llm: agentKitResult.tools,
+        },
+        precomputedModelRequirements: agentModelRequirements,
+        assistantPreparationMode: 'external',
+        isVerbose: true,
+        teacherAgent: null,
+    });
+}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 474fd839d14b2bdab324e5f59fce0b15ddf95afd..bc9cfde73332af5d107ad35780a31963bc4e68e9
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,8 @@
+-   Refactored Agents Server `handleChatCompletion.ts` into a thinner chat-completion facade plus focused private helper modules without changing external behavior:
+
+    -   Extracted OpenAI-compatible request parsing, runtime/agent resolution, prompt-context creation, frozen-chat persistence, shared post-response side effects, and the streaming vs non-streaming response branches into dedicated private files behind the existing `handleChatCompletion` entrypoint.
+    -   Kept the existing API-key validation, deleted-agent handling, dynamic system-context injection, AgentKit cache usage, private-mode behavior, frozen audit snapshots, self-learning persistence, streaming chunk format, and OpenAI-compatible JSON response shape intact while reducing responsibility density in the main file.
+
 -   Refactored Agents Server `errorMessages.ts` into smaller focused helpers without changing external behavior:
 
     -   Split the branching-heavy `categorizeError` flow into dedicated API-type, native-error-message, and status-code classification helpers while preserving the existing precedence.

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Item apps/agents-server/src/app/agents/[agentName]/_utils.ts | Select-Object FullName' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts -TotalCount 60' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts -TotalCount 80' in C:/Users/me/work/ai/promptbook succeeded in 716ms:
import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
import { resolveAppendOnlySelfLearningAgentSource } from '@/src/utils/resolveAppendOnlySelfLearningAgentSource';
import type { ToolCall, Usage } from '@promptbook-local/types';
import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';

/**
 * Runs the shared side effects after one successful model response.
 *
 * @private function of `handleChatCompletion`
 */
export async function finalizeHandleChatCompletionResult(options: {
    responseContentWithSuffix: string;
    usage: Usage;
    toolCalls?: ReadonlyArray<ToolCall>;
    parsedRequest: HandleChatCompletionParsedRequest;
    runtime: HandleChatCompletionRuntime;
    promptContext: HandleChatCompletionPromptContext;
 succeeded in 720ms:

 succeeded in 838ms:
import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
import { appendMessageSuffix } from '@/src/utils/chat/messageSuffix';
import type { UncertainNumber, Usage, UsageCounts } from '@promptbook-local/types';
import { NextResponse } from 'next/server';
import { computeUsageCounts } from '../../../../../src/execution/utils/computeUsageCounts';
import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';

/**
 * Handles the non-streaming OpenAI-compatible response branch.
 *
 * @private function of `handleChatCompletion`
 */
export async function createHandleChatCompletionJsonResponse(options: {
    title: string;
    parsedRequest: HandleChatCompletionParsedRequest;
    runtime: HandleChatCompletionRuntime;
    promptContext: HandleChatCompletionPromptContext;
file update:
diff --git a/apps/agents-server/src/utils/handleChatCompletion.ts b/apps/agents-server/src/utils/handleChatCompletion.ts
index 9c96d36fad9cf24bf26d89f57372332add4e4595..cff56fafd6cfa90bb4094f9f202c4f01f6f74af1
--- a/apps/agents-server/src/utils/handleChatCompletion.ts
+++ b/apps/agents-server/src/utils/handleChatCompletion.ts
@@ -1,392 +1,16 @@
-import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
-import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
-import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
-import {
-    createBookScopedAgentReferenceResolver,
-    parseBookScopedAgentIdentifier,
-} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
-import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
-import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
-import {
-    appendMessageSuffix,
-    createMessageSuffixAppendix,
-    emulateMessageSuffixStreaming,
-    resolveMessageSuffixFromAgentSource,
-} from '@/src/utils/chat/messageSuffix';
-import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
-import { prepareToolCallsForStreaming } from './toolCallStreaming';
-import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
-import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
-import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
-import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
-import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
-import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
-import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
-import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
-import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
-import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
-import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
-import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
-import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
-import type {
-    ChatMessage,
-    ChatPrompt,
-    LlmToolDefinition,
-    string_book,
-    ToolCall,
-    TODO_any,
-    UncertainNumber,
-    Usage,
-    UsageCounts,
-} from '@promptbook-local/types';
-import { $getCurrentDate } from '@promptbook-local/utils';
 import { NextRequest, NextResponse } from 'next/server';
-import type OpenAI from 'openai';
-import { computeUsageCounts } from '../../../../src/execution/utils/computeUsageCounts';
-import { encodeChatStreamWhitespaceForTransport } from '../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
-import { isAgentDeleted } from '../app/agents/[agentName]/_utils';
 import { HTTP_STATUS_CODES } from '../constants';
-import { AgentKitCacheManager } from './cache/AgentKitCacheManager';
 import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
+import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
+import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
+import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
+import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
+import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
 import { validateApiKey } from './validateApiKey';
-import {
-    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    resolveAgentCollectionTablePrefix,
-    waitForRunningAgentPreparation,
-} from './agentPreparation';
-import {
-    resolveCachedServerAgentContext,
-    resolveCachedServerAgentModelRequirements,
-} from './cachedServerAgentRuntime';
-import { resolveAppendOnlySelfLearningAgentSource } from './resolveAppendOnlySelfLearningAgentSource';
 
 /**
- * Falls back to the estimated value when the original token count is unknown.
- *
- * @param tokenCount - Token count reported by the execution tools.
- * @param fallbackValue - Estimated token count based on text length.
- * @returns Token count to report in the OpenAI-compatible response.
- */
-function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
-    if (tokenCount.value === 0 && tokenCount.isUncertain) {
-        return {
-            value: fallbackValue,
-            isUncertain: true,
-        };
-    }
-
-    return tokenCount;
-}
-
-/**
- * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
- *
- * @param promptContent - Prompt content used for the request.
- * @param completionContent - Assistant response content.
- * @param usage - Native Promptbook usage details.
- * @returns Usage payload for compatibility responses.
- */
-function createCompatibilityUsage(
-    promptContent: string,
-    completionContent: string,
-    usage: Usage,
-): {
-    prompt_tokens: number;
-    completion_tokens: number;
-    total_tokens: number;
-    details: Usage;
-} {
-    const promptCounts = computeUsageCounts(promptContent);
-    const completionCounts = computeUsageCounts(completionContent);
-    const inputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
-        ...promptCounts,
-    };
-    const outputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
-        ...completionCounts,
-    };
-
-    const promptTokens = inputUsage.tokensCount.value;
-    const completionTokens = outputUsage.tokensCount.value;
-
-    return {
-        prompt_tokens: promptTokens,
-        completion_tokens: completionTokens,
-        total_tokens: promptTokens + completionTokens,
-        details: {
-            price: usage.price,
-            duration: usage.duration,
-            input: inputUsage,
-            output: outputUsage,
-        },
-    };
-}
-
-/**
- * Definition of open AI chat tool.
- */
-type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
-
-/**
- * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
- */
-type OpenAiCompatibilityMessageLike = {
-    role?: unknown;
-    content?: unknown;
-};
-
-/**
- * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
- */
-type HandleChatCompletionParsedRequest = {
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    threadMessages: ReadonlyArray<TODO_any>;
-    stream: TODO_any;
-    model: TODO_any;
-    responseFormat: TODO_any;
-    runtimeTools?: Array<LlmToolDefinition>;
-    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-    incomingParameters: Record<string, unknown>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Runtime state resolved before the chat model is executed.
- */
-type HandleChatCompletionRuntime = {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    agent: Agent;
-    agentSource: string_book;
-    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
-    messageSuffix: string | null;
-    agentId: string;
-    agentHash: string;
-    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Prompt, history recorder, and persistence context created for one chat call.
- */
-type HandleChatCompletionPromptContext = {
-    prompt: ChatPrompt;
-    userMessageHash: string;
-    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
-    persistedFrozenChatId?: string;
-};
-
-/**
- * Handles convert open Ai tools.
- */
-function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
-    if (!Array.isArray(rawTools)) {
-        return undefined;
-    }
-
-    const converted = rawTools
-        .map((tool) => convertOpenAiTool(tool))
-        .filter((tool): tool is LlmToolDefinition => tool !== null);
-
-    return converted.length > 0 ? converted : undefined;
-}
-
-/**
- * Handles convert open Ai tool.
- */
-function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
-    const tool = rawTool as OpenAIChatToolDefinition;
-    if (!tool || tool.type !== 'function') {
-        return null;
-    }
-
-    const functionDefinition = tool.function;
-    if (!functionDefinition) {
-        return null;
-    }
-
-    const { name, description, parameters } = functionDefinition;
-    if (typeof name !== 'string' || name.trim().length === 0) {
-        return null;
-    }
-
-    const parameterSchema = parameters ?? {};
-    if (parameterSchema.type !== 'object') {
-        return null;
-    }
-
-    const properties = parameterSchema.properties ?? {};
-    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
-
-    for (const [key, value] of Object.entries(properties)) {
-        if (!value || typeof value !== 'object') {
-            continue;
-        }
-
-        normalizedProperties[key] = {
-            type: typeof value.type === 'string' ? value.type : 'string',
-            description: typeof value.description === 'string' ? value.description : undefined,
-        };
-    }
-
-    const required =
-        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
-            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
-            : undefined;
-
-    return {
-        name,
-        description: typeof description === 'string' ? description : '',
-        parameters: {
-            type: 'object',
-            properties: normalizedProperties,
-            required,
-            additionalProperties:
-                typeof parameterSchema.additionalProperties === 'boolean'
-                    ? parameterSchema.additionalProperties
-                    : undefined,
-        },
-    };
-}
-
-/**
- * Parses open Ai tool choice.
- */
-function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
-    if (value === undefined || value === null) {
-        return undefined;
-    }
-
-    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-}
-
-/**
- * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
- */
-function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
-    if (typeof value === 'string') {
-        return value;
-    }
-
-    if (Array.isArray(value)) {
-        return value
-            .map((part) => {
-                if (typeof part === 'string') {
-                    return part;
-                }
-
-                if (!part || typeof part !== 'object') {
-                    return '';
-                }
-
-                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
-                if (typeof partLike.text === 'string') {
-                    return partLike.text;
-                }
-
-                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
-                    return '[Image input]';
-                }
-
-                if (partLike.image_url) {
-                    return '[Image input]';
-                }
-
-                return '';
-            })
-            .filter(Boolean)
-            .join('\n');
-    }
-
-    if (value === null || value === undefined) {
-        return '';
-    }
-
-    try {
-        return JSON.stringify(value, null, 2);
-    } catch {
-        return String(value);
-    }
-}
-
-/**
- * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ * Handles convert open Ai chat completion requests routed through the Agents Server.
  */
-function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
-    if (typeof role !== 'string') {
-        return 'USER';
-    }
-
-    switch (role) {
-        case 'assistant':
-            return 'AGENT';
-        case 'system':
-            return 'SYSTEM';
-        case 'tool':
-            return 'TOOL';
-        case 'developer':
-            return 'SYSTEM';
-        default:
-            return 'USER';
-    }
-}
-
-/**
- * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
- */
-function createFrozenOpenAiCompatibilityMessages(
-    rawMessages: ReadonlyArray<unknown>,
-    assistantContent?: string,
-    options: {
-        includeAssistantPlaceholder?: boolean;
-    } = {},
-): Array<ChatMessage> {
-    const startedAt = Date.now();
-    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
-        const message = rawMessage as OpenAiCompatibilityMessageLike;
-
-        return {
-            id: `openai-frozen-${index}`,
-            sender: mapOpenAiCompatibilityRoleToSender(message.role),
-            content: normalizeOpenAiCompatibilityMessageContent(message.content),
-            isComplete: true,
-            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage;
-    });
-
-    if (assistantContent !== undefined) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: assistantContent,
-            isComplete: true,
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    } else if (options.includeAssistantPlaceholder) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: '',
-            isComplete: false,
-            lifecycleState: 'running',
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    }
-
-    return messages;
-}
-
-/**
- * Handles chat completion.
- */
 export async function handleChatCompletion(
     request: NextRequest,
     params: { agentName?: string },
@@ -399,7 +23,6 @@
         return versionMismatchResponse;
     }
 
-    // Validate API key explicitly (in addition to middleware)
     const apiKeyValidation = await validateApiKey(request);
     if (!apiKeyValidation.isValid) {
         return NextResponse.json(
@@ -462,724 +85,5 @@
             { error: { message: (error as Error).message || 'Internal Server Error', type: 'server_error' } },
             { status: HTTP_STATUS_CODES.INTERNAL_SERVER_ERROR },
         );
-    }
-}
-
-/**
- * Parses and validates the OpenAI-compatible request payload.
- */
-function parseHandleChatCompletionRequest(options: {
-    body: TODO_any;
-    agentNameFromParams?: string;
-    request: NextRequest;
-}): HandleChatCompletionParsedRequest | NextResponse {
-    const { body, agentNameFromParams, request } = options;
-    const {
-        messages,
-        stream,
-        model,
-        response_format: responseFormat,
-        tools: rawTools,
-        tool_choice: toolChoice,
-        parameters: rawParameters = {},
-    } = body;
-    const agentName = (agentNameFromParams || model) as string | undefined;
-    if (!agentName) {
-        return createHandleChatCompletionErrorResponse(
-            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    if (!messages || !Array.isArray(messages) || messages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array is required and cannot be empty.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
-    if (threadMessages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array must contain at least one non-system message.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    return {
-        agentName,
-        messages,
-        threadMessages,
-        stream,
-        model,
-        responseFormat,
-        runtimeTools: convertOpenAiTools(rawTools),
-        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
-        incomingParameters:
-            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
-                ? (rawParameters as Record<string, unknown>)
-                : {},
-        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
-    };
-}
-
-/**
- * Resolves agent/runtime dependencies needed before prompt execution.
- */
-async function resolveHandleChatCompletionRuntime(options: {
-    request: NextRequest;
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    isPrivateModeEnabled: boolean;
-}): Promise<HandleChatCompletionRuntime | NextResponse> {
-    const { request, agentName, messages, isPrivateModeEnabled } = options;
-    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
-    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
-
-    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
-        return createHandleChatCompletionErrorResponse(
-            'This agent has been deleted. You can restore it from the Recycle Bin.',
-            'agent_deleted',
-            410,
-        );
-    }
-
-    const [collection, baseAgentReferenceResolver] = await Promise.all([
-        $provideAgentCollectionForServer(),
-        $provideAgentReferenceResolver(),
-    ]);
-    const localServerUrl = new URL(request.url).origin;
-    const resolvedAgentContext = await resolveCachedServerAgentContext({
-        collection,
-        agentIdentifier: agentName,
-        localServerUrl,
-        fallbackResolver: baseAgentReferenceResolver,
-    }).catch(() => null);
-
-    if (!resolvedAgentContext?.resolvedAgentSource) {
-        return createHandleChatCompletionErrorResponse(
-            `Agent '${agentName}' not found.`,
-            'invalid_request_error',
-            HTTP_STATUS_CODES.NOT_FOUND,
-        );
-    }
-
-    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
-    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
-    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
-    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
-    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
-    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
-    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
-    agentSource = agentSourceWithContext;
-
-    const preparedAgentModelRequirements = !hasDynamicContext
-        ? await resolveCachedServerAgentModelRequirements({
-              resolvedAgentContext,
-              localServerUrl,
-              fallbackResolver: baseAgentReferenceResolver,
-          })
-        : null;
-    const agentId = resolvedAgentContext.parentAgentPermanentId;
-    const agentHash = computeAgentHash(agentSource);
-    await waitForHandleChatCompletionAgentPreparation({
-        collection,
-        agentId,
-        agentHash,
-    });
-
-    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
-    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
-        await resolveHandleChatCompletionCredentials({
-            currentUserIdentity,
-            agentId,
-            calendarConnections,
-            useEmailConfiguration,
-        });
-    const agent = await createHandleChatCompletionAgent({
-        agentName,
-        agentSource,
-        agentId,
-        resolvedAgentContext,
-        localServerUrl,
-        baseAgentReferenceResolver,
-        hasDynamicContext,
-        preparedAgentModelRequirements,
-    });
-
-    return {
-        collection,
-        resolvedAgentContext,
-        agent,
-        agentSource,
-        unresolvedAgentSource,
-        messageSuffix,
-        agentId,
-        agentHash,
-        projectRepositories,
-        calendarConnections,
-        useEmailConfiguration,
-        currentUserIdentity,
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-        isPrivateModeEnabled,
-    };
-}
-
-/**
- * Creates prompt and persistence helpers for one chat completion request.
- */
-async function createHandleChatCompletionPromptContext(options: {
-    request: NextRequest;
-    title: string;
-    apiKey: string | null;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-}): Promise<HandleChatCompletionPromptContext> {
-    const { request, title, apiKey, parsedRequest, runtime } = options;
-    const timezone = request.headers.get('x-timezone') || 'UTC';
-    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
-    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
-    const recordChatHistoryMessage = await createChatHistoryRecorder({
-        request,
-        agentIdentifier: runtime.agentId,
-        agentHash: runtime.agentHash,
-        source: 'OPENAI_API_COMPATIBILITY',
-        apiKey,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        isEnabled: !runtime.isPrivateModeEnabled,
-    });
-    const userMessageHash = await recordChatHistoryMessage({
-        message: {
-            role: 'USER',
-            content: lastMessage.content,
-        },
-        previousMessageHash: null,
-    });
-    const promptParameters = composePromptParametersWithMemoryContext({
-        baseParameters: {
-            ...parsedRequest.incomingParameters,
-            timezone,
-        },
-        currentUserIdentity: runtime.currentUserIdentity,
-        agentPermanentId: runtime.agentId,
-        agentName: runtime.resolvedAgentContext.resolvedAgentName,
-        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
-        projectRepositories: runtime.projectRepositories,
-        projectGithubToken: runtime.projectGithubToken,
-        emailSmtpCredential: runtime.emailSmtpCredential,
-        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
-        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
-        calendarConnections: runtime.calendarConnections,
-    });
-    const prompt: ChatPrompt = {
-        title,
-        content: lastMessage.content,
-        modelRequirements: {
-            modelVariant: 'CHAT',
-            responseFormat: parsedRequest.responseFormat,
-            toolChoice: parsedRequest.runtimeToolChoice,
-            // We could pass 'model' from body if we wanted to enforce it, but Agent usually has its own config
-        },
-        parameters: promptParameters,
-        thread,
-        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
-    };
-
-    return {
-        prompt,
-        userMessageHash,
-        recordChatHistoryMessage,
-        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
-            isEnabled: !runtime.isPrivateModeEnabled,
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentId: runtime.agentId,
-            messages: parsedRequest.messages,
-            includeAssistantPlaceholder: true,
-            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
-        }),
-    };
-}
-
-/**
- * Handles the streaming OpenAI-compatible response branch.
- */
-function createHandleChatCompletionStreamResponse(options: {
-    request: NextRequest;
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Response {
-    const { request, title, parsedRequest, runtime, promptContext } = options;
-    const encoder = new TextEncoder();
-    const responseModel = parsedRequest.model || 'promptbook-agent';
-    const readableStream = new ReadableStream({
-        async start(controller) {
-            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
-            const created = Math.floor(Date.now() / 1000);
-            const emitSsePayload = (payload: unknown) => {
-                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
-            };
-            const emitDeltaChunk = (deltaContent: string) => {
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {
-                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
-                        },
-                        finishReason: null,
-                    }),
-                );
-            };
-
-            emitSsePayload(
-                createHandleChatCompletionChunk({
-                    runId,
-                    created,
-                    model: responseModel,
-                    delta: {
-                        role: 'assistant',
-                        content: '',
-                    },
-                    finishReason: null,
-                }),
-            );
-
-            let hasMeaningfulDelta = false;
-
-            try {
-                const handleStreamChunk = createChatStreamHandler({
-                    onDelta: (deltaContent) => {
-                        if (deltaContent.trim().length > 0) {
-                            hasMeaningfulDelta = true;
-                        }
-                        emitDeltaChunk(deltaContent);
-                    },
-                    onToolCalls: (toolCalls) => {
-                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
-                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
-                    },
-                });
-                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
-                    signal: request.signal,
-                });
-                const normalizedResponse = ensureNonEmptyChatContent({
-                    content: result.content,
-                    context: title,
-                });
-
-                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
-                    emitDeltaChunk(normalizedResponse.content);
-                }
-
-                const messageSuffixAppendix = createMessageSuffixAppendix(
-                    normalizedResponse.content,
-                    runtime.messageSuffix,
-                );
-                if (messageSuffixAppendix) {
-                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
-                        emitDeltaChunk(delta);
-                    });
-                }
-
-                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-                await finalizeHandleChatCompletionResult({
-                    responseContentWithSuffix,
-                    usage: result.usage,
-                    toolCalls: result.toolCalls,
-                    parsedRequest,
-                    runtime,
-                    promptContext,
-                    // Preserve historical behavior: streamed OpenAI-compatible calls still persist learning in private mode.
-                    shouldPersistLearnedAgentSource: true,
-                });
-
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {},
-                        finishReason: 'stop',
-                    }),
-                );
-                controller.enqueue(encoder.encode('[DONE]'));
-            } catch (error) {
-                console.error('Error during streaming:', error);
-                controller.error(error);
-            }
-            controller.close();
-        },
-    });
-
-    return new Response(readableStream, {
-        headers: {
-            'Content-Type': 'text/event-stream',
-            'Cache-Control': 'no-cache',
-            Connection: 'keep-alive',
-        },
-    });
-}
-
-/**
- * Handles the non-streaming OpenAI-compatible response branch.
- */
-async function createHandleChatCompletionJsonResponse(options: {
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Promise<NextResponse> {
-    const { title, parsedRequest, runtime, promptContext } = options;
-    const result = await runtime.agent.callChatModel(promptContext.prompt);
-    const normalizedResponse = ensureNonEmptyChatContent({
-        content: result.content,
-        context: title,
-    });
-    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-
-    await finalizeHandleChatCompletionResult({
-        responseContentWithSuffix,
-        usage: result.usage,
-        toolCalls: result.toolCalls,
-        parsedRequest,
-        runtime,
-        promptContext,
-        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
-    });
-
-    return NextResponse.json({
-        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
-        object: 'chat.completion',
-        created: Math.floor(Date.now() / 1000),
-        model: parsedRequest.model || 'promptbook-agent',
-        choices: [
-            {
-                index: 0,
-                message: {
-                    role: 'assistant',
-                    content: responseContentWithSuffix,
-                },
-                finish_reason: 'stop',
-            },
-        ],
-        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
-    });
-}
-
-/**
- * Runs the shared side effects after one successful model response.
- */
-async function finalizeHandleChatCompletionResult(options: {
-    responseContentWithSuffix: string;
-    usage: Usage;
-    toolCalls?: ReadonlyArray<ToolCall>;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
-
-    await promptContext.recordChatHistoryMessage({
-        message: {
-            role: 'MODEL',
-            content: responseContentWithSuffix,
-        },
-        previousMessageHash: promptContext.userMessageHash,
-        usage,
-    });
-
-    if (toolCalls && toolCalls.length > 0) {
-        await logCalendarToolCallsActivity({
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentPermanentId: runtime.agentId,
-            toolCalls,
-        });
-    }
-
-    await persistOpenAiCompatibilityFrozenChat({
-        isEnabled: !runtime.isPrivateModeEnabled,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        agentId: runtime.agentId,
-        chatId: promptContext.persistedFrozenChatId,
-        messages: parsedRequest.messages,
-        assistantContent: responseContentWithSuffix,
-        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
-    });
-    await persistHandleChatCompletionLearnedAgentSource({
-        runtime,
-        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
-    });
-}
-
-/**
- * Applies append-only self-learning updates when the current branch should persist them.
- */
-async function persistHandleChatCompletionLearnedAgentSource(options: {
-    runtime: HandleChatCompletionRuntime;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { runtime, shouldPersistLearnedAgentSource } = options;
-    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
-        return;
-    }
-
-    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
-        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
-        resolvedAgentSourceBeforeLearning: runtime.agentSource,
-        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
-    });
-
-    if (learnedAgentSource !== null) {
-        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
     }
 }
-
-/**
- * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
- */
-async function persistOpenAiCompatibilityFrozenChat(options: {
-    isEnabled: boolean;
-    userId: number | null | undefined;
-    agentId: string;
-    messages: ReadonlyArray<unknown>;
-    chatId?: string;
-    assistantContent?: string;
-    includeAssistantPlaceholder?: boolean;
-    failureMessage: string;
-}): Promise<string | undefined> {
-    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
-        options;
-    if (!isEnabled || !userId) {
-        return undefined;
-    }
-
-    const persistedFrozenChat = await persistFrozenUserChat({
-        userId,
-        agentPermanentId: agentId,
-        source: USER_CHAT_SOURCES.OPENAI_API,
-        chatId,
-        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
-            includeAssistantPlaceholder,
-        }),
-    }).catch((error) => {
-        console.error(failureMessage, error);
-        return null;
-    });
-
-    return persistedFrozenChat?.id;
-}
-
-/**
- * Builds one OpenAI-compatible thread from the earlier request messages.
- */
-function createHandleChatCompletionThread(previousMessages: ReadonlyArray<TODO_any>): ChatMessage[] {
-    return previousMessages.map((message: TODO_any, index: number) => ({
-        id: `msg-${index}`,
-        sender: message.role === 'assistant' ? 'agent' : 'user',
-        content: message.content,
-        isComplete: true,
-        createdAt: $getCurrentDate(),
-    }));
-}
-
-/**
- * Appends system messages to the agent source as dynamic `CONTEXT`.
- */
-function appendSystemMessagesToAgentSource(
-    agentSource: string_book,
-    messages: ReadonlyArray<TODO_any>,
-): {
-    agentSourceWithContext: string_book;
-    hasDynamicContext: boolean;
-} {
-    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
-    if (systemMessages.length === 0) {
-        return {
-            agentSourceWithContext: agentSource,
-            hasDynamicContext: false,
-        };
-    }
-
-    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
-    return {
-        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
-        hasDynamicContext: true,
-    };
-}
-
-/**
- * Waits for any running preparation job of the current agent before executing the chat call.
- */
-async function waitForHandleChatCompletionAgentPreparation(options: {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    agentId: string;
-    agentHash: string;
-}): Promise<void> {
-    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
-    const preparationWaitResult = await waitForRunningAgentPreparation({
-        tablePrefix,
-        agentPermanentId: options.agentId,
-        fingerprint: options.agentHash,
-        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    });
-
-    if (preparationWaitResult !== 'not_running') {
-        console.info('[pre-index]', 'openai_chat_wait_result', {
-            tablePrefix,
-            agentPermanentId: options.agentId,
-            agentHash: options.agentHash,
-            preparationWaitResult,
-        });
-    }
-}
-
-/**
- * Resolves request-scoped project, calendar, and email credentials for the current agent.
- */
-async function resolveHandleChatCompletionCredentials(options: {
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    agentId: string;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-}): Promise<{
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-}> {
-    const userId = options.currentUserIdentity?.userId;
-    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
-        resolveUseProjectGithubToken({
-            userId,
-            agentPermanentId: options.agentId,
-        }),
-        options.calendarConnections.length > 0
-            ? resolveUseCalendarGoogleToken({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-        options.useEmailConfiguration.isEnabled
-            ? resolveUseEmailSmtpCredential({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-    ]);
-
-    return {
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-    };
-}
-
-/**
- * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
- */
-async function createHandleChatCompletionAgent(options: {
-    agentName: string;
-    agentSource: string_book;
-    agentId: string;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    localServerUrl: string;
-    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
-    hasDynamicContext: boolean;
-    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
-}): Promise<Agent> {
-    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
-    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
-    const runtimeAgentReferenceResolver = options.hasDynamicContext
-        ? createBookScopedAgentReferenceResolver({
-              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
-              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
-              localServerUrl: options.localServerUrl,
-              fallbackResolver: options.baseAgentReferenceResolver,
-          })
-        : undefined;
-    const agentModelRequirements =
-        options.preparedAgentModelRequirements?.modelRequirements ||
-        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
-            agentReferenceResolver: runtimeAgentReferenceResolver,
-            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
-        }));
-
-    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
-        options.agentSource,
-        options.resolvedAgentContext.resolvedAgentName,
-        await baseOpenAiToolsPromise,
-        {
-            includeDynamicContext: true,
-            agentId: options.agentId,
-            modelRequirements: agentModelRequirements,
-        },
-    );
-
-    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
-        agentName: options.agentName,
-        assistantCacheKey: agentKitResult.assistantCacheKey,
-        vectorStoreHash: agentKitResult.vectorStoreHash,
-        vectorStoreId: agentKitResult.vectorStoreId,
-    });
-
-    return new Agent({
-        agentSource: options.agentSource,
-        executionTools: {
-            llm: agentKitResult.tools,
-        },
-        precomputedModelRequirements: agentModelRequirements,
-        assistantPreparationMode: 'external',
-        isVerbose: true,
-        teacherAgent: null,
-    });
-}
-
-/**
- * Creates one OpenAI-compatible streaming chunk payload.
- */
-function createHandleChatCompletionChunk(options: {
-    runId: string;
-    created: number;
-    model: TODO_any;
-    delta: Record<string, unknown>;
-    finishReason: 'stop' | null;
-}) {
-    return {
-        id: options.runId,
-        object: 'chat.completion.chunk',
-        created: options.created,
-        model: options.model,
-        choices: [
-            {
-                index: 0,
-                delta: options.delta,
-                finish_reason: options.finishReason,
-            },
-        ],
-    };
-}
-
-/**
- * Creates one consistent OpenAI-compatible error response.
- */
-function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
-    return NextResponse.json(
-        {
-            error: {
-                message,
-                type,
-            },
-        },
-        { status },
-    );
-}
-
-// TODO: [🈹] Maybe move chat thread handling here
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f49a4fa80fa21f140e25de59596e5546d62e3f7
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
@@ -0,0 +1,18 @@
+import { NextResponse } from 'next/server';
+
+/**
+ * Creates one consistent OpenAI-compatible error response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
+    return NextResponse.json(
+        {
+            error: {
+                message,
+                type,
+            },
+        },
+        { status },
+    );
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b1979f4621921bc664fed75a05851c6fcb8fba6d
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
@@ -0,0 +1,120 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import { appendMessageSuffix } from '@/src/utils/chat/messageSuffix';
+import type { UncertainNumber, Usage, UsageCounts } from '@promptbook-local/types';
+import { NextResponse } from 'next/server';
+import { computeUsageCounts } from '../../../../../src/execution/utils/computeUsageCounts';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the non-streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionJsonResponse(options: {
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Promise<NextResponse> {
+    const { title, parsedRequest, runtime, promptContext } = options;
+    const result = await runtime.agent.callChatModel(promptContext.prompt);
+    const normalizedResponse = ensureNonEmptyChatContent({
+        content: result.content,
+        context: title,
+    });
+    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+
+    await finalizeHandleChatCompletionResult({
+        responseContentWithSuffix,
+        usage: result.usage,
+        toolCalls: result.toolCalls,
+        parsedRequest,
+        runtime,
+        promptContext,
+        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
+    });
+
+    return NextResponse.json({
+        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
+        object: 'chat.completion',
+        created: Math.floor(Date.now() / 1000),
+        model: parsedRequest.model || 'promptbook-agent',
+        choices: [
+            {
+                index: 0,
+                message: {
+                    role: 'assistant',
+                    content: responseContentWithSuffix,
+                },
+                finish_reason: 'stop',
+            },
+        ],
+        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
+    });
+}
+
+/**
+ * Falls back to the estimated value when the original token count is unknown.
+ *
+ * @param tokenCount - Token count reported by the execution tools.
+ * @param fallbackValue - Estimated token count based on text length.
+ * @returns Token count to report in the OpenAI-compatible response.
+ */
+function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
+    if (tokenCount.value === 0 && tokenCount.isUncertain) {
+        return {
+            value: fallbackValue,
+            isUncertain: true,
+        };
+    }
+
+    return tokenCount;
+}
+
+/**
+ * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
+ *
+ * @param promptContent - Prompt content used for the request.
+ * @param completionContent - Assistant response content.
+ * @param usage - Native Promptbook usage details.
+ * @returns Usage payload for compatibility responses.
+ */
+function createCompatibilityUsage(
+    promptContent: string,
+    completionContent: string,
+    usage: Usage,
+): {
+    prompt_tokens: number;
+    completion_tokens: number;
+    total_tokens: number;
+    details: Usage;
+} {
+    const promptCounts = computeUsageCounts(promptContent);
+    const completionCounts = computeUsageCounts(completionContent);
+    const inputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
+        ...promptCounts,
+    };
+    const outputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
+        ...completionCounts,
+    };
+
+    const promptTokens = inputUsage.tokensCount.value;
+    const completionTokens = outputUsage.tokensCount.value;
+
+    return {
+        prompt_tokens: promptTokens,
+        completion_tokens: completionTokens,
+        total_tokens: promptTokens + completionTokens,
+        details: {
+            price: usage.price,
+            duration: usage.duration,
+            input: inputUsage,
+            output: outputUsage,
+        },
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..7aac8141a8b1237ce36430b4b4598a618f0383ac
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
@@ -0,0 +1,111 @@
+import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
+import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
+import type { ChatMessage, ChatPrompt } from '@promptbook-local/types';
+import { $getCurrentDate } from '@promptbook-local/utils';
+import type { NextRequest } from 'next/server';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Prompt, history recorder, and persistence context created for one chat call.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionPromptContext = {
+    prompt: ChatPrompt;
+    userMessageHash: string;
+    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
+    persistedFrozenChatId?: string;
+};
+
+/**
+ * Creates prompt and persistence helpers for one chat completion request.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionPromptContext(options: {
+    request: NextRequest;
+    title: string;
+    apiKey: string | null;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+}): Promise<HandleChatCompletionPromptContext> {
+    const { request, title, apiKey, parsedRequest, runtime } = options;
+    const timezone = request.headers.get('x-timezone') || 'UTC';
+    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
+    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
+    const recordChatHistoryMessage = await createChatHistoryRecorder({
+        request,
+        agentIdentifier: runtime.agentId,
+        agentHash: runtime.agentHash,
+        source: 'OPENAI_API_COMPATIBILITY',
+        apiKey,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        isEnabled: !runtime.isPrivateModeEnabled,
+    });
+    const userMessageHash = await recordChatHistoryMessage({
+        message: {
+            role: 'USER',
+            content: lastMessage.content,
+        },
+        previousMessageHash: null,
+    });
+    const promptParameters = composePromptParametersWithMemoryContext({
+        baseParameters: {
+            ...parsedRequest.incomingParameters,
+            timezone,
+        },
+        currentUserIdentity: runtime.currentUserIdentity,
+        agentPermanentId: runtime.agentId,
+        agentName: runtime.resolvedAgentContext.resolvedAgentName,
+        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
+        projectRepositories: runtime.projectRepositories,
+        projectGithubToken: runtime.projectGithubToken,
+        emailSmtpCredential: runtime.emailSmtpCredential,
+        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
+        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
+        calendarConnections: runtime.calendarConnections,
+    });
+    const prompt: ChatPrompt = {
+        title,
+        content: lastMessage.content,
+        modelRequirements: {
+            modelVariant: 'CHAT',
+            responseFormat: parsedRequest.responseFormat,
+            toolChoice: parsedRequest.runtimeToolChoice,
+        },
+        parameters: promptParameters,
+        thread,
+        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
+    };
+
+    return {
+        prompt,
+        userMessageHash,
+        recordChatHistoryMessage,
+        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
+            isEnabled: !runtime.isPrivateModeEnabled,
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentId: runtime.agentId,
+            messages: parsedRequest.messages,
+            includeAssistantPlaceholder: true,
+            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
+        }),
+    };
+}
+
+/**
+ * Builds one OpenAI-compatible thread from the earlier request messages.
+ */
+function createHandleChatCompletionThread(
+    previousMessages: HandleChatCompletionParsedRequest['threadMessages'],
+): Array<ChatMessage> {
+    return previousMessages.map((message, index) => ({
+        id: `msg-${index}`,
+        sender: message.role === 'assistant' ? 'agent' : 'user',
+        content: message.content,
+        isComplete: true,
+        createdAt: $getCurrentDate(),
+    }));
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..85e096027760bc45fb730e74f8ae3bbb721b1029
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
@@ -0,0 +1,162 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import {
+    appendMessageSuffix,
+    createMessageSuffixAppendix,
+    emulateMessageSuffixStreaming,
+} from '@/src/utils/chat/messageSuffix';
+import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
+import { encodeChatStreamWhitespaceForTransport } from '../../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
+import { prepareToolCallsForStreaming } from '../toolCallStreaming';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionStreamResponse(options: {
+    request: Request;
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Response {
+    const { request, title, parsedRequest, runtime, promptContext } = options;
+    const encoder = new TextEncoder();
+    const responseModel = parsedRequest.model || 'promptbook-agent';
+    const readableStream = new ReadableStream({
+        async start(controller) {
+            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
+            const created = Math.floor(Date.now() / 1000);
+            const emitSsePayload = (payload: unknown) => {
+                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
+            };
+            const emitDeltaChunk = (deltaContent: string) => {
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {
+                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
+                        },
+                        finishReason: null,
+                    }),
+                );
+            };
+
+            emitSsePayload(
+                createHandleChatCompletionChunk({
+                    runId,
+                    created,
+                    model: responseModel,
+                    delta: {
+                        role: 'assistant',
+                        content: '',
+                    },
+                    finishReason: null,
+                }),
+            );
+
+            let hasMeaningfulDelta = false;
+
+            try {
+                const handleStreamChunk = createChatStreamHandler({
+                    onDelta: (deltaContent) => {
+                        if (deltaContent.trim().length > 0) {
+                            hasMeaningfulDelta = true;
+                        }
+                        emitDeltaChunk(deltaContent);
+                    },
+                    onToolCalls: (toolCalls) => {
+                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
+                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
+                    },
+                });
+                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
+                    signal: request.signal,
+                });
+                const normalizedResponse = ensureNonEmptyChatContent({
+                    content: result.content,
+                    context: title,
+                });
+
+                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
+                    emitDeltaChunk(normalizedResponse.content);
+                }
+
+                const messageSuffixAppendix = createMessageSuffixAppendix(
+                    normalizedResponse.content,
+                    runtime.messageSuffix,
+                );
+                if (messageSuffixAppendix) {
+                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
+                        emitDeltaChunk(delta);
+                    });
+                }
+
+                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+                await finalizeHandleChatCompletionResult({
+                    responseContentWithSuffix,
+                    usage: result.usage,
+                    toolCalls: result.toolCalls,
+                    parsedRequest,
+                    runtime,
+                    promptContext,
+                    shouldPersistLearnedAgentSource: true,
+                });
+
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {},
+                        finishReason: 'stop',
+                    }),
+                );
+                controller.enqueue(encoder.encode('[DONE]'));
+            } catch (error) {
+                console.error('Error during streaming:', error);
+                controller.error(error);
+            }
+            controller.close();
+        },
+    });
+
+    return new Response(readableStream, {
+        headers: {
+            'Content-Type': 'text/event-stream',
+            'Cache-Control': 'no-cache',
+            Connection: 'keep-alive',
+        },
+    });
+}
+
+/**
+ * Creates one OpenAI-compatible streaming chunk payload.
+ */
+function createHandleChatCompletionChunk(options: {
+    runId: string;
+    created: number;
+    model: HandleChatCompletionParsedRequest['model'];
+    delta: Record<string, unknown>;
+    finishReason: 'stop' | null;
+}) {
+    return {
+        id: options.runId,
+        object: 'chat.completion.chunk',
+        created: options.created,
+        model: options.model,
+        choices: [
+            {
+                index: 0,
+                delta: options.delta,
+                finish_reason: options.finishReason,
+            },
+        ],
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
new file mode 100644
index 0000000000000000000000000000000000000000..dd6a7d87ea491df245dbf93d8bcb277a9a10b034
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
@@ -0,0 +1,78 @@
+import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
+import { resolveAppendOnlySelfLearningAgentSource } from '@/src/utils/resolveAppendOnlySelfLearningAgentSource';
+import type { ToolCall, Usage } from '@promptbook-local/types';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Runs the shared side effects after one successful model response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function finalizeHandleChatCompletionResult(options: {
+    responseContentWithSuffix: string;
+    usage: Usage;
+    toolCalls?: ReadonlyArray<ToolCall>;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
+
+    await promptContext.recordChatHistoryMessage({
+        message: {
+            role: 'MODEL',
+            content: responseContentWithSuffix,
+        },
+        previousMessageHash: promptContext.userMessageHash,
+        usage,
+    });
+
+    if (toolCalls && toolCalls.length > 0) {
+        await logCalendarToolCallsActivity({
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentPermanentId: runtime.agentId,
+            toolCalls,
+        });
+    }
+
+    await persistOpenAiCompatibilityFrozenChat({
+        isEnabled: !runtime.isPrivateModeEnabled,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        agentId: runtime.agentId,
+        chatId: promptContext.persistedFrozenChatId,
+        messages: parsedRequest.messages,
+        assistantContent: responseContentWithSuffix,
+        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
+    });
+    await persistHandleChatCompletionLearnedAgentSource({
+        runtime,
+        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
+    });
+}
+
+/**
+ * Applies append-only self-learning updates when the current branch should persist them.
+ */
+async function persistHandleChatCompletionLearnedAgentSource(options: {
+    runtime: HandleChatCompletionRuntime;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { runtime, shouldPersistLearnedAgentSource } = options;
+    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
+        return;
+    }
+
+    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
+        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
+        resolvedAgentSourceBeforeLearning: runtime.agentSource,
+        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
+    });
+
+    if (learnedAgentSource !== null) {
+        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
+    }
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
new file mode 100644
index 0000000000000000000000000000000000000000..58e1c26d7f428c1bdb39b36411b3f75256e3de51
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
@@ -0,0 +1,176 @@
+import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
+import type { LlmToolDefinition, TODO_any } from '@promptbook-local/types';
+import { NextRequest, NextResponse } from 'next/server';
+import type OpenAI from 'openai';
+import { HTTP_STATUS_CODES } from '../../constants';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Definition of open AI chat tool.
+ */
+type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
+
+/**
+ * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionParsedRequest = {
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    threadMessages: ReadonlyArray<TODO_any>;
+    stream: TODO_any;
+    model: TODO_any;
+    responseFormat: TODO_any;
+    runtimeTools?: Array<LlmToolDefinition>;
+    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+    incomingParameters: Record<string, unknown>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Parses and validates the OpenAI-compatible request payload.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function parseHandleChatCompletionRequest(options: {
+    body: TODO_any;
+    agentNameFromParams?: string;
+    request: NextRequest;
+}): HandleChatCompletionParsedRequest | NextResponse {
+    const { body, agentNameFromParams, request } = options;
+    const {
+        messages,
+        stream,
+        model,
+        response_format: responseFormat,
+        tools: rawTools,
+        tool_choice: toolChoice,
+        parameters: rawParameters = {},
+    } = body;
+    const agentName = (agentNameFromParams || model) as string | undefined;
+    if (!agentName) {
+        return createHandleChatCompletionErrorResponse(
+            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    if (!messages || !Array.isArray(messages) || messages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array is required and cannot be empty.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
+    if (threadMessages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array must contain at least one non-system message.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    return {
+        agentName,
+        messages,
+        threadMessages,
+        stream,
+        model,
+        responseFormat,
+        runtimeTools: convertOpenAiTools(rawTools),
+        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
+        incomingParameters:
+            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
+                ? (rawParameters as Record<string, unknown>)
+                : {},
+        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
+    };
+}
+
+/**
+ * Handles convert open Ai tools.
+ */
+function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
+    if (!Array.isArray(rawTools)) {
+        return undefined;
+    }
+
+    const converted = rawTools
+        .map((tool) => convertOpenAiTool(tool))
+        .filter((tool): tool is LlmToolDefinition => tool !== null);
+
+    return converted.length > 0 ? converted : undefined;
+}
+
+/**
+ * Handles convert open Ai tool.
+ */
+function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
+    const tool = rawTool as OpenAIChatToolDefinition;
+    if (!tool || tool.type !== 'function') {
+        return null;
+    }
+
+    const functionDefinition = tool.function;
+    if (!functionDefinition) {
+        return null;
+    }
+
+    const { name, description, parameters } = functionDefinition;
+    if (typeof name !== 'string' || name.trim().length === 0) {
+        return null;
+    }
+
+    const parameterSchema = parameters ?? {};
+    if (parameterSchema.type !== 'object') {
+        return null;
+    }
+
+    const properties = parameterSchema.properties ?? {};
+    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
+
+    for (const [key, value] of Object.entries(properties)) {
+        if (!value || typeof value !== 'object') {
+            continue;
+        }
+
+        normalizedProperties[key] = {
+            type: typeof value.type === 'string' ? value.type : 'string',
+            description: typeof value.description === 'string' ? value.description : undefined,
+        };
+    }
+
+    const required =
+        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
+            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
+            : undefined;
+
+    return {
+        name,
+        description: typeof description === 'string' ? description : '',
+        parameters: {
+            type: 'object',
+            properties: normalizedProperties,
+            required,
+            additionalProperties:
+                typeof parameterSchema.additionalProperties === 'boolean'
+                    ? parameterSchema.additionalProperties
+                    : undefined,
+        },
+    };
+}
+
+/**
+ * Parses open Ai tool choice.
+ */
+function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
+    if (value === undefined || value === null) {
+        return undefined;
+    }
+
+    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
new file mode 100644
index 0000000000000000000000000000000000000000..60e5f1b008bcf5a6a1836c06c4c3d9ebcb909a9f
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
@@ -0,0 +1,163 @@
+import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
+import type { ChatMessage } from '@promptbook-local/types';
+
+/**
+ * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
+ */
+type OpenAiCompatibilityMessageLike = {
+    role?: unknown;
+    content?: unknown;
+};
+
+/**
+ * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function persistOpenAiCompatibilityFrozenChat(options: {
+    isEnabled: boolean;
+    userId: number | null | undefined;
+    agentId: string;
+    messages: ReadonlyArray<unknown>;
+    chatId?: string;
+    assistantContent?: string;
+    includeAssistantPlaceholder?: boolean;
+    failureMessage: string;
+}): Promise<string | undefined> {
+    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
+        options;
+    if (!isEnabled || !userId) {
+        return undefined;
+    }
+
+    const persistedFrozenChat = await persistFrozenUserChat({
+        userId,
+        agentPermanentId: agentId,
+        source: USER_CHAT_SOURCES.OPENAI_API,
+        chatId,
+        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
+            includeAssistantPlaceholder,
+        }),
+    }).catch((error) => {
+        console.error(failureMessage, error);
+        return null;
+    });
+
+    return persistedFrozenChat?.id;
+}
+
+/**
+ * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
+ */
+function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
+    if (typeof value === 'string') {
+        return value;
+    }
+
+    if (Array.isArray(value)) {
+        return value
+            .map((part) => {
+                if (typeof part === 'string') {
+                    return part;
+                }
+
+                if (!part || typeof part !== 'object') {
+                    return '';
+                }
+
+                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
+                if (typeof partLike.text === 'string') {
+                    return partLike.text;
+                }
+
+                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
+                    return '[Image input]';
+                }
+
+                if (partLike.image_url) {
+                    return '[Image input]';
+                }
+
+                return '';
+            })
+            .filter(Boolean)
+            .join('\n');
+    }
+
+    if (value === null || value === undefined) {
+        return '';
+    }
+
+    try {
+        return JSON.stringify(value, null, 2);
+    } catch {
+        return String(value);
+    }
+}
+
+/**
+ * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ */
+function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
+    if (typeof role !== 'string') {
+        return 'USER';
+    }
+
+    switch (role) {
+        case 'assistant':
+            return 'AGENT';
+        case 'system':
+            return 'SYSTEM';
+        case 'tool':
+            return 'TOOL';
+        case 'developer':
+            return 'SYSTEM';
+        default:
+            return 'USER';
+    }
+}
+
+/**
+ * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
+ */
+function createFrozenOpenAiCompatibilityMessages(
+    rawMessages: ReadonlyArray<unknown>,
+    assistantContent?: string,
+    options: {
+        includeAssistantPlaceholder?: boolean;
+    } = {},
+): Array<ChatMessage> {
+    const startedAt = Date.now();
+    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
+        const message = rawMessage as OpenAiCompatibilityMessageLike;
+
+        return {
+            id: `openai-frozen-${index}`,
+            sender: mapOpenAiCompatibilityRoleToSender(message.role),
+            content: normalizeOpenAiCompatibilityMessageContent(message.content),
+            isComplete: true,
+            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage;
+    });
+
+    if (assistantContent !== undefined) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: assistantContent,
+            isComplete: true,
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    } else if (options.includeAssistantPlaceholder) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: '',
+            isComplete: false,
+            lifecycleState: 'running',
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    }
+
+    return messages;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fd44b7ea5a87168725a9e99784038137c96537ab
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
@@ -0,0 +1,305 @@
+import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
+import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
+import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
+import type { string_book, TODO_any } from '@promptbook-local/types';
+import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
+import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
+import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
+import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
+import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
+import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
+import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
+import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
+import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
+import {
+    createBookScopedAgentReferenceResolver,
+    parseBookScopedAgentIdentifier,
+} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
+import { resolveMessageSuffixFromAgentSource } from '@/src/utils/chat/messageSuffix';
+import { NextRequest, NextResponse } from 'next/server';
+import { isAgentDeleted } from '../../app/agents/[agentName]/_utils';
+import {
+    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    resolveAgentCollectionTablePrefix,
+    waitForRunningAgentPreparation,
+} from '../agentPreparation';
+import { resolveCachedServerAgentContext, resolveCachedServerAgentModelRequirements } from '../cachedServerAgentRuntime';
+import { AgentKitCacheManager } from '../cache/AgentKitCacheManager';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Runtime state resolved before the chat model is executed.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionRuntime = {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    agent: Agent;
+    agentSource: string_book;
+    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
+    messageSuffix: string | null;
+    agentId: string;
+    agentHash: string;
+    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Resolves agent/runtime dependencies needed before prompt execution.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function resolveHandleChatCompletionRuntime(options: {
+    request: NextRequest;
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    isPrivateModeEnabled: boolean;
+}): Promise<HandleChatCompletionRuntime | NextResponse> {
+    const { request, agentName, messages, isPrivateModeEnabled } = options;
+    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
+    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
+
+    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
+        return createHandleChatCompletionErrorResponse(
+            'This agent has been deleted. You can restore it from the Recycle Bin.',
+            'agent_deleted',
+            410,
+        );
+    }
+
+    const [collection, baseAgentReferenceResolver] = await Promise.all([
+        $provideAgentCollectionForServer(),
+        $provideAgentReferenceResolver(),
+    ]);
+    const localServerUrl = new URL(request.url).origin;
+    const resolvedAgentContext = await resolveCachedServerAgentContext({
+        collection,
+        agentIdentifier: agentName,
+        localServerUrl,
+        fallbackResolver: baseAgentReferenceResolver,
+    }).catch(() => null);
+
+    if (!resolvedAgentContext?.resolvedAgentSource) {
+        return createHandleChatCompletionErrorResponse(`Agent '${agentName}' not found.`, 'invalid_request_error', 404);
+    }
+
+    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
+    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
+    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
+    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
+    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
+    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
+    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
+    agentSource = agentSourceWithContext;
+
+    const preparedAgentModelRequirements = !hasDynamicContext
+        ? await resolveCachedServerAgentModelRequirements({
+              resolvedAgentContext,
+              localServerUrl,
+              fallbackResolver: baseAgentReferenceResolver,
+          })
+        : null;
+    const agentId = resolvedAgentContext.parentAgentPermanentId;
+    const agentHash = computeAgentHash(agentSource);
+    await waitForHandleChatCompletionAgentPreparation({
+        collection,
+        agentId,
+        agentHash,
+    });
+
+    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
+    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
+        await resolveHandleChatCompletionCredentials({
+            currentUserIdentity,
+            agentId,
+            calendarConnections,
+            useEmailConfiguration,
+        });
+    const agent = await createHandleChatCompletionAgent({
+        agentName,
+        agentSource,
+        agentId,
+        resolvedAgentContext,
+        localServerUrl,
+        baseAgentReferenceResolver,
+        hasDynamicContext,
+        preparedAgentModelRequirements,
+    });
+
+    return {
+        collection,
+        resolvedAgentContext,
+        agent,
+        agentSource,
+        unresolvedAgentSource,
+        messageSuffix,
+        agentId,
+        agentHash,
+        projectRepositories,
+        calendarConnections,
+        useEmailConfiguration,
+        currentUserIdentity,
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+        isPrivateModeEnabled,
+    };
+}
+
+/**
+ * Appends system messages to the agent source as dynamic `CONTEXT`.
+ */
+function appendSystemMessagesToAgentSource(
+    agentSource: string_book,
+    messages: ReadonlyArray<TODO_any>,
+): {
+    agentSourceWithContext: string_book;
+    hasDynamicContext: boolean;
+} {
+    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
+    if (systemMessages.length === 0) {
+        return {
+            agentSourceWithContext: agentSource,
+            hasDynamicContext: false,
+        };
+    }
+
+    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
+    return {
+        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
+        hasDynamicContext: true,
+    };
+}
+
+/**
+ * Waits for any running preparation job of the current agent before executing the chat call.
+ */
+async function waitForHandleChatCompletionAgentPreparation(options: {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    agentId: string;
+    agentHash: string;
+}): Promise<void> {
+    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
+    const preparationWaitResult = await waitForRunningAgentPreparation({
+        tablePrefix,
+        agentPermanentId: options.agentId,
+        fingerprint: options.agentHash,
+        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    });
+
+    if (preparationWaitResult !== 'not_running') {
+        console.info('[pre-index]', 'openai_chat_wait_result', {
+            tablePrefix,
+            agentPermanentId: options.agentId,
+            agentHash: options.agentHash,
+            preparationWaitResult,
+        });
+    }
+}
+
+/**
+ * Resolves request-scoped project, calendar, and email credentials for the current agent.
+ */
+async function resolveHandleChatCompletionCredentials(options: {
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    agentId: string;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+}): Promise<{
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+}> {
+    const userId = options.currentUserIdentity?.userId;
+    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
+        resolveUseProjectGithubToken({
+            userId,
+            agentPermanentId: options.agentId,
+        }),
+        options.calendarConnections.length > 0
+            ? resolveUseCalendarGoogleToken({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+        options.useEmailConfiguration.isEnabled
+            ? resolveUseEmailSmtpCredential({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+    ]);
+
+    return {
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+    };
+}
+
+/**
+ * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
+ */
+async function createHandleChatCompletionAgent(options: {
+    agentName: string;
+    agentSource: string_book;
+    agentId: string;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    localServerUrl: string;
+    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
+    hasDynamicContext: boolean;
+    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
+}): Promise<Agent> {
+    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
+    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
+    const runtimeAgentReferenceResolver = options.hasDynamicContext
+        ? createBookScopedAgentReferenceResolver({
+              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
+              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
+              localServerUrl: options.localServerUrl,
+              fallbackResolver: options.baseAgentReferenceResolver,
+          })
+        : undefined;
+    const agentModelRequirements =
+        options.preparedAgentModelRequirements?.modelRequirements ||
+        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
+            agentReferenceResolver: runtimeAgentReferenceResolver,
+            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
+        }));
+
+    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
+        options.agentSource,
+        options.resolvedAgentContext.resolvedAgentName,
+        await baseOpenAiToolsPromise,
+        {
+            includeDynamicContext: true,
+            agentId: options.agentId,
+            modelRequirements: agentModelRequirements,
+        },
+    );
+
+    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
+        agentName: options.agentName,
+        assistantCacheKey: agentKitResult.assistantCacheKey,
+        vectorStoreHash: agentKitResult.vectorStoreHash,
+        vectorStoreId: agentKitResult.vectorStoreId,
+    });
+
+    return new Agent({
+        agentSource: options.agentSource,
+        executionTools: {
+            llm: agentKitResult.tools,
+        },
+        precomputedModelRequirements: agentModelRequirements,
+        assistantPreparationMode: 'external',
+        isVerbose: true,
+        teacherAgent: null,
+    });
+}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 474fd839d14b2bdab324e5f59fce0b15ddf95afd..bc9cfde73332af5d107ad35780a31963bc4e68e9
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,8 @@
+-   Refactored Agents Server `handleChatCompletion.ts` into a thinner chat-completion facade plus focused private helper modules without changing external behavior:
+
+    -   Extracted OpenAI-compatible request parsing, runtime/agent resolution, prompt-context creation, frozen-chat persistence, shared post-response side effects, and the streaming vs non-streaming response branches into dedicated private files behind the existing `handleChatCompletion` entrypoint.
+    -   Kept the existing API-key validation, deleted-agent handling, dynamic system-context injection, AgentKit cache usage, private-mode behavior, frozen audit snapshots, self-learning persistence, streaming chunk format, and OpenAI-compatible JSON response shape intact while reducing responsibility density in the main file.
+
 -   Refactored Agents Server `errorMessages.ts` into smaller focused helpers without changing external behavior:
 
     -   Split the branching-heavy `categorizeError` flow into dedicated API-type, native-error-message, and status-code classification helpers while preserving the existing precedence.


thinking
**Investigating PowerShell output**

I'm considering why the Get-Item output is blank; it might have to do with wildcard paths. PowerShell does treat wildcards in specific ways, like with brackets. However, the path likely exists, so I don't see this as a huge issue. The user did mention wanting tests, so perhaps I should first run `npm run test-name-discrepancies`. If that fails due to types, I might explore package generation catching the compile error. Let's see how this goes!
codex
The refactor is staged and the changelog is updated. I’m on verification now, starting with `test-name-discrepancies` and then `test-package-generation` as requested.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm run test-name-discrepancies' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm run test-package-generation' in C:/Users/me/work/ai/promptbook succeeded in 9.50s:

> promptbook@0.112.0-45 test-name-discrepancies
> ts-node ./scripts/find-name-discrepancies/find-name-discrepancies.ts

🔎🩹 Find name discrepancies
[32mNo name discrepancies found[39m
 succeeded in 23.70s:

> promptbook@0.112.0-45 test-package-generation
> ts-node ./scripts/generate-packages/generate-packages.ts --skip-bundler

📦  Generating packages
[36m0️⃣  Prepare the needed information about the packages[39m
Promptbook version 0.112.0-45
[36m1️⃣  Generate entry file for each package[39m
[32mGenerated index file ./src/_packages/anthropic-claude.index.ts[39m
[32mGenerated index file ./src/_packages/azure-openai.index.ts[39m
[32mGenerated index file ./src/_packages/browser.index.ts[39m
[32mGenerated index file ./src/_packages/cli.index.ts[39m
[32mGenerated index file ./src/_packages/color.index.ts[39m
[32mGenerated index file ./src/_packages/components.index.ts[39m
[32mGenerated index file ./src/_packages/core.index.ts[39m
[32mGenerated index file ./src/_packages/deepseek.index.ts[39m
[32mGenerated index file ./src/_packages/documents.index.ts[39m
[32mGenerated index file ./src/_packages/editable.index.ts[39m
[32mGenerated index file ./src/_packages/fake-llm.index.ts[39m
[32mGenerated index file ./src/_packages/google.index.ts[39m
file update:
diff --git a/apps/agents-server/src/utils/handleChatCompletion.ts b/apps/agents-server/src/utils/handleChatCompletion.ts
index 9c96d36fad9cf24bf26d89f57372332add4e4595..cff56fafd6cfa90bb4094f9f202c4f01f6f74af1
--- a/apps/agents-server/src/utils/handleChatCompletion.ts
+++ b/apps/agents-server/src/utils/handleChatCompletion.ts
@@ -1,392 +1,16 @@
-import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
-import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
-import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
-import {
-    createBookScopedAgentReferenceResolver,
-    parseBookScopedAgentIdentifier,
-} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
-import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
-import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
-import {
-    appendMessageSuffix,
-    createMessageSuffixAppendix,
-    emulateMessageSuffixStreaming,
-    resolveMessageSuffixFromAgentSource,
-} from '@/src/utils/chat/messageSuffix';
-import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
-import { prepareToolCallsForStreaming } from './toolCallStreaming';
-import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
-import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
-import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
-import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
-import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
-import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
-import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
-import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
-import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
-import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
-import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
-import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
-import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
-import type {
-    ChatMessage,
-    ChatPrompt,
-    LlmToolDefinition,
-    string_book,
-    ToolCall,
-    TODO_any,
-    UncertainNumber,
-    Usage,
-    UsageCounts,
-} from '@promptbook-local/types';
-import { $getCurrentDate } from '@promptbook-local/utils';
 import { NextRequest, NextResponse } from 'next/server';
-import type OpenAI from 'openai';
-import { computeUsageCounts } from '../../../../src/execution/utils/computeUsageCounts';
-import { encodeChatStreamWhitespaceForTransport } from '../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
-import { isAgentDeleted } from '../app/agents/[agentName]/_utils';
 import { HTTP_STATUS_CODES } from '../constants';
-import { AgentKitCacheManager } from './cache/AgentKitCacheManager';
 import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
+import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
+import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
+import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
+import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
+import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
 import { validateApiKey } from './validateApiKey';
-import {
-    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    resolveAgentCollectionTablePrefix,
-    waitForRunningAgentPreparation,
-} from './agentPreparation';
-import {
-    resolveCachedServerAgentContext,
-    resolveCachedServerAgentModelRequirements,
-} from './cachedServerAgentRuntime';
-import { resolveAppendOnlySelfLearningAgentSource } from './resolveAppendOnlySelfLearningAgentSource';
 
 /**
- * Falls back to the estimated value when the original token count is unknown.
- *
- * @param tokenCount - Token count reported by the execution tools.
- * @param fallbackValue - Estimated token count based on text length.
- * @returns Token count to report in the OpenAI-compatible response.
- */
-function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
-    if (tokenCount.value === 0 && tokenCount.isUncertain) {
-        return {
-            value: fallbackValue,
-            isUncertain: true,
-        };
-    }
-
-    return tokenCount;
-}
-
-/**
- * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
- *
- * @param promptContent - Prompt content used for the request.
- * @param completionContent - Assistant response content.
- * @param usage - Native Promptbook usage details.
- * @returns Usage payload for compatibility responses.
- */
-function createCompatibilityUsage(
-    promptContent: string,
-    completionContent: string,
-    usage: Usage,
-): {
-    prompt_tokens: number;
-    completion_tokens: number;
-    total_tokens: number;
-    details: Usage;
-} {
-    const promptCounts = computeUsageCounts(promptContent);
-    const completionCounts = computeUsageCounts(completionContent);
-    const inputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
-        ...promptCounts,
-    };
-    const outputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
-        ...completionCounts,
-    };
-
-    const promptTokens = inputUsage.tokensCount.value;
-    const completionTokens = outputUsage.tokensCount.value;
-
-    return {
-        prompt_tokens: promptTokens,
-        completion_tokens: completionTokens,
-        total_tokens: promptTokens + completionTokens,
-        details: {
-            price: usage.price,
-            duration: usage.duration,
-            input: inputUsage,
-            output: outputUsage,
-        },
-    };
-}
-
-/**
- * Definition of open AI chat tool.
- */
-type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
-
-/**
- * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
- */
-type OpenAiCompatibilityMessageLike = {
-    role?: unknown;
-    content?: unknown;
-};
-
-/**
- * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
- */
-type HandleChatCompletionParsedRequest = {
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    threadMessages: ReadonlyArray<TODO_any>;
-    stream: TODO_any;
-    model: TODO_any;
-    responseFormat: TODO_any;
-    runtimeTools?: Array<LlmToolDefinition>;
-    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-    incomingParameters: Record<string, unknown>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Runtime state resolved before the chat model is executed.
- */
-type HandleChatCompletionRuntime = {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    agent: Agent;
-    agentSource: string_book;
-    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
-    messageSuffix: string | null;
-    agentId: string;
-    agentHash: string;
-    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Prompt, history recorder, and persistence context created for one chat call.
- */
-type HandleChatCompletionPromptContext = {
-    prompt: ChatPrompt;
-    userMessageHash: string;
-    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
-    persistedFrozenChatId?: string;
-};
-
-/**
- * Handles convert open Ai tools.
- */
-function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
-    if (!Array.isArray(rawTools)) {
-        return undefined;
-    }
-
-    const converted = rawTools
-        .map((tool) => convertOpenAiTool(tool))
-        .filter((tool): tool is LlmToolDefinition => tool !== null);
-
-    return converted.length > 0 ? converted : undefined;
-}
-
-/**
- * Handles convert open Ai tool.
- */
-function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
-    const tool = rawTool as OpenAIChatToolDefinition;
-    if (!tool || tool.type !== 'function') {
-        return null;
-    }
-
-    const functionDefinition = tool.function;
-    if (!functionDefinition) {
-        return null;
-    }
-
-    const { name, description, parameters } = functionDefinition;
-    if (typeof name !== 'string' || name.trim().length === 0) {
-        return null;
-    }
-
-    const parameterSchema = parameters ?? {};
-    if (parameterSchema.type !== 'object') {
-        return null;
-    }
-
-    const properties = parameterSchema.properties ?? {};
-    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
-
-    for (const [key, value] of Object.entries(properties)) {
-        if (!value || typeof value !== 'object') {
-            continue;
-        }
-
-        normalizedProperties[key] = {
-            type: typeof value.type === 'string' ? value.type : 'string',
-            description: typeof value.description === 'string' ? value.description : undefined,
-        };
-    }
-
-    const required =
-        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
-            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
-            : undefined;
-
-    return {
-        name,
-        description: typeof description === 'string' ? description : '',
-        parameters: {
-            type: 'object',
-            properties: normalizedProperties,
-            required,
-            additionalProperties:
-                typeof parameterSchema.additionalProperties === 'boolean'
-                    ? parameterSchema.additionalProperties
-                    : undefined,
-        },
-    };
-}
-
-/**
- * Parses open Ai tool choice.
- */
-function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
-    if (value === undefined || value === null) {
-        return undefined;
-    }
-
-    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-}
-
-/**
- * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
- */
-function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
-    if (typeof value === 'string') {
-        return value;
-    }
-
-    if (Array.isArray(value)) {
-        return value
-            .map((part) => {
-                if (typeof part === 'string') {
-                    return part;
-                }
-
-                if (!part || typeof part !== 'object') {
-                    return '';
-                }
-
-                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
-                if (typeof partLike.text === 'string') {
-                    return partLike.text;
-                }
-
-                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
-                    return '[Image input]';
-                }
-
-                if (partLike.image_url) {
-                    return '[Image input]';
-                }
-
-                return '';
-            })
-            .filter(Boolean)
-            .join('\n');
-    }
-
-    if (value === null || value === undefined) {
-        return '';
-    }
-
-    try {
-        return JSON.stringify(value, null, 2);
-    } catch {
-        return String(value);
-    }
-}
-
-/**
- * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ * Handles convert open Ai chat completion requests routed through the Agents Server.
  */
-function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
-    if (typeof role !== 'string') {
-        return 'USER';
-    }
-
-    switch (role) {
-        case 'assistant':
-            return 'AGENT';
-        case 'system':
-            return 'SYSTEM';
-        case 'tool':
-            return 'TOOL';
-        case 'developer':
-            return 'SYSTEM';
-        default:
-            return 'USER';
-    }
-}
-
-/**
- * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
- */
-function createFrozenOpenAiCompatibilityMessages(
-    rawMessages: ReadonlyArray<unknown>,
-    assistantContent?: string,
-    options: {
-        includeAssistantPlaceholder?: boolean;
-    } = {},
-): Array<ChatMessage> {
-    const startedAt = Date.now();
-    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
-        const message = rawMessage as OpenAiCompatibilityMessageLike;
-
-        return {
-            id: `openai-frozen-${index}`,
-            sender: mapOpenAiCompatibilityRoleToSender(message.role),
-            content: normalizeOpenAiCompatibilityMessageContent(message.content),
-            isComplete: true,
-            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage;
-    });
-
-    if (assistantContent !== undefined) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: assistantContent,
-            isComplete: true,
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    } else if (options.includeAssistantPlaceholder) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: '',
-            isComplete: false,
-            lifecycleState: 'running',
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    }
-
-    return messages;
-}
-
-/**
- * Handles chat completion.
- */
 export async function handleChatCompletion(
     request: NextRequest,
     params: { agentName?: string },
@@ -399,7 +23,6 @@
         return versionMismatchResponse;
     }
 
-    // Validate API key explicitly (in addition to middleware)
     const apiKeyValidation = await validateApiKey(request);
     if (!apiKeyValidation.isValid) {
         return NextResponse.json(
@@ -462,724 +85,5 @@
             { error: { message: (error as Error).message || 'Internal Server Error', type: 'server_error' } },
             { status: HTTP_STATUS_CODES.INTERNAL_SERVER_ERROR },
         );
-    }
-}
-
-/**
- * Parses and validates the OpenAI-compatible request payload.
- */
-function parseHandleChatCompletionRequest(options: {
-    body: TODO_any;
-    agentNameFromParams?: string;
-    request: NextRequest;
-}): HandleChatCompletionParsedRequest | NextResponse {
-    const { body, agentNameFromParams, request } = options;
-    const {
-        messages,
-        stream,
-        model,
-        response_format: responseFormat,
-        tools: rawTools,
-        tool_choice: toolChoice,
-        parameters: rawParameters = {},
-    } = body;
-    const agentName = (agentNameFromParams || model) as string | undefined;
-    if (!agentName) {
-        return createHandleChatCompletionErrorResponse(
-            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    if (!messages || !Array.isArray(messages) || messages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array is required and cannot be empty.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
-    if (threadMessages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array must contain at least one non-system message.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    return {
-        agentName,
-        messages,
-        threadMessages,
-        stream,
-        model,
-        responseFormat,
-        runtimeTools: convertOpenAiTools(rawTools),
-        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
-        incomingParameters:
-            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
-                ? (rawParameters as Record<string, unknown>)
-                : {},
-        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
-    };
-}
-
-/**
- * Resolves agent/runtime dependencies needed before prompt execution.
- */
-async function resolveHandleChatCompletionRuntime(options: {
-    request: NextRequest;
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    isPrivateModeEnabled: boolean;
-}): Promise<HandleChatCompletionRuntime | NextResponse> {
-    const { request, agentName, messages, isPrivateModeEnabled } = options;
-    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
-    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
-
-    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
-        return createHandleChatCompletionErrorResponse(
-            'This agent has been deleted. You can restore it from the Recycle Bin.',
-            'agent_deleted',
-            410,
-        );
-    }
-
-    const [collection, baseAgentReferenceResolver] = await Promise.all([
-        $provideAgentCollectionForServer(),
-        $provideAgentReferenceResolver(),
-    ]);
-    const localServerUrl = new URL(request.url).origin;
-    const resolvedAgentContext = await resolveCachedServerAgentContext({
-        collection,
-        agentIdentifier: agentName,
-        localServerUrl,
-        fallbackResolver: baseAgentReferenceResolver,
-    }).catch(() => null);
-
-    if (!resolvedAgentContext?.resolvedAgentSource) {
-        return createHandleChatCompletionErrorResponse(
-            `Agent '${agentName}' not found.`,
-            'invalid_request_error',
-            HTTP_STATUS_CODES.NOT_FOUND,
-        );
-    }
-
-    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
-    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
-    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
-    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
-    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
-    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
-    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
-    agentSource = agentSourceWithContext;
-
-    const preparedAgentModelRequirements = !hasDynamicContext
-        ? await resolveCachedServerAgentModelRequirements({
-              resolvedAgentContext,
-              localServerUrl,
-              fallbackResolver: baseAgentReferenceResolver,
-          })
-        : null;
-    const agentId = resolvedAgentContext.parentAgentPermanentId;
-    const agentHash = computeAgentHash(agentSource);
-    await waitForHandleChatCompletionAgentPreparation({
-        collection,
-        agentId,
-        agentHash,
-    });
-
-    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
-    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
-        await resolveHandleChatCompletionCredentials({
-            currentUserIdentity,
-            agentId,
-            calendarConnections,
-            useEmailConfiguration,
-        });
-    const agent = await createHandleChatCompletionAgent({
-        agentName,
-        agentSource,
-        agentId,
-        resolvedAgentContext,
-        localServerUrl,
-        baseAgentReferenceResolver,
-        hasDynamicContext,
-        preparedAgentModelRequirements,
-    });
-
-    return {
-        collection,
-        resolvedAgentContext,
-        agent,
-        agentSource,
-        unresolvedAgentSource,
-        messageSuffix,
-        agentId,
-        agentHash,
-        projectRepositories,
-        calendarConnections,
-        useEmailConfiguration,
-        currentUserIdentity,
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-        isPrivateModeEnabled,
-    };
-}
-
-/**
- * Creates prompt and persistence helpers for one chat completion request.
- */
-async function createHandleChatCompletionPromptContext(options: {
-    request: NextRequest;
-    title: string;
-    apiKey: string | null;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-}): Promise<HandleChatCompletionPromptContext> {
-    const { request, title, apiKey, parsedRequest, runtime } = options;
-    const timezone = request.headers.get('x-timezone') || 'UTC';
-    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
-    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
-    const recordChatHistoryMessage = await createChatHistoryRecorder({
-        request,
-        agentIdentifier: runtime.agentId,
-        agentHash: runtime.agentHash,
-        source: 'OPENAI_API_COMPATIBILITY',
-        apiKey,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        isEnabled: !runtime.isPrivateModeEnabled,
-    });
-    const userMessageHash = await recordChatHistoryMessage({
-        message: {
-            role: 'USER',
-            content: lastMessage.content,
-        },
-        previousMessageHash: null,
-    });
-    const promptParameters = composePromptParametersWithMemoryContext({
-        baseParameters: {
-            ...parsedRequest.incomingParameters,
-            timezone,
-        },
-        currentUserIdentity: runtime.currentUserIdentity,
-        agentPermanentId: runtime.agentId,
-        agentName: runtime.resolvedAgentContext.resolvedAgentName,
-        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
-        projectRepositories: runtime.projectRepositories,
-        projectGithubToken: runtime.projectGithubToken,
-        emailSmtpCredential: runtime.emailSmtpCredential,
-        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
-        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
-        calendarConnections: runtime.calendarConnections,
-    });
-    const prompt: ChatPrompt = {
-        title,
-        content: lastMessage.content,
-        modelRequirements: {
-            modelVariant: 'CHAT',
-            responseFormat: parsedRequest.responseFormat,
-            toolChoice: parsedRequest.runtimeToolChoice,
-            // We could pass 'model' from body if we wanted to enforce it, but Agent usually has its own config
-        },
-        parameters: promptParameters,
-        thread,
-        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
-    };
-
-    return {
-        prompt,
-        userMessageHash,
-        recordChatHistoryMessage,
-        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
-            isEnabled: !runtime.isPrivateModeEnabled,
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentId: runtime.agentId,
-            messages: parsedRequest.messages,
-            includeAssistantPlaceholder: true,
-            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
-        }),
-    };
-}
-
-/**
- * Handles the streaming OpenAI-compatible response branch.
- */
-function createHandleChatCompletionStreamResponse(options: {
-    request: NextRequest;
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Response {
-    const { request, title, parsedRequest, runtime, promptContext } = options;
-    const encoder = new TextEncoder();
-    const responseModel = parsedRequest.model || 'promptbook-agent';
-    const readableStream = new ReadableStream({
-        async start(controller) {
-            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
-            const created = Math.floor(Date.now() / 1000);
-            const emitSsePayload = (payload: unknown) => {
-                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
-            };
-            const emitDeltaChunk = (deltaContent: string) => {
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {
-                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
-                        },
-                        finishReason: null,
-                    }),
-                );
-            };
-
-            emitSsePayload(
-                createHandleChatCompletionChunk({
-                    runId,
-                    created,
-                    model: responseModel,
-                    delta: {
-                        role: 'assistant',
-                        content: '',
-                    },
-                    finishReason: null,
-                }),
-            );
-
-            let hasMeaningfulDelta = false;
-
-            try {
-                const handleStreamChunk = createChatStreamHandler({
-                    onDelta: (deltaContent) => {
-                        if (deltaContent.trim().length > 0) {
-                            hasMeaningfulDelta = true;
-                        }
-                        emitDeltaChunk(deltaContent);
-                    },
-                    onToolCalls: (toolCalls) => {
-                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
-                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
-                    },
-                });
-                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
-                    signal: request.signal,
-                });
-                const normalizedResponse = ensureNonEmptyChatContent({
-                    content: result.content,
-                    context: title,
-                });
-
-                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
-                    emitDeltaChunk(normalizedResponse.content);
-                }
-
-                const messageSuffixAppendix = createMessageSuffixAppendix(
-                    normalizedResponse.content,
-                    runtime.messageSuffix,
-                );
-                if (messageSuffixAppendix) {
-                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
-                        emitDeltaChunk(delta);
-                    });
-                }
-
-                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-                await finalizeHandleChatCompletionResult({
-                    responseContentWithSuffix,
-                    usage: result.usage,
-                    toolCalls: result.toolCalls,
-                    parsedRequest,
-                    runtime,
-                    promptContext,
-                    // Preserve historical behavior: streamed OpenAI-compatible calls still persist learning in private mode.
-                    shouldPersistLearnedAgentSource: true,
-                });
-
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {},
-                        finishReason: 'stop',
-                    }),
-                );
-                controller.enqueue(encoder.encode('[DONE]'));
-            } catch (error) {
-                console.error('Error during streaming:', error);
-                controller.error(error);
-            }
-            controller.close();
-        },
-    });
-
-    return new Response(readableStream, {
-        headers: {
-            'Content-Type': 'text/event-stream',
-            'Cache-Control': 'no-cache',
-            Connection: 'keep-alive',
-        },
-    });
-}
-
-/**
- * Handles the non-streaming OpenAI-compatible response branch.
- */
-async function createHandleChatCompletionJsonResponse(options: {
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Promise<NextResponse> {
-    const { title, parsedRequest, runtime, promptContext } = options;
-    const result = await runtime.agent.callChatModel(promptContext.prompt);
-    const normalizedResponse = ensureNonEmptyChatContent({
-        content: result.content,
-        context: title,
-    });
-    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-
-    await finalizeHandleChatCompletionResult({
-        responseContentWithSuffix,
-        usage: result.usage,
-        toolCalls: result.toolCalls,
-        parsedRequest,
-        runtime,
-        promptContext,
-        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
-    });
-
-    return NextResponse.json({
-        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
-        object: 'chat.completion',
-        created: Math.floor(Date.now() / 1000),
-        model: parsedRequest.model || 'promptbook-agent',
-        choices: [
-            {
-                index: 0,
-                message: {
-                    role: 'assistant',
-                    content: responseContentWithSuffix,
-                },
-                finish_reason: 'stop',
-            },
-        ],
-        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
-    });
-}
-
-/**
- * Runs the shared side effects after one successful model response.
- */
-async function finalizeHandleChatCompletionResult(options: {
-    responseContentWithSuffix: string;
-    usage: Usage;
-    toolCalls?: ReadonlyArray<ToolCall>;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
-
-    await promptContext.recordChatHistoryMessage({
-        message: {
-            role: 'MODEL',
-            content: responseContentWithSuffix,
-        },
-        previousMessageHash: promptContext.userMessageHash,
-        usage,
-    });
-
-    if (toolCalls && toolCalls.length > 0) {
-        await logCalendarToolCallsActivity({
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentPermanentId: runtime.agentId,
-            toolCalls,
-        });
-    }
-
-    await persistOpenAiCompatibilityFrozenChat({
-        isEnabled: !runtime.isPrivateModeEnabled,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        agentId: runtime.agentId,
-        chatId: promptContext.persistedFrozenChatId,
-        messages: parsedRequest.messages,
-        assistantContent: responseContentWithSuffix,
-        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
-    });
-    await persistHandleChatCompletionLearnedAgentSource({
-        runtime,
-        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
-    });
-}
-
-/**
- * Applies append-only self-learning updates when the current branch should persist them.
- */
-async function persistHandleChatCompletionLearnedAgentSource(options: {
-    runtime: HandleChatCompletionRuntime;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { runtime, shouldPersistLearnedAgentSource } = options;
-    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
-        return;
-    }
-
-    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
-        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
-        resolvedAgentSourceBeforeLearning: runtime.agentSource,
-        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
-    });
-
-    if (learnedAgentSource !== null) {
-        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
     }
 }
-
-/**
- * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
- */
-async function persistOpenAiCompatibilityFrozenChat(options: {
-    isEnabled: boolean;
-    userId: number | null | undefined;
-    agentId: string;
-    messages: ReadonlyArray<unknown>;
-    chatId?: string;
-    assistantContent?: string;
-    includeAssistantPlaceholder?: boolean;
-    failureMessage: string;
-}): Promise<string | undefined> {
-    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
-        options;
-    if (!isEnabled || !userId) {
-        return undefined;
-    }
-
-    const persistedFrozenChat = await persistFrozenUserChat({
-        userId,
-        agentPermanentId: agentId,
-        source: USER_CHAT_SOURCES.OPENAI_API,
-        chatId,
-        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
-            includeAssistantPlaceholder,
-        }),
-    }).catch((error) => {
-        console.error(failureMessage, error);
-        return null;
-    });
-
-    return persistedFrozenChat?.id;
-}
-
-/**
- * Builds one OpenAI-compatible thread from the earlier request messages.
- */
-function createHandleChatCompletionThread(previousMessages: ReadonlyArray<TODO_any>): ChatMessage[] {
-    return previousMessages.map((message: TODO_any, index: number) => ({
-        id: `msg-${index}`,
-        sender: message.role === 'assistant' ? 'agent' : 'user',
-        content: message.content,
-        isComplete: true,
-        createdAt: $getCurrentDate(),
-    }));
-}
-
-/**
- * Appends system messages to the agent source as dynamic `CONTEXT`.
- */
-function appendSystemMessagesToAgentSource(
-    agentSource: string_book,
-    messages: ReadonlyArray<TODO_any>,
-): {
-    agentSourceWithContext: string_book;
-    hasDynamicContext: boolean;
-} {
-    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
-    if (systemMessages.length === 0) {
-        return {
-            agentSourceWithContext: agentSource,
-            hasDynamicContext: false,
-        };
-    }
-
-    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
-    return {
-        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
-        hasDynamicContext: true,
-    };
-}
-
-/**
- * Waits for any running preparation job of the current agent before executing the chat call.
- */
-async function waitForHandleChatCompletionAgentPreparation(options: {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    agentId: string;
-    agentHash: string;
-}): Promise<void> {
-    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
-    const preparationWaitResult = await waitForRunningAgentPreparation({
-        tablePrefix,
-        agentPermanentId: options.agentId,
-        fingerprint: options.agentHash,
-        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    });
-
-    if (preparationWaitResult !== 'not_running') {
-        console.info('[pre-index]', 'openai_chat_wait_result', {
-            tablePrefix,
-            agentPermanentId: options.agentId,
-            agentHash: options.agentHash,
-            preparationWaitResult,
-        });
-    }
-}
-
-/**
- * Resolves request-scoped project, calendar, and email credentials for the current agent.
- */
-async function resolveHandleChatCompletionCredentials(options: {
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    agentId: string;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-}): Promise<{
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-}> {
-    const userId = options.currentUserIdentity?.userId;
-    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
-        resolveUseProjectGithubToken({
-            userId,
-            agentPermanentId: options.agentId,
-        }),
-        options.calendarConnections.length > 0
-            ? resolveUseCalendarGoogleToken({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-        options.useEmailConfiguration.isEnabled
-            ? resolveUseEmailSmtpCredential({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-    ]);
-
-    return {
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-    };
-}
-
-/**
- * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
- */
-async function createHandleChatCompletionAgent(options: {
-    agentName: string;
-    agentSource: string_book;
-    agentId: string;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    localServerUrl: string;
-    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
-    hasDynamicContext: boolean;
-    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
-}): Promise<Agent> {
-    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
-    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
-    const runtimeAgentReferenceResolver = options.hasDynamicContext
-        ? createBookScopedAgentReferenceResolver({
-              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
-              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
-              localServerUrl: options.localServerUrl,
-              fallbackResolver: options.baseAgentReferenceResolver,
-          })
-        : undefined;
-    const agentModelRequirements =
-        options.preparedAgentModelRequirements?.modelRequirements ||
-        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
-            agentReferenceResolver: runtimeAgentReferenceResolver,
-            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
-        }));
-
-    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
-        options.agentSource,
-        options.resolvedAgentContext.resolvedAgentName,
-        await baseOpenAiToolsPromise,
-        {
-            includeDynamicContext: true,
-            agentId: options.agentId,
-            modelRequirements: agentModelRequirements,
-        },
-    );
-
-    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
-        agentName: options.agentName,
-        assistantCacheKey: agentKitResult.assistantCacheKey,
-        vectorStoreHash: agentKitResult.vectorStoreHash,
-        vectorStoreId: agentKitResult.vectorStoreId,
-    });
-
-    return new Agent({
-        agentSource: options.agentSource,
-        executionTools: {
-            llm: agentKitResult.tools,
-        },
-        precomputedModelRequirements: agentModelRequirements,
-        assistantPreparationMode: 'external',
-        isVerbose: true,
-        teacherAgent: null,
-    });
-}
-
-/**
- * Creates one OpenAI-compatible streaming chunk payload.
- */
-function createHandleChatCompletionChunk(options: {
-    runId: string;
-    created: number;
-    model: TODO_any;
-    delta: Record<string, unknown>;
-    finishReason: 'stop' | null;
-}) {
-    return {
-        id: options.runId,
-        object: 'chat.completion.chunk',
-        created: options.created,
-        model: options.model,
-        choices: [
-            {
-                index: 0,
-                delta: options.delta,
-                finish_reason: options.finishReason,
-            },
-        ],
-    };
-}
-
-/**
- * Creates one consistent OpenAI-compatible error response.
- */
-function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
-    return NextResponse.json(
-        {
-            error: {
-                message,
-                type,
-            },
-        },
-        { status },
-    );
-}
-
-// TODO: [🈹] Maybe move chat thread handling here
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f49a4fa80fa21f140e25de59596e5546d62e3f7
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
@@ -0,0 +1,18 @@
+import { NextResponse } from 'next/server';
+
+/**
+ * Creates one consistent OpenAI-compatible error response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
+    return NextResponse.json(
+        {
+            error: {
+                message,
+                type,
+            },
+        },
+        { status },
+    );
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b1979f4621921bc664fed75a05851c6fcb8fba6d
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
@@ -0,0 +1,120 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import { appendMessageSuffix } from '@/src/utils/chat/messageSuffix';
+import type { UncertainNumber, Usage, UsageCounts } from '@promptbook-local/types';
+import { NextResponse } from 'next/server';
+import { computeUsageCounts } from '../../../../../src/execution/utils/computeUsageCounts';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the non-streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionJsonResponse(options: {
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Promise<NextResponse> {
+    const { title, parsedRequest, runtime, promptContext } = options;
+    const result = await runtime.agent.callChatModel(promptContext.prompt);
+    const normalizedResponse = ensureNonEmptyChatContent({
+        content: result.content,
+        context: title,
+    });
+    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+
+    await finalizeHandleChatCompletionResult({
+        responseContentWithSuffix,
+        usage: result.usage,
+        toolCalls: result.toolCalls,
+        parsedRequest,
+        runtime,
+        promptContext,
+        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
+    });
+
+    return NextResponse.json({
+        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
+        object: 'chat.completion',
+        created: Math.floor(Date.now() / 1000),
+        model: parsedRequest.model || 'promptbook-agent',
+        choices: [
+            {
+                index: 0,
+                message: {
+                    role: 'assistant',
+                    content: responseContentWithSuffix,
+                },
+                finish_reason: 'stop',
+            },
+        ],
+        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
+    });
+}
+
+/**
+ * Falls back to the estimated value when the original token count is unknown.
+ *
+ * @param tokenCount - Token count reported by the execution tools.
+ * @param fallbackValue - Estimated token count based on text length.
+ * @returns Token count to report in the OpenAI-compatible response.
+ */
+function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
+    if (tokenCount.value === 0 && tokenCount.isUncertain) {
+        return {
+            value: fallbackValue,
+            isUncertain: true,
+        };
+    }
+
+    return tokenCount;
+}
+
+/**
+ * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
+ *
+ * @param promptContent - Prompt content used for the request.
+ * @param completionContent - Assistant response content.
+ * @param usage - Native Promptbook usage details.
+ * @returns Usage payload for compatibility responses.
+ */
+function createCompatibilityUsage(
+    promptContent: string,
+    completionContent: string,
+    usage: Usage,
+): {
+    prompt_tokens: number;
+    completion_tokens: number;
+    total_tokens: number;
+    details: Usage;
+} {
+    const promptCounts = computeUsageCounts(promptContent);
+    const completionCounts = computeUsageCounts(completionContent);
+    const inputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
+        ...promptCounts,
+    };
+    const outputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
+        ...completionCounts,
+    };
+
+    const promptTokens = inputUsage.tokensCount.value;
+    const completionTokens = outputUsage.tokensCount.value;
+
+    return {
+        prompt_tokens: promptTokens,
+        completion_tokens: completionTokens,
+        total_tokens: promptTokens + completionTokens,
+        details: {
+            price: usage.price,
+            duration: usage.duration,
+            input: inputUsage,
+            output: outputUsage,
+        },
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..7aac8141a8b1237ce36430b4b4598a618f0383ac
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
@@ -0,0 +1,111 @@
+import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
+import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
+import type { ChatMessage, ChatPrompt } from '@promptbook-local/types';
+import { $getCurrentDate } from '@promptbook-local/utils';
+import type { NextRequest } from 'next/server';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Prompt, history recorder, and persistence context created for one chat call.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionPromptContext = {
+    prompt: ChatPrompt;
+    userMessageHash: string;
+    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
+    persistedFrozenChatId?: string;
+};
+
+/**
+ * Creates prompt and persistence helpers for one chat completion request.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionPromptContext(options: {
+    request: NextRequest;
+    title: string;
+    apiKey: string | null;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+}): Promise<HandleChatCompletionPromptContext> {
+    const { request, title, apiKey, parsedRequest, runtime } = options;
+    const timezone = request.headers.get('x-timezone') || 'UTC';
+    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
+    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
+    const recordChatHistoryMessage = await createChatHistoryRecorder({
+        request,
+        agentIdentifier: runtime.agentId,
+        agentHash: runtime.agentHash,
+        source: 'OPENAI_API_COMPATIBILITY',
+        apiKey,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        isEnabled: !runtime.isPrivateModeEnabled,
+    });
+    const userMessageHash = await recordChatHistoryMessage({
+        message: {
+            role: 'USER',
+            content: lastMessage.content,
+        },
+        previousMessageHash: null,
+    });
+    const promptParameters = composePromptParametersWithMemoryContext({
+        baseParameters: {
+            ...parsedRequest.incomingParameters,
+            timezone,
+        },
+        currentUserIdentity: runtime.currentUserIdentity,
+        agentPermanentId: runtime.agentId,
+        agentName: runtime.resolvedAgentContext.resolvedAgentName,
+        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
+        projectRepositories: runtime.projectRepositories,
+        projectGithubToken: runtime.projectGithubToken,
+        emailSmtpCredential: runtime.emailSmtpCredential,
+        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
+        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
+        calendarConnections: runtime.calendarConnections,
+    });
+    const prompt: ChatPrompt = {
+        title,
+        content: lastMessage.content,
+        modelRequirements: {
+            modelVariant: 'CHAT',
+            responseFormat: parsedRequest.responseFormat,
+            toolChoice: parsedRequest.runtimeToolChoice,
+        },
+        parameters: promptParameters,
+        thread,
+        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
+    };
+
+    return {
+        prompt,
+        userMessageHash,
+        recordChatHistoryMessage,
+        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
+            isEnabled: !runtime.isPrivateModeEnabled,
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentId: runtime.agentId,
+            messages: parsedRequest.messages,
+            includeAssistantPlaceholder: true,
+            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
+        }),
+    };
+}
+
+/**
+ * Builds one OpenAI-compatible thread from the earlier request messages.
+ */
+function createHandleChatCompletionThread(
+    previousMessages: HandleChatCompletionParsedRequest['threadMessages'],
+): Array<ChatMessage> {
+    return previousMessages.map((message, index) => ({
+        id: `msg-${index}`,
+        sender: message.role === 'assistant' ? 'agent' : 'user',
+        content: message.content,
+        isComplete: true,
+        createdAt: $getCurrentDate(),
+    }));
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..85e096027760bc45fb730e74f8ae3bbb721b1029
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
@@ -0,0 +1,162 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import {
+    appendMessageSuffix,
+    createMessageSuffixAppendix,
+    emulateMessageSuffixStreaming,
+} from '@/src/utils/chat/messageSuffix';
+import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
+import { encodeChatStreamWhitespaceForTransport } from '../../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
+import { prepareToolCallsForStreaming } from '../toolCallStreaming';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionStreamResponse(options: {
+    request: Request;
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Response {
+    const { request, title, parsedRequest, runtime, promptContext } = options;
+    const encoder = new TextEncoder();
+    const responseModel = parsedRequest.model || 'promptbook-agent';
+    const readableStream = new ReadableStream({
+        async start(controller) {
+            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
+            const created = Math.floor(Date.now() / 1000);
+            const emitSsePayload = (payload: unknown) => {
+                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
+            };
+            const emitDeltaChunk = (deltaContent: string) => {
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {
+                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
+                        },
+                        finishReason: null,
+                    }),
+                );
+            };
+
+            emitSsePayload(
+                createHandleChatCompletionChunk({
+                    runId,
+                    created,
+                    model: responseModel,
+                    delta: {
+                        role: 'assistant',
+                        content: '',
+                    },
+                    finishReason: null,
+                }),
+            );
+
+            let hasMeaningfulDelta = false;
+
+            try {
+                const handleStreamChunk = createChatStreamHandler({
+                    onDelta: (deltaContent) => {
+                        if (deltaContent.trim().length > 0) {
+                            hasMeaningfulDelta = true;
+                        }
+                        emitDeltaChunk(deltaContent);
+                    },
+                    onToolCalls: (toolCalls) => {
+                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
+                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
+                    },
+                });
+                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
+                    signal: request.signal,
+                });
+                const normalizedResponse = ensureNonEmptyChatContent({
+                    content: result.content,
+                    context: title,
+                });
+
+                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
+                    emitDeltaChunk(normalizedResponse.content);
+                }
+
+                const messageSuffixAppendix = createMessageSuffixAppendix(
+                    normalizedResponse.content,
+                    runtime.messageSuffix,
+                );
+                if (messageSuffixAppendix) {
+                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
+                        emitDeltaChunk(delta);
+                    });
+                }
+
+                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+                await finalizeHandleChatCompletionResult({
+                    responseContentWithSuffix,
+                    usage: result.usage,
+                    toolCalls: result.toolCalls,
+                    parsedRequest,
+                    runtime,
+                    promptContext,
+                    shouldPersistLearnedAgentSource: true,
+                });
+
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {},
+                        finishReason: 'stop',
+                    }),
+                );
+                controller.enqueue(encoder.encode('[DONE]'));
+            } catch (error) {
+                console.error('Error during streaming:', error);
+                controller.error(error);
+            }
+            controller.close();
+        },
+    });
+
+    return new Response(readableStream, {
+        headers: {
+            'Content-Type': 'text/event-stream',
+            'Cache-Control': 'no-cache',
+            Connection: 'keep-alive',
+        },
+    });
+}
+
+/**
+ * Creates one OpenAI-compatible streaming chunk payload.
+ */
+function createHandleChatCompletionChunk(options: {
+    runId: string;
+    created: number;
+    model: HandleChatCompletionParsedRequest['model'];
+    delta: Record<string, unknown>;
+    finishReason: 'stop' | null;
+}) {
+    return {
+        id: options.runId,
+        object: 'chat.completion.chunk',
+        created: options.created,
+        model: options.model,
+        choices: [
+            {
+                index: 0,
+                delta: options.delta,
+                finish_reason: options.finishReason,
+            },
+        ],
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
new file mode 100644
index 0000000000000000000000000000000000000000..dd6a7d87ea491df245dbf93d8bcb277a9a10b034
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
@@ -0,0 +1,78 @@
+import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
+import { resolveAppendOnlySelfLearningAgentSource } from '@/src/utils/resolveAppendOnlySelfLearningAgentSource';
+import type { ToolCall, Usage } from '@promptbook-local/types';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Runs the shared side effects after one successful model response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function finalizeHandleChatCompletionResult(options: {
+    responseContentWithSuffix: string;
+    usage: Usage;
+    toolCalls?: ReadonlyArray<ToolCall>;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
+
+    await promptContext.recordChatHistoryMessage({
+        message: {
+            role: 'MODEL',
+            content: responseContentWithSuffix,
+        },
+        previousMessageHash: promptContext.userMessageHash,
+        usage,
+    });
+
+    if (toolCalls && toolCalls.length > 0) {
+        await logCalendarToolCallsActivity({
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentPermanentId: runtime.agentId,
+            toolCalls,
+        });
+    }
+
+    await persistOpenAiCompatibilityFrozenChat({
+        isEnabled: !runtime.isPrivateModeEnabled,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        agentId: runtime.agentId,
+        chatId: promptContext.persistedFrozenChatId,
+        messages: parsedRequest.messages,
+        assistantContent: responseContentWithSuffix,
+        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
+    });
+    await persistHandleChatCompletionLearnedAgentSource({
+        runtime,
+        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
+    });
+}
+
+/**
+ * Applies append-only self-learning updates when the current branch should persist them.
+ */
+async function persistHandleChatCompletionLearnedAgentSource(options: {
+    runtime: HandleChatCompletionRuntime;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { runtime, shouldPersistLearnedAgentSource } = options;
+    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
+        return;
+    }
+
+    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
+        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
+        resolvedAgentSourceBeforeLearning: runtime.agentSource,
+        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
+    });
+
+    if (learnedAgentSource !== null) {
+        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
+    }
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
new file mode 100644
index 0000000000000000000000000000000000000000..58e1c26d7f428c1bdb39b36411b3f75256e3de51
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
@@ -0,0 +1,176 @@
+import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
+import type { LlmToolDefinition, TODO_any } from '@promptbook-local/types';
+import { NextRequest, NextResponse } from 'next/server';
+import type OpenAI from 'openai';
+import { HTTP_STATUS_CODES } from '../../constants';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Definition of open AI chat tool.
+ */
+type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
+
+/**
+ * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionParsedRequest = {
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    threadMessages: ReadonlyArray<TODO_any>;
+    stream: TODO_any;
+    model: TODO_any;
+    responseFormat: TODO_any;
+    runtimeTools?: Array<LlmToolDefinition>;
+    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+    incomingParameters: Record<string, unknown>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Parses and validates the OpenAI-compatible request payload.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function parseHandleChatCompletionRequest(options: {
+    body: TODO_any;
+    agentNameFromParams?: string;
+    request: NextRequest;
+}): HandleChatCompletionParsedRequest | NextResponse {
+    const { body, agentNameFromParams, request } = options;
+    const {
+        messages,
+        stream,
+        model,
+        response_format: responseFormat,
+        tools: rawTools,
+        tool_choice: toolChoice,
+        parameters: rawParameters = {},
+    } = body;
+    const agentName = (agentNameFromParams || model) as string | undefined;
+    if (!agentName) {
+        return createHandleChatCompletionErrorResponse(
+            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    if (!messages || !Array.isArray(messages) || messages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array is required and cannot be empty.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
+    if (threadMessages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array must contain at least one non-system message.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    return {
+        agentName,
+        messages,
+        threadMessages,
+        stream,
+        model,
+        responseFormat,
+        runtimeTools: convertOpenAiTools(rawTools),
+        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
+        incomingParameters:
+            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
+                ? (rawParameters as Record<string, unknown>)
+                : {},
+        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
+    };
+}
+
+/**
+ * Handles convert open Ai tools.
+ */
+function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
+    if (!Array.isArray(rawTools)) {
+        return undefined;
+    }
+
+    const converted = rawTools
+        .map((tool) => convertOpenAiTool(tool))
+        .filter((tool): tool is LlmToolDefinition => tool !== null);
+
+    return converted.length > 0 ? converted : undefined;
+}
+
+/**
+ * Handles convert open Ai tool.
+ */
+function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
+    const tool = rawTool as OpenAIChatToolDefinition;
+    if (!tool || tool.type !== 'function') {
+        return null;
+    }
+
+    const functionDefinition = tool.function;
+    if (!functionDefinition) {
+        return null;
+    }
+
+    const { name, description, parameters } = functionDefinition;
+    if (typeof name !== 'string' || name.trim().length === 0) {
+        return null;
+    }
+
+    const parameterSchema = parameters ?? {};
+    if (parameterSchema.type !== 'object') {
+        return null;
+    }
+
+    const properties = parameterSchema.properties ?? {};
+    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
+
+    for (const [key, value] of Object.entries(properties)) {
+        if (!value || typeof value !== 'object') {
+            continue;
+        }
+
+        normalizedProperties[key] = {
+            type: typeof value.type === 'string' ? value.type : 'string',
+            description: typeof value.description === 'string' ? value.description : undefined,
+        };
+    }
+
+    const required =
+        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
+            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
+            : undefined;
+
+    return {
+        name,
+        description: typeof description === 'string' ? description : '',
+        parameters: {
+            type: 'object',
+            properties: normalizedProperties,
+            required,
+            additionalProperties:
+                typeof parameterSchema.additionalProperties === 'boolean'
+                    ? parameterSchema.additionalProperties
+                    : undefined,
+        },
+    };
+}
+
+/**
+ * Parses open Ai tool choice.
+ */
+function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
+    if (value === undefined || value === null) {
+        return undefined;
+    }
+
+    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
new file mode 100644
index 0000000000000000000000000000000000000000..60e5f1b008bcf5a6a1836c06c4c3d9ebcb909a9f
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
@@ -0,0 +1,163 @@
+import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
+import type { ChatMessage } from '@promptbook-local/types';
+
+/**
+ * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
+ */
+type OpenAiCompatibilityMessageLike = {
+    role?: unknown;
+    content?: unknown;
+};
+
+/**
+ * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function persistOpenAiCompatibilityFrozenChat(options: {
+    isEnabled: boolean;
+    userId: number | null | undefined;
+    agentId: string;
+    messages: ReadonlyArray<unknown>;
+    chatId?: string;
+    assistantContent?: string;
+    includeAssistantPlaceholder?: boolean;
+    failureMessage: string;
+}): Promise<string | undefined> {
+    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
+        options;
+    if (!isEnabled || !userId) {
+        return undefined;
+    }
+
+    const persistedFrozenChat = await persistFrozenUserChat({
+        userId,
+        agentPermanentId: agentId,
+        source: USER_CHAT_SOURCES.OPENAI_API,
+        chatId,
+        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
+            includeAssistantPlaceholder,
+        }),
+    }).catch((error) => {
+        console.error(failureMessage, error);
+        return null;
+    });
+
+    return persistedFrozenChat?.id;
+}
+
+/**
+ * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
+ */
+function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
+    if (typeof value === 'string') {
+        return value;
+    }
+
+    if (Array.isArray(value)) {
+        return value
+            .map((part) => {
+                if (typeof part === 'string') {
+                    return part;
+                }
+
+                if (!part || typeof part !== 'object') {
+                    return '';
+                }
+
+                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
+                if (typeof partLike.text === 'string') {
+                    return partLike.text;
+                }
+
+                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
+                    return '[Image input]';
+                }
+
+                if (partLike.image_url) {
+                    return '[Image input]';
+                }
+
+                return '';
+            })
+            .filter(Boolean)
+            .join('\n');
+    }
+
+    if (value === null || value === undefined) {
+        return '';
+    }
+
+    try {
+        return JSON.stringify(value, null, 2);
+    } catch {
+        return String(value);
+    }
+}
+
+/**
+ * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ */
+function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
+    if (typeof role !== 'string') {
+        return 'USER';
+    }
+
+    switch (role) {
+        case 'assistant':
+            return 'AGENT';
+        case 'system':
+            return 'SYSTEM';
+        case 'tool':
+            return 'TOOL';
+        case 'developer':
+            return 'SYSTEM';
+        default:
+            return 'USER';
+    }
+}
+
+/**
+ * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
+ */
+function createFrozenOpenAiCompatibilityMessages(
+    rawMessages: ReadonlyArray<unknown>,
+    assistantContent?: string,
+    options: {
+        includeAssistantPlaceholder?: boolean;
+    } = {},
+): Array<ChatMessage> {
+    const startedAt = Date.now();
+    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
+        const message = rawMessage as OpenAiCompatibilityMessageLike;
+
+        return {
+            id: `openai-frozen-${index}`,
+            sender: mapOpenAiCompatibilityRoleToSender(message.role),
+            content: normalizeOpenAiCompatibilityMessageContent(message.content),
+            isComplete: true,
+            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage;
+    });
+
+    if (assistantContent !== undefined) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: assistantContent,
+            isComplete: true,
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    } else if (options.includeAssistantPlaceholder) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: '',
+            isComplete: false,
+            lifecycleState: 'running',
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    }
+
+    return messages;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fd44b7ea5a87168725a9e99784038137c96537ab
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
@@ -0,0 +1,305 @@
+import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
+import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
+import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
+import type { string_book, TODO_any } from '@promptbook-local/types';
+import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
+import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
+import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
+import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
+import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
+import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
+import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
+import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
+import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
+import {
+    createBookScopedAgentReferenceResolver,
+    parseBookScopedAgentIdentifier,
+} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
+import { resolveMessageSuffixFromAgentSource } from '@/src/utils/chat/messageSuffix';
+import { NextRequest, NextResponse } from 'next/server';
+import { isAgentDeleted } from '../../app/agents/[agentName]/_utils';
+import {
+    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    resolveAgentCollectionTablePrefix,
+    waitForRunningAgentPreparation,
+} from '../agentPreparation';
+import { resolveCachedServerAgentContext, resolveCachedServerAgentModelRequirements } from '../cachedServerAgentRuntime';
+import { AgentKitCacheManager } from '../cache/AgentKitCacheManager';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Runtime state resolved before the chat model is executed.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionRuntime = {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    agent: Agent;
+    agentSource: string_book;
+    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
+    messageSuffix: string | null;
+    agentId: string;
+    agentHash: string;
+    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Resolves agent/runtime dependencies needed before prompt execution.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function resolveHandleChatCompletionRuntime(options: {
+    request: NextRequest;
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    isPrivateModeEnabled: boolean;
+}): Promise<HandleChatCompletionRuntime | NextResponse> {
+    const { request, agentName, messages, isPrivateModeEnabled } = options;
+    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
+    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
+
+    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
+        return createHandleChatCompletionErrorResponse(
+            'This agent has been deleted. You can restore it from the Recycle Bin.',
+            'agent_deleted',
+            410,
+        );
+    }
+
+    const [collection, baseAgentReferenceResolver] = await Promise.all([
+        $provideAgentCollectionForServer(),
+        $provideAgentReferenceResolver(),
+    ]);
+    const localServerUrl = new URL(request.url).origin;
+    const resolvedAgentContext = await resolveCachedServerAgentContext({
+        collection,
+        agentIdentifier: agentName,
+        localServerUrl,
+        fallbackResolver: baseAgentReferenceResolver,
+    }).catch(() => null);
+
+    if (!resolvedAgentContext?.resolvedAgentSource) {
+        return createHandleChatCompletionErrorResponse(`Agent '${agentName}' not found.`, 'invalid_request_error', 404);
+    }
+
+    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
+    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
+    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
+    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
+    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
+    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
+    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
+    agentSource = agentSourceWithContext;
+
+    const preparedAgentModelRequirements = !hasDynamicContext
+        ? await resolveCachedServerAgentModelRequirements({
+              resolvedAgentContext,
+              localServerUrl,
+              fallbackResolver: baseAgentReferenceResolver,
+          })
+        : null;
+    const agentId = resolvedAgentContext.parentAgentPermanentId;
+    const agentHash = computeAgentHash(agentSource);
+    await waitForHandleChatCompletionAgentPreparation({
+        collection,
+        agentId,
+        agentHash,
+    });
+
+    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
+    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
+        await resolveHandleChatCompletionCredentials({
+            currentUserIdentity,
+            agentId,
+            calendarConnections,
+            useEmailConfiguration,
+        });
+    const agent = await createHandleChatCompletionAgent({
+        agentName,
+        agentSource,
+        agentId,
+        resolvedAgentContext,
+        localServerUrl,
+        baseAgentReferenceResolver,
+        hasDynamicContext,
+        preparedAgentModelRequirements,
+    });
+
+    return {
+        collection,
+        resolvedAgentContext,
+        agent,
+        agentSource,
+        unresolvedAgentSource,
+        messageSuffix,
+        agentId,
+        agentHash,
+        projectRepositories,
+        calendarConnections,
+        useEmailConfiguration,
+        currentUserIdentity,
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+        isPrivateModeEnabled,
+    };
+}
+
+/**
+ * Appends system messages to the agent source as dynamic `CONTEXT`.
+ */
+function appendSystemMessagesToAgentSource(
+    agentSource: string_book,
+    messages: ReadonlyArray<TODO_any>,
+): {
+    agentSourceWithContext: string_book;
+    hasDynamicContext: boolean;
+} {
+    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
+    if (systemMessages.length === 0) {
+        return {
+            agentSourceWithContext: agentSource,
+            hasDynamicContext: false,
+        };
+    }
+
+    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
+    return {
+        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
+        hasDynamicContext: true,
+    };
+}
+
+/**
+ * Waits for any running preparation job of the current agent before executing the chat call.
+ */
+async function waitForHandleChatCompletionAgentPreparation(options: {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    agentId: string;
+    agentHash: string;
+}): Promise<void> {
+    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
+    const preparationWaitResult = await waitForRunningAgentPreparation({
+        tablePrefix,
+        agentPermanentId: options.agentId,
+        fingerprint: options.agentHash,
+        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    });
+
+    if (preparationWaitResult !== 'not_running') {
+        console.info('[pre-index]', 'openai_chat_wait_result', {
+            tablePrefix,
+            agentPermanentId: options.agentId,
+            agentHash: options.agentHash,
+            preparationWaitResult,
+        });
+    }
+}
+
+/**
+ * Resolves request-scoped project, calendar, and email credentials for the current agent.
+ */
+async function resolveHandleChatCompletionCredentials(options: {
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    agentId: string;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+}): Promise<{
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+}> {
+    const userId = options.currentUserIdentity?.userId;
+    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
+        resolveUseProjectGithubToken({
+            userId,
+            agentPermanentId: options.agentId,
+        }),
+        options.calendarConnections.length > 0
+            ? resolveUseCalendarGoogleToken({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+        options.useEmailConfiguration.isEnabled
+            ? resolveUseEmailSmtpCredential({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+    ]);
+
+    return {
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+    };
+}
+
+/**
+ * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
+ */
+async function createHandleChatCompletionAgent(options: {
+    agentName: string;
+    agentSource: string_book;
+    agentId: string;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    localServerUrl: string;
+    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
+    hasDynamicContext: boolean;
+    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
+}): Promise<Agent> {
+    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
+    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
+    const runtimeAgentReferenceResolver = options.hasDynamicContext
+        ? createBookScopedAgentReferenceResolver({
+              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
+              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
+              localServerUrl: options.localServerUrl,
+              fallbackResolver: options.baseAgentReferenceResolver,
+          })
+        : undefined;
+    const agentModelRequirements =
+        options.preparedAgentModelRequirements?.modelRequirements ||
+        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
+            agentReferenceResolver: runtimeAgentReferenceResolver,
+            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
+        }));
+
+    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
+        options.agentSource,
+        options.resolvedAgentContext.resolvedAgentName,
+        await baseOpenAiToolsPromise,
+        {
+            includeDynamicContext: true,
+            agentId: options.agentId,
+            modelRequirements: agentModelRequirements,
+        },
+    );
+
+    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
+        agentName: options.agentName,
+        assistantCacheKey: agentKitResult.assistantCacheKey,
+        vectorStoreHash: agentKitResult.vectorStoreHash,
+        vectorStoreId: agentKitResult.vectorStoreId,
+    });
+
+    return new Agent({
+        agentSource: options.agentSource,
+        executionTools: {
+            llm: agentKitResult.tools,
+        },
+        precomputedModelRequirements: agentModelRequirements,
+        assistantPreparationMode: 'external',
+        isVerbose: true,
+        teacherAgent: null,
+    });
+}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 474fd839d14b2bdab324e5f59fce0b15ddf95afd..bc9cfde73332af5d107ad35780a31963bc4e68e9
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,8 @@
+-   Refactored Agents Server `handleChatCompletion.ts` into a thinner chat-completion facade plus focused private helper modules without changing external behavior:
+
+    -   Extracted OpenAI-compatible request parsing, runtime/agent resolution, prompt-context creation, frozen-chat persistence, shared post-response side effects, and the streaming vs non-streaming response branches into dedicated private files behind the existing `handleChatCompletion` entrypoint.
+    -   Kept the existing API-key validation, deleted-agent handling, dynamic system-context injection, AgentKit cache usage, private-mode behavior, frozen audit snapshots, self-learning persistence, streaming chunk format, and OpenAI-compatible JSON response shape intact while reducing responsibility density in the main file.
+
 -   Refactored Agents Server `errorMessages.ts` into smaller focused helpers without changing external behavior:
 
     -   Split the branching-heavy `categorizeError` flow into dedicated API-type, native-error-message, and status-code classification helpers while preserving the existing precedence.


thinking
**Checking git status**

I need to summarize the results from the Git repository. I think running `git diff` or `git status` would be helpful to see if any files, especially generated package index files, have changed. I'm trying to ensure that only our files and the changelog are modified. If the generated files are altered without any diffs, that might not be an issue. I should also mention that tests passed, but first, I'll double-check the status for any unexpected changes.
codex
The two required checks passed. I’m doing one last status review now to confirm exactly which files changed and whether package generation left any unrelated diffs behind.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git status --short' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git diff --stat -- apps/agents-server/src/utils/handleChatCompletion.ts apps/agents-server/src/utils/handleChatCompletion changelog/_current-preversion.md' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content apps/agents-server/src/utils/handleChatCompletion.ts -TotalCount 120' in C:/Users/me/work/ai/promptbook succeeded in 1.00s:
import { NextRequest, NextResponse } from 'next/server';
import { HTTP_STATUS_CODES } from '../constants';
import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
import { validateApiKey } from './validateApiKey';

/**
 * Handles convert open Ai chat completion requests routed through the Agents Server.
 */
export async function handleChatCompletion(
    request: NextRequest,
    params: { agentName?: string },
    title: string = 'API Chat Completion',
) {
    const { agentName: agentNameFromParams } = params;

 succeeded in 1.04s:
 .../src/utils/handleChatCompletion.ts              | 1108 +-------------------
 changelog/_current-preversion.md                   |    5 +
 2 files changed, 11 insertions(+), 1102 deletions(-)
 succeeded in 1.11s:
 M apps/agents-server/src/utils/handleChatCompletion.ts
 M changelog/_current-preversion.md
 D prompts/2026-04-5920-refactor-apps-agents-server-src-utils-error-messages-ts.log.txt
?? apps/agents-server/src/utils/handleChatCompletion/
?? prompts/2026-04-5930-refactor-apps-agents-server-src-utils-handle-chat-completion-ts.log.txt
?? prompts/2026-04-5930-refactor-apps-agents-server-src-utils-handle-chat-completion-ts.sh
file update:
diff --git a/apps/agents-server/src/utils/handleChatCompletion.ts b/apps/agents-server/src/utils/handleChatCompletion.ts
index 9c96d36fad9cf24bf26d89f57372332add4e4595..cff56fafd6cfa90bb4094f9f202c4f01f6f74af1
--- a/apps/agents-server/src/utils/handleChatCompletion.ts
+++ b/apps/agents-server/src/utils/handleChatCompletion.ts
@@ -1,392 +1,16 @@
-import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
-import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
-import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
-import {
-    createBookScopedAgentReferenceResolver,
-    parseBookScopedAgentIdentifier,
-} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
-import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
-import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
-import {
-    appendMessageSuffix,
-    createMessageSuffixAppendix,
-    emulateMessageSuffixStreaming,
-    resolveMessageSuffixFromAgentSource,
-} from '@/src/utils/chat/messageSuffix';
-import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
-import { prepareToolCallsForStreaming } from './toolCallStreaming';
-import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
-import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
-import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
-import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
-import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
-import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
-import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
-import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
-import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
-import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
-import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
-import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
-import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
-import type {
-    ChatMessage,
-    ChatPrompt,
-    LlmToolDefinition,
-    string_book,
-    ToolCall,
-    TODO_any,
-    UncertainNumber,
-    Usage,
-    UsageCounts,
-} from '@promptbook-local/types';
-import { $getCurrentDate } from '@promptbook-local/utils';
 import { NextRequest, NextResponse } from 'next/server';
-import type OpenAI from 'openai';
-import { computeUsageCounts } from '../../../../src/execution/utils/computeUsageCounts';
-import { encodeChatStreamWhitespaceForTransport } from '../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
-import { isAgentDeleted } from '../app/agents/[agentName]/_utils';
 import { HTTP_STATUS_CODES } from '../constants';
-import { AgentKitCacheManager } from './cache/AgentKitCacheManager';
 import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
+import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
+import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
+import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
+import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
+import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
 import { validateApiKey } from './validateApiKey';
-import {
-    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    resolveAgentCollectionTablePrefix,
-    waitForRunningAgentPreparation,
-} from './agentPreparation';
-import {
-    resolveCachedServerAgentContext,
-    resolveCachedServerAgentModelRequirements,
-} from './cachedServerAgentRuntime';
-import { resolveAppendOnlySelfLearningAgentSource } from './resolveAppendOnlySelfLearningAgentSource';
 
 /**
- * Falls back to the estimated value when the original token count is unknown.
- *
- * @param tokenCount - Token count reported by the execution tools.
- * @param fallbackValue - Estimated token count based on text length.
- * @returns Token count to report in the OpenAI-compatible response.
- */
-function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
-    if (tokenCount.value === 0 && tokenCount.isUncertain) {
-        return {
-            value: fallbackValue,
-            isUncertain: true,
-        };
-    }
-
-    return tokenCount;
-}
-
-/**
- * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
- *
- * @param promptContent - Prompt content used for the request.
- * @param completionContent - Assistant response content.
- * @param usage - Native Promptbook usage details.
- * @returns Usage payload for compatibility responses.
- */
-function createCompatibilityUsage(
-    promptContent: string,
-    completionContent: string,
-    usage: Usage,
-): {
-    prompt_tokens: number;
-    completion_tokens: number;
-    total_tokens: number;
-    details: Usage;
-} {
-    const promptCounts = computeUsageCounts(promptContent);
-    const completionCounts = computeUsageCounts(completionContent);
-    const inputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
-        ...promptCounts,
-    };
-    const outputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
-        ...completionCounts,
-    };
-
-    const promptTokens = inputUsage.tokensCount.value;
-    const completionTokens = outputUsage.tokensCount.value;
-
-    return {
-        prompt_tokens: promptTokens,
-        completion_tokens: completionTokens,
-        total_tokens: promptTokens + completionTokens,
-        details: {
-            price: usage.price,
-            duration: usage.duration,
-            input: inputUsage,
-            output: outputUsage,
-        },
-    };
-}
-
-/**
- * Definition of open AI chat tool.
- */
-type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
-
-/**
- * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
- */
-type OpenAiCompatibilityMessageLike = {
-    role?: unknown;
-    content?: unknown;
-};
-
-/**
- * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
- */
-type HandleChatCompletionParsedRequest = {
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    threadMessages: ReadonlyArray<TODO_any>;
-    stream: TODO_any;
-    model: TODO_any;
-    responseFormat: TODO_any;
-    runtimeTools?: Array<LlmToolDefinition>;
-    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-    incomingParameters: Record<string, unknown>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Runtime state resolved before the chat model is executed.
- */
-type HandleChatCompletionRuntime = {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    agent: Agent;
-    agentSource: string_book;
-    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
-    messageSuffix: string | null;
-    agentId: string;
-    agentHash: string;
-    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Prompt, history recorder, and persistence context created for one chat call.
- */
-type HandleChatCompletionPromptContext = {
-    prompt: ChatPrompt;
-    userMessageHash: string;
-    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
-    persistedFrozenChatId?: string;
-};
-
-/**
- * Handles convert open Ai tools.
- */
-function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
-    if (!Array.isArray(rawTools)) {
-        return undefined;
-    }
-
-    const converted = rawTools
-        .map((tool) => convertOpenAiTool(tool))
-        .filter((tool): tool is LlmToolDefinition => tool !== null);
-
-    return converted.length > 0 ? converted : undefined;
-}
-
-/**
- * Handles convert open Ai tool.
- */
-function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
-    const tool = rawTool as OpenAIChatToolDefinition;
-    if (!tool || tool.type !== 'function') {
-        return null;
-    }
-
-    const functionDefinition = tool.function;
-    if (!functionDefinition) {
-        return null;
-    }
-
-    const { name, description, parameters } = functionDefinition;
-    if (typeof name !== 'string' || name.trim().length === 0) {
-        return null;
-    }
-
-    const parameterSchema = parameters ?? {};
-    if (parameterSchema.type !== 'object') {
-        return null;
-    }
-
-    const properties = parameterSchema.properties ?? {};
-    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
-
-    for (const [key, value] of Object.entries(properties)) {
-        if (!value || typeof value !== 'object') {
-            continue;
-        }
-
-        normalizedProperties[key] = {
-            type: typeof value.type === 'string' ? value.type : 'string',
-            description: typeof value.description === 'string' ? value.description : undefined,
-        };
-    }
-
-    const required =
-        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
-            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
-            : undefined;
-
-    return {
-        name,
-        description: typeof description === 'string' ? description : '',
-        parameters: {
-            type: 'object',
-            properties: normalizedProperties,
-            required,
-            additionalProperties:
-                typeof parameterSchema.additionalProperties === 'boolean'
-                    ? parameterSchema.additionalProperties
-                    : undefined,
-        },
-    };
-}
-
-/**
- * Parses open Ai tool choice.
- */
-function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
-    if (value === undefined || value === null) {
-        return undefined;
-    }
-
-    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-}
-
-/**
- * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
- */
-function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
-    if (typeof value === 'string') {
-        return value;
-    }
-
-    if (Array.isArray(value)) {
-        return value
-            .map((part) => {
-                if (typeof part === 'string') {
-                    return part;
-                }
-
-                if (!part || typeof part !== 'object') {
-                    return '';
-                }
-
-                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
-                if (typeof partLike.text === 'string') {
-                    return partLike.text;
-                }
-
-                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
-                    return '[Image input]';
-                }
-
-                if (partLike.image_url) {
-                    return '[Image input]';
-                }
-
-                return '';
-            })
-            .filter(Boolean)
-            .join('\n');
-    }
-
-    if (value === null || value === undefined) {
-        return '';
-    }
-
-    try {
-        return JSON.stringify(value, null, 2);
-    } catch {
-        return String(value);
-    }
-}
-
-/**
- * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ * Handles convert open Ai chat completion requests routed through the Agents Server.
  */
-function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
-    if (typeof role !== 'string') {
-        return 'USER';
-    }
-
-    switch (role) {
-        case 'assistant':
-            return 'AGENT';
-        case 'system':
-            return 'SYSTEM';
-        case 'tool':
-            return 'TOOL';
-        case 'developer':
-            return 'SYSTEM';
-        default:
-            return 'USER';
-    }
-}
-
-/**
- * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
- */
-function createFrozenOpenAiCompatibilityMessages(
-    rawMessages: ReadonlyArray<unknown>,
-    assistantContent?: string,
-    options: {
-        includeAssistantPlaceholder?: boolean;
-    } = {},
-): Array<ChatMessage> {
-    const startedAt = Date.now();
-    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
-        const message = rawMessage as OpenAiCompatibilityMessageLike;
-
-        return {
-            id: `openai-frozen-${index}`,
-            sender: mapOpenAiCompatibilityRoleToSender(message.role),
-            content: normalizeOpenAiCompatibilityMessageContent(message.content),
-            isComplete: true,
-            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage;
-    });
-
-    if (assistantContent !== undefined) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: assistantContent,
-            isComplete: true,
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    } else if (options.includeAssistantPlaceholder) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: '',
-            isComplete: false,
-            lifecycleState: 'running',
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    }
-
-    return messages;
-}
-
-/**
- * Handles chat completion.
- */
 export async function handleChatCompletion(
     request: NextRequest,
     params: { agentName?: string },
@@ -399,7 +23,6 @@
         return versionMismatchResponse;
     }
 
-    // Validate API key explicitly (in addition to middleware)
     const apiKeyValidation = await validateApiKey(request);
     if (!apiKeyValidation.isValid) {
         return NextResponse.json(
@@ -462,724 +85,5 @@
             { error: { message: (error as Error).message || 'Internal Server Error', type: 'server_error' } },
             { status: HTTP_STATUS_CODES.INTERNAL_SERVER_ERROR },
         );
-    }
-}
-
-/**
- * Parses and validates the OpenAI-compatible request payload.
- */
-function parseHandleChatCompletionRequest(options: {
-    body: TODO_any;
-    agentNameFromParams?: string;
-    request: NextRequest;
-}): HandleChatCompletionParsedRequest | NextResponse {
-    const { body, agentNameFromParams, request } = options;
-    const {
-        messages,
-        stream,
-        model,
-        response_format: responseFormat,
-        tools: rawTools,
-        tool_choice: toolChoice,
-        parameters: rawParameters = {},
-    } = body;
-    const agentName = (agentNameFromParams || model) as string | undefined;
-    if (!agentName) {
-        return createHandleChatCompletionErrorResponse(
-            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    if (!messages || !Array.isArray(messages) || messages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array is required and cannot be empty.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
-    if (threadMessages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array must contain at least one non-system message.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    return {
-        agentName,
-        messages,
-        threadMessages,
-        stream,
-        model,
-        responseFormat,
-        runtimeTools: convertOpenAiTools(rawTools),
-        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
-        incomingParameters:
-            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
-                ? (rawParameters as Record<string, unknown>)
-                : {},
-        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
-    };
-}
-
-/**
- * Resolves agent/runtime dependencies needed before prompt execution.
- */
-async function resolveHandleChatCompletionRuntime(options: {
-    request: NextRequest;
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    isPrivateModeEnabled: boolean;
-}): Promise<HandleChatCompletionRuntime | NextResponse> {
-    const { request, agentName, messages, isPrivateModeEnabled } = options;
-    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
-    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
-
-    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
-        return createHandleChatCompletionErrorResponse(
-            'This agent has been deleted. You can restore it from the Recycle Bin.',
-            'agent_deleted',
-            410,
-        );
-    }
-
-    const [collection, baseAgentReferenceResolver] = await Promise.all([
-        $provideAgentCollectionForServer(),
-        $provideAgentReferenceResolver(),
-    ]);
-    const localServerUrl = new URL(request.url).origin;
-    const resolvedAgentContext = await resolveCachedServerAgentContext({
-        collection,
-        agentIdentifier: agentName,
-        localServerUrl,
-        fallbackResolver: baseAgentReferenceResolver,
-    }).catch(() => null);
-
-    if (!resolvedAgentContext?.resolvedAgentSource) {
-        return createHandleChatCompletionErrorResponse(
-            `Agent '${agentName}' not found.`,
-            'invalid_request_error',
-            HTTP_STATUS_CODES.NOT_FOUND,
-        );
-    }
-
-    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
-    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
-    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
-    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
-    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
-    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
-    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
-    agentSource = agentSourceWithContext;
-
-    const preparedAgentModelRequirements = !hasDynamicContext
-        ? await resolveCachedServerAgentModelRequirements({
-              resolvedAgentContext,
-              localServerUrl,
-              fallbackResolver: baseAgentReferenceResolver,
-          })
-        : null;
-    const agentId = resolvedAgentContext.parentAgentPermanentId;
-    const agentHash = computeAgentHash(agentSource);
-    await waitForHandleChatCompletionAgentPreparation({
-        collection,
-        agentId,
-        agentHash,
-    });
-
-    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
-    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
-        await resolveHandleChatCompletionCredentials({
-            currentUserIdentity,
-            agentId,
-            calendarConnections,
-            useEmailConfiguration,
-        });
-    const agent = await createHandleChatCompletionAgent({
-        agentName,
-        agentSource,
-        agentId,
-        resolvedAgentContext,
-        localServerUrl,
-        baseAgentReferenceResolver,
-        hasDynamicContext,
-        preparedAgentModelRequirements,
-    });
-
-    return {
-        collection,
-        resolvedAgentContext,
-        agent,
-        agentSource,
-        unresolvedAgentSource,
-        messageSuffix,
-        agentId,
-        agentHash,
-        projectRepositories,
-        calendarConnections,
-        useEmailConfiguration,
-        currentUserIdentity,
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-        isPrivateModeEnabled,
-    };
-}
-
-/**
- * Creates prompt and persistence helpers for one chat completion request.
- */
-async function createHandleChatCompletionPromptContext(options: {
-    request: NextRequest;
-    title: string;
-    apiKey: string | null;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-}): Promise<HandleChatCompletionPromptContext> {
-    const { request, title, apiKey, parsedRequest, runtime } = options;
-    const timezone = request.headers.get('x-timezone') || 'UTC';
-    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
-    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
-    const recordChatHistoryMessage = await createChatHistoryRecorder({
-        request,
-        agentIdentifier: runtime.agentId,
-        agentHash: runtime.agentHash,
-        source: 'OPENAI_API_COMPATIBILITY',
-        apiKey,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        isEnabled: !runtime.isPrivateModeEnabled,
-    });
-    const userMessageHash = await recordChatHistoryMessage({
-        message: {
-            role: 'USER',
-            content: lastMessage.content,
-        },
-        previousMessageHash: null,
-    });
-    const promptParameters = composePromptParametersWithMemoryContext({
-        baseParameters: {
-            ...parsedRequest.incomingParameters,
-            timezone,
-        },
-        currentUserIdentity: runtime.currentUserIdentity,
-        agentPermanentId: runtime.agentId,
-        agentName: runtime.resolvedAgentContext.resolvedAgentName,
-        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
-        projectRepositories: runtime.projectRepositories,
-        projectGithubToken: runtime.projectGithubToken,
-        emailSmtpCredential: runtime.emailSmtpCredential,
-        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
-        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
-        calendarConnections: runtime.calendarConnections,
-    });
-    const prompt: ChatPrompt = {
-        title,
-        content: lastMessage.content,
-        modelRequirements: {
-            modelVariant: 'CHAT',
-            responseFormat: parsedRequest.responseFormat,
-            toolChoice: parsedRequest.runtimeToolChoice,
-            // We could pass 'model' from body if we wanted to enforce it, but Agent usually has its own config
-        },
-        parameters: promptParameters,
-        thread,
-        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
-    };
-
-    return {
-        prompt,
-        userMessageHash,
-        recordChatHistoryMessage,
-        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
-            isEnabled: !runtime.isPrivateModeEnabled,
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentId: runtime.agentId,
-            messages: parsedRequest.messages,
-            includeAssistantPlaceholder: true,
-            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
-        }),
-    };
-}
-
-/**
- * Handles the streaming OpenAI-compatible response branch.
- */
-function createHandleChatCompletionStreamResponse(options: {
-    request: NextRequest;
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Response {
-    const { request, title, parsedRequest, runtime, promptContext } = options;
-    const encoder = new TextEncoder();
-    const responseModel = parsedRequest.model || 'promptbook-agent';
-    const readableStream = new ReadableStream({
-        async start(controller) {
-            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
-            const created = Math.floor(Date.now() / 1000);
-            const emitSsePayload = (payload: unknown) => {
-                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
-            };
-            const emitDeltaChunk = (deltaContent: string) => {
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {
-                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
-                        },
-                        finishReason: null,
-                    }),
-                );
-            };
-
-            emitSsePayload(
-                createHandleChatCompletionChunk({
-                    runId,
-                    created,
-                    model: responseModel,
-                    delta: {
-                        role: 'assistant',
-                        content: '',
-                    },
-                    finishReason: null,
-                }),
-            );
-
-            let hasMeaningfulDelta = false;
-
-            try {
-                const handleStreamChunk = createChatStreamHandler({
-                    onDelta: (deltaContent) => {
-                        if (deltaContent.trim().length > 0) {
-                            hasMeaningfulDelta = true;
-                        }
-                        emitDeltaChunk(deltaContent);
-                    },
-                    onToolCalls: (toolCalls) => {
-                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
-                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
-                    },
-                });
-                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
-                    signal: request.signal,
-                });
-                const normalizedResponse = ensureNonEmptyChatContent({
-                    content: result.content,
-                    context: title,
-                });
-
-                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
-                    emitDeltaChunk(normalizedResponse.content);
-                }
-
-                const messageSuffixAppendix = createMessageSuffixAppendix(
-                    normalizedResponse.content,
-                    runtime.messageSuffix,
-                );
-                if (messageSuffixAppendix) {
-                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
-                        emitDeltaChunk(delta);
-                    });
-                }
-
-                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-                await finalizeHandleChatCompletionResult({
-                    responseContentWithSuffix,
-                    usage: result.usage,
-                    toolCalls: result.toolCalls,
-                    parsedRequest,
-                    runtime,
-                    promptContext,
-                    // Preserve historical behavior: streamed OpenAI-compatible calls still persist learning in private mode.
-                    shouldPersistLearnedAgentSource: true,
-                });
-
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {},
-                        finishReason: 'stop',
-                    }),
-                );
-                controller.enqueue(encoder.encode('[DONE]'));
-            } catch (error) {
-                console.error('Error during streaming:', error);
-                controller.error(error);
-            }
-            controller.close();
-        },
-    });
-
-    return new Response(readableStream, {
-        headers: {
-            'Content-Type': 'text/event-stream',
-            'Cache-Control': 'no-cache',
-            Connection: 'keep-alive',
-        },
-    });
-}
-
-/**
- * Handles the non-streaming OpenAI-compatible response branch.
- */
-async function createHandleChatCompletionJsonResponse(options: {
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Promise<NextResponse> {
-    const { title, parsedRequest, runtime, promptContext } = options;
-    const result = await runtime.agent.callChatModel(promptContext.prompt);
-    const normalizedResponse = ensureNonEmptyChatContent({
-        content: result.content,
-        context: title,
-    });
-    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-
-    await finalizeHandleChatCompletionResult({
-        responseContentWithSuffix,
-        usage: result.usage,
-        toolCalls: result.toolCalls,
-        parsedRequest,
-        runtime,
-        promptContext,
-        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
-    });
-
-    return NextResponse.json({
-        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
-        object: 'chat.completion',
-        created: Math.floor(Date.now() / 1000),
-        model: parsedRequest.model || 'promptbook-agent',
-        choices: [
-            {
-                index: 0,
-                message: {
-                    role: 'assistant',
-                    content: responseContentWithSuffix,
-                },
-                finish_reason: 'stop',
-            },
-        ],
-        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
-    });
-}
-
-/**
- * Runs the shared side effects after one successful model response.
- */
-async function finalizeHandleChatCompletionResult(options: {
-    responseContentWithSuffix: string;
-    usage: Usage;
-    toolCalls?: ReadonlyArray<ToolCall>;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
-
-    await promptContext.recordChatHistoryMessage({
-        message: {
-            role: 'MODEL',
-            content: responseContentWithSuffix,
-        },
-        previousMessageHash: promptContext.userMessageHash,
-        usage,
-    });
-
-    if (toolCalls && toolCalls.length > 0) {
-        await logCalendarToolCallsActivity({
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentPermanentId: runtime.agentId,
-            toolCalls,
-        });
-    }
-
-    await persistOpenAiCompatibilityFrozenChat({
-        isEnabled: !runtime.isPrivateModeEnabled,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        agentId: runtime.agentId,
-        chatId: promptContext.persistedFrozenChatId,
-        messages: parsedRequest.messages,
-        assistantContent: responseContentWithSuffix,
-        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
-    });
-    await persistHandleChatCompletionLearnedAgentSource({
-        runtime,
-        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
-    });
-}
-
-/**
- * Applies append-only self-learning updates when the current branch should persist them.
- */
-async function persistHandleChatCompletionLearnedAgentSource(options: {
-    runtime: HandleChatCompletionRuntime;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { runtime, shouldPersistLearnedAgentSource } = options;
-    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
-        return;
-    }
-
-    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
-        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
-        resolvedAgentSourceBeforeLearning: runtime.agentSource,
-        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
-    });
-
-    if (learnedAgentSource !== null) {
-        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
     }
 }
-
-/**
- * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
- */
-async function persistOpenAiCompatibilityFrozenChat(options: {
-    isEnabled: boolean;
-    userId: number | null | undefined;
-    agentId: string;
-    messages: ReadonlyArray<unknown>;
-    chatId?: string;
-    assistantContent?: string;
-    includeAssistantPlaceholder?: boolean;
-    failureMessage: string;
-}): Promise<string | undefined> {
-    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
-        options;
-    if (!isEnabled || !userId) {
-        return undefined;
-    }
-
-    const persistedFrozenChat = await persistFrozenUserChat({
-        userId,
-        agentPermanentId: agentId,
-        source: USER_CHAT_SOURCES.OPENAI_API,
-        chatId,
-        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
-            includeAssistantPlaceholder,
-        }),
-    }).catch((error) => {
-        console.error(failureMessage, error);
-        return null;
-    });
-
-    return persistedFrozenChat?.id;
-}
-
-/**
- * Builds one OpenAI-compatible thread from the earlier request messages.
- */
-function createHandleChatCompletionThread(previousMessages: ReadonlyArray<TODO_any>): ChatMessage[] {
-    return previousMessages.map((message: TODO_any, index: number) => ({
-        id: `msg-${index}`,
-        sender: message.role === 'assistant' ? 'agent' : 'user',
-        content: message.content,
-        isComplete: true,
-        createdAt: $getCurrentDate(),
-    }));
-}
-
-/**
- * Appends system messages to the agent source as dynamic `CONTEXT`.
- */
-function appendSystemMessagesToAgentSource(
-    agentSource: string_book,
-    messages: ReadonlyArray<TODO_any>,
-): {
-    agentSourceWithContext: string_book;
-    hasDynamicContext: boolean;
-} {
-    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
-    if (systemMessages.length === 0) {
-        return {
-            agentSourceWithContext: agentSource,
-            hasDynamicContext: false,
-        };
-    }
-
-    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
-    return {
-        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
-        hasDynamicContext: true,
-    };
-}
-
-/**
- * Waits for any running preparation job of the current agent before executing the chat call.
- */
-async function waitForHandleChatCompletionAgentPreparation(options: {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    agentId: string;
-    agentHash: string;
-}): Promise<void> {
-    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
-    const preparationWaitResult = await waitForRunningAgentPreparation({
-        tablePrefix,
-        agentPermanentId: options.agentId,
-        fingerprint: options.agentHash,
-        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    });
-
-    if (preparationWaitResult !== 'not_running') {
-        console.info('[pre-index]', 'openai_chat_wait_result', {
-            tablePrefix,
-            agentPermanentId: options.agentId,
-            agentHash: options.agentHash,
-            preparationWaitResult,
-        });
-    }
-}
-
-/**
- * Resolves request-scoped project, calendar, and email credentials for the current agent.
- */
-async function resolveHandleChatCompletionCredentials(options: {
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    agentId: string;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-}): Promise<{
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-}> {
-    const userId = options.currentUserIdentity?.userId;
-    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
-        resolveUseProjectGithubToken({
-            userId,
-            agentPermanentId: options.agentId,
-        }),
-        options.calendarConnections.length > 0
-            ? resolveUseCalendarGoogleToken({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-        options.useEmailConfiguration.isEnabled
-            ? resolveUseEmailSmtpCredential({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-    ]);
-
-    return {
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-    };
-}
-
-/**
- * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
- */
-async function createHandleChatCompletionAgent(options: {
-    agentName: string;
-    agentSource: string_book;
-    agentId: string;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    localServerUrl: string;
-    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
-    hasDynamicContext: boolean;
-    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
-}): Promise<Agent> {
-    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
-    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
-    const runtimeAgentReferenceResolver = options.hasDynamicContext
-        ? createBookScopedAgentReferenceResolver({
-              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
-              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
-              localServerUrl: options.localServerUrl,
-              fallbackResolver: options.baseAgentReferenceResolver,
-          })
-        : undefined;
-    const agentModelRequirements =
-        options.preparedAgentModelRequirements?.modelRequirements ||
-        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
-            agentReferenceResolver: runtimeAgentReferenceResolver,
-            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
-        }));
-
-    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
-        options.agentSource,
-        options.resolvedAgentContext.resolvedAgentName,
-        await baseOpenAiToolsPromise,
-        {
-            includeDynamicContext: true,
-            agentId: options.agentId,
-            modelRequirements: agentModelRequirements,
-        },
-    );
-
-    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
-        agentName: options.agentName,
-        assistantCacheKey: agentKitResult.assistantCacheKey,
-        vectorStoreHash: agentKitResult.vectorStoreHash,
-        vectorStoreId: agentKitResult.vectorStoreId,
-    });
-
-    return new Agent({
-        agentSource: options.agentSource,
-        executionTools: {
-            llm: agentKitResult.tools,
-        },
-        precomputedModelRequirements: agentModelRequirements,
-        assistantPreparationMode: 'external',
-        isVerbose: true,
-        teacherAgent: null,
-    });
-}
-
-/**
- * Creates one OpenAI-compatible streaming chunk payload.
- */
-function createHandleChatCompletionChunk(options: {
-    runId: string;
-    created: number;
-    model: TODO_any;
-    delta: Record<string, unknown>;
-    finishReason: 'stop' | null;
-}) {
-    return {
-        id: options.runId,
-        object: 'chat.completion.chunk',
-        created: options.created,
-        model: options.model,
-        choices: [
-            {
-                index: 0,
-                delta: options.delta,
-                finish_reason: options.finishReason,
-            },
-        ],
-    };
-}
-
-/**
- * Creates one consistent OpenAI-compatible error response.
- */
-function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
-    return NextResponse.json(
-        {
-            error: {
-                message,
-                type,
-            },
-        },
-        { status },
-    );
-}
-
-// TODO: [🈹] Maybe move chat thread handling here
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f49a4fa80fa21f140e25de59596e5546d62e3f7
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
@@ -0,0 +1,18 @@
+import { NextResponse } from 'next/server';
+
+/**
+ * Creates one consistent OpenAI-compatible error response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
+    return NextResponse.json(
+        {
+            error: {
+                message,
+                type,
+            },
+        },
+        { status },
+    );
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b1979f4621921bc664fed75a05851c6fcb8fba6d
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
@@ -0,0 +1,120 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import { appendMessageSuffix } from '@/src/utils/chat/messageSuffix';
+import type { UncertainNumber, Usage, UsageCounts } from '@promptbook-local/types';
+import { NextResponse } from 'next/server';
+import { computeUsageCounts } from '../../../../../src/execution/utils/computeUsageCounts';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the non-streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionJsonResponse(options: {
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Promise<NextResponse> {
+    const { title, parsedRequest, runtime, promptContext } = options;
+    const result = await runtime.agent.callChatModel(promptContext.prompt);
+    const normalizedResponse = ensureNonEmptyChatContent({
+        content: result.content,
+        context: title,
+    });
+    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+
+    await finalizeHandleChatCompletionResult({
+        responseContentWithSuffix,
+        usage: result.usage,
+        toolCalls: result.toolCalls,
+        parsedRequest,
+        runtime,
+        promptContext,
+        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
+    });
+
+    return NextResponse.json({
+        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
+        object: 'chat.completion',
+        created: Math.floor(Date.now() / 1000),
+        model: parsedRequest.model || 'promptbook-agent',
+        choices: [
+            {
+                index: 0,
+                message: {
+                    role: 'assistant',
+                    content: responseContentWithSuffix,
+                },
+                finish_reason: 'stop',
+            },
+        ],
+        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
+    });
+}
+
+/**
+ * Falls back to the estimated value when the original token count is unknown.
+ *
+ * @param tokenCount - Token count reported by the execution tools.
+ * @param fallbackValue - Estimated token count based on text length.
+ * @returns Token count to report in the OpenAI-compatible response.
+ */
+function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
+    if (tokenCount.value === 0 && tokenCount.isUncertain) {
+        return {
+            value: fallbackValue,
+            isUncertain: true,
+        };
+    }
+
+    return tokenCount;
+}
+
+/**
+ * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
+ *
+ * @param promptContent - Prompt content used for the request.
+ * @param completionContent - Assistant response content.
+ * @param usage - Native Promptbook usage details.
+ * @returns Usage payload for compatibility responses.
+ */
+function createCompatibilityUsage(
+    promptContent: string,
+    completionContent: string,
+    usage: Usage,
+): {
+    prompt_tokens: number;
+    completion_tokens: number;
+    total_tokens: number;
+    details: Usage;
+} {
+    const promptCounts = computeUsageCounts(promptContent);
+    const completionCounts = computeUsageCounts(completionContent);
+    const inputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
+        ...promptCounts,
+    };
+    const outputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
+        ...completionCounts,
+    };
+
+    const promptTokens = inputUsage.tokensCount.value;
+    const completionTokens = outputUsage.tokensCount.value;
+
+    return {
+        prompt_tokens: promptTokens,
+        completion_tokens: completionTokens,
+        total_tokens: promptTokens + completionTokens,
+        details: {
+            price: usage.price,
+            duration: usage.duration,
+            input: inputUsage,
+            output: outputUsage,
+        },
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..7aac8141a8b1237ce36430b4b4598a618f0383ac
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
@@ -0,0 +1,111 @@
+import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
+import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
+import type { ChatMessage, ChatPrompt } from '@promptbook-local/types';
+import { $getCurrentDate } from '@promptbook-local/utils';
+import type { NextRequest } from 'next/server';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Prompt, history recorder, and persistence context created for one chat call.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionPromptContext = {
+    prompt: ChatPrompt;
+    userMessageHash: string;
+    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
+    persistedFrozenChatId?: string;
+};
+
+/**
+ * Creates prompt and persistence helpers for one chat completion request.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionPromptContext(options: {
+    request: NextRequest;
+    title: string;
+    apiKey: string | null;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+}): Promise<HandleChatCompletionPromptContext> {
+    const { request, title, apiKey, parsedRequest, runtime } = options;
+    const timezone = request.headers.get('x-timezone') || 'UTC';
+    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
+    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
+    const recordChatHistoryMessage = await createChatHistoryRecorder({
+        request,
+        agentIdentifier: runtime.agentId,
+        agentHash: runtime.agentHash,
+        source: 'OPENAI_API_COMPATIBILITY',
+        apiKey,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        isEnabled: !runtime.isPrivateModeEnabled,
+    });
+    const userMessageHash = await recordChatHistoryMessage({
+        message: {
+            role: 'USER',
+            content: lastMessage.content,
+        },
+        previousMessageHash: null,
+    });
+    const promptParameters = composePromptParametersWithMemoryContext({
+        baseParameters: {
+            ...parsedRequest.incomingParameters,
+            timezone,
+        },
+        currentUserIdentity: runtime.currentUserIdentity,
+        agentPermanentId: runtime.agentId,
+        agentName: runtime.resolvedAgentContext.resolvedAgentName,
+        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
+        projectRepositories: runtime.projectRepositories,
+        projectGithubToken: runtime.projectGithubToken,
+        emailSmtpCredential: runtime.emailSmtpCredential,
+        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
+        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
+        calendarConnections: runtime.calendarConnections,
+    });
+    const prompt: ChatPrompt = {
+        title,
+        content: lastMessage.content,
+        modelRequirements: {
+            modelVariant: 'CHAT',
+            responseFormat: parsedRequest.responseFormat,
+            toolChoice: parsedRequest.runtimeToolChoice,
+        },
+        parameters: promptParameters,
+        thread,
+        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
+    };
+
+    return {
+        prompt,
+        userMessageHash,
+        recordChatHistoryMessage,
+        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
+            isEnabled: !runtime.isPrivateModeEnabled,
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentId: runtime.agentId,
+            messages: parsedRequest.messages,
+            includeAssistantPlaceholder: true,
+            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
+        }),
+    };
+}
+
+/**
+ * Builds one OpenAI-compatible thread from the earlier request messages.
+ */
+function createHandleChatCompletionThread(
+    previousMessages: HandleChatCompletionParsedRequest['threadMessages'],
+): Array<ChatMessage> {
+    return previousMessages.map((message, index) => ({
+        id: `msg-${index}`,
+        sender: message.role === 'assistant' ? 'agent' : 'user',
+        content: message.content,
+        isComplete: true,
+        createdAt: $getCurrentDate(),
+    }));
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..85e096027760bc45fb730e74f8ae3bbb721b1029
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
@@ -0,0 +1,162 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import {
+    appendMessageSuffix,
+    createMessageSuffixAppendix,
+    emulateMessageSuffixStreaming,
+} from '@/src/utils/chat/messageSuffix';
+import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
+import { encodeChatStreamWhitespaceForTransport } from '../../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
+import { prepareToolCallsForStreaming } from '../toolCallStreaming';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionStreamResponse(options: {
+    request: Request;
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Response {
+    const { request, title, parsedRequest, runtime, promptContext } = options;
+    const encoder = new TextEncoder();
+    const responseModel = parsedRequest.model || 'promptbook-agent';
+    const readableStream = new ReadableStream({
+        async start(controller) {
+            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
+            const created = Math.floor(Date.now() / 1000);
+            const emitSsePayload = (payload: unknown) => {
+                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
+            };
+            const emitDeltaChunk = (deltaContent: string) => {
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {
+                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
+                        },
+                        finishReason: null,
+                    }),
+                );
+            };
+
+            emitSsePayload(
+                createHandleChatCompletionChunk({
+                    runId,
+                    created,
+                    model: responseModel,
+                    delta: {
+                        role: 'assistant',
+                        content: '',
+                    },
+                    finishReason: null,
+                }),
+            );
+
+            let hasMeaningfulDelta = false;
+
+            try {
+                const handleStreamChunk = createChatStreamHandler({
+                    onDelta: (deltaContent) => {
+                        if (deltaContent.trim().length > 0) {
+                            hasMeaningfulDelta = true;
+                        }
+                        emitDeltaChunk(deltaContent);
+                    },
+                    onToolCalls: (toolCalls) => {
+                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
+                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
+                    },
+                });
+                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
+                    signal: request.signal,
+                });
+                const normalizedResponse = ensureNonEmptyChatContent({
+                    content: result.content,
+                    context: title,
+                });
+
+                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
+                    emitDeltaChunk(normalizedResponse.content);
+                }
+
+                const messageSuffixAppendix = createMessageSuffixAppendix(
+                    normalizedResponse.content,
+                    runtime.messageSuffix,
+                );
+                if (messageSuffixAppendix) {
+                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
+                        emitDeltaChunk(delta);
+                    });
+                }
+
+                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+                await finalizeHandleChatCompletionResult({
+                    responseContentWithSuffix,
+                    usage: result.usage,
+                    toolCalls: result.toolCalls,
+                    parsedRequest,
+                    runtime,
+                    promptContext,
+                    shouldPersistLearnedAgentSource: true,
+                });
+
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {},
+                        finishReason: 'stop',
+                    }),
+                );
+                controller.enqueue(encoder.encode('[DONE]'));
+            } catch (error) {
+                console.error('Error during streaming:', error);
+                controller.error(error);
+            }
+            controller.close();
+        },
+    });
+
+    return new Response(readableStream, {
+        headers: {
+            'Content-Type': 'text/event-stream',
+            'Cache-Control': 'no-cache',
+            Connection: 'keep-alive',
+        },
+    });
+}
+
+/**
+ * Creates one OpenAI-compatible streaming chunk payload.
+ */
+function createHandleChatCompletionChunk(options: {
+    runId: string;
+    created: number;
+    model: HandleChatCompletionParsedRequest['model'];
+    delta: Record<string, unknown>;
+    finishReason: 'stop' | null;
+}) {
+    return {
+        id: options.runId,
+        object: 'chat.completion.chunk',
+        created: options.created,
+        model: options.model,
+        choices: [
+            {
+                index: 0,
+                delta: options.delta,
+                finish_reason: options.finishReason,
+            },
+        ],
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
new file mode 100644
index 0000000000000000000000000000000000000000..dd6a7d87ea491df245dbf93d8bcb277a9a10b034
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
@@ -0,0 +1,78 @@
+import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
+import { resolveAppendOnlySelfLearningAgentSource } from '@/src/utils/resolveAppendOnlySelfLearningAgentSource';
+import type { ToolCall, Usage } from '@promptbook-local/types';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Runs the shared side effects after one successful model response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function finalizeHandleChatCompletionResult(options: {
+    responseContentWithSuffix: string;
+    usage: Usage;
+    toolCalls?: ReadonlyArray<ToolCall>;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
+
+    await promptContext.recordChatHistoryMessage({
+        message: {
+            role: 'MODEL',
+            content: responseContentWithSuffix,
+        },
+        previousMessageHash: promptContext.userMessageHash,
+        usage,
+    });
+
+    if (toolCalls && toolCalls.length > 0) {
+        await logCalendarToolCallsActivity({
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentPermanentId: runtime.agentId,
+            toolCalls,
+        });
+    }
+
+    await persistOpenAiCompatibilityFrozenChat({
+        isEnabled: !runtime.isPrivateModeEnabled,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        agentId: runtime.agentId,
+        chatId: promptContext.persistedFrozenChatId,
+        messages: parsedRequest.messages,
+        assistantContent: responseContentWithSuffix,
+        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
+    });
+    await persistHandleChatCompletionLearnedAgentSource({
+        runtime,
+        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
+    });
+}
+
+/**
+ * Applies append-only self-learning updates when the current branch should persist them.
+ */
+async function persistHandleChatCompletionLearnedAgentSource(options: {
+    runtime: HandleChatCompletionRuntime;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { runtime, shouldPersistLearnedAgentSource } = options;
+    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
+        return;
+    }
+
+    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
+        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
+        resolvedAgentSourceBeforeLearning: runtime.agentSource,
+        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
+    });
+
+    if (learnedAgentSource !== null) {
+        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
+    }
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
new file mode 100644
index 0000000000000000000000000000000000000000..58e1c26d7f428c1bdb39b36411b3f75256e3de51
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
@@ -0,0 +1,176 @@
+import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
+import type { LlmToolDefinition, TODO_any } from '@promptbook-local/types';
+import { NextRequest, NextResponse } from 'next/server';
+import type OpenAI from 'openai';
+import { HTTP_STATUS_CODES } from '../../constants';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Definition of open AI chat tool.
+ */
+type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
+
+/**
+ * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionParsedRequest = {
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    threadMessages: ReadonlyArray<TODO_any>;
+    stream: TODO_any;
+    model: TODO_any;
+    responseFormat: TODO_any;
+    runtimeTools?: Array<LlmToolDefinition>;
+    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+    incomingParameters: Record<string, unknown>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Parses and validates the OpenAI-compatible request payload.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function parseHandleChatCompletionRequest(options: {
+    body: TODO_any;
+    agentNameFromParams?: string;
+    request: NextRequest;
+}): HandleChatCompletionParsedRequest | NextResponse {
+    const { body, agentNameFromParams, request } = options;
+    const {
+        messages,
+        stream,
+        model,
+        response_format: responseFormat,
+        tools: rawTools,
+        tool_choice: toolChoice,
+        parameters: rawParameters = {},
+    } = body;
+    const agentName = (agentNameFromParams || model) as string | undefined;
+    if (!agentName) {
+        return createHandleChatCompletionErrorResponse(
+            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    if (!messages || !Array.isArray(messages) || messages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array is required and cannot be empty.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
+    if (threadMessages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array must contain at least one non-system message.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    return {
+        agentName,
+        messages,
+        threadMessages,
+        stream,
+        model,
+        responseFormat,
+        runtimeTools: convertOpenAiTools(rawTools),
+        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
+        incomingParameters:
+            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
+                ? (rawParameters as Record<string, unknown>)
+                : {},
+        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
+    };
+}
+
+/**
+ * Handles convert open Ai tools.
+ */
+function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
+    if (!Array.isArray(rawTools)) {
+        return undefined;
+    }
+
+    const converted = rawTools
+        .map((tool) => convertOpenAiTool(tool))
+        .filter((tool): tool is LlmToolDefinition => tool !== null);
+
+    return converted.length > 0 ? converted : undefined;
+}
+
+/**
+ * Handles convert open Ai tool.
+ */
+function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
+    const tool = rawTool as OpenAIChatToolDefinition;
+    if (!tool || tool.type !== 'function') {
+        return null;
+    }
+
+    const functionDefinition = tool.function;
+    if (!functionDefinition) {
+        return null;
+    }
+
+    const { name, description, parameters } = functionDefinition;
+    if (typeof name !== 'string' || name.trim().length === 0) {
+        return null;
+    }
+
+    const parameterSchema = parameters ?? {};
+    if (parameterSchema.type !== 'object') {
+        return null;
+    }
+
+    const properties = parameterSchema.properties ?? {};
+    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
+
+    for (const [key, value] of Object.entries(properties)) {
+        if (!value || typeof value !== 'object') {
+            continue;
+        }
+
+        normalizedProperties[key] = {
+            type: typeof value.type === 'string' ? value.type : 'string',
+            description: typeof value.description === 'string' ? value.description : undefined,
+        };
+    }
+
+    const required =
+        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
+            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
+            : undefined;
+
+    return {
+        name,
+        description: typeof description === 'string' ? description : '',
+        parameters: {
+            type: 'object',
+            properties: normalizedProperties,
+            required,
+            additionalProperties:
+                typeof parameterSchema.additionalProperties === 'boolean'
+                    ? parameterSchema.additionalProperties
+                    : undefined,
+        },
+    };
+}
+
+/**
+ * Parses open Ai tool choice.
+ */
+function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
+    if (value === undefined || value === null) {
+        return undefined;
+    }
+
+    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
new file mode 100644
index 0000000000000000000000000000000000000000..60e5f1b008bcf5a6a1836c06c4c3d9ebcb909a9f
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
@@ -0,0 +1,163 @@
+import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
+import type { ChatMessage } from '@promptbook-local/types';
+
+/**
+ * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
+ */
+type OpenAiCompatibilityMessageLike = {
+    role?: unknown;
+    content?: unknown;
+};
+
+/**
+ * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function persistOpenAiCompatibilityFrozenChat(options: {
+    isEnabled: boolean;
+    userId: number | null | undefined;
+    agentId: string;
+    messages: ReadonlyArray<unknown>;
+    chatId?: string;
+    assistantContent?: string;
+    includeAssistantPlaceholder?: boolean;
+    failureMessage: string;
+}): Promise<string | undefined> {
+    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
+        options;
+    if (!isEnabled || !userId) {
+        return undefined;
+    }
+
+    const persistedFrozenChat = await persistFrozenUserChat({
+        userId,
+        agentPermanentId: agentId,
+        source: USER_CHAT_SOURCES.OPENAI_API,
+        chatId,
+        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
+            includeAssistantPlaceholder,
+        }),
+    }).catch((error) => {
+        console.error(failureMessage, error);
+        return null;
+    });
+
+    return persistedFrozenChat?.id;
+}
+
+/**
+ * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
+ */
+function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
+    if (typeof value === 'string') {
+        return value;
+    }
+
+    if (Array.isArray(value)) {
+        return value
+            .map((part) => {
+                if (typeof part === 'string') {
+                    return part;
+                }
+
+                if (!part || typeof part !== 'object') {
+                    return '';
+                }
+
+                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
+                if (typeof partLike.text === 'string') {
+                    return partLike.text;
+                }
+
+                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
+                    return '[Image input]';
+                }
+
+                if (partLike.image_url) {
+                    return '[Image input]';
+                }
+
+                return '';
+            })
+            .filter(Boolean)
+            .join('\n');
+    }
+
+    if (value === null || value === undefined) {
+        return '';
+    }
+
+    try {
+        return JSON.stringify(value, null, 2);
+    } catch {
+        return String(value);
+    }
+}
+
+/**
+ * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ */
+function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
+    if (typeof role !== 'string') {
+        return 'USER';
+    }
+
+    switch (role) {
+        case 'assistant':
+            return 'AGENT';
+        case 'system':
+            return 'SYSTEM';
+        case 'tool':
+            return 'TOOL';
+        case 'developer':
+            return 'SYSTEM';
+        default:
+            return 'USER';
+    }
+}
+
+/**
+ * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
+ */
+function createFrozenOpenAiCompatibilityMessages(
+    rawMessages: ReadonlyArray<unknown>,
+    assistantContent?: string,
+    options: {
+        includeAssistantPlaceholder?: boolean;
+    } = {},
+): Array<ChatMessage> {
+    const startedAt = Date.now();
+    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
+        const message = rawMessage as OpenAiCompatibilityMessageLike;
+
+        return {
+            id: `openai-frozen-${index}`,
+            sender: mapOpenAiCompatibilityRoleToSender(message.role),
+            content: normalizeOpenAiCompatibilityMessageContent(message.content),
+            isComplete: true,
+            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage;
+    });
+
+    if (assistantContent !== undefined) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: assistantContent,
+            isComplete: true,
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    } else if (options.includeAssistantPlaceholder) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: '',
+            isComplete: false,
+            lifecycleState: 'running',
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    }
+
+    return messages;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fd44b7ea5a87168725a9e99784038137c96537ab
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
@@ -0,0 +1,305 @@
+import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
+import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
+import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
+import type { string_book, TODO_any } from '@promptbook-local/types';
+import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
+import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
+import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
+import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
+import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
+import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
+import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
+import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
+import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
+import {
+    createBookScopedAgentReferenceResolver,
+    parseBookScopedAgentIdentifier,
+} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
+import { resolveMessageSuffixFromAgentSource } from '@/src/utils/chat/messageSuffix';
+import { NextRequest, NextResponse } from 'next/server';
+import { isAgentDeleted } from '../../app/agents/[agentName]/_utils';
+import {
+    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    resolveAgentCollectionTablePrefix,
+    waitForRunningAgentPreparation,
+} from '../agentPreparation';
+import { resolveCachedServerAgentContext, resolveCachedServerAgentModelRequirements } from '../cachedServerAgentRuntime';
+import { AgentKitCacheManager } from '../cache/AgentKitCacheManager';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Runtime state resolved before the chat model is executed.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionRuntime = {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    agent: Agent;
+    agentSource: string_book;
+    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
+    messageSuffix: string | null;
+    agentId: string;
+    agentHash: string;
+    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Resolves agent/runtime dependencies needed before prompt execution.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function resolveHandleChatCompletionRuntime(options: {
+    request: NextRequest;
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    isPrivateModeEnabled: boolean;
+}): Promise<HandleChatCompletionRuntime | NextResponse> {
+    const { request, agentName, messages, isPrivateModeEnabled } = options;
+    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
+    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
+
+    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
+        return createHandleChatCompletionErrorResponse(
+            'This agent has been deleted. You can restore it from the Recycle Bin.',
+            'agent_deleted',
+            410,
+        );
+    }
+
+    const [collection, baseAgentReferenceResolver] = await Promise.all([
+        $provideAgentCollectionForServer(),
+        $provideAgentReferenceResolver(),
+    ]);
+    const localServerUrl = new URL(request.url).origin;
+    const resolvedAgentContext = await resolveCachedServerAgentContext({
+        collection,
+        agentIdentifier: agentName,
+        localServerUrl,
+        fallbackResolver: baseAgentReferenceResolver,
+    }).catch(() => null);
+
+    if (!resolvedAgentContext?.resolvedAgentSource) {
+        return createHandleChatCompletionErrorResponse(`Agent '${agentName}' not found.`, 'invalid_request_error', 404);
+    }
+
+    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
+    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
+    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
+    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
+    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
+    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
+    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
+    agentSource = agentSourceWithContext;
+
+    const preparedAgentModelRequirements = !hasDynamicContext
+        ? await resolveCachedServerAgentModelRequirements({
+              resolvedAgentContext,
+              localServerUrl,
+              fallbackResolver: baseAgentReferenceResolver,
+          })
+        : null;
+    const agentId = resolvedAgentContext.parentAgentPermanentId;
+    const agentHash = computeAgentHash(agentSource);
+    await waitForHandleChatCompletionAgentPreparation({
+        collection,
+        agentId,
+        agentHash,
+    });
+
+    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
+    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
+        await resolveHandleChatCompletionCredentials({
+            currentUserIdentity,
+            agentId,
+            calendarConnections,
+            useEmailConfiguration,
+        });
+    const agent = await createHandleChatCompletionAgent({
+        agentName,
+        agentSource,
+        agentId,
+        resolvedAgentContext,
+        localServerUrl,
+        baseAgentReferenceResolver,
+        hasDynamicContext,
+        preparedAgentModelRequirements,
+    });
+
+    return {
+        collection,
+        resolvedAgentContext,
+        agent,
+        agentSource,
+        unresolvedAgentSource,
+        messageSuffix,
+        agentId,
+        agentHash,
+        projectRepositories,
+        calendarConnections,
+        useEmailConfiguration,
+        currentUserIdentity,
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+        isPrivateModeEnabled,
+    };
+}
+
+/**
+ * Appends system messages to the agent source as dynamic `CONTEXT`.
+ */
+function appendSystemMessagesToAgentSource(
+    agentSource: string_book,
+    messages: ReadonlyArray<TODO_any>,
+): {
+    agentSourceWithContext: string_book;
+    hasDynamicContext: boolean;
+} {
+    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
+    if (systemMessages.length === 0) {
+        return {
+            agentSourceWithContext: agentSource,
+            hasDynamicContext: false,
+        };
+    }
+
+    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
+    return {
+        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
+        hasDynamicContext: true,
+    };
+}
+
+/**
+ * Waits for any running preparation job of the current agent before executing the chat call.
+ */
+async function waitForHandleChatCompletionAgentPreparation(options: {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    agentId: string;
+    agentHash: string;
+}): Promise<void> {
+    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
+    const preparationWaitResult = await waitForRunningAgentPreparation({
+        tablePrefix,
+        agentPermanentId: options.agentId,
+        fingerprint: options.agentHash,
+        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    });
+
+    if (preparationWaitResult !== 'not_running') {
+        console.info('[pre-index]', 'openai_chat_wait_result', {
+            tablePrefix,
+            agentPermanentId: options.agentId,
+            agentHash: options.agentHash,
+            preparationWaitResult,
+        });
+    }
+}
+
+/**
+ * Resolves request-scoped project, calendar, and email credentials for the current agent.
+ */
+async function resolveHandleChatCompletionCredentials(options: {
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    agentId: string;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+}): Promise<{
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+}> {
+    const userId = options.currentUserIdentity?.userId;
+    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
+        resolveUseProjectGithubToken({
+            userId,
+            agentPermanentId: options.agentId,
+        }),
+        options.calendarConnections.length > 0
+            ? resolveUseCalendarGoogleToken({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+        options.useEmailConfiguration.isEnabled
+            ? resolveUseEmailSmtpCredential({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+    ]);
+
+    return {
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+    };
+}
+
+/**
+ * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
+ */
+async function createHandleChatCompletionAgent(options: {
+    agentName: string;
+    agentSource: string_book;
+    agentId: string;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    localServerUrl: string;
+    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
+    hasDynamicContext: boolean;
+    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
+}): Promise<Agent> {
+    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
+    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
+    const runtimeAgentReferenceResolver = options.hasDynamicContext
+        ? createBookScopedAgentReferenceResolver({
+              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
+              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
+              localServerUrl: options.localServerUrl,
+              fallbackResolver: options.baseAgentReferenceResolver,
+          })
+        : undefined;
+    const agentModelRequirements =
+        options.preparedAgentModelRequirements?.modelRequirements ||
+        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
+            agentReferenceResolver: runtimeAgentReferenceResolver,
+            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
+        }));
+
+    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
+        options.agentSource,
+        options.resolvedAgentContext.resolvedAgentName,
+        await baseOpenAiToolsPromise,
+        {
+            includeDynamicContext: true,
+            agentId: options.agentId,
+            modelRequirements: agentModelRequirements,
+        },
+    );
+
+    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
+        agentName: options.agentName,
+        assistantCacheKey: agentKitResult.assistantCacheKey,
+        vectorStoreHash: agentKitResult.vectorStoreHash,
+        vectorStoreId: agentKitResult.vectorStoreId,
+    });
+
+    return new Agent({
+        agentSource: options.agentSource,
+        executionTools: {
+            llm: agentKitResult.tools,
+        },
+        precomputedModelRequirements: agentModelRequirements,
+        assistantPreparationMode: 'external',
+        isVerbose: true,
+        teacherAgent: null,
+    });
+}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 474fd839d14b2bdab324e5f59fce0b15ddf95afd..bc9cfde73332af5d107ad35780a31963bc4e68e9
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,8 @@
+-   Refactored Agents Server `handleChatCompletion.ts` into a thinner chat-completion facade plus focused private helper modules without changing external behavior:
+
+    -   Extracted OpenAI-compatible request parsing, runtime/agent resolution, prompt-context creation, frozen-chat persistence, shared post-response side effects, and the streaming vs non-streaming response branches into dedicated private files behind the existing `handleChatCompletion` entrypoint.
+    -   Kept the existing API-key validation, deleted-agent handling, dynamic system-context injection, AgentKit cache usage, private-mode behavior, frozen audit snapshots, self-learning persistence, streaming chunk format, and OpenAI-compatible JSON response shape intact while reducing responsibility density in the main file.
+
 -   Refactored Agents Server `errorMessages.ts` into smaller focused helpers without changing external behavior:
 
     -   Split the branching-heavy `categorizeError` flow into dedicated API-type, native-error-message, and status-code classification helpers while preserving the existing precedence.


thinking
**Evaluating code status**

I see that the git status shows some deletions of prompt logs and untracked prompts, which weren't created by us, likely from the system. I wonder if it's necessary to mention these if the user only cares about the code itself. However, I don’t want to leave any confusion. It might be helpful to say there are unrelated prompt artifact files. I could run a diff on the new files to figure out the line numbers for reference.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg -n "Refactored Agents Server `handleChatCompletion.ts`" changelog/_current-preversion.md' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg -n "export async function handleChatCompletion|export function handleChatCompletion" apps/agents-server/src/utils/handleChatCompletion.ts' in C:/Users/me/work/ai/promptbookexec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg -n "export function parseHandleChatCompletionRequest|export async function resolveHandleChatCompletionRuntime|export async function createHandleChatCompletionPromptContext|export async function createHandleChatCompletionJsonResponse|export function createHandleChatCompletionStreamResponse|export async function finalizeHandleChatCompletionResult|export async function persistOpenAiCompatibilityFrozenChat|export function createHandleChatCompletionErrorResponse" apps/agents-server/src/utils/handleChatCompletion -S' in C:/Users/me/work/ai/promptbook succeeded in 1.18s:
14:export async function handleChatCompletion(
 exited 1 in 1.18s:

 succeeded in 1.15s:
apps/agents-server/src/utils/handleChatCompletion\resolveHandleChatCompletionRuntime.ts:59:export async function resolveHandleChatCompletionRuntime(options: {
apps/agents-server/src/utils/handleChatCompletion\persistOpenAiCompatibilityFrozenChat.ts:17:export async function persistOpenAiCompatibilityFrozenChat(options: {
apps/agents-server/src/utils/handleChatCompletion\parseHandleChatCompletionRequest.ts:36:export function parseHandleChatCompletionRequest(options: {
apps/agents-server/src/utils/handleChatCompletion\finalizeHandleChatCompletionResult.ts:14:export async function finalizeHandleChatCompletionResult(options: {
apps/agents-server/src/utils/handleChatCompletion\createHandleChatCompletionStreamResponse.ts:20:export function createHandleChatCompletionStreamResponse(options: {
apps/agents-server/src/utils/handleChatCompletion\createHandleChatCompletionPromptContext.ts:27:export async function createHandleChatCompletionPromptContext(options: {
apps/agents-server/src/utils/handleChatCompletion\createHandleChatCompletionJsonResponse.ts:16:export async function createHandleChatCompletionJsonResponse(options: {
apps/agents-server/src/utils/handleChatCompletion\createHandleChatCompletionErrorResponse.ts:8:export function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
file update:
diff --git a/apps/agents-server/src/utils/handleChatCompletion.ts b/apps/agents-server/src/utils/handleChatCompletion.ts
index 9c96d36fad9cf24bf26d89f57372332add4e4595..cff56fafd6cfa90bb4094f9f202c4f01f6f74af1
--- a/apps/agents-server/src/utils/handleChatCompletion.ts
+++ b/apps/agents-server/src/utils/handleChatCompletion.ts
@@ -1,392 +1,16 @@
-import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
-import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
-import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
-import {
-    createBookScopedAgentReferenceResolver,
-    parseBookScopedAgentIdentifier,
-} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
-import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
-import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
-import {
-    appendMessageSuffix,
-    createMessageSuffixAppendix,
-    emulateMessageSuffixStreaming,
-    resolveMessageSuffixFromAgentSource,
-} from '@/src/utils/chat/messageSuffix';
-import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
-import { prepareToolCallsForStreaming } from './toolCallStreaming';
-import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
-import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
-import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
-import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
-import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
-import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
-import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
-import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
-import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
-import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
-import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
-import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
-import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
-import type {
-    ChatMessage,
-    ChatPrompt,
-    LlmToolDefinition,
-    string_book,
-    ToolCall,
-    TODO_any,
-    UncertainNumber,
-    Usage,
-    UsageCounts,
-} from '@promptbook-local/types';
-import { $getCurrentDate } from '@promptbook-local/utils';
 import { NextRequest, NextResponse } from 'next/server';
-import type OpenAI from 'openai';
-import { computeUsageCounts } from '../../../../src/execution/utils/computeUsageCounts';
-import { encodeChatStreamWhitespaceForTransport } from '../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
-import { isAgentDeleted } from '../app/agents/[agentName]/_utils';
 import { HTTP_STATUS_CODES } from '../constants';
-import { AgentKitCacheManager } from './cache/AgentKitCacheManager';
 import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
+import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
+import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
+import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
+import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
+import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
 import { validateApiKey } from './validateApiKey';
-import {
-    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    resolveAgentCollectionTablePrefix,
-    waitForRunningAgentPreparation,
-} from './agentPreparation';
-import {
-    resolveCachedServerAgentContext,
-    resolveCachedServerAgentModelRequirements,
-} from './cachedServerAgentRuntime';
-import { resolveAppendOnlySelfLearningAgentSource } from './resolveAppendOnlySelfLearningAgentSource';
 
 /**
- * Falls back to the estimated value when the original token count is unknown.
- *
- * @param tokenCount - Token count reported by the execution tools.
- * @param fallbackValue - Estimated token count based on text length.
- * @returns Token count to report in the OpenAI-compatible response.
- */
-function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
-    if (tokenCount.value === 0 && tokenCount.isUncertain) {
-        return {
-            value: fallbackValue,
-            isUncertain: true,
-        };
-    }
-
-    return tokenCount;
-}
-
-/**
- * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
- *
- * @param promptContent - Prompt content used for the request.
- * @param completionContent - Assistant response content.
- * @param usage - Native Promptbook usage details.
- * @returns Usage payload for compatibility responses.
- */
-function createCompatibilityUsage(
-    promptContent: string,
-    completionContent: string,
-    usage: Usage,
-): {
-    prompt_tokens: number;
-    completion_tokens: number;
-    total_tokens: number;
-    details: Usage;
-} {
-    const promptCounts = computeUsageCounts(promptContent);
-    const completionCounts = computeUsageCounts(completionContent);
-    const inputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
-        ...promptCounts,
-    };
-    const outputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
-        ...completionCounts,
-    };
-
-    const promptTokens = inputUsage.tokensCount.value;
-    const completionTokens = outputUsage.tokensCount.value;
-
-    return {
-        prompt_tokens: promptTokens,
-        completion_tokens: completionTokens,
-        total_tokens: promptTokens + completionTokens,
-        details: {
-            price: usage.price,
-            duration: usage.duration,
-            input: inputUsage,
-            output: outputUsage,
-        },
-    };
-}
-
-/**
- * Definition of open AI chat tool.
- */
-type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
-
-/**
- * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
- */
-type OpenAiCompatibilityMessageLike = {
-    role?: unknown;
-    content?: unknown;
-};
-
-/**
- * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
- */
-type HandleChatCompletionParsedRequest = {
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    threadMessages: ReadonlyArray<TODO_any>;
-    stream: TODO_any;
-    model: TODO_any;
-    responseFormat: TODO_any;
-    runtimeTools?: Array<LlmToolDefinition>;
-    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-    incomingParameters: Record<string, unknown>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Runtime state resolved before the chat model is executed.
- */
-type HandleChatCompletionRuntime = {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    agent: Agent;
-    agentSource: string_book;
-    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
-    messageSuffix: string | null;
-    agentId: string;
-    agentHash: string;
-    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Prompt, history recorder, and persistence context created for one chat call.
- */
-type HandleChatCompletionPromptContext = {
-    prompt: ChatPrompt;
-    userMessageHash: string;
-    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
-    persistedFrozenChatId?: string;
-};
-
-/**
- * Handles convert open Ai tools.
- */
-function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
-    if (!Array.isArray(rawTools)) {
-        return undefined;
-    }
-
-    const converted = rawTools
-        .map((tool) => convertOpenAiTool(tool))
-        .filter((tool): tool is LlmToolDefinition => tool !== null);
-
-    return converted.length > 0 ? converted : undefined;
-}
-
-/**
- * Handles convert open Ai tool.
- */
-function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
-    const tool = rawTool as OpenAIChatToolDefinition;
-    if (!tool || tool.type !== 'function') {
-        return null;
-    }
-
-    const functionDefinition = tool.function;
-    if (!functionDefinition) {
-        return null;
-    }
-
-    const { name, description, parameters } = functionDefinition;
-    if (typeof name !== 'string' || name.trim().length === 0) {
-        return null;
-    }
-
-    const parameterSchema = parameters ?? {};
-    if (parameterSchema.type !== 'object') {
-        return null;
-    }
-
-    const properties = parameterSchema.properties ?? {};
-    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
-
-    for (const [key, value] of Object.entries(properties)) {
-        if (!value || typeof value !== 'object') {
-            continue;
-        }
-
-        normalizedProperties[key] = {
-            type: typeof value.type === 'string' ? value.type : 'string',
-            description: typeof value.description === 'string' ? value.description : undefined,
-        };
-    }
-
-    const required =
-        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
-            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
-            : undefined;
-
-    return {
-        name,
-        description: typeof description === 'string' ? description : '',
-        parameters: {
-            type: 'object',
-            properties: normalizedProperties,
-            required,
-            additionalProperties:
-                typeof parameterSchema.additionalProperties === 'boolean'
-                    ? parameterSchema.additionalProperties
-                    : undefined,
-        },
-    };
-}
-
-/**
- * Parses open Ai tool choice.
- */
-function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
-    if (value === undefined || value === null) {
-        return undefined;
-    }
-
-    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-}
-
-/**
- * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
- */
-function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
-    if (typeof value === 'string') {
-        return value;
-    }
-
-    if (Array.isArray(value)) {
-        return value
-            .map((part) => {
-                if (typeof part === 'string') {
-                    return part;
-                }
-
-                if (!part || typeof part !== 'object') {
-                    return '';
-                }
-
-                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
-                if (typeof partLike.text === 'string') {
-                    return partLike.text;
-                }
-
-                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
-                    return '[Image input]';
-                }
-
-                if (partLike.image_url) {
-                    return '[Image input]';
-                }
-
-                return '';
-            })
-            .filter(Boolean)
-            .join('\n');
-    }
-
-    if (value === null || value === undefined) {
-        return '';
-    }
-
-    try {
-        return JSON.stringify(value, null, 2);
-    } catch {
-        return String(value);
-    }
-}
-
-/**
- * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ * Handles convert open Ai chat completion requests routed through the Agents Server.
  */
-function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
-    if (typeof role !== 'string') {
-        return 'USER';
-    }
-
-    switch (role) {
-        case 'assistant':
-            return 'AGENT';
-        case 'system':
-            return 'SYSTEM';
-        case 'tool':
-            return 'TOOL';
-        case 'developer':
-            return 'SYSTEM';
-        default:
-            return 'USER';
-    }
-}
-
-/**
- * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
- */
-function createFrozenOpenAiCompatibilityMessages(
-    rawMessages: ReadonlyArray<unknown>,
-    assistantContent?: string,
-    options: {
-        includeAssistantPlaceholder?: boolean;
-    } = {},
-): Array<ChatMessage> {
-    const startedAt = Date.now();
-    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
-        const message = rawMessage as OpenAiCompatibilityMessageLike;
-
-        return {
-            id: `openai-frozen-${index}`,
-            sender: mapOpenAiCompatibilityRoleToSender(message.role),
-            content: normalizeOpenAiCompatibilityMessageContent(message.content),
-            isComplete: true,
-            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage;
-    });
-
-    if (assistantContent !== undefined) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: assistantContent,
-            isComplete: true,
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    } else if (options.includeAssistantPlaceholder) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: '',
-            isComplete: false,
-            lifecycleState: 'running',
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    }
-
-    return messages;
-}
-
-/**
- * Handles chat completion.
- */
 export async function handleChatCompletion(
     request: NextRequest,
     params: { agentName?: string },
@@ -399,7 +23,6 @@
         return versionMismatchResponse;
     }
 
-    // Validate API key explicitly (in addition to middleware)
     const apiKeyValidation = await validateApiKey(request);
     if (!apiKeyValidation.isValid) {
         return NextResponse.json(
@@ -462,724 +85,5 @@
             { error: { message: (error as Error).message || 'Internal Server Error', type: 'server_error' } },
             { status: HTTP_STATUS_CODES.INTERNAL_SERVER_ERROR },
         );
-    }
-}
-
-/**
- * Parses and validates the OpenAI-compatible request payload.
- */
-function parseHandleChatCompletionRequest(options: {
-    body: TODO_any;
-    agentNameFromParams?: string;
-    request: NextRequest;
-}): HandleChatCompletionParsedRequest | NextResponse {
-    const { body, agentNameFromParams, request } = options;
-    const {
-        messages,
-        stream,
-        model,
-        response_format: responseFormat,
-        tools: rawTools,
-        tool_choice: toolChoice,
-        parameters: rawParameters = {},
-    } = body;
-    const agentName = (agentNameFromParams || model) as string | undefined;
-    if (!agentName) {
-        return createHandleChatCompletionErrorResponse(
-            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    if (!messages || !Array.isArray(messages) || messages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array is required and cannot be empty.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
-    if (threadMessages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array must contain at least one non-system message.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    return {
-        agentName,
-        messages,
-        threadMessages,
-        stream,
-        model,
-        responseFormat,
-        runtimeTools: convertOpenAiTools(rawTools),
-        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
-        incomingParameters:
-            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
-                ? (rawParameters as Record<string, unknown>)
-                : {},
-        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
-    };
-}
-
-/**
- * Resolves agent/runtime dependencies needed before prompt execution.
- */
-async function resolveHandleChatCompletionRuntime(options: {
-    request: NextRequest;
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    isPrivateModeEnabled: boolean;
-}): Promise<HandleChatCompletionRuntime | NextResponse> {
-    const { request, agentName, messages, isPrivateModeEnabled } = options;
-    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
-    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
-
-    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
-        return createHandleChatCompletionErrorResponse(
-            'This agent has been deleted. You can restore it from the Recycle Bin.',
-            'agent_deleted',
-            410,
-        );
-    }
-
-    const [collection, baseAgentReferenceResolver] = await Promise.all([
-        $provideAgentCollectionForServer(),
-        $provideAgentReferenceResolver(),
-    ]);
-    const localServerUrl = new URL(request.url).origin;
-    const resolvedAgentContext = await resolveCachedServerAgentContext({
-        collection,
-        agentIdentifier: agentName,
-        localServerUrl,
-        fallbackResolver: baseAgentReferenceResolver,
-    }).catch(() => null);
-
-    if (!resolvedAgentContext?.resolvedAgentSource) {
-        return createHandleChatCompletionErrorResponse(
-            `Agent '${agentName}' not found.`,
-            'invalid_request_error',
-            HTTP_STATUS_CODES.NOT_FOUND,
-        );
-    }
-
-    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
-    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
-    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
-    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
-    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
-    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
-    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
-    agentSource = agentSourceWithContext;
-
-    const preparedAgentModelRequirements = !hasDynamicContext
-        ? await resolveCachedServerAgentModelRequirements({
-              resolvedAgentContext,
-              localServerUrl,
-              fallbackResolver: baseAgentReferenceResolver,
-          })
-        : null;
-    const agentId = resolvedAgentContext.parentAgentPermanentId;
-    const agentHash = computeAgentHash(agentSource);
-    await waitForHandleChatCompletionAgentPreparation({
-        collection,
-        agentId,
-        agentHash,
-    });
-
-    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
-    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
-        await resolveHandleChatCompletionCredentials({
-            currentUserIdentity,
-            agentId,
-            calendarConnections,
-            useEmailConfiguration,
-        });
-    const agent = await createHandleChatCompletionAgent({
-        agentName,
-        agentSource,
-        agentId,
-        resolvedAgentContext,
-        localServerUrl,
-        baseAgentReferenceResolver,
-        hasDynamicContext,
-        preparedAgentModelRequirements,
-    });
-
-    return {
-        collection,
-        resolvedAgentContext,
-        agent,
-        agentSource,
-        unresolvedAgentSource,
-        messageSuffix,
-        agentId,
-        agentHash,
-        projectRepositories,
-        calendarConnections,
-        useEmailConfiguration,
-        currentUserIdentity,
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-        isPrivateModeEnabled,
-    };
-}
-
-/**
- * Creates prompt and persistence helpers for one chat completion request.
- */
-async function createHandleChatCompletionPromptContext(options: {
-    request: NextRequest;
-    title: string;
-    apiKey: string | null;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-}): Promise<HandleChatCompletionPromptContext> {
-    const { request, title, apiKey, parsedRequest, runtime } = options;
-    const timezone = request.headers.get('x-timezone') || 'UTC';
-    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
-    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
-    const recordChatHistoryMessage = await createChatHistoryRecorder({
-        request,
-        agentIdentifier: runtime.agentId,
-        agentHash: runtime.agentHash,
-        source: 'OPENAI_API_COMPATIBILITY',
-        apiKey,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        isEnabled: !runtime.isPrivateModeEnabled,
-    });
-    const userMessageHash = await recordChatHistoryMessage({
-        message: {
-            role: 'USER',
-            content: lastMessage.content,
-        },
-        previousMessageHash: null,
-    });
-    const promptParameters = composePromptParametersWithMemoryContext({
-        baseParameters: {
-            ...parsedRequest.incomingParameters,
-            timezone,
-        },
-        currentUserIdentity: runtime.currentUserIdentity,
-        agentPermanentId: runtime.agentId,
-        agentName: runtime.resolvedAgentContext.resolvedAgentName,
-        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
-        projectRepositories: runtime.projectRepositories,
-        projectGithubToken: runtime.projectGithubToken,
-        emailSmtpCredential: runtime.emailSmtpCredential,
-        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
-        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
-        calendarConnections: runtime.calendarConnections,
-    });
-    const prompt: ChatPrompt = {
-        title,
-        content: lastMessage.content,
-        modelRequirements: {
-            modelVariant: 'CHAT',
-            responseFormat: parsedRequest.responseFormat,
-            toolChoice: parsedRequest.runtimeToolChoice,
-            // We could pass 'model' from body if we wanted to enforce it, but Agent usually has its own config
-        },
-        parameters: promptParameters,
-        thread,
-        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
-    };
-
-    return {
-        prompt,
-        userMessageHash,
-        recordChatHistoryMessage,
-        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
-            isEnabled: !runtime.isPrivateModeEnabled,
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentId: runtime.agentId,
-            messages: parsedRequest.messages,
-            includeAssistantPlaceholder: true,
-            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
-        }),
-    };
-}
-
-/**
- * Handles the streaming OpenAI-compatible response branch.
- */
-function createHandleChatCompletionStreamResponse(options: {
-    request: NextRequest;
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Response {
-    const { request, title, parsedRequest, runtime, promptContext } = options;
-    const encoder = new TextEncoder();
-    const responseModel = parsedRequest.model || 'promptbook-agent';
-    const readableStream = new ReadableStream({
-        async start(controller) {
-            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
-            const created = Math.floor(Date.now() / 1000);
-            const emitSsePayload = (payload: unknown) => {
-                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
-            };
-            const emitDeltaChunk = (deltaContent: string) => {
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {
-                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
-                        },
-                        finishReason: null,
-                    }),
-                );
-            };
-
-            emitSsePayload(
-                createHandleChatCompletionChunk({
-                    runId,
-                    created,
-                    model: responseModel,
-                    delta: {
-                        role: 'assistant',
-                        content: '',
-                    },
-                    finishReason: null,
-                }),
-            );
-
-            let hasMeaningfulDelta = false;
-
-            try {
-                const handleStreamChunk = createChatStreamHandler({
-                    onDelta: (deltaContent) => {
-                        if (deltaContent.trim().length > 0) {
-                            hasMeaningfulDelta = true;
-                        }
-                        emitDeltaChunk(deltaContent);
-                    },
-                    onToolCalls: (toolCalls) => {
-                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
-                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
-                    },
-                });
-                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
-                    signal: request.signal,
-                });
-                const normalizedResponse = ensureNonEmptyChatContent({
-                    content: result.content,
-                    context: title,
-                });
-
-                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
-                    emitDeltaChunk(normalizedResponse.content);
-                }
-
-                const messageSuffixAppendix = createMessageSuffixAppendix(
-                    normalizedResponse.content,
-                    runtime.messageSuffix,
-                );
-                if (messageSuffixAppendix) {
-                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
-                        emitDeltaChunk(delta);
-                    });
-                }
-
-                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-                await finalizeHandleChatCompletionResult({
-                    responseContentWithSuffix,
-                    usage: result.usage,
-                    toolCalls: result.toolCalls,
-                    parsedRequest,
-                    runtime,
-                    promptContext,
-                    // Preserve historical behavior: streamed OpenAI-compatible calls still persist learning in private mode.
-                    shouldPersistLearnedAgentSource: true,
-                });
-
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {},
-                        finishReason: 'stop',
-                    }),
-                );
-                controller.enqueue(encoder.encode('[DONE]'));
-            } catch (error) {
-                console.error('Error during streaming:', error);
-                controller.error(error);
-            }
-            controller.close();
-        },
-    });
-
-    return new Response(readableStream, {
-        headers: {
-            'Content-Type': 'text/event-stream',
-            'Cache-Control': 'no-cache',
-            Connection: 'keep-alive',
-        },
-    });
-}
-
-/**
- * Handles the non-streaming OpenAI-compatible response branch.
- */
-async function createHandleChatCompletionJsonResponse(options: {
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Promise<NextResponse> {
-    const { title, parsedRequest, runtime, promptContext } = options;
-    const result = await runtime.agent.callChatModel(promptContext.prompt);
-    const normalizedResponse = ensureNonEmptyChatContent({
-        content: result.content,
-        context: title,
-    });
-    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-
-    await finalizeHandleChatCompletionResult({
-        responseContentWithSuffix,
-        usage: result.usage,
-        toolCalls: result.toolCalls,
-        parsedRequest,
-        runtime,
-        promptContext,
-        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
-    });
-
-    return NextResponse.json({
-        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
-        object: 'chat.completion',
-        created: Math.floor(Date.now() / 1000),
-        model: parsedRequest.model || 'promptbook-agent',
-        choices: [
-            {
-                index: 0,
-                message: {
-                    role: 'assistant',
-                    content: responseContentWithSuffix,
-                },
-                finish_reason: 'stop',
-            },
-        ],
-        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
-    });
-}
-
-/**
- * Runs the shared side effects after one successful model response.
- */
-async function finalizeHandleChatCompletionResult(options: {
-    responseContentWithSuffix: string;
-    usage: Usage;
-    toolCalls?: ReadonlyArray<ToolCall>;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
-
-    await promptContext.recordChatHistoryMessage({
-        message: {
-            role: 'MODEL',
-            content: responseContentWithSuffix,
-        },
-        previousMessageHash: promptContext.userMessageHash,
-        usage,
-    });
-
-    if (toolCalls && toolCalls.length > 0) {
-        await logCalendarToolCallsActivity({
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentPermanentId: runtime.agentId,
-            toolCalls,
-        });
-    }
-
-    await persistOpenAiCompatibilityFrozenChat({
-        isEnabled: !runtime.isPrivateModeEnabled,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        agentId: runtime.agentId,
-        chatId: promptContext.persistedFrozenChatId,
-        messages: parsedRequest.messages,
-        assistantContent: responseContentWithSuffix,
-        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
-    });
-    await persistHandleChatCompletionLearnedAgentSource({
-        runtime,
-        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
-    });
-}
-
-/**
- * Applies append-only self-learning updates when the current branch should persist them.
- */
-async function persistHandleChatCompletionLearnedAgentSource(options: {
-    runtime: HandleChatCompletionRuntime;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { runtime, shouldPersistLearnedAgentSource } = options;
-    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
-        return;
-    }
-
-    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
-        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
-        resolvedAgentSourceBeforeLearning: runtime.agentSource,
-        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
-    });
-
-    if (learnedAgentSource !== null) {
-        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
     }
 }
-
-/**
- * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
- */
-async function persistOpenAiCompatibilityFrozenChat(options: {
-    isEnabled: boolean;
-    userId: number | null | undefined;
-    agentId: string;
-    messages: ReadonlyArray<unknown>;
-    chatId?: string;
-    assistantContent?: string;
-    includeAssistantPlaceholder?: boolean;
-    failureMessage: string;
-}): Promise<string | undefined> {
-    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
-        options;
-    if (!isEnabled || !userId) {
-        return undefined;
-    }
-
-    const persistedFrozenChat = await persistFrozenUserChat({
-        userId,
-        agentPermanentId: agentId,
-        source: USER_CHAT_SOURCES.OPENAI_API,
-        chatId,
-        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
-            includeAssistantPlaceholder,
-        }),
-    }).catch((error) => {
-        console.error(failureMessage, error);
-        return null;
-    });
-
-    return persistedFrozenChat?.id;
-}
-
-/**
- * Builds one OpenAI-compatible thread from the earlier request messages.
- */
-function createHandleChatCompletionThread(previousMessages: ReadonlyArray<TODO_any>): ChatMessage[] {
-    return previousMessages.map((message: TODO_any, index: number) => ({
-        id: `msg-${index}`,
-        sender: message.role === 'assistant' ? 'agent' : 'user',
-        content: message.content,
-        isComplete: true,
-        createdAt: $getCurrentDate(),
-    }));
-}
-
-/**
- * Appends system messages to the agent source as dynamic `CONTEXT`.
- */
-function appendSystemMessagesToAgentSource(
-    agentSource: string_book,
-    messages: ReadonlyArray<TODO_any>,
-): {
-    agentSourceWithContext: string_book;
-    hasDynamicContext: boolean;
-} {
-    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
-    if (systemMessages.length === 0) {
-        return {
-            agentSourceWithContext: agentSource,
-            hasDynamicContext: false,
-        };
-    }
-
-    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
-    return {
-        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
-        hasDynamicContext: true,
-    };
-}
-
-/**
- * Waits for any running preparation job of the current agent before executing the chat call.
- */
-async function waitForHandleChatCompletionAgentPreparation(options: {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    agentId: string;
-    agentHash: string;
-}): Promise<void> {
-    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
-    const preparationWaitResult = await waitForRunningAgentPreparation({
-        tablePrefix,
-        agentPermanentId: options.agentId,
-        fingerprint: options.agentHash,
-        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    });
-
-    if (preparationWaitResult !== 'not_running') {
-        console.info('[pre-index]', 'openai_chat_wait_result', {
-            tablePrefix,
-            agentPermanentId: options.agentId,
-            agentHash: options.agentHash,
-            preparationWaitResult,
-        });
-    }
-}
-
-/**
- * Resolves request-scoped project, calendar, and email credentials for the current agent.
- */
-async function resolveHandleChatCompletionCredentials(options: {
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    agentId: string;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-}): Promise<{
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-}> {
-    const userId = options.currentUserIdentity?.userId;
-    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
-        resolveUseProjectGithubToken({
-            userId,
-            agentPermanentId: options.agentId,
-        }),
-        options.calendarConnections.length > 0
-            ? resolveUseCalendarGoogleToken({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-        options.useEmailConfiguration.isEnabled
-            ? resolveUseEmailSmtpCredential({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-    ]);
-
-    return {
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-    };
-}
-
-/**
- * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
- */
-async function createHandleChatCompletionAgent(options: {
-    agentName: string;
-    agentSource: string_book;
-    agentId: string;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    localServerUrl: string;
-    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
-    hasDynamicContext: boolean;
-    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
-}): Promise<Agent> {
-    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
-    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
-    const runtimeAgentReferenceResolver = options.hasDynamicContext
-        ? createBookScopedAgentReferenceResolver({
-              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
-              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
-              localServerUrl: options.localServerUrl,
-              fallbackResolver: options.baseAgentReferenceResolver,
-          })
-        : undefined;
-    const agentModelRequirements =
-        options.preparedAgentModelRequirements?.modelRequirements ||
-        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
-            agentReferenceResolver: runtimeAgentReferenceResolver,
-            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
-        }));
-
-    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
-        options.agentSource,
-        options.resolvedAgentContext.resolvedAgentName,
-        await baseOpenAiToolsPromise,
-        {
-            includeDynamicContext: true,
-            agentId: options.agentId,
-            modelRequirements: agentModelRequirements,
-        },
-    );
-
-    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
-        agentName: options.agentName,
-        assistantCacheKey: agentKitResult.assistantCacheKey,
-        vectorStoreHash: agentKitResult.vectorStoreHash,
-        vectorStoreId: agentKitResult.vectorStoreId,
-    });
-
-    return new Agent({
-        agentSource: options.agentSource,
-        executionTools: {
-            llm: agentKitResult.tools,
-        },
-        precomputedModelRequirements: agentModelRequirements,
-        assistantPreparationMode: 'external',
-        isVerbose: true,
-        teacherAgent: null,
-    });
-}
-
-/**
- * Creates one OpenAI-compatible streaming chunk payload.
- */
-function createHandleChatCompletionChunk(options: {
-    runId: string;
-    created: number;
-    model: TODO_any;
-    delta: Record<string, unknown>;
-    finishReason: 'stop' | null;
-}) {
-    return {
-        id: options.runId,
-        object: 'chat.completion.chunk',
-        created: options.created,
-        model: options.model,
-        choices: [
-            {
-                index: 0,
-                delta: options.delta,
-                finish_reason: options.finishReason,
-            },
-        ],
-    };
-}
-
-/**
- * Creates one consistent OpenAI-compatible error response.
- */
-function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
-    return NextResponse.json(
-        {
-            error: {
-                message,
-                type,
-            },
-        },
-        { status },
-    );
-}
-
-// TODO: [🈹] Maybe move chat thread handling here
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f49a4fa80fa21f140e25de59596e5546d62e3f7
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
@@ -0,0 +1,18 @@
+import { NextResponse } from 'next/server';
+
+/**
+ * Creates one consistent OpenAI-compatible error response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
+    return NextResponse.json(
+        {
+            error: {
+                message,
+                type,
+            },
+        },
+        { status },
+    );
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b1979f4621921bc664fed75a05851c6fcb8fba6d
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
@@ -0,0 +1,120 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import { appendMessageSuffix } from '@/src/utils/chat/messageSuffix';
+import type { UncertainNumber, Usage, UsageCounts } from '@promptbook-local/types';
+import { NextResponse } from 'next/server';
+import { computeUsageCounts } from '../../../../../src/execution/utils/computeUsageCounts';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the non-streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionJsonResponse(options: {
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Promise<NextResponse> {
+    const { title, parsedRequest, runtime, promptContext } = options;
+    const result = await runtime.agent.callChatModel(promptContext.prompt);
+    const normalizedResponse = ensureNonEmptyChatContent({
+        content: result.content,
+        context: title,
+    });
+    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+
+    await finalizeHandleChatCompletionResult({
+        responseContentWithSuffix,
+        usage: result.usage,
+        toolCalls: result.toolCalls,
+        parsedRequest,
+        runtime,
+        promptContext,
+        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
+    });
+
+    return NextResponse.json({
+        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
+        object: 'chat.completion',
+        created: Math.floor(Date.now() / 1000),
+        model: parsedRequest.model || 'promptbook-agent',
+        choices: [
+            {
+                index: 0,
+                message: {
+                    role: 'assistant',
+                    content: responseContentWithSuffix,
+                },
+                finish_reason: 'stop',
+            },
+        ],
+        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
+    });
+}
+
+/**
+ * Falls back to the estimated value when the original token count is unknown.
+ *
+ * @param tokenCount - Token count reported by the execution tools.
+ * @param fallbackValue - Estimated token count based on text length.
+ * @returns Token count to report in the OpenAI-compatible response.
+ */
+function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
+    if (tokenCount.value === 0 && tokenCount.isUncertain) {
+        return {
+            value: fallbackValue,
+            isUncertain: true,
+        };
+    }
+
+    return tokenCount;
+}
+
+/**
+ * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
+ *
+ * @param promptContent - Prompt content used for the request.
+ * @param completionContent - Assistant response content.
+ * @param usage - Native Promptbook usage details.
+ * @returns Usage payload for compatibility responses.
+ */
+function createCompatibilityUsage(
+    promptContent: string,
+    completionContent: string,
+    usage: Usage,
+): {
+    prompt_tokens: number;
+    completion_tokens: number;
+    total_tokens: number;
+    details: Usage;
+} {
+    const promptCounts = computeUsageCounts(promptContent);
+    const completionCounts = computeUsageCounts(completionContent);
+    const inputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
+        ...promptCounts,
+    };
+    const outputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
+        ...completionCounts,
+    };
+
+    const promptTokens = inputUsage.tokensCount.value;
+    const completionTokens = outputUsage.tokensCount.value;
+
+    return {
+        prompt_tokens: promptTokens,
+        completion_tokens: completionTokens,
+        total_tokens: promptTokens + completionTokens,
+        details: {
+            price: usage.price,
+            duration: usage.duration,
+            input: inputUsage,
+            output: outputUsage,
+        },
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..7aac8141a8b1237ce36430b4b4598a618f0383ac
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
@@ -0,0 +1,111 @@
+import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
+import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
+import type { ChatMessage, ChatPrompt } from '@promptbook-local/types';
+import { $getCurrentDate } from '@promptbook-local/utils';
+import type { NextRequest } from 'next/server';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Prompt, history recorder, and persistence context created for one chat call.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionPromptContext = {
+    prompt: ChatPrompt;
+    userMessageHash: string;
+    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
+    persistedFrozenChatId?: string;
+};
+
+/**
+ * Creates prompt and persistence helpers for one chat completion request.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionPromptContext(options: {
+    request: NextRequest;
+    title: string;
+    apiKey: string | null;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+}): Promise<HandleChatCompletionPromptContext> {
+    const { request, title, apiKey, parsedRequest, runtime } = options;
+    const timezone = request.headers.get('x-timezone') || 'UTC';
+    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
+    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
+    const recordChatHistoryMessage = await createChatHistoryRecorder({
+        request,
+        agentIdentifier: runtime.agentId,
+        agentHash: runtime.agentHash,
+        source: 'OPENAI_API_COMPATIBILITY',
+        apiKey,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        isEnabled: !runtime.isPrivateModeEnabled,
+    });
+    const userMessageHash = await recordChatHistoryMessage({
+        message: {
+            role: 'USER',
+            content: lastMessage.content,
+        },
+        previousMessageHash: null,
+    });
+    const promptParameters = composePromptParametersWithMemoryContext({
+        baseParameters: {
+            ...parsedRequest.incomingParameters,
+            timezone,
+        },
+        currentUserIdentity: runtime.currentUserIdentity,
+        agentPermanentId: runtime.agentId,
+        agentName: runtime.resolvedAgentContext.resolvedAgentName,
+        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
+        projectRepositories: runtime.projectRepositories,
+        projectGithubToken: runtime.projectGithubToken,
+        emailSmtpCredential: runtime.emailSmtpCredential,
+        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
+        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
+        calendarConnections: runtime.calendarConnections,
+    });
+    const prompt: ChatPrompt = {
+        title,
+        content: lastMessage.content,
+        modelRequirements: {
+            modelVariant: 'CHAT',
+            responseFormat: parsedRequest.responseFormat,
+            toolChoice: parsedRequest.runtimeToolChoice,
+        },
+        parameters: promptParameters,
+        thread,
+        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
+    };
+
+    return {
+        prompt,
+        userMessageHash,
+        recordChatHistoryMessage,
+        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
+            isEnabled: !runtime.isPrivateModeEnabled,
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentId: runtime.agentId,
+            messages: parsedRequest.messages,
+            includeAssistantPlaceholder: true,
+            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
+        }),
+    };
+}
+
+/**
+ * Builds one OpenAI-compatible thread from the earlier request messages.
+ */
+function createHandleChatCompletionThread(
+    previousMessages: HandleChatCompletionParsedRequest['threadMessages'],
+): Array<ChatMessage> {
+    return previousMessages.map((message, index) => ({
+        id: `msg-${index}`,
+        sender: message.role === 'assistant' ? 'agent' : 'user',
+        content: message.content,
+        isComplete: true,
+        createdAt: $getCurrentDate(),
+    }));
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..85e096027760bc45fb730e74f8ae3bbb721b1029
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
@@ -0,0 +1,162 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import {
+    appendMessageSuffix,
+    createMessageSuffixAppendix,
+    emulateMessageSuffixStreaming,
+} from '@/src/utils/chat/messageSuffix';
+import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
+import { encodeChatStreamWhitespaceForTransport } from '../../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
+import { prepareToolCallsForStreaming } from '../toolCallStreaming';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionStreamResponse(options: {
+    request: Request;
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Response {
+    const { request, title, parsedRequest, runtime, promptContext } = options;
+    const encoder = new TextEncoder();
+    const responseModel = parsedRequest.model || 'promptbook-agent';
+    const readableStream = new ReadableStream({
+        async start(controller) {
+            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
+            const created = Math.floor(Date.now() / 1000);
+            const emitSsePayload = (payload: unknown) => {
+                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
+            };
+            const emitDeltaChunk = (deltaContent: string) => {
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {
+                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
+                        },
+                        finishReason: null,
+                    }),
+                );
+            };
+
+            emitSsePayload(
+                createHandleChatCompletionChunk({
+                    runId,
+                    created,
+                    model: responseModel,
+                    delta: {
+                        role: 'assistant',
+                        content: '',
+                    },
+                    finishReason: null,
+                }),
+            );
+
+            let hasMeaningfulDelta = false;
+
+            try {
+                const handleStreamChunk = createChatStreamHandler({
+                    onDelta: (deltaContent) => {
+                        if (deltaContent.trim().length > 0) {
+                            hasMeaningfulDelta = true;
+                        }
+                        emitDeltaChunk(deltaContent);
+                    },
+                    onToolCalls: (toolCalls) => {
+                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
+                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
+                    },
+                });
+                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
+                    signal: request.signal,
+                });
+                const normalizedResponse = ensureNonEmptyChatContent({
+                    content: result.content,
+                    context: title,
+                });
+
+                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
+                    emitDeltaChunk(normalizedResponse.content);
+                }
+
+                const messageSuffixAppendix = createMessageSuffixAppendix(
+                    normalizedResponse.content,
+                    runtime.messageSuffix,
+                );
+                if (messageSuffixAppendix) {
+                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
+                        emitDeltaChunk(delta);
+                    });
+                }
+
+                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+                await finalizeHandleChatCompletionResult({
+                    responseContentWithSuffix,
+                    usage: result.usage,
+                    toolCalls: result.toolCalls,
+                    parsedRequest,
+                    runtime,
+                    promptContext,
+                    shouldPersistLearnedAgentSource: true,
+                });
+
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {},
+                        finishReason: 'stop',
+                    }),
+                );
+                controller.enqueue(encoder.encode('[DONE]'));
+            } catch (error) {
+                console.error('Error during streaming:', error);
+                controller.error(error);
+            }
+            controller.close();
+        },
+    });
+
+    return new Response(readableStream, {
+        headers: {
+            'Content-Type': 'text/event-stream',
+            'Cache-Control': 'no-cache',
+            Connection: 'keep-alive',
+        },
+    });
+}
+
+/**
+ * Creates one OpenAI-compatible streaming chunk payload.
+ */
+function createHandleChatCompletionChunk(options: {
+    runId: string;
+    created: number;
+    model: HandleChatCompletionParsedRequest['model'];
+    delta: Record<string, unknown>;
+    finishReason: 'stop' | null;
+}) {
+    return {
+        id: options.runId,
+        object: 'chat.completion.chunk',
+        created: options.created,
+        model: options.model,
+        choices: [
+            {
+                index: 0,
+                delta: options.delta,
+                finish_reason: options.finishReason,
+            },
+        ],
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
new file mode 100644
index 0000000000000000000000000000000000000000..dd6a7d87ea491df245dbf93d8bcb277a9a10b034
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
@@ -0,0 +1,78 @@
+import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
+import { resolveAppendOnlySelfLearningAgentSource } from '@/src/utils/resolveAppendOnlySelfLearningAgentSource';
+import type { ToolCall, Usage } from '@promptbook-local/types';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Runs the shared side effects after one successful model response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function finalizeHandleChatCompletionResult(options: {
+    responseContentWithSuffix: string;
+    usage: Usage;
+    toolCalls?: ReadonlyArray<ToolCall>;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
+
+    await promptContext.recordChatHistoryMessage({
+        message: {
+            role: 'MODEL',
+            content: responseContentWithSuffix,
+        },
+        previousMessageHash: promptContext.userMessageHash,
+        usage,
+    });
+
+    if (toolCalls && toolCalls.length > 0) {
+        await logCalendarToolCallsActivity({
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentPermanentId: runtime.agentId,
+            toolCalls,
+        });
+    }
+
+    await persistOpenAiCompatibilityFrozenChat({
+        isEnabled: !runtime.isPrivateModeEnabled,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        agentId: runtime.agentId,
+        chatId: promptContext.persistedFrozenChatId,
+        messages: parsedRequest.messages,
+        assistantContent: responseContentWithSuffix,
+        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
+    });
+    await persistHandleChatCompletionLearnedAgentSource({
+        runtime,
+        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
+    });
+}
+
+/**
+ * Applies append-only self-learning updates when the current branch should persist them.
+ */
+async function persistHandleChatCompletionLearnedAgentSource(options: {
+    runtime: HandleChatCompletionRuntime;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { runtime, shouldPersistLearnedAgentSource } = options;
+    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
+        return;
+    }
+
+    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
+        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
+        resolvedAgentSourceBeforeLearning: runtime.agentSource,
+        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
+    });
+
+    if (learnedAgentSource !== null) {
+        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
+    }
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
new file mode 100644
index 0000000000000000000000000000000000000000..58e1c26d7f428c1bdb39b36411b3f75256e3de51
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
@@ -0,0 +1,176 @@
+import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
+import type { LlmToolDefinition, TODO_any } from '@promptbook-local/types';
+import { NextRequest, NextResponse } from 'next/server';
+import type OpenAI from 'openai';
+import { HTTP_STATUS_CODES } from '../../constants';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Definition of open AI chat tool.
+ */
+type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
+
+/**
+ * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionParsedRequest = {
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    threadMessages: ReadonlyArray<TODO_any>;
+    stream: TODO_any;
+    model: TODO_any;
+    responseFormat: TODO_any;
+    runtimeTools?: Array<LlmToolDefinition>;
+    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+    incomingParameters: Record<string, unknown>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Parses and validates the OpenAI-compatible request payload.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function parseHandleChatCompletionRequest(options: {
+    body: TODO_any;
+    agentNameFromParams?: string;
+    request: NextRequest;
+}): HandleChatCompletionParsedRequest | NextResponse {
+    const { body, agentNameFromParams, request } = options;
+    const {
+        messages,
+        stream,
+        model,
+        response_format: responseFormat,
+        tools: rawTools,
+        tool_choice: toolChoice,
+        parameters: rawParameters = {},
+    } = body;
+    const agentName = (agentNameFromParams || model) as string | undefined;
+    if (!agentName) {
+        return createHandleChatCompletionErrorResponse(
+            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    if (!messages || !Array.isArray(messages) || messages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array is required and cannot be empty.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
+    if (threadMessages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array must contain at least one non-system message.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    return {
+        agentName,
+        messages,
+        threadMessages,
+        stream,
+        model,
+        responseFormat,
+        runtimeTools: convertOpenAiTools(rawTools),
+        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
+        incomingParameters:
+            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
+                ? (rawParameters as Record<string, unknown>)
+                : {},
+        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
+    };
+}
+
+/**
+ * Handles convert open Ai tools.
+ */
+function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
+    if (!Array.isArray(rawTools)) {
+        return undefined;
+    }
+
+    const converted = rawTools
+        .map((tool) => convertOpenAiTool(tool))
+        .filter((tool): tool is LlmToolDefinition => tool !== null);
+
+    return converted.length > 0 ? converted : undefined;
+}
+
+/**
+ * Handles convert open Ai tool.
+ */
+function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
+    const tool = rawTool as OpenAIChatToolDefinition;
+    if (!tool || tool.type !== 'function') {
+        return null;
+    }
+
+    const functionDefinition = tool.function;
+    if (!functionDefinition) {
+        return null;
+    }
+
+    const { name, description, parameters } = functionDefinition;
+    if (typeof name !== 'string' || name.trim().length === 0) {
+        return null;
+    }
+
+    const parameterSchema = parameters ?? {};
+    if (parameterSchema.type !== 'object') {
+        return null;
+    }
+
+    const properties = parameterSchema.properties ?? {};
+    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
+
+    for (const [key, value] of Object.entries(properties)) {
+        if (!value || typeof value !== 'object') {
+            continue;
+        }
+
+        normalizedProperties[key] = {
+            type: typeof value.type === 'string' ? value.type : 'string',
+            description: typeof value.description === 'string' ? value.description : undefined,
+        };
+    }
+
+    const required =
+        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
+            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
+            : undefined;
+
+    return {
+        name,
+        description: typeof description === 'string' ? description : '',
+        parameters: {
+            type: 'object',
+            properties: normalizedProperties,
+            required,
+            additionalProperties:
+                typeof parameterSchema.additionalProperties === 'boolean'
+                    ? parameterSchema.additionalProperties
+                    : undefined,
+        },
+    };
+}
+
+/**
+ * Parses open Ai tool choice.
+ */
+function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
+    if (value === undefined || value === null) {
+        return undefined;
+    }
+
+    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
new file mode 100644
index 0000000000000000000000000000000000000000..60e5f1b008bcf5a6a1836c06c4c3d9ebcb909a9f
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
@@ -0,0 +1,163 @@
+import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
+import type { ChatMessage } from '@promptbook-local/types';
+
+/**
+ * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
+ */
+type OpenAiCompatibilityMessageLike = {
+    role?: unknown;
+    content?: unknown;
+};
+
+/**
+ * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function persistOpenAiCompatibilityFrozenChat(options: {
+    isEnabled: boolean;
+    userId: number | null | undefined;
+    agentId: string;
+    messages: ReadonlyArray<unknown>;
+    chatId?: string;
+    assistantContent?: string;
+    includeAssistantPlaceholder?: boolean;
+    failureMessage: string;
+}): Promise<string | undefined> {
+    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
+        options;
+    if (!isEnabled || !userId) {
+        return undefined;
+    }
+
+    const persistedFrozenChat = await persistFrozenUserChat({
+        userId,
+        agentPermanentId: agentId,
+        source: USER_CHAT_SOURCES.OPENAI_API,
+        chatId,
+        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
+            includeAssistantPlaceholder,
+        }),
+    }).catch((error) => {
+        console.error(failureMessage, error);
+        return null;
+    });
+
+    return persistedFrozenChat?.id;
+}
+
+/**
+ * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
+ */
+function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
+    if (typeof value === 'string') {
+        return value;
+    }
+
+    if (Array.isArray(value)) {
+        return value
+            .map((part) => {
+                if (typeof part === 'string') {
+                    return part;
+                }
+
+                if (!part || typeof part !== 'object') {
+                    return '';
+                }
+
+                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
+                if (typeof partLike.text === 'string') {
+                    return partLike.text;
+                }
+
+                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
+                    return '[Image input]';
+                }
+
+                if (partLike.image_url) {
+                    return '[Image input]';
+                }
+
+                return '';
+            })
+            .filter(Boolean)
+            .join('\n');
+    }
+
+    if (value === null || value === undefined) {
+        return '';
+    }
+
+    try {
+        return JSON.stringify(value, null, 2);
+    } catch {
+        return String(value);
+    }
+}
+
+/**
+ * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ */
+function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
+    if (typeof role !== 'string') {
+        return 'USER';
+    }
+
+    switch (role) {
+        case 'assistant':
+            return 'AGENT';
+        case 'system':
+            return 'SYSTEM';
+        case 'tool':
+            return 'TOOL';
+        case 'developer':
+            return 'SYSTEM';
+        default:
+            return 'USER';
+    }
+}
+
+/**
+ * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
+ */
+function createFrozenOpenAiCompatibilityMessages(
+    rawMessages: ReadonlyArray<unknown>,
+    assistantContent?: string,
+    options: {
+        includeAssistantPlaceholder?: boolean;
+    } = {},
+): Array<ChatMessage> {
+    const startedAt = Date.now();
+    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
+        const message = rawMessage as OpenAiCompatibilityMessageLike;
+
+        return {
+            id: `openai-frozen-${index}`,
+            sender: mapOpenAiCompatibilityRoleToSender(message.role),
+            content: normalizeOpenAiCompatibilityMessageContent(message.content),
+            isComplete: true,
+            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage;
+    });
+
+    if (assistantContent !== undefined) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: assistantContent,
+            isComplete: true,
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    } else if (options.includeAssistantPlaceholder) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: '',
+            isComplete: false,
+            lifecycleState: 'running',
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    }
+
+    return messages;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fd44b7ea5a87168725a9e99784038137c96537ab
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
@@ -0,0 +1,305 @@
+import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
+import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
+import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
+import type { string_book, TODO_any } from '@promptbook-local/types';
+import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
+import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
+import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
+import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
+import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
+import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
+import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
+import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
+import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
+import {
+    createBookScopedAgentReferenceResolver,
+    parseBookScopedAgentIdentifier,
+} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
+import { resolveMessageSuffixFromAgentSource } from '@/src/utils/chat/messageSuffix';
+import { NextRequest, NextResponse } from 'next/server';
+import { isAgentDeleted } from '../../app/agents/[agentName]/_utils';
+import {
+    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    resolveAgentCollectionTablePrefix,
+    waitForRunningAgentPreparation,
+} from '../agentPreparation';
+import { resolveCachedServerAgentContext, resolveCachedServerAgentModelRequirements } from '../cachedServerAgentRuntime';
+import { AgentKitCacheManager } from '../cache/AgentKitCacheManager';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Runtime state resolved before the chat model is executed.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionRuntime = {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    agent: Agent;
+    agentSource: string_book;
+    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
+    messageSuffix: string | null;
+    agentId: string;
+    agentHash: string;
+    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Resolves agent/runtime dependencies needed before prompt execution.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function resolveHandleChatCompletionRuntime(options: {
+    request: NextRequest;
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    isPrivateModeEnabled: boolean;
+}): Promise<HandleChatCompletionRuntime | NextResponse> {
+    const { request, agentName, messages, isPrivateModeEnabled } = options;
+    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
+    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
+
+    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
+        return createHandleChatCompletionErrorResponse(
+            'This agent has been deleted. You can restore it from the Recycle Bin.',
+            'agent_deleted',
+            410,
+        );
+    }
+
+    const [collection, baseAgentReferenceResolver] = await Promise.all([
+        $provideAgentCollectionForServer(),
+        $provideAgentReferenceResolver(),
+    ]);
+    const localServerUrl = new URL(request.url).origin;
+    const resolvedAgentContext = await resolveCachedServerAgentContext({
+        collection,
+        agentIdentifier: agentName,
+        localServerUrl,
+        fallbackResolver: baseAgentReferenceResolver,
+    }).catch(() => null);
+
+    if (!resolvedAgentContext?.resolvedAgentSource) {
+        return createHandleChatCompletionErrorResponse(`Agent '${agentName}' not found.`, 'invalid_request_error', 404);
+    }
+
+    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
+    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
+    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
+    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
+    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
+    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
+    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
+    agentSource = agentSourceWithContext;
+
+    const preparedAgentModelRequirements = !hasDynamicContext
+        ? await resolveCachedServerAgentModelRequirements({
+              resolvedAgentContext,
+              localServerUrl,
+              fallbackResolver: baseAgentReferenceResolver,
+          })
+        : null;
+    const agentId = resolvedAgentContext.parentAgentPermanentId;
+    const agentHash = computeAgentHash(agentSource);
+    await waitForHandleChatCompletionAgentPreparation({
+        collection,
+        agentId,
+        agentHash,
+    });
+
+    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
+    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
+        await resolveHandleChatCompletionCredentials({
+            currentUserIdentity,
+            agentId,
+            calendarConnections,
+            useEmailConfiguration,
+        });
+    const agent = await createHandleChatCompletionAgent({
+        agentName,
+        agentSource,
+        agentId,
+        resolvedAgentContext,
+        localServerUrl,
+        baseAgentReferenceResolver,
+        hasDynamicContext,
+        preparedAgentModelRequirements,
+    });
+
+    return {
+        collection,
+        resolvedAgentContext,
+        agent,
+        agentSource,
+        unresolvedAgentSource,
+        messageSuffix,
+        agentId,
+        agentHash,
+        projectRepositories,
+        calendarConnections,
+        useEmailConfiguration,
+        currentUserIdentity,
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+        isPrivateModeEnabled,
+    };
+}
+
+/**
+ * Appends system messages to the agent source as dynamic `CONTEXT`.
+ */
+function appendSystemMessagesToAgentSource(
+    agentSource: string_book,
+    messages: ReadonlyArray<TODO_any>,
+): {
+    agentSourceWithContext: string_book;
+    hasDynamicContext: boolean;
+} {
+    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
+    if (systemMessages.length === 0) {
+        return {
+            agentSourceWithContext: agentSource,
+            hasDynamicContext: false,
+        };
+    }
+
+    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
+    return {
+        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
+        hasDynamicContext: true,
+    };
+}
+
+/**
+ * Waits for any running preparation job of the current agent before executing the chat call.
+ */
+async function waitForHandleChatCompletionAgentPreparation(options: {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    agentId: string;
+    agentHash: string;
+}): Promise<void> {
+    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
+    const preparationWaitResult = await waitForRunningAgentPreparation({
+        tablePrefix,
+        agentPermanentId: options.agentId,
+        fingerprint: options.agentHash,
+        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    });
+
+    if (preparationWaitResult !== 'not_running') {
+        console.info('[pre-index]', 'openai_chat_wait_result', {
+            tablePrefix,
+            agentPermanentId: options.agentId,
+            agentHash: options.agentHash,
+            preparationWaitResult,
+        });
+    }
+}
+
+/**
+ * Resolves request-scoped project, calendar, and email credentials for the current agent.
+ */
+async function resolveHandleChatCompletionCredentials(options: {
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    agentId: string;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+}): Promise<{
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+}> {
+    const userId = options.currentUserIdentity?.userId;
+    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
+        resolveUseProjectGithubToken({
+            userId,
+            agentPermanentId: options.agentId,
+        }),
+        options.calendarConnections.length > 0
+            ? resolveUseCalendarGoogleToken({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+        options.useEmailConfiguration.isEnabled
+            ? resolveUseEmailSmtpCredential({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+    ]);
+
+    return {
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+    };
+}
+
+/**
+ * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
+ */
+async function createHandleChatCompletionAgent(options: {
+    agentName: string;
+    agentSource: string_book;
+    agentId: string;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    localServerUrl: string;
+    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
+    hasDynamicContext: boolean;
+    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
+}): Promise<Agent> {
+    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
+    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
+    const runtimeAgentReferenceResolver = options.hasDynamicContext
+        ? createBookScopedAgentReferenceResolver({
+              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
+              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
+              localServerUrl: options.localServerUrl,
+              fallbackResolver: options.baseAgentReferenceResolver,
+          })
+        : undefined;
+    const agentModelRequirements =
+        options.preparedAgentModelRequirements?.modelRequirements ||
+        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
+            agentReferenceResolver: runtimeAgentReferenceResolver,
+            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
+        }));
+
+    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
+        options.agentSource,
+        options.resolvedAgentContext.resolvedAgentName,
+        await baseOpenAiToolsPromise,
+        {
+            includeDynamicContext: true,
+            agentId: options.agentId,
+            modelRequirements: agentModelRequirements,
+        },
+    );
+
+    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
+        agentName: options.agentName,
+        assistantCacheKey: agentKitResult.assistantCacheKey,
+        vectorStoreHash: agentKitResult.vectorStoreHash,
+        vectorStoreId: agentKitResult.vectorStoreId,
+    });
+
+    return new Agent({
+        agentSource: options.agentSource,
+        executionTools: {
+            llm: agentKitResult.tools,
+        },
+        precomputedModelRequirements: agentModelRequirements,
+        assistantPreparationMode: 'external',
+        isVerbose: true,
+        teacherAgent: null,
+    });
+}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 474fd839d14b2bdab324e5f59fce0b15ddf95afd..bc9cfde73332af5d107ad35780a31963bc4e68e9
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,8 @@
+-   Refactored Agents Server `handleChatCompletion.ts` into a thinner chat-completion facade plus focused private helper modules without changing external behavior:
+
+    -   Extracted OpenAI-compatible request parsing, runtime/agent resolution, prompt-context creation, frozen-chat persistence, shared post-response side effects, and the streaming vs non-streaming response branches into dedicated private files behind the existing `handleChatCompletion` entrypoint.
+    -   Kept the existing API-key validation, deleted-agent handling, dynamic system-context injection, AgentKit cache usage, private-mode behavior, frozen audit snapshots, self-learning persistence, streaming chunk format, and OpenAI-compatible JSON response shape intact while reducing responsibility density in the main file.
+
 -   Refactored Agents Server `errorMessages.ts` into smaller focused helpers without changing external behavior:
 
     -   Split the branching-heavy `categorizeError` flow into dedicated API-type, native-error-message, and status-code classification helpers while preserving the existing precedence.

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Select-String -Path changelog/_current-preversion.md -Pattern "Refactored Agents Server `handleChatCompletion.ts`" | ForEach-Object { $_.LineNumber }' in C:/Users/me/work/ai/promptbook exited 1 in 485ms:

file update:
diff --git a/apps/agents-server/src/utils/handleChatCompletion.ts b/apps/agents-server/src/utils/handleChatCompletion.ts
index 9c96d36fad9cf24bf26d89f57372332add4e4595..cff56fafd6cfa90bb4094f9f202c4f01f6f74af1
--- a/apps/agents-server/src/utils/handleChatCompletion.ts
+++ b/apps/agents-server/src/utils/handleChatCompletion.ts
@@ -1,392 +1,16 @@
-import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
-import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
-import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
-import {
-    createBookScopedAgentReferenceResolver,
-    parseBookScopedAgentIdentifier,
-} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
-import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
-import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
-import {
-    appendMessageSuffix,
-    createMessageSuffixAppendix,
-    emulateMessageSuffixStreaming,
-    resolveMessageSuffixFromAgentSource,
-} from '@/src/utils/chat/messageSuffix';
-import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
-import { prepareToolCallsForStreaming } from './toolCallStreaming';
-import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
-import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
-import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
-import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
-import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
-import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
-import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
-import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
-import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
-import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
-import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
-import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
-import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
-import type {
-    ChatMessage,
-    ChatPrompt,
-    LlmToolDefinition,
-    string_book,
-    ToolCall,
-    TODO_any,
-    UncertainNumber,
-    Usage,
-    UsageCounts,
-} from '@promptbook-local/types';
-import { $getCurrentDate } from '@promptbook-local/utils';
 import { NextRequest, NextResponse } from 'next/server';
-import type OpenAI from 'openai';
-import { computeUsageCounts } from '../../../../src/execution/utils/computeUsageCounts';
-import { encodeChatStreamWhitespaceForTransport } from '../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
-import { isAgentDeleted } from '../app/agents/[agentName]/_utils';
 import { HTTP_STATUS_CODES } from '../constants';
-import { AgentKitCacheManager } from './cache/AgentKitCacheManager';
 import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
+import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
+import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
+import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
+import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
+import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
 import { validateApiKey } from './validateApiKey';
-import {
-    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    resolveAgentCollectionTablePrefix,
-    waitForRunningAgentPreparation,
-} from './agentPreparation';
-import {
-    resolveCachedServerAgentContext,
-    resolveCachedServerAgentModelRequirements,
-} from './cachedServerAgentRuntime';
-import { resolveAppendOnlySelfLearningAgentSource } from './resolveAppendOnlySelfLearningAgentSource';
 
 /**
- * Falls back to the estimated value when the original token count is unknown.
- *
- * @param tokenCount - Token count reported by the execution tools.
- * @param fallbackValue - Estimated token count based on text length.
- * @returns Token count to report in the OpenAI-compatible response.
- */
-function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
-    if (tokenCount.value === 0 && tokenCount.isUncertain) {
-        return {
-            value: fallbackValue,
-            isUncertain: true,
-        };
-    }
-
-    return tokenCount;
-}
-
-/**
- * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
- *
- * @param promptContent - Prompt content used for the request.
- * @param completionContent - Assistant response content.
- * @param usage - Native Promptbook usage details.
- * @returns Usage payload for compatibility responses.
- */
-function createCompatibilityUsage(
-    promptContent: string,
-    completionContent: string,
-    usage: Usage,
-): {
-    prompt_tokens: number;
-    completion_tokens: number;
-    total_tokens: number;
-    details: Usage;
-} {
-    const promptCounts = computeUsageCounts(promptContent);
-    const completionCounts = computeUsageCounts(completionContent);
-    const inputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
-        ...promptCounts,
-    };
-    const outputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
-        ...completionCounts,
-    };
-
-    const promptTokens = inputUsage.tokensCount.value;
-    const completionTokens = outputUsage.tokensCount.value;
-
-    return {
-        prompt_tokens: promptTokens,
-        completion_tokens: completionTokens,
-        total_tokens: promptTokens + completionTokens,
-        details: {
-            price: usage.price,
-            duration: usage.duration,
-            input: inputUsage,
-            output: outputUsage,
-        },
-    };
-}
-
-/**
- * Definition of open AI chat tool.
- */
-type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
-
-/**
- * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
- */
-type OpenAiCompatibilityMessageLike = {
-    role?: unknown;
-    content?: unknown;
-};
-
-/**
- * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
- */
-type HandleChatCompletionParsedRequest = {
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    threadMessages: ReadonlyArray<TODO_any>;
-    stream: TODO_any;
-    model: TODO_any;
-    responseFormat: TODO_any;
-    runtimeTools?: Array<LlmToolDefinition>;
-    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-    incomingParameters: Record<string, unknown>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Runtime state resolved before the chat model is executed.
- */
-type HandleChatCompletionRuntime = {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    agent: Agent;
-    agentSource: string_book;
-    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
-    messageSuffix: string | null;
-    agentId: string;
-    agentHash: string;
-    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Prompt, history recorder, and persistence context created for one chat call.
- */
-type HandleChatCompletionPromptContext = {
-    prompt: ChatPrompt;
-    userMessageHash: string;
-    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
-    persistedFrozenChatId?: string;
-};
-
-/**
- * Handles convert open Ai tools.
- */
-function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
-    if (!Array.isArray(rawTools)) {
-        return undefined;
-    }
-
-    const converted = rawTools
-        .map((tool) => convertOpenAiTool(tool))
-        .filter((tool): tool is LlmToolDefinition => tool !== null);
-
-    return converted.length > 0 ? converted : undefined;
-}
-
-/**
- * Handles convert open Ai tool.
- */
-function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
-    const tool = rawTool as OpenAIChatToolDefinition;
-    if (!tool || tool.type !== 'function') {
-        return null;
-    }
-
-    const functionDefinition = tool.function;
-    if (!functionDefinition) {
-        return null;
-    }
-
-    const { name, description, parameters } = functionDefinition;
-    if (typeof name !== 'string' || name.trim().length === 0) {
-        return null;
-    }
-
-    const parameterSchema = parameters ?? {};
-    if (parameterSchema.type !== 'object') {
-        return null;
-    }
-
-    const properties = parameterSchema.properties ?? {};
-    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
-
-    for (const [key, value] of Object.entries(properties)) {
-        if (!value || typeof value !== 'object') {
-            continue;
-        }
-
-        normalizedProperties[key] = {
-            type: typeof value.type === 'string' ? value.type : 'string',
-            description: typeof value.description === 'string' ? value.description : undefined,
-        };
-    }
-
-    const required =
-        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
-            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
-            : undefined;
-
-    return {
-        name,
-        description: typeof description === 'string' ? description : '',
-        parameters: {
-            type: 'object',
-            properties: normalizedProperties,
-            required,
-            additionalProperties:
-                typeof parameterSchema.additionalProperties === 'boolean'
-                    ? parameterSchema.additionalProperties
-                    : undefined,
-        },
-    };
-}
-
-/**
- * Parses open Ai tool choice.
- */
-function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
-    if (value === undefined || value === null) {
-        return undefined;
-    }
-
-    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-}
-
-/**
- * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
- */
-function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
-    if (typeof value === 'string') {
-        return value;
-    }
-
-    if (Array.isArray(value)) {
-        return value
-            .map((part) => {
-                if (typeof part === 'string') {
-                    return part;
-                }
-
-                if (!part || typeof part !== 'object') {
-                    return '';
-                }
-
-                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
-                if (typeof partLike.text === 'string') {
-                    return partLike.text;
-                }
-
-                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
-                    return '[Image input]';
-                }
-
-                if (partLike.image_url) {
-                    return '[Image input]';
-                }
-
-                return '';
-            })
-            .filter(Boolean)
-            .join('\n');
-    }
-
-    if (value === null || value === undefined) {
-        return '';
-    }
-
-    try {
-        return JSON.stringify(value, null, 2);
-    } catch {
-        return String(value);
-    }
-}
-
-/**
- * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ * Handles convert open Ai chat completion requests routed through the Agents Server.
  */
-function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
-    if (typeof role !== 'string') {
-        return 'USER';
-    }
-
-    switch (role) {
-        case 'assistant':
-            return 'AGENT';
-        case 'system':
-            return 'SYSTEM';
-        case 'tool':
-            return 'TOOL';
-        case 'developer':
-            return 'SYSTEM';
-        default:
-            return 'USER';
-    }
-}
-
-/**
- * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
- */
-function createFrozenOpenAiCompatibilityMessages(
-    rawMessages: ReadonlyArray<unknown>,
-    assistantContent?: string,
-    options: {
-        includeAssistantPlaceholder?: boolean;
-    } = {},
-): Array<ChatMessage> {
-    const startedAt = Date.now();
-    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
-        const message = rawMessage as OpenAiCompatibilityMessageLike;
-
-        return {
-            id: `openai-frozen-${index}`,
-            sender: mapOpenAiCompatibilityRoleToSender(message.role),
-            content: normalizeOpenAiCompatibilityMessageContent(message.content),
-            isComplete: true,
-            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage;
-    });
-
-    if (assistantContent !== undefined) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: assistantContent,
-            isComplete: true,
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    } else if (options.includeAssistantPlaceholder) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: '',
-            isComplete: false,
-            lifecycleState: 'running',
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    }
-
-    return messages;
-}
-
-/**
- * Handles chat completion.
- */
 export async function handleChatCompletion(
     request: NextRequest,
     params: { agentName?: string },
@@ -399,7 +23,6 @@
         return versionMismatchResponse;
     }
 
-    // Validate API key explicitly (in addition to middleware)
     const apiKeyValidation = await validateApiKey(request);
     if (!apiKeyValidation.isValid) {
         return NextResponse.json(
@@ -462,724 +85,5 @@
             { error: { message: (error as Error).message || 'Internal Server Error', type: 'server_error' } },
             { status: HTTP_STATUS_CODES.INTERNAL_SERVER_ERROR },
         );
-    }
-}
-
-/**
- * Parses and validates the OpenAI-compatible request payload.
- */
-function parseHandleChatCompletionRequest(options: {
-    body: TODO_any;
-    agentNameFromParams?: string;
-    request: NextRequest;
-}): HandleChatCompletionParsedRequest | NextResponse {
-    const { body, agentNameFromParams, request } = options;
-    const {
-        messages,
-        stream,
-        model,
-        response_format: responseFormat,
-        tools: rawTools,
-        tool_choice: toolChoice,
-        parameters: rawParameters = {},
-    } = body;
-    const agentName = (agentNameFromParams || model) as string | undefined;
-    if (!agentName) {
-        return createHandleChatCompletionErrorResponse(
-            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    if (!messages || !Array.isArray(messages) || messages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array is required and cannot be empty.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
-    if (threadMessages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array must contain at least one non-system message.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    return {
-        agentName,
-        messages,
-        threadMessages,
-        stream,
-        model,
-        responseFormat,
-        runtimeTools: convertOpenAiTools(rawTools),
-        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
-        incomingParameters:
-            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
-                ? (rawParameters as Record<string, unknown>)
-                : {},
-        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
-    };
-}
-
-/**
- * Resolves agent/runtime dependencies needed before prompt execution.
- */
-async function resolveHandleChatCompletionRuntime(options: {
-    request: NextRequest;
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    isPrivateModeEnabled: boolean;
-}): Promise<HandleChatCompletionRuntime | NextResponse> {
-    const { request, agentName, messages, isPrivateModeEnabled } = options;
-    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
-    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
-
-    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
-        return createHandleChatCompletionErrorResponse(
-            'This agent has been deleted. You can restore it from the Recycle Bin.',
-            'agent_deleted',
-            410,
-        );
-    }
-
-    const [collection, baseAgentReferenceResolver] = await Promise.all([
-        $provideAgentCollectionForServer(),
-        $provideAgentReferenceResolver(),
-    ]);
-    const localServerUrl = new URL(request.url).origin;
-    const resolvedAgentContext = await resolveCachedServerAgentContext({
-        collection,
-        agentIdentifier: agentName,
-        localServerUrl,
-        fallbackResolver: baseAgentReferenceResolver,
-    }).catch(() => null);
-
-    if (!resolvedAgentContext?.resolvedAgentSource) {
-        return createHandleChatCompletionErrorResponse(
-            `Agent '${agentName}' not found.`,
-            'invalid_request_error',
-            HTTP_STATUS_CODES.NOT_FOUND,
-        );
-    }
-
-    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
-    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
-    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
-    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
-    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
-    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
-    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
-    agentSource = agentSourceWithContext;
-
-    const preparedAgentModelRequirements = !hasDynamicContext
-        ? await resolveCachedServerAgentModelRequirements({
-              resolvedAgentContext,
-              localServerUrl,
-              fallbackResolver: baseAgentReferenceResolver,
-          })
-        : null;
-    const agentId = resolvedAgentContext.parentAgentPermanentId;
-    const agentHash = computeAgentHash(agentSource);
-    await waitForHandleChatCompletionAgentPreparation({
-        collection,
-        agentId,
-        agentHash,
-    });
-
-    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
-    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
-        await resolveHandleChatCompletionCredentials({
-            currentUserIdentity,
-            agentId,
-            calendarConnections,
-            useEmailConfiguration,
-        });
-    const agent = await createHandleChatCompletionAgent({
-        agentName,
-        agentSource,
-        agentId,
-        resolvedAgentContext,
-        localServerUrl,
-        baseAgentReferenceResolver,
-        hasDynamicContext,
-        preparedAgentModelRequirements,
-    });
-
-    return {
-        collection,
-        resolvedAgentContext,
-        agent,
-        agentSource,
-        unresolvedAgentSource,
-        messageSuffix,
-        agentId,
-        agentHash,
-        projectRepositories,
-        calendarConnections,
-        useEmailConfiguration,
-        currentUserIdentity,
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-        isPrivateModeEnabled,
-    };
-}
-
-/**
- * Creates prompt and persistence helpers for one chat completion request.
- */
-async function createHandleChatCompletionPromptContext(options: {
-    request: NextRequest;
-    title: string;
-    apiKey: string | null;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-}): Promise<HandleChatCompletionPromptContext> {
-    const { request, title, apiKey, parsedRequest, runtime } = options;
-    const timezone = request.headers.get('x-timezone') || 'UTC';
-    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
-    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
-    const recordChatHistoryMessage = await createChatHistoryRecorder({
-        request,
-        agentIdentifier: runtime.agentId,
-        agentHash: runtime.agentHash,
-        source: 'OPENAI_API_COMPATIBILITY',
-        apiKey,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        isEnabled: !runtime.isPrivateModeEnabled,
-    });
-    const userMessageHash = await recordChatHistoryMessage({
-        message: {
-            role: 'USER',
-            content: lastMessage.content,
-        },
-        previousMessageHash: null,
-    });
-    const promptParameters = composePromptParametersWithMemoryContext({
-        baseParameters: {
-            ...parsedRequest.incomingParameters,
-            timezone,
-        },
-        currentUserIdentity: runtime.currentUserIdentity,
-        agentPermanentId: runtime.agentId,
-        agentName: runtime.resolvedAgentContext.resolvedAgentName,
-        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
-        projectRepositories: runtime.projectRepositories,
-        projectGithubToken: runtime.projectGithubToken,
-        emailSmtpCredential: runtime.emailSmtpCredential,
-        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
-        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
-        calendarConnections: runtime.calendarConnections,
-    });
-    const prompt: ChatPrompt = {
-        title,
-        content: lastMessage.content,
-        modelRequirements: {
-            modelVariant: 'CHAT',
-            responseFormat: parsedRequest.responseFormat,
-            toolChoice: parsedRequest.runtimeToolChoice,
-            // We could pass 'model' from body if we wanted to enforce it, but Agent usually has its own config
-        },
-        parameters: promptParameters,
-        thread,
-        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
-    };
-
-    return {
-        prompt,
-        userMessageHash,
-        recordChatHistoryMessage,
-        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
-            isEnabled: !runtime.isPrivateModeEnabled,
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentId: runtime.agentId,
-            messages: parsedRequest.messages,
-            includeAssistantPlaceholder: true,
-            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
-        }),
-    };
-}
-
-/**
- * Handles the streaming OpenAI-compatible response branch.
- */
-function createHandleChatCompletionStreamResponse(options: {
-    request: NextRequest;
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Response {
-    const { request, title, parsedRequest, runtime, promptContext } = options;
-    const encoder = new TextEncoder();
-    const responseModel = parsedRequest.model || 'promptbook-agent';
-    const readableStream = new ReadableStream({
-        async start(controller) {
-            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
-            const created = Math.floor(Date.now() / 1000);
-            const emitSsePayload = (payload: unknown) => {
-                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
-            };
-            const emitDeltaChunk = (deltaContent: string) => {
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {
-                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
-                        },
-                        finishReason: null,
-                    }),
-                );
-            };
-
-            emitSsePayload(
-                createHandleChatCompletionChunk({
-                    runId,
-                    created,
-                    model: responseModel,
-                    delta: {
-                        role: 'assistant',
-                        content: '',
-                    },
-                    finishReason: null,
-                }),
-            );
-
-            let hasMeaningfulDelta = false;
-
-            try {
-                const handleStreamChunk = createChatStreamHandler({
-                    onDelta: (deltaContent) => {
-                        if (deltaContent.trim().length > 0) {
-                            hasMeaningfulDelta = true;
-                        }
-                        emitDeltaChunk(deltaContent);
-                    },
-                    onToolCalls: (toolCalls) => {
-                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
-                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
-                    },
-                });
-                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
-                    signal: request.signal,
-                });
-                const normalizedResponse = ensureNonEmptyChatContent({
-                    content: result.content,
-                    context: title,
-                });
-
-                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
-                    emitDeltaChunk(normalizedResponse.content);
-                }
-
-                const messageSuffixAppendix = createMessageSuffixAppendix(
-                    normalizedResponse.content,
-                    runtime.messageSuffix,
-                );
-                if (messageSuffixAppendix) {
-                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
-                        emitDeltaChunk(delta);
-                    });
-                }
-
-                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-                await finalizeHandleChatCompletionResult({
-                    responseContentWithSuffix,
-                    usage: result.usage,
-                    toolCalls: result.toolCalls,
-                    parsedRequest,
-                    runtime,
-                    promptContext,
-                    // Preserve historical behavior: streamed OpenAI-compatible calls still persist learning in private mode.
-                    shouldPersistLearnedAgentSource: true,
-                });
-
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {},
-                        finishReason: 'stop',
-                    }),
-                );
-                controller.enqueue(encoder.encode('[DONE]'));
-            } catch (error) {
-                console.error('Error during streaming:', error);
-                controller.error(error);
-            }
-            controller.close();
-        },
-    });
-
-    return new Response(readableStream, {
-        headers: {
-            'Content-Type': 'text/event-stream',
-            'Cache-Control': 'no-cache',
-            Connection: 'keep-alive',
-        },
-    });
-}
-
-/**
- * Handles the non-streaming OpenAI-compatible response branch.
- */
-async function createHandleChatCompletionJsonResponse(options: {
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Promise<NextResponse> {
-    const { title, parsedRequest, runtime, promptContext } = options;
-    const result = await runtime.agent.callChatModel(promptContext.prompt);
-    const normalizedResponse = ensureNonEmptyChatContent({
-        content: result.content,
-        context: title,
-    });
-    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-
-    await finalizeHandleChatCompletionResult({
-        responseContentWithSuffix,
-        usage: result.usage,
-        toolCalls: result.toolCalls,
-        parsedRequest,
-        runtime,
-        promptContext,
-        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
-    });
-
-    return NextResponse.json({
-        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
-        object: 'chat.completion',
-        created: Math.floor(Date.now() / 1000),
-        model: parsedRequest.model || 'promptbook-agent',
-        choices: [
-            {
-                index: 0,
-                message: {
-                    role: 'assistant',
-                    content: responseContentWithSuffix,
-                },
-                finish_reason: 'stop',
-            },
-        ],
-        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
-    });
-}
-
-/**
- * Runs the shared side effects after one successful model response.
- */
-async function finalizeHandleChatCompletionResult(options: {
-    responseContentWithSuffix: string;
-    usage: Usage;
-    toolCalls?: ReadonlyArray<ToolCall>;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
-
-    await promptContext.recordChatHistoryMessage({
-        message: {
-            role: 'MODEL',
-            content: responseContentWithSuffix,
-        },
-        previousMessageHash: promptContext.userMessageHash,
-        usage,
-    });
-
-    if (toolCalls && toolCalls.length > 0) {
-        await logCalendarToolCallsActivity({
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentPermanentId: runtime.agentId,
-            toolCalls,
-        });
-    }
-
-    await persistOpenAiCompatibilityFrozenChat({
-        isEnabled: !runtime.isPrivateModeEnabled,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        agentId: runtime.agentId,
-        chatId: promptContext.persistedFrozenChatId,
-        messages: parsedRequest.messages,
-        assistantContent: responseContentWithSuffix,
-        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
-    });
-    await persistHandleChatCompletionLearnedAgentSource({
-        runtime,
-        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
-    });
-}
-
-/**
- * Applies append-only self-learning updates when the current branch should persist them.
- */
-async function persistHandleChatCompletionLearnedAgentSource(options: {
-    runtime: HandleChatCompletionRuntime;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { runtime, shouldPersistLearnedAgentSource } = options;
-    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
-        return;
-    }
-
-    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
-        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
-        resolvedAgentSourceBeforeLearning: runtime.agentSource,
-        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
-    });
-
-    if (learnedAgentSource !== null) {
-        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
     }
 }
-
-/**
- * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
- */
-async function persistOpenAiCompatibilityFrozenChat(options: {
-    isEnabled: boolean;
-    userId: number | null | undefined;
-    agentId: string;
-    messages: ReadonlyArray<unknown>;
-    chatId?: string;
-    assistantContent?: string;
-    includeAssistantPlaceholder?: boolean;
-    failureMessage: string;
-}): Promise<string | undefined> {
-    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
-        options;
-    if (!isEnabled || !userId) {
-        return undefined;
-    }
-
-    const persistedFrozenChat = await persistFrozenUserChat({
-        userId,
-        agentPermanentId: agentId,
-        source: USER_CHAT_SOURCES.OPENAI_API,
-        chatId,
-        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
-            includeAssistantPlaceholder,
-        }),
-    }).catch((error) => {
-        console.error(failureMessage, error);
-        return null;
-    });
-
-    return persistedFrozenChat?.id;
-}
-
-/**
- * Builds one OpenAI-compatible thread from the earlier request messages.
- */
-function createHandleChatCompletionThread(previousMessages: ReadonlyArray<TODO_any>): ChatMessage[] {
-    return previousMessages.map((message: TODO_any, index: number) => ({
-        id: `msg-${index}`,
-        sender: message.role === 'assistant' ? 'agent' : 'user',
-        content: message.content,
-        isComplete: true,
-        createdAt: $getCurrentDate(),
-    }));
-}
-
-/**
- * Appends system messages to the agent source as dynamic `CONTEXT`.
- */
-function appendSystemMessagesToAgentSource(
-    agentSource: string_book,
-    messages: ReadonlyArray<TODO_any>,
-): {
-    agentSourceWithContext: string_book;
-    hasDynamicContext: boolean;
-} {
-    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
-    if (systemMessages.length === 0) {
-        return {
-            agentSourceWithContext: agentSource,
-            hasDynamicContext: false,
-        };
-    }
-
-    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
-    return {
-        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
-        hasDynamicContext: true,
-    };
-}
-
-/**
- * Waits for any running preparation job of the current agent before executing the chat call.
- */
-async function waitForHandleChatCompletionAgentPreparation(options: {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    agentId: string;
-    agentHash: string;
-}): Promise<void> {
-    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
-    const preparationWaitResult = await waitForRunningAgentPreparation({
-        tablePrefix,
-        agentPermanentId: options.agentId,
-        fingerprint: options.agentHash,
-        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    });
-
-    if (preparationWaitResult !== 'not_running') {
-        console.info('[pre-index]', 'openai_chat_wait_result', {
-            tablePrefix,
-            agentPermanentId: options.agentId,
-            agentHash: options.agentHash,
-            preparationWaitResult,
-        });
-    }
-}
-
-/**
- * Resolves request-scoped project, calendar, and email credentials for the current agent.
- */
-async function resolveHandleChatCompletionCredentials(options: {
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    agentId: string;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-}): Promise<{
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-}> {
-    const userId = options.currentUserIdentity?.userId;
-    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
-        resolveUseProjectGithubToken({
-            userId,
-            agentPermanentId: options.agentId,
-        }),
-        options.calendarConnections.length > 0
-            ? resolveUseCalendarGoogleToken({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-        options.useEmailConfiguration.isEnabled
-            ? resolveUseEmailSmtpCredential({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-    ]);
-
-    return {
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-    };
-}
-
-/**
- * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
- */
-async function createHandleChatCompletionAgent(options: {
-    agentName: string;
-    agentSource: string_book;
-    agentId: string;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    localServerUrl: string;
-    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
-    hasDynamicContext: boolean;
-    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
-}): Promise<Agent> {
-    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
-    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
-    const runtimeAgentReferenceResolver = options.hasDynamicContext
-        ? createBookScopedAgentReferenceResolver({
-              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
-              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
-              localServerUrl: options.localServerUrl,
-              fallbackResolver: options.baseAgentReferenceResolver,
-          })
-        : undefined;
-    const agentModelRequirements =
-        options.preparedAgentModelRequirements?.modelRequirements ||
-        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
-            agentReferenceResolver: runtimeAgentReferenceResolver,
-            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
-        }));
-
-    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
-        options.agentSource,
-        options.resolvedAgentContext.resolvedAgentName,
-        await baseOpenAiToolsPromise,
-        {
-            includeDynamicContext: true,
-            agentId: options.agentId,
-            modelRequirements: agentModelRequirements,
-        },
-    );
-
-    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
-        agentName: options.agentName,
-        assistantCacheKey: agentKitResult.assistantCacheKey,
-        vectorStoreHash: agentKitResult.vectorStoreHash,
-        vectorStoreId: agentKitResult.vectorStoreId,
-    });
-
-    return new Agent({
-        agentSource: options.agentSource,
-        executionTools: {
-            llm: agentKitResult.tools,
-        },
-        precomputedModelRequirements: agentModelRequirements,
-        assistantPreparationMode: 'external',
-        isVerbose: true,
-        teacherAgent: null,
-    });
-}
-
-/**
- * Creates one OpenAI-compatible streaming chunk payload.
- */
-function createHandleChatCompletionChunk(options: {
-    runId: string;
-    created: number;
-    model: TODO_any;
-    delta: Record<string, unknown>;
-    finishReason: 'stop' | null;
-}) {
-    return {
-        id: options.runId,
-        object: 'chat.completion.chunk',
-        created: options.created,
-        model: options.model,
-        choices: [
-            {
-                index: 0,
-                delta: options.delta,
-                finish_reason: options.finishReason,
-            },
-        ],
-    };
-}
-
-/**
- * Creates one consistent OpenAI-compatible error response.
- */
-function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
-    return NextResponse.json(
-        {
-            error: {
-                message,
-                type,
-            },
-        },
-        { status },
-    );
-}
-
-// TODO: [🈹] Maybe move chat thread handling here
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f49a4fa80fa21f140e25de59596e5546d62e3f7
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
@@ -0,0 +1,18 @@
+import { NextResponse } from 'next/server';
+
+/**
+ * Creates one consistent OpenAI-compatible error response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
+    return NextResponse.json(
+        {
+            error: {
+                message,
+                type,
+            },
+        },
+        { status },
+    );
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b1979f4621921bc664fed75a05851c6fcb8fba6d
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
@@ -0,0 +1,120 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import { appendMessageSuffix } from '@/src/utils/chat/messageSuffix';
+import type { UncertainNumber, Usage, UsageCounts } from '@promptbook-local/types';
+import { NextResponse } from 'next/server';
+import { computeUsageCounts } from '../../../../../src/execution/utils/computeUsageCounts';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the non-streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionJsonResponse(options: {
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Promise<NextResponse> {
+    const { title, parsedRequest, runtime, promptContext } = options;
+    const result = await runtime.agent.callChatModel(promptContext.prompt);
+    const normalizedResponse = ensureNonEmptyChatContent({
+        content: result.content,
+        context: title,
+    });
+    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+
+    await finalizeHandleChatCompletionResult({
+        responseContentWithSuffix,
+        usage: result.usage,
+        toolCalls: result.toolCalls,
+        parsedRequest,
+        runtime,
+        promptContext,
+        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
+    });
+
+    return NextResponse.json({
+        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
+        object: 'chat.completion',
+        created: Math.floor(Date.now() / 1000),
+        model: parsedRequest.model || 'promptbook-agent',
+        choices: [
+            {
+                index: 0,
+                message: {
+                    role: 'assistant',
+                    content: responseContentWithSuffix,
+                },
+                finish_reason: 'stop',
+            },
+        ],
+        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
+    });
+}
+
+/**
+ * Falls back to the estimated value when the original token count is unknown.
+ *
+ * @param tokenCount - Token count reported by the execution tools.
+ * @param fallbackValue - Estimated token count based on text length.
+ * @returns Token count to report in the OpenAI-compatible response.
+ */
+function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
+    if (tokenCount.value === 0 && tokenCount.isUncertain) {
+        return {
+            value: fallbackValue,
+            isUncertain: true,
+        };
+    }
+
+    return tokenCount;
+}
+
+/**
+ * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
+ *
+ * @param promptContent - Prompt content used for the request.
+ * @param completionContent - Assistant response content.
+ * @param usage - Native Promptbook usage details.
+ * @returns Usage payload for compatibility responses.
+ */
+function createCompatibilityUsage(
+    promptContent: string,
+    completionContent: string,
+    usage: Usage,
+): {
+    prompt_tokens: number;
+    completion_tokens: number;
+    total_tokens: number;
+    details: Usage;
+} {
+    const promptCounts = computeUsageCounts(promptContent);
+    const completionCounts = computeUsageCounts(completionContent);
+    const inputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
+        ...promptCounts,
+    };
+    const outputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
+        ...completionCounts,
+    };
+
+    const promptTokens = inputUsage.tokensCount.value;
+    const completionTokens = outputUsage.tokensCount.value;
+
+    return {
+        prompt_tokens: promptTokens,
+        completion_tokens: completionTokens,
+        total_tokens: promptTokens + completionTokens,
+        details: {
+            price: usage.price,
+            duration: usage.duration,
+            input: inputUsage,
+            output: outputUsage,
+        },
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..7aac8141a8b1237ce36430b4b4598a618f0383ac
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
@@ -0,0 +1,111 @@
+import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
+import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
+import type { ChatMessage, ChatPrompt } from '@promptbook-local/types';
+import { $getCurrentDate } from '@promptbook-local/utils';
+import type { NextRequest } from 'next/server';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Prompt, history recorder, and persistence context created for one chat call.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionPromptContext = {
+    prompt: ChatPrompt;
+    userMessageHash: string;
+    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
+    persistedFrozenChatId?: string;
+};
+
+/**
+ * Creates prompt and persistence helpers for one chat completion request.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionPromptContext(options: {
+    request: NextRequest;
+    title: string;
+    apiKey: string | null;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+}): Promise<HandleChatCompletionPromptContext> {
+    const { request, title, apiKey, parsedRequest, runtime } = options;
+    const timezone = request.headers.get('x-timezone') || 'UTC';
+    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
+    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
+    const recordChatHistoryMessage = await createChatHistoryRecorder({
+        request,
+        agentIdentifier: runtime.agentId,
+        agentHash: runtime.agentHash,
+        source: 'OPENAI_API_COMPATIBILITY',
+        apiKey,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        isEnabled: !runtime.isPrivateModeEnabled,
+    });
+    const userMessageHash = await recordChatHistoryMessage({
+        message: {
+            role: 'USER',
+            content: lastMessage.content,
+        },
+        previousMessageHash: null,
+    });
+    const promptParameters = composePromptParametersWithMemoryContext({
+        baseParameters: {
+            ...parsedRequest.incomingParameters,
+            timezone,
+        },
+        currentUserIdentity: runtime.currentUserIdentity,
+        agentPermanentId: runtime.agentId,
+        agentName: runtime.resolvedAgentContext.resolvedAgentName,
+        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
+        projectRepositories: runtime.projectRepositories,
+        projectGithubToken: runtime.projectGithubToken,
+        emailSmtpCredential: runtime.emailSmtpCredential,
+        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
+        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
+        calendarConnections: runtime.calendarConnections,
+    });
+    const prompt: ChatPrompt = {
+        title,
+        content: lastMessage.content,
+        modelRequirements: {
+            modelVariant: 'CHAT',
+            responseFormat: parsedRequest.responseFormat,
+            toolChoice: parsedRequest.runtimeToolChoice,
+        },
+        parameters: promptParameters,
+        thread,
+        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
+    };
+
+    return {
+        prompt,
+        userMessageHash,
+        recordChatHistoryMessage,
+        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
+            isEnabled: !runtime.isPrivateModeEnabled,
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentId: runtime.agentId,
+            messages: parsedRequest.messages,
+            includeAssistantPlaceholder: true,
+            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
+        }),
+    };
+}
+
+/**
+ * Builds one OpenAI-compatible thread from the earlier request messages.
+ */
+function createHandleChatCompletionThread(
+    previousMessages: HandleChatCompletionParsedRequest['threadMessages'],
+): Array<ChatMessage> {
+    return previousMessages.map((message, index) => ({
+        id: `msg-${index}`,
+        sender: message.role === 'assistant' ? 'agent' : 'user',
+        content: message.content,
+        isComplete: true,
+        createdAt: $getCurrentDate(),
+    }));
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..85e096027760bc45fb730e74f8ae3bbb721b1029
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
@@ -0,0 +1,162 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import {
+    appendMessageSuffix,
+    createMessageSuffixAppendix,
+    emulateMessageSuffixStreaming,
+} from '@/src/utils/chat/messageSuffix';
+import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
+import { encodeChatStreamWhitespaceForTransport } from '../../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
+import { prepareToolCallsForStreaming } from '../toolCallStreaming';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionStreamResponse(options: {
+    request: Request;
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Response {
+    const { request, title, parsedRequest, runtime, promptContext } = options;
+    const encoder = new TextEncoder();
+    const responseModel = parsedRequest.model || 'promptbook-agent';
+    const readableStream = new ReadableStream({
+        async start(controller) {
+            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
+            const created = Math.floor(Date.now() / 1000);
+            const emitSsePayload = (payload: unknown) => {
+                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
+            };
+            const emitDeltaChunk = (deltaContent: string) => {
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {
+                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
+                        },
+                        finishReason: null,
+                    }),
+                );
+            };
+
+            emitSsePayload(
+                createHandleChatCompletionChunk({
+                    runId,
+                    created,
+                    model: responseModel,
+                    delta: {
+                        role: 'assistant',
+                        content: '',
+                    },
+                    finishReason: null,
+                }),
+            );
+
+            let hasMeaningfulDelta = false;
+
+            try {
+                const handleStreamChunk = createChatStreamHandler({
+                    onDelta: (deltaContent) => {
+                        if (deltaContent.trim().length > 0) {
+                            hasMeaningfulDelta = true;
+                        }
+                        emitDeltaChunk(deltaContent);
+                    },
+                    onToolCalls: (toolCalls) => {
+                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
+                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
+                    },
+                });
+                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
+                    signal: request.signal,
+                });
+                const normalizedResponse = ensureNonEmptyChatContent({
+                    content: result.content,
+                    context: title,
+                });
+
+                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
+                    emitDeltaChunk(normalizedResponse.content);
+                }
+
+                const messageSuffixAppendix = createMessageSuffixAppendix(
+                    normalizedResponse.content,
+                    runtime.messageSuffix,
+                );
+                if (messageSuffixAppendix) {
+                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
+                        emitDeltaChunk(delta);
+                    });
+                }
+
+                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+                await finalizeHandleChatCompletionResult({
+                    responseContentWithSuffix,
+                    usage: result.usage,
+                    toolCalls: result.toolCalls,
+                    parsedRequest,
+                    runtime,
+                    promptContext,
+                    shouldPersistLearnedAgentSource: true,
+                });
+
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {},
+                        finishReason: 'stop',
+                    }),
+                );
+                controller.enqueue(encoder.encode('[DONE]'));
+            } catch (error) {
+                console.error('Error during streaming:', error);
+                controller.error(error);
+            }
+            controller.close();
+        },
+    });
+
+    return new Response(readableStream, {
+        headers: {
+            'Content-Type': 'text/event-stream',
+            'Cache-Control': 'no-cache',
+            Connection: 'keep-alive',
+        },
+    });
+}
+
+/**
+ * Creates one OpenAI-compatible streaming chunk payload.
+ */
+function createHandleChatCompletionChunk(options: {
+    runId: string;
+    created: number;
+    model: HandleChatCompletionParsedRequest['model'];
+    delta: Record<string, unknown>;
+    finishReason: 'stop' | null;
+}) {
+    return {
+        id: options.runId,
+        object: 'chat.completion.chunk',
+        created: options.created,
+        model: options.model,
+        choices: [
+            {
+                index: 0,
+                delta: options.delta,
+                finish_reason: options.finishReason,
+            },
+        ],
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
new file mode 100644
index 0000000000000000000000000000000000000000..dd6a7d87ea491df245dbf93d8bcb277a9a10b034
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
@@ -0,0 +1,78 @@
+import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
+import { resolveAppendOnlySelfLearningAgentSource } from '@/src/utils/resolveAppendOnlySelfLearningAgentSource';
+import type { ToolCall, Usage } from '@promptbook-local/types';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Runs the shared side effects after one successful model response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function finalizeHandleChatCompletionResult(options: {
+    responseContentWithSuffix: string;
+    usage: Usage;
+    toolCalls?: ReadonlyArray<ToolCall>;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
+
+    await promptContext.recordChatHistoryMessage({
+        message: {
+            role: 'MODEL',
+            content: responseContentWithSuffix,
+        },
+        previousMessageHash: promptContext.userMessageHash,
+        usage,
+    });
+
+    if (toolCalls && toolCalls.length > 0) {
+        await logCalendarToolCallsActivity({
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentPermanentId: runtime.agentId,
+            toolCalls,
+        });
+    }
+
+    await persistOpenAiCompatibilityFrozenChat({
+        isEnabled: !runtime.isPrivateModeEnabled,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        agentId: runtime.agentId,
+        chatId: promptContext.persistedFrozenChatId,
+        messages: parsedRequest.messages,
+        assistantContent: responseContentWithSuffix,
+        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
+    });
+    await persistHandleChatCompletionLearnedAgentSource({
+        runtime,
+        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
+    });
+}
+
+/**
+ * Applies append-only self-learning updates when the current branch should persist them.
+ */
+async function persistHandleChatCompletionLearnedAgentSource(options: {
+    runtime: HandleChatCompletionRuntime;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { runtime, shouldPersistLearnedAgentSource } = options;
+    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
+        return;
+    }
+
+    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
+        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
+        resolvedAgentSourceBeforeLearning: runtime.agentSource,
+        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
+    });
+
+    if (learnedAgentSource !== null) {
+        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
+    }
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
new file mode 100644
index 0000000000000000000000000000000000000000..58e1c26d7f428c1bdb39b36411b3f75256e3de51
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
@@ -0,0 +1,176 @@
+import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
+import type { LlmToolDefinition, TODO_any } from '@promptbook-local/types';
+import { NextRequest, NextResponse } from 'next/server';
+import type OpenAI from 'openai';
+import { HTTP_STATUS_CODES } from '../../constants';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Definition of open AI chat tool.
+ */
+type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
+
+/**
+ * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionParsedRequest = {
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    threadMessages: ReadonlyArray<TODO_any>;
+    stream: TODO_any;
+    model: TODO_any;
+    responseFormat: TODO_any;
+    runtimeTools?: Array<LlmToolDefinition>;
+    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+    incomingParameters: Record<string, unknown>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Parses and validates the OpenAI-compatible request payload.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function parseHandleChatCompletionRequest(options: {
+    body: TODO_any;
+    agentNameFromParams?: string;
+    request: NextRequest;
+}): HandleChatCompletionParsedRequest | NextResponse {
+    const { body, agentNameFromParams, request } = options;
+    const {
+        messages,
+        stream,
+        model,
+        response_format: responseFormat,
+        tools: rawTools,
+        tool_choice: toolChoice,
+        parameters: rawParameters = {},
+    } = body;
+    const agentName = (agentNameFromParams || model) as string | undefined;
+    if (!agentName) {
+        return createHandleChatCompletionErrorResponse(
+            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    if (!messages || !Array.isArray(messages) || messages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array is required and cannot be empty.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
+    if (threadMessages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array must contain at least one non-system message.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    return {
+        agentName,
+        messages,
+        threadMessages,
+        stream,
+        model,
+        responseFormat,
+        runtimeTools: convertOpenAiTools(rawTools),
+        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
+        incomingParameters:
+            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
+                ? (rawParameters as Record<string, unknown>)
+                : {},
+        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
+    };
+}
+
+/**
+ * Handles convert open Ai tools.
+ */
+function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
+    if (!Array.isArray(rawTools)) {
+        return undefined;
+    }
+
+    const converted = rawTools
+        .map((tool) => convertOpenAiTool(tool))
+        .filter((tool): tool is LlmToolDefinition => tool !== null);
+
+    return converted.length > 0 ? converted : undefined;
+}
+
+/**
+ * Handles convert open Ai tool.
+ */
+function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
+    const tool = rawTool as OpenAIChatToolDefinition;
+    if (!tool || tool.type !== 'function') {
+        return null;
+    }
+
+    const functionDefinition = tool.function;
+    if (!functionDefinition) {
+        return null;
+    }
+
+    const { name, description, parameters } = functionDefinition;
+    if (typeof name !== 'string' || name.trim().length === 0) {
+        return null;
+    }
+
+    const parameterSchema = parameters ?? {};
+    if (parameterSchema.type !== 'object') {
+        return null;
+    }
+
+    const properties = parameterSchema.properties ?? {};
+    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
+
+    for (const [key, value] of Object.entries(properties)) {
+        if (!value || typeof value !== 'object') {
+            continue;
+        }
+
+        normalizedProperties[key] = {
+            type: typeof value.type === 'string' ? value.type : 'string',
+            description: typeof value.description === 'string' ? value.description : undefined,
+        };
+    }
+
+    const required =
+        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
+            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
+            : undefined;
+
+    return {
+        name,
+        description: typeof description === 'string' ? description : '',
+        parameters: {
+            type: 'object',
+            properties: normalizedProperties,
+            required,
+            additionalProperties:
+                typeof parameterSchema.additionalProperties === 'boolean'
+                    ? parameterSchema.additionalProperties
+                    : undefined,
+        },
+    };
+}
+
+/**
+ * Parses open Ai tool choice.
+ */
+function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
+    if (value === undefined || value === null) {
+        return undefined;
+    }
+
+    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
new file mode 100644
index 0000000000000000000000000000000000000000..60e5f1b008bcf5a6a1836c06c4c3d9ebcb909a9f
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
@@ -0,0 +1,163 @@
+import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
+import type { ChatMessage } from '@promptbook-local/types';
+
+/**
+ * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
+ */
+type OpenAiCompatibilityMessageLike = {
+    role?: unknown;
+    content?: unknown;
+};
+
+/**
+ * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function persistOpenAiCompatibilityFrozenChat(options: {
+    isEnabled: boolean;
+    userId: number | null | undefined;
+    agentId: string;
+    messages: ReadonlyArray<unknown>;
+    chatId?: string;
+    assistantContent?: string;
+    includeAssistantPlaceholder?: boolean;
+    failureMessage: string;
+}): Promise<string | undefined> {
+    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
+        options;
+    if (!isEnabled || !userId) {
+        return undefined;
+    }
+
+    const persistedFrozenChat = await persistFrozenUserChat({
+        userId,
+        agentPermanentId: agentId,
+        source: USER_CHAT_SOURCES.OPENAI_API,
+        chatId,
+        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
+            includeAssistantPlaceholder,
+        }),
+    }).catch((error) => {
+        console.error(failureMessage, error);
+        return null;
+    });
+
+    return persistedFrozenChat?.id;
+}
+
+/**
+ * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
+ */
+function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
+    if (typeof value === 'string') {
+        return value;
+    }
+
+    if (Array.isArray(value)) {
+        return value
+            .map((part) => {
+                if (typeof part === 'string') {
+                    return part;
+                }
+
+                if (!part || typeof part !== 'object') {
+                    return '';
+                }
+
+                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
+                if (typeof partLike.text === 'string') {
+                    return partLike.text;
+                }
+
+                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
+                    return '[Image input]';
+                }
+
+                if (partLike.image_url) {
+                    return '[Image input]';
+                }
+
+                return '';
+            })
+            .filter(Boolean)
+            .join('\n');
+    }
+
+    if (value === null || value === undefined) {
+        return '';
+    }
+
+    try {
+        return JSON.stringify(value, null, 2);
+    } catch {
+        return String(value);
+    }
+}
+
+/**
+ * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ */
+function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
+    if (typeof role !== 'string') {
+        return 'USER';
+    }
+
+    switch (role) {
+        case 'assistant':
+            return 'AGENT';
+        case 'system':
+            return 'SYSTEM';
+        case 'tool':
+            return 'TOOL';
+        case 'developer':
+            return 'SYSTEM';
+        default:
+            return 'USER';
+    }
+}
+
+/**
+ * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
+ */
+function createFrozenOpenAiCompatibilityMessages(
+    rawMessages: ReadonlyArray<unknown>,
+    assistantContent?: string,
+    options: {
+        includeAssistantPlaceholder?: boolean;
+    } = {},
+): Array<ChatMessage> {
+    const startedAt = Date.now();
+    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
+        const message = rawMessage as OpenAiCompatibilityMessageLike;
+
+        return {
+            id: `openai-frozen-${index}`,
+            sender: mapOpenAiCompatibilityRoleToSender(message.role),
+            content: normalizeOpenAiCompatibilityMessageContent(message.content),
+            isComplete: true,
+            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage;
+    });
+
+    if (assistantContent !== undefined) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: assistantContent,
+            isComplete: true,
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    } else if (options.includeAssistantPlaceholder) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: '',
+            isComplete: false,
+            lifecycleState: 'running',
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    }
+
+    return messages;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fd44b7ea5a87168725a9e99784038137c96537ab
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
@@ -0,0 +1,305 @@
+import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
+import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
+import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
+import type { string_book, TODO_any } from '@promptbook-local/types';
+import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
+import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
+import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
+import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
+import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
+import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
+import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
+import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
+import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
+import {
+    createBookScopedAgentReferenceResolver,
+    parseBookScopedAgentIdentifier,
+} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
+import { resolveMessageSuffixFromAgentSource } from '@/src/utils/chat/messageSuffix';
+import { NextRequest, NextResponse } from 'next/server';
+import { isAgentDeleted } from '../../app/agents/[agentName]/_utils';
+import {
+    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    resolveAgentCollectionTablePrefix,
+    waitForRunningAgentPreparation,
+} from '../agentPreparation';
+import { resolveCachedServerAgentContext, resolveCachedServerAgentModelRequirements } from '../cachedServerAgentRuntime';
+import { AgentKitCacheManager } from '../cache/AgentKitCacheManager';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Runtime state resolved before the chat model is executed.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionRuntime = {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    agent: Agent;
+    agentSource: string_book;
+    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
+    messageSuffix: string | null;
+    agentId: string;
+    agentHash: string;
+    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Resolves agent/runtime dependencies needed before prompt execution.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function resolveHandleChatCompletionRuntime(options: {
+    request: NextRequest;
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    isPrivateModeEnabled: boolean;
+}): Promise<HandleChatCompletionRuntime | NextResponse> {
+    const { request, agentName, messages, isPrivateModeEnabled } = options;
+    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
+    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
+
+    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
+        return createHandleChatCompletionErrorResponse(
+            'This agent has been deleted. You can restore it from the Recycle Bin.',
+            'agent_deleted',
+            410,
+        );
+    }
+
+    const [collection, baseAgentReferenceResolver] = await Promise.all([
+        $provideAgentCollectionForServer(),
+        $provideAgentReferenceResolver(),
+    ]);
+    const localServerUrl = new URL(request.url).origin;
+    const resolvedAgentContext = await resolveCachedServerAgentContext({
+        collection,
+        agentIdentifier: agentName,
+        localServerUrl,
+        fallbackResolver: baseAgentReferenceResolver,
+    }).catch(() => null);
+
+    if (!resolvedAgentContext?.resolvedAgentSource) {
+        return createHandleChatCompletionErrorResponse(`Agent '${agentName}' not found.`, 'invalid_request_error', 404);
+    }
+
+    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
+    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
+    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
+    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
+    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
+    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
+    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
+    agentSource = agentSourceWithContext;
+
+    const preparedAgentModelRequirements = !hasDynamicContext
+        ? await resolveCachedServerAgentModelRequirements({
+              resolvedAgentContext,
+              localServerUrl,
+              fallbackResolver: baseAgentReferenceResolver,
+          })
+        : null;
+    const agentId = resolvedAgentContext.parentAgentPermanentId;
+    const agentHash = computeAgentHash(agentSource);
+    await waitForHandleChatCompletionAgentPreparation({
+        collection,
+        agentId,
+        agentHash,
+    });
+
+    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
+    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
+        await resolveHandleChatCompletionCredentials({
+            currentUserIdentity,
+            agentId,
+            calendarConnections,
+            useEmailConfiguration,
+        });
+    const agent = await createHandleChatCompletionAgent({
+        agentName,
+        agentSource,
+        agentId,
+        resolvedAgentContext,
+        localServerUrl,
+        baseAgentReferenceResolver,
+        hasDynamicContext,
+        preparedAgentModelRequirements,
+    });
+
+    return {
+        collection,
+        resolvedAgentContext,
+        agent,
+        agentSource,
+        unresolvedAgentSource,
+        messageSuffix,
+        agentId,
+        agentHash,
+        projectRepositories,
+        calendarConnections,
+        useEmailConfiguration,
+        currentUserIdentity,
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+        isPrivateModeEnabled,
+    };
+}
+
+/**
+ * Appends system messages to the agent source as dynamic `CONTEXT`.
+ */
+function appendSystemMessagesToAgentSource(
+    agentSource: string_book,
+    messages: ReadonlyArray<TODO_any>,
+): {
+    agentSourceWithContext: string_book;
+    hasDynamicContext: boolean;
+} {
+    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
+    if (systemMessages.length === 0) {
+        return {
+            agentSourceWithContext: agentSource,
+            hasDynamicContext: false,
+        };
+    }
+
+    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
+    return {
+        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
+        hasDynamicContext: true,
+    };
+}
+
+/**
+ * Waits for any running preparation job of the current agent before executing the chat call.
+ */
+async function waitForHandleChatCompletionAgentPreparation(options: {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    agentId: string;
+    agentHash: string;
+}): Promise<void> {
+    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
+    const preparationWaitResult = await waitForRunningAgentPreparation({
+        tablePrefix,
+        agentPermanentId: options.agentId,
+        fingerprint: options.agentHash,
+        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    });
+
+    if (preparationWaitResult !== 'not_running') {
+        console.info('[pre-index]', 'openai_chat_wait_result', {
+            tablePrefix,
+            agentPermanentId: options.agentId,
+            agentHash: options.agentHash,
+            preparationWaitResult,
+        });
+    }
+}
+
+/**
+ * Resolves request-scoped project, calendar, and email credentials for the current agent.
+ */
+async function resolveHandleChatCompletionCredentials(options: {
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    agentId: string;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+}): Promise<{
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+}> {
+    const userId = options.currentUserIdentity?.userId;
+    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
+        resolveUseProjectGithubToken({
+            userId,
+            agentPermanentId: options.agentId,
+        }),
+        options.calendarConnections.length > 0
+            ? resolveUseCalendarGoogleToken({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+        options.useEmailConfiguration.isEnabled
+            ? resolveUseEmailSmtpCredential({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+    ]);
+
+    return {
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+    };
+}
+
+/**
+ * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
+ */
+async function createHandleChatCompletionAgent(options: {
+    agentName: string;
+    agentSource: string_book;
+    agentId: string;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    localServerUrl: string;
+    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
+    hasDynamicContext: boolean;
+    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
+}): Promise<Agent> {
+    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
+    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
+    const runtimeAgentReferenceResolver = options.hasDynamicContext
+        ? createBookScopedAgentReferenceResolver({
+              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
+              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
+              localServerUrl: options.localServerUrl,
+              fallbackResolver: options.baseAgentReferenceResolver,
+          })
+        : undefined;
+    const agentModelRequirements =
+        options.preparedAgentModelRequirements?.modelRequirements ||
+        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
+            agentReferenceResolver: runtimeAgentReferenceResolver,
+            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
+        }));
+
+    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
+        options.agentSource,
+        options.resolvedAgentContext.resolvedAgentName,
+        await baseOpenAiToolsPromise,
+        {
+            includeDynamicContext: true,
+            agentId: options.agentId,
+            modelRequirements: agentModelRequirements,
+        },
+    );
+
+    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
+        agentName: options.agentName,
+        assistantCacheKey: agentKitResult.assistantCacheKey,
+        vectorStoreHash: agentKitResult.vectorStoreHash,
+        vectorStoreId: agentKitResult.vectorStoreId,
+    });
+
+    return new Agent({
+        agentSource: options.agentSource,
+        executionTools: {
+            llm: agentKitResult.tools,
+        },
+        precomputedModelRequirements: agentModelRequirements,
+        assistantPreparationMode: 'external',
+        isVerbose: true,
+        teacherAgent: null,
+    });
+}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 474fd839d14b2bdab324e5f59fce0b15ddf95afd..bc9cfde73332af5d107ad35780a31963bc4e68e9
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,8 @@
+-   Refactored Agents Server `handleChatCompletion.ts` into a thinner chat-completion facade plus focused private helper modules without changing external behavior:
+
+    -   Extracted OpenAI-compatible request parsing, runtime/agent resolution, prompt-context creation, frozen-chat persistence, shared post-response side effects, and the streaming vs non-streaming response branches into dedicated private files behind the existing `handleChatCompletion` entrypoint.
+    -   Kept the existing API-key validation, deleted-agent handling, dynamic system-context injection, AgentKit cache usage, private-mode behavior, frozen audit snapshots, self-learning persistence, streaming chunk format, and OpenAI-compatible JSON response shape intact while reducing responsibility density in the main file.
+
 -   Refactored Agents Server `errorMessages.ts` into smaller focused helpers without changing external behavior:
 
     -   Split the branching-heavy `categorizeError` flow into dedicated API-type, native-error-message, and status-code classification helpers while preserving the existing precedence.


thinking
**Searching PowerShell solution**

I’m encountering an issue with backticks in PowerShell. I think I should search for “Refactored Agents Server” because it might lead me to a helpful solution or the top result related to this problem. It seems like getting the right information could simplify things for me. I'll dig into that to see what I can find!
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Select-String -Path changelog/_current-preversion.md -Pattern "Refactored Agents Server" | Select-Object -First 1 | ForEach-Object { $_.LineNumber }' in C:/Users/me/work/ai/promptbook succeeded in 441ms:
1
file update:
diff --git a/apps/agents-server/src/utils/handleChatCompletion.ts b/apps/agents-server/src/utils/handleChatCompletion.ts
index 9c96d36fad9cf24bf26d89f57372332add4e4595..cff56fafd6cfa90bb4094f9f202c4f01f6f74af1
--- a/apps/agents-server/src/utils/handleChatCompletion.ts
+++ b/apps/agents-server/src/utils/handleChatCompletion.ts
@@ -1,392 +1,16 @@
-import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
-import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
-import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
-import {
-    createBookScopedAgentReferenceResolver,
-    parseBookScopedAgentIdentifier,
-} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
-import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
-import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
-import {
-    appendMessageSuffix,
-    createMessageSuffixAppendix,
-    emulateMessageSuffixStreaming,
-    resolveMessageSuffixFromAgentSource,
-} from '@/src/utils/chat/messageSuffix';
-import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
-import { prepareToolCallsForStreaming } from './toolCallStreaming';
-import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
-import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
-import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
-import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
-import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
-import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
-import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
-import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
-import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
-import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
-import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
-import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
-import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
-import type {
-    ChatMessage,
-    ChatPrompt,
-    LlmToolDefinition,
-    string_book,
-    ToolCall,
-    TODO_any,
-    UncertainNumber,
-    Usage,
-    UsageCounts,
-} from '@promptbook-local/types';
-import { $getCurrentDate } from '@promptbook-local/utils';
 import { NextRequest, NextResponse } from 'next/server';
-import type OpenAI from 'openai';
-import { computeUsageCounts } from '../../../../src/execution/utils/computeUsageCounts';
-import { encodeChatStreamWhitespaceForTransport } from '../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
-import { isAgentDeleted } from '../app/agents/[agentName]/_utils';
 import { HTTP_STATUS_CODES } from '../constants';
-import { AgentKitCacheManager } from './cache/AgentKitCacheManager';
 import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
+import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
+import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
+import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
+import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
+import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
 import { validateApiKey } from './validateApiKey';
-import {
-    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    resolveAgentCollectionTablePrefix,
-    waitForRunningAgentPreparation,
-} from './agentPreparation';
-import {
-    resolveCachedServerAgentContext,
-    resolveCachedServerAgentModelRequirements,
-} from './cachedServerAgentRuntime';
-import { resolveAppendOnlySelfLearningAgentSource } from './resolveAppendOnlySelfLearningAgentSource';
 
 /**
- * Falls back to the estimated value when the original token count is unknown.
- *
- * @param tokenCount - Token count reported by the execution tools.
- * @param fallbackValue - Estimated token count based on text length.
- * @returns Token count to report in the OpenAI-compatible response.
- */
-function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
-    if (tokenCount.value === 0 && tokenCount.isUncertain) {
-        return {
-            value: fallbackValue,
-            isUncertain: true,
-        };
-    }
-
-    return tokenCount;
-}
-
-/**
- * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
- *
- * @param promptContent - Prompt content used for the request.
- * @param completionContent - Assistant response content.
- * @param usage - Native Promptbook usage details.
- * @returns Usage payload for compatibility responses.
- */
-function createCompatibilityUsage(
-    promptContent: string,
-    completionContent: string,
-    usage: Usage,
-): {
-    prompt_tokens: number;
-    completion_tokens: number;
-    total_tokens: number;
-    details: Usage;
-} {
-    const promptCounts = computeUsageCounts(promptContent);
-    const completionCounts = computeUsageCounts(completionContent);
-    const inputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
-        ...promptCounts,
-    };
-    const outputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
-        ...completionCounts,
-    };
-
-    const promptTokens = inputUsage.tokensCount.value;
-    const completionTokens = outputUsage.tokensCount.value;
-
-    return {
-        prompt_tokens: promptTokens,
-        completion_tokens: completionTokens,
-        total_tokens: promptTokens + completionTokens,
-        details: {
-            price: usage.price,
-            duration: usage.duration,
-            input: inputUsage,
-            output: outputUsage,
-        },
-    };
-}
-
-/**
- * Definition of open AI chat tool.
- */
-type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
-
-/**
- * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
- */
-type OpenAiCompatibilityMessageLike = {
-    role?: unknown;
-    content?: unknown;
-};
-
-/**
- * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
- */
-type HandleChatCompletionParsedRequest = {
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    threadMessages: ReadonlyArray<TODO_any>;
-    stream: TODO_any;
-    model: TODO_any;
-    responseFormat: TODO_any;
-    runtimeTools?: Array<LlmToolDefinition>;
-    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-    incomingParameters: Record<string, unknown>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Runtime state resolved before the chat model is executed.
- */
-type HandleChatCompletionRuntime = {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    agent: Agent;
-    agentSource: string_book;
-    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
-    messageSuffix: string | null;
-    agentId: string;
-    agentHash: string;
-    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Prompt, history recorder, and persistence context created for one chat call.
- */
-type HandleChatCompletionPromptContext = {
-    prompt: ChatPrompt;
-    userMessageHash: string;
-    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
-    persistedFrozenChatId?: string;
-};
-
-/**
- * Handles convert open Ai tools.
- */
-function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
-    if (!Array.isArray(rawTools)) {
-        return undefined;
-    }
-
-    const converted = rawTools
-        .map((tool) => convertOpenAiTool(tool))
-        .filter((tool): tool is LlmToolDefinition => tool !== null);
-
-    return converted.length > 0 ? converted : undefined;
-}
-
-/**
- * Handles convert open Ai tool.
- */
-function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
-    const tool = rawTool as OpenAIChatToolDefinition;
-    if (!tool || tool.type !== 'function') {
-        return null;
-    }
-
-    const functionDefinition = tool.function;
-    if (!functionDefinition) {
-        return null;
-    }
-
-    const { name, description, parameters } = functionDefinition;
-    if (typeof name !== 'string' || name.trim().length === 0) {
-        return null;
-    }
-
-    const parameterSchema = parameters ?? {};
-    if (parameterSchema.type !== 'object') {
-        return null;
-    }
-
-    const properties = parameterSchema.properties ?? {};
-    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
-
-    for (const [key, value] of Object.entries(properties)) {
-        if (!value || typeof value !== 'object') {
-            continue;
-        }
-
-        normalizedProperties[key] = {
-            type: typeof value.type === 'string' ? value.type : 'string',
-            description: typeof value.description === 'string' ? value.description : undefined,
-        };
-    }
-
-    const required =
-        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
-            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
-            : undefined;
-
-    return {
-        name,
-        description: typeof description === 'string' ? description : '',
-        parameters: {
-            type: 'object',
-            properties: normalizedProperties,
-            required,
-            additionalProperties:
-                typeof parameterSchema.additionalProperties === 'boolean'
-                    ? parameterSchema.additionalProperties
-                    : undefined,
-        },
-    };
-}
-
-/**
- * Parses open Ai tool choice.
- */
-function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
-    if (value === undefined || value === null) {
-        return undefined;
-    }
-
-    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-}
-
-/**
- * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
- */
-function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
-    if (typeof value === 'string') {
-        return value;
-    }
-
-    if (Array.isArray(value)) {
-        return value
-            .map((part) => {
-                if (typeof part === 'string') {
-                    return part;
-                }
-
-                if (!part || typeof part !== 'object') {
-                    return '';
-                }
-
-                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
-                if (typeof partLike.text === 'string') {
-                    return partLike.text;
-                }
-
-                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
-                    return '[Image input]';
-                }
-
-                if (partLike.image_url) {
-                    return '[Image input]';
-                }
-
-                return '';
-            })
-            .filter(Boolean)
-            .join('\n');
-    }
-
-    if (value === null || value === undefined) {
-        return '';
-    }
-
-    try {
-        return JSON.stringify(value, null, 2);
-    } catch {
-        return String(value);
-    }
-}
-
-/**
- * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ * Handles convert open Ai chat completion requests routed through the Agents Server.
  */
-function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
-    if (typeof role !== 'string') {
-        return 'USER';
-    }
-
-    switch (role) {
-        case 'assistant':
-            return 'AGENT';
-        case 'system':
-            return 'SYSTEM';
-        case 'tool':
-            return 'TOOL';
-        case 'developer':
-            return 'SYSTEM';
-        default:
-            return 'USER';
-    }
-}
-
-/**
- * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
- */
-function createFrozenOpenAiCompatibilityMessages(
-    rawMessages: ReadonlyArray<unknown>,
-    assistantContent?: string,
-    options: {
-        includeAssistantPlaceholder?: boolean;
-    } = {},
-): Array<ChatMessage> {
-    const startedAt = Date.now();
-    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
-        const message = rawMessage as OpenAiCompatibilityMessageLike;
-
-        return {
-            id: `openai-frozen-${index}`,
-            sender: mapOpenAiCompatibilityRoleToSender(message.role),
-            content: normalizeOpenAiCompatibilityMessageContent(message.content),
-            isComplete: true,
-            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage;
-    });
-
-    if (assistantContent !== undefined) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: assistantContent,
-            isComplete: true,
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    } else if (options.includeAssistantPlaceholder) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: '',
-            isComplete: false,
-            lifecycleState: 'running',
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    }
-
-    return messages;
-}
-
-/**
- * Handles chat completion.
- */
 export async function handleChatCompletion(
     request: NextRequest,
     params: { agentName?: string },
@@ -399,7 +23,6 @@
         return versionMismatchResponse;
     }
 
-    // Validate API key explicitly (in addition to middleware)
     const apiKeyValidation = await validateApiKey(request);
     if (!apiKeyValidation.isValid) {
         return NextResponse.json(
@@ -462,724 +85,5 @@
             { error: { message: (error as Error).message || 'Internal Server Error', type: 'server_error' } },
             { status: HTTP_STATUS_CODES.INTERNAL_SERVER_ERROR },
         );
-    }
-}
-
-/**
- * Parses and validates the OpenAI-compatible request payload.
- */
-function parseHandleChatCompletionRequest(options: {
-    body: TODO_any;
-    agentNameFromParams?: string;
-    request: NextRequest;
-}): HandleChatCompletionParsedRequest | NextResponse {
-    const { body, agentNameFromParams, request } = options;
-    const {
-        messages,
-        stream,
-        model,
-        response_format: responseFormat,
-        tools: rawTools,
-        tool_choice: toolChoice,
-        parameters: rawParameters = {},
-    } = body;
-    const agentName = (agentNameFromParams || model) as string | undefined;
-    if (!agentName) {
-        return createHandleChatCompletionErrorResponse(
-            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    if (!messages || !Array.isArray(messages) || messages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array is required and cannot be empty.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
-    if (threadMessages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array must contain at least one non-system message.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    return {
-        agentName,
-        messages,
-        threadMessages,
-        stream,
-        model,
-        responseFormat,
-        runtimeTools: convertOpenAiTools(rawTools),
-        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
-        incomingParameters:
-            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
-                ? (rawParameters as Record<string, unknown>)
-                : {},
-        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
-    };
-}
-
-/**
- * Resolves agent/runtime dependencies needed before prompt execution.
- */
-async function resolveHandleChatCompletionRuntime(options: {
-    request: NextRequest;
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    isPrivateModeEnabled: boolean;
-}): Promise<HandleChatCompletionRuntime | NextResponse> {
-    const { request, agentName, messages, isPrivateModeEnabled } = options;
-    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
-    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
-
-    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
-        return createHandleChatCompletionErrorResponse(
-            'This agent has been deleted. You can restore it from the Recycle Bin.',
-            'agent_deleted',
-            410,
-        );
-    }
-
-    const [collection, baseAgentReferenceResolver] = await Promise.all([
-        $provideAgentCollectionForServer(),
-        $provideAgentReferenceResolver(),
-    ]);
-    const localServerUrl = new URL(request.url).origin;
-    const resolvedAgentContext = await resolveCachedServerAgentContext({
-        collection,
-        agentIdentifier: agentName,
-        localServerUrl,
-        fallbackResolver: baseAgentReferenceResolver,
-    }).catch(() => null);
-
-    if (!resolvedAgentContext?.resolvedAgentSource) {
-        return createHandleChatCompletionErrorResponse(
-            `Agent '${agentName}' not found.`,
-            'invalid_request_error',
-            HTTP_STATUS_CODES.NOT_FOUND,
-        );
-    }
-
-    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
-    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
-    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
-    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
-    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
-    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
-    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
-    agentSource = agentSourceWithContext;
-
-    const preparedAgentModelRequirements = !hasDynamicContext
-        ? await resolveCachedServerAgentModelRequirements({
-              resolvedAgentContext,
-              localServerUrl,
-              fallbackResolver: baseAgentReferenceResolver,
-          })
-        : null;
-    const agentId = resolvedAgentContext.parentAgentPermanentId;
-    const agentHash = computeAgentHash(agentSource);
-    await waitForHandleChatCompletionAgentPreparation({
-        collection,
-        agentId,
-        agentHash,
-    });
-
-    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
-    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
-        await resolveHandleChatCompletionCredentials({
-            currentUserIdentity,
-            agentId,
-            calendarConnections,
-            useEmailConfiguration,
-        });
-    const agent = await createHandleChatCompletionAgent({
-        agentName,
-        agentSource,
-        agentId,
-        resolvedAgentContext,
-        localServerUrl,
-        baseAgentReferenceResolver,
-        hasDynamicContext,
-        preparedAgentModelRequirements,
-    });
-
-    return {
-        collection,
-        resolvedAgentContext,
-        agent,
-        agentSource,
-        unresolvedAgentSource,
-        messageSuffix,
-        agentId,
-        agentHash,
-        projectRepositories,
-        calendarConnections,
-        useEmailConfiguration,
-        currentUserIdentity,
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-        isPrivateModeEnabled,
-    };
-}
-
-/**
- * Creates prompt and persistence helpers for one chat completion request.
- */
-async function createHandleChatCompletionPromptContext(options: {
-    request: NextRequest;
-    title: string;
-    apiKey: string | null;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-}): Promise<HandleChatCompletionPromptContext> {
-    const { request, title, apiKey, parsedRequest, runtime } = options;
-    const timezone = request.headers.get('x-timezone') || 'UTC';
-    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
-    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
-    const recordChatHistoryMessage = await createChatHistoryRecorder({
-        request,
-        agentIdentifier: runtime.agentId,
-        agentHash: runtime.agentHash,
-        source: 'OPENAI_API_COMPATIBILITY',
-        apiKey,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        isEnabled: !runtime.isPrivateModeEnabled,
-    });
-    const userMessageHash = await recordChatHistoryMessage({
-        message: {
-            role: 'USER',
-            content: lastMessage.content,
-        },
-        previousMessageHash: null,
-    });
-    const promptParameters = composePromptParametersWithMemoryContext({
-        baseParameters: {
-            ...parsedRequest.incomingParameters,
-            timezone,
-        },
-        currentUserIdentity: runtime.currentUserIdentity,
-        agentPermanentId: runtime.agentId,
-        agentName: runtime.resolvedAgentContext.resolvedAgentName,
-        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
-        projectRepositories: runtime.projectRepositories,
-        projectGithubToken: runtime.projectGithubToken,
-        emailSmtpCredential: runtime.emailSmtpCredential,
-        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
-        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
-        calendarConnections: runtime.calendarConnections,
-    });
-    const prompt: ChatPrompt = {
-        title,
-        content: lastMessage.content,
-        modelRequirements: {
-            modelVariant: 'CHAT',
-            responseFormat: parsedRequest.responseFormat,
-            toolChoice: parsedRequest.runtimeToolChoice,
-            // We could pass 'model' from body if we wanted to enforce it, but Agent usually has its own config
-        },
-        parameters: promptParameters,
-        thread,
-        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
-    };
-
-    return {
-        prompt,
-        userMessageHash,
-        recordChatHistoryMessage,
-        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
-            isEnabled: !runtime.isPrivateModeEnabled,
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentId: runtime.agentId,
-            messages: parsedRequest.messages,
-            includeAssistantPlaceholder: true,
-            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
-        }),
-    };
-}
-
-/**
- * Handles the streaming OpenAI-compatible response branch.
- */
-function createHandleChatCompletionStreamResponse(options: {
-    request: NextRequest;
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Response {
-    const { request, title, parsedRequest, runtime, promptContext } = options;
-    const encoder = new TextEncoder();
-    const responseModel = parsedRequest.model || 'promptbook-agent';
-    const readableStream = new ReadableStream({
-        async start(controller) {
-            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
-            const created = Math.floor(Date.now() / 1000);
-            const emitSsePayload = (payload: unknown) => {
-                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
-            };
-            const emitDeltaChunk = (deltaContent: string) => {
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {
-                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
-                        },
-                        finishReason: null,
-                    }),
-                );
-            };
-
-            emitSsePayload(
-                createHandleChatCompletionChunk({
-                    runId,
-                    created,
-                    model: responseModel,
-                    delta: {
-                        role: 'assistant',
-                        content: '',
-                    },
-                    finishReason: null,
-                }),
-            );
-
-            let hasMeaningfulDelta = false;
-
-            try {
-                const handleStreamChunk = createChatStreamHandler({
-                    onDelta: (deltaContent) => {
-                        if (deltaContent.trim().length > 0) {
-                            hasMeaningfulDelta = true;
-                        }
-                        emitDeltaChunk(deltaContent);
-                    },
-                    onToolCalls: (toolCalls) => {
-                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
-                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
-                    },
-                });
-                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
-                    signal: request.signal,
-                });
-                const normalizedResponse = ensureNonEmptyChatContent({
-                    content: result.content,
-                    context: title,
-                });
-
-                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
-                    emitDeltaChunk(normalizedResponse.content);
-                }
-
-                const messageSuffixAppendix = createMessageSuffixAppendix(
-                    normalizedResponse.content,
-                    runtime.messageSuffix,
-                );
-                if (messageSuffixAppendix) {
-                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
-                        emitDeltaChunk(delta);
-                    });
-                }
-
-                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-                await finalizeHandleChatCompletionResult({
-                    responseContentWithSuffix,
-                    usage: result.usage,
-                    toolCalls: result.toolCalls,
-                    parsedRequest,
-                    runtime,
-                    promptContext,
-                    // Preserve historical behavior: streamed OpenAI-compatible calls still persist learning in private mode.
-                    shouldPersistLearnedAgentSource: true,
-                });
-
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {},
-                        finishReason: 'stop',
-                    }),
-                );
-                controller.enqueue(encoder.encode('[DONE]'));
-            } catch (error) {
-                console.error('Error during streaming:', error);
-                controller.error(error);
-            }
-            controller.close();
-        },
-    });
-
-    return new Response(readableStream, {
-        headers: {
-            'Content-Type': 'text/event-stream',
-            'Cache-Control': 'no-cache',
-            Connection: 'keep-alive',
-        },
-    });
-}
-
-/**
- * Handles the non-streaming OpenAI-compatible response branch.
- */
-async function createHandleChatCompletionJsonResponse(options: {
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Promise<NextResponse> {
-    const { title, parsedRequest, runtime, promptContext } = options;
-    const result = await runtime.agent.callChatModel(promptContext.prompt);
-    const normalizedResponse = ensureNonEmptyChatContent({
-        content: result.content,
-        context: title,
-    });
-    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-
-    await finalizeHandleChatCompletionResult({
-        responseContentWithSuffix,
-        usage: result.usage,
-        toolCalls: result.toolCalls,
-        parsedRequest,
-        runtime,
-        promptContext,
-        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
-    });
-
-    return NextResponse.json({
-        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
-        object: 'chat.completion',
-        created: Math.floor(Date.now() / 1000),
-        model: parsedRequest.model || 'promptbook-agent',
-        choices: [
-            {
-                index: 0,
-                message: {
-                    role: 'assistant',
-                    content: responseContentWithSuffix,
-                },
-                finish_reason: 'stop',
-            },
-        ],
-        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
-    });
-}
-
-/**
- * Runs the shared side effects after one successful model response.
- */
-async function finalizeHandleChatCompletionResult(options: {
-    responseContentWithSuffix: string;
-    usage: Usage;
-    toolCalls?: ReadonlyArray<ToolCall>;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
-
-    await promptContext.recordChatHistoryMessage({
-        message: {
-            role: 'MODEL',
-            content: responseContentWithSuffix,
-        },
-        previousMessageHash: promptContext.userMessageHash,
-        usage,
-    });
-
-    if (toolCalls && toolCalls.length > 0) {
-        await logCalendarToolCallsActivity({
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentPermanentId: runtime.agentId,
-            toolCalls,
-        });
-    }
-
-    await persistOpenAiCompatibilityFrozenChat({
-        isEnabled: !runtime.isPrivateModeEnabled,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        agentId: runtime.agentId,
-        chatId: promptContext.persistedFrozenChatId,
-        messages: parsedRequest.messages,
-        assistantContent: responseContentWithSuffix,
-        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
-    });
-    await persistHandleChatCompletionLearnedAgentSource({
-        runtime,
-        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
-    });
-}
-
-/**
- * Applies append-only self-learning updates when the current branch should persist them.
- */
-async function persistHandleChatCompletionLearnedAgentSource(options: {
-    runtime: HandleChatCompletionRuntime;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { runtime, shouldPersistLearnedAgentSource } = options;
-    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
-        return;
-    }
-
-    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
-        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
-        resolvedAgentSourceBeforeLearning: runtime.agentSource,
-        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
-    });
-
-    if (learnedAgentSource !== null) {
-        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
     }
 }
-
-/**
- * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
- */
-async function persistOpenAiCompatibilityFrozenChat(options: {
-    isEnabled: boolean;
-    userId: number | null | undefined;
-    agentId: string;
-    messages: ReadonlyArray<unknown>;
-    chatId?: string;
-    assistantContent?: string;
-    includeAssistantPlaceholder?: boolean;
-    failureMessage: string;
-}): Promise<string | undefined> {
-    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
-        options;
-    if (!isEnabled || !userId) {
-        return undefined;
-    }
-
-    const persistedFrozenChat = await persistFrozenUserChat({
-        userId,
-        agentPermanentId: agentId,
-        source: USER_CHAT_SOURCES.OPENAI_API,
-        chatId,
-        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
-            includeAssistantPlaceholder,
-        }),
-    }).catch((error) => {
-        console.error(failureMessage, error);
-        return null;
-    });
-
-    return persistedFrozenChat?.id;
-}
-
-/**
- * Builds one OpenAI-compatible thread from the earlier request messages.
- */
-function createHandleChatCompletionThread(previousMessages: ReadonlyArray<TODO_any>): ChatMessage[] {
-    return previousMessages.map((message: TODO_any, index: number) => ({
-        id: `msg-${index}`,
-        sender: message.role === 'assistant' ? 'agent' : 'user',
-        content: message.content,
-        isComplete: true,
-        createdAt: $getCurrentDate(),
-    }));
-}
-
-/**
- * Appends system messages to the agent source as dynamic `CONTEXT`.
- */
-function appendSystemMessagesToAgentSource(
-    agentSource: string_book,
-    messages: ReadonlyArray<TODO_any>,
-): {
-    agentSourceWithContext: string_book;
-    hasDynamicContext: boolean;
-} {
-    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
-    if (systemMessages.length === 0) {
-        return {
-            agentSourceWithContext: agentSource,
-            hasDynamicContext: false,
-        };
-    }
-
-    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
-    return {
-        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
-        hasDynamicContext: true,
-    };
-}
-
-/**
- * Waits for any running preparation job of the current agent before executing the chat call.
- */
-async function waitForHandleChatCompletionAgentPreparation(options: {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    agentId: string;
-    agentHash: string;
-}): Promise<void> {
-    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
-    const preparationWaitResult = await waitForRunningAgentPreparation({
-        tablePrefix,
-        agentPermanentId: options.agentId,
-        fingerprint: options.agentHash,
-        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    });
-
-    if (preparationWaitResult !== 'not_running') {
-        console.info('[pre-index]', 'openai_chat_wait_result', {
-            tablePrefix,
-            agentPermanentId: options.agentId,
-            agentHash: options.agentHash,
-            preparationWaitResult,
-        });
-    }
-}
-
-/**
- * Resolves request-scoped project, calendar, and email credentials for the current agent.
- */
-async function resolveHandleChatCompletionCredentials(options: {
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    agentId: string;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-}): Promise<{
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-}> {
-    const userId = options.currentUserIdentity?.userId;
-    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
-        resolveUseProjectGithubToken({
-            userId,
-            agentPermanentId: options.agentId,
-        }),
-        options.calendarConnections.length > 0
-            ? resolveUseCalendarGoogleToken({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-        options.useEmailConfiguration.isEnabled
-            ? resolveUseEmailSmtpCredential({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-    ]);
-
-    return {
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-    };
-}
-
-/**
- * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
- */
-async function createHandleChatCompletionAgent(options: {
-    agentName: string;
-    agentSource: string_book;
-    agentId: string;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    localServerUrl: string;
-    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
-    hasDynamicContext: boolean;
-    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
-}): Promise<Agent> {
-    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
-    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
-    const runtimeAgentReferenceResolver = options.hasDynamicContext
-        ? createBookScopedAgentReferenceResolver({
-              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
-              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
-              localServerUrl: options.localServerUrl,
-              fallbackResolver: options.baseAgentReferenceResolver,
-          })
-        : undefined;
-    const agentModelRequirements =
-        options.preparedAgentModelRequirements?.modelRequirements ||
-        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
-            agentReferenceResolver: runtimeAgentReferenceResolver,
-            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
-        }));
-
-    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
-        options.agentSource,
-        options.resolvedAgentContext.resolvedAgentName,
-        await baseOpenAiToolsPromise,
-        {
-            includeDynamicContext: true,
-            agentId: options.agentId,
-            modelRequirements: agentModelRequirements,
-        },
-    );
-
-    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
-        agentName: options.agentName,
-        assistantCacheKey: agentKitResult.assistantCacheKey,
-        vectorStoreHash: agentKitResult.vectorStoreHash,
-        vectorStoreId: agentKitResult.vectorStoreId,
-    });
-
-    return new Agent({
-        agentSource: options.agentSource,
-        executionTools: {
-            llm: agentKitResult.tools,
-        },
-        precomputedModelRequirements: agentModelRequirements,
-        assistantPreparationMode: 'external',
-        isVerbose: true,
-        teacherAgent: null,
-    });
-}
-
-/**
- * Creates one OpenAI-compatible streaming chunk payload.
- */
-function createHandleChatCompletionChunk(options: {
-    runId: string;
-    created: number;
-    model: TODO_any;
-    delta: Record<string, unknown>;
-    finishReason: 'stop' | null;
-}) {
-    return {
-        id: options.runId,
-        object: 'chat.completion.chunk',
-        created: options.created,
-        model: options.model,
-        choices: [
-            {
-                index: 0,
-                delta: options.delta,
-                finish_reason: options.finishReason,
-            },
-        ],
-    };
-}
-
-/**
- * Creates one consistent OpenAI-compatible error response.
- */
-function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
-    return NextResponse.json(
-        {
-            error: {
-                message,
-                type,
-            },
-        },
-        { status },
-    );
-}
-
-// TODO: [🈹] Maybe move chat thread handling here
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f49a4fa80fa21f140e25de59596e5546d62e3f7
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
@@ -0,0 +1,18 @@
+import { NextResponse } from 'next/server';
+
+/**
+ * Creates one consistent OpenAI-compatible error response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
+    return NextResponse.json(
+        {
+            error: {
+                message,
+                type,
+            },
+        },
+        { status },
+    );
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b1979f4621921bc664fed75a05851c6fcb8fba6d
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
@@ -0,0 +1,120 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import { appendMessageSuffix } from '@/src/utils/chat/messageSuffix';
+import type { UncertainNumber, Usage, UsageCounts } from '@promptbook-local/types';
+import { NextResponse } from 'next/server';
+import { computeUsageCounts } from '../../../../../src/execution/utils/computeUsageCounts';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the non-streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionJsonResponse(options: {
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Promise<NextResponse> {
+    const { title, parsedRequest, runtime, promptContext } = options;
+    const result = await runtime.agent.callChatModel(promptContext.prompt);
+    const normalizedResponse = ensureNonEmptyChatContent({
+        content: result.content,
+        context: title,
+    });
+    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+
+    await finalizeHandleChatCompletionResult({
+        responseContentWithSuffix,
+        usage: result.usage,
+        toolCalls: result.toolCalls,
+        parsedRequest,
+        runtime,
+        promptContext,
+        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
+    });
+
+    return NextResponse.json({
+        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
+        object: 'chat.completion',
+        created: Math.floor(Date.now() / 1000),
+        model: parsedRequest.model || 'promptbook-agent',
+        choices: [
+            {
+                index: 0,
+                message: {
+                    role: 'assistant',
+                    content: responseContentWithSuffix,
+                },
+                finish_reason: 'stop',
+            },
+        ],
+        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
+    });
+}
+
+/**
+ * Falls back to the estimated value when the original token count is unknown.
+ *
+ * @param tokenCount - Token count reported by the execution tools.
+ * @param fallbackValue - Estimated token count based on text length.
+ * @returns Token count to report in the OpenAI-compatible response.
+ */
+function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
+    if (tokenCount.value === 0 && tokenCount.isUncertain) {
+        return {
+            value: fallbackValue,
+            isUncertain: true,
+        };
+    }
+
+    return tokenCount;
+}
+
+/**
+ * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
+ *
+ * @param promptContent - Prompt content used for the request.
+ * @param completionContent - Assistant response content.
+ * @param usage - Native Promptbook usage details.
+ * @returns Usage payload for compatibility responses.
+ */
+function createCompatibilityUsage(
+    promptContent: string,
+    completionContent: string,
+    usage: Usage,
+): {
+    prompt_tokens: number;
+    completion_tokens: number;
+    total_tokens: number;
+    details: Usage;
+} {
+    const promptCounts = computeUsageCounts(promptContent);
+    const completionCounts = computeUsageCounts(completionContent);
+    const inputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
+        ...promptCounts,
+    };
+    const outputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
+        ...completionCounts,
+    };
+
+    const promptTokens = inputUsage.tokensCount.value;
+    const completionTokens = outputUsage.tokensCount.value;
+
+    return {
+        prompt_tokens: promptTokens,
+        completion_tokens: completionTokens,
+        total_tokens: promptTokens + completionTokens,
+        details: {
+            price: usage.price,
+            duration: usage.duration,
+            input: inputUsage,
+            output: outputUsage,
+        },
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..7aac8141a8b1237ce36430b4b4598a618f0383ac
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
@@ -0,0 +1,111 @@
+import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
+import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
+import type { ChatMessage, ChatPrompt } from '@promptbook-local/types';
+import { $getCurrentDate } from '@promptbook-local/utils';
+import type { NextRequest } from 'next/server';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Prompt, history recorder, and persistence context created for one chat call.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionPromptContext = {
+    prompt: ChatPrompt;
+    userMessageHash: string;
+    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
+    persistedFrozenChatId?: string;
+};
+
+/**
+ * Creates prompt and persistence helpers for one chat completion request.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionPromptContext(options: {
+    request: NextRequest;
+    title: string;
+    apiKey: string | null;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+}): Promise<HandleChatCompletionPromptContext> {
+    const { request, title, apiKey, parsedRequest, runtime } = options;
+    const timezone = request.headers.get('x-timezone') || 'UTC';
+    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
+    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
+    const recordChatHistoryMessage = await createChatHistoryRecorder({
+        request,
+        agentIdentifier: runtime.agentId,
+        agentHash: runtime.agentHash,
+        source: 'OPENAI_API_COMPATIBILITY',
+        apiKey,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        isEnabled: !runtime.isPrivateModeEnabled,
+    });
+    const userMessageHash = await recordChatHistoryMessage({
+        message: {
+            role: 'USER',
+            content: lastMessage.content,
+        },
+        previousMessageHash: null,
+    });
+    const promptParameters = composePromptParametersWithMemoryContext({
+        baseParameters: {
+            ...parsedRequest.incomingParameters,
+            timezone,
+        },
+        currentUserIdentity: runtime.currentUserIdentity,
+        agentPermanentId: runtime.agentId,
+        agentName: runtime.resolvedAgentContext.resolvedAgentName,
+        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
+        projectRepositories: runtime.projectRepositories,
+        projectGithubToken: runtime.projectGithubToken,
+        emailSmtpCredential: runtime.emailSmtpCredential,
+        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
+        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
+        calendarConnections: runtime.calendarConnections,
+    });
+    const prompt: ChatPrompt = {
+        title,
+        content: lastMessage.content,
+        modelRequirements: {
+            modelVariant: 'CHAT',
+            responseFormat: parsedRequest.responseFormat,
+            toolChoice: parsedRequest.runtimeToolChoice,
+        },
+        parameters: promptParameters,
+        thread,
+        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
+    };
+
+    return {
+        prompt,
+        userMessageHash,
+        recordChatHistoryMessage,
+        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
+            isEnabled: !runtime.isPrivateModeEnabled,
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentId: runtime.agentId,
+            messages: parsedRequest.messages,
+            includeAssistantPlaceholder: true,
+            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
+        }),
+    };
+}
+
+/**
+ * Builds one OpenAI-compatible thread from the earlier request messages.
+ */
+function createHandleChatCompletionThread(
+    previousMessages: HandleChatCompletionParsedRequest['threadMessages'],
+): Array<ChatMessage> {
+    return previousMessages.map((message, index) => ({
+        id: `msg-${index}`,
+        sender: message.role === 'assistant' ? 'agent' : 'user',
+        content: message.content,
+        isComplete: true,
+        createdAt: $getCurrentDate(),
+    }));
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..85e096027760bc45fb730e74f8ae3bbb721b1029
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
@@ -0,0 +1,162 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import {
+    appendMessageSuffix,
+    createMessageSuffixAppendix,
+    emulateMessageSuffixStreaming,
+} from '@/src/utils/chat/messageSuffix';
+import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
+import { encodeChatStreamWhitespaceForTransport } from '../../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
+import { prepareToolCallsForStreaming } from '../toolCallStreaming';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionStreamResponse(options: {
+    request: Request;
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Response {
+    const { request, title, parsedRequest, runtime, promptContext } = options;
+    const encoder = new TextEncoder();
+    const responseModel = parsedRequest.model || 'promptbook-agent';
+    const readableStream = new ReadableStream({
+        async start(controller) {
+            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
+            const created = Math.floor(Date.now() / 1000);
+            const emitSsePayload = (payload: unknown) => {
+                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
+            };
+            const emitDeltaChunk = (deltaContent: string) => {
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {
+                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
+                        },
+                        finishReason: null,
+                    }),
+                );
+            };
+
+            emitSsePayload(
+                createHandleChatCompletionChunk({
+                    runId,
+                    created,
+                    model: responseModel,
+                    delta: {
+                        role: 'assistant',
+                        content: '',
+                    },
+                    finishReason: null,
+                }),
+            );
+
+            let hasMeaningfulDelta = false;
+
+            try {
+                const handleStreamChunk = createChatStreamHandler({
+                    onDelta: (deltaContent) => {
+                        if (deltaContent.trim().length > 0) {
+                            hasMeaningfulDelta = true;
+                        }
+                        emitDeltaChunk(deltaContent);
+                    },
+                    onToolCalls: (toolCalls) => {
+                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
+                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
+                    },
+                });
+                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
+                    signal: request.signal,
+                });
+                const normalizedResponse = ensureNonEmptyChatContent({
+                    content: result.content,
+                    context: title,
+                });
+
+                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
+                    emitDeltaChunk(normalizedResponse.content);
+                }
+
+                const messageSuffixAppendix = createMessageSuffixAppendix(
+                    normalizedResponse.content,
+                    runtime.messageSuffix,
+                );
+                if (messageSuffixAppendix) {
+                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
+                        emitDeltaChunk(delta);
+                    });
+                }
+
+                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+                await finalizeHandleChatCompletionResult({
+                    responseContentWithSuffix,
+                    usage: result.usage,
+                    toolCalls: result.toolCalls,
+                    parsedRequest,
+                    runtime,
+                    promptContext,
+                    shouldPersistLearnedAgentSource: true,
+                });
+
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {},
+                        finishReason: 'stop',
+                    }),
+                );
+                controller.enqueue(encoder.encode('[DONE]'));
+            } catch (error) {
+                console.error('Error during streaming:', error);
+                controller.error(error);
+            }
+            controller.close();
+        },
+    });
+
+    return new Response(readableStream, {
+        headers: {
+            'Content-Type': 'text/event-stream',
+            'Cache-Control': 'no-cache',
+            Connection: 'keep-alive',
+        },
+    });
+}
+
+/**
+ * Creates one OpenAI-compatible streaming chunk payload.
+ */
+function createHandleChatCompletionChunk(options: {
+    runId: string;
+    created: number;
+    model: HandleChatCompletionParsedRequest['model'];
+    delta: Record<string, unknown>;
+    finishReason: 'stop' | null;
+}) {
+    return {
+        id: options.runId,
+        object: 'chat.completion.chunk',
+        created: options.created,
+        model: options.model,
+        choices: [
+            {
+                index: 0,
+                delta: options.delta,
+                finish_reason: options.finishReason,
+            },
+        ],
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
new file mode 100644
index 0000000000000000000000000000000000000000..dd6a7d87ea491df245dbf93d8bcb277a9a10b034
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
@@ -0,0 +1,78 @@
+import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
+import { resolveAppendOnlySelfLearningAgentSource } from '@/src/utils/resolveAppendOnlySelfLearningAgentSource';
+import type { ToolCall, Usage } from '@promptbook-local/types';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Runs the shared side effects after one successful model response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function finalizeHandleChatCompletionResult(options: {
+    responseContentWithSuffix: string;
+    usage: Usage;
+    toolCalls?: ReadonlyArray<ToolCall>;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
+
+    await promptContext.recordChatHistoryMessage({
+        message: {
+            role: 'MODEL',
+            content: responseContentWithSuffix,
+        },
+        previousMessageHash: promptContext.userMessageHash,
+        usage,
+    });
+
+    if (toolCalls && toolCalls.length > 0) {
+        await logCalendarToolCallsActivity({
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentPermanentId: runtime.agentId,
+            toolCalls,
+        });
+    }
+
+    await persistOpenAiCompatibilityFrozenChat({
+        isEnabled: !runtime.isPrivateModeEnabled,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        agentId: runtime.agentId,
+        chatId: promptContext.persistedFrozenChatId,
+        messages: parsedRequest.messages,
+        assistantContent: responseContentWithSuffix,
+        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
+    });
+    await persistHandleChatCompletionLearnedAgentSource({
+        runtime,
+        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
+    });
+}
+
+/**
+ * Applies append-only self-learning updates when the current branch should persist them.
+ */
+async function persistHandleChatCompletionLearnedAgentSource(options: {
+    runtime: HandleChatCompletionRuntime;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { runtime, shouldPersistLearnedAgentSource } = options;
+    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
+        return;
+    }
+
+    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
+        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
+        resolvedAgentSourceBeforeLearning: runtime.agentSource,
+        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
+    });
+
+    if (learnedAgentSource !== null) {
+        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
+    }
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
new file mode 100644
index 0000000000000000000000000000000000000000..58e1c26d7f428c1bdb39b36411b3f75256e3de51
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
@@ -0,0 +1,176 @@
+import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
+import type { LlmToolDefinition, TODO_any } from '@promptbook-local/types';
+import { NextRequest, NextResponse } from 'next/server';
+import type OpenAI from 'openai';
+import { HTTP_STATUS_CODES } from '../../constants';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Definition of open AI chat tool.
+ */
+type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
+
+/**
+ * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionParsedRequest = {
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    threadMessages: ReadonlyArray<TODO_any>;
+    stream: TODO_any;
+    model: TODO_any;
+    responseFormat: TODO_any;
+    runtimeTools?: Array<LlmToolDefinition>;
+    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+    incomingParameters: Record<string, unknown>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Parses and validates the OpenAI-compatible request payload.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function parseHandleChatCompletionRequest(options: {
+    body: TODO_any;
+    agentNameFromParams?: string;
+    request: NextRequest;
+}): HandleChatCompletionParsedRequest | NextResponse {
+    const { body, agentNameFromParams, request } = options;
+    const {
+        messages,
+        stream,
+        model,
+        response_format: responseFormat,
+        tools: rawTools,
+        tool_choice: toolChoice,
+        parameters: rawParameters = {},
+    } = body;
+    const agentName = (agentNameFromParams || model) as string | undefined;
+    if (!agentName) {
+        return createHandleChatCompletionErrorResponse(
+            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    if (!messages || !Array.isArray(messages) || messages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array is required and cannot be empty.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
+    if (threadMessages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array must contain at least one non-system message.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    return {
+        agentName,
+        messages,
+        threadMessages,
+        stream,
+        model,
+        responseFormat,
+        runtimeTools: convertOpenAiTools(rawTools),
+        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
+        incomingParameters:
+            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
+                ? (rawParameters as Record<string, unknown>)
+                : {},
+        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
+    };
+}
+
+/**
+ * Handles convert open Ai tools.
+ */
+function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
+    if (!Array.isArray(rawTools)) {
+        return undefined;
+    }
+
+    const converted = rawTools
+        .map((tool) => convertOpenAiTool(tool))
+        .filter((tool): tool is LlmToolDefinition => tool !== null);
+
+    return converted.length > 0 ? converted : undefined;
+}
+
+/**
+ * Handles convert open Ai tool.
+ */
+function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
+    const tool = rawTool as OpenAIChatToolDefinition;
+    if (!tool || tool.type !== 'function') {
+        return null;
+    }
+
+    const functionDefinition = tool.function;
+    if (!functionDefinition) {
+        return null;
+    }
+
+    const { name, description, parameters } = functionDefinition;
+    if (typeof name !== 'string' || name.trim().length === 0) {
+        return null;
+    }
+
+    const parameterSchema = parameters ?? {};
+    if (parameterSchema.type !== 'object') {
+        return null;
+    }
+
+    const properties = parameterSchema.properties ?? {};
+    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
+
+    for (const [key, value] of Object.entries(properties)) {
+        if (!value || typeof value !== 'object') {
+            continue;
+        }
+
+        normalizedProperties[key] = {
+            type: typeof value.type === 'string' ? value.type : 'string',
+            description: typeof value.description === 'string' ? value.description : undefined,
+        };
+    }
+
+    const required =
+        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
+            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
+            : undefined;
+
+    return {
+        name,
+        description: typeof description === 'string' ? description : '',
+        parameters: {
+            type: 'object',
+            properties: normalizedProperties,
+            required,
+            additionalProperties:
+                typeof parameterSchema.additionalProperties === 'boolean'
+                    ? parameterSchema.additionalProperties
+                    : undefined,
+        },
+    };
+}
+
+/**
+ * Parses open Ai tool choice.
+ */
+function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
+    if (value === undefined || value === null) {
+        return undefined;
+    }
+
+    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
new file mode 100644
index 0000000000000000000000000000000000000000..60e5f1b008bcf5a6a1836c06c4c3d9ebcb909a9f
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
@@ -0,0 +1,163 @@
+import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
+import type { ChatMessage } from '@promptbook-local/types';
+
+/**
+ * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
+ */
+type OpenAiCompatibilityMessageLike = {
+    role?: unknown;
+    content?: unknown;
+};
+
+/**
+ * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function persistOpenAiCompatibilityFrozenChat(options: {
+    isEnabled: boolean;
+    userId: number | null | undefined;
+    agentId: string;
+    messages: ReadonlyArray<unknown>;
+    chatId?: string;
+    assistantContent?: string;
+    includeAssistantPlaceholder?: boolean;
+    failureMessage: string;
+}): Promise<string | undefined> {
+    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
+        options;
+    if (!isEnabled || !userId) {
+        return undefined;
+    }
+
+    const persistedFrozenChat = await persistFrozenUserChat({
+        userId,
+        agentPermanentId: agentId,
+        source: USER_CHAT_SOURCES.OPENAI_API,
+        chatId,
+        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
+            includeAssistantPlaceholder,
+        }),
+    }).catch((error) => {
+        console.error(failureMessage, error);
+        return null;
+    });
+
+    return persistedFrozenChat?.id;
+}
+
+/**
+ * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
+ */
+function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
+    if (typeof value === 'string') {
+        return value;
+    }
+
+    if (Array.isArray(value)) {
+        return value
+            .map((part) => {
+                if (typeof part === 'string') {
+                    return part;
+                }
+
+                if (!part || typeof part !== 'object') {
+                    return '';
+                }
+
+                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
+                if (typeof partLike.text === 'string') {
+                    return partLike.text;
+                }
+
+                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
+                    return '[Image input]';
+                }
+
+                if (partLike.image_url) {
+                    return '[Image input]';
+                }
+
+                return '';
+            })
+            .filter(Boolean)
+            .join('\n');
+    }
+
+    if (value === null || value === undefined) {
+        return '';
+    }
+
+    try {
+        return JSON.stringify(value, null, 2);
+    } catch {
+        return String(value);
+    }
+}
+
+/**
+ * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ */
+function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
+    if (typeof role !== 'string') {
+        return 'USER';
+    }
+
+    switch (role) {
+        case 'assistant':
+            return 'AGENT';
+        case 'system':
+            return 'SYSTEM';
+        case 'tool':
+            return 'TOOL';
+        case 'developer':
+            return 'SYSTEM';
+        default:
+            return 'USER';
+    }
+}
+
+/**
+ * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
+ */
+function createFrozenOpenAiCompatibilityMessages(
+    rawMessages: ReadonlyArray<unknown>,
+    assistantContent?: string,
+    options: {
+        includeAssistantPlaceholder?: boolean;
+    } = {},
+): Array<ChatMessage> {
+    const startedAt = Date.now();
+    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
+        const message = rawMessage as OpenAiCompatibilityMessageLike;
+
+        return {
+            id: `openai-frozen-${index}`,
+            sender: mapOpenAiCompatibilityRoleToSender(message.role),
+            content: normalizeOpenAiCompatibilityMessageContent(message.content),
+            isComplete: true,
+            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage;
+    });
+
+    if (assistantContent !== undefined) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: assistantContent,
+            isComplete: true,
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    } else if (options.includeAssistantPlaceholder) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: '',
+            isComplete: false,
+            lifecycleState: 'running',
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    }
+
+    return messages;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fd44b7ea5a87168725a9e99784038137c96537ab
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
@@ -0,0 +1,305 @@
+import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
+import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
+import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
+import type { string_book, TODO_any } from '@promptbook-local/types';
+import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
+import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
+import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
+import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
+import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
+import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
+import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
+import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
+import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
+import {
+    createBookScopedAgentReferenceResolver,
+    parseBookScopedAgentIdentifier,
+} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
+import { resolveMessageSuffixFromAgentSource } from '@/src/utils/chat/messageSuffix';
+import { NextRequest, NextResponse } from 'next/server';
+import { isAgentDeleted } from '../../app/agents/[agentName]/_utils';
+import {
+    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    resolveAgentCollectionTablePrefix,
+    waitForRunningAgentPreparation,
+} from '../agentPreparation';
+import { resolveCachedServerAgentContext, resolveCachedServerAgentModelRequirements } from '../cachedServerAgentRuntime';
+import { AgentKitCacheManager } from '../cache/AgentKitCacheManager';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Runtime state resolved before the chat model is executed.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionRuntime = {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    agent: Agent;
+    agentSource: string_book;
+    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
+    messageSuffix: string | null;
+    agentId: string;
+    agentHash: string;
+    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Resolves agent/runtime dependencies needed before prompt execution.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function resolveHandleChatCompletionRuntime(options: {
+    request: NextRequest;
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    isPrivateModeEnabled: boolean;
+}): Promise<HandleChatCompletionRuntime | NextResponse> {
+    const { request, agentName, messages, isPrivateModeEnabled } = options;
+    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
+    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
+
+    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
+        return createHandleChatCompletionErrorResponse(
+            'This agent has been deleted. You can restore it from the Recycle Bin.',
+            'agent_deleted',
+            410,
+        );
+    }
+
+    const [collection, baseAgentReferenceResolver] = await Promise.all([
+        $provideAgentCollectionForServer(),
+        $provideAgentReferenceResolver(),
+    ]);
+    const localServerUrl = new URL(request.url).origin;
+    const resolvedAgentContext = await resolveCachedServerAgentContext({
+        collection,
+        agentIdentifier: agentName,
+        localServerUrl,
+        fallbackResolver: baseAgentReferenceResolver,
+    }).catch(() => null);
+
+    if (!resolvedAgentContext?.resolvedAgentSource) {
+        return createHandleChatCompletionErrorResponse(`Agent '${agentName}' not found.`, 'invalid_request_error', 404);
+    }
+
+    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
+    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
+    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
+    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
+    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
+    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
+    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
+    agentSource = agentSourceWithContext;
+
+    const preparedAgentModelRequirements = !hasDynamicContext
+        ? await resolveCachedServerAgentModelRequirements({
+              resolvedAgentContext,
+              localServerUrl,
+              fallbackResolver: baseAgentReferenceResolver,
+          })
+        : null;
+    const agentId = resolvedAgentContext.parentAgentPermanentId;
+    const agentHash = computeAgentHash(agentSource);
+    await waitForHandleChatCompletionAgentPreparation({
+        collection,
+        agentId,
+        agentHash,
+    });
+
+    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
+    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
+        await resolveHandleChatCompletionCredentials({
+            currentUserIdentity,
+            agentId,
+            calendarConnections,
+            useEmailConfiguration,
+        });
+    const agent = await createHandleChatCompletionAgent({
+        agentName,
+        agentSource,
+        agentId,
+        resolvedAgentContext,
+        localServerUrl,
+        baseAgentReferenceResolver,
+        hasDynamicContext,
+        preparedAgentModelRequirements,
+    });
+
+    return {
+        collection,
+        resolvedAgentContext,
+        agent,
+        agentSource,
+        unresolvedAgentSource,
+        messageSuffix,
+        agentId,
+        agentHash,
+        projectRepositories,
+        calendarConnections,
+        useEmailConfiguration,
+        currentUserIdentity,
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+        isPrivateModeEnabled,
+    };
+}
+
+/**
+ * Appends system messages to the agent source as dynamic `CONTEXT`.
+ */
+function appendSystemMessagesToAgentSource(
+    agentSource: string_book,
+    messages: ReadonlyArray<TODO_any>,
+): {
+    agentSourceWithContext: string_book;
+    hasDynamicContext: boolean;
+} {
+    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
+    if (systemMessages.length === 0) {
+        return {
+            agentSourceWithContext: agentSource,
+            hasDynamicContext: false,
+        };
+    }
+
+    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
+    return {
+        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
+        hasDynamicContext: true,
+    };
+}
+
+/**
+ * Waits for any running preparation job of the current agent before executing the chat call.
+ */
+async function waitForHandleChatCompletionAgentPreparation(options: {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    agentId: string;
+    agentHash: string;
+}): Promise<void> {
+    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
+    const preparationWaitResult = await waitForRunningAgentPreparation({
+        tablePrefix,
+        agentPermanentId: options.agentId,
+        fingerprint: options.agentHash,
+        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    });
+
+    if (preparationWaitResult !== 'not_running') {
+        console.info('[pre-index]', 'openai_chat_wait_result', {
+            tablePrefix,
+            agentPermanentId: options.agentId,
+            agentHash: options.agentHash,
+            preparationWaitResult,
+        });
+    }
+}
+
+/**
+ * Resolves request-scoped project, calendar, and email credentials for the current agent.
+ */
+async function resolveHandleChatCompletionCredentials(options: {
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    agentId: string;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+}): Promise<{
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+}> {
+    const userId = options.currentUserIdentity?.userId;
+    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
+        resolveUseProjectGithubToken({
+            userId,
+            agentPermanentId: options.agentId,
+        }),
+        options.calendarConnections.length > 0
+            ? resolveUseCalendarGoogleToken({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+        options.useEmailConfiguration.isEnabled
+            ? resolveUseEmailSmtpCredential({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+    ]);
+
+    return {
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+    };
+}
+
+/**
+ * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
+ */
+async function createHandleChatCompletionAgent(options: {
+    agentName: string;
+    agentSource: string_book;
+    agentId: string;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    localServerUrl: string;
+    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
+    hasDynamicContext: boolean;
+    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
+}): Promise<Agent> {
+    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
+    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
+    const runtimeAgentReferenceResolver = options.hasDynamicContext
+        ? createBookScopedAgentReferenceResolver({
+              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
+              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
+              localServerUrl: options.localServerUrl,
+              fallbackResolver: options.baseAgentReferenceResolver,
+          })
+        : undefined;
+    const agentModelRequirements =
+        options.preparedAgentModelRequirements?.modelRequirements ||
+        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
+            agentReferenceResolver: runtimeAgentReferenceResolver,
+            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
+        }));
+
+    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
+        options.agentSource,
+        options.resolvedAgentContext.resolvedAgentName,
+        await baseOpenAiToolsPromise,
+        {
+            includeDynamicContext: true,
+            agentId: options.agentId,
+            modelRequirements: agentModelRequirements,
+        },
+    );
+
+    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
+        agentName: options.agentName,
+        assistantCacheKey: agentKitResult.assistantCacheKey,
+        vectorStoreHash: agentKitResult.vectorStoreHash,
+        vectorStoreId: agentKitResult.vectorStoreId,
+    });
+
+    return new Agent({
+        agentSource: options.agentSource,
+        executionTools: {
+            llm: agentKitResult.tools,
+        },
+        precomputedModelRequirements: agentModelRequirements,
+        assistantPreparationMode: 'external',
+        isVerbose: true,
+        teacherAgent: null,
+    });
+}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 474fd839d14b2bdab324e5f59fce0b15ddf95afd..bc9cfde73332af5d107ad35780a31963bc4e68e9
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,8 @@
+-   Refactored Agents Server `handleChatCompletion.ts` into a thinner chat-completion facade plus focused private helper modules without changing external behavior:
+
+    -   Extracted OpenAI-compatible request parsing, runtime/agent resolution, prompt-context creation, frozen-chat persistence, shared post-response side effects, and the streaming vs non-streaming response branches into dedicated private files behind the existing `handleChatCompletion` entrypoint.
+    -   Kept the existing API-key validation, deleted-agent handling, dynamic system-context injection, AgentKit cache usage, private-mode behavior, frozen audit snapshots, self-learning persistence, streaming chunk format, and OpenAI-compatible JSON response shape intact while reducing responsibility density in the main file.
+
 -   Refactored Agents Server `errorMessages.ts` into smaller focused helpers without changing external behavior:
 
     -   Split the branching-heavy `categorizeError` flow into dedicated API-type, native-error-message, and status-code classification helpers while preserving the existing precedence.



thinking
**Considering link formatting**

I’m trying to figure out how to format paths safely. It seems like using backticked relative paths might be safer than markdown links, especially since the instructions prefer clickable ones. However, Windows paths with colons may be an issue with markdown parsing. Since I’m uncertain, I think I’ll stick with simpler relative paths in backticks to avoid broken links. Yet the instructions do say that for local files, I should prefer clickable markdown links.
thinking
**Considering file structure**

I might mention a subfolder without specifying a line or providing a link. It could be sufficient to reference the main file along with one or two modules. For example, I could say "private modules under `apps/agents-server/src/utils/handleChatCompletion/`" in code ticks. I'll keep in mind to mention any unrelated prompt artifacts if it's appropriate, ensuring I don’t start with an interjection for clarity.
codex
Refactoring is in place. [handleChatCompletion.ts](<C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/handleChatCompletion.ts:14>) is now a thin orchestration facade, and the internal responsibilities were split into focused private modules under `apps/agents-server/src/utils/handleChatCompletion/` for request parsing, runtime/agent resolution, prompt setup, frozen-chat persistence, shared post-response side effects, and the JSON vs streaming response branches. The extracted helpers use matching filenames/exports and `@private` JSDoc as requested, while preserving the existing behavior.

I also added the changelog entry at [_current-preversion.md](<C:/Users/me/work/ai/promptbook/changelog/_current-preversion.md:1>).

Verification passed:
- `npm run test-name-discrepancies`
- `npm run test-package-generation`

`git status` still shows unrelated prompt artifact changes under `prompts/`; I left those untouched.
file update:
diff --git a/apps/agents-server/src/utils/handleChatCompletion.ts b/apps/agents-server/src/utils/handleChatCompletion.ts
index 9c96d36fad9cf24bf26d89f57372332add4e4595..cff56fafd6cfa90bb4094f9f202c4f01f6f74af1
--- a/apps/agents-server/src/utils/handleChatCompletion.ts
+++ b/apps/agents-server/src/utils/handleChatCompletion.ts
@@ -1,392 +1,16 @@
-import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
-import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
-import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
-import {
-    createBookScopedAgentReferenceResolver,
-    parseBookScopedAgentIdentifier,
-} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
-import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
-import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
-import {
-    appendMessageSuffix,
-    createMessageSuffixAppendix,
-    emulateMessageSuffixStreaming,
-    resolveMessageSuffixFromAgentSource,
-} from '@/src/utils/chat/messageSuffix';
-import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
-import { prepareToolCallsForStreaming } from './toolCallStreaming';
-import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
-import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
-import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
-import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
-import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
-import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
-import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
-import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
-import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
-import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
-import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
-import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
-import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
-import type {
-    ChatMessage,
-    ChatPrompt,
-    LlmToolDefinition,
-    string_book,
-    ToolCall,
-    TODO_any,
-    UncertainNumber,
-    Usage,
-    UsageCounts,
-} from '@promptbook-local/types';
-import { $getCurrentDate } from '@promptbook-local/utils';
 import { NextRequest, NextResponse } from 'next/server';
-import type OpenAI from 'openai';
-import { computeUsageCounts } from '../../../../src/execution/utils/computeUsageCounts';
-import { encodeChatStreamWhitespaceForTransport } from '../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
-import { isAgentDeleted } from '../app/agents/[agentName]/_utils';
 import { HTTP_STATUS_CODES } from '../constants';
-import { AgentKitCacheManager } from './cache/AgentKitCacheManager';
 import { respondIfClientVersionIsOutdated } from './clientVersionGuard';
+import { createHandleChatCompletionJsonResponse } from './handleChatCompletion/createHandleChatCompletionJsonResponse';
+import { createHandleChatCompletionPromptContext } from './handleChatCompletion/createHandleChatCompletionPromptContext';
+import { createHandleChatCompletionStreamResponse } from './handleChatCompletion/createHandleChatCompletionStreamResponse';
+import { parseHandleChatCompletionRequest } from './handleChatCompletion/parseHandleChatCompletionRequest';
+import { resolveHandleChatCompletionRuntime } from './handleChatCompletion/resolveHandleChatCompletionRuntime';
 import { validateApiKey } from './validateApiKey';
-import {
-    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    resolveAgentCollectionTablePrefix,
-    waitForRunningAgentPreparation,
-} from './agentPreparation';
-import {
-    resolveCachedServerAgentContext,
-    resolveCachedServerAgentModelRequirements,
-} from './cachedServerAgentRuntime';
-import { resolveAppendOnlySelfLearningAgentSource } from './resolveAppendOnlySelfLearningAgentSource';
 
 /**
- * Falls back to the estimated value when the original token count is unknown.
- *
- * @param tokenCount - Token count reported by the execution tools.
- * @param fallbackValue - Estimated token count based on text length.
- * @returns Token count to report in the OpenAI-compatible response.
- */
-function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
-    if (tokenCount.value === 0 && tokenCount.isUncertain) {
-        return {
-            value: fallbackValue,
-            isUncertain: true,
-        };
-    }
-
-    return tokenCount;
-}
-
-/**
- * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
- *
- * @param promptContent - Prompt content used for the request.
- * @param completionContent - Assistant response content.
- * @param usage - Native Promptbook usage details.
- * @returns Usage payload for compatibility responses.
- */
-function createCompatibilityUsage(
-    promptContent: string,
-    completionContent: string,
-    usage: Usage,
-): {
-    prompt_tokens: number;
-    completion_tokens: number;
-    total_tokens: number;
-    details: Usage;
-} {
-    const promptCounts = computeUsageCounts(promptContent);
-    const completionCounts = computeUsageCounts(completionContent);
-    const inputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
-        ...promptCounts,
-    };
-    const outputUsage: UsageCounts = {
-        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
-        ...completionCounts,
-    };
-
-    const promptTokens = inputUsage.tokensCount.value;
-    const completionTokens = outputUsage.tokensCount.value;
-
-    return {
-        prompt_tokens: promptTokens,
-        completion_tokens: completionTokens,
-        total_tokens: promptTokens + completionTokens,
-        details: {
-            price: usage.price,
-            duration: usage.duration,
-            input: inputUsage,
-            output: outputUsage,
-        },
-    };
-}
-
-/**
- * Definition of open AI chat tool.
- */
-type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
-
-/**
- * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
- */
-type OpenAiCompatibilityMessageLike = {
-    role?: unknown;
-    content?: unknown;
-};
-
-/**
- * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
- */
-type HandleChatCompletionParsedRequest = {
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    threadMessages: ReadonlyArray<TODO_any>;
-    stream: TODO_any;
-    model: TODO_any;
-    responseFormat: TODO_any;
-    runtimeTools?: Array<LlmToolDefinition>;
-    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-    incomingParameters: Record<string, unknown>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Runtime state resolved before the chat model is executed.
- */
-type HandleChatCompletionRuntime = {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    agent: Agent;
-    agentSource: string_book;
-    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
-    messageSuffix: string | null;
-    agentId: string;
-    agentHash: string;
-    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-    isPrivateModeEnabled: boolean;
-};
-
-/**
- * Prompt, history recorder, and persistence context created for one chat call.
- */
-type HandleChatCompletionPromptContext = {
-    prompt: ChatPrompt;
-    userMessageHash: string;
-    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
-    persistedFrozenChatId?: string;
-};
-
-/**
- * Handles convert open Ai tools.
- */
-function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
-    if (!Array.isArray(rawTools)) {
-        return undefined;
-    }
-
-    const converted = rawTools
-        .map((tool) => convertOpenAiTool(tool))
-        .filter((tool): tool is LlmToolDefinition => tool !== null);
-
-    return converted.length > 0 ? converted : undefined;
-}
-
-/**
- * Handles convert open Ai tool.
- */
-function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
-    const tool = rawTool as OpenAIChatToolDefinition;
-    if (!tool || tool.type !== 'function') {
-        return null;
-    }
-
-    const functionDefinition = tool.function;
-    if (!functionDefinition) {
-        return null;
-    }
-
-    const { name, description, parameters } = functionDefinition;
-    if (typeof name !== 'string' || name.trim().length === 0) {
-        return null;
-    }
-
-    const parameterSchema = parameters ?? {};
-    if (parameterSchema.type !== 'object') {
-        return null;
-    }
-
-    const properties = parameterSchema.properties ?? {};
-    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
-
-    for (const [key, value] of Object.entries(properties)) {
-        if (!value || typeof value !== 'object') {
-            continue;
-        }
-
-        normalizedProperties[key] = {
-            type: typeof value.type === 'string' ? value.type : 'string',
-            description: typeof value.description === 'string' ? value.description : undefined,
-        };
-    }
-
-    const required =
-        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
-            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
-            : undefined;
-
-    return {
-        name,
-        description: typeof description === 'string' ? description : '',
-        parameters: {
-            type: 'object',
-            properties: normalizedProperties,
-            required,
-            additionalProperties:
-                typeof parameterSchema.additionalProperties === 'boolean'
-                    ? parameterSchema.additionalProperties
-                    : undefined,
-        },
-    };
-}
-
-/**
- * Parses open Ai tool choice.
- */
-function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
-    if (value === undefined || value === null) {
-        return undefined;
-    }
-
-    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-}
-
-/**
- * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
- */
-function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
-    if (typeof value === 'string') {
-        return value;
-    }
-
-    if (Array.isArray(value)) {
-        return value
-            .map((part) => {
-                if (typeof part === 'string') {
-                    return part;
-                }
-
-                if (!part || typeof part !== 'object') {
-                    return '';
-                }
-
-                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
-                if (typeof partLike.text === 'string') {
-                    return partLike.text;
-                }
-
-                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
-                    return '[Image input]';
-                }
-
-                if (partLike.image_url) {
-                    return '[Image input]';
-                }
-
-                return '';
-            })
-            .filter(Boolean)
-            .join('\n');
-    }
-
-    if (value === null || value === undefined) {
-        return '';
-    }
-
-    try {
-        return JSON.stringify(value, null, 2);
-    } catch {
-        return String(value);
-    }
-}
-
-/**
- * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ * Handles convert open Ai chat completion requests routed through the Agents Server.
  */
-function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
-    if (typeof role !== 'string') {
-        return 'USER';
-    }
-
-    switch (role) {
-        case 'assistant':
-            return 'AGENT';
-        case 'system':
-            return 'SYSTEM';
-        case 'tool':
-            return 'TOOL';
-        case 'developer':
-            return 'SYSTEM';
-        default:
-            return 'USER';
-    }
-}
-
-/**
- * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
- */
-function createFrozenOpenAiCompatibilityMessages(
-    rawMessages: ReadonlyArray<unknown>,
-    assistantContent?: string,
-    options: {
-        includeAssistantPlaceholder?: boolean;
-    } = {},
-): Array<ChatMessage> {
-    const startedAt = Date.now();
-    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
-        const message = rawMessage as OpenAiCompatibilityMessageLike;
-
-        return {
-            id: `openai-frozen-${index}`,
-            sender: mapOpenAiCompatibilityRoleToSender(message.role),
-            content: normalizeOpenAiCompatibilityMessageContent(message.content),
-            isComplete: true,
-            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage;
-    });
-
-    if (assistantContent !== undefined) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: assistantContent,
-            isComplete: true,
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    } else if (options.includeAssistantPlaceholder) {
-        messages.push({
-            id: `openai-frozen-${messages.length}`,
-            sender: 'AGENT',
-            content: '',
-            isComplete: false,
-            lifecycleState: 'running',
-            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
-        } satisfies ChatMessage);
-    }
-
-    return messages;
-}
-
-/**
- * Handles chat completion.
- */
 export async function handleChatCompletion(
     request: NextRequest,
     params: { agentName?: string },
@@ -399,7 +23,6 @@
         return versionMismatchResponse;
     }
 
-    // Validate API key explicitly (in addition to middleware)
     const apiKeyValidation = await validateApiKey(request);
     if (!apiKeyValidation.isValid) {
         return NextResponse.json(
@@ -462,724 +85,5 @@
             { error: { message: (error as Error).message || 'Internal Server Error', type: 'server_error' } },
             { status: HTTP_STATUS_CODES.INTERNAL_SERVER_ERROR },
         );
-    }
-}
-
-/**
- * Parses and validates the OpenAI-compatible request payload.
- */
-function parseHandleChatCompletionRequest(options: {
-    body: TODO_any;
-    agentNameFromParams?: string;
-    request: NextRequest;
-}): HandleChatCompletionParsedRequest | NextResponse {
-    const { body, agentNameFromParams, request } = options;
-    const {
-        messages,
-        stream,
-        model,
-        response_format: responseFormat,
-        tools: rawTools,
-        tool_choice: toolChoice,
-        parameters: rawParameters = {},
-    } = body;
-    const agentName = (agentNameFromParams || model) as string | undefined;
-    if (!agentName) {
-        return createHandleChatCompletionErrorResponse(
-            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    if (!messages || !Array.isArray(messages) || messages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array is required and cannot be empty.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
-    if (threadMessages.length === 0) {
-        return createHandleChatCompletionErrorResponse(
-            'Messages array must contain at least one non-system message.',
-            'invalid_request_error',
-            HTTP_STATUS_CODES.BAD_REQUEST,
-        );
-    }
-
-    return {
-        agentName,
-        messages,
-        threadMessages,
-        stream,
-        model,
-        responseFormat,
-        runtimeTools: convertOpenAiTools(rawTools),
-        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
-        incomingParameters:
-            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
-                ? (rawParameters as Record<string, unknown>)
-                : {},
-        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
-    };
-}
-
-/**
- * Resolves agent/runtime dependencies needed before prompt execution.
- */
-async function resolveHandleChatCompletionRuntime(options: {
-    request: NextRequest;
-    agentName: string;
-    messages: ReadonlyArray<TODO_any>;
-    isPrivateModeEnabled: boolean;
-}): Promise<HandleChatCompletionRuntime | NextResponse> {
-    const { request, agentName, messages, isPrivateModeEnabled } = options;
-    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
-    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
-
-    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
-        return createHandleChatCompletionErrorResponse(
-            'This agent has been deleted. You can restore it from the Recycle Bin.',
-            'agent_deleted',
-            410,
-        );
-    }
-
-    const [collection, baseAgentReferenceResolver] = await Promise.all([
-        $provideAgentCollectionForServer(),
-        $provideAgentReferenceResolver(),
-    ]);
-    const localServerUrl = new URL(request.url).origin;
-    const resolvedAgentContext = await resolveCachedServerAgentContext({
-        collection,
-        agentIdentifier: agentName,
-        localServerUrl,
-        fallbackResolver: baseAgentReferenceResolver,
-    }).catch(() => null);
-
-    if (!resolvedAgentContext?.resolvedAgentSource) {
-        return createHandleChatCompletionErrorResponse(
-            `Agent '${agentName}' not found.`,
-            'invalid_request_error',
-            HTTP_STATUS_CODES.NOT_FOUND,
-        );
-    }
-
-    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
-    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
-    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
-    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
-    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
-    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
-    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
-    agentSource = agentSourceWithContext;
-
-    const preparedAgentModelRequirements = !hasDynamicContext
-        ? await resolveCachedServerAgentModelRequirements({
-              resolvedAgentContext,
-              localServerUrl,
-              fallbackResolver: baseAgentReferenceResolver,
-          })
-        : null;
-    const agentId = resolvedAgentContext.parentAgentPermanentId;
-    const agentHash = computeAgentHash(agentSource);
-    await waitForHandleChatCompletionAgentPreparation({
-        collection,
-        agentId,
-        agentHash,
-    });
-
-    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
-    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
-        await resolveHandleChatCompletionCredentials({
-            currentUserIdentity,
-            agentId,
-            calendarConnections,
-            useEmailConfiguration,
-        });
-    const agent = await createHandleChatCompletionAgent({
-        agentName,
-        agentSource,
-        agentId,
-        resolvedAgentContext,
-        localServerUrl,
-        baseAgentReferenceResolver,
-        hasDynamicContext,
-        preparedAgentModelRequirements,
-    });
-
-    return {
-        collection,
-        resolvedAgentContext,
-        agent,
-        agentSource,
-        unresolvedAgentSource,
-        messageSuffix,
-        agentId,
-        agentHash,
-        projectRepositories,
-        calendarConnections,
-        useEmailConfiguration,
-        currentUserIdentity,
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-        isPrivateModeEnabled,
-    };
-}
-
-/**
- * Creates prompt and persistence helpers for one chat completion request.
- */
-async function createHandleChatCompletionPromptContext(options: {
-    request: NextRequest;
-    title: string;
-    apiKey: string | null;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-}): Promise<HandleChatCompletionPromptContext> {
-    const { request, title, apiKey, parsedRequest, runtime } = options;
-    const timezone = request.headers.get('x-timezone') || 'UTC';
-    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
-    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
-    const recordChatHistoryMessage = await createChatHistoryRecorder({
-        request,
-        agentIdentifier: runtime.agentId,
-        agentHash: runtime.agentHash,
-        source: 'OPENAI_API_COMPATIBILITY',
-        apiKey,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        isEnabled: !runtime.isPrivateModeEnabled,
-    });
-    const userMessageHash = await recordChatHistoryMessage({
-        message: {
-            role: 'USER',
-            content: lastMessage.content,
-        },
-        previousMessageHash: null,
-    });
-    const promptParameters = composePromptParametersWithMemoryContext({
-        baseParameters: {
-            ...parsedRequest.incomingParameters,
-            timezone,
-        },
-        currentUserIdentity: runtime.currentUserIdentity,
-        agentPermanentId: runtime.agentId,
-        agentName: runtime.resolvedAgentContext.resolvedAgentName,
-        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
-        projectRepositories: runtime.projectRepositories,
-        projectGithubToken: runtime.projectGithubToken,
-        emailSmtpCredential: runtime.emailSmtpCredential,
-        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
-        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
-        calendarConnections: runtime.calendarConnections,
-    });
-    const prompt: ChatPrompt = {
-        title,
-        content: lastMessage.content,
-        modelRequirements: {
-            modelVariant: 'CHAT',
-            responseFormat: parsedRequest.responseFormat,
-            toolChoice: parsedRequest.runtimeToolChoice,
-            // We could pass 'model' from body if we wanted to enforce it, but Agent usually has its own config
-        },
-        parameters: promptParameters,
-        thread,
-        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
-    };
-
-    return {
-        prompt,
-        userMessageHash,
-        recordChatHistoryMessage,
-        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
-            isEnabled: !runtime.isPrivateModeEnabled,
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentId: runtime.agentId,
-            messages: parsedRequest.messages,
-            includeAssistantPlaceholder: true,
-            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
-        }),
-    };
-}
-
-/**
- * Handles the streaming OpenAI-compatible response branch.
- */
-function createHandleChatCompletionStreamResponse(options: {
-    request: NextRequest;
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Response {
-    const { request, title, parsedRequest, runtime, promptContext } = options;
-    const encoder = new TextEncoder();
-    const responseModel = parsedRequest.model || 'promptbook-agent';
-    const readableStream = new ReadableStream({
-        async start(controller) {
-            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
-            const created = Math.floor(Date.now() / 1000);
-            const emitSsePayload = (payload: unknown) => {
-                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
-            };
-            const emitDeltaChunk = (deltaContent: string) => {
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {
-                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
-                        },
-                        finishReason: null,
-                    }),
-                );
-            };
-
-            emitSsePayload(
-                createHandleChatCompletionChunk({
-                    runId,
-                    created,
-                    model: responseModel,
-                    delta: {
-                        role: 'assistant',
-                        content: '',
-                    },
-                    finishReason: null,
-                }),
-            );
-
-            let hasMeaningfulDelta = false;
-
-            try {
-                const handleStreamChunk = createChatStreamHandler({
-                    onDelta: (deltaContent) => {
-                        if (deltaContent.trim().length > 0) {
-                            hasMeaningfulDelta = true;
-                        }
-                        emitDeltaChunk(deltaContent);
-                    },
-                    onToolCalls: (toolCalls) => {
-                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
-                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
-                    },
-                });
-                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
-                    signal: request.signal,
-                });
-                const normalizedResponse = ensureNonEmptyChatContent({
-                    content: result.content,
-                    context: title,
-                });
-
-                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
-                    emitDeltaChunk(normalizedResponse.content);
-                }
-
-                const messageSuffixAppendix = createMessageSuffixAppendix(
-                    normalizedResponse.content,
-                    runtime.messageSuffix,
-                );
-                if (messageSuffixAppendix) {
-                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
-                        emitDeltaChunk(delta);
-                    });
-                }
-
-                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-                await finalizeHandleChatCompletionResult({
-                    responseContentWithSuffix,
-                    usage: result.usage,
-                    toolCalls: result.toolCalls,
-                    parsedRequest,
-                    runtime,
-                    promptContext,
-                    // Preserve historical behavior: streamed OpenAI-compatible calls still persist learning in private mode.
-                    shouldPersistLearnedAgentSource: true,
-                });
-
-                emitSsePayload(
-                    createHandleChatCompletionChunk({
-                        runId,
-                        created,
-                        model: responseModel,
-                        delta: {},
-                        finishReason: 'stop',
-                    }),
-                );
-                controller.enqueue(encoder.encode('[DONE]'));
-            } catch (error) {
-                console.error('Error during streaming:', error);
-                controller.error(error);
-            }
-            controller.close();
-        },
-    });
-
-    return new Response(readableStream, {
-        headers: {
-            'Content-Type': 'text/event-stream',
-            'Cache-Control': 'no-cache',
-            Connection: 'keep-alive',
-        },
-    });
-}
-
-/**
- * Handles the non-streaming OpenAI-compatible response branch.
- */
-async function createHandleChatCompletionJsonResponse(options: {
-    title: string;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-}): Promise<NextResponse> {
-    const { title, parsedRequest, runtime, promptContext } = options;
-    const result = await runtime.agent.callChatModel(promptContext.prompt);
-    const normalizedResponse = ensureNonEmptyChatContent({
-        content: result.content,
-        context: title,
-    });
-    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
-
-    await finalizeHandleChatCompletionResult({
-        responseContentWithSuffix,
-        usage: result.usage,
-        toolCalls: result.toolCalls,
-        parsedRequest,
-        runtime,
-        promptContext,
-        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
-    });
-
-    return NextResponse.json({
-        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
-        object: 'chat.completion',
-        created: Math.floor(Date.now() / 1000),
-        model: parsedRequest.model || 'promptbook-agent',
-        choices: [
-            {
-                index: 0,
-                message: {
-                    role: 'assistant',
-                    content: responseContentWithSuffix,
-                },
-                finish_reason: 'stop',
-            },
-        ],
-        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
-    });
-}
-
-/**
- * Runs the shared side effects after one successful model response.
- */
-async function finalizeHandleChatCompletionResult(options: {
-    responseContentWithSuffix: string;
-    usage: Usage;
-    toolCalls?: ReadonlyArray<ToolCall>;
-    parsedRequest: HandleChatCompletionParsedRequest;
-    runtime: HandleChatCompletionRuntime;
-    promptContext: HandleChatCompletionPromptContext;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
-
-    await promptContext.recordChatHistoryMessage({
-        message: {
-            role: 'MODEL',
-            content: responseContentWithSuffix,
-        },
-        previousMessageHash: promptContext.userMessageHash,
-        usage,
-    });
-
-    if (toolCalls && toolCalls.length > 0) {
-        await logCalendarToolCallsActivity({
-            userId: runtime.currentUserIdentity?.userId ?? null,
-            agentPermanentId: runtime.agentId,
-            toolCalls,
-        });
-    }
-
-    await persistOpenAiCompatibilityFrozenChat({
-        isEnabled: !runtime.isPrivateModeEnabled,
-        userId: runtime.currentUserIdentity?.userId ?? null,
-        agentId: runtime.agentId,
-        chatId: promptContext.persistedFrozenChatId,
-        messages: parsedRequest.messages,
-        assistantContent: responseContentWithSuffix,
-        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
-    });
-    await persistHandleChatCompletionLearnedAgentSource({
-        runtime,
-        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
-    });
-}
-
-/**
- * Applies append-only self-learning updates when the current branch should persist them.
- */
-async function persistHandleChatCompletionLearnedAgentSource(options: {
-    runtime: HandleChatCompletionRuntime;
-    shouldPersistLearnedAgentSource: boolean;
-}): Promise<void> {
-    const { runtime, shouldPersistLearnedAgentSource } = options;
-    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
-        return;
-    }
-
-    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
-        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
-        resolvedAgentSourceBeforeLearning: runtime.agentSource,
-        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
-    });
-
-    if (learnedAgentSource !== null) {
-        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
     }
 }
-
-/**
- * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
- */
-async function persistOpenAiCompatibilityFrozenChat(options: {
-    isEnabled: boolean;
-    userId: number | null | undefined;
-    agentId: string;
-    messages: ReadonlyArray<unknown>;
-    chatId?: string;
-    assistantContent?: string;
-    includeAssistantPlaceholder?: boolean;
-    failureMessage: string;
-}): Promise<string | undefined> {
-    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
-        options;
-    if (!isEnabled || !userId) {
-        return undefined;
-    }
-
-    const persistedFrozenChat = await persistFrozenUserChat({
-        userId,
-        agentPermanentId: agentId,
-        source: USER_CHAT_SOURCES.OPENAI_API,
-        chatId,
-        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
-            includeAssistantPlaceholder,
-        }),
-    }).catch((error) => {
-        console.error(failureMessage, error);
-        return null;
-    });
-
-    return persistedFrozenChat?.id;
-}
-
-/**
- * Builds one OpenAI-compatible thread from the earlier request messages.
- */
-function createHandleChatCompletionThread(previousMessages: ReadonlyArray<TODO_any>): ChatMessage[] {
-    return previousMessages.map((message: TODO_any, index: number) => ({
-        id: `msg-${index}`,
-        sender: message.role === 'assistant' ? 'agent' : 'user',
-        content: message.content,
-        isComplete: true,
-        createdAt: $getCurrentDate(),
-    }));
-}
-
-/**
- * Appends system messages to the agent source as dynamic `CONTEXT`.
- */
-function appendSystemMessagesToAgentSource(
-    agentSource: string_book,
-    messages: ReadonlyArray<TODO_any>,
-): {
-    agentSourceWithContext: string_book;
-    hasDynamicContext: boolean;
-} {
-    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
-    if (systemMessages.length === 0) {
-        return {
-            agentSourceWithContext: agentSource,
-            hasDynamicContext: false,
-        };
-    }
-
-    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
-    return {
-        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
-        hasDynamicContext: true,
-    };
-}
-
-/**
- * Waits for any running preparation job of the current agent before executing the chat call.
- */
-async function waitForHandleChatCompletionAgentPreparation(options: {
-    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
-    agentId: string;
-    agentHash: string;
-}): Promise<void> {
-    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
-    const preparationWaitResult = await waitForRunningAgentPreparation({
-        tablePrefix,
-        agentPermanentId: options.agentId,
-        fingerprint: options.agentHash,
-        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
-    });
-
-    if (preparationWaitResult !== 'not_running') {
-        console.info('[pre-index]', 'openai_chat_wait_result', {
-            tablePrefix,
-            agentPermanentId: options.agentId,
-            agentHash: options.agentHash,
-            preparationWaitResult,
-        });
-    }
-}
-
-/**
- * Resolves request-scoped project, calendar, and email credentials for the current agent.
- */
-async function resolveHandleChatCompletionCredentials(options: {
-    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
-    agentId: string;
-    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
-    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
-}): Promise<{
-    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
-    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
-    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
-}> {
-    const userId = options.currentUserIdentity?.userId;
-    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
-        resolveUseProjectGithubToken({
-            userId,
-            agentPermanentId: options.agentId,
-        }),
-        options.calendarConnections.length > 0
-            ? resolveUseCalendarGoogleToken({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-        options.useEmailConfiguration.isEnabled
-            ? resolveUseEmailSmtpCredential({
-                  userId,
-                  agentPermanentId: options.agentId,
-              })
-            : Promise.resolve(undefined),
-    ]);
-
-    return {
-        projectGithubToken,
-        calendarGoogleAccessToken,
-        emailSmtpCredential,
-    };
-}
-
-/**
- * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
- */
-async function createHandleChatCompletionAgent(options: {
-    agentName: string;
-    agentSource: string_book;
-    agentId: string;
-    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
-    localServerUrl: string;
-    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
-    hasDynamicContext: boolean;
-    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
-}): Promise<Agent> {
-    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
-    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
-    const runtimeAgentReferenceResolver = options.hasDynamicContext
-        ? createBookScopedAgentReferenceResolver({
-              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
-              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
-              localServerUrl: options.localServerUrl,
-              fallbackResolver: options.baseAgentReferenceResolver,
-          })
-        : undefined;
-    const agentModelRequirements =
-        options.preparedAgentModelRequirements?.modelRequirements ||
-        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
-            agentReferenceResolver: runtimeAgentReferenceResolver,
-            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
-        }));
-
-    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
-        options.agentSource,
-        options.resolvedAgentContext.resolvedAgentName,
-        await baseOpenAiToolsPromise,
-        {
-            includeDynamicContext: true,
-            agentId: options.agentId,
-            modelRequirements: agentModelRequirements,
-        },
-    );
-
-    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
-        agentName: options.agentName,
-        assistantCacheKey: agentKitResult.assistantCacheKey,
-        vectorStoreHash: agentKitResult.vectorStoreHash,
-        vectorStoreId: agentKitResult.vectorStoreId,
-    });
-
-    return new Agent({
-        agentSource: options.agentSource,
-        executionTools: {
-            llm: agentKitResult.tools,
-        },
-        precomputedModelRequirements: agentModelRequirements,
-        assistantPreparationMode: 'external',
-        isVerbose: true,
-        teacherAgent: null,
-    });
-}
-
-/**
- * Creates one OpenAI-compatible streaming chunk payload.
- */
-function createHandleChatCompletionChunk(options: {
-    runId: string;
-    created: number;
-    model: TODO_any;
-    delta: Record<string, unknown>;
-    finishReason: 'stop' | null;
-}) {
-    return {
-        id: options.runId,
-        object: 'chat.completion.chunk',
-        created: options.created,
-        model: options.model,
-        choices: [
-            {
-                index: 0,
-                delta: options.delta,
-                finish_reason: options.finishReason,
-            },
-        ],
-    };
-}
-
-/**
- * Creates one consistent OpenAI-compatible error response.
- */
-function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
-    return NextResponse.json(
-        {
-            error: {
-                message,
-                type,
-            },
-        },
-        { status },
-    );
-}
-
-// TODO: [🈹] Maybe move chat thread handling here
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..1f49a4fa80fa21f140e25de59596e5546d62e3f7
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionErrorResponse.ts
@@ -0,0 +1,18 @@
+import { NextResponse } from 'next/server';
+
+/**
+ * Creates one consistent OpenAI-compatible error response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionErrorResponse(message: string, type: string, status: number): NextResponse {
+    return NextResponse.json(
+        {
+            error: {
+                message,
+                type,
+            },
+        },
+        { status },
+    );
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b1979f4621921bc664fed75a05851c6fcb8fba6d
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionJsonResponse.ts
@@ -0,0 +1,120 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import { appendMessageSuffix } from '@/src/utils/chat/messageSuffix';
+import type { UncertainNumber, Usage, UsageCounts } from '@promptbook-local/types';
+import { NextResponse } from 'next/server';
+import { computeUsageCounts } from '../../../../../src/execution/utils/computeUsageCounts';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the non-streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionJsonResponse(options: {
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Promise<NextResponse> {
+    const { title, parsedRequest, runtime, promptContext } = options;
+    const result = await runtime.agent.callChatModel(promptContext.prompt);
+    const normalizedResponse = ensureNonEmptyChatContent({
+        content: result.content,
+        context: title,
+    });
+    const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+
+    await finalizeHandleChatCompletionResult({
+        responseContentWithSuffix,
+        usage: result.usage,
+        toolCalls: result.toolCalls,
+        parsedRequest,
+        runtime,
+        promptContext,
+        shouldPersistLearnedAgentSource: !runtime.isPrivateModeEnabled,
+    });
+
+    return NextResponse.json({
+        id: `chatcmpl-${Math.random().toString(36).substring(2, 15)}`,
+        object: 'chat.completion',
+        created: Math.floor(Date.now() / 1000),
+        model: parsedRequest.model || 'promptbook-agent',
+        choices: [
+            {
+                index: 0,
+                message: {
+                    role: 'assistant',
+                    content: responseContentWithSuffix,
+                },
+                finish_reason: 'stop',
+            },
+        ],
+        usage: createCompatibilityUsage(promptContext.prompt.content, responseContentWithSuffix, result.usage),
+    });
+}
+
+/**
+ * Falls back to the estimated value when the original token count is unknown.
+ *
+ * @param tokenCount - Token count reported by the execution tools.
+ * @param fallbackValue - Estimated token count based on text length.
+ * @returns Token count to report in the OpenAI-compatible response.
+ */
+function ensureTokenCount(tokenCount: UncertainNumber, fallbackValue: number): UncertainNumber {
+    if (tokenCount.value === 0 && tokenCount.isUncertain) {
+        return {
+            value: fallbackValue,
+            isUncertain: true,
+        };
+    }
+
+    return tokenCount;
+}
+
+/**
+ * Creates OpenAI-compatible usage fields based on the agent usage and computed text statistics.
+ *
+ * @param promptContent - Prompt content used for the request.
+ * @param completionContent - Assistant response content.
+ * @param usage - Native Promptbook usage details.
+ * @returns Usage payload for compatibility responses.
+ */
+function createCompatibilityUsage(
+    promptContent: string,
+    completionContent: string,
+    usage: Usage,
+): {
+    prompt_tokens: number;
+    completion_tokens: number;
+    total_tokens: number;
+    details: Usage;
+} {
+    const promptCounts = computeUsageCounts(promptContent);
+    const completionCounts = computeUsageCounts(completionContent);
+    const inputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.input.tokensCount, promptCounts.wordsCount.value),
+        ...promptCounts,
+    };
+    const outputUsage: UsageCounts = {
+        tokensCount: ensureTokenCount(usage.output.tokensCount, completionCounts.wordsCount.value),
+        ...completionCounts,
+    };
+
+    const promptTokens = inputUsage.tokensCount.value;
+    const completionTokens = outputUsage.tokensCount.value;
+
+    return {
+        prompt_tokens: promptTokens,
+        completion_tokens: completionTokens,
+        total_tokens: promptTokens + completionTokens,
+        details: {
+            price: usage.price,
+            duration: usage.duration,
+            input: inputUsage,
+            output: outputUsage,
+        },
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..7aac8141a8b1237ce36430b4b4598a618f0383ac
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionPromptContext.ts
@@ -0,0 +1,111 @@
+import { createChatHistoryRecorder } from '@/src/utils/chat/createChatHistoryRecorder';
+import { composePromptParametersWithMemoryContext } from '@/src/utils/memoryRuntimeContext';
+import type { ChatMessage, ChatPrompt } from '@promptbook-local/types';
+import { $getCurrentDate } from '@promptbook-local/utils';
+import type { NextRequest } from 'next/server';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Prompt, history recorder, and persistence context created for one chat call.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionPromptContext = {
+    prompt: ChatPrompt;
+    userMessageHash: string;
+    recordChatHistoryMessage: Awaited<ReturnType<typeof createChatHistoryRecorder>>;
+    persistedFrozenChatId?: string;
+};
+
+/**
+ * Creates prompt and persistence helpers for one chat completion request.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function createHandleChatCompletionPromptContext(options: {
+    request: NextRequest;
+    title: string;
+    apiKey: string | null;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+}): Promise<HandleChatCompletionPromptContext> {
+    const { request, title, apiKey, parsedRequest, runtime } = options;
+    const timezone = request.headers.get('x-timezone') || 'UTC';
+    const lastMessage = parsedRequest.threadMessages[parsedRequest.threadMessages.length - 1];
+    const thread = createHandleChatCompletionThread(parsedRequest.threadMessages.slice(0, -1));
+    const recordChatHistoryMessage = await createChatHistoryRecorder({
+        request,
+        agentIdentifier: runtime.agentId,
+        agentHash: runtime.agentHash,
+        source: 'OPENAI_API_COMPATIBILITY',
+        apiKey,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        isEnabled: !runtime.isPrivateModeEnabled,
+    });
+    const userMessageHash = await recordChatHistoryMessage({
+        message: {
+            role: 'USER',
+            content: lastMessage.content,
+        },
+        previousMessageHash: null,
+    });
+    const promptParameters = composePromptParametersWithMemoryContext({
+        baseParameters: {
+            ...parsedRequest.incomingParameters,
+            timezone,
+        },
+        currentUserIdentity: runtime.currentUserIdentity,
+        agentPermanentId: runtime.agentId,
+        agentName: runtime.resolvedAgentContext.resolvedAgentName,
+        isPrivateModeEnabled: runtime.isPrivateModeEnabled,
+        projectRepositories: runtime.projectRepositories,
+        projectGithubToken: runtime.projectGithubToken,
+        emailSmtpCredential: runtime.emailSmtpCredential,
+        emailFromAddress: runtime.useEmailConfiguration.senderEmail,
+        calendarGoogleAccessToken: runtime.calendarGoogleAccessToken,
+        calendarConnections: runtime.calendarConnections,
+    });
+    const prompt: ChatPrompt = {
+        title,
+        content: lastMessage.content,
+        modelRequirements: {
+            modelVariant: 'CHAT',
+            responseFormat: parsedRequest.responseFormat,
+            toolChoice: parsedRequest.runtimeToolChoice,
+        },
+        parameters: promptParameters,
+        thread,
+        ...(parsedRequest.runtimeTools ? { tools: parsedRequest.runtimeTools } : {}),
+    };
+
+    return {
+        prompt,
+        userMessageHash,
+        recordChatHistoryMessage,
+        persistedFrozenChatId: await persistOpenAiCompatibilityFrozenChat({
+            isEnabled: !runtime.isPrivateModeEnabled,
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentId: runtime.agentId,
+            messages: parsedRequest.messages,
+            includeAssistantPlaceholder: true,
+            failureMessage: '[user-chat] Failed to persist OpenAI-compatible frozen chat',
+        }),
+    };
+}
+
+/**
+ * Builds one OpenAI-compatible thread from the earlier request messages.
+ */
+function createHandleChatCompletionThread(
+    previousMessages: HandleChatCompletionParsedRequest['threadMessages'],
+): Array<ChatMessage> {
+    return previousMessages.map((message, index) => ({
+        id: `msg-${index}`,
+        sender: message.role === 'assistant' ? 'agent' : 'user',
+        content: message.content,
+        isComplete: true,
+        createdAt: $getCurrentDate(),
+    }));
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
new file mode 100644
index 0000000000000000000000000000000000000000..85e096027760bc45fb730e74f8ae3bbb721b1029
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/createHandleChatCompletionStreamResponse.ts
@@ -0,0 +1,162 @@
+import { ensureNonEmptyChatContent } from '@/src/utils/chat/ensureNonEmptyChatContent';
+import {
+    appendMessageSuffix,
+    createMessageSuffixAppendix,
+    emulateMessageSuffixStreaming,
+} from '@/src/utils/chat/messageSuffix';
+import { createChatStreamHandler } from '@/src/utils/createChatStreamHandler';
+import { encodeChatStreamWhitespaceForTransport } from '../../../../../src/utils/chat/encodeChatStreamWhitespaceForTransport';
+import { prepareToolCallsForStreaming } from '../toolCallStreaming';
+import { finalizeHandleChatCompletionResult } from './finalizeHandleChatCompletionResult';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Handles the streaming OpenAI-compatible response branch.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function createHandleChatCompletionStreamResponse(options: {
+    request: Request;
+    title: string;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+}): Response {
+    const { request, title, parsedRequest, runtime, promptContext } = options;
+    const encoder = new TextEncoder();
+    const responseModel = parsedRequest.model || 'promptbook-agent';
+    const readableStream = new ReadableStream({
+        async start(controller) {
+            const runId = `chatcmpl-${Math.random().toString(36).substring(2, 15)}`;
+            const created = Math.floor(Date.now() / 1000);
+            const emitSsePayload = (payload: unknown) => {
+                controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`));
+            };
+            const emitDeltaChunk = (deltaContent: string) => {
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {
+                            content: encodeChatStreamWhitespaceForTransport(deltaContent),
+                        },
+                        finishReason: null,
+                    }),
+                );
+            };
+
+            emitSsePayload(
+                createHandleChatCompletionChunk({
+                    runId,
+                    created,
+                    model: responseModel,
+                    delta: {
+                        role: 'assistant',
+                        content: '',
+                    },
+                    finishReason: null,
+                }),
+            );
+
+            let hasMeaningfulDelta = false;
+
+            try {
+                const handleStreamChunk = createChatStreamHandler({
+                    onDelta: (deltaContent) => {
+                        if (deltaContent.trim().length > 0) {
+                            hasMeaningfulDelta = true;
+                        }
+                        emitDeltaChunk(deltaContent);
+                    },
+                    onToolCalls: (toolCalls) => {
+                        const preparedToolCalls = prepareToolCallsForStreaming(toolCalls);
+                        controller.enqueue(encoder.encode('\n' + JSON.stringify({ toolCalls: preparedToolCalls }) + '\n'));
+                    },
+                });
+                const result = await runtime.agent.callChatModelStream(promptContext.prompt, handleStreamChunk, {
+                    signal: request.signal,
+                });
+                const normalizedResponse = ensureNonEmptyChatContent({
+                    content: result.content,
+                    context: title,
+                });
+
+                if (normalizedResponse.wasEmpty && !hasMeaningfulDelta) {
+                    emitDeltaChunk(normalizedResponse.content);
+                }
+
+                const messageSuffixAppendix = createMessageSuffixAppendix(
+                    normalizedResponse.content,
+                    runtime.messageSuffix,
+                );
+                if (messageSuffixAppendix) {
+                    await emulateMessageSuffixStreaming(messageSuffixAppendix, (delta) => {
+                        emitDeltaChunk(delta);
+                    });
+                }
+
+                const responseContentWithSuffix = appendMessageSuffix(normalizedResponse.content, runtime.messageSuffix);
+                await finalizeHandleChatCompletionResult({
+                    responseContentWithSuffix,
+                    usage: result.usage,
+                    toolCalls: result.toolCalls,
+                    parsedRequest,
+                    runtime,
+                    promptContext,
+                    shouldPersistLearnedAgentSource: true,
+                });
+
+                emitSsePayload(
+                    createHandleChatCompletionChunk({
+                        runId,
+                        created,
+                        model: responseModel,
+                        delta: {},
+                        finishReason: 'stop',
+                    }),
+                );
+                controller.enqueue(encoder.encode('[DONE]'));
+            } catch (error) {
+                console.error('Error during streaming:', error);
+                controller.error(error);
+            }
+            controller.close();
+        },
+    });
+
+    return new Response(readableStream, {
+        headers: {
+            'Content-Type': 'text/event-stream',
+            'Cache-Control': 'no-cache',
+            Connection: 'keep-alive',
+        },
+    });
+}
+
+/**
+ * Creates one OpenAI-compatible streaming chunk payload.
+ */
+function createHandleChatCompletionChunk(options: {
+    runId: string;
+    created: number;
+    model: HandleChatCompletionParsedRequest['model'];
+    delta: Record<string, unknown>;
+    finishReason: 'stop' | null;
+}) {
+    return {
+        id: options.runId,
+        object: 'chat.completion.chunk',
+        created: options.created,
+        model: options.model,
+        choices: [
+            {
+                index: 0,
+                delta: options.delta,
+                finish_reason: options.finishReason,
+            },
+        ],
+    };
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
new file mode 100644
index 0000000000000000000000000000000000000000..dd6a7d87ea491df245dbf93d8bcb277a9a10b034
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/finalizeHandleChatCompletionResult.ts
@@ -0,0 +1,78 @@
+import { logCalendarToolCallsActivity } from '@/src/utils/calendars/logCalendarToolCallsActivity';
+import { resolveAppendOnlySelfLearningAgentSource } from '@/src/utils/resolveAppendOnlySelfLearningAgentSource';
+import type { ToolCall, Usage } from '@promptbook-local/types';
+import { persistOpenAiCompatibilityFrozenChat } from './persistOpenAiCompatibilityFrozenChat';
+import type { HandleChatCompletionPromptContext } from './createHandleChatCompletionPromptContext';
+import type { HandleChatCompletionParsedRequest } from './parseHandleChatCompletionRequest';
+import type { HandleChatCompletionRuntime } from './resolveHandleChatCompletionRuntime';
+
+/**
+ * Runs the shared side effects after one successful model response.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function finalizeHandleChatCompletionResult(options: {
+    responseContentWithSuffix: string;
+    usage: Usage;
+    toolCalls?: ReadonlyArray<ToolCall>;
+    parsedRequest: HandleChatCompletionParsedRequest;
+    runtime: HandleChatCompletionRuntime;
+    promptContext: HandleChatCompletionPromptContext;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { responseContentWithSuffix, usage, toolCalls, parsedRequest, runtime, promptContext } = options;
+
+    await promptContext.recordChatHistoryMessage({
+        message: {
+            role: 'MODEL',
+            content: responseContentWithSuffix,
+        },
+        previousMessageHash: promptContext.userMessageHash,
+        usage,
+    });
+
+    if (toolCalls && toolCalls.length > 0) {
+        await logCalendarToolCallsActivity({
+            userId: runtime.currentUserIdentity?.userId ?? null,
+            agentPermanentId: runtime.agentId,
+            toolCalls,
+        });
+    }
+
+    await persistOpenAiCompatibilityFrozenChat({
+        isEnabled: !runtime.isPrivateModeEnabled,
+        userId: runtime.currentUserIdentity?.userId ?? null,
+        agentId: runtime.agentId,
+        chatId: promptContext.persistedFrozenChatId,
+        messages: parsedRequest.messages,
+        assistantContent: responseContentWithSuffix,
+        failureMessage: '[user-chat] Failed to refresh OpenAI-compatible frozen chat',
+    });
+    await persistHandleChatCompletionLearnedAgentSource({
+        runtime,
+        shouldPersistLearnedAgentSource: options.shouldPersistLearnedAgentSource,
+    });
+}
+
+/**
+ * Applies append-only self-learning updates when the current branch should persist them.
+ */
+async function persistHandleChatCompletionLearnedAgentSource(options: {
+    runtime: HandleChatCompletionRuntime;
+    shouldPersistLearnedAgentSource: boolean;
+}): Promise<void> {
+    const { runtime, shouldPersistLearnedAgentSource } = options;
+    if (!shouldPersistLearnedAgentSource || runtime.resolvedAgentContext.isBookScopedAgent) {
+        return;
+    }
+
+    const learnedAgentSource = resolveAppendOnlySelfLearningAgentSource({
+        unresolvedAgentSourceBeforeLearning: runtime.unresolvedAgentSource,
+        resolvedAgentSourceBeforeLearning: runtime.agentSource,
+        resolvedAgentSourceAfterLearning: runtime.agent.agentSource.value,
+    });
+
+    if (learnedAgentSource !== null) {
+        await runtime.collection.updateAgentSource(runtime.agentId, learnedAgentSource);
+    }
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
new file mode 100644
index 0000000000000000000000000000000000000000..58e1c26d7f428c1bdb39b36411b3f75256e3de51
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/parseHandleChatCompletionRequest.ts
@@ -0,0 +1,176 @@
+import { isPrivateModeEnabledFromRequest } from '@/src/utils/privateMode';
+import type { LlmToolDefinition, TODO_any } from '@promptbook-local/types';
+import { NextRequest, NextResponse } from 'next/server';
+import type OpenAI from 'openai';
+import { HTTP_STATUS_CODES } from '../../constants';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Definition of open AI chat tool.
+ */
+type OpenAIChatToolDefinition = OpenAI.Chat.Completions.ChatCompletionTool & OpenAI.Beta.AssistantTool;
+
+/**
+ * Parsed OpenAI-compatible request payload used by `handleChatCompletion`.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionParsedRequest = {
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    threadMessages: ReadonlyArray<TODO_any>;
+    stream: TODO_any;
+    model: TODO_any;
+    responseFormat: TODO_any;
+    runtimeTools?: Array<LlmToolDefinition>;
+    runtimeToolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+    incomingParameters: Record<string, unknown>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Parses and validates the OpenAI-compatible request payload.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export function parseHandleChatCompletionRequest(options: {
+    body: TODO_any;
+    agentNameFromParams?: string;
+    request: NextRequest;
+}): HandleChatCompletionParsedRequest | NextResponse {
+    const { body, agentNameFromParams, request } = options;
+    const {
+        messages,
+        stream,
+        model,
+        response_format: responseFormat,
+        tools: rawTools,
+        tool_choice: toolChoice,
+        parameters: rawParameters = {},
+    } = body;
+    const agentName = (agentNameFromParams || model) as string | undefined;
+    if (!agentName) {
+        return createHandleChatCompletionErrorResponse(
+            'Agent name is required. Please provide it in the URL or as the "model" parameter.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    if (!messages || !Array.isArray(messages) || messages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array is required and cannot be empty.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    const threadMessages = messages.filter((message: TODO_any) => message.role !== 'system');
+    if (threadMessages.length === 0) {
+        return createHandleChatCompletionErrorResponse(
+            'Messages array must contain at least one non-system message.',
+            'invalid_request_error',
+            HTTP_STATUS_CODES.BAD_REQUEST,
+        );
+    }
+
+    return {
+        agentName,
+        messages,
+        threadMessages,
+        stream,
+        model,
+        responseFormat,
+        runtimeTools: convertOpenAiTools(rawTools),
+        runtimeToolChoice: parseOpenAiToolChoice(toolChoice),
+        incomingParameters:
+            rawParameters && typeof rawParameters === 'object' && !Array.isArray(rawParameters)
+                ? (rawParameters as Record<string, unknown>)
+                : {},
+        isPrivateModeEnabled: isPrivateModeEnabledFromRequest(request),
+    };
+}
+
+/**
+ * Handles convert open Ai tools.
+ */
+function convertOpenAiTools(rawTools: unknown): Array<LlmToolDefinition> | undefined {
+    if (!Array.isArray(rawTools)) {
+        return undefined;
+    }
+
+    const converted = rawTools
+        .map((tool) => convertOpenAiTool(tool))
+        .filter((tool): tool is LlmToolDefinition => tool !== null);
+
+    return converted.length > 0 ? converted : undefined;
+}
+
+/**
+ * Handles convert open Ai tool.
+ */
+function convertOpenAiTool(rawTool: unknown): LlmToolDefinition | null {
+    const tool = rawTool as OpenAIChatToolDefinition;
+    if (!tool || tool.type !== 'function') {
+        return null;
+    }
+
+    const functionDefinition = tool.function;
+    if (!functionDefinition) {
+        return null;
+    }
+
+    const { name, description, parameters } = functionDefinition;
+    if (typeof name !== 'string' || name.trim().length === 0) {
+        return null;
+    }
+
+    const parameterSchema = parameters ?? {};
+    if (parameterSchema.type !== 'object') {
+        return null;
+    }
+
+    const properties = parameterSchema.properties ?? {};
+    const normalizedProperties: Record<string, { type: string; description?: string }> = {};
+
+    for (const [key, value] of Object.entries(properties)) {
+        if (!value || typeof value !== 'object') {
+            continue;
+        }
+
+        normalizedProperties[key] = {
+            type: typeof value.type === 'string' ? value.type : 'string',
+            description: typeof value.description === 'string' ? value.description : undefined,
+        };
+    }
+
+    const required =
+        Array.isArray(parameterSchema.required) && parameterSchema.required.length > 0
+            ? parameterSchema.required.filter((item): item is string => typeof item === 'string')
+            : undefined;
+
+    return {
+        name,
+        description: typeof description === 'string' ? description : '',
+        parameters: {
+            type: 'object',
+            properties: normalizedProperties,
+            required,
+            additionalProperties:
+                typeof parameterSchema.additionalProperties === 'boolean'
+                    ? parameterSchema.additionalProperties
+                    : undefined,
+        },
+    };
+}
+
+/**
+ * Parses open Ai tool choice.
+ */
+function parseOpenAiToolChoice(value: unknown): OpenAI.Chat.Completions.ChatCompletionToolChoiceOption | undefined {
+    if (value === undefined || value === null) {
+        return undefined;
+    }
+
+    return value as OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
new file mode 100644
index 0000000000000000000000000000000000000000..60e5f1b008bcf5a6a1836c06c4c3d9ebcb909a9f
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/persistOpenAiCompatibilityFrozenChat.ts
@@ -0,0 +1,163 @@
+import { persistFrozenUserChat, USER_CHAT_SOURCES } from '@/src/utils/userChat';
+import type { ChatMessage } from '@promptbook-local/types';
+
+/**
+ * Best-effort OpenAI-compatible message shape used for frozen audit snapshots.
+ */
+type OpenAiCompatibilityMessageLike = {
+    role?: unknown;
+    content?: unknown;
+};
+
+/**
+ * Persists one OpenAI-compatible frozen chat snapshot when the current request should store it.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function persistOpenAiCompatibilityFrozenChat(options: {
+    isEnabled: boolean;
+    userId: number | null | undefined;
+    agentId: string;
+    messages: ReadonlyArray<unknown>;
+    chatId?: string;
+    assistantContent?: string;
+    includeAssistantPlaceholder?: boolean;
+    failureMessage: string;
+}): Promise<string | undefined> {
+    const { isEnabled, userId, agentId, messages, chatId, assistantContent, includeAssistantPlaceholder, failureMessage } =
+        options;
+    if (!isEnabled || !userId) {
+        return undefined;
+    }
+
+    const persistedFrozenChat = await persistFrozenUserChat({
+        userId,
+        agentPermanentId: agentId,
+        source: USER_CHAT_SOURCES.OPENAI_API,
+        chatId,
+        messages: createFrozenOpenAiCompatibilityMessages(messages, assistantContent, {
+            includeAssistantPlaceholder,
+        }),
+    }).catch((error) => {
+        console.error(failureMessage, error);
+        return null;
+    });
+
+    return persistedFrozenChat?.id;
+}
+
+/**
+ * Converts arbitrary OpenAI-compatible message content into plain text for frozen chat replay.
+ */
+function normalizeOpenAiCompatibilityMessageContent(value: unknown): string {
+    if (typeof value === 'string') {
+        return value;
+    }
+
+    if (Array.isArray(value)) {
+        return value
+            .map((part) => {
+                if (typeof part === 'string') {
+                    return part;
+                }
+
+                if (!part || typeof part !== 'object') {
+                    return '';
+                }
+
+                const partLike = part as { type?: unknown; text?: unknown; image_url?: unknown };
+                if (typeof partLike.text === 'string') {
+                    return partLike.text;
+                }
+
+                if (typeof partLike.type === 'string' && partLike.type.toLowerCase().includes('image')) {
+                    return '[Image input]';
+                }
+
+                if (partLike.image_url) {
+                    return '[Image input]';
+                }
+
+                return '';
+            })
+            .filter(Boolean)
+            .join('\n');
+    }
+
+    if (value === null || value === undefined) {
+        return '';
+    }
+
+    try {
+        return JSON.stringify(value, null, 2);
+    } catch {
+        return String(value);
+    }
+}
+
+/**
+ * Maps OpenAI-compatible roles to Promptbook chat senders for frozen chat replay.
+ */
+function mapOpenAiCompatibilityRoleToSender(role: unknown): ChatMessage['sender'] {
+    if (typeof role !== 'string') {
+        return 'USER';
+    }
+
+    switch (role) {
+        case 'assistant':
+            return 'AGENT';
+        case 'system':
+            return 'SYSTEM';
+        case 'tool':
+            return 'TOOL';
+        case 'developer':
+            return 'SYSTEM';
+        default:
+            return 'USER';
+    }
+}
+
+/**
+ * Creates a frozen chat transcript from one OpenAI-compatible request snapshot.
+ */
+function createFrozenOpenAiCompatibilityMessages(
+    rawMessages: ReadonlyArray<unknown>,
+    assistantContent?: string,
+    options: {
+        includeAssistantPlaceholder?: boolean;
+    } = {},
+): Array<ChatMessage> {
+    const startedAt = Date.now();
+    const messages: Array<ChatMessage> = rawMessages.map((rawMessage, index) => {
+        const message = rawMessage as OpenAiCompatibilityMessageLike;
+
+        return {
+            id: `openai-frozen-${index}`,
+            sender: mapOpenAiCompatibilityRoleToSender(message.role),
+            content: normalizeOpenAiCompatibilityMessageContent(message.content),
+            isComplete: true,
+            createdAt: new Date(startedAt + index).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage;
+    });
+
+    if (assistantContent !== undefined) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: assistantContent,
+            isComplete: true,
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    } else if (options.includeAssistantPlaceholder) {
+        messages.push({
+            id: `openai-frozen-${messages.length}`,
+            sender: 'AGENT',
+            content: '',
+            isComplete: false,
+            lifecycleState: 'running',
+            createdAt: new Date(startedAt + messages.length).toISOString() as NonNullable<ChatMessage['createdAt']>,
+        } satisfies ChatMessage);
+    }
+
+    return messages;
+}
diff --git a/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
new file mode 100644
index 0000000000000000000000000000000000000000..fd44b7ea5a87168725a9e99784038137c96537ab
--- /dev/null
+++ b/apps/agents-server/src/utils/handleChatCompletion/resolveHandleChatCompletionRuntime.ts
@@ -0,0 +1,305 @@
+import { $provideAgentCollectionForServer } from '@/src/tools/$provideAgentCollectionForServer';
+import { $provideOpenAiAgentKitExecutionToolsForServer } from '@/src/tools/$provideOpenAiAgentKitExecutionToolsForServer';
+import { Agent, computeAgentHash, createAgentModelRequirements } from '@promptbook-local/core';
+import type { string_book, TODO_any } from '@promptbook-local/types';
+import { createInlineKnowledgeSourceUploader } from '@/src/utils/knowledge/createInlineKnowledgeSourceUploader';
+import { resolveCurrentUserMemoryIdentity } from '@/src/utils/userMemory';
+import { extractUseCalendarConnectionsFromAgentSource } from '@/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource';
+import { extractUseEmailConfigurationFromAgentSource } from '@/src/utils/emails/extractUseEmailConfigurationFromAgentSource';
+import { extractProjectRepositoriesFromAgentSource } from '@/src/utils/projects/extractProjectRepositoriesFromAgentSource';
+import { resolveUseEmailSmtpCredential } from '@/src/utils/resolveUseEmailSmtpCredential';
+import { resolveUseCalendarGoogleToken } from '@/src/utils/resolveUseCalendarGoogleToken';
+import { resolveUseProjectGithubToken } from '@/src/utils/resolveUseProjectGithubToken';
+import { $provideAgentReferenceResolver } from '@/src/utils/agentReferenceResolver/$provideAgentReferenceResolver';
+import {
+    createBookScopedAgentReferenceResolver,
+    parseBookScopedAgentIdentifier,
+} from '@/src/utils/agentReferenceResolver/bookScopedAgentReferences';
+import { resolveMessageSuffixFromAgentSource } from '@/src/utils/chat/messageSuffix';
+import { NextRequest, NextResponse } from 'next/server';
+import { isAgentDeleted } from '../../app/agents/[agentName]/_utils';
+import {
+    AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    resolveAgentCollectionTablePrefix,
+    waitForRunningAgentPreparation,
+} from '../agentPreparation';
+import { resolveCachedServerAgentContext, resolveCachedServerAgentModelRequirements } from '../cachedServerAgentRuntime';
+import { AgentKitCacheManager } from '../cache/AgentKitCacheManager';
+import { createHandleChatCompletionErrorResponse } from './createHandleChatCompletionErrorResponse';
+
+/**
+ * Runtime state resolved before the chat model is executed.
+ *
+ * @private type of `handleChatCompletion`
+ */
+export type HandleChatCompletionRuntime = {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    agent: Agent;
+    agentSource: string_book;
+    unresolvedAgentSource: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>['unresolvedAgentSource'];
+    messageSuffix: string | null;
+    agentId: string;
+    agentHash: string;
+    projectRepositories: ReturnType<typeof extractProjectRepositoriesFromAgentSource>;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+    isPrivateModeEnabled: boolean;
+};
+
+/**
+ * Resolves agent/runtime dependencies needed before prompt execution.
+ *
+ * @private function of `handleChatCompletion`
+ */
+export async function resolveHandleChatCompletionRuntime(options: {
+    request: NextRequest;
+    agentName: string;
+    messages: ReadonlyArray<TODO_any>;
+    isPrivateModeEnabled: boolean;
+}): Promise<HandleChatCompletionRuntime | NextResponse> {
+    const { request, agentName, messages, isPrivateModeEnabled } = options;
+    const parsedBookScopedAgentIdentifier = parseBookScopedAgentIdentifier(agentName);
+    const deletedCheckAgentIdentifier = parsedBookScopedAgentIdentifier?.parentAgentIdentifier || agentName;
+
+    if (await isAgentDeleted(deletedCheckAgentIdentifier)) {
+        return createHandleChatCompletionErrorResponse(
+            'This agent has been deleted. You can restore it from the Recycle Bin.',
+            'agent_deleted',
+            410,
+        );
+    }
+
+    const [collection, baseAgentReferenceResolver] = await Promise.all([
+        $provideAgentCollectionForServer(),
+        $provideAgentReferenceResolver(),
+    ]);
+    const localServerUrl = new URL(request.url).origin;
+    const resolvedAgentContext = await resolveCachedServerAgentContext({
+        collection,
+        agentIdentifier: agentName,
+        localServerUrl,
+        fallbackResolver: baseAgentReferenceResolver,
+    }).catch(() => null);
+
+    if (!resolvedAgentContext?.resolvedAgentSource) {
+        return createHandleChatCompletionErrorResponse(`Agent '${agentName}' not found.`, 'invalid_request_error', 404);
+    }
+
+    let agentSource: string_book = resolvedAgentContext.resolvedAgentSource;
+    const unresolvedAgentSource = resolvedAgentContext.unresolvedAgentSource;
+    const projectRepositories = extractProjectRepositoriesFromAgentSource(agentSource);
+    const calendarConnections = extractUseCalendarConnectionsFromAgentSource(agentSource);
+    const useEmailConfiguration = extractUseEmailConfigurationFromAgentSource(agentSource);
+    const messageSuffix = resolveMessageSuffixFromAgentSource(agentSource);
+    const { agentSourceWithContext, hasDynamicContext } = appendSystemMessagesToAgentSource(agentSource, messages);
+    agentSource = agentSourceWithContext;
+
+    const preparedAgentModelRequirements = !hasDynamicContext
+        ? await resolveCachedServerAgentModelRequirements({
+              resolvedAgentContext,
+              localServerUrl,
+              fallbackResolver: baseAgentReferenceResolver,
+          })
+        : null;
+    const agentId = resolvedAgentContext.parentAgentPermanentId;
+    const agentHash = computeAgentHash(agentSource);
+    await waitForHandleChatCompletionAgentPreparation({
+        collection,
+        agentId,
+        agentHash,
+    });
+
+    const currentUserIdentity = await resolveCurrentUserMemoryIdentity();
+    const { projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential } =
+        await resolveHandleChatCompletionCredentials({
+            currentUserIdentity,
+            agentId,
+            calendarConnections,
+            useEmailConfiguration,
+        });
+    const agent = await createHandleChatCompletionAgent({
+        agentName,
+        agentSource,
+        agentId,
+        resolvedAgentContext,
+        localServerUrl,
+        baseAgentReferenceResolver,
+        hasDynamicContext,
+        preparedAgentModelRequirements,
+    });
+
+    return {
+        collection,
+        resolvedAgentContext,
+        agent,
+        agentSource,
+        unresolvedAgentSource,
+        messageSuffix,
+        agentId,
+        agentHash,
+        projectRepositories,
+        calendarConnections,
+        useEmailConfiguration,
+        currentUserIdentity,
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+        isPrivateModeEnabled,
+    };
+}
+
+/**
+ * Appends system messages to the agent source as dynamic `CONTEXT`.
+ */
+function appendSystemMessagesToAgentSource(
+    agentSource: string_book,
+    messages: ReadonlyArray<TODO_any>,
+): {
+    agentSourceWithContext: string_book;
+    hasDynamicContext: boolean;
+} {
+    const systemMessages = messages.filter((message: TODO_any) => message.role === 'system');
+    if (systemMessages.length === 0) {
+        return {
+            agentSourceWithContext: agentSource,
+            hasDynamicContext: false,
+        };
+    }
+
+    const contextString = systemMessages.map((message: TODO_any) => `CONTEXT ${message.content}`).join('\n');
+    return {
+        agentSourceWithContext: `${agentSource}\n\n${contextString}` as string_book,
+        hasDynamicContext: true,
+    };
+}
+
+/**
+ * Waits for any running preparation job of the current agent before executing the chat call.
+ */
+async function waitForHandleChatCompletionAgentPreparation(options: {
+    collection: Awaited<ReturnType<typeof $provideAgentCollectionForServer>>;
+    agentId: string;
+    agentHash: string;
+}): Promise<void> {
+    const tablePrefix = resolveAgentCollectionTablePrefix(options.collection);
+    const preparationWaitResult = await waitForRunningAgentPreparation({
+        tablePrefix,
+        agentPermanentId: options.agentId,
+        fingerprint: options.agentHash,
+        timeoutMs: AGENT_PREPARATION_CHAT_WAIT_TIMEOUT_MS,
+    });
+
+    if (preparationWaitResult !== 'not_running') {
+        console.info('[pre-index]', 'openai_chat_wait_result', {
+            tablePrefix,
+            agentPermanentId: options.agentId,
+            agentHash: options.agentHash,
+            preparationWaitResult,
+        });
+    }
+}
+
+/**
+ * Resolves request-scoped project, calendar, and email credentials for the current agent.
+ */
+async function resolveHandleChatCompletionCredentials(options: {
+    currentUserIdentity: Awaited<ReturnType<typeof resolveCurrentUserMemoryIdentity>>;
+    agentId: string;
+    calendarConnections: ReturnType<typeof extractUseCalendarConnectionsFromAgentSource>;
+    useEmailConfiguration: ReturnType<typeof extractUseEmailConfigurationFromAgentSource>;
+}): Promise<{
+    projectGithubToken: Awaited<ReturnType<typeof resolveUseProjectGithubToken>>;
+    calendarGoogleAccessToken: Awaited<ReturnType<typeof resolveUseCalendarGoogleToken>>;
+    emailSmtpCredential: Awaited<ReturnType<typeof resolveUseEmailSmtpCredential>>;
+}> {
+    const userId = options.currentUserIdentity?.userId;
+    const [projectGithubToken, calendarGoogleAccessToken, emailSmtpCredential] = await Promise.all([
+        resolveUseProjectGithubToken({
+            userId,
+            agentPermanentId: options.agentId,
+        }),
+        options.calendarConnections.length > 0
+            ? resolveUseCalendarGoogleToken({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+        options.useEmailConfiguration.isEnabled
+            ? resolveUseEmailSmtpCredential({
+                  userId,
+                  agentPermanentId: options.agentId,
+              })
+            : Promise.resolve(undefined),
+    ]);
+
+    return {
+        projectGithubToken,
+        calendarGoogleAccessToken,
+        emailSmtpCredential,
+    };
+}
+
+/**
+ * Creates the request-scoped Promptbook agent that backs one compatibility chat call.
+ */
+async function createHandleChatCompletionAgent(options: {
+    agentName: string;
+    agentSource: string_book;
+    agentId: string;
+    resolvedAgentContext: Awaited<ReturnType<typeof resolveCachedServerAgentContext>>;
+    localServerUrl: string;
+    baseAgentReferenceResolver: Awaited<ReturnType<typeof $provideAgentReferenceResolver>>;
+    hasDynamicContext: boolean;
+    preparedAgentModelRequirements: Awaited<ReturnType<typeof resolveCachedServerAgentModelRequirements>> | null;
+}): Promise<Agent> {
+    const agentKitCacheManager = new AgentKitCacheManager({ isVerbose: true });
+    const baseOpenAiToolsPromise = $provideOpenAiAgentKitExecutionToolsForServer();
+    const runtimeAgentReferenceResolver = options.hasDynamicContext
+        ? createBookScopedAgentReferenceResolver({
+              parentAgentSource: options.resolvedAgentContext.parentAgentSource,
+              parentAgentIdentifier: options.resolvedAgentContext.parentAgentPermanentId,
+              localServerUrl: options.localServerUrl,
+              fallbackResolver: options.baseAgentReferenceResolver,
+          })
+        : undefined;
+    const agentModelRequirements =
+        options.preparedAgentModelRequirements?.modelRequirements ||
+        (await createAgentModelRequirements(options.agentSource, undefined, undefined, undefined, {
+            agentReferenceResolver: runtimeAgentReferenceResolver,
+            inlineKnowledgeSourceUploader: createInlineKnowledgeSourceUploader(),
+        }));
+
+    const agentKitResult = await agentKitCacheManager.getOrCreateAgentKitAgent(
+        options.agentSource,
+        options.resolvedAgentContext.resolvedAgentName,
+        await baseOpenAiToolsPromise,
+        {
+            includeDynamicContext: true,
+            agentId: options.agentId,
+            modelRequirements: agentModelRequirements,
+        },
+    );
+
+    console.info('[🤰]', `AgentKit cache ${agentKitResult.fromCache ? 'hit' : 'miss'} (OpenAI)`, {
+        agentName: options.agentName,
+        assistantCacheKey: agentKitResult.assistantCacheKey,
+        vectorStoreHash: agentKitResult.vectorStoreHash,
+        vectorStoreId: agentKitResult.vectorStoreId,
+    });
+
+    return new Agent({
+        agentSource: options.agentSource,
+        executionTools: {
+            llm: agentKitResult.tools,
+        },
+        precomputedModelRequirements: agentModelRequirements,
+        assistantPreparationMode: 'external',
+        isVerbose: true,
+        teacherAgent: null,
+    });
+}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 474fd839d14b2bdab324e5f59fce0b15ddf95afd..bc9cfde73332af5d107ad35780a31963bc4e68e9
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,8 @@
+-   Refactored Agents Server `handleChatCompletion.ts` into a thinner chat-completion facade plus focused private helper modules without changing external behavior:
+
+    -   Extracted OpenAI-compatible request parsing, runtime/agent resolution, prompt-context creation, frozen-chat persistence, shared post-response side effects, and the streaming vs non-streaming response branches into dedicated private files behind the existing `handleChatCompletion` entrypoint.
+    -   Kept the existing API-key validation, deleted-agent handling, dynamic system-context injection, AgentKit cache usage, private-mode behavior, frozen audit snapshots, self-learning persistence, streaming chunk format, and OpenAI-compatible JSON response shape intact while reducing responsibility density in the main file.
+
 -   Refactored Agents Server `errorMessages.ts` into smaller focused helpers without changing external behavior:
 
     -   Split the branching-heavy `categorizeError` flow into dedicated API-type, native-error-message, and status-code classification helpers while preserving the existing precedence.

tokens used
95,663
Refactoring is in place. [handleChatCompletion.ts](<C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/handleChatCompletion.ts:14>) is now a thin orchestration facade, and the internal responsibilities were split into focused private modules under `apps/agents-server/src/utils/handleChatCompletion/` for request parsing, runtime/agent resolution, prompt setup, frozen-chat persistence, shared post-response side effects, and the JSON vs streaming response branches. The extracted helpers use matching filenames/exports and `@private` JSDoc as requested, while preserving the existing behavior.

I also added the changelog entry at [_current-preversion.md](<C:/Users/me/work/ai/promptbook/changelog/_current-preversion.md:1>).

Verification passed:
- `npm run test-name-discrepancies`
- `npm run test-package-generation`

`git status` still shows unrelated prompt artifact changes under `prompts/`; I left those untouched.

=== runner shell finished at 2026-04-18T07:57:07.376Z ===
Status: succeeded

=== test shell started at 2026-04-18T07:57:07.454Z ===
Script path: /c/Users/me/work/ai/promptbook/prompts/2026-04-5930-refactor-apps-agents-server-src-utils-handle-chat-completion-ts.test.sh

--- raw input ---
cd "/c/Users/me/work/ai/promptbook"
npm run test-for-ptbk-coder

--- raw output ---


> promptbook@0.112.0-45 test-for-ptbk-coder
> echo "Note: Running from most lightweight to heaviest tests" && npm run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm run test-books && npm run test-book-components-build && npm run test-package-generation && npm run test-unit && (cd ./apps/agents-server/ && npm run lint && npm run test-build) && echo "🎉 All tests passed!"

"Note: Running from most lightweight to heaviest tests" 

> promptbook@0.112.0-45 test-name-discrepancies
> ts-node ./scripts/find-name-discrepancies/find-name-discrepancies.ts

🔎🩹 Find name discrepancies
[32mNo name discrepancies found[39m

> promptbook@0.112.0-45 test-spellcheck
> bash ./scripts/spellcheck.sh

   1/1641 src/_packages/anthropic-claude.index.ts 976.08ms
   2/1641 src/_packages/anthropic-claude.readme.md 106.40ms
   3/1641 src/_packages/azure-openai.index.ts 8.79ms
   4/1641 src/_packages/azure-openai.readme.md 19.46ms
   5/1641 src/_packages/browser.index.ts 10.57ms
   6/1641 src/_packages/browser.readme.md 14.20ms
   7/1641 src/_packages/cli.index.ts 49.40ms
   8/1641 src/_packages/cli.readme.md 130.09ms
   9/1641 src/_packages/color.index.ts 12.60ms
  10/1641 src/_packages/color.readme.md 11.69ms
  11/1641 src/_packages/components.index.ts 101.03ms
  12/1641 src/_packages/components.readme.md 11.66ms
  13/1641 src/_packages/content/$provideExecutionToolsForNode.md 5.10ms
  14/1641 src/_packages/content/providers.md 1.90ms
  15/1641 src/_packages/core.index.ts 97.91ms
  16/1641 src/_packages/core.readme.md 52.54ms
  17/1641 src/_packages/deepseek.index.ts 2.52ms
  18/1641 src/_packages/deepseek.readme.md 2.22ms
  19/1641 src/_packages/documents.index.ts 2.56ms
  20/1641 src/_packages/documents.readme.md 2.06ms
  21/1641 src/_packages/editable.index.ts 12.11ms
  22/1641 src/_packages/editable.readme.md 4.63ms
  23/1641 src/_packages/fake-llm.index.ts 2.77ms
  24/1641 src/_packages/fake-llm.readme.md 5.59ms
  25/1641 src/_packages/google.index.ts 2.89ms
  26/1641 src/_packages/google.readme.md 12.40ms
  27/1641 src/_packages/javascript.index.ts 3.48ms
  28/1641 src/_packages/javascript.readme.md 10.37ms
  29/1641 src/_packages/legacy-documents.index.ts 3.19ms
  30/1641 src/_packages/legacy-documents.readme.md 4.40ms
  31/1641 src/_packages/markdown-utils.index.ts 8.42ms
  32/1641 src/_packages/markdown-utils.readme.md 11.97ms
  33/1641 src/_packages/markitdown.index.ts 6.86ms
  34/1641 src/_packages/markitdown.readme.md 1.54ms
  35/1641 src/_packages/node.index.ts 6.51ms
  36/1641 src/_packages/node.readme.md 6.87ms
  37/1641 src/_packages/ollama.index.ts 2.86ms
  38/1641 src/_packages/ollama.readme.md 2.23ms
  39/1641 src/_packages/openai.index.ts 7.71ms
  40/1641 src/_packages/openai.readme.md 24.29ms
  41/1641 src/_packages/pdf.index.ts 3.66ms
  42/1641 src/_packages/pdf.readme.md 1.91ms
  43/1641 src/_packages/promptbook.readme.md 3.35ms
  44/1641 src/_packages/ptbk.readme.md 1.66ms
  45/1641 src/_packages/README.md 3.69ms
  46/1641 src/_packages/remote-client.index.ts 6.42ms
  47/1641 src/_packages/remote-client.readme.md 2.03ms
  48/1641 src/_packages/remote-server.index.ts 3.86ms
  49/1641 src/_packages/remote-server.readme.md 7.36ms
  50/1641 src/_packages/templates.index.ts 2.39ms
  51/1641 src/_packages/templates.readme.md 2.20ms
  52/1641 src/_packages/types.index.ts 66.87ms
  53/1641 src/_packages/types.readme.md 21.74ms
  54/1641 src/_packages/utils.index.ts 21.81ms
  55/1641 src/_packages/utils.readme.md 30.21ms
  56/1641 src/_packages/vercel.index.ts 3.25ms
  57/1641 src/_packages/vercel.readme.md 11.37ms
  58/1641 src/_packages/website-crawler.index.ts 2.56ms
  59/1641 src/_packages/website-crawler.readme.md 1.49ms
  60/1641 src/_packages/wizard.index.ts 9.49ms
  61/1641 src/_packages/wizard.readme.md 2.27ms
  62/1641 src/avatars/Avatar.tsx 45.47ms
  63/1641 src/avatars/avatarRenderingUtils.ts 13.97ms
  64/1641 src/avatars/index.ts 4.13ms
  65/1641 src/avatars/renderAvatarVisual.ts 5.02ms
  66/1641 src/avatars/types/AvatarDefinition.ts 3.40ms
  67/1641 src/avatars/types/AvatarVisualDefinition.ts 4.60ms
  68/1641 src/avatars/visuals/avatarVisualRegistry.ts 5.38ms
  69/1641 src/avatars/visuals/fractalAvatarVisual.ts 14.42ms
  70/1641 src/avatars/visuals/minecraftAvatarVisual.ts 10.73ms
  71/1641 src/avatars/visuals/octopusAvatarVisual.ts 13.10ms
  72/1641 src/avatars/visuals/pixelArtAvatarVisual.ts 10.66ms
  73/1641 src/book-2.0/agent-source/AgentBasicInformation.ts 13.07ms
  74/1641 src/book-2.0/agent-source/AgentModelRequirements.ts 11.45ms
  75/1641 src/book-2.0/agent-source/AgentReferenceResolver.ts 6.29ms
  76/1641 src/book-2.0/agent-source/AgentSourceParseResult.ts 3.74ms
  77/1641 src/book-2.0/agent-source/BookEditable.ts 4.33ms
  78/1641 src/book-2.0/agent-source/communication-samples.test.ts 10.04ms
  79/1641 src/book-2.0/agent-source/computeAgentHash.test.ts 2.64ms
  80/1641 src/book-2.0/agent-source/computeAgentHash.ts 6.55ms
  81/1641 src/book-2.0/agent-source/createAgentModelRequirements.tools.test.ts 14.15ms
  82/1641 src/book-2.0/agent-source/createAgentModelRequirements.ts 9.38ms
  83/1641 src/book-2.0/agent-source/createAgentModelRequirements.useCommitmentAggregation.test.ts 6.06ms
  84/1641 src/book-2.0/agent-source/createAgentModelRequirements.writing.test.ts 3.38ms
  85/1641 src/book-2.0/agent-source/CreateAgentModelRequirementsOptions.ts 6.81ms
  86/1641 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts 9.93ms
  87/1641 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.blocks.test.ts 4.15ms
  88/1641 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.closed.test.ts 3.17ms
  89/1641 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.delete.test.ts 8.35ms
  90/1641 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.import.test.ts 2.92ms
  91/1641 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.knowledge.test.ts 6.92ms
  92/1641 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.ts 40.48ms
  93/1641 src/book-2.0/agent-source/createCommitmentRegex.ts 5.41ms
  94/1641 src/book-2.0/agent-source/createDefaultAgentName.ts 4.24ms
  95/1641 src/book-2.0/agent-source/createTeamToolName.ts 3.03ms
  96/1641 src/book-2.0/agent-source/extractMetaLinks.ts 3.02ms
  97/1641 src/book-2.0/agent-source/extractOpenTeacherInstructions.test.ts 3.03ms
  98/1641 src/book-2.0/agent-source/extractOpenTeacherInstructions.ts 3.12ms
  99/1641 src/book-2.0/agent-source/normalizeAgentName.test.ts 5.71ms
 100/1641 src/book-2.0/agent-source/normalizeAgentName.ts 2.57ms
 101/1641 src/book-2.0/agent-source/padBook.ts 3.38ms
 102/1641 src/book-2.0/agent-source/parseAgentSource.import.test.ts 4.93ms
 103/1641 src/book-2.0/agent-source/parseAgentSource.test.ts 13.85ms
 104/1641 src/book-2.0/agent-source/parseAgentSource.ts 26.59ms
 105/1641 src/book-2.0/agent-source/parseAgentSourcePrelude.ts 3.56ms
 106/1641 src/book-2.0/agent-source/parseAgentSourceWithCommitments.blocks.test.ts 4.70ms
 107/1641 src/book-2.0/agent-source/parseAgentSourceWithCommitments.title.test.ts 6.95ms
 108/1641 src/book-2.0/agent-source/parseAgentSourceWithCommitments.ts 14.61ms
 109/1641 src/book-2.0/agent-source/parseParameters.ts 6.43ms
 110/1641 src/book-2.0/agent-source/parseTeamCommitment.ts 11.50ms
 111/1641 src/book-2.0/agent-source/pseudoAgentReferences.ts 8.67ms
 112/1641 src/book-2.0/agent-source/README.md 4.09ms
 113/1641 src/book-2.0/agent-source/removeCommentsFromSystemMessage.ts 2.65ms
 114/1641 src/book-2.0/agent-source/string_book.ts 3.24ms
 115/1641 src/book-2.0/agent-source/TeammateProfileResolver.ts 3.16ms
 116/1641 src/book-2.0/README.md 2.00ms
 117/1641 src/book-2.0/utils/generatePlaceholderAgentProfileImageUrl.ts 2.91ms
 118/1641 src/book-components/_common/Dropdown/Dropdown.tsx 3.04ms
 119/1641 src/book-components/_common/HamburgerMenu/HamburgerMenu.tsx 3.13ms
 120/1641 src/book-components/_common/MenuHoisting/MenuHoistingContext.tsx 4.03ms
 121/1641 src/book-components/_common/Modal/Modal.tsx 2.92ms
 122/1641 src/book-components/_common/MonacoEditorWithShadowDom.tsx 2.34ms
 123/1641 src/book-components/_common/react-utils/classNames.ts 2.73ms
 124/1641 src/book-components/_common/react-utils/collectCssTextsForClass.tsx 4.12ms
 125/1641 src/book-components/_common/react-utils/escapeHtml.tsx 2.61ms
 126/1641 src/book-components/_common/react-utils/escapeRegex.tsx 2.20ms
 127/1641 src/book-components/_common/Tooltip/Tooltip.tsx 8.62ms
 128/1641 src/book-components/AvatarProfile/AvatarChip/AvatarChip.tsx 6.00ms
 129/1641 src/book-components/AvatarProfile/AvatarChip/AvatarChipFromSource.tsx 3.38ms
 130/1641 src/book-components/AvatarProfile/AvatarChip/index.ts 6.95ms
 131/1641 src/book-components/AvatarProfile/AvatarProfile/AvatarProfile.tsx 6.89ms
 132/1641 src/book-components/AvatarProfile/AvatarProfile/AvatarProfileFromSource.tsx 7.30ms
 133/1641 src/book-components/AvatarProfile/AvatarProfile/AvatarProfileTooltip.tsx 5.82ms
 134/1641 src/book-components/AvatarProfile/README.md 1.69ms
 135/1641 src/book-components/BookEditor/BookEditor.tsx 17.11ms
 136/1641 src/book-components/BookEditor/BookEditorActionbar.tsx 11.92ms
 137/1641 src/book-components/BookEditor/BookEditorMonaco.tsx 40.58ms
 138/1641 src/book-components/BookEditor/BookEditorMonacoConstants.ts 2.76ms
 139/1641 src/book-components/BookEditor/BookEditorMonacoFormatting.ts 8.37ms
 140/1641 src/book-components/BookEditor/BookEditorMonacoTokenization.test.ts 3.46ms
 141/1641 src/book-components/BookEditor/BookEditorMonacoTokenization.ts 27.67ms
 142/1641 src/book-components/BookEditor/BookEditorMonacoUploadPanel.tsx 9.49ms
 143/1641 src/book-components/BookEditor/createDeprecatedCommitmentDiagnostics.test.ts 5.00ms
 144/1641 src/book-components/BookEditor/createDeprecatedCommitmentDiagnostics.ts 6.74ms
 145/1641 src/book-components/BookEditor/useBookEditorMonacoDecorations.ts 9.76ms
 146/1641 src/book-components/BookEditor/useBookEditorMonacoDiagnostics.ts 10.47ms
 147/1641 src/book-components/BookEditor/useBookEditorMonacoLanguage.test.ts 13.34ms
 148/1641 src/book-components/BookEditor/useBookEditorMonacoLanguage.ts 22.90ms
 149/1641 src/book-components/BookEditor/useBookEditorMonacoStyles.ts 4.94ms
 150/1641 src/book-components/BookEditor/useBookEditorMonacoUploads.ts 31.68ms
 151/1641 src/book-components/Chat/AgentChat/AgentChat.test.tsx 10.48ms
 152/1641 src/book-components/Chat/AgentChat/AgentChat.tsx 8.62ms
 153/1641 src/book-components/Chat/AgentChat/AgentChatProps.tsx 4.04ms
 154/1641 src/book-components/Chat/AgentChip/AgentChip.tsx 7.86ms
 155/1641 src/book-components/Chat/AgentChip/index.ts 3.26ms
 156/1641 src/book-components/Chat/Chat/Chat.tsx 24.19ms
 157/1641 src/book-components/Chat/Chat/ChatActionsBar.test.tsx 6.41ms
 158/1641 src/book-components/Chat/Chat/ChatActionsBar.tsx 17.31ms
 159/1641 src/book-components/Chat/Chat/ChatCitationModal.tsx 16.32ms
 160/1641 src/book-components/Chat/Chat/chatCssClassNames.ts 8.94ms
 161/1641 src/book-components/Chat/Chat/ChatInputArea.test.tsx 9.78ms
 162/1641 src/book-components/Chat/Chat/ChatInputArea.tsx 24.96ms
 163/1641 src/book-components/Chat/Chat/ChatInputAreaDictationPanel.tsx 15.23ms
 164/1641 src/book-components/Chat/Chat/ChatInputUploadedFile.ts 3.05ms
 165/1641 src/book-components/Chat/Chat/ChatMessageItem.test.tsx 19.83ms
 166/1641 src/book-components/Chat/Chat/ChatMessageItem.tsx 67.07ms
 167/1641 src/book-components/Chat/Chat/ChatMessageList.tsx 11.50ms
 168/1641 src/book-components/Chat/Chat/ChatMessageMap.tsx 15.69ms
 169/1641 src/book-components/Chat/Chat/ChatMessageRichContent.tsx 4.62ms
 170/1641 src/book-components/Chat/Chat/ChatMessageToolCallChips.tsx 6.14ms
 171/1641 src/book-components/Chat/Chat/ChatProps.tsx 42.97ms
 172/1641 src/book-components/Chat/Chat/ChatRatingModal.tsx 13.13ms
 173/1641 src/book-components/Chat/Chat/ChatReplyPreview.tsx 6.52ms
 174/1641 src/book-components/Chat/Chat/ChatSelfLearningSummary.ts 8.79ms
 175/1641 src/book-components/Chat/Chat/ChatSoundToggle.tsx 14.09ms
 176/1641 src/book-components/Chat/Chat/ChatToolCallModal.test.tsx 17.33ms
 177/1641 src/book-components/Chat/Chat/ChatToolCallModal.tsx 21.82ms
 178/1641 src/book-components/Chat/Chat/ChatToolCallModalComponents.tsx 10.82ms
 179/1641 src/book-components/Chat/Chat/ChatToolCallModalContent.tsx 9.67ms
 180/1641 src/book-components/Chat/Chat/ClockIcon.tsx 3.23ms
 181/1641 src/book-components/Chat/Chat/constants.tsx 2.40ms
 182/1641 src/book-components/Chat/Chat/createChatMessageToolCallRenderModel.ts 12.33ms
 183/1641 src/book-components/Chat/Chat/createProgressCardChecklistMarkdown.ts 8.53ms
 184/1641 src/book-components/Chat/Chat/ImagePromptRenderer.tsx 8.10ms
 185/1641 src/book-components/Chat/Chat/insertDictationChunk.ts 4.37ms
 186/1641 src/book-components/Chat/Chat/learnDictationDictionary.ts 3.51ms
 187/1641 src/book-components/Chat/Chat/refineFinalDictationChunk.ts 9.57ms
 188/1641 src/book-components/Chat/Chat/renderAdvancedToolCallDetails.tsx 30.54ms
 189/1641 src/book-components/Chat/Chat/renderEmailToolCallDetails.tsx 14.39ms
 190/1641 src/book-components/Chat/Chat/renderMemoryToolCallDetails.tsx 16.37ms
 191/1641 src/book-components/Chat/Chat/renderPopupToolCallDetails.tsx 8.26ms
 192/1641 src/book-components/Chat/Chat/renderRunBrowserToolCallDetails.tsx 34.47ms
 193/1641 src/book-components/Chat/Chat/renderSearchToolCallDetails.tsx 11.91ms
 194/1641 src/book-components/Chat/Chat/renderSelfLearningToolCallDetails.tsx 10.15ms
 195/1641 src/book-components/Chat/Chat/renderTimeoutToolCallDetails.tsx 20.62ms
 196/1641 src/book-components/Chat/Chat/renderTimeToolCallDetails.tsx 6.57ms
 197/1641 src/book-components/Chat/Chat/renderToolCallClockPanel.tsx 5.36ms
 198/1641 src/book-components/Chat/Chat/renderToolCallDetails.tsx 34.88ms
 199/1641 src/book-components/Chat/Chat/renderToolCallProgressPlaceholder.tsx 6.30ms
 200/1641 src/book-components/Chat/Chat/renderWalletCredentialToolCallDetails.tsx 7.28ms
 201/1641 src/book-components/Chat/Chat/resolveRunBrowserToolCallDetailsState.ts 11.58ms
 202/1641 src/book-components/Chat/Chat/resolveSpeechRecognitionUiDescriptor.ts 4.50ms
 203/1641 src/book-components/Chat/Chat/resolveToolCallProgressMessage.ts 3.03ms
 204/1641 src/book-components/Chat/Chat/StreamingFeaturePlaceholder.tsx 8.39ms
 205/1641 src/book-components/Chat/Chat/TeamToolCallModalContent.tsx 17.81ms
 206/1641 src/book-components/Chat/Chat/useChatInputAreaAttachments.ts 15.40ms
 207/1641 src/book-components/Chat/Chat/useChatInputAreaComposer.ts 19.99ms
 208/1641 src/book-components/Chat/Chat/useChatInputAreaDictation.ts 21.76ms
 209/1641 src/book-components/Chat/Chat/useChatInputAreaDictationPersistence.ts 5.76ms
 210/1641 src/book-components/Chat/Chat/useChatInputAreaDictationSupport.ts 6.80ms
 211/1641 src/book-components/Chat/Chat/useChatMessageAvatarTooltip.ts 7.00ms
 212/1641 src/book-components/Chat/Chat/useChatMessageSpeechPlayback.ts 17.07ms
 213/1641 src/book-components/Chat/Chat/useChatPostprocessedMessages.ts 13.88ms
 214/1641 src/book-components/Chat/Chat/useChatScrollState.ts 31.21ms
 215/1641 src/book-components/Chat/Chat/useChatToolCallModalState.ts 25.33ms
 216/1641 src/book-components/Chat/Chat/useChatToolCallState.ts 7.28ms
 217/1641 src/book-components/Chat/CodeBlock/CodeBlock.tsx 12.16ms
 218/1641 src/book-components/Chat/CodeBlock/resolveCodeBlockLanguage.test.ts 3.50ms
 219/1641 src/book-components/Chat/CodeBlock/resolveCodeBlockLanguage.ts 3.22ms
 220/1641 src/book-components/Chat/effects/ChatEffectsSystem.tsx 9.52ms
 221/1641 src/book-components/Chat/effects/components/ConfettiEffect.tsx 8.04ms
 222/1641 src/book-components/Chat/effects/components/HeartsEffect.tsx 4.30ms
 223/1641 src/book-components/Chat/effects/configs/defaultEffectConfigs.ts 9.65ms
 224/1641 src/book-components/Chat/effects/index.ts 3.59ms
 225/1641 src/book-components/Chat/effects/types/ChatEffect.ts 2.27ms
 226/1641 src/book-components/Chat/effects/types/ChatEffectConfig.ts 3.04ms
 227/1641 src/book-components/Chat/effects/types/ChatEffectsSystemProps.ts 4.81ms
 228/1641 src/book-components/Chat/effects/types/ChatEffectType.ts 2.21ms
 229/1641 src/book-components/Chat/effects/utils/detectEffects.ts 4.33ms
 230/1641 src/book-components/Chat/hooks/index.ts 4.90ms
 231/1641 src/book-components/Chat/hooks/useChatActionsOverlap.ts 7.92ms
 232/1641 src/book-components/Chat/hooks/useChatAutoScroll.test.tsx 11.49ms
 233/1641 src/book-components/Chat/hooks/useChatAutoScroll.ts 19.31ms
 234/1641 src/book-components/Chat/hooks/useChatCompleteNotification.test.tsx 9.53ms
 235/1641 src/book-components/Chat/hooks/useChatCompleteNotification.ts 4.29ms
 236/1641 src/book-components/Chat/hooks/useChatRatings.ts 8.93ms
 237/1641 src/book-components/Chat/hooks/useSendMessageToLlmChat.ts 7.33ms
 238/1641 src/book-components/Chat/LlmChat/defaults.ts 2.02ms
 239/1641 src/book-components/Chat/LlmChat/FriendlyErrorMessage.ts 2.72ms
 240/1641 src/book-components/Chat/LlmChat/LlmChat.test.tsx 14.85ms
 241/1641 src/book-components/Chat/LlmChat/LlmChat.tsx 6.58ms
 242/1641 src/book-components/Chat/LlmChat/LlmChatProps.tsx 8.08ms
 243/1641 src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts 25.04ms
 244/1641 src/book-components/Chat/LlmChat/useLlmChatMessages.ts 10.98ms
 245/1641 src/book-components/Chat/LlmChat/useLlmChatState.ts 21.69ms
 246/1641 src/book-components/Chat/MarkdownContent/MarkdownContent.test.tsx 8.40ms
 247/1641 src/book-components/Chat/MarkdownContent/MarkdownContent.tsx 26.62ms
 248/1641 src/book-components/Chat/MockedChat/constants.ts 3.35ms
 249/1641 src/book-components/Chat/MockedChat/MockedChat.tsx 20.40ms
 250/1641 src/book-components/Chat/README.md 11.94ms
 251/1641 src/book-components/Chat/save/_common/ChatSaveFormatDefinition.ts 6.09ms
 252/1641 src/book-components/Chat/save/_common/getChatSaveFormatDefinitions.ts 3.13ms
 253/1641 src/book-components/Chat/save/_common/string_chat_format_name.ts 2.54ms
 254/1641 src/book-components/Chat/save/html/htmlSaveFormatDefinition.ts 23.77ms
 255/1641 src/book-components/Chat/save/index.ts 4.82ms
 256/1641 src/book-components/Chat/save/json/jsonSaveFormatDefinition.ts 9.20ms
 257/1641 src/book-components/Chat/save/markdown/mdSaveFormatDefinition.ts 3.38ms
 258/1641 src/book-components/Chat/save/pdf/buildChatPdf.ts 12.19ms
 259/1641 src/book-components/Chat/save/pdf/pdfSaveFormatDefinition.ts 2.60ms
 260/1641 src/book-components/Chat/save/react/exports/chat-preview-2025-10-13 (1).jsx 5.73ms
 261/1641 src/book-components/Chat/save/react/reactSaveFormatDefinition.ts 7.11ms
 262/1641 src/book-components/Chat/save/text/txtSaveFormatDefinition.ts 2.74ms
 263/1641 src/book-components/Chat/SourceChip/index.ts 1.67ms
 264/1641 src/book-components/Chat/SourceChip/SourceChip.tsx 9.34ms
 265/1641 src/book-components/Chat/types/ChatMessage.ts 15.86ms
 266/1641 src/book-components/Chat/types/ChatParticipant.ts 5.89ms
 267/1641 src/book-components/Chat/utils/addUtmParamsToUrl.ts 2.36ms
 268/1641 src/book-components/Chat/utils/ChatPersistence.test.ts 5.87ms
 269/1641 src/book-components/Chat/utils/ChatPersistence.ts 7.65ms
 270/1641 src/book-components/Chat/utils/citationHelpers.test.ts 8.93ms
 271/1641 src/book-components/Chat/utils/citationHelpers.ts 8.53ms
 272/1641 src/book-components/Chat/utils/collectTeamToolCallSummary.ts 13.15ms
 273/1641 src/book-components/Chat/utils/createCitationFootnoteRenderModel.test.ts 3.16ms
 274/1641 src/book-components/Chat/utils/createCitationFootnoteRenderModel.ts 17.36ms
 275/1641 src/book-components/Chat/utils/createShortLinkForChat.ts 2.74ms
 276/1641 src/book-components/Chat/utils/createTeamToolNameFromUrl.ts 4.12ms
 277/1641 src/book-components/Chat/utils/downloadFile.ts 3.28ms
 278/1641 src/book-components/Chat/utils/exportChatHistory.ts 8.14ms
 279/1641 src/book-components/Chat/utils/ExportFormat.ts 1.72ms
 280/1641 src/book-components/Chat/utils/formatToolCallDateTime.ts 13.80ms
 281/1641 src/book-components/Chat/utils/formatToolCallLocalTime.ts 7.31ms
 282/1641 src/book-components/Chat/utils/formatToolCallTranslationTemplate.ts 5.07ms
 283/1641 src/book-components/Chat/utils/generatePdfContent.ts 4.35ms
 284/1641 src/book-components/Chat/utils/generateQrDataUrl.ts 1.73ms
 285/1641 src/book-components/Chat/utils/getChatMessageTimingDisplay.ts 4.04ms
 286/1641 src/book-components/Chat/utils/getPromptbookBranding.ts 3.24ms
 287/1641 src/book-components/Chat/utils/getToolCallChipletInfo.timeout.test.ts 3.80ms
 288/1641 src/book-components/Chat/utils/getToolCallChipletInfo.ts 19.80ms
 289/1641 src/book-components/Chat/utils/loadAgentProfile.ts 7.16ms
 290/1641 src/book-components/Chat/utils/messagesToHtml.ts 8.55ms
 291/1641 src/book-components/Chat/utils/messagesToJson.ts 4.18ms
 292/1641 src/book-components/Chat/utils/messagesToMarkdown.ts 5.65ms
 293/1641 src/book-components/Chat/utils/messagesToText.ts 3.97ms
 294/1641 src/book-components/Chat/utils/parseCitationMarker.ts 8.15ms
 295/1641 src/book-components/Chat/utils/parseCitationsFromContent.test.ts 4.72ms
 296/1641 src/book-components/Chat/utils/parseCitationsFromContent.ts 6.13ms
 297/1641 src/book-components/Chat/utils/parseImagePrompts.test.ts 2.72ms
 298/1641 src/book-components/Chat/utils/parseImagePrompts.ts 6.37ms
 299/1641 src/book-components/Chat/utils/parseMessageButtons.test.ts 4.20ms
 300/1641 src/book-components/Chat/utils/parseMessageButtons.ts 5.50ms
 301/1641 src/book-components/Chat/utils/resolveChatMessageReplyPreviewText.ts 11.23ms
 302/1641 src/book-components/Chat/utils/resolveChatMessageReplySenderLabel.ts 4.43ms
 303/1641 src/book-components/Chat/utils/resolveCitationUrl.test.ts 7.99ms
 304/1641 src/book-components/Chat/utils/resolveCitationUrl.ts 4.62ms
 305/1641 src/book-components/Chat/utils/resolveToolCallFromChatMessages.ts 9.47ms
 306/1641 src/book-components/Chat/utils/resolveToolCallState.ts 2.90ms
 307/1641 src/book-components/Chat/utils/sanitizeStreamingMessageContent.test.ts 11.01ms
 308/1641 src/book-components/Chat/utils/sanitizeStreamingMessageContent.ts 5.61ms
 309/1641 src/book-components/Chat/utils/splitMessageContentIntoSegments.test.ts 5.39ms
 310/1641 src/book-components/Chat/utils/splitMessageContentIntoSegments.ts 19.47ms
 311/1641 src/book-components/Chat/utils/thinkingMessageVariants.test.ts 3.02ms
 312/1641 src/book-components/Chat/utils/thinkingMessageVariants.ts 4.07ms
 313/1641 src/book-components/Chat/utils/timeoutToolCallPresentation.test.ts 4.91ms
 314/1641 src/book-components/Chat/utils/timeoutToolCallPresentation.ts 16.02ms
 315/1641 src/book-components/Chat/utils/toolCallParsing.test.ts 13.06ms
 316/1641 src/book-components/Chat/utils/toolCallParsing.ts 3.00ms
 317/1641 src/book-components/Chat/utils/toolCallParsing/extractSearchResults.ts 7.97ms
 318/1641 src/book-components/Chat/utils/toolCallParsing/getToolCallResultDate.ts 35.76ms
 319/1641 src/book-components/Chat/utils/toolCallParsing/getToolCallTimestamp.ts 2.76ms
 320/1641 src/book-components/Chat/utils/toolCallParsing/parseRunBrowserToolResult.ts 13.07ms
 321/1641 src/book-components/Chat/utils/toolCallParsing/parseTeamToolResult.ts 7.17ms
 322/1641 src/book-components/Chat/utils/toolCallParsing/parseToolCallArguments.ts 3.28ms
 323/1641 src/book-components/Chat/utils/toolCallParsing/parseToolCallResult.ts 11.41ms
 324/1641 src/book-components/Chat/utils/toolCallParsing/resolveRunBrowserArtifactUrl.ts 8.53ms
 325/1641 src/book-components/Chat/utils/toolCallParsing/RunBrowserToolResult.ts 6.24ms
 326/1641 src/book-components/Chat/utils/toolCallParsing/TeamToolResult.ts 3.60ms
 327/1641 src/book-components/Chat/utils/walletCredentialToolCall.test.ts 15.81ms
 328/1641 src/book-components/Chat/utils/walletCredentialToolCall.ts 21.53ms
 329/1641 src/book-components/fonts/README.md 1.79ms
 330/1641 src/book-components/icons/AboutIcon.tsx 3.30ms
 331/1641 src/book-components/icons/ArrowIcon.tsx 5.61ms
 332/1641 src/book-components/icons/AttachmentIcon.tsx 3.81ms
 333/1641 src/book-components/icons/CameraIcon.tsx 5.76ms
 334/1641 src/book-components/icons/CloseIcon.tsx 2.79ms
 335/1641 src/book-components/icons/DownloadIcon.tsx 5.40ms
 336/1641 src/book-components/icons/EmailIcon.tsx 7.36ms
 337/1641 src/book-components/icons/ExitFullscreenIcon.tsx 4.07ms
 338/1641 src/book-components/icons/FullscreenIcon.tsx 5.41ms
 339/1641 src/book-components/icons/MenuIcon.tsx 2.85ms
 340/1641 src/book-components/icons/MicIcon.tsx 3.63ms
 341/1641 src/book-components/icons/PauseIcon.tsx 2.10ms
 342/1641 src/book-components/icons/PlayIcon.tsx 2.03ms
 343/1641 src/book-components/icons/ResetIcon.tsx 2.17ms
 344/1641 src/book-components/icons/SaveIcon.tsx 3.29ms
 345/1641 src/book-components/icons/SendIcon.tsx 2.43ms
 346/1641 src/book-components/icons/SolidArrowButton.tsx 7.49ms
 347/1641 src/book-components/icons/StopIcon.tsx 2.80ms
 348/1641 src/book-components/icons/TeacherIcon.tsx 3.29ms
 349/1641 src/book-components/icons/TemplateIcon.tsx 2.81ms
 350/1641 src/book-components/PromptbookAgent/PromptbookAgentIntegration.tsx 4.93ms
 351/1641 src/book-components/PromptbookAgent/PromptbookAgentSeamlessIntegration.tsx 24.71ms
 352/1641 src/book-components/Qr/BrandedQrCode.tsx 6.75ms
 353/1641 src/book-components/Qr/GenericQrCode.tsx 2.25ms
 354/1641 src/book-components/Qr/PromptbookQrCode.tsx 18.11ms
 355/1641 src/book-components/Qr/useQrCode.ts 4.44ms
 356/1641 src/cli/$runPromptbookCli.ts 2.50ms
 357/1641 src/cli/cli-commands/_boilerplate.ts 3.08ms
 358/1641 src/cli/cli-commands/about.ts 14.08ms
 359/1641 src/cli/cli-commands/coder.ts 6.25ms
 360/1641 src/cli/cli-commands/coder/agentCodingFile.ts 10.38ms
 361/1641 src/cli/cli-commands/coder/agentsFile.ts 4.78ms
 362/1641 src/cli/cli-commands/coder/appendBlock.ts 3.31ms
 363/1641 src/cli/cli-commands/coder/boilerplateTemplates.test.ts 13.41ms
 364/1641 src/cli/cli-commands/coder/boilerplateTemplates.ts 13.51ms
 365/1641 src/cli/cli-commands/coder/ensureCoderEnvFile.ts 7.17ms
 366/1641 src/cli/cli-commands/coder/ensureCoderGitignoreFile.ts 5.27ms
 367/1641 src/cli/cli-commands/coder/ensureCoderMarkdownFile.ts 3.22ms
 368/1641 src/cli/cli-commands/coder/ensureCoderPackageJsonFile.ts 5.02ms
 369/1641 src/cli/cli-commands/coder/ensureCoderVscodeSettingsFile.ts 8.59ms
 370/1641 src/cli/cli-commands/coder/ensureDirectory.ts 4.10ms
 371/1641 src/cli/cli-commands/coder/find-fresh-emoji-tags.ts 3.45ms
 372/1641 src/cli/cli-commands/coder/find-refactor-candidates.test.ts 6.72ms
 373/1641 src/cli/cli-commands/coder/find-refactor-candidates.ts 38.01ms
 374/1641 src/cli/cli-commands/coder/formatDisplayPath.ts 4.72ms
 375/1641 src/cli/cli-commands/coder/generate-boilerplates.ts 9.87ms
 376/1641 src/cli/cli-commands/coder/getDefaultCoderPackageJsonScripts.ts 2.69ms
 377/1641 src/cli/cli-commands/coder/getDefaultCoderVscodeSettings.ts 12.82ms
 378/1641 src/cli/cli-commands/coder/getTypescriptModule.test.ts 3.63ms
 379/1641 src/cli/cli-commands/coder/getTypescriptModule.ts 3.19ms
 380/1641 src/cli/cli-commands/coder/init.ts 6.89ms
 381/1641 src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts 8.81ms
 382/1641 src/cli/cli-commands/coder/mergeStringRecordJsonFile.test.ts 5.06ms
 383/1641 src/cli/cli-commands/coder/mergeStringRecordJsonFile.ts 13.50ms
 384/1641 src/cli/cli-commands/coder/printInitializationSummary.ts 6.97ms
 385/1641 src/cli/cli-commands/coder/readTextFileIfExists.ts 2.43ms
 386/1641 src/cli/cli-commands/coder/run.test.ts 7.74ms
 387/1641 src/cli/cli-commands/coder/run.ts 13.12ms
 388/1641 src/cli/cli-commands/coder/ThinkingLevel.ts 3.29ms
 389/1641 src/cli/cli-commands/coder/verify.test.ts 4.12ms
 390/1641 src/cli/cli-commands/coder/verify.ts 5.31ms
 391/1641 src/cli/cli-commands/common/handleActionErrors.ts 2.96ms
 392/1641 src/cli/cli-commands/hello.ts 3.50ms
 393/1641 src/cli/cli-commands/list-models.ts 5.02ms
 394/1641 src/cli/cli-commands/list-scrapers.ts 5.26ms
 395/1641 src/cli/cli-commands/login.ts 3.94ms
 396/1641 src/cli/cli-commands/make.ts 22.26ms
 397/1641 src/cli/cli-commands/prettify.ts 19.39ms
 398/1641 src/cli/cli-commands/run.ts 27.37ms
 399/1641 src/cli/cli-commands/runInteractiveChatbot.ts 7.82ms
 400/1641 src/cli/cli-commands/start-agents-server.ts 7.67ms
 401/1641 src/cli/cli-commands/start-pipelines-server.ts 10.14ms
 402/1641 src/cli/cli-commands/test-command.ts 10.16ms
 403/1641 src/cli/common/$addGlobalOptionsToCommand.ts 2.79ms
 404/1641 src/cli/common/$provideLlmToolsForCli.ts 13.03ms
 405/1641 src/cli/main.ts 2.09ms
 406/1641 src/cli/promptbookCli.ts 9.36ms
 407/1641 src/cli/test/ptbk.test.ts 5.24ms
 408/1641 src/cli/test/ptbk.ts 3.40ms
 409/1641 src/cli/test/tsconfig.json 2.86ms
 410/1641 src/collection/agent-collection/AgentCollection.ts 5.97ms
 411/1641 src/collection/agent-collection/constructors/agent-collection-in-supabase/AgentCollectionInSupabase.ts 18.56ms
 412/1641 src/collection/agent-collection/constructors/agent-collection-in-supabase/AgentCollectionInSupabaseOptions.ts 2.91ms
 413/1641 src/collection/agent-collection/constructors/agent-collection-in-supabase/AgentsDatabaseSchema.ts 13.74ms
 414/1641 src/collection/agent-collection/constructors/agent-collection-in-supabase/createAgentPersistenceRecords.test.ts 5.87ms
 415/1641 src/collection/agent-collection/constructors/agent-collection-in-supabase/createAgentPersistenceRecords.ts 6.04ms
 416/1641 src/collection/agent-collection/constructors/agent-collection-in-supabase/prepareAgentSourceForPersistence.ts 4.19ms
 417/1641 src/collection/agent-collection/CreateAgentInput.test.ts 3.64ms
 418/1641 src/collection/agent-collection/CreateAgentInput.ts 10.62ms
 419/1641 src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts 7.27ms
 420/1641 src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts 22.06ms
 421/1641 src/collection/pipeline-collection/constructors/createPipelineCollectionFromJson.test.ts 3.11ms
 422/1641 src/collection/pipeline-collection/constructors/createPipelineCollectionFromJson.ts 2.55ms
 423/1641 src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.test.ts 5.31ms
 424/1641 src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts 4.60ms
 425/1641 src/collection/pipeline-collection/constructors/createPipelineCollectionFromUrl.ts 6.83ms
 426/1641 src/collection/pipeline-collection/constructors/createPipelineSubcollection.ts 4.92ms
 427/1641 src/collection/pipeline-collection/PipelineCollection.ts 5.45ms
 428/1641 src/collection/pipeline-collection/pipelineCollectionToJson.test.ts 3.79ms
 429/1641 src/collection/pipeline-collection/pipelineCollectionToJson.ts 2.58ms
 430/1641 src/collection/pipeline-collection/SimplePipelineCollection.ts 5.95ms
 431/1641 src/commands/_BOILERPLATE/boilerplateCommand.test.ts 3.98ms
 432/1641 src/commands/_BOILERPLATE/BoilerplateCommand.ts 1.76ms
 433/1641 src/commands/_BOILERPLATE/boilerplateCommandParser.ts 6.48ms
 434/1641 src/commands/_common/getParserForCommand.test.ts 2.64ms
 435/1641 src/commands/_common/getParserForCommand.ts 3.03ms
 436/1641 src/commands/_common/parseCommand.ts 11.66ms
 437/1641 src/commands/_common/stringifyCommand.test.ts 4.67ms
 438/1641 src/commands/_common/stringifyCommand.ts 2.93ms
 439/1641 src/commands/_common/types/Command.ts 6.72ms
 440/1641 src/commands/_common/types/CommandParser.ts 17.01ms
 441/1641 src/commands/_common/types/CommandType.ts 2.67ms
 442/1641 src/commands/_common/types/CommandUsagePlaces.ts 4.97ms
 443/1641 src/commands/BOOK_VERSION/bookVersionCommand.test.ts 3.22ms
 444/1641 src/commands/BOOK_VERSION/BookVersionCommand.ts 2.72ms
 445/1641 src/commands/BOOK_VERSION/bookVersionCommandParser.ts 6.31ms
 446/1641 src/commands/EXPECT/expectCommand.test.ts 4.21ms
 447/1641 src/commands/EXPECT/ExpectCommand.ts 7.22ms
 448/1641 src/commands/EXPECT/expectCommandParser.ts 9.26ms
 449/1641 src/commands/FOREACH/foreachCommand.test.ts 7.28ms
 450/1641 src/commands/FOREACH/ForeachCommand.ts 4.53ms
 451/1641 src/commands/FOREACH/foreachCommandParser.ts 20.55ms
 452/1641 src/commands/FOREACH/ForeachJson.ts 2.89ms
 453/1641 src/commands/FORMAT/formatCommand.test.ts 2.97ms
 454/1641 src/commands/FORMAT/FormatCommand.ts 3.85ms
 455/1641 src/commands/FORMAT/formatCommandParser.ts 8.18ms
 456/1641 src/commands/FORMFACTOR/formfactorCommand.test.ts 4.03ms
 457/1641 src/commands/FORMFACTOR/FormfactorCommand.ts 3.45ms
 458/1641 src/commands/FORMFACTOR/formfactorCommandParser.ts 6.05ms
 459/1641 src/commands/index.ts 5.08ms
 460/1641 src/commands/JOKER/jokerCommand.test.ts 3.08ms
 461/1641 src/commands/JOKER/JokerCommand.ts 2.62ms
 462/1641 src/commands/JOKER/jokerCommandParser.ts 6.69ms
 463/1641 src/commands/KNOWLEDGE/knowledgeCommand.test.ts 2.86ms
 464/1641 src/commands/KNOWLEDGE/KnowledgeCommand.ts 3.84ms
 465/1641 src/commands/KNOWLEDGE/knowledgeCommandParser.ts 5.84ms
 466/1641 src/commands/KNOWLEDGE/utils/knowledgeSourceContentToName.test.ts 2.40ms
 467/1641 src/commands/KNOWLEDGE/utils/knowledgeSourceContentToName.ts 3.19ms
 468/1641 src/commands/MODEL/modelCommand.test.ts 3.12ms
 469/1641 src/commands/MODEL/ModelCommand.ts 1.91ms
 470/1641 src/commands/MODEL/modelCommandParser.ts 9.51ms
 471/1641 src/commands/PARAMETER/parameterCommand.test.ts 6.41ms
 472/1641 src/commands/PARAMETER/ParameterCommand.ts 1.96ms
 473/1641 src/commands/PARAMETER/parameterCommandParser.ts 8.81ms
 474/1641 src/commands/PERSONA/personaCommand.test.ts 7.63ms
 475/1641 src/commands/PERSONA/PersonaCommand.ts 3.44ms
 476/1641 src/commands/PERSONA/personaCommandParser.ts 10.59ms
 477/1641 src/commands/POSTPROCESS/postprocessCommand.test.ts 3.29ms
 478/1641 src/commands/POSTPROCESS/PostprocessCommand.ts 3.11ms
 479/1641 src/commands/POSTPROCESS/postprocessCommandParser.ts 6.99ms
 480/1641 src/commands/README.md 1.82ms
 481/1641 src/commands/SECTION/sectionCommand.test.ts 3.76ms
 482/1641 src/commands/SECTION/SectionCommand.ts 2.66ms
 483/1641 src/commands/SECTION/sectionCommandParser.ts 13.75ms
 484/1641 src/commands/URL/urlCommand.test.ts 5.80ms
 485/1641 src/commands/URL/UrlCommand.ts 1.76ms
 486/1641 src/commands/URL/urlCommandParser.ts 8.43ms
 487/1641 src/commands/X_ACTION/actionCommand.test.ts 2.15ms
 488/1641 src/commands/X_ACTION/ActionCommand.ts 1.63ms
 489/1641 src/commands/X_ACTION/actionCommandParser.ts 7.21ms
 490/1641 src/commands/X_INSTRUMENT/instrumentCommand.test.ts 2.32ms
 491/1641 src/commands/X_INSTRUMENT/InstrumentCommand.ts 2.81ms
 492/1641 src/commands/X_INSTRUMENT/instrumentCommandParser.ts 4.99ms
 493/1641 src/commitments/_base/BaseCommitmentDefinition.ts 8.59ms
 494/1641 src/commitments/_base/BookCommitment.ts 1.67ms
 495/1641 src/commitments/_base/CommitmentDefinition.ts 45.11ms
 496/1641 src/commitments/_base/createEmptyAgentModelRequirements.ts 3.56ms
 497/1641 src/commitments/_base/formatOptionalInstructionBlock.ts 1.86ms
 498/1641 src/commitments/_base/NotYetImplementedCommitmentDefinition.ts 3.78ms
 499/1641 src/commitments/_base/ParsedCommitment.ts 2.14ms
 500/1641 src/commitments/_common/commitmentToolFunctions.ts 4.13ms
 501/1641 src/commitments/_common/createWritingCommitmentSections.ts 39.87ms
 502/1641 src/commitments/_common/getAllCommitmentDefinitions.ts 2.26ms
 503/1641 src/commitments/_common/getAllCommitmentsToolFunctionsForBrowser.ts 4.33ms
 504/1641 src/commitments/_common/getAllCommitmentsToolFunctionsForNode.ts 5.97ms
 505/1641 src/commitments/_common/getAllCommitmentsToolTitles.ts 3.59ms
 506/1641 src/commitments/_common/getAllCommitmentTypes.ts 2.34ms
 507/1641 src/commitments/_common/getCommitmentDefinition.ts 2.61ms
 508/1641 src/commitments/_common/getGroupedCommitmentDefinitions.ts 3.27ms
 509/1641 src/commitments/_common/getGroupedCommitmentDefinitions.writing.test.ts 2.98ms
 510/1641 src/commitments/_common/isCommitmentSupported.ts 2.04ms
 511/1641 src/commitments/_common/toolExecutionEnvelope.ts 3.88ms
 512/1641 src/commitments/_common/toolRuntimeContext.ts 11.18ms
 513/1641 src/commitments/ACTION/ACTION.ts 23.63ms
 514/1641 src/commitments/CLOSED/CLOSED.test.ts 2.65ms
 515/1641 src/commitments/CLOSED/CLOSED.ts 3.77ms
 516/1641 src/commitments/COMPONENT/COMPONENT.ts 3.35ms
 517/1641 src/commitments/DELETE/DELETE.ts 5.81ms
 518/1641 src/commitments/DICTIONARY/DICTIONARY.ts 7.95ms
 519/1641 src/commitments/FORMAT/FORMAT.ts 8.40ms
 520/1641 src/commitments/FROM/FROM.ts 4.32ms
 521/1641 src/commitments/GOAL/GOAL.ts 5.07ms
 522/1641 src/commitments/IMPORT/IMPORT.ts 4.04ms
 523/1641 src/commitments/index.ts 12.72ms
 524/1641 src/commitments/KNOWLEDGE/KNOWLEDGE.ts 7.58ms
 525/1641 src/commitments/LANGUAGE/LANGUAGE.ts 3.78ms
 526/1641 src/commitments/MEMORY/createMemorySystemMessage.ts 3.35ms
 527/1641 src/commitments/MEMORY/createMemoryToolFunctions.ts 23.88ms
 528/1641 src/commitments/MEMORY/createMemoryTools.ts 4.63ms
 529/1641 src/commitments/MEMORY/getMemoryCommitmentDocumentation.ts 10.75ms
 530/1641 src/commitments/MEMORY/getMemoryToolRuntimeAdapterOrDisabledResult.ts 4.52ms
 531/1641 src/commitments/MEMORY/getMemoryToolTitles.ts 2.33ms
 532/1641 src/commitments/MEMORY/MEMORY.test.ts 8.31ms
 533/1641 src/commitments/MEMORY/MEMORY.ts 6.09ms
 534/1641 src/commitments/MEMORY/MemoryToolNames.ts 2.10ms
 535/1641 src/commitments/MEMORY/MemoryToolRuntimeAdapter.ts 7.54ms
 536/1641 src/commitments/MEMORY/parseMemoryToolArgs.ts 4.24ms
 537/1641 src/commitments/MEMORY/resolveMemoryRuntimeContext.ts 3.01ms
 538/1641 src/commitments/MEMORY/setMemoryToolRuntimeAdapter.ts 3.60ms
 539/1641 src/commitments/MESSAGE_SUFFIX/MESSAGE_SUFFIX.test.ts 3.12ms
 540/1641 src/commitments/MESSAGE_SUFFIX/MESSAGE_SUFFIX.ts 3.74ms
 541/1641 src/commitments/MESSAGE/AgentMessageCommitmentDefinition.ts 4.47ms
 542/1641 src/commitments/MESSAGE/InitialMessageCommitmentDefinition.ts 4.50ms
 543/1641 src/commitments/MESSAGE/InternalMessageCommitmentDefinition.ts 4.20ms
 544/1641 src/commitments/MESSAGE/MESSAGE.ts 10.51ms
 545/1641 src/commitments/MESSAGE/UserMessageCommitmentDefinition.ts 5.30ms
 546/1641 src/commitments/META_COLOR/META_COLOR.ts 5.17ms
 547/1641 src/commitments/META_DISCLAIMER/META_DISCLAIMER.test.ts 2.58ms
 548/1641 src/commitments/META_DISCLAIMER/META_DISCLAIMER.ts 3.46ms
 549/1641 src/commitments/META_DOMAIN/META_DOMAIN.ts 4.00ms
 550/1641 src/commitments/META_FONT/META_FONT.ts 3.80ms
 551/1641 src/commitments/META_IMAGE/META_IMAGE.ts 7.42ms
 552/1641 src/commitments/META_INPUT_PLACEHOLDER/META_INPUT_PLACEHOLDER.ts 7.77ms
 553/1641 src/commitments/META_LINK/META_LINK.ts 5.51ms
 554/1641 src/commitments/META_VOICE/META_VOICE.ts 7.31ms
 555/1641 src/commitments/META/META_DESCRIPTION.ts 4.11ms
 556/1641 src/commitments/META/META.ts 9.14ms
 557/1641 src/commitments/MODEL/MODEL.ts 11.18ms
 558/1641 src/commitments/NOTE/NOTE.ts 4.45ms
 559/1641 src/commitments/OPEN/OPEN.ts 5.08ms
 560/1641 src/commitments/PERSONA/persona-verification-example.md 4.17ms
 561/1641 src/commitments/PERSONA/PERSONA.ts 7.51ms
 562/1641 src/commitments/README.md 3.11ms
 563/1641 src/commitments/RULE/RULE.ts 4.93ms
 564/1641 src/commitments/SAMPLE/SAMPLE.ts 5.32ms
 565/1641 src/commitments/SCENARIO/SCENARIO.ts 7.70ms
 566/1641 src/commitments/STYLE/STYLE.ts 5.20ms
 567/1641 src/commitments/TEAM/TEAM.ts 21.17ms
 568/1641 src/commitments/TEMPLATE/README.md 5.52ms
 569/1641 src/commitments/TEMPLATE/TEMPLATE.test.ts 4.82ms
 570/1641 src/commitments/TEMPLATE/TEMPLATE.ts 5.59ms
 571/1641 src/commitments/USE_BROWSER/fetchUrlContent.ts 6.52ms
 572/1641 src/commitments/USE_BROWSER/fetchUrlContentViaBrowser.ts 2.80ms
 573/1641 src/commitments/USE_BROWSER/resolveRunBrowserToolForNode.test.ts 2.17ms
 574/1641 src/commitments/USE_BROWSER/resolveRunBrowserToolForNode.ts 4.19ms
 575/1641 src/commitments/USE_BROWSER/USE_BROWSER.test.ts 6.35ms
 576/1641 src/commitments/USE_BROWSER/USE_BROWSER.ts 13.80ms
 577/1641 src/commitments/USE_CALENDAR/calendarReference.ts 10.91ms
 578/1641 src/commitments/USE_CALENDAR/callGoogleCalendarApi.ts 6.35ms
 579/1641 src/commitments/USE_CALENDAR/createUseCalendarToolFunctions.ts 15.12ms
 580/1641 src/commitments/USE_CALENDAR/createUseCalendarTools.ts 10.23ms
 581/1641 src/commitments/USE_CALENDAR/getUseCalendarToolTitles.ts 2.79ms
 582/1641 src/commitments/USE_CALENDAR/normalizeConfiguredCalendars.ts 7.32ms
 583/1641 src/commitments/USE_CALENDAR/resolveUseCalendarToolRuntimeOrWalletCredentialResult.ts 9.77ms
 584/1641 src/commitments/USE_CALENDAR/USE_CALENDAR.test.ts 5.98ms
 585/1641 src/commitments/USE_CALENDAR/USE_CALENDAR.ts 11.19ms
 586/1641 src/commitments/USE_CALENDAR/UseCalendarToolNames.ts 2.28ms
 587/1641 src/commitments/USE_CALENDAR/UseCalendarWallet.ts 1.93ms
 588/1641 src/commitments/USE_EMAIL/parseUseEmailCommitmentContent.test.ts 4.12ms
 589/1641 src/commitments/USE_EMAIL/parseUseEmailCommitmentContent.ts 5.24ms
 590/1641 src/commitments/USE_EMAIL/resolveSendEmailToolForNode.ts 7.61ms
 591/1641 src/commitments/USE_EMAIL/sendEmailViaBrowser.ts 4.39ms
 592/1641 src/commitments/USE_EMAIL/USE_EMAIL.test.ts 6.98ms
 593/1641 src/commitments/USE_EMAIL/USE_EMAIL.ts 14.60ms
 594/1641 src/commitments/USE_IMAGE_GENERATOR/USE_IMAGE_GENERATOR.test.ts 3.63ms
 595/1641 src/commitments/USE_IMAGE_GENERATOR/USE_IMAGE_GENERATOR.ts 5.12ms
 596/1641 src/commitments/USE_MCP/USE_MCP.ts 5.76ms
 597/1641 src/commitments/USE_POPUP/USE_POPUP.ts 9.20ms
 598/1641 src/commitments/USE_PRIVACY/USE_PRIVACY.test.ts 4.80ms
 599/1641 src/commitments/USE_PRIVACY/USE_PRIVACY.ts 14.33ms
 600/1641 src/commitments/USE_PROJECT/callGitHubApi.ts 6.42ms
 601/1641 src/commitments/USE_PROJECT/createUseProjectToolFunctions.ts 17.97ms
 602/1641 src/commitments/USE_PROJECT/createUseProjectTools.ts 8.93ms
 603/1641 src/commitments/USE_PROJECT/getUseProjectToolTitles.ts 2.73ms
 604/1641 src/commitments/USE_PROJECT/normalizeConfiguredProjects.ts 3.65ms
 605/1641 src/commitments/USE_PROJECT/normalizeOptionalToolText.ts 2.23ms
 606/1641 src/commitments/USE_PROJECT/projectReference.test.ts 3.21ms
 607/1641 src/commitments/USE_PROJECT/projectReference.ts 7.09ms
 608/1641 src/commitments/USE_PROJECT/resolveUseProjectToolRuntimeOrWalletCredentialResult.ts 7.86ms
 609/1641 src/commitments/USE_PROJECT/USE_PROJECT.test.ts 8.84ms
 610/1641 src/commitments/USE_PROJECT/USE_PROJECT.ts 9.53ms
 611/1641 src/commitments/USE_PROJECT/UseProjectToolNames.ts 4.16ms
 612/1641 src/commitments/USE_PROJECT/UseProjectWallet.ts 3.03ms
 613/1641 src/commitments/USE_SEARCH_ENGINE/USE_SEARCH_ENGINE.test.ts 2.98ms
 614/1641 src/commitments/USE_SEARCH_ENGINE/USE_SEARCH_ENGINE.ts 10.77ms
 615/1641 src/commitments/USE_SPAWN/resolveSpawnAgentToolForNode.ts 7.49ms
 616/1641 src/commitments/USE_SPAWN/spawnAgentViaBrowser.ts 5.71ms
 617/1641 src/commitments/USE_SPAWN/USE_SPAWN.test.ts 3.69ms
 618/1641 src/commitments/USE_SPAWN/USE_SPAWN.ts 8.39ms
 619/1641 src/commitments/USE_TIME/USE_TIME.test.ts 3.24ms
 620/1641 src/commitments/USE_TIME/USE_TIME.ts 6.72ms
 621/1641 src/commitments/USE_TIMEOUT/createTimeoutSystemMessage.ts 5.41ms
 622/1641 src/commitments/USE_TIMEOUT/createTimeoutToolFunctions.ts 11.26ms
 623/1641 src/commitments/USE_TIMEOUT/createTimeoutTools.ts 4.60ms
 624/1641 src/commitments/USE_TIMEOUT/getTimeoutToolRuntimeAdapterOrDisabledResult.ts 5.81ms
 625/1641 src/commitments/USE_TIMEOUT/parseTimeoutToolArgs.ts 6.82ms
 626/1641 src/commitments/USE_TIMEOUT/resolveTimeoutRuntimeContext.ts 3.44ms
 627/1641 src/commitments/USE_TIMEOUT/setTimeoutToolRuntimeAdapter.ts 2.96ms
 628/1641 src/commitments/USE_TIMEOUT/TimeoutToolNames.ts 1.79ms
 629/1641 src/commitments/USE_TIMEOUT/TimeoutToolRuntimeAdapter.ts 6.84ms
 630/1641 src/commitments/USE_TIMEOUT/USE_TIMEOUT.test.ts 10.73ms
 631/1641 src/commitments/USE_TIMEOUT/USE_TIMEOUT.ts 7.26ms
 632/1641 src/commitments/USE_USER_LOCATION/USE_USER_LOCATION.test.ts 4.98ms
 633/1641 src/commitments/USE_USER_LOCATION/USE_USER_LOCATION.ts 10.65ms
 634/1641 src/commitments/USE/aggregateUseCommitmentSystemMessages.ts 9.27ms
 635/1641 src/commitments/USE/USE.ts 7.65ms
 636/1641 src/commitments/WALLET/createWalletSystemMessage.ts 2.56ms
 637/1641 src/commitments/WALLET/createWalletToolFunctions.ts 7.08ms
 638/1641 src/commitments/WALLET/createWalletTools.ts 5.30ms
 639/1641 src/commitments/WALLET/getWalletCommitmentDocumentation.ts 1.93ms
 640/1641 src/commitments/WALLET/getWalletToolRuntimeAdapterOrDisabledResult.ts 5.60ms
 641/1641 src/commitments/WALLET/getWalletToolTitles.ts 3.94ms
 642/1641 src/commitments/WALLET/parseWalletToolArgs.ts 7.60ms
 643/1641 src/commitments/WALLET/resolveWalletRuntimeContext.ts 7.58ms
 644/1641 src/commitments/WALLET/setWalletToolRuntimeAdapter.ts 4.52ms
 645/1641 src/commitments/WALLET/WALLET.test.ts 7.71ms
 646/1641 src/commitments/WALLET/WALLET.ts 5.78ms
 647/1641 src/commitments/WALLET/WalletToolNames.ts 2.06ms
 648/1641 src/commitments/WALLET/WalletToolRuntimeAdapter.ts 7.32ms
 649/1641 src/commitments/WRITING_RULES/WRITING_RULES.ts 7.39ms
 650/1641 src/commitments/WRITING_SAMPLE/WRITING_SAMPLE.ts 6.90ms
 651/1641 src/config.test.ts 3.32ms
 652/1641 src/config.ts 16.94ms
 653/1641 src/constants.ts 9.45ms
 654/1641 src/constants/streaming.ts 3.57ms
 655/1641 src/conversion/archive/loadArchive.ts 5.18ms
 656/1641 src/conversion/archive/saveArchive.ts 5.84ms
 657/1641 src/conversion/compilePipeline.test.ts 3.15ms
 658/1641 src/conversion/compilePipeline.ts 3.71ms
 659/1641 src/conversion/compilePipelineOnRemoteServer.ts 3.50ms
 660/1641 src/conversion/parsePipeline.test.ts 3.49ms
 661/1641 src/conversion/parsePipeline.ts 32.98ms
 662/1641 src/conversion/pipelineJsonToString.ts 9.23ms
 663/1641 src/conversion/prettify/PrettifyOptions.ts 3.27ms
 664/1641 src/conversion/prettify/prettifyPipelineString.ts 4.22ms
 665/1641 src/conversion/prettify/renderPipelineMermaidOptions.ts 8.47ms
 666/1641 src/conversion/utils/extractParameterNamesFromTask.test.ts 2.11ms
 667/1641 src/conversion/utils/extractParameterNamesFromTask.ts 8.34ms
 668/1641 src/conversion/validation/_importPipeline.ts 7.32ms
 669/1641 src/conversion/validation/pipelineStringToJson-parseErrors.test.ts 6.06ms
 670/1641 src/conversion/validation/validatePipeline-logicErrors.test.ts 4.17ms
 671/1641 src/conversion/validation/validatePipeline.test.ts 3.65ms
 672/1641 src/conversion/validation/validatePipeline.ts 11.71ms
 673/1641 src/dialogs/callback/CallbackInterfaceTools.ts 2.93ms
 674/1641 src/dialogs/callback/CallbackInterfaceToolsOptions.ts 13.65ms
 675/1641 src/dialogs/simple-prompt/SimplePromptInterfaceTools.ts 3.52ms
 676/1641 src/dialogs/user-interface-execution-tools.test.ts 6.18ms
 677/1641 src/errors/0-BoilerplateError.ts 1.96ms
 678/1641 src/errors/0-index.ts 9.90ms
 679/1641 src/errors/AbstractFormatError.ts 2.82ms
 680/1641 src/errors/assertsError.ts 2.67ms
 681/1641 src/errors/AuthenticationError.ts 1.75ms
 682/1641 src/errors/CollectionError.ts 1.76ms
 683/1641 src/errors/ConflictError.ts 2.92ms
 684/1641 src/errors/DatabaseError.ts 3.20ms
 685/1641 src/errors/EnvironmentMismatchError.ts 1.87ms
 686/1641 src/errors/ExpectError.ts 2.45ms
 687/1641 src/errors/KnowledgeScrapeError.ts 1.76ms
 688/1641 src/errors/LimitReachedError.ts 1.69ms
 689/1641 src/errors/MissingToolsError.ts 1.91ms
 690/1641 src/errors/NotAllowed.ts 1.80ms
 691/1641 src/errors/NotFoundError.ts 3.34ms
 692/1641 src/errors/NotYetImplementedError.ts 2.65ms
 693/1641 src/errors/ParseError.ts 1.93ms
 694/1641 src/errors/PipelineExecutionError.ts 2.49ms
 695/1641 src/errors/PipelineLogicError.ts 1.78ms
 696/1641 src/errors/PipelineUrlError.ts 1.73ms
 697/1641 src/errors/PromptbookFetchError.ts 1.64ms
 698/1641 src/errors/UnexpectedError.ts 2.65ms
 699/1641 src/errors/utils/deserializeError.test.ts 2.38ms
 700/1641 src/errors/utils/deserializeError.ts 2.49ms
 701/1641 src/errors/utils/ErrorJson.ts 2.15ms
 702/1641 src/errors/utils/getErrorReportUrl.ts 4.17ms
 703/1641 src/errors/utils/serializeError.test.ts 2.11ms
 704/1641 src/errors/utils/serializeError.ts 2.47ms
 705/1641 src/errors/WrappedError.ts 2.53ms
 706/1641 src/executables/$provideExecutablesForNode.ts 4.26ms
 707/1641 src/executables/apps/locateLibreoffice.test.ts 1.68ms
 708/1641 src/executables/apps/locateLibreoffice.ts 2.67ms
 709/1641 src/executables/apps/locatePandoc.test.ts 1.84ms
 710/1641 src/executables/apps/locatePandoc.ts 3.26ms
 711/1641 src/executables/apps/locateVscode.test.ts 1.80ms
 712/1641 src/executables/apps/locateVscode.ts 2.18ms
 713/1641 src/executables/browsers/locateBrowser.test.ts 3.02ms
 714/1641 src/executables/browsers/locateBrowser.ts 3.21ms
 715/1641 src/executables/browsers/locateChrome.test.ts 2.86ms
 716/1641 src/executables/browsers/locateChrome.ts 4.28ms
 717/1641 src/executables/browsers/locateDefaultSystemBrowser.test.ts 1.88ms
 718/1641 src/executables/browsers/locateDefaultSystemBrowser.ts 2.24ms
 719/1641 src/executables/browsers/locateEdge.test.ts 1.79ms
 720/1641 src/executables/browsers/locateEdge.ts 2.33ms
 721/1641 src/executables/browsers/locateFirefox.test.ts 1.60ms
 722/1641 src/executables/browsers/locateFirefox.ts 2.82ms
 723/1641 src/executables/browsers/locateInternetExplorer.test.ts 1.87ms
 724/1641 src/executables/browsers/locateInternetExplorer.ts 1.89ms
 725/1641 src/executables/browsers/locateSafari.ts 1.96ms
 726/1641 src/executables/locateApp.test.ts 2.72ms
 727/1641 src/executables/locateApp.ts 5.60ms
 728/1641 src/executables/platforms/locateAppOnLinux.ts 2.94ms
 729/1641 src/executables/platforms/locateAppOnMacOs.ts 19.23ms
 730/1641 src/executables/platforms/locateAppOnWindows.ts 5.08ms
 731/1641 src/executables/README.md 1.74ms
 732/1641 src/execution/AbstractTaskResult.ts 2.93ms
 733/1641 src/execution/assertsTaskSuccessful.ts 3.73ms
 734/1641 src/execution/AvailableModel.ts 6.53ms
 735/1641 src/execution/CommonToolsOptions.ts 3.35ms
 736/1641 src/execution/createPipelineExecutor/$OngoingTaskResult.ts 5.04ms
 737/1641 src/execution/createPipelineExecutor/00-createPipelineExecutor.ts 15.60ms
 738/1641 src/execution/createPipelineExecutor/00-CreatePipelineExecutorOptions.ts 3.97ms
 739/1641 src/execution/createPipelineExecutor/10-executePipeline.ts 21.08ms
 740/1641 src/execution/createPipelineExecutor/20-executeTask.ts 15.03ms
 741/1641 src/execution/createPipelineExecutor/30-executeFormatSubvalues.ts 21.67ms
 742/1641 src/execution/createPipelineExecutor/40-executeAttempts.ts 77.55ms
 743/1641 src/execution/createPipelineExecutor/computeCosineSimilarity.ts 2.43ms
 744/1641 src/execution/createPipelineExecutor/filterJustOutputParameters.ts 6.36ms
 745/1641 src/execution/createPipelineExecutor/getContextForTask.ts 3.21ms
 746/1641 src/execution/createPipelineExecutor/getExamplesForTask.ts 3.05ms
 747/1641 src/execution/createPipelineExecutor/getKnowledgeForTask.ts 15.49ms
 748/1641 src/execution/createPipelineExecutor/getReservedParametersForTask.ts 5.54ms
 749/1641 src/execution/createPipelineExecutor/knowledgePiecesToString.ts 3.22ms
 750/1641 src/execution/EmbeddingVector.ts 5.35ms
 751/1641 src/execution/embeddingVectorToString.ts 3.04ms
 752/1641 src/execution/Executables.ts 4.32ms
 753/1641 src/execution/execution-report/countWorkingDuration.test.ts 8.07ms
 754/1641 src/execution/execution-report/countWorkingDuration.ts 5.37ms
 755/1641 src/execution/execution-report/ExecutionPromptReportJson.ts 4.10ms
 756/1641 src/execution/execution-report/ExecutionReportJson.ts 6.33ms
 757/1641 src/execution/execution-report/executionReportJsonToString.ts 10.60ms
 758/1641 src/execution/execution-report/ExecutionReportString.ts 4.17ms
 759/1641 src/execution/execution-report/ExecutionReportStringOptions.ts 2.54ms
 760/1641 src/execution/ExecutionTask.ts 24.43ms
 761/1641 src/execution/ExecutionTools.ts 6.04ms
 762/1641 src/execution/FilesystemTools.ts 2.27ms
 763/1641 src/execution/LlmExecutionTools.ts 9.22ms
 764/1641 src/execution/LlmExecutionToolsConstructor.ts 1.99ms
 765/1641 src/execution/PipelineExecutor.ts 2.14ms
 766/1641 src/execution/PipelineExecutorResult.ts 3.34ms
 767/1641 src/execution/PromptbookFetch.test-type.ts 3.01ms
 768/1641 src/execution/PromptbookFetch.ts 1.87ms
 769/1641 src/execution/PromptResult.ts 4.50ms
 770/1641 src/execution/ScriptExecutionTools.ts 4.28ms
 771/1641 src/execution/translation/automatic-translate/automatic-translators/AutomaticTranslator.ts 1.65ms
 772/1641 src/execution/translation/automatic-translate/automatic-translators/DebugAutomaticTranslator.ts 2.57ms
 773/1641 src/execution/translation/automatic-translate/automatic-translators/FakeAutomaticTranslator.ts 2.48ms
 774/1641 src/execution/translation/automatic-translate/automatic-translators/LindatAutomaticTranslator.ts 5.19ms
 775/1641 src/execution/translation/automatic-translate/automatic-translators/README.md 1.45ms
 776/1641 src/execution/translation/automatic-translate/automatic-translators/TranslatorOptions.ts 4.29ms
 777/1641 src/execution/translation/automatic-translate/automatic-translators/utils/extractMultiplicatedOccurrence.test.ts 2.38ms
 778/1641 src/execution/translation/automatic-translate/automatic-translators/utils/extractMultiplicatedOccurrence.ts 2.35ms
 779/1641 src/execution/translation/automatic-translate/translateMessages.ts 4.07ms
 780/1641 src/execution/UncertainNumber.ts 1.89ms
 781/1641 src/execution/Usage.ts 3.24ms
 782/1641 src/execution/UserInterfaceTools.ts 2.97ms
 783/1641 src/execution/utils/$provideExecutionToolsForNode.ts 4.94ms
 784/1641 src/execution/utils/addUsage.test.ts 4.28ms
 785/1641 src/execution/utils/addUsage.ts 2.83ms
 786/1641 src/execution/utils/checkExpectations.test.ts 4.42ms
 787/1641 src/execution/utils/checkExpectations.ts 3.58ms
 788/1641 src/execution/utils/computeUsageCounts.ts 2.91ms
 789/1641 src/execution/utils/forEachAsync.ts 2.72ms
 790/1641 src/execution/utils/uncertainNumber.ts 2.94ms
 791/1641 src/execution/utils/usage-constants.ts 4.96ms
 792/1641 src/execution/utils/usageToHuman.ts 4.50ms
 793/1641 src/execution/utils/usageToWorktime.test.ts 2.64ms
 794/1641 src/execution/utils/usageToWorktime.ts 2.89ms
 795/1641 src/execution/utils/validatePromptResult.ts 5.16ms
 796/1641 src/expectations/README.md 1.46ms
 797/1641 src/file-security-checkers/FileSecurityChecker.ts 2.81ms
 798/1641 src/file-security-checkers/FileSecurityCheckResult.ts 3.06ms
 799/1641 src/file-security-checkers/virustotal/VirusTotalFileSecurityChecker.test.ts 3.63ms
 800/1641 src/file-security-checkers/virustotal/VirusTotalFileSecurityChecker.ts 7.99ms
 801/1641 src/formats/_common/FormatParser.ts 7.25ms
 802/1641 src/formats/_common/FormatSubvalueParser.ts 4.93ms
 803/1641 src/formats/csv/CsvFormatError.ts 1.74ms
 804/1641 src/formats/csv/CsvFormatParser.ts 7.54ms
 805/1641 src/formats/csv/CsvSettings.ts 3.07ms
 806/1641 src/formats/csv/utils/csvParse.ts 2.88ms
 807/1641 src/formats/csv/utils/isValidCsvString.test.ts 2.17ms
 808/1641 src/formats/csv/utils/isValidCsvString.ts 3.16ms
 809/1641 src/formats/index.ts 1.88ms
 810/1641 src/formats/json/JsonFormatParser.ts 4.60ms
 811/1641 src/formats/json/utils/isValidJsonString.test.ts 1.95ms
 812/1641 src/formats/json/utils/isValidJsonString.ts 2.24ms
 813/1641 src/formats/json/utils/jsonParse.ts 2.20ms
 814/1641 src/formats/README.md 2.03ms
 815/1641 src/formats/text/TextFormatParser.ts 5.41ms
 816/1641 src/formats/xml/utils/isValidXmlString.test.ts 4.39ms
 817/1641 src/formats/xml/utils/isValidXmlString.ts 2.81ms
 818/1641 src/formats/xml/XmlFormatParser.ts 4.20ms
 819/1641 src/formfactors/_boilerplate/BoilerplateFormfactorDefinition.ts 5.35ms
 820/1641 src/formfactors/_common/AbstractFormfactorDefinition.ts 4.16ms
 821/1641 src/formfactors/_common/FormfactorDefinition.ts 3.59ms
 822/1641 src/formfactors/_common/string_formfactor_name.ts 2.13ms
 823/1641 src/formfactors/chatbot/ChatbotFormfactorDefinition.ts 3.79ms
 824/1641 src/formfactors/completion/CompletionFormfactorDefinition.ts 4.14ms
 825/1641 src/formfactors/generator/GeneratorFormfactorDefinition.ts 3.51ms
 826/1641 src/formfactors/generic/GenericFormfactorDefinition.ts 2.54ms
 827/1641 src/formfactors/image-generator/ImageGeneratorFormfactorDefinition.ts 2.47ms
 828/1641 src/formfactors/index.ts 2.94ms
 829/1641 src/formfactors/matcher/MatcherFormfactorDefinition.ts 4.26ms
 830/1641 src/formfactors/README.md 3.04ms
 831/1641 src/formfactors/sheets/SheetsFormfactorDefinition.ts 4.13ms
 832/1641 src/formfactors/translator/TranslatorFormfactorDefinition.ts 3.59ms
 833/1641 src/globals.d.ts 1.93ms
 834/1641 src/high-level-abstractions/_common/HighLevelAbstraction.ts 3.91ms
 835/1641 src/high-level-abstractions/implicit-formfactor/ImplicitFormfactorHla.ts 3.80ms
 836/1641 src/high-level-abstractions/index.ts 2.61ms
 837/1641 src/high-level-abstractions/quick-chatbot/QuickChatbotHla.ts 9.39ms
 838/1641 src/high-level-abstractions/README.md 1.43ms
 839/1641 src/import-plugins/$fileImportPlugins.ts 2.13ms
 840/1641 src/import-plugins/AgentFileImportPlugin.ts 5.79ms
 841/1641 src/import-plugins/FileImportPlugin.ts 2.17ms
 842/1641 src/import-plugins/JsonFileImportPlugin.ts 2.19ms
 843/1641 src/import-plugins/TextFileImportPlugin.ts 2.84ms
 844/1641 src/llm-providers/_common/filterModels.ts 4.74ms
 845/1641 src/llm-providers/_common/register/$llmToolsMetadataRegister.ts 3.24ms
 846/1641 src/llm-providers/_common/register/$llmToolsRegister.ts 2.14ms
 847/1641 src/llm-providers/_common/register/$provideEnvFilename.ts 3.89ms
 848/1641 src/llm-providers/_common/register/$provideLlmToolsConfigurationFromEnv.ts 3.87ms
 849/1641 src/llm-providers/_common/register/$provideLlmToolsForTestingAndScriptsAndPlayground.ts 6.45ms
 850/1641 src/llm-providers/_common/register/$provideLlmToolsForWizardOrCli.ts 14.86ms
 851/1641 src/llm-providers/_common/register/$provideLlmToolsFromEnv.ts 8.21ms
 852/1641 src/llm-providers/_common/register/$registeredLlmToolsMessage.ts 7.63ms
 853/1641 src/llm-providers/_common/register/createLlmToolsFromConfiguration.test.ts 5.16ms
 854/1641 src/llm-providers/_common/register/createLlmToolsFromConfiguration.ts 7.56ms
 855/1641 src/llm-providers/_common/register/LlmToolsConfiguration.ts 3.21ms
 856/1641 src/llm-providers/_common/register/LlmToolsMetadata.ts 4.98ms
 857/1641 src/llm-providers/_common/register/LlmToolsOptions.ts 2.16ms
 858/1641 src/llm-providers/_common/utils/assertUniqueModels.ts 2.72ms
 859/1641 src/llm-providers/_common/utils/cache/CacheItem.ts 2.96ms
 860/1641 src/llm-providers/_common/utils/cache/cacheLlmTools.ts 10.96ms
 861/1641 src/llm-providers/_common/utils/cache/CacheLlmToolsOptions.ts 4.24ms
 862/1641 src/llm-providers/_common/utils/count-total-usage/countUsage.ts 6.11ms
 863/1641 src/llm-providers/_common/utils/count-total-usage/limitTotalUsage.ts 6.91ms
 864/1641 src/llm-providers/_common/utils/count-total-usage/LlmExecutionToolsWithTotalUsage.ts 2.60ms
 865/1641 src/llm-providers/_common/utils/pricing.test.ts 1.58ms
 866/1641 src/llm-providers/_common/utils/pricing.ts 1.74ms
 867/1641 src/llm-providers/_common/utils/removeUnsupportedModelRequirements.test.ts 4.08ms
 868/1641 src/llm-providers/_common/utils/removeUnsupportedModelRequirements.ts 4.51ms
 869/1641 src/llm-providers/_multiple/getSingleLlmExecutionTools.ts 3.29ms
 870/1641 src/llm-providers/_multiple/joinLlmExecutionTools.ts 6.35ms
 871/1641 src/llm-providers/_multiple/MultipleLlmExecutionTools.ts 10.97ms
 872/1641 src/llm-providers/_multiple/playground/playground.ts 10.63ms
 873/1641 src/llm-providers/_multiple/playground/tsconfig.json 3.58ms
 874/1641 src/llm-providers/agent/Agent.test.ts 5.86ms
 875/1641 src/llm-providers/agent/Agent.ts 22.00ms
 876/1641 src/llm-providers/agent/AgentLlmExecutionTools.test.ts 12.84ms
 877/1641 src/llm-providers/agent/AgentLlmExecutionTools.ts 35.24ms
 878/1641 src/llm-providers/agent/AgentOptions.ts 4.05ms
 879/1641 src/llm-providers/agent/createAgentLlmExecutionTools.ts 2.55ms
 880/1641 src/llm-providers/agent/CreateAgentLlmExecutionToolsOptions.ts 3.66ms
 881/1641 src/llm-providers/agent/playground/playground.ts 9.36ms
 882/1641 src/llm-providers/agent/playground/tsconfig.json 2.64ms
 883/1641 src/llm-providers/agent/register-configuration.ts 4.97ms
 884/1641 src/llm-providers/agent/register-constructor.ts 2.20ms
 885/1641 src/llm-providers/agent/RemoteAgent.test.ts 8.55ms
 886/1641 src/llm-providers/agent/RemoteAgent.ts 29.15ms
 887/1641 src/llm-providers/agent/RemoteAgentOptions.ts 2.59ms
 888/1641 src/llm-providers/agent/self-learning/SelfLearningManager.test.ts 6.54ms
 889/1641 src/llm-providers/agent/self-learning/SelfLearningManager.ts 18.06ms
 890/1641 src/llm-providers/anthropic-claude/anthropic-claude-models.ts 12.40ms
 891/1641 src/llm-providers/anthropic-claude/AnthropicClaudeExecutionTools.ts 16.81ms
 892/1641 src/llm-providers/anthropic-claude/AnthropicClaudeExecutionToolsOptions.ts 9.30ms
 893/1641 src/llm-providers/anthropic-claude/computeAnthropicClaudeUsage.test.ts 3.02ms
 894/1641 src/llm-providers/anthropic-claude/computeAnthropicClaudeUsage.ts 7.10ms
 895/1641 src/llm-providers/anthropic-claude/createAnthropicClaudeExecutionTools.ts 5.61ms
 896/1641 src/llm-providers/anthropic-claude/playground/playground.ts 7.91ms
 897/1641 src/llm-providers/anthropic-claude/playground/tsconfig.json 9.87ms
 898/1641 src/llm-providers/anthropic-claude/register-configuration.ts 13.40ms
 899/1641 src/llm-providers/anthropic-claude/register-constructor.ts 6.68ms
 900/1641 src/llm-providers/azure-openai/AzureOpenAiExecutionTools.ts 85.52ms
 901/1641 src/llm-providers/azure-openai/AzureOpenAiExecutionToolsOptions.ts 3.28ms
 902/1641 src/llm-providers/azure-openai/createAzureOpenAiExecutionTools.ts 2.64ms
 903/1641 src/llm-providers/azure-openai/playground/playground.ts 7.35ms
 904/1641 src/llm-providers/azure-openai/playground/tsconfig.json 3.34ms
 905/1641 src/llm-providers/azure-openai/register-configuration.ts 14.36ms
 906/1641 src/llm-providers/azure-openai/register-constructor.ts 4.03ms
 907/1641 src/llm-providers/deepseek/createDeepseekExecutionTools.ts 6.00ms
 908/1641 src/llm-providers/deepseek/deepseek-models.ts 10.64ms
 909/1641 src/llm-providers/deepseek/DeepseekExecutionToolsOptions.ts 2.48ms
 910/1641 src/llm-providers/deepseek/other/compatibility-issue.md 4.66ms
 911/1641 src/llm-providers/deepseek/register-configuration.ts 4.86ms
 912/1641 src/llm-providers/deepseek/register-constructor.ts 2.73ms
 913/1641 src/llm-providers/google/createGoogleExecutionTools.ts 13.47ms
 914/1641 src/llm-providers/google/google-models.ts 12.59ms
 915/1641 src/llm-providers/google/GoogleExecutionToolsOptions.ts 3.16ms
 916/1641 src/llm-providers/google/other/compatibility-issue.md 2.71ms
 917/1641 src/llm-providers/google/register-configuration.ts 4.27ms
 918/1641 src/llm-providers/google/register-constructor.ts 2.56ms
 919/1641 src/llm-providers/mocked/$fakeTextToExpectations.ts 7.28ms
 920/1641 src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts 9.52ms
 921/1641 src/llm-providers/mocked/MockedFackedLlmExecutionTools.ts 7.62ms
 922/1641 src/llm-providers/mocked/test/faked-completion.test.ts 6.22ms
 923/1641 src/llm-providers/mocked/test/fakeTextToExpectations.test.ts 2.59ms
 924/1641 src/llm-providers/mocked/test/joker.test.ts 9.77ms
 925/1641 src/llm-providers/mocked/test/mocked-chat.test.ts 13.72ms
 926/1641 src/llm-providers/mocked/test/mocked-completion.test.ts 7.97ms
 927/1641 src/llm-providers/ollama/createOllamaExecutionTools.ts 2.57ms
 928/1641 src/llm-providers/ollama/ollama-models.ts 12.33ms
 929/1641 src/llm-providers/ollama/OllamaExecutionTools.ts 9.82ms
 930/1641 src/llm-providers/ollama/OllamaExecutionToolsOptions.ts 2.34ms
 931/1641 src/llm-providers/ollama/playground/playground.ts 10.40ms
 932/1641 src/llm-providers/ollama/playground/tsconfig.json 3.79ms
 933/1641 src/llm-providers/ollama/register-configuration.ts 4.69ms
 934/1641 src/llm-providers/ollama/register-constructor.ts 3.67ms
 935/1641 src/llm-providers/openai/computeOpenAiUsage.test.ts 2.85ms
 936/1641 src/llm-providers/openai/computeOpenAiUsage.ts 6.42ms
 937/1641 src/llm-providers/openai/createOpenAiAssistantExecutionTools.ts 3.55ms
 938/1641 src/llm-providers/openai/createOpenAiCompatibleExecutionTools.ts 9.33ms
 939/1641 src/llm-providers/openai/createOpenAiExecutionTools.ts 4.27ms
 940/1641 src/llm-providers/openai/openai-models.test.ts 1.68ms
 941/1641 src/llm-providers/openai/openai-models.ts 20.98ms
 942/1641 src/llm-providers/openai/OpenAiAgentKitExecutionTools.ts 35.02ms
 943/1641 src/llm-providers/openai/OpenAiAgentKitExecutionToolsOptions.ts 3.53ms
 944/1641 src/llm-providers/openai/OpenAiAssistantExecutionTools.ts 44.16ms
 945/1641 src/llm-providers/openai/OpenAiAssistantExecutionToolsOptions.ts 3.19ms
 946/1641 src/llm-providers/openai/OpenAiCompatibleExecutionTools.ts 26.91ms
 947/1641 src/llm-providers/openai/OpenAiCompatibleExecutionToolsOptions.ts 12.89ms
 948/1641 src/llm-providers/openai/OpenAiExecutionTools.ts 6.49ms
 949/1641 src/llm-providers/openai/OpenAiExecutionToolsOptions.ts 2.99ms
 950/1641 src/llm-providers/openai/OpenAiVectorStoreHandler.ts 55.35ms
 951/1641 src/llm-providers/openai/playground/playground.ts 16.20ms
 952/1641 src/llm-providers/openai/playground/tsconfig.json 2.79ms
 953/1641 src/llm-providers/openai/register-configuration.ts 10.20ms
 954/1641 src/llm-providers/openai/register-constructor.ts 4.64ms
 955/1641 src/llm-providers/openai/utils/buildToolInvocationScript.ts 2.74ms
 956/1641 src/llm-providers/openai/utils/callOpenAiCompatibleChatModel.ts 31.32ms
 957/1641 src/llm-providers/openai/utils/mapToolsToOpenAi.ts 3.52ms
 958/1641 src/llm-providers/openai/utils/OpenAiCompatibleUnsupportedParameterRetrier.ts 6.32ms
 959/1641 src/llm-providers/openai/utils/uploadFilesToOpenAi.ts 3.86ms
 960/1641 src/llm-providers/README.md 1.31ms
 961/1641 src/llm-providers/remote/playground/playground.ts 10.22ms
 962/1641 src/llm-providers/remote/playground/tsconfig.json 3.39ms
 963/1641 src/llm-providers/remote/RemoteLlmExecutionTools.ts 10.26ms
 964/1641 src/llm-providers/vercel/createExecutionToolsFromVercelProvider.ts 11.83ms
 965/1641 src/llm-providers/vercel/playground/playground.ts 13.21ms
 966/1641 src/llm-providers/vercel/playground/tsconfig.json 4.41ms
 967/1641 src/llm-providers/vercel/VercelExecutionToolsOptions.ts 4.30ms
 968/1641 src/llm-providers/vercel/VercelProvider.ts 2.61ms
 969/1641 src/migrations/migratePipeline.ts 7.12ms
 970/1641 src/other/README.md 1.38ms
 971/1641 src/other/templates/getBookTemplates.ts 5.11ms
 972/1641 src/personas/preparePersona.ts 10.02ms
 973/1641 src/personas/README.md 1.62ms
 974/1641 src/pipeline/book-notation.ts 7.45ms
 975/1641 src/pipeline/isValidPipelineString.test.ts 2.45ms
 976/1641 src/pipeline/isValidPipelineString.ts 3.54ms
 977/1641 src/pipeline/PipelineInterface/constants.ts 3.07ms
 978/1641 src/pipeline/PipelineInterface/getPipelineInterface.ts 3.25ms
 979/1641 src/pipeline/PipelineInterface/isPipelineImplementingInterface.ts 10.27ms
 980/1641 src/pipeline/PipelineInterface/isPipelineInterfacesEqual.ts 3.25ms
 981/1641 src/pipeline/PipelineInterface/PipelineInterface.ts 4.60ms
 982/1641 src/pipeline/PipelineInterface/README.md 1.55ms
 983/1641 src/pipeline/PipelineJson/CommonTaskJson.ts 15.04ms
 984/1641 src/pipeline/PipelineJson/DialogTaskJson.ts 2.33ms
 985/1641 src/pipeline/PipelineJson/Expectations.ts 4.20ms
 986/1641 src/pipeline/PipelineJson/KnowledgePieceJson.ts 3.64ms
 987/1641 src/pipeline/PipelineJson/KnowledgeSourceJson.ts 4.01ms
 988/1641 src/pipeline/PipelineJson/ParameterJson.ts 7.75ms
 989/1641 src/pipeline/PipelineJson/PersonaJson.ts 3.24ms
 990/1641 src/pipeline/PipelineJson/PipelineJson.ts 10.16ms
 991/1641 src/pipeline/PipelineJson/PreparationJson.ts 2.80ms
 992/1641 src/pipeline/PipelineJson/PromptTaskJson.ts 4.42ms
 993/1641 src/pipeline/PipelineJson/ScriptTaskJson.ts 5.16ms
 994/1641 src/pipeline/PipelineJson/SimpleTaskJson.ts 5.44ms
 995/1641 src/pipeline/PipelineJson/TaskJson.ts 6.85ms
 996/1641 src/pipeline/PipelineString.ts 8.27ms
 997/1641 src/pipeline/prompt-notation.test.ts 5.59ms
 998/1641 src/pipeline/prompt-notation.ts 13.83ms
 999/1641 src/pipeline/prompt-notation/helpers/ParameterEscaping.ts 5.85ms
1000/1641 src/pipeline/prompt-notation/helpers/ParameterNaming.ts 7.30ms
1001/1641 src/pipeline/prompt-notation/helpers/ParameterSection.ts 11.18ms
1002/1641 src/pipeline/validatePipelineString.ts 4.85ms
1003/1641 src/playground/permanent/_boilerplate.ts 1.85ms
1004/1641 src/playground/permanent/agent-with-browser-playground.ts 2.50ms
1005/1641 src/playground/permanent/error-handling-playground.ts 2.83ms
1006/1641 src/playground/playground.ts 7.39ms
1007/1641 src/playground/tsconfig.json 4.29ms
1008/1641 src/postprocessing/README.md 1.64ms
1009/1641 src/postprocessing/utils/extractBlock.ts 2.76ms
1010/1641 src/postprocessing/utils/extractJsonBlock.test.ts 7.11ms
1011/1641 src/postprocessing/utils/extractJsonBlock.ts 3.96ms
1012/1641 src/prepare/isPipelinePrepared.test.ts 2.85ms
1013/1641 src/prepare/isPipelinePrepared.ts 5.03ms
1014/1641 src/prepare/PrepareAndScrapeOptions.ts 6.82ms
1015/1641 src/prepare/preparePipeline.ts 26.99ms
1016/1641 src/prepare/preparePipelineOnRemoteServer.ts 4.62ms
1017/1641 src/prepare/prepareTasks.ts 13.03ms
1018/1641 src/prepare/unpreparePipeline.ts 5.35ms
1019/1641 src/remote-server/createRemoteClient.ts 6.56ms
1020/1641 src/remote-server/http-sample/README.md 1.60ms
1021/1641 src/remote-server/openapi-types.ts 9.49ms
1022/1641 src/remote-server/openapi.ts 9.34ms
1023/1641 src/remote-server/README.md 1.76ms
1024/1641 src/remote-server/RemoteServer.ts 2.11ms
1025/1641 src/remote-server/socket-types/_common/PromptbookServer_Error.ts 1.82ms
1026/1641 src/remote-server/socket-types/_subtypes/Identification.ts 3.86ms
1027/1641 src/remote-server/socket-types/_subtypes/identificationToPromptbookToken.ts 2.86ms
1028/1641 src/remote-server/socket-types/_subtypes/promptbookTokenToIdentification.ts 2.49ms
1029/1641 src/remote-server/socket-types/listModels/PromptbookServer_ListModels_Request.ts 3.92ms
1030/1641 src/remote-server/socket-types/listModels/PromptbookServer_ListModels_Response.ts 2.12ms
1031/1641 src/remote-server/socket-types/prepare/PromptbookServer_PreparePipeline_Request.ts 2.53ms
1032/1641 src/remote-server/socket-types/prepare/PromptbookServer_PreparePipeline_Response.ts 2.70ms
1033/1641 src/remote-server/socket-types/prompt/PromptbookServer_Prompt_Request.ts 1.80ms
1034/1641 src/remote-server/socket-types/prompt/PromptbookServer_Prompt_Response.ts 4.51ms
1035/1641 src/remote-server/startAgentServer.ts 4.91ms
1036/1641 src/remote-server/startRemoteServer.ts 40.82ms
1037/1641 src/remote-server/types/RemoteClientOptions.ts 2.33ms
1038/1641 src/remote-server/types/RemoteServerOptions.ts 9.03ms
1039/1641 src/remote-server/ui/renderServerIndexHtml.ts 4.80ms
1040/1641 src/remote-server/ui/ServerApp.tsx 6.73ms
1041/1641 src/remote-server/ui/types.ts 2.38ms
1042/1641 src/scrapers/_boilerplate/BoilerplateScraper.ts 10.69ms
1043/1641 src/scrapers/_boilerplate/createBoilerplateScraper.ts 2.77ms
1044/1641 src/scrapers/_boilerplate/playground/boilerplate-scraper-playground.ts 4.29ms
1045/1641 src/scrapers/_boilerplate/playground/tsconfig.json 6.23ms
1046/1641 src/scrapers/_boilerplate/README.md 1.39ms
1047/1641 src/scrapers/_boilerplate/register-constructor.ts 3.81ms
1048/1641 src/scrapers/_boilerplate/register-metadata.ts 4.02ms
1049/1641 src/scrapers/_common/Converter.ts 3.31ms
1050/1641 src/scrapers/_common/prepareKnowledgePieces.test.ts 3.63ms
1051/1641 src/scrapers/_common/prepareKnowledgePieces.ts 7.49ms
1052/1641 src/scrapers/_common/register/$provideFilesystemForNode.ts 5.25ms
1053/1641 src/scrapers/_common/register/$provideScrapersForBrowser.ts 3.81ms
1054/1641 src/scrapers/_common/register/$provideScrapersForNode.ts 4.85ms
1055/1641 src/scrapers/_common/register/$provideScriptingForNode.ts 5.04ms
1056/1641 src/scrapers/_common/register/$registeredScrapersMessage.ts 7.59ms
1057/1641 src/scrapers/_common/register/$scrapersMetadataRegister.ts 6.82ms
1058/1641 src/scrapers/_common/register/$scrapersRegister.ts 4.75ms
1059/1641 src/scrapers/_common/register/ScraperAndConverterMetadata.ts 6.06ms
1060/1641 src/scrapers/_common/register/ScraperConstructor.ts 2.61ms
1061/1641 src/scrapers/_common/Scraper.ts 4.36ms
1062/1641 src/scrapers/_common/ScraperIntermediateSource.ts 4.12ms
1063/1641 src/scrapers/_common/utils/getScraperIntermediateSource.test.ts 8.67ms
1064/1641 src/scrapers/_common/utils/getScraperIntermediateSource.ts 9.64ms
1065/1641 src/scrapers/_common/utils/makeKnowledgeSourceHandler.ts 14.54ms
1066/1641 src/scrapers/_common/utils/promptbookFetch.test.ts 4.01ms
1067/1641 src/scrapers/_common/utils/promptbookFetch.ts 3.22ms
1068/1641 src/scrapers/document-legacy/createLegacyDocumentScraper.ts 2.91ms
1069/1641 src/scrapers/document-legacy/examples/10-simple.doc.knowledge.json 38.83ms
1070/1641 src/scrapers/document-legacy/examples/10-simple.rtf.knowledge.json 18.00ms
1071/1641 src/scrapers/document-legacy/LegacyDocumentScraper.test.ts 5.97ms
1072/1641 src/scrapers/document-legacy/LegacyDocumentScraper.ts 13.90ms
1073/1641 src/scrapers/document-legacy/playground/legacy-document-scraper-playground.ts 6.11ms
1074/1641 src/scrapers/document-legacy/playground/tsconfig.json 3.63ms
1075/1641 src/scrapers/document-legacy/README.md 1.60ms
1076/1641 src/scrapers/document-legacy/register-constructor.ts 2.41ms
1077/1641 src/scrapers/document-legacy/register-metadata.ts 4.17ms
1078/1641 src/scrapers/document/createDocumentScraper.ts 4.52ms
1079/1641 src/scrapers/document/DocumentScraper.test.ts 7.46ms
1080/1641 src/scrapers/document/DocumentScraper.ts 9.68ms
1081/1641 src/scrapers/document/examples/10-simple.docx.knowledge.json 29.82ms
1082/1641 src/scrapers/document/examples/10-simple.odt.knowledge.json 33.20ms
1083/1641 src/scrapers/document/playground/document-scraper-playground.ts 6.69ms
1084/1641 src/scrapers/document/playground/tsconfig.json 5.11ms
1085/1641 src/scrapers/document/README.md 2.21ms
1086/1641 src/scrapers/document/register-constructor.ts 6.06ms
1087/1641 src/scrapers/document/register-metadata.ts 11.26ms
1088/1641 src/scrapers/markdown/createMarkdownScraper.ts 2.91ms
1089/1641 src/scrapers/markdown/MarkdownScraper.test.ts 6.41ms
1090/1641 src/scrapers/markdown/MarkdownScraper.ts 21.91ms
1091/1641 src/scrapers/markdown/playground/markdown-scraper-playground.ts 6.17ms
1092/1641 src/scrapers/markdown/playground/tsconfig.json 3.02ms
1093/1641 src/scrapers/markdown/README.md 7.24ms
1094/1641 src/scrapers/markdown/register-constructor.ts 2.86ms
1095/1641 src/scrapers/markdown/register-metadata.ts 4.23ms
1096/1641 src/scrapers/markitdown/createMarkitdownScraper.ts 4.29ms
1097/1641 src/scrapers/markitdown/examples/10-simple.pdf.knowledge.json 16.34ms
1098/1641 src/scrapers/markitdown/examples/collboard-fra1-cdn-digitaloceanspaces-com-ptbk-user-knowledge-source-b-d-10-simple-pdf.knowledge.json 16.48ms
1099/1641 src/scrapers/markitdown/MarkitdownScraper.ts 9.27ms
1100/1641 src/scrapers/markitdown/playground/markitdown-scraper-playground.ts 6.95ms
1101/1641 src/scrapers/markitdown/playground/tsconfig.json 2.65ms
1102/1641 src/scrapers/markitdown/README.md 1.65ms
1103/1641 src/scrapers/markitdown/register-constructor.ts 3.27ms
1104/1641 src/scrapers/markitdown/register-metadata.ts 4.59ms
1105/1641 src/scrapers/pdf/createPdfScraper.ts 8.31ms
1106/1641 src/scrapers/pdf/lib/pdf2md/models/Annotation.js 2.88ms
1107/1641 src/scrapers/pdf/lib/pdf2md/models/HeadlineFinder.js 4.02ms
1108/1641 src/scrapers/pdf/lib/pdf2md/models/LineConverter.js 9.52ms
1109/1641 src/scrapers/pdf/lib/pdf2md/models/LineItem.js 10.05ms
1110/1641 src/scrapers/pdf/lib/pdf2md/models/LineItemBlock.js 5.40ms
1111/1641 src/scrapers/pdf/lib/pdf2md/models/markdown/BlockType.js 12.39ms
1112/1641 src/scrapers/pdf/lib/pdf2md/models/markdown/WordFormat.js 2.95ms
1113/1641 src/scrapers/pdf/lib/pdf2md/models/markdown/WordType.js 9.54ms
1114/1641 src/scrapers/pdf/lib/pdf2md/models/Metadata.js 2.46ms
1115/1641 src/scrapers/pdf/lib/pdf2md/models/Page.js 1.83ms
1116/1641 src/scrapers/pdf/lib/pdf2md/models/PageItem.js 3.58ms
1117/1641 src/scrapers/pdf/lib/pdf2md/models/ParsedElements.js 2.13ms
1118/1641 src/scrapers/pdf/lib/pdf2md/models/ParseResult.js 2.42ms
1119/1641 src/scrapers/pdf/lib/pdf2md/models/StashingStream.js 3.82ms
1120/1641 src/scrapers/pdf/lib/pdf2md/models/TextItem.js 2.61ms
1121/1641 src/scrapers/pdf/lib/pdf2md/models/TextItemLineGrouper.js 4.73ms
1122/1641 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item-block/DetectCodeQuoteBlocks.js 3.49ms
1123/1641 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item-block/DetectListLevels.js 4.02ms
1124/1641 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item-block/GatherBlocks.js 5.47ms
1125/1641 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/CompactLines.js 4.96ms
1126/1641 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/DetectHeaders.js 6.98ms
1127/1641 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/DetectListItems.js 4.10ms
1128/1641 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/DetectTOC.js 15.34ms
1129/1641 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/RemoveRepetitiveElements.js 7.03ms
1130/1641 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/VerticalToHorizontal.js 4.31ms
1131/1641 src/scrapers/pdf/lib/pdf2md/models/transformations/text-item/CalculateGlobalStats.js 10.55ms
1132/1641 src/scrapers/pdf/lib/pdf2md/models/transformations/ToLineItemBlockTransformation.js 6.42ms
1133/1641 src/scrapers/pdf/lib/pdf2md/models/transformations/ToLineItemTransformation.js 9.31ms
1134/1641 src/scrapers/pdf/lib/pdf2md/models/transformations/ToMarkdown.js 7.05ms
1135/1641 src/scrapers/pdf/lib/pdf2md/models/transformations/ToTextBlocks.js 5.94ms
1136/1641 src/scrapers/pdf/lib/pdf2md/models/transformations/ToTextItemTransformation.js 3.90ms
1137/1641 src/scrapers/pdf/lib/pdf2md/models/transformations/Transformation.js 8.69ms
1138/1641 src/scrapers/pdf/lib/pdf2md/models/Word.js 1.93ms
1139/1641 src/scrapers/pdf/lib/pdf2md/pdf2md-cli.js 5.89ms
1140/1641 src/scrapers/pdf/lib/pdf2md/pdf2md.js 3.56ms
1141/1641 src/scrapers/pdf/lib/pdf2md/README.md 1.45ms
1142/1641 src/scrapers/pdf/lib/pdf2md/util/cli.js 3.44ms
1143/1641 src/scrapers/pdf/lib/pdf2md/util/dom-stubs.js 68.98ms
1144/1641 src/scrapers/pdf/lib/pdf2md/util/page-item-functions.js 2.29ms
1145/1641 src/scrapers/pdf/lib/pdf2md/util/page-number-functions.js 13.09ms
1146/1641 src/scrapers/pdf/lib/pdf2md/util/pdf.js 62.04ms
1147/1641 src/scrapers/pdf/lib/pdf2md/util/string-functions.js 7.99ms
1148/1641 src/scrapers/pdf/lib/pdf2md/util/transformations.js 4.14ms
1149/1641 src/scrapers/pdf/PdfScraper.ts 12.25ms
1150/1641 src/scrapers/pdf/playground/pdf-scraper-playground.ts 62.20ms
1151/1641 src/scrapers/pdf/playground/tsconfig.json 2.96ms
1152/1641 src/scrapers/pdf/README.md 1.95ms
1153/1641 src/scrapers/pdf/register-constructor.ts 4.83ms
1154/1641 src/scrapers/pdf/register-metadata.ts 4.48ms
1155/1641 src/scrapers/README.md 1.36ms
1156/1641 src/scrapers/website/createWebsiteScraper.ts 2.97ms
1157/1641 src/scrapers/website/playground/tsconfig.json 2.59ms
1158/1641 src/scrapers/website/playground/website-scraper-playground.ts 5.75ms
1159/1641 src/scrapers/website/README.md 1.49ms
1160/1641 src/scrapers/website/register-constructor.ts 2.53ms
1161/1641 src/scrapers/website/register-metadata.ts 3.58ms
1162/1641 src/scrapers/website/utils/createShowdownConverter.test.ts 3.07ms
1163/1641 src/scrapers/website/utils/createShowdownConverter.ts 2.50ms
1164/1641 src/scrapers/website/WebsiteScraper.ts 10.87ms
1165/1641 src/scripting/_test/custom-function-async.test.ts.test.ts 5.25ms
1166/1641 src/scripting/_test/custom-function-missing.test.ts 4.53ms
1167/1641 src/scripting/_test/custom-function-with-dependencies.test.ts 5.50ms
1168/1641 src/scripting/_test/custom-function.test.ts 4.39ms
1169/1641 src/scripting/_test/postprocessing.test.ts 29.54ms
1170/1641 src/scripting/_test/script-execution-errors.test.ts 12.24ms
1171/1641 src/scripting/_test/script-execution-tools.test.ts 7.29ms
1172/1641 src/scripting/javascript/JavascriptEvalExecutionTools.test.ts 10.03ms
1173/1641 src/scripting/javascript/JavascriptEvalExecutionTools.ts 10.89ms
1174/1641 src/scripting/javascript/JavascriptExecutionTools.ts 1.97ms
1175/1641 src/scripting/javascript/JavascriptExecutionToolsOptions.ts 3.35ms
1176/1641 src/scripting/javascript/postprocessing-functions.ts 8.03ms
1177/1641 src/scripting/javascript/utils/extractVariablesFromJavascript.test.ts 2.40ms
1178/1641 src/scripting/javascript/utils/extractVariablesFromJavascript.ts 29.47ms
1179/1641 src/scripting/javascript/utils/extractVariablesFromScript.test.ts 6.17ms
1180/1641 src/scripting/python/PythonExecutionTools.ts 4.90ms
1181/1641 src/scripting/typescript/TypescriptExecutionTools.ts 6.05ms
1182/1641 src/search-engines/_index.ts 2.42ms
1183/1641 src/search-engines/bing/BingSearchEngine.ts 4.11ms
1184/1641 src/search-engines/dummy/DummySearchEngine.ts 7.43ms
1185/1641 src/search-engines/google/GoogleSearchEngine.ts 3.75ms
1186/1641 src/search-engines/SearchEngine.ts 2.52ms
1187/1641 src/search-engines/SearchResult.ts 2.71ms
1188/1641 src/search-engines/serp/SerpSearchEngine.ts 4.66ms
1189/1641 src/speech-recognition/BrowserSpeechRecognition.ts 5.46ms
1190/1641 src/speech-recognition/OpenAiSpeechRecognition.ts 25.15ms
1191/1641 src/storage/_common/PromptbookStorage.test-type.ts 2.73ms
1192/1641 src/storage/_common/PromptbookStorage.ts 4.41ms
1193/1641 src/storage/blackhole/BlackholeStorage.ts 3.64ms
1194/1641 src/storage/env-storage/$EnvStorage.ts 14.66ms
1195/1641 src/storage/file-cache-storage/FileCacheStorage.ts 9.92ms
1196/1641 src/storage/file-cache-storage/FileCacheStorageOptions.ts 2.77ms
1197/1641 src/storage/file-cache-storage/utils/nameToSubfolderPath.test.ts 1.71ms
1198/1641 src/storage/file-cache-storage/utils/nameToSubfolderPath.ts 3.95ms
1199/1641 src/storage/local-storage/getIndexedDbStorage.ts 7.08ms
1200/1641 src/storage/local-storage/getLocalStorage.ts 3.70ms
1201/1641 src/storage/local-storage/getSessionStorage.ts 6.71ms
1202/1641 src/storage/local-storage/utils/IndexedDbStorageOptions.ts 2.03ms
1203/1641 src/storage/local-storage/utils/makePromptbookStorageFromIndexedDb.ts 5.16ms
1204/1641 src/storage/local-storage/utils/makePromptbookStorageFromWebStorage.ts 6.74ms
1205/1641 src/storage/memory/MemoryStorage.ts 3.48ms
1206/1641 src/storage/utils/PrefixStorage.ts 8.22ms
1207/1641 src/transpilers/_common/BookTranspiler.ts 6.36ms
1208/1641 src/transpilers/_common/BookTranspilerOptions.ts 4.59ms
1209/1641 src/transpilers/_common/register/$bookTranspilersRegister.ts 3.38ms
1210/1641 src/transpilers/formatted-book-in-markdown/FormattedBookInMarkdownTranspiler.ts 3.64ms
1211/1641 src/transpilers/formatted-book-in-markdown/register.ts 4.59ms
1212/1641 src/transpilers/openai-sdk/OpenAiSdkTranspiler.test.ts 11.01ms
1213/1641 src/transpilers/openai-sdk/OpenAiSdkTranspiler.ts 11.55ms
1214/1641 src/transpilers/openai-sdk/playground/playground.ts 2.37ms
1215/1641 src/transpilers/openai-sdk/playground/tsconfig.json 2.63ms
1216/1641 src/transpilers/openai-sdk/register.ts 4.86ms
1217/1641 src/types/Arrayable.ts 3.22ms
1218/1641 src/types/InputParameters_private.ts 2.16ms
1219/1641 src/types/IntermediateFilesStrategy.ts 3.43ms
1220/1641 src/types/LlmCall.ts 2.88ms
1221/1641 src/types/LlmToolDefinition.ts 2.17ms
1222/1641 src/types/Message.ts 3.26ms
1223/1641 src/types/ModelRequirements.ts 7.32ms
1224/1641 src/types/ModelVariant.ts 3.63ms
1225/1641 src/types/NonEmptyArray.ts 2.52ms
1226/1641 src/types/number_bytes.ts 2.28ms
1227/1641 src/types/number_id.ts 3.01ms
1228/1641 src/types/number_likeness.ts 3.32ms
1229/1641 src/types/number_milliseconds.ts 1.72ms
1230/1641 src/types/number_percent.ts 1.64ms
1231/1641 src/types/number_positive.ts 1.52ms
1232/1641 src/types/number_usd.ts 1.75ms
1233/1641 src/types/Parameters_private.ts 2.92ms
1234/1641 src/types/Parameters.ts 2.29ms
1235/1641 src/types/Prompt.ts 7.64ms
1236/1641 src/types/ReservedParameters_private.ts 2.51ms
1237/1641 src/types/ScriptLanguage.ts 5.47ms
1238/1641 src/types/SectionType.ts 2.59ms
1239/1641 src/types/SpeechRecognition.ts 6.86ms
1240/1641 src/types/string_agent_hash_private.ts 2.23ms
1241/1641 src/types/string_agent_name_in_book_private.ts 2.22ms
1242/1641 src/types/string_agent_name_private.ts 4.91ms
1243/1641 src/types/string_agent_name.ts 3.95ms
1244/1641 src/types/string_agent_permanent_id_private.ts 2.09ms
1245/1641 src/types/string_agent_url_private.ts 1.73ms
1246/1641 src/types/string_agent_url.ts 1.57ms
1247/1641 src/types/string_base_url_private.ts 1.53ms
1248/1641 src/types/string_base_url.ts 1.61ms
1249/1641 src/types/string_base64_private.ts 1.85ms
1250/1641 src/types/string_base64.ts 1.63ms
1251/1641 src/types/string_business_category_name_private.ts 3.89ms
1252/1641 src/types/string_business_category_name.ts 4.32ms
1253/1641 src/types/string_char_private.ts 1.35ms
1254/1641 src/types/string_chat_prompt_private.ts 2.56ms
1255/1641 src/types/string_completion_prompt_private.ts 3.23ms
1256/1641 src/types/string_email_private.ts 1.87ms
1257/1641 src/types/string_email.ts 1.56ms
1258/1641 src/types/string_filename.ts 3.89ms
1259/1641 src/types/string_host_private.ts 3.60ms
1260/1641 src/types/string_host.ts 3.92ms
1261/1641 src/types/string_href_private.ts 4.15ms
1262/1641 src/types/string_href.ts 2.28ms
1263/1641 src/types/string_knowledge_source_content.ts 2.24ms
1264/1641 src/types/string_markdown.ts 6.70ms
1265/1641 src/types/string_mime_type_private.ts 2.52ms
1266/1641 src/types/string_mime_type.ts 4.07ms
1267/1641 src/types/string_model_description_private.ts 1.49ms
1268/1641 src/types/string_model_name_private.ts 9.42ms
1269/1641 src/types/string_model_name.ts 1.76ms
1270/1641 src/types/string_name_private.ts 1.77ms
1271/1641 src/types/string_name.ts 2.16ms
1272/1641 src/types/string_page_private.ts 1.45ms
1273/1641 src/types/string_page.ts 1.48ms
1274/1641 src/types/string_parameter_name.ts 3.25ms
1275/1641 src/types/string_parameter_value_private.ts 2.17ms
1276/1641 src/types/string_person_fullname.ts 4.04ms
1277/1641 src/types/string_persona_description_private.ts 1.59ms
1278/1641 src/types/string_persona_description.ts 3.20ms
1279/1641 src/types/string_pipeline_root_url_private.ts 2.06ms
1280/1641 src/types/string_pipeline_root_url.ts 1.64ms
1281/1641 src/types/string_pipeline_url_private.ts 1.47ms
1282/1641 src/types/string_pipeline_url.ts 1.78ms
1283/1641 src/types/string_prompt_image_private.ts 1.65ms
1284/1641 src/types/string_prompt_private.ts 1.96ms
1285/1641 src/types/string_prompt.ts 3.01ms
1286/1641 src/types/string_promptbook_server_url_private.ts 1.64ms
1287/1641 src/types/string_promptbook_server_url.ts 2.71ms
1288/1641 src/types/string_reserved_parameter_name_private.ts 1.74ms
1289/1641 src/types/string_sha256.ts 2.77ms
1290/1641 src/types/string_system_message_private.ts 2.82ms
1291/1641 src/types/string_template_private.ts 1.65ms
1292/1641 src/types/string_text_prompt_private.ts 1.48ms
1293/1641 src/types/string_title_private.ts 4.16ms
1294/1641 src/types/string_title.ts 6.07ms
1295/1641 src/types/string_token.ts 2.80ms
1296/1641 src/types/string_url_image_private.ts 2.18ms
1297/1641 src/types/string_url_image.ts 1.93ms
1298/1641 src/types/string_url_private.ts 7.89ms
1299/1641 src/types/string_url.ts 2.93ms
1300/1641 src/types/TaskType.ts 2.86ms
1301/1641 src/types/ToolCall.ts 11.57ms
1302/1641 src/types/typeAliasEmoji.ts 10.88ms
1303/1641 src/types/typeAliases.ts 6.60ms
1304/1641 src/types/Updatable.ts 3.28ms
1305/1641 src/utils/agents/resolveAgentAvatarImageUrl.ts 5.82ms
1306/1641 src/utils/chat/chatAttachments.test.ts 9.91ms
1307/1641 src/utils/chat/chatAttachments.ts 4.93ms
1308/1641 src/utils/chat/chatAttachments/appendChatAttachmentContext.ts 2.56ms
1309/1641 src/utils/chat/chatAttachments/appendChatAttachmentContextWithContent.ts 4.14ms
1310/1641 src/utils/chat/chatAttachments/appendChatContextSections.ts 2.52ms
1311/1641 src/utils/chat/chatAttachments/formatChatAttachmentContentContext.ts 4.28ms
1312/1641 src/utils/chat/chatAttachments/formatChatAttachmentContext.ts 3.08ms
1313/1641 src/utils/chat/chatAttachments/normalizeChatAttachments.ts 9.06ms
1314/1641 src/utils/chat/chatAttachments/resolveChatAttachmentContent.ts 8.24ms
1315/1641 src/utils/chat/chatAttachments/resolveChatAttachmentContents.ts 10.77ms
1316/1641 src/utils/chat/constants.ts 9.89ms
1317/1641 src/utils/chat/decodeChatStreamWhitespaceFromTransport.ts 3.34ms
1318/1641 src/utils/chat/encodeChatStreamWhitespaceForTransport.ts 3.68ms
1319/1641 src/utils/chat/escapeRegExp.ts 1.80ms
1320/1641 src/utils/clientVersion.ts 7.93ms
1321/1641 src/utils/color/$randomColor.ts 2.98ms
1322/1641 src/utils/color/Color.test.ts 6.56ms
1323/1641 src/utils/color/Color.ts 9.20ms
1324/1641 src/utils/color/css-colors.ts 22.16ms
1325/1641 src/utils/color/internal-utils/checkChannelValue.ts 4.44ms
1326/1641 src/utils/color/internal-utils/hslToRgb.ts 3.61ms
1327/1641 src/utils/color/internal-utils/rgbToHsl.ts 2.37ms
1328/1641 src/utils/color/operators/ColorTransformer.ts 1.48ms
1329/1641 src/utils/color/operators/darken.ts 1.87ms
1330/1641 src/utils/color/operators/furthest.ts 2.81ms
1331/1641 src/utils/color/operators/grayscale.ts 5.55ms
1332/1641 src/utils/color/operators/lighten.ts 12.57ms
1333/1641 src/utils/color/operators/mixWithColor.ts 3.19ms
1334/1641 src/utils/color/operators/nearest.ts 4.46ms
1335/1641 src/utils/color/operators/negative.ts 3.59ms
1336/1641 src/utils/color/operators/negativeLightness.ts 2.09ms
1337/1641 src/utils/color/operators/README.md 1.27ms
1338/1641 src/utils/color/operators/saturate.ts 2.65ms
1339/1641 src/utils/color/operators/withAlpha.ts 3.05ms
1340/1641 src/utils/color/parsers/ColorChannelSet.ts 1.90ms
1341/1641 src/utils/color/parsers/parseHexColor.ts 3.08ms
1342/1641 src/utils/color/parsers/parseHslColor.ts 4.81ms
1343/1641 src/utils/color/parsers/parseRgbColor.ts 3.86ms
1344/1641 src/utils/color/utils/areColorsEqual.ts 3.07ms
1345/1641 src/utils/color/utils/colorDistance.ts 3.01ms
1346/1641 src/utils/color/utils/colorHue.ts 1.82ms
1347/1641 src/utils/color/utils/colorHueDistance.test.ts 4.64ms
1348/1641 src/utils/color/utils/colorHueDistance.ts 3.14ms
1349/1641 src/utils/color/utils/colorLuminance.ts 1.63ms
1350/1641 src/utils/color/utils/colorSatulightion.ts 2.19ms
1351/1641 src/utils/color/utils/colorSaturation.ts 2.31ms
1352/1641 src/utils/color/utils/colorToDataUrl.ts 5.71ms
1353/1641 src/utils/color/utils/mixColors.ts 2.19ms
1354/1641 src/utils/database/uniqueConstraint.ts 4.38ms
1355/1641 src/utils/DEFAULT_THINKING_MESSAGES.ts 3.04ms
1356/1641 src/utils/editable/edit-pipeline-string/addPipelineCommand.test.ts 3.46ms
1357/1641 src/utils/editable/edit-pipeline-string/addPipelineCommand.ts 5.95ms
1358/1641 src/utils/editable/edit-pipeline-string/deflatePipeline.test.ts 7.48ms
1359/1641 src/utils/editable/edit-pipeline-string/deflatePipeline.ts 15.81ms
1360/1641 src/utils/editable/edit-pipeline-string/removePipelineCommand.test.ts 4.78ms
1361/1641 src/utils/editable/edit-pipeline-string/removePipelineCommand.ts 6.54ms
1362/1641 src/utils/editable/types/PipelineEditableSerialized.ts 4.66ms
1363/1641 src/utils/editable/utils/isFlatPipeline.test.ts 12.93ms
1364/1641 src/utils/editable/utils/isFlatPipeline.ts 9.00ms
1365/1641 src/utils/editable/utils/renamePipelineParameter.test.ts 11.05ms
1366/1641 src/utils/editable/utils/renamePipelineParameter.ts 4.19ms
1367/1641 src/utils/editable/utils/stringifyPipelineJson.ts 7.83ms
1368/1641 src/utils/environment/$detectRuntimeEnvironment.ts 2.79ms
1369/1641 src/utils/environment/$getGlobalScope.ts 1.89ms
1370/1641 src/utils/environment/$isRunningInBrowser.ts 6.54ms
1371/1641 src/utils/environment/$isRunningInJest.ts 2.92ms
1372/1641 src/utils/environment/$isRunningInNode.ts 2.47ms
1373/1641 src/utils/environment/$isRunningInWebWorker.ts 3.69ms
1374/1641 src/utils/execCommand/$execCommand.ts 8.65ms
1375/1641 src/utils/execCommand/$execCommandNormalizeOptions.ts 5.11ms
1376/1641 src/utils/execCommand/$execCommands.ts 2.68ms
1377/1641 src/utils/execCommand/execCommand.test.ts 8.89ms
1378/1641 src/utils/execCommand/execCommandNormalizeOptions.test.ts 3.79ms
1379/1641 src/utils/execCommand/ExecCommandOptions.ts 5.61ms
1380/1641 src/utils/expectation-counters/constants.ts 1.85ms
1381/1641 src/utils/expectation-counters/countCharacters.test.ts 2.62ms
1382/1641 src/utils/expectation-counters/countCharacters.ts 5.90ms
1383/1641 src/utils/expectation-counters/countLines.test.ts 3.09ms
1384/1641 src/utils/expectation-counters/countLines.ts 3.82ms
1385/1641 src/utils/expectation-counters/countPages.test.ts 12.40ms
1386/1641 src/utils/expectation-counters/countPages.ts 5.74ms
1387/1641 src/utils/expectation-counters/countParagraphs.test.ts 5.37ms
1388/1641 src/utils/expectation-counters/countParagraphs.ts 2.33ms
1389/1641 src/utils/expectation-counters/countSentences.test.ts 3.89ms
1390/1641 src/utils/expectation-counters/countSentences.ts 2.65ms
1391/1641 src/utils/expectation-counters/countWords.test.ts 2.99ms
1392/1641 src/utils/expectation-counters/countWords.ts 2.93ms
1393/1641 src/utils/expectation-counters/index.ts 3.31ms
1394/1641 src/utils/expectation-counters/other/countPages.md 3.33ms
1395/1641 src/utils/files/$induceBookDownload.ts 4.85ms
1396/1641 src/utils/files/$induceFileDownload.ts 4.75ms
1397/1641 src/utils/files/decodeAttachmentAsText.test.ts 5.80ms
1398/1641 src/utils/files/decodeAttachmentAsText.ts 18.68ms
1399/1641 src/utils/files/extensionToMimeType.test.ts 2.86ms
1400/1641 src/utils/files/extensionToMimeType.ts 1.73ms
1401/1641 src/utils/files/getFileExtension.test.ts 4.10ms
1402/1641 src/utils/files/getFileExtension.ts 3.53ms
1403/1641 src/utils/files/isDirectoryExisting.test.ts 2.14ms
1404/1641 src/utils/files/isDirectoryExisting.ts 2.68ms
1405/1641 src/utils/files/isExecutable.ts 2.84ms
1406/1641 src/utils/files/isFileExisting.test.ts 2.94ms
1407/1641 src/utils/files/isFileExisting.ts 2.69ms
1408/1641 src/utils/files/listAllFiles.test.ts 2.27ms
1409/1641 src/utils/files/listAllFiles.ts 9.51ms
1410/1641 src/utils/files/mimeTypeToExtension.test.ts 2.69ms
1411/1641 src/utils/files/mimeTypeToExtension.ts 5.08ms
1412/1641 src/utils/files/ObjectUrl.ts 4.32ms
1413/1641 src/utils/files/readResponseBytes.ts 3.10ms
1414/1641 src/utils/knowledge/inlineKnowledgeSource.test.ts 3.94ms
1415/1641 src/utils/knowledge/inlineKnowledgeSource.ts 9.61ms
1416/1641 src/utils/knowledge/simplifyKnowledgeLabel.test.ts 2.98ms
1417/1641 src/utils/knowledge/simplifyKnowledgeLabel.ts 10.62ms
1418/1641 src/utils/language/getBrowserPreferredSpeechRecognitionLanguage.ts 7.94ms
1419/1641 src/utils/linguistic-hash/linguisticHash.test.ts 4.74ms
1420/1641 src/utils/linguistic-hash/linguisticHash.ts 3.42ms
1421/1641 src/utils/linguistic-hash/LinguisticHashLanguage.ts 3.39ms
1422/1641 src/utils/linguistic-hash/linguisticHashTypes.ts 2.04ms
1423/1641 src/utils/linguistic-hash/linguisticHashWordCount.ts 2.53ms
1424/1641 src/utils/linguistic-hash/linguisticHashWords.cs.ts 4.56ms
1425/1641 src/utils/linguistic-hash/linguisticHashWords.en.ts 37.47ms
1426/1641 src/utils/linguistic-hash/linguisticHashWordSelection.ts 4.19ms
1427/1641 src/utils/markdown/addAutoGeneratedSection.test.ts 4.27ms
1428/1641 src/utils/markdown/addAutoGeneratedSection.ts 3.91ms
1429/1641 src/utils/markdown/createMarkdownChart.test.ts 3.94ms
1430/1641 src/utils/markdown/createMarkdownChart.ts 4.71ms
1431/1641 src/utils/markdown/createMarkdownTable.test.ts 6.39ms
1432/1641 src/utils/markdown/createMarkdownTable.ts 3.83ms
1433/1641 src/utils/markdown/escapeMarkdownBlock.test.ts 2.15ms
1434/1641 src/utils/markdown/escapeMarkdownBlock.ts 2.15ms
1435/1641 src/utils/markdown/extractAllBlocksFromMarkdown-real.test.ts 3.36ms
1436/1641 src/utils/markdown/extractAllBlocksFromMarkdown.test.ts 14.56ms
1437/1641 src/utils/markdown/extractAllBlocksFromMarkdown.ts 4.70ms
1438/1641 src/utils/markdown/extractAllListItemsFromMarkdown.test.ts 5.28ms
1439/1641 src/utils/markdown/extractAllListItemsFromMarkdown.ts 2.82ms
1440/1641 src/utils/markdown/extractOneBlockFromMarkdown.test.ts 6.66ms
1441/1641 src/utils/markdown/extractOneBlockFromMarkdown.ts 5.20ms
1442/1641 src/utils/markdown/flattenMarkdown.test.ts 5.48ms
1443/1641 src/utils/markdown/flattenMarkdown.ts 7.90ms
1444/1641 src/utils/markdown/humanizeAiText.test.ts 11.27ms
1445/1641 src/utils/markdown/humanizeAiText.ts 3.97ms
1446/1641 src/utils/markdown/humanizeAiTextEllipsis.ts 2.49ms
1447/1641 src/utils/markdown/humanizeAiTextEmdashed.ts 3.12ms
1448/1641 src/utils/markdown/humanizeAiTextQuotes.ts 6.53ms
1449/1641 src/utils/markdown/humanizeAiTextSources.ts 5.67ms
1450/1641 src/utils/markdown/humanizeAiTextWhitespace.ts 4.04ms
1451/1641 src/utils/markdown/parseMarkdownSection.test.ts 3.09ms
1452/1641 src/utils/markdown/parseMarkdownSection.ts 3.77ms
1453/1641 src/utils/markdown/prettifyMarkdown.ts 2.61ms
1454/1641 src/utils/markdown/promptbookifyAiText.test.ts 4.36ms
1455/1641 src/utils/markdown/promptbookifyAiText.ts 5.14ms
1456/1641 src/utils/markdown/README.md 8.96ms
1457/1641 src/utils/markdown/removeMarkdownComments.test.ts 3.13ms
1458/1641 src/utils/markdown/removeMarkdownComments.ts 2.96ms
1459/1641 src/utils/markdown/removeMarkdownFormatting.test.ts 3.79ms
1460/1641 src/utils/markdown/removeMarkdownFormatting.ts 1.93ms
1461/1641 src/utils/markdown/removeMarkdownLinks.test.ts 2.21ms
1462/1641 src/utils/markdown/removeMarkdownLinks.ts 2.06ms
1463/1641 src/utils/markdown/splitMarkdownIntoSections.test.ts 3.29ms
1464/1641 src/utils/markdown/splitMarkdownIntoSections.ts 3.11ms
1465/1641 src/utils/markdown/trimCodeBlock.test.ts 3.89ms
1466/1641 src/utils/markdown/trimCodeBlock.ts 2.13ms
1467/1641 src/utils/markdown/trimEndOfCodeBlock.test.ts 2.23ms
1468/1641 src/utils/markdown/trimEndOfCodeBlock.ts 2.10ms
1469/1641 src/utils/misc/$getCurrentDate.ts 2.19ms
1470/1641 src/utils/misc/$Register.ts 4.87ms
1471/1641 src/utils/misc/aboutPromptbookInformation.ts 7.39ms
1472/1641 src/utils/misc/arrayableToArray.test.ts 2.54ms
1473/1641 src/utils/misc/arrayableToArray.ts 2.16ms
1474/1641 src/utils/misc/computeHash.test.ts 1.94ms
1475/1641 src/utils/misc/computeHash.ts 2.23ms
1476/1641 src/utils/misc/debounce.ts 2.10ms
1477/1641 src/utils/misc/emojis.ts 5.27ms
1478/1641 src/utils/misc/FromtoItems.ts 3.26ms
1479/1641 src/utils/misc/injectCssModuleIntoShadowRoot.tsx 4.28ms
1480/1641 src/utils/misc/parseNumber.test.ts 3.12ms
1481/1641 src/utils/misc/parseNumber.ts 3.55ms
1482/1641 src/utils/misc/xAboutPromptbookInformation.tsx 6.16ms
1483/1641 src/utils/normalization/capitalize.test.ts 1.92ms
1484/1641 src/utils/normalization/capitalize.ts 1.65ms
1485/1641 src/utils/normalization/constructImageFilename.test.ts 5.58ms
1486/1641 src/utils/normalization/constructImageFilename.ts 3.39ms
1487/1641 src/utils/normalization/decapitalize.test.ts 4.26ms
1488/1641 src/utils/normalization/decapitalize.ts 3.16ms
1489/1641 src/utils/normalization/DIACRITIC_VARIANTS_LETTERS.ts 5.16ms
1490/1641 src/utils/normalization/IKeywords.ts 1.71ms
1491/1641 src/utils/normalization/index.ts 2.01ms
1492/1641 src/utils/normalization/isValidKeyword.test.ts 2.38ms
1493/1641 src/utils/normalization/isValidKeyword.ts 2.17ms
1494/1641 src/utils/normalization/nameToUriPart.test.ts 3.02ms
1495/1641 src/utils/normalization/nameToUriPart.ts 6.88ms
1496/1641 src/utils/normalization/nameToUriParts.test.ts 11.47ms
1497/1641 src/utils/normalization/nameToUriParts.ts 2.54ms
1498/1641 src/utils/normalization/normalize-to-kebab-case.test.ts 5.36ms
1499/1641 src/utils/normalization/normalize-to-kebab-case.ts 10.69ms
1500/1641 src/utils/normalization/normalizeMessageText.test.ts 2.51ms
1501/1641 src/utils/normalization/normalizeMessageText.ts 2.59ms
1502/1641 src/utils/normalization/normalizeTo_camelCase.test.ts 6.75ms
1503/1641 src/utils/normalization/normalizeTo_camelCase.ts 10.74ms
1504/1641 src/utils/normalization/normalizeTo_PascalCase.test.ts 2.58ms
1505/1641 src/utils/normalization/normalizeTo_PascalCase.ts 4.46ms
1506/1641 src/utils/normalization/normalizeTo_SCREAMING_CASE.test.ts 2.48ms
1507/1641 src/utils/normalization/normalizeTo_SCREAMING_CASE.ts 32.01ms
1508/1641 src/utils/normalization/normalizeTo_snake_case.test.ts 6.92ms
1509/1641 src/utils/normalization/normalizeTo_snake_case.ts 3.88ms
1510/1641 src/utils/normalization/normalizeWhitespaces.test.ts 13.21ms
1511/1641 src/utils/normalization/normalizeWhitespaces.ts 2.30ms
1512/1641 src/utils/normalization/orderJson.test.ts 8.17ms
1513/1641 src/utils/normalization/orderJson.ts 2.36ms
1514/1641 src/utils/normalization/parseKeywords.test.ts 3.87ms
1515/1641 src/utils/normalization/parseKeywords.ts 4.64ms
1516/1641 src/utils/normalization/parseKeywordsFromString.test.ts 4.32ms
1517/1641 src/utils/normalization/parseKeywordsFromString.ts 2.25ms
1518/1641 src/utils/normalization/removeDiacritics.test.ts 2.59ms
1519/1641 src/utils/normalization/removeDiacritics.ts 2.12ms
1520/1641 src/utils/normalization/removeEmojis.test.ts 2.48ms
1521/1641 src/utils/normalization/removeEmojis.ts 1.86ms
1522/1641 src/utils/normalization/removeQuotes.test.ts 2.12ms
1523/1641 src/utils/normalization/removeQuotes.ts 4.16ms
1524/1641 src/utils/normalization/searchKeywords.test.ts 2.15ms
1525/1641 src/utils/normalization/searchKeywords.ts 3.27ms
1526/1641 src/utils/normalization/suffixUrl.test.ts 4.31ms
1527/1641 src/utils/normalization/suffixUrl.ts 1.87ms
1528/1641 src/utils/normalization/titleToName.test.ts 2.47ms
1529/1641 src/utils/normalization/titleToName.ts 3.60ms
1530/1641 src/utils/normalization/unwrapResult.test.ts 6.94ms
1531/1641 src/utils/normalization/unwrapResult.ts 7.28ms
1532/1641 src/utils/organization/___and___.ts 1.64ms
1533/1641 src/utils/organization/___or___.ts 1.24ms
1534/1641 src/utils/organization/$side_effect.ts 2.06ms
1535/1641 src/utils/organization/$sideEffect.ts 2.10ms
1536/1641 src/utils/organization/empty_object.ts 2.07ms
1537/1641 src/utils/organization/just_empty_object.ts 1.83ms
1538/1641 src/utils/organization/just.ts 1.88ms
1539/1641 src/utils/organization/keepImported.ts 2.04ms
1540/1641 src/utils/organization/keepTypeImported.ts 2.01ms
1541/1641 src/utils/organization/keepUnused.ts 2.50ms
1542/1641 src/utils/organization/preserve.ts 2.55ms
1543/1641 src/utils/organization/README.md 1.56ms
1544/1641 src/utils/organization/really_any.ts 1.84ms
1545/1641 src/utils/organization/really_unknown.ts 1.89ms
1546/1641 src/utils/organization/spaceTrim.ts 2.04ms
1547/1641 src/utils/organization/TODO_any.ts 1.59ms
1548/1641 src/utils/organization/TODO_narrow.ts 2.37ms
1549/1641 src/utils/organization/TODO_object.ts 1.36ms
1550/1641 src/utils/organization/TODO_remove_as.ts 1.50ms
1551/1641 src/utils/organization/TODO_string.ts 1.59ms
1552/1641 src/utils/organization/TODO_unknown.ts 1.79ms
1553/1641 src/utils/organization/TODO_USE.ts 1.95ms
1554/1641 src/utils/parameters/extractParameterNames.test.ts 3.16ms
1555/1641 src/utils/parameters/extractParameterNames.ts 2.65ms
1556/1641 src/utils/parameters/mapAvailableToExpectedParameters.test.ts 3.67ms
1557/1641 src/utils/parameters/mapAvailableToExpectedParameters.ts 4.98ms
1558/1641 src/utils/parameters/numberToString.test.ts 1.82ms
1559/1641 src/utils/parameters/numberToString.ts 2.33ms
1560/1641 src/utils/parameters/templateParameters.test.ts 10.22ms
1561/1641 src/utils/parameters/templateParameters.ts 6.40ms
1562/1641 src/utils/parameters/valueToString.test.ts 2.21ms
1563/1641 src/utils/parameters/valueToString.ts 3.50ms
1564/1641 src/utils/random/$generateBookBoilerplate.ts 5.31ms
1565/1641 src/utils/random/$randomAgentPersona.ts 3.36ms
1566/1641 src/utils/random/$randomAgentRule.ts 2.94ms
1567/1641 src/utils/random/$randomBase58.ts 4.74ms
1568/1641 src/utils/random/$randomFullnameWithColor.ts 3.85ms
1569/1641 src/utils/random/$randomItem.ts 2.28ms
1570/1641 src/utils/random/$randomSeed.ts 2.67ms
1571/1641 src/utils/random/$randomToken.ts 4.52ms
1572/1641 src/utils/random/CzechNamePool.ts 14.97ms
1573/1641 src/utils/random/EnglishNamePool.ts 9.08ms
1574/1641 src/utils/random/getNamePool.ts 2.46ms
1575/1641 src/utils/random/NamePool.ts 2.29ms
1576/1641 src/utils/serialization/$deepFreeze.ts 7.95ms
1577/1641 src/utils/serialization/asSerializable.test.ts 3.56ms
1578/1641 src/utils/serialization/asSerializable.ts 3.85ms
1579/1641 src/utils/serialization/checkSerializableAsJson.ts 10.12ms
1580/1641 src/utils/serialization/clonePipeline.ts 8.65ms
1581/1641 src/utils/serialization/deepClone.test.ts 7.52ms
1582/1641 src/utils/serialization/deepClone.ts 3.74ms
1583/1641 src/utils/serialization/exportJson.ts 4.72ms
1584/1641 src/utils/serialization/isSerializableAsJson.test.ts 2.99ms
1585/1641 src/utils/serialization/isSerializableAsJson.ts 5.22ms
1586/1641 src/utils/serialization/jsonStringsToJsons.test.ts 6.05ms
1587/1641 src/utils/serialization/jsonStringsToJsons.ts 3.46ms
1588/1641 src/utils/serialization/serializeToPromptbookJavascript.ts 4.43ms
1589/1641 src/utils/sets/difference.test.ts 4.69ms
1590/1641 src/utils/sets/difference.ts 2.03ms
1591/1641 src/utils/sets/intersection.test.ts 1.61ms
1592/1641 src/utils/sets/intersection.ts 1.74ms
1593/1641 src/utils/sets/union.test.ts 1.72ms
1594/1641 src/utils/sets/union.ts 1.67ms
1595/1641 src/utils/take/classes/TakeChain.ts 2.00ms
1596/1641 src/utils/take/interfaces/ITakeChain.ts 2.05ms
1597/1641 src/utils/take/interfaces/Takeable.ts 3.94ms
1598/1641 src/utils/take/take.test.ts 3.88ms
1599/1641 src/utils/take/take.ts 2.36ms
1600/1641 src/utils/toolCalls/getToolCallIdentity.ts 2.25ms
1601/1641 src/utils/toolCalls/mergeToolCalls.ts 11.29ms
1602/1641 src/utils/toolCalls/resolveToolCallIdempotencyKey.ts 4.46ms
1603/1641 src/utils/validators/email/isValidEmail.test.ts 2.29ms
1604/1641 src/utils/validators/email/isValidEmail.ts 1.71ms
1605/1641 src/utils/validators/filePath/isRootPath.test.ts 1.59ms
1606/1641 src/utils/validators/filePath/isRootPath.ts 1.67ms
1607/1641 src/utils/validators/filePath/isValidFilePath.test.ts 6.41ms
1608/1641 src/utils/validators/filePath/isValidFilePath.ts 4.32ms
1609/1641 src/utils/validators/javascriptName/isValidJavascriptName.test.ts 5.66ms
1610/1641 src/utils/validators/javascriptName/isValidJavascriptName.ts 3.26ms
1611/1641 src/utils/validators/parameterName/validateParameterName.test.ts 2.95ms
1612/1641 src/utils/validators/parameterName/validateParameterName.ts 5.83ms
1613/1641 src/utils/validators/semanticVersion/isValidPromptbookVersion.test.ts 1.91ms
1614/1641 src/utils/validators/semanticVersion/isValidPromptbookVersion.ts 2.96ms
1615/1641 src/utils/validators/semanticVersion/isValidSemanticVersion.test.ts 1.87ms
1616/1641 src/utils/validators/semanticVersion/isValidSemanticVersion.ts 3.88ms
1617/1641 src/utils/validators/url/extractUrlsFromText.test.ts 2.87ms
1618/1641 src/utils/validators/url/extractUrlsFromText.ts 4.13ms
1619/1641 src/utils/validators/url/isHostnameOnPrivateNetwork.test.ts 2.12ms
1620/1641 src/utils/validators/url/isHostnameOnPrivateNetwork.ts 3.03ms
1621/1641 src/utils/validators/url/isUrlOnPrivateNetwork.test.ts 3.06ms
1622/1641 src/utils/validators/url/isUrlOnPrivateNetwork.ts 2.32ms
1623/1641 src/utils/validators/url/isValidAgentUrl.test.ts 3.23ms
1624/1641 src/utils/validators/url/isValidAgentUrl.ts 3.62ms
1625/1641 src/utils/validators/url/isValidPipelineUrl.test.ts 2.05ms
1626/1641 src/utils/validators/url/isValidPipelineUrl.ts 2.45ms
1627/1641 src/utils/validators/url/isValidUrl.test.ts 2.57ms
1628/1641 src/utils/validators/url/isValidUrl.ts 2.80ms
1629/1641 src/utils/validators/url/normalizeDomainForMatching.test.ts 2.93ms
1630/1641 src/utils/validators/url/normalizeDomainForMatching.ts 6.48ms
1631/1641 src/utils/validators/uuid/isValidUuid.test.ts 2.62ms
1632/1641 src/utils/validators/uuid/isValidUuid.ts 1.99ms
1633/1641 src/version.ts 1.98ms
1634/1641 src/wizard/$getCompiledBook.ts 18.04ms
1635/1641 src/wizard/test/books/test.book.md 1.95ms
1636/1641 src/wizard/test/README.md 1.47ms
1637/1641 src/wizard/test/sub/books/test.book.md 1.60ms
1638/1641 src/wizard/test/sub/subsub/books/test.book.md 1.84ms
1639/1641 src/wizard/test/sub/subsub/subsubsub/books/test.book.md 1.65ms
1640/1641 src/wizard/test/sub/subsub/subsubsub/README.md 1.77ms
1641/1641 src/wizard/wizard.ts 7.74ms
CSpell: Files checked: 1641, Issues found: 0 in 0 files.

> promptbook@0.112.0-45 test-lint
> eslint src


> promptbook@0.112.0-45 test-types
> tsc


> promptbook@0.112.0-45 test-books
> echo "Note: [⚡️] Skipping the book test because it tests just the old pipeline books which are deprecated"

"Note: [⚡️] Skipping the book test because it tests just the old pipeline books which are deprecated"

> promptbook@0.112.0-45 test-book-components-build
> echo "Note: Skipping book components build tests because of constants problems with this test"

"Note: Skipping book components build tests because of constants problems with this test"

> promptbook@0.112.0-45 test-package-generation
> ts-node ./scripts/generate-packages/generate-packages.ts --skip-bundler

📦  Generating packages
[36m0️⃣  Prepare the needed information about the packages[39m
Promptbook version 0.112.0-45
[36m1️⃣  Generate entry file for each package[39m
[32mGenerated index file ./src/_packages/anthropic-claude.index.ts[39m
[32mGenerated index file ./src/_packages/azure-openai.index.ts[39m
[32mGenerated index file ./src/_packages/browser.index.ts[39m
[32mGenerated index file ./src/_packages/cli.index.ts[39m
[32mGenerated index file ./src/_packages/color.index.ts[39m
[32mGenerated index file ./src/_packages/components.index.ts[39m
[32mGenerated index file ./src/_packages/core.index.ts[39m
[32mGenerated index file ./src/_packages/deepseek.index.ts[39m
[32mGenerated index file ./src/_packages/documents.index.ts[39m
[32mGenerated index file ./src/_packages/editable.index.ts[39m
[32mGenerated index file ./src/_packages/fake-llm.index.ts[39m
[32mGenerated index file ./src/_packages/google.index.ts[39m
[32mGenerated index file ./src/_packages/javascript.index.ts[39m
[32mGenerated index file ./src/_packages/legacy-documents.index.ts[39m
[32mGenerated index file ./src/_packages/markdown-utils.index.ts[39m
[32mGenerated index file ./src/_packages/markitdown.index.ts[39m
[32mGenerated index file ./src/_packages/node.index.ts[39m
[32mGenerated index file ./src/_packages/ollama.index.ts[39m
[32mGenerated index file ./src/_packages/openai.index.ts[39m
[32mGenerated index file ./src/_packages/pdf.index.ts[39m
[32mGenerated index file ./src/_packages/remote-client.index.ts[39m
[32mGenerated index file ./src/_packages/remote-server.index.ts[39m
[32mGenerated index file ./src/_packages/templates.index.ts[39m
[32mGenerated index file ./src/_packages/types.index.ts[39m
[32mGenerated index file ./src/_packages/utils.index.ts[39m
[32mGenerated index file ./src/_packages/vercel.index.ts[39m
[32mGenerated index file ./src/_packages/website-crawler.index.ts[39m
[32mGenerated index file ./src/_packages/wizard.index.ts[39m
[36m2️⃣  Generate package.json, README and other crucial files for each package[39m
[36m3️⃣  Cleanup build directories for each package[39m
[33mSkipping the cleanup for bundler[39m
[36m4️⃣  Generate bundle for each package[39m
[33mSkipping the bundler[39m
[36m5️⃣  Postprocess the generated bundle[39m
[33mSkipping postprocessing[39m
[36m6️⃣  Test that nothing what should not be published is published[39m
[33mSkipping the bundler, skipping the tests for bundle content[39m
[36m7️⃣  Add dependencies for each package[39m
[36m9️⃣  Make publishing instructions for Github Actions[39m

> promptbook@0.112.0-45 test-unit
> jest --forceExit

(node:51676) [DEP0040] DeprecationWarning: The `punycode` module is deprecated. Please use a userland alternative instead.
(Use `node --trace-deprecation ...` to show where the warning was created)
PASS apps/agents-server/src/utils/userChat/heartbeatUserChatJob.test.ts (11.652 s)
  ●  process.exit called with "0"

    [0m [90m 23 |[39m         [36mtry[39m {
     [90m 24 |[39m             [36mawait[39m action([33m...[39margs)[33m;[39m
    [31m[1m>[22m[39m[90m 25 |[39m             [36mreturn[39m process[33m.[39mexit([35m0[39m)[33m;[39m
     [90m    |[39m                            [31m[1m^[22m[39m
     [90m 26 |[39m         } [36mcatch[39m (error) {
     [90m 27 |[39m             assertsError(error)[33m;[39m
     [90m 28 |[39m[0m

      at Command.exit (src/cli/cli-commands/common/handleActionErrors.ts:25:28)
      at Command.parseAsync (node_modules/commander/lib/command.js:1015:5)
      at Object.<anonymous> (src/cli/test/ptbk.test.ts:63:13)

> lint
> eslint src/ --max-warnings=0


> pretest-build
> npx kill-port 4021

Process on port 4021 killed

> test-build
> next build && node ./scripts/prerender-homepage.js

   ▲ Next.js 15.4.11
   - Environments: .env
   - Experiments (use with caution):
     ✓ externalDir

   Creating an optimized production build ...
 ✓ Compiled successfully in 2.0min
   Linting and checking validity of types ...
   Collecting page data ...
(node:40928) [DEP0040] DeprecationWarning: The `punycode` module is deprecated. Please use a userland alternative instead.
(Use `node --trace-deprecation ...` to show where the warning was created)
(node:6256) [DEP0040] DeprecationWarning: The `punycode` module is deprecated. Please use a userland alternative instead.
(Use `node --trace-deprecation ...` to show where the warning was created)
(node:53900) [DEP0040] DeprecationWarning: The `punycode` module is deprecated. Please use a userland alternative instead.
(Use `node --trace-deprecation ...` to show where the warning was created)
(node:30084) [DEP0040] DeprecationWarning: The `punycode` module is deprecated. Please use a userland alternative instead.
(Use `node --trace-deprecation ...` to show where the warning was created)
(node:22748) [DEP0040] DeprecationWarning: The `punycode` module is deprecated. Please use a userland alternative instead.
(Use `node --trace-deprecation ...` to show where the warning was created)
 ⚠ Using edge runtime on a page currently disables static generation for that page
   Generating static pages (0/109) ...
   Generating static pages (27/109) 
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /admin/backup couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/backup couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /admin/backup couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/backup couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /admin/about couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/about couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /admin/about couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/about couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /admin/browser-test couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/browser-test couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /admin/browser-test couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/browser-test couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /admin/chat-history couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/chat-history couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /admin/chat-history couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/chat-history couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /admin/custom-css couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/custom-css couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /admin/custom-css couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/custom-css couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /admin/custom-js couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/custom-js couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /admin/custom-js couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/custom-js couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /admin/error-simulation couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/error-simulation couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /admin/error-simulation couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/error-simulation couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /admin/files couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/files couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /admin/files couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/files couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /admin/chat-feedback couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/chat-feedback couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /admin/chat-feedback couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/chat-feedback couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /admin/image-generator-test couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/image-generator-test couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /admin/image-generator-test couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/image-generator-test couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /admin/limits couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/limits couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /admin/limits couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/limits couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /admin/messages/send-email couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/messages/send-email couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /admin/messages/send-email couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/messages/send-email couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /admin/messages couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/messages couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /admin/messages couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/messages couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /admin/metadata couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/metadata couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /admin/metadata couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/metadata couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /admin/servers couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/servers couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /admin/servers couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/servers couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /admin/search-engine-test couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/search-engine-test couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /admin/search-engine-test couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/search-engine-test couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /admin/tool-limits couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/tool-limits couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /admin/tool-limits couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/tool-limits couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /admin/task-manager couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/task-manager couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /admin/task-manager couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/task-manager couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /admin/transcriptions couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/transcriptions couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /admin/transcriptions couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/transcriptions couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /admin/usage couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/usage couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /admin/usage couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/usage couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /admin/users couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/users couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /admin/users couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/users couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /admin/voice-input-test couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/voice-input-test couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /admin/voice-input-test couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/voice-input-test couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
(node:45296) [DEP0040] DeprecationWarning: The `punycode` module is deprecated. Please use a userland alternative instead.
(Use `node --trace-deprecation ...` to show where the warning was created)
   Generating static pages (54/109) 
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /admin/api-tokens couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/api-tokens couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /admin/api-tokens couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/api-tokens couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /_not-found couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /_not-found couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /_not-found couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /_not-found couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
   Generating static pages (81/109) 
(node:28456) [DEP0040] DeprecationWarning: The `punycode` module is deprecated. Please use a userland alternative instead.
(Use `node --trace-deprecation ...` to show where the warning was created)
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /admin/models couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/models couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /admin/models couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/models couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
(node:33312) [DEP0040] DeprecationWarning: The `punycode` module is deprecated. Please use a userland alternative instead.
(Use `node --trace-deprecation ...` to show where the warning was created)
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /docs couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /docs couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /docs couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /docs couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /experiments/story couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /experiments/story couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /experiments/story couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /experiments/story couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /agents couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /agents couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /agents couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /agents couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route / couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route / couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route / couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route / couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /recycle-bin couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /recycle-bin couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /recycle-bin couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /recycle-bin couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /restricted couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /restricted couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /restricted couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /restricted couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /search couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /search couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /search couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /search couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /swagger couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /swagger couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /swagger couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /swagger couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /system/profile couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /system/profile couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /system/profile couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /system/profile couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /system/utilities/mocked-chats couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /system/utilities/mocked-chats couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /system/utilities/mocked-chats couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /system/utilities/mocked-chats couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /system/user-memory couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /system/user-memory couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /system/user-memory couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /system/user-memory couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /system/utilities/mocked-chats/view couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /system/utilities/mocked-chats/view couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /system/utilities/mocked-chats/view couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /system/utilities/mocked-chats/view couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /system/user-wallet couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /system/user-wallet couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /system/user-wallet couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /system/user-wallet couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /system/settings couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /system/settings couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /system/settings couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /system/settings couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /system/utilities couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /system/utilities couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /system/utilities couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /system/utilities couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /test/og-image couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /test/og-image couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /test/og-image couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /test/og-image couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /admin/images couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/images couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /admin/images couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /admin/images couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom stylesheet CSS Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /embed couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Failed to load custom JavaScript Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /embed couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
}
Unhandled rejection Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `cookies`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:9504)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\10.js:1:3122)
    at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\10.js:1:3179)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\10.js:180:307)
    at j (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\10.js:180:953)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3534) {
  description: "Route /embed couldn't be rendered statically because it used `cookies`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
} Promise {
  <rejected> Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `cookies`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
      at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
      at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:9504)
      at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\10.js:1:3122)
      at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\10.js:1:3179)
      at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\10.js:180:307)
      at j (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\10.js:180:953)
      at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3534) {
    description: "Route /embed couldn't be rendered statically because it used `cookies`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
    digest: 'DYNAMIC_SERVER_USAGE'
  },
  [Symbol(async_id_symbol)]: 14121,
  [Symbol(trigger_async_id_symbol)]: 14103,
  [Symbol(kResourceStore)]: {
    isStaticGeneration: true,
    page: '/embed/page',
    fallbackRouteParams: null,
    route: '/embed',
    incrementalCache: IncrementalCache {
      locks: Map(0) {},
      hasCustomCacheHandler: false,
      dev: false,
      disableForTestmode: false,
      minimalMode: false,
      requestHeaders: {},
      allowedRevalidateHeaderKeys: undefined,
      prerenderManifest: [Object],
      cacheControls: [SharedCacheControls],
      fetchCacheKeyPrefix: '',
      cacheHandler: [FileSystemCache]
    },
    cacheLifeProfiles: {
      default: [Object],
      seconds: [Object],
      minutes: [Object],
      hours: [Object],
      days: [Object],
      weeks: [Object],
      max: [Object]
    },
    isRevalidate: undefined,
    isBuildTimePrerendering: true,
    hasReadableErrorStacks: false,
    fetchCache: undefined,
    isOnDemandRevalidate: undefined,
    isDraftMode: undefined,
    requestEndedState: { ended: false },
    isPrefetchRequest: false,
    buildId: 'senL24mes0wCj4J8hXgfg',
    reactLoadableManifest: {
      '..\\..\\..\\node_modules\\jspdf\\dist\\jspdf.es.min.js -> canvg': [Object],
      '..\\..\\..\\node_modules\\jspdf\\dist\\jspdf.es.min.js -> dompurify': [Object],
      '..\\..\\..\\node_modules\\jspdf\\dist\\jspdf.es.min.js -> html2canvas': [Object],
      '..\\..\\..\\node_modules\\next\\dist\\client\\index.js -> ../pages/_app': [Object],
      '..\\..\\..\\node_modules\\next\\dist\\client\\index.js -> ../pages/_error': [Object],
      '..\\..\\..\\src\\book-components\\Chat\\Chat\\ChatMessageMap.tsx -> leaflet': [Object],
      '..\\..\\..\\src\\book-components\\Qr\\PromptbookQrCode.tsx -> qrcode': [Object],
      '..\\..\\..\\src\\commitments\\TEAM\\TEAM.ts -> ../../llm-providers/agent/RemoteAgent': [Object],
      'app\\agents\\[agentName]\\DeferredAgentProfileChat.tsx -> ./AgentProfileChat': [Object],
      'app\\swagger\\SwaggerPageClient.tsx -> swagger-ui-react': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ../AgentContextMenu/AgentContextMenu': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ../FolderContextMenu/FolderContextMenu': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ./AgentQrCodeModal': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ./FolderEditDialog': [Object],
      'components\\Homepage\\AgentsListViewContent.tsx -> ./AgentsGraph': [Object],
      'components\\Homepage\\AgentsListViewContent.tsx -> ./AgentsOffice': [Object],
      'components\\Homepage\\AgentsListViewContent.tsx -> ./AgentsPixelOffice': [Object]
    },
    assetPrefix: '',
    afterContext: eJ {
      workUnitStores: Set(0) {},
      waitUntil: [Function: bound ],
      onClose: [Function: bound onClose],
      onTaskError: [Function: onTaskError],
      callbackQueue: [o]
    },
    dynamicIOEnabled: false,
    dev: false,
    previouslyRevalidatedTags: [],
    refreshTagsByCacheKind: Map(2) { 'default' => [Object], 'remote' => [Object] },
    runInCleanSnapshot: [Function: bound] { asyncResource: [Getter/Setter] },
    fetchMetrics: [],
    dynamicUsageDescription: 'cookies',
    dynamicUsageStack: "Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `cookies`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error\n" +
      '    at r (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next\\server\\chunks\\657.js:12:54555)\n' +
      '    at n (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next\\server\\chunks\\5208.js:1:9504)\n' +
      '    at N (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next\\server\\chunks\\4810.js:240:3768)\n' +
      '    at ek (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:32629)\n' +
      '    at e (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:36665)\n' +
      '    at eI (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:37127)\n' +
      '    at Array.toJSON (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:34252)\n' +
      '    at stringify (<anonymous>)\n' +
      '    at eH (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:46044)\n' +
      '    at ez (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:46422)'
  },
  [Symbol(kResourceStore)]: eg {
    type: 20,
    status: 14,
    flushScheduled: false,
    fatalError: null,
    destination: null,
    bundlerConfig: {
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\client-page.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\client-page.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\client-segment.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\client-segment.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\http-access-fallback\\error-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\http-access-fallback\\error-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\layout-router.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\layout-router.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\async-metadata.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\metadata\\async-metadata.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\metadata\\metadata-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\render-from-template-context.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\render-from-template-context.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\lib\\metadata\\generate\\icon-mark.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\lib\\metadata\\generate\\icon-mark.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\global-error.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\public\\favicon.ico': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\LayoutWrapper\\LayoutWrapper.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\font\\google\\target.css?{"path":"src\\\\utils\\\\applicationFonts.ts","import":"Barlow_Condensed","arguments":[{"subsets":["latin"],"weight":["300","400","500","600","700"],"display":"swap","fallback":["Arial","Helvetica","sans-serif"],"variable":"--font-barlow-condensed"}],"variableName":"barlowCondensed"}': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\font\\google\\target.css?{"path":"src\\\\utils\\\\applicationFonts.ts","import":"Poppins","arguments":[{"subsets":["latin"],"weight":["400","500","600","700","800"],"display":"swap","fallback":["Arial","Helvetica","sans-serif"],"variable":"--font-poppins"}],"variableName":"poppins"}': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\globals.css': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\error.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\app-dir\\link.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\app-dir\\link.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\AgentProfileWrapper.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\DeferredAgentProfileChat.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\AgentProfile\\AgentProfile.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\DeletedAgentBanner.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\api-tokens\\ApiTokensClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\ForbiddenPage\\ForbiddenPage.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\backup\\BackupClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\browser-test\\BrowserTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\chat-feedback\\ChatFeedbackClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\chat-history\\ChatHistoryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\custom-css\\CustomCssClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\error-simulation\\ErrorSimulationClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\files\\FilesGalleryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\image-generator-test\\ImageGeneratorTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\images\\ImagesGalleryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\limits\\LimitsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\custom-js\\CustomJsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\messages\\MessagesClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\messages\\send-email\\SendEmailClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\metadata\\MetadataClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\search-engine-test\\SearchEngineTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\servers\\ServersClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\task-manager\\TaskManagerClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\transcriptions\\TranscriptionsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\usage\\UsageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\users\\[userId]\\UserDetailClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\UsersList\\UsersList.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\voice-input-test\\VoiceInputTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\Homepage\\AgentsList.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\Homepage\\ExternalAgentsSectionClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\src\\_packages\\components.index.ts': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\DocsToolbar\\DocsToolbar.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\experiments\\story\\page.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\search\\SearchPageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\profile\\UserProfileClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\settings\\KeybindingsSettingsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\user-memory\\UserMemoryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\user-wallet\\UserWalletClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\utilities\\mocked-chats\\MockedChatsEditorClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\utilities\\mocked-chats\\view\\MockedChatsViewerClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\src\\book-components\\Chat\\MarkdownContent\\MarkdownContent.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\Homepage\\RecycleBinList.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\swagger\\SwaggerPageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\book+chat\\AgentBookAndChat.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\book\\BookEditorWrapper.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\chat\\loading.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\chat\\AgentChatHistoryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\export-as-transpiled-code\\AgentCodePageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\history\\RestoreVersionButton.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\_common\\components\\CodePreview\\CodePreview.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\ApiKeyIntegrationSections.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\CalendarIntegrationSection.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\PromptbookSdkTabs.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\WebsiteIntegrationTabs.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\system-message\\SystemMessageBookEditor.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\textarea\\AgentTextareaClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\timeouts\\AgentTimeoutsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\embed\\page.tsx': [Object]
    },
    cache: Map(1) { [Function: E] => [WeakMap] },
    cacheController: AbortController { signal: AbortSignal { aborted: true } },
    nextChunkId: 30,
    pendingChunks: 0,
    hints: Set(6) {
      'L[style]/_next/static/css/de7c0ac3e7516811.css',
      'L[style]/_next/static/css/ed0f0e0535a22473.css',
      'L[style]/_next/static/css/2de9726a3c08d309.css',
      'L[style]/_next/static/css/07199a57b6717f23.css',
      'L[style]/_next/static/css/b1f7517bfd677245.css',
      'L[style]/_next/static/css/950d264b0bfdfd57.css'
    },
    abortableTasks: Set(0) {},
    pingedTasks: [],
    completedImportChunks: [],
    completedHintChunks: [],
    completedRegularChunks: [],
    completedErrorChunks: [],
    writtenSymbols: Map(2) {
      Symbol(react.fragment) => 1,
      Symbol(react.suspense) => 28
    },
    writtenClientReferences: Map(9) {
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\layout-router.js#' => 3,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\render-from-template-context.js#' => 4,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\client-page.js#ClientPageRoot' => 5,
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\embed\\page.tsx#default' => 6,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js#OutletBoundary' => 9,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\async-metadata.js#AsyncMetadataOutlet' => 11,
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\global-error.tsx#default' => 22,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js#ViewportBoundary' => 25,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js#MetadataBoundary' => 27
    },
    writtenServerReferences: Map(0) {},
    writtenObjects: WeakMap { <items unknown> },
    temporaryReferences: undefined,
    identifierPrefix: '',
    identifierCount: 1,
    taintCleanupQueue: [],
    onError: [Function (anonymous)],
    onPostpone: [Function: X],
    onAllReady: [Function: X],
    onFatalError: [Function: X]
  },
  [Symbol(kResourceStore)]: undefined,
  [Symbol(kResourceStore)]: {
    type: 'prerender-legacy',
    phase: 'render',
    rootParams: {},
    implicitTags: { tags: [Array], expirationsByCacheKind: [Map] },
    revalidate: 0,
    expire: 4294967294,
    stale: 4294967294,
    tags: [
      '_N_T_/layout',
      '_N_T_/embed/layout',
      '_N_T_/embed/page',
      '_N_T_/embed'
    ]
  }
}
Unhandled rejection Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `cookies`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:9504)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\10.js:1:3122)
    at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\10.js:1:3179)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\10.js:180:307)
    at j (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\10.js:180:953)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3534) {
  description: "Route /embed couldn't be rendered statically because it used `cookies`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
} Promise {
  <rejected> Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `cookies`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
      at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
      at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:9504)
      at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\10.js:1:3122)
      at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\10.js:1:3179)
      at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\10.js:180:307)
      at j (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\10.js:180:953)
      at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3534) {
    description: "Route /embed couldn't be rendered statically because it used `cookies`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
    digest: 'DYNAMIC_SERVER_USAGE'
  },
  [Symbol(async_id_symbol)]: 14130,
  [Symbol(trigger_async_id_symbol)]: 14103,
  [Symbol(kResourceStore)]: {
    isStaticGeneration: true,
    page: '/embed/page',
    fallbackRouteParams: null,
    route: '/embed',
    incrementalCache: IncrementalCache {
      locks: Map(0) {},
      hasCustomCacheHandler: false,
      dev: false,
      disableForTestmode: false,
      minimalMode: false,
      requestHeaders: {},
      allowedRevalidateHeaderKeys: undefined,
      prerenderManifest: [Object],
      cacheControls: [SharedCacheControls],
      fetchCacheKeyPrefix: '',
      cacheHandler: [FileSystemCache]
    },
    cacheLifeProfiles: {
      default: [Object],
      seconds: [Object],
      minutes: [Object],
      hours: [Object],
      days: [Object],
      weeks: [Object],
      max: [Object]
    },
    isRevalidate: undefined,
    isBuildTimePrerendering: true,
    hasReadableErrorStacks: false,
    fetchCache: undefined,
    isOnDemandRevalidate: undefined,
    isDraftMode: undefined,
    requestEndedState: { ended: false },
    isPrefetchRequest: false,
    buildId: 'senL24mes0wCj4J8hXgfg',
    reactLoadableManifest: {
      '..\\..\\..\\node_modules\\jspdf\\dist\\jspdf.es.min.js -> canvg': [Object],
      '..\\..\\..\\node_modules\\jspdf\\dist\\jspdf.es.min.js -> dompurify': [Object],
      '..\\..\\..\\node_modules\\jspdf\\dist\\jspdf.es.min.js -> html2canvas': [Object],
      '..\\..\\..\\node_modules\\next\\dist\\client\\index.js -> ../pages/_app': [Object],
      '..\\..\\..\\node_modules\\next\\dist\\client\\index.js -> ../pages/_error': [Object],
      '..\\..\\..\\src\\book-components\\Chat\\Chat\\ChatMessageMap.tsx -> leaflet': [Object],
      '..\\..\\..\\src\\book-components\\Qr\\PromptbookQrCode.tsx -> qrcode': [Object],
      '..\\..\\..\\src\\commitments\\TEAM\\TEAM.ts -> ../../llm-providers/agent/RemoteAgent': [Object],
      'app\\agents\\[agentName]\\DeferredAgentProfileChat.tsx -> ./AgentProfileChat': [Object],
      'app\\swagger\\SwaggerPageClient.tsx -> swagger-ui-react': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ../AgentContextMenu/AgentContextMenu': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ../FolderContextMenu/FolderContextMenu': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ./AgentQrCodeModal': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ./FolderEditDialog': [Object],
      'components\\Homepage\\AgentsListViewContent.tsx -> ./AgentsGraph': [Object],
      'components\\Homepage\\AgentsListViewContent.tsx -> ./AgentsOffice': [Object],
      'components\\Homepage\\AgentsListViewContent.tsx -> ./AgentsPixelOffice': [Object]
    },
    assetPrefix: '',
    afterContext: eJ {
      workUnitStores: Set(0) {},
      waitUntil: [Function: bound ],
      onClose: [Function: bound onClose],
      onTaskError: [Function: onTaskError],
      callbackQueue: [o]
    },
    dynamicIOEnabled: false,
    dev: false,
    previouslyRevalidatedTags: [],
    refreshTagsByCacheKind: Map(2) { 'default' => [Object], 'remote' => [Object] },
    runInCleanSnapshot: [Function: bound] { asyncResource: [Getter/Setter] },
    fetchMetrics: [],
    dynamicUsageDescription: 'cookies',
    dynamicUsageStack: "Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `cookies`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error\n" +
      '    at r (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next\\server\\chunks\\657.js:12:54555)\n' +
      '    at n (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next\\server\\chunks\\5208.js:1:9504)\n' +
      '    at N (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next\\server\\chunks\\4810.js:240:3768)\n' +
      '    at ek (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:32629)\n' +
      '    at e (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:36665)\n' +
      '    at eI (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:37127)\n' +
      '    at Array.toJSON (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:34252)\n' +
      '    at stringify (<anonymous>)\n' +
      '    at eH (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:46044)\n' +
      '    at ez (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:46422)'
  },
  [Symbol(kResourceStore)]: eg {
    type: 20,
    status: 14,
    flushScheduled: false,
    fatalError: null,
    destination: null,
    bundlerConfig: {
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\client-page.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\client-page.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\client-segment.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\client-segment.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\http-access-fallback\\error-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\http-access-fallback\\error-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\layout-router.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\layout-router.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\async-metadata.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\metadata\\async-metadata.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\metadata\\metadata-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\render-from-template-context.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\render-from-template-context.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\lib\\metadata\\generate\\icon-mark.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\lib\\metadata\\generate\\icon-mark.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\global-error.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\public\\favicon.ico': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\LayoutWrapper\\LayoutWrapper.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\font\\google\\target.css?{"path":"src\\\\utils\\\\applicationFonts.ts","import":"Barlow_Condensed","arguments":[{"subsets":["latin"],"weight":["300","400","500","600","700"],"display":"swap","fallback":["Arial","Helvetica","sans-serif"],"variable":"--font-barlow-condensed"}],"variableName":"barlowCondensed"}': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\font\\google\\target.css?{"path":"src\\\\utils\\\\applicationFonts.ts","import":"Poppins","arguments":[{"subsets":["latin"],"weight":["400","500","600","700","800"],"display":"swap","fallback":["Arial","Helvetica","sans-serif"],"variable":"--font-poppins"}],"variableName":"poppins"}': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\globals.css': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\error.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\app-dir\\link.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\app-dir\\link.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\AgentProfileWrapper.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\DeferredAgentProfileChat.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\AgentProfile\\AgentProfile.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\DeletedAgentBanner.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\api-tokens\\ApiTokensClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\ForbiddenPage\\ForbiddenPage.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\backup\\BackupClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\browser-test\\BrowserTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\chat-feedback\\ChatFeedbackClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\chat-history\\ChatHistoryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\custom-css\\CustomCssClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\error-simulation\\ErrorSimulationClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\files\\FilesGalleryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\image-generator-test\\ImageGeneratorTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\images\\ImagesGalleryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\limits\\LimitsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\custom-js\\CustomJsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\messages\\MessagesClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\messages\\send-email\\SendEmailClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\metadata\\MetadataClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\search-engine-test\\SearchEngineTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\servers\\ServersClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\task-manager\\TaskManagerClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\transcriptions\\TranscriptionsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\usage\\UsageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\users\\[userId]\\UserDetailClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\UsersList\\UsersList.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\voice-input-test\\VoiceInputTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\Homepage\\AgentsList.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\Homepage\\ExternalAgentsSectionClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\src\\_packages\\components.index.ts': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\DocsToolbar\\DocsToolbar.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\experiments\\story\\page.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\search\\SearchPageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\profile\\UserProfileClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\settings\\KeybindingsSettingsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\user-memory\\UserMemoryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\user-wallet\\UserWalletClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\utilities\\mocked-chats\\MockedChatsEditorClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\utilities\\mocked-chats\\view\\MockedChatsViewerClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\src\\book-components\\Chat\\MarkdownContent\\MarkdownContent.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\Homepage\\RecycleBinList.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\swagger\\SwaggerPageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\book+chat\\AgentBookAndChat.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\book\\BookEditorWrapper.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\chat\\loading.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\chat\\AgentChatHistoryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\export-as-transpiled-code\\AgentCodePageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\history\\RestoreVersionButton.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\_common\\components\\CodePreview\\CodePreview.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\ApiKeyIntegrationSections.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\CalendarIntegrationSection.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\PromptbookSdkTabs.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\WebsiteIntegrationTabs.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\system-message\\SystemMessageBookEditor.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\textarea\\AgentTextareaClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\timeouts\\AgentTimeoutsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\embed\\page.tsx': [Object]
    },
    cache: Map(1) { [Function: E] => [WeakMap] },
    cacheController: AbortController { signal: AbortSignal { aborted: true } },
    nextChunkId: 30,
    pendingChunks: 0,
    hints: Set(6) {
      'L[style]/_next/static/css/de7c0ac3e7516811.css',
      'L[style]/_next/static/css/ed0f0e0535a22473.css',
      'L[style]/_next/static/css/2de9726a3c08d309.css',
      'L[style]/_next/static/css/07199a57b6717f23.css',
      'L[style]/_next/static/css/b1f7517bfd677245.css',
      'L[style]/_next/static/css/950d264b0bfdfd57.css'
    },
    abortableTasks: Set(0) {},
    pingedTasks: [],
    completedImportChunks: [],
    completedHintChunks: [],
    completedRegularChunks: [],
    completedErrorChunks: [],
    writtenSymbols: Map(2) {
      Symbol(react.fragment) => 1,
      Symbol(react.suspense) => 28
    },
    writtenClientReferences: Map(9) {
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\layout-router.js#' => 3,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\render-from-template-context.js#' => 4,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\client-page.js#ClientPageRoot' => 5,
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\embed\\page.tsx#default' => 6,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js#OutletBoundary' => 9,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\async-metadata.js#AsyncMetadataOutlet' => 11,
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\global-error.tsx#default' => 22,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js#ViewportBoundary' => 25,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js#MetadataBoundary' => 27
    },
    writtenServerReferences: Map(0) {},
    writtenObjects: WeakMap { <items unknown> },
    temporaryReferences: undefined,
    identifierPrefix: '',
    identifierCount: 1,
    taintCleanupQueue: [],
    onError: [Function (anonymous)],
    onPostpone: [Function: X],
    onAllReady: [Function: X],
    onFatalError: [Function: X]
  },
  [Symbol(kResourceStore)]: undefined,
  [Symbol(kResourceStore)]: {
    type: 'prerender-legacy',
    phase: 'render',
    rootParams: {},
    implicitTags: { tags: [Array], expirationsByCacheKind: [Map] },
    revalidate: 0,
    expire: 4294967294,
    stale: 4294967294,
    tags: [
      '_N_T_/layout',
      '_N_T_/embed/layout',
      '_N_T_/embed/page',
      '_N_T_/embed'
    ]
  }
}
Unhandled rejection Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /embed couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
} Promise {
  <rejected> Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
      at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
      at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
      at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
      at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
      at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
      at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
      at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
      at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
    description: "Route /embed couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
    digest: 'DYNAMIC_SERVER_USAGE'
  },
  [Symbol(async_id_symbol)]: 14113,
  [Symbol(trigger_async_id_symbol)]: 14103,
  [Symbol(kResourceStore)]: {
    isStaticGeneration: true,
    page: '/embed/page',
    fallbackRouteParams: null,
    route: '/embed',
    incrementalCache: IncrementalCache {
      locks: Map(0) {},
      hasCustomCacheHandler: false,
      dev: false,
      disableForTestmode: false,
      minimalMode: false,
      requestHeaders: {},
      allowedRevalidateHeaderKeys: undefined,
      prerenderManifest: [Object],
      cacheControls: [SharedCacheControls],
      fetchCacheKeyPrefix: '',
      cacheHandler: [FileSystemCache]
    },
    cacheLifeProfiles: {
      default: [Object],
      seconds: [Object],
      minutes: [Object],
      hours: [Object],
      days: [Object],
      weeks: [Object],
      max: [Object]
    },
    isRevalidate: undefined,
    isBuildTimePrerendering: true,
    hasReadableErrorStacks: false,
    fetchCache: undefined,
    isOnDemandRevalidate: undefined,
    isDraftMode: undefined,
    requestEndedState: { ended: false },
    isPrefetchRequest: false,
    buildId: 'senL24mes0wCj4J8hXgfg',
    reactLoadableManifest: {
      '..\\..\\..\\node_modules\\jspdf\\dist\\jspdf.es.min.js -> canvg': [Object],
      '..\\..\\..\\node_modules\\jspdf\\dist\\jspdf.es.min.js -> dompurify': [Object],
      '..\\..\\..\\node_modules\\jspdf\\dist\\jspdf.es.min.js -> html2canvas': [Object],
      '..\\..\\..\\node_modules\\next\\dist\\client\\index.js -> ../pages/_app': [Object],
      '..\\..\\..\\node_modules\\next\\dist\\client\\index.js -> ../pages/_error': [Object],
      '..\\..\\..\\src\\book-components\\Chat\\Chat\\ChatMessageMap.tsx -> leaflet': [Object],
      '..\\..\\..\\src\\book-components\\Qr\\PromptbookQrCode.tsx -> qrcode': [Object],
      '..\\..\\..\\src\\commitments\\TEAM\\TEAM.ts -> ../../llm-providers/agent/RemoteAgent': [Object],
      'app\\agents\\[agentName]\\DeferredAgentProfileChat.tsx -> ./AgentProfileChat': [Object],
      'app\\swagger\\SwaggerPageClient.tsx -> swagger-ui-react': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ../AgentContextMenu/AgentContextMenu': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ../FolderContextMenu/FolderContextMenu': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ./AgentQrCodeModal': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ./FolderEditDialog': [Object],
      'components\\Homepage\\AgentsListViewContent.tsx -> ./AgentsGraph': [Object],
      'components\\Homepage\\AgentsListViewContent.tsx -> ./AgentsOffice': [Object],
      'components\\Homepage\\AgentsListViewContent.tsx -> ./AgentsPixelOffice': [Object]
    },
    assetPrefix: '',
    afterContext: eJ {
      workUnitStores: Set(0) {},
      waitUntil: [Function: bound ],
      onClose: [Function: bound onClose],
      onTaskError: [Function: onTaskError],
      callbackQueue: [o]
    },
    dynamicIOEnabled: false,
    dev: false,
    previouslyRevalidatedTags: [],
    refreshTagsByCacheKind: Map(2) { 'default' => [Object], 'remote' => [Object] },
    runInCleanSnapshot: [Function: bound] { asyncResource: [Getter/Setter] },
    fetchMetrics: [],
    dynamicUsageDescription: 'cookies',
    dynamicUsageStack: "Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `cookies`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error\n" +
      '    at r (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next\\server\\chunks\\657.js:12:54555)\n' +
      '    at n (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next\\server\\chunks\\5208.js:1:9504)\n' +
      '    at N (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next\\server\\chunks\\4810.js:240:3768)\n' +
      '    at ek (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:32629)\n' +
      '    at e (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:36665)\n' +
      '    at eI (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:37127)\n' +
      '    at Array.toJSON (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:34252)\n' +
      '    at stringify (<anonymous>)\n' +
      '    at eH (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:46044)\n' +
      '    at ez (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:46422)'
  },
  [Symbol(kResourceStore)]: eg {
    type: 20,
    status: 14,
    flushScheduled: false,
    fatalError: null,
    destination: null,
    bundlerConfig: {
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\client-page.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\client-page.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\client-segment.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\client-segment.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\http-access-fallback\\error-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\http-access-fallback\\error-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\layout-router.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\layout-router.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\async-metadata.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\metadata\\async-metadata.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\metadata\\metadata-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\render-from-template-context.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\render-from-template-context.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\lib\\metadata\\generate\\icon-mark.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\lib\\metadata\\generate\\icon-mark.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\global-error.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\public\\favicon.ico': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\LayoutWrapper\\LayoutWrapper.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\font\\google\\target.css?{"path":"src\\\\utils\\\\applicationFonts.ts","import":"Barlow_Condensed","arguments":[{"subsets":["latin"],"weight":["300","400","500","600","700"],"display":"swap","fallback":["Arial","Helvetica","sans-serif"],"variable":"--font-barlow-condensed"}],"variableName":"barlowCondensed"}': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\font\\google\\target.css?{"path":"src\\\\utils\\\\applicationFonts.ts","import":"Poppins","arguments":[{"subsets":["latin"],"weight":["400","500","600","700","800"],"display":"swap","fallback":["Arial","Helvetica","sans-serif"],"variable":"--font-poppins"}],"variableName":"poppins"}': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\globals.css': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\error.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\app-dir\\link.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\app-dir\\link.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\AgentProfileWrapper.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\DeferredAgentProfileChat.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\AgentProfile\\AgentProfile.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\DeletedAgentBanner.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\api-tokens\\ApiTokensClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\ForbiddenPage\\ForbiddenPage.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\backup\\BackupClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\browser-test\\BrowserTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\chat-feedback\\ChatFeedbackClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\chat-history\\ChatHistoryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\custom-css\\CustomCssClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\error-simulation\\ErrorSimulationClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\files\\FilesGalleryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\image-generator-test\\ImageGeneratorTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\images\\ImagesGalleryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\limits\\LimitsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\custom-js\\CustomJsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\messages\\MessagesClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\messages\\send-email\\SendEmailClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\metadata\\MetadataClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\search-engine-test\\SearchEngineTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\servers\\ServersClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\task-manager\\TaskManagerClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\transcriptions\\TranscriptionsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\usage\\UsageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\users\\[userId]\\UserDetailClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\UsersList\\UsersList.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\voice-input-test\\VoiceInputTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\Homepage\\AgentsList.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\Homepage\\ExternalAgentsSectionClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\src\\_packages\\components.index.ts': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\DocsToolbar\\DocsToolbar.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\experiments\\story\\page.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\search\\SearchPageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\profile\\UserProfileClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\settings\\KeybindingsSettingsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\user-memory\\UserMemoryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\user-wallet\\UserWalletClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\utilities\\mocked-chats\\MockedChatsEditorClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\utilities\\mocked-chats\\view\\MockedChatsViewerClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\src\\book-components\\Chat\\MarkdownContent\\MarkdownContent.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\Homepage\\RecycleBinList.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\swagger\\SwaggerPageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\book+chat\\AgentBookAndChat.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\book\\BookEditorWrapper.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\chat\\loading.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\chat\\AgentChatHistoryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\export-as-transpiled-code\\AgentCodePageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\history\\RestoreVersionButton.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\_common\\components\\CodePreview\\CodePreview.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\ApiKeyIntegrationSections.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\CalendarIntegrationSection.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\PromptbookSdkTabs.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\WebsiteIntegrationTabs.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\system-message\\SystemMessageBookEditor.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\textarea\\AgentTextareaClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\timeouts\\AgentTimeoutsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\embed\\page.tsx': [Object]
    },
    cache: Map(1) { [Function: E] => [WeakMap] },
    cacheController: AbortController { signal: AbortSignal { aborted: true } },
    nextChunkId: 30,
    pendingChunks: 0,
    hints: Set(6) {
      'L[style]/_next/static/css/de7c0ac3e7516811.css',
      'L[style]/_next/static/css/ed0f0e0535a22473.css',
      'L[style]/_next/static/css/2de9726a3c08d309.css',
      'L[style]/_next/static/css/07199a57b6717f23.css',
      'L[style]/_next/static/css/b1f7517bfd677245.css',
      'L[style]/_next/static/css/950d264b0bfdfd57.css'
    },
    abortableTasks: Set(0) {},
    pingedTasks: [],
    completedImportChunks: [],
    completedHintChunks: [],
    completedRegularChunks: [],
    completedErrorChunks: [],
    writtenSymbols: Map(2) {
      Symbol(react.fragment) => 1,
      Symbol(react.suspense) => 28
    },
    writtenClientReferences: Map(9) {
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\layout-router.js#' => 3,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\render-from-template-context.js#' => 4,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\client-page.js#ClientPageRoot' => 5,
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\embed\\page.tsx#default' => 6,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js#OutletBoundary' => 9,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\async-metadata.js#AsyncMetadataOutlet' => 11,
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\global-error.tsx#default' => 22,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js#ViewportBoundary' => 25,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js#MetadataBoundary' => 27
    },
    writtenServerReferences: Map(0) {},
    writtenObjects: WeakMap { <items unknown> },
    temporaryReferences: undefined,
    identifierPrefix: '',
    identifierCount: 1,
    taintCleanupQueue: [],
    onError: [Function (anonymous)],
    onPostpone: [Function: X],
    onAllReady: [Function: X],
    onFatalError: [Function: X]
  },
  [Symbol(kResourceStore)]: undefined,
  [Symbol(kResourceStore)]: {
    type: 'prerender-legacy',
    phase: 'render',
    rootParams: {},
    implicitTags: { tags: [Array], expirationsByCacheKind: [Map] },
    revalidate: 0,
    expire: 4294967294,
    stale: 4294967294,
    tags: [
      '_N_T_/layout',
      '_N_T_/embed/layout',
      '_N_T_/embed/page',
      '_N_T_/embed'
    ]
  }
}
Unhandled rejection Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /embed couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
} Promise {
  <rejected> Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
      at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
      at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
      at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
      at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
      at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
      at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
      at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
      at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
    description: "Route /embed couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
    digest: 'DYNAMIC_SERVER_USAGE'
  },
  [Symbol(async_id_symbol)]: 14148,
  [Symbol(trigger_async_id_symbol)]: 14103,
  [Symbol(kResourceStore)]: {
    isStaticGeneration: true,
    page: '/embed/page',
    fallbackRouteParams: null,
    route: '/embed',
    incrementalCache: IncrementalCache {
      locks: Map(0) {},
      hasCustomCacheHandler: false,
      dev: false,
      disableForTestmode: false,
      minimalMode: false,
      requestHeaders: {},
      allowedRevalidateHeaderKeys: undefined,
      prerenderManifest: [Object],
      cacheControls: [SharedCacheControls],
      fetchCacheKeyPrefix: '',
      cacheHandler: [FileSystemCache]
    },
    cacheLifeProfiles: {
      default: [Object],
      seconds: [Object],
      minutes: [Object],
      hours: [Object],
      days: [Object],
      weeks: [Object],
      max: [Object]
    },
    isRevalidate: undefined,
    isBuildTimePrerendering: true,
    hasReadableErrorStacks: false,
    fetchCache: undefined,
    isOnDemandRevalidate: undefined,
    isDraftMode: undefined,
    requestEndedState: { ended: false },
    isPrefetchRequest: false,
    buildId: 'senL24mes0wCj4J8hXgfg',
    reactLoadableManifest: {
      '..\\..\\..\\node_modules\\jspdf\\dist\\jspdf.es.min.js -> canvg': [Object],
      '..\\..\\..\\node_modules\\jspdf\\dist\\jspdf.es.min.js -> dompurify': [Object],
      '..\\..\\..\\node_modules\\jspdf\\dist\\jspdf.es.min.js -> html2canvas': [Object],
      '..\\..\\..\\node_modules\\next\\dist\\client\\index.js -> ../pages/_app': [Object],
      '..\\..\\..\\node_modules\\next\\dist\\client\\index.js -> ../pages/_error': [Object],
      '..\\..\\..\\src\\book-components\\Chat\\Chat\\ChatMessageMap.tsx -> leaflet': [Object],
      '..\\..\\..\\src\\book-components\\Qr\\PromptbookQrCode.tsx -> qrcode': [Object],
      '..\\..\\..\\src\\commitments\\TEAM\\TEAM.ts -> ../../llm-providers/agent/RemoteAgent': [Object],
      'app\\agents\\[agentName]\\DeferredAgentProfileChat.tsx -> ./AgentProfileChat': [Object],
      'app\\swagger\\SwaggerPageClient.tsx -> swagger-ui-react': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ../AgentContextMenu/AgentContextMenu': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ../FolderContextMenu/FolderContextMenu': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ./AgentQrCodeModal': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ./FolderEditDialog': [Object],
      'components\\Homepage\\AgentsListViewContent.tsx -> ./AgentsGraph': [Object],
      'components\\Homepage\\AgentsListViewContent.tsx -> ./AgentsOffice': [Object],
      'components\\Homepage\\AgentsListViewContent.tsx -> ./AgentsPixelOffice': [Object]
    },
    assetPrefix: '',
    afterContext: eJ {
      workUnitStores: Set(0) {},
      waitUntil: [Function: bound ],
      onClose: [Function: bound onClose],
      onTaskError: [Function: onTaskError],
      callbackQueue: [o]
    },
    dynamicIOEnabled: false,
    dev: false,
    previouslyRevalidatedTags: [],
    refreshTagsByCacheKind: Map(2) { 'default' => [Object], 'remote' => [Object] },
    runInCleanSnapshot: [Function: bound] { asyncResource: [Getter/Setter] },
    fetchMetrics: [],
    dynamicUsageDescription: 'cookies',
    dynamicUsageStack: "Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `cookies`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error\n" +
      '    at r (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next\\server\\chunks\\657.js:12:54555)\n' +
      '    at n (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next\\server\\chunks\\5208.js:1:9504)\n' +
      '    at N (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next\\server\\chunks\\4810.js:240:3768)\n' +
      '    at ek (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:32629)\n' +
      '    at e (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:36665)\n' +
      '    at eI (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:37127)\n' +
      '    at Array.toJSON (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:34252)\n' +
      '    at stringify (<anonymous>)\n' +
      '    at eH (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:46044)\n' +
      '    at ez (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:46422)'
  },
  [Symbol(kResourceStore)]: eg {
    type: 20,
    status: 14,
    flushScheduled: false,
    fatalError: null,
    destination: null,
    bundlerConfig: {
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\client-page.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\client-page.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\client-segment.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\client-segment.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\http-access-fallback\\error-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\http-access-fallback\\error-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\layout-router.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\layout-router.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\async-metadata.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\metadata\\async-metadata.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\metadata\\metadata-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\render-from-template-context.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\render-from-template-context.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\lib\\metadata\\generate\\icon-mark.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\lib\\metadata\\generate\\icon-mark.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\global-error.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\public\\favicon.ico': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\LayoutWrapper\\LayoutWrapper.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\font\\google\\target.css?{"path":"src\\\\utils\\\\applicationFonts.ts","import":"Barlow_Condensed","arguments":[{"subsets":["latin"],"weight":["300","400","500","600","700"],"display":"swap","fallback":["Arial","Helvetica","sans-serif"],"variable":"--font-barlow-condensed"}],"variableName":"barlowCondensed"}': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\font\\google\\target.css?{"path":"src\\\\utils\\\\applicationFonts.ts","import":"Poppins","arguments":[{"subsets":["latin"],"weight":["400","500","600","700","800"],"display":"swap","fallback":["Arial","Helvetica","sans-serif"],"variable":"--font-poppins"}],"variableName":"poppins"}': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\globals.css': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\error.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\app-dir\\link.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\app-dir\\link.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\AgentProfileWrapper.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\DeferredAgentProfileChat.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\AgentProfile\\AgentProfile.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\DeletedAgentBanner.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\api-tokens\\ApiTokensClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\ForbiddenPage\\ForbiddenPage.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\backup\\BackupClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\browser-test\\BrowserTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\chat-feedback\\ChatFeedbackClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\chat-history\\ChatHistoryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\custom-css\\CustomCssClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\error-simulation\\ErrorSimulationClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\files\\FilesGalleryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\image-generator-test\\ImageGeneratorTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\images\\ImagesGalleryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\limits\\LimitsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\custom-js\\CustomJsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\messages\\MessagesClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\messages\\send-email\\SendEmailClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\metadata\\MetadataClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\search-engine-test\\SearchEngineTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\servers\\ServersClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\task-manager\\TaskManagerClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\transcriptions\\TranscriptionsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\usage\\UsageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\users\\[userId]\\UserDetailClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\UsersList\\UsersList.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\voice-input-test\\VoiceInputTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\Homepage\\AgentsList.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\Homepage\\ExternalAgentsSectionClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\src\\_packages\\components.index.ts': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\DocsToolbar\\DocsToolbar.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\experiments\\story\\page.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\search\\SearchPageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\profile\\UserProfileClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\settings\\KeybindingsSettingsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\user-memory\\UserMemoryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\user-wallet\\UserWalletClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\utilities\\mocked-chats\\MockedChatsEditorClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\utilities\\mocked-chats\\view\\MockedChatsViewerClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\src\\book-components\\Chat\\MarkdownContent\\MarkdownContent.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\Homepage\\RecycleBinList.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\swagger\\SwaggerPageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\book+chat\\AgentBookAndChat.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\book\\BookEditorWrapper.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\chat\\loading.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\chat\\AgentChatHistoryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\export-as-transpiled-code\\AgentCodePageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\history\\RestoreVersionButton.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\_common\\components\\CodePreview\\CodePreview.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\ApiKeyIntegrationSections.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\CalendarIntegrationSection.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\PromptbookSdkTabs.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\WebsiteIntegrationTabs.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\system-message\\SystemMessageBookEditor.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\textarea\\AgentTextareaClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\timeouts\\AgentTimeoutsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\embed\\page.tsx': [Object]
    },
    cache: Map(1) { [Function: E] => [WeakMap] },
    cacheController: AbortController { signal: AbortSignal { aborted: true } },
    nextChunkId: 30,
    pendingChunks: 0,
    hints: Set(6) {
      'L[style]/_next/static/css/de7c0ac3e7516811.css',
      'L[style]/_next/static/css/ed0f0e0535a22473.css',
      'L[style]/_next/static/css/2de9726a3c08d309.css',
      'L[style]/_next/static/css/07199a57b6717f23.css',
      'L[style]/_next/static/css/b1f7517bfd677245.css',
      'L[style]/_next/static/css/950d264b0bfdfd57.css'
    },
    abortableTasks: Set(0) {},
    pingedTasks: [],
    completedImportChunks: [],
    completedHintChunks: [],
    completedRegularChunks: [],
    completedErrorChunks: [],
    writtenSymbols: Map(2) {
      Symbol(react.fragment) => 1,
      Symbol(react.suspense) => 28
    },
    writtenClientReferences: Map(9) {
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\layout-router.js#' => 3,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\render-from-template-context.js#' => 4,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\client-page.js#ClientPageRoot' => 5,
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\embed\\page.tsx#default' => 6,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js#OutletBoundary' => 9,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\async-metadata.js#AsyncMetadataOutlet' => 11,
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\global-error.tsx#default' => 22,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js#ViewportBoundary' => 25,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js#MetadataBoundary' => 27
    },
    writtenServerReferences: Map(0) {},
    writtenObjects: WeakMap { <items unknown> },
    temporaryReferences: undefined,
    identifierPrefix: '',
    identifierCount: 1,
    taintCleanupQueue: [],
    onError: [Function (anonymous)],
    onPostpone: [Function: X],
    onAllReady: [Function: X],
    onFatalError: [Function: X]
  },
  [Symbol(kResourceStore)]: undefined,
  [Symbol(kResourceStore)]: {
    type: 'prerender-legacy',
    phase: 'render',
    rootParams: {},
    implicitTags: { tags: [Array], expirationsByCacheKind: [Map] },
    revalidate: 0,
    expire: 4294967294,
    stale: 4294967294,
    tags: [
      '_N_T_/layout',
      '_N_T_/embed/layout',
      '_N_T_/embed/page',
      '_N_T_/embed'
    ]
  }
}
Unhandled rejection Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /embed couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
} Promise {
  <rejected> Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
      at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
      at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
      at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
      at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
      at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
      at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
      at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
      at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
    description: "Route /embed couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
    digest: 'DYNAMIC_SERVER_USAGE'
  },
  [Symbol(async_id_symbol)]: 14152,
  [Symbol(trigger_async_id_symbol)]: 14103,
  [Symbol(kResourceStore)]: {
    isStaticGeneration: true,
    page: '/embed/page',
    fallbackRouteParams: null,
    route: '/embed',
    incrementalCache: IncrementalCache {
      locks: Map(0) {},
      hasCustomCacheHandler: false,
      dev: false,
      disableForTestmode: false,
      minimalMode: false,
      requestHeaders: {},
      allowedRevalidateHeaderKeys: undefined,
      prerenderManifest: [Object],
      cacheControls: [SharedCacheControls],
      fetchCacheKeyPrefix: '',
      cacheHandler: [FileSystemCache]
    },
    cacheLifeProfiles: {
      default: [Object],
      seconds: [Object],
      minutes: [Object],
      hours: [Object],
      days: [Object],
      weeks: [Object],
      max: [Object]
    },
    isRevalidate: undefined,
    isBuildTimePrerendering: true,
    hasReadableErrorStacks: false,
    fetchCache: undefined,
    isOnDemandRevalidate: undefined,
    isDraftMode: undefined,
    requestEndedState: { ended: false },
    isPrefetchRequest: false,
    buildId: 'senL24mes0wCj4J8hXgfg',
    reactLoadableManifest: {
      '..\\..\\..\\node_modules\\jspdf\\dist\\jspdf.es.min.js -> canvg': [Object],
      '..\\..\\..\\node_modules\\jspdf\\dist\\jspdf.es.min.js -> dompurify': [Object],
      '..\\..\\..\\node_modules\\jspdf\\dist\\jspdf.es.min.js -> html2canvas': [Object],
      '..\\..\\..\\node_modules\\next\\dist\\client\\index.js -> ../pages/_app': [Object],
      '..\\..\\..\\node_modules\\next\\dist\\client\\index.js -> ../pages/_error': [Object],
      '..\\..\\..\\src\\book-components\\Chat\\Chat\\ChatMessageMap.tsx -> leaflet': [Object],
      '..\\..\\..\\src\\book-components\\Qr\\PromptbookQrCode.tsx -> qrcode': [Object],
      '..\\..\\..\\src\\commitments\\TEAM\\TEAM.ts -> ../../llm-providers/agent/RemoteAgent': [Object],
      'app\\agents\\[agentName]\\DeferredAgentProfileChat.tsx -> ./AgentProfileChat': [Object],
      'app\\swagger\\SwaggerPageClient.tsx -> swagger-ui-react': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ../AgentContextMenu/AgentContextMenu': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ../FolderContextMenu/FolderContextMenu': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ./AgentQrCodeModal': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ./FolderEditDialog': [Object],
      'components\\Homepage\\AgentsListViewContent.tsx -> ./AgentsGraph': [Object],
      'components\\Homepage\\AgentsListViewContent.tsx -> ./AgentsOffice': [Object],
      'components\\Homepage\\AgentsListViewContent.tsx -> ./AgentsPixelOffice': [Object]
    },
    assetPrefix: '',
    afterContext: eJ {
      workUnitStores: Set(0) {},
      waitUntil: [Function: bound ],
      onClose: [Function: bound onClose],
      onTaskError: [Function: onTaskError],
      callbackQueue: [o]
    },
    dynamicIOEnabled: false,
    dev: false,
    previouslyRevalidatedTags: [],
    refreshTagsByCacheKind: Map(2) { 'default' => [Object], 'remote' => [Object] },
    runInCleanSnapshot: [Function: bound] { asyncResource: [Getter/Setter] },
    fetchMetrics: [],
    dynamicUsageDescription: 'cookies',
    dynamicUsageStack: "Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `cookies`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error\n" +
      '    at r (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next\\server\\chunks\\657.js:12:54555)\n' +
      '    at n (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next\\server\\chunks\\5208.js:1:9504)\n' +
      '    at N (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next\\server\\chunks\\4810.js:240:3768)\n' +
      '    at ek (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:32629)\n' +
      '    at e (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:36665)\n' +
      '    at eI (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:37127)\n' +
      '    at Array.toJSON (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:34252)\n' +
      '    at stringify (<anonymous>)\n' +
      '    at eH (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:46044)\n' +
      '    at ez (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:46422)'
  },
  [Symbol(kResourceStore)]: eg {
    type: 20,
    status: 14,
    flushScheduled: false,
    fatalError: null,
    destination: null,
    bundlerConfig: {
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\client-page.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\client-page.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\client-segment.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\client-segment.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\http-access-fallback\\error-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\http-access-fallback\\error-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\layout-router.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\layout-router.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\async-metadata.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\metadata\\async-metadata.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\metadata\\metadata-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\render-from-template-context.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\render-from-template-context.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\lib\\metadata\\generate\\icon-mark.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\lib\\metadata\\generate\\icon-mark.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\global-error.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\public\\favicon.ico': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\LayoutWrapper\\LayoutWrapper.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\font\\google\\target.css?{"path":"src\\\\utils\\\\applicationFonts.ts","import":"Barlow_Condensed","arguments":[{"subsets":["latin"],"weight":["300","400","500","600","700"],"display":"swap","fallback":["Arial","Helvetica","sans-serif"],"variable":"--font-barlow-condensed"}],"variableName":"barlowCondensed"}': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\font\\google\\target.css?{"path":"src\\\\utils\\\\applicationFonts.ts","import":"Poppins","arguments":[{"subsets":["latin"],"weight":["400","500","600","700","800"],"display":"swap","fallback":["Arial","Helvetica","sans-serif"],"variable":"--font-poppins"}],"variableName":"poppins"}': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\globals.css': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\error.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\app-dir\\link.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\app-dir\\link.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\AgentProfileWrapper.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\DeferredAgentProfileChat.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\AgentProfile\\AgentProfile.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\DeletedAgentBanner.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\api-tokens\\ApiTokensClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\ForbiddenPage\\ForbiddenPage.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\backup\\BackupClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\browser-test\\BrowserTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\chat-feedback\\ChatFeedbackClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\chat-history\\ChatHistoryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\custom-css\\CustomCssClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\error-simulation\\ErrorSimulationClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\files\\FilesGalleryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\image-generator-test\\ImageGeneratorTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\images\\ImagesGalleryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\limits\\LimitsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\custom-js\\CustomJsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\messages\\MessagesClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\messages\\send-email\\SendEmailClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\metadata\\MetadataClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\search-engine-test\\SearchEngineTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\servers\\ServersClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\task-manager\\TaskManagerClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\transcriptions\\TranscriptionsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\usage\\UsageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\users\\[userId]\\UserDetailClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\UsersList\\UsersList.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\voice-input-test\\VoiceInputTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\Homepage\\AgentsList.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\Homepage\\ExternalAgentsSectionClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\src\\_packages\\components.index.ts': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\DocsToolbar\\DocsToolbar.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\experiments\\story\\page.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\search\\SearchPageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\profile\\UserProfileClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\settings\\KeybindingsSettingsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\user-memory\\UserMemoryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\user-wallet\\UserWalletClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\utilities\\mocked-chats\\MockedChatsEditorClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\utilities\\mocked-chats\\view\\MockedChatsViewerClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\src\\book-components\\Chat\\MarkdownContent\\MarkdownContent.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\Homepage\\RecycleBinList.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\swagger\\SwaggerPageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\book+chat\\AgentBookAndChat.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\book\\BookEditorWrapper.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\chat\\loading.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\chat\\AgentChatHistoryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\export-as-transpiled-code\\AgentCodePageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\history\\RestoreVersionButton.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\_common\\components\\CodePreview\\CodePreview.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\ApiKeyIntegrationSections.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\CalendarIntegrationSection.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\PromptbookSdkTabs.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\WebsiteIntegrationTabs.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\system-message\\SystemMessageBookEditor.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\textarea\\AgentTextareaClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\timeouts\\AgentTimeoutsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\embed\\page.tsx': [Object]
    },
    cache: Map(1) { [Function: E] => [WeakMap] },
    cacheController: AbortController { signal: AbortSignal { aborted: true } },
    nextChunkId: 30,
    pendingChunks: 0,
    hints: Set(6) {
      'L[style]/_next/static/css/de7c0ac3e7516811.css',
      'L[style]/_next/static/css/ed0f0e0535a22473.css',
      'L[style]/_next/static/css/2de9726a3c08d309.css',
      'L[style]/_next/static/css/07199a57b6717f23.css',
      'L[style]/_next/static/css/b1f7517bfd677245.css',
      'L[style]/_next/static/css/950d264b0bfdfd57.css'
    },
    abortableTasks: Set(0) {},
    pingedTasks: [],
    completedImportChunks: [],
    completedHintChunks: [],
    completedRegularChunks: [],
    completedErrorChunks: [],
    writtenSymbols: Map(2) {
      Symbol(react.fragment) => 1,
      Symbol(react.suspense) => 28
    },
    writtenClientReferences: Map(9) {
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\layout-router.js#' => 3,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\render-from-template-context.js#' => 4,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\client-page.js#ClientPageRoot' => 5,
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\embed\\page.tsx#default' => 6,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js#OutletBoundary' => 9,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\async-metadata.js#AsyncMetadataOutlet' => 11,
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\global-error.tsx#default' => 22,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js#ViewportBoundary' => 25,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js#MetadataBoundary' => 27
    },
    writtenServerReferences: Map(0) {},
    writtenObjects: WeakMap { <items unknown> },
    temporaryReferences: undefined,
    identifierPrefix: '',
    identifierCount: 1,
    taintCleanupQueue: [],
    onError: [Function (anonymous)],
    onPostpone: [Function: X],
    onAllReady: [Function: X],
    onFatalError: [Function: X]
  },
  [Symbol(kResourceStore)]: undefined,
  [Symbol(kResourceStore)]: {
    type: 'prerender-legacy',
    phase: 'render',
    rootParams: {},
    implicitTags: { tags: [Array], expirationsByCacheKind: [Map] },
    revalidate: 0,
    expire: 4294967294,
    stale: 4294967294,
    tags: [
      '_N_T_/layout',
      '_N_T_/embed/layout',
      '_N_T_/embed/page',
      '_N_T_/embed'
    ]
  }
}
Unhandled rejection Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /embed couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
} Promise {
  <rejected> Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
      at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
      at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
      at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
      at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
      at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
      at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
      at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
      at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
    description: "Route /embed couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
    digest: 'DYNAMIC_SERVER_USAGE'
  },
  [Symbol(async_id_symbol)]: 14141,
  [Symbol(trigger_async_id_symbol)]: 14103,
  [Symbol(kResourceStore)]: {
    isStaticGeneration: true,
    page: '/embed/page',
    fallbackRouteParams: null,
    route: '/embed',
    incrementalCache: IncrementalCache {
      locks: Map(0) {},
      hasCustomCacheHandler: false,
      dev: false,
      disableForTestmode: false,
      minimalMode: false,
      requestHeaders: {},
      allowedRevalidateHeaderKeys: undefined,
      prerenderManifest: [Object],
      cacheControls: [SharedCacheControls],
      fetchCacheKeyPrefix: '',
      cacheHandler: [FileSystemCache]
    },
    cacheLifeProfiles: {
      default: [Object],
      seconds: [Object],
      minutes: [Object],
      hours: [Object],
      days: [Object],
      weeks: [Object],
      max: [Object]
    },
    isRevalidate: undefined,
    isBuildTimePrerendering: true,
    hasReadableErrorStacks: false,
    fetchCache: undefined,
    isOnDemandRevalidate: undefined,
    isDraftMode: undefined,
    requestEndedState: { ended: false },
    isPrefetchRequest: false,
    buildId: 'senL24mes0wCj4J8hXgfg',
    reactLoadableManifest: {
      '..\\..\\..\\node_modules\\jspdf\\dist\\jspdf.es.min.js -> canvg': [Object],
      '..\\..\\..\\node_modules\\jspdf\\dist\\jspdf.es.min.js -> dompurify': [Object],
      '..\\..\\..\\node_modules\\jspdf\\dist\\jspdf.es.min.js -> html2canvas': [Object],
      '..\\..\\..\\node_modules\\next\\dist\\client\\index.js -> ../pages/_app': [Object],
      '..\\..\\..\\node_modules\\next\\dist\\client\\index.js -> ../pages/_error': [Object],
      '..\\..\\..\\src\\book-components\\Chat\\Chat\\ChatMessageMap.tsx -> leaflet': [Object],
      '..\\..\\..\\src\\book-components\\Qr\\PromptbookQrCode.tsx -> qrcode': [Object],
      '..\\..\\..\\src\\commitments\\TEAM\\TEAM.ts -> ../../llm-providers/agent/RemoteAgent': [Object],
      'app\\agents\\[agentName]\\DeferredAgentProfileChat.tsx -> ./AgentProfileChat': [Object],
      'app\\swagger\\SwaggerPageClient.tsx -> swagger-ui-react': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ../AgentContextMenu/AgentContextMenu': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ../FolderContextMenu/FolderContextMenu': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ./AgentQrCodeModal': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ./FolderEditDialog': [Object],
      'components\\Homepage\\AgentsListViewContent.tsx -> ./AgentsGraph': [Object],
      'components\\Homepage\\AgentsListViewContent.tsx -> ./AgentsOffice': [Object],
      'components\\Homepage\\AgentsListViewContent.tsx -> ./AgentsPixelOffice': [Object]
    },
    assetPrefix: '',
    afterContext: eJ {
      workUnitStores: Set(0) {},
      waitUntil: [Function: bound ],
      onClose: [Function: bound onClose],
      onTaskError: [Function: onTaskError],
      callbackQueue: [o]
    },
    dynamicIOEnabled: false,
    dev: false,
    previouslyRevalidatedTags: [],
    refreshTagsByCacheKind: Map(2) { 'default' => [Object], 'remote' => [Object] },
    runInCleanSnapshot: [Function: bound] { asyncResource: [Getter/Setter] },
    fetchMetrics: [],
    dynamicUsageDescription: 'cookies',
    dynamicUsageStack: "Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `cookies`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error\n" +
      '    at r (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next\\server\\chunks\\657.js:12:54555)\n' +
      '    at n (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next\\server\\chunks\\5208.js:1:9504)\n' +
      '    at N (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next\\server\\chunks\\4810.js:240:3768)\n' +
      '    at ek (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:32629)\n' +
      '    at e (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:36665)\n' +
      '    at eI (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:37127)\n' +
      '    at Array.toJSON (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:34252)\n' +
      '    at stringify (<anonymous>)\n' +
      '    at eH (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:46044)\n' +
      '    at ez (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:46422)'
  },
  [Symbol(kResourceStore)]: eg {
    type: 20,
    status: 14,
    flushScheduled: false,
    fatalError: null,
    destination: null,
    bundlerConfig: {
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\client-page.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\client-page.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\client-segment.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\client-segment.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\http-access-fallback\\error-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\http-access-fallback\\error-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\layout-router.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\layout-router.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\async-metadata.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\metadata\\async-metadata.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\metadata\\metadata-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\render-from-template-context.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\render-from-template-context.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\lib\\metadata\\generate\\icon-mark.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\lib\\metadata\\generate\\icon-mark.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\global-error.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\public\\favicon.ico': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\LayoutWrapper\\LayoutWrapper.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\font\\google\\target.css?{"path":"src\\\\utils\\\\applicationFonts.ts","import":"Barlow_Condensed","arguments":[{"subsets":["latin"],"weight":["300","400","500","600","700"],"display":"swap","fallback":["Arial","Helvetica","sans-serif"],"variable":"--font-barlow-condensed"}],"variableName":"barlowCondensed"}': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\font\\google\\target.css?{"path":"src\\\\utils\\\\applicationFonts.ts","import":"Poppins","arguments":[{"subsets":["latin"],"weight":["400","500","600","700","800"],"display":"swap","fallback":["Arial","Helvetica","sans-serif"],"variable":"--font-poppins"}],"variableName":"poppins"}': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\globals.css': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\error.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\app-dir\\link.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\app-dir\\link.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\AgentProfileWrapper.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\DeferredAgentProfileChat.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\AgentProfile\\AgentProfile.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\DeletedAgentBanner.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\api-tokens\\ApiTokensClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\ForbiddenPage\\ForbiddenPage.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\backup\\BackupClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\browser-test\\BrowserTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\chat-feedback\\ChatFeedbackClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\chat-history\\ChatHistoryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\custom-css\\CustomCssClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\error-simulation\\ErrorSimulationClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\files\\FilesGalleryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\image-generator-test\\ImageGeneratorTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\images\\ImagesGalleryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\limits\\LimitsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\custom-js\\CustomJsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\messages\\MessagesClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\messages\\send-email\\SendEmailClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\metadata\\MetadataClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\search-engine-test\\SearchEngineTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\servers\\ServersClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\task-manager\\TaskManagerClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\transcriptions\\TranscriptionsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\usage\\UsageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\users\\[userId]\\UserDetailClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\UsersList\\UsersList.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\voice-input-test\\VoiceInputTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\Homepage\\AgentsList.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\Homepage\\ExternalAgentsSectionClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\src\\_packages\\components.index.ts': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\DocsToolbar\\DocsToolbar.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\experiments\\story\\page.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\search\\SearchPageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\profile\\UserProfileClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\settings\\KeybindingsSettingsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\user-memory\\UserMemoryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\user-wallet\\UserWalletClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\utilities\\mocked-chats\\MockedChatsEditorClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\utilities\\mocked-chats\\view\\MockedChatsViewerClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\src\\book-components\\Chat\\MarkdownContent\\MarkdownContent.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\Homepage\\RecycleBinList.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\swagger\\SwaggerPageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\book+chat\\AgentBookAndChat.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\book\\BookEditorWrapper.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\chat\\loading.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\chat\\AgentChatHistoryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\export-as-transpiled-code\\AgentCodePageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\history\\RestoreVersionButton.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\_common\\components\\CodePreview\\CodePreview.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\ApiKeyIntegrationSections.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\CalendarIntegrationSection.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\PromptbookSdkTabs.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\WebsiteIntegrationTabs.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\system-message\\SystemMessageBookEditor.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\textarea\\AgentTextareaClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\timeouts\\AgentTimeoutsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\embed\\page.tsx': [Object]
    },
    cache: Map(1) { [Function: E] => [WeakMap] },
    cacheController: AbortController { signal: AbortSignal { aborted: true } },
    nextChunkId: 30,
    pendingChunks: 0,
    hints: Set(6) {
      'L[style]/_next/static/css/de7c0ac3e7516811.css',
      'L[style]/_next/static/css/ed0f0e0535a22473.css',
      'L[style]/_next/static/css/2de9726a3c08d309.css',
      'L[style]/_next/static/css/07199a57b6717f23.css',
      'L[style]/_next/static/css/b1f7517bfd677245.css',
      'L[style]/_next/static/css/950d264b0bfdfd57.css'
    },
    abortableTasks: Set(0) {},
    pingedTasks: [],
    completedImportChunks: [],
    completedHintChunks: [],
    completedRegularChunks: [],
    completedErrorChunks: [],
    writtenSymbols: Map(2) {
      Symbol(react.fragment) => 1,
      Symbol(react.suspense) => 28
    },
    writtenClientReferences: Map(9) {
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\layout-router.js#' => 3,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\render-from-template-context.js#' => 4,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\client-page.js#ClientPageRoot' => 5,
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\embed\\page.tsx#default' => 6,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js#OutletBoundary' => 9,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\async-metadata.js#AsyncMetadataOutlet' => 11,
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\global-error.tsx#default' => 22,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js#ViewportBoundary' => 25,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js#MetadataBoundary' => 27
    },
    writtenServerReferences: Map(0) {},
    writtenObjects: WeakMap { <items unknown> },
    temporaryReferences: undefined,
    identifierPrefix: '',
    identifierCount: 1,
    taintCleanupQueue: [],
    onError: [Function (anonymous)],
    onPostpone: [Function: X],
    onAllReady: [Function: X],
    onFatalError: [Function: X]
  },
  [Symbol(kResourceStore)]: undefined,
  [Symbol(kResourceStore)]: {
    type: 'prerender-legacy',
    phase: 'render',
    rootParams: {},
    implicitTags: { tags: [Array], expirationsByCacheKind: [Map] },
    revalidate: 0,
    expire: 4294967294,
    stale: 4294967294,
    tags: [
      '_N_T_/layout',
      '_N_T_/embed/layout',
      '_N_T_/embed/page',
      '_N_T_/embed'
    ]
  }
}
Unhandled rejection Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `cookies`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:9504)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\10.js:1:3122)
    at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\10.js:1:3179)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\10.js:180:307)
    at j (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\10.js:180:953)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3534) {
  description: "Route /embed couldn't be rendered statically because it used `cookies`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
} Promise {
  <rejected> Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `cookies`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
      at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
      at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:9504)
      at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\10.js:1:3122)
      at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\10.js:1:3179)
      at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\10.js:180:307)
      at j (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\10.js:180:953)
      at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3534) {
    description: "Route /embed couldn't be rendered statically because it used `cookies`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
    digest: 'DYNAMIC_SERVER_USAGE'
  },
  [Symbol(async_id_symbol)]: 14147,
  [Symbol(trigger_async_id_symbol)]: 14126,
  [Symbol(kResourceStore)]: {
    isStaticGeneration: true,
    page: '/embed/page',
    fallbackRouteParams: null,
    route: '/embed',
    incrementalCache: IncrementalCache {
      locks: Map(0) {},
      hasCustomCacheHandler: false,
      dev: false,
      disableForTestmode: false,
      minimalMode: false,
      requestHeaders: {},
      allowedRevalidateHeaderKeys: undefined,
      prerenderManifest: [Object],
      cacheControls: [SharedCacheControls],
      fetchCacheKeyPrefix: '',
      cacheHandler: [FileSystemCache]
    },
    cacheLifeProfiles: {
      default: [Object],
      seconds: [Object],
      minutes: [Object],
      hours: [Object],
      days: [Object],
      weeks: [Object],
      max: [Object]
    },
    isRevalidate: undefined,
    isBuildTimePrerendering: true,
    hasReadableErrorStacks: false,
    fetchCache: undefined,
    isOnDemandRevalidate: undefined,
    isDraftMode: undefined,
    requestEndedState: { ended: false },
    isPrefetchRequest: false,
    buildId: 'senL24mes0wCj4J8hXgfg',
    reactLoadableManifest: {
      '..\\..\\..\\node_modules\\jspdf\\dist\\jspdf.es.min.js -> canvg': [Object],
      '..\\..\\..\\node_modules\\jspdf\\dist\\jspdf.es.min.js -> dompurify': [Object],
      '..\\..\\..\\node_modules\\jspdf\\dist\\jspdf.es.min.js -> html2canvas': [Object],
      '..\\..\\..\\node_modules\\next\\dist\\client\\index.js -> ../pages/_app': [Object],
      '..\\..\\..\\node_modules\\next\\dist\\client\\index.js -> ../pages/_error': [Object],
      '..\\..\\..\\src\\book-components\\Chat\\Chat\\ChatMessageMap.tsx -> leaflet': [Object],
      '..\\..\\..\\src\\book-components\\Qr\\PromptbookQrCode.tsx -> qrcode': [Object],
      '..\\..\\..\\src\\commitments\\TEAM\\TEAM.ts -> ../../llm-providers/agent/RemoteAgent': [Object],
      'app\\agents\\[agentName]\\DeferredAgentProfileChat.tsx -> ./AgentProfileChat': [Object],
      'app\\swagger\\SwaggerPageClient.tsx -> swagger-ui-react': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ../AgentContextMenu/AgentContextMenu': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ../FolderContextMenu/FolderContextMenu': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ./AgentQrCodeModal': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ./FolderEditDialog': [Object],
      'components\\Homepage\\AgentsListViewContent.tsx -> ./AgentsGraph': [Object],
      'components\\Homepage\\AgentsListViewContent.tsx -> ./AgentsOffice': [Object],
      'components\\Homepage\\AgentsListViewContent.tsx -> ./AgentsPixelOffice': [Object]
    },
    assetPrefix: '',
    afterContext: eJ {
      workUnitStores: Set(0) {},
      waitUntil: [Function: bound ],
      onClose: [Function: bound onClose],
      onTaskError: [Function: onTaskError],
      callbackQueue: [o]
    },
    dynamicIOEnabled: false,
    dev: false,
    previouslyRevalidatedTags: [],
    refreshTagsByCacheKind: Map(2) { 'default' => [Object], 'remote' => [Object] },
    runInCleanSnapshot: [Function: bound] { asyncResource: [Getter/Setter] },
    fetchMetrics: [],
    dynamicUsageDescription: 'cookies',
    dynamicUsageStack: "Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `cookies`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error\n" +
      '    at r (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next\\server\\chunks\\657.js:12:54555)\n' +
      '    at n (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next\\server\\chunks\\5208.js:1:9504)\n' +
      '    at N (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next\\server\\chunks\\4810.js:240:3768)\n' +
      '    at ek (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:32629)\n' +
      '    at e (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:36665)\n' +
      '    at eI (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:37127)\n' +
      '    at Array.toJSON (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:34252)\n' +
      '    at stringify (<anonymous>)\n' +
      '    at eH (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:46044)\n' +
      '    at ez (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:46422)'
  },
  [Symbol(kResourceStore)]: eg {
    type: 20,
    status: 14,
    flushScheduled: false,
    fatalError: null,
    destination: null,
    bundlerConfig: {
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\client-page.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\client-page.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\client-segment.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\client-segment.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\http-access-fallback\\error-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\http-access-fallback\\error-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\layout-router.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\layout-router.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\async-metadata.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\metadata\\async-metadata.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\metadata\\metadata-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\render-from-template-context.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\render-from-template-context.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\lib\\metadata\\generate\\icon-mark.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\lib\\metadata\\generate\\icon-mark.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\global-error.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\public\\favicon.ico': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\LayoutWrapper\\LayoutWrapper.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\font\\google\\target.css?{"path":"src\\\\utils\\\\applicationFonts.ts","import":"Barlow_Condensed","arguments":[{"subsets":["latin"],"weight":["300","400","500","600","700"],"display":"swap","fallback":["Arial","Helvetica","sans-serif"],"variable":"--font-barlow-condensed"}],"variableName":"barlowCondensed"}': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\font\\google\\target.css?{"path":"src\\\\utils\\\\applicationFonts.ts","import":"Poppins","arguments":[{"subsets":["latin"],"weight":["400","500","600","700","800"],"display":"swap","fallback":["Arial","Helvetica","sans-serif"],"variable":"--font-poppins"}],"variableName":"poppins"}': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\globals.css': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\error.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\app-dir\\link.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\app-dir\\link.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\AgentProfileWrapper.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\DeferredAgentProfileChat.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\AgentProfile\\AgentProfile.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\DeletedAgentBanner.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\api-tokens\\ApiTokensClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\ForbiddenPage\\ForbiddenPage.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\backup\\BackupClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\browser-test\\BrowserTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\chat-feedback\\ChatFeedbackClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\chat-history\\ChatHistoryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\custom-css\\CustomCssClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\error-simulation\\ErrorSimulationClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\files\\FilesGalleryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\image-generator-test\\ImageGeneratorTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\images\\ImagesGalleryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\limits\\LimitsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\custom-js\\CustomJsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\messages\\MessagesClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\messages\\send-email\\SendEmailClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\metadata\\MetadataClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\search-engine-test\\SearchEngineTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\servers\\ServersClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\task-manager\\TaskManagerClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\transcriptions\\TranscriptionsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\usage\\UsageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\users\\[userId]\\UserDetailClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\UsersList\\UsersList.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\voice-input-test\\VoiceInputTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\Homepage\\AgentsList.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\Homepage\\ExternalAgentsSectionClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\src\\_packages\\components.index.ts': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\DocsToolbar\\DocsToolbar.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\experiments\\story\\page.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\search\\SearchPageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\profile\\UserProfileClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\settings\\KeybindingsSettingsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\user-memory\\UserMemoryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\user-wallet\\UserWalletClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\utilities\\mocked-chats\\MockedChatsEditorClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\utilities\\mocked-chats\\view\\MockedChatsViewerClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\src\\book-components\\Chat\\MarkdownContent\\MarkdownContent.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\Homepage\\RecycleBinList.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\swagger\\SwaggerPageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\book+chat\\AgentBookAndChat.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\book\\BookEditorWrapper.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\chat\\loading.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\chat\\AgentChatHistoryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\export-as-transpiled-code\\AgentCodePageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\history\\RestoreVersionButton.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\_common\\components\\CodePreview\\CodePreview.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\ApiKeyIntegrationSections.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\CalendarIntegrationSection.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\PromptbookSdkTabs.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\WebsiteIntegrationTabs.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\system-message\\SystemMessageBookEditor.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\textarea\\AgentTextareaClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\timeouts\\AgentTimeoutsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\embed\\page.tsx': [Object]
    },
    cache: Map(1) { [Function: E] => [WeakMap] },
    cacheController: AbortController { signal: AbortSignal { aborted: true } },
    nextChunkId: 30,
    pendingChunks: 0,
    hints: Set(6) {
      'L[style]/_next/static/css/de7c0ac3e7516811.css',
      'L[style]/_next/static/css/ed0f0e0535a22473.css',
      'L[style]/_next/static/css/2de9726a3c08d309.css',
      'L[style]/_next/static/css/07199a57b6717f23.css',
      'L[style]/_next/static/css/b1f7517bfd677245.css',
      'L[style]/_next/static/css/950d264b0bfdfd57.css'
    },
    abortableTasks: Set(0) {},
    pingedTasks: [],
    completedImportChunks: [],
    completedHintChunks: [],
    completedRegularChunks: [],
    completedErrorChunks: [],
    writtenSymbols: Map(2) {
      Symbol(react.fragment) => 1,
      Symbol(react.suspense) => 28
    },
    writtenClientReferences: Map(9) {
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\layout-router.js#' => 3,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\render-from-template-context.js#' => 4,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\client-page.js#ClientPageRoot' => 5,
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\embed\\page.tsx#default' => 6,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js#OutletBoundary' => 9,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\async-metadata.js#AsyncMetadataOutlet' => 11,
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\global-error.tsx#default' => 22,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js#ViewportBoundary' => 25,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js#MetadataBoundary' => 27
    },
    writtenServerReferences: Map(0) {},
    writtenObjects: WeakMap { <items unknown> },
    temporaryReferences: undefined,
    identifierPrefix: '',
    identifierCount: 1,
    taintCleanupQueue: [],
    onError: [Function (anonymous)],
    onPostpone: [Function: X],
    onAllReady: [Function: X],
    onFatalError: [Function: X]
  },
  [Symbol(kResourceStore)]: undefined,
  [Symbol(kResourceStore)]: {
    type: 'prerender-legacy',
    phase: 'render',
    rootParams: {},
    implicitTags: { tags: [Array], expirationsByCacheKind: [Map] },
    revalidate: 0,
    expire: 4294967294,
    stale: 4294967294,
    tags: [
      '_N_T_/layout',
      '_N_T_/embed/layout',
      '_N_T_/embed/page',
      '_N_T_/embed'
    ]
  }
}
Unhandled rejection Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
    at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
    at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
    at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
    at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
    at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
    at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
  description: "Route /embed couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
  digest: 'DYNAMIC_SERVER_USAGE'
} Promise {
  <rejected> Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error
      at r (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\657.js:12:54555)
      at m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\5208.js:1:2857)
      at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:604)
      at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:31:1134)
      at e (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\7183.js:1:3890)
      at <unknown> (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2677)
      at k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4613.js:66:2974)
      at N (C:\Users\me\work\ai\promptbook\apps\agents-server\.next\server\chunks\4810.js:240:3303) {
    description: "Route /embed couldn't be rendered statically because it used `headers`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error",
    digest: 'DYNAMIC_SERVER_USAGE'
  },
  [Symbol(async_id_symbol)]: 14134,
  [Symbol(trigger_async_id_symbol)]: 14103,
  [Symbol(kResourceStore)]: {
    isStaticGeneration: true,
    page: '/embed/page',
    fallbackRouteParams: null,
    route: '/embed',
    incrementalCache: IncrementalCache {
      locks: Map(0) {},
      hasCustomCacheHandler: false,
      dev: false,
      disableForTestmode: false,
      minimalMode: false,
      requestHeaders: {},
      allowedRevalidateHeaderKeys: undefined,
      prerenderManifest: [Object],
      cacheControls: [SharedCacheControls],
      fetchCacheKeyPrefix: '',
      cacheHandler: [FileSystemCache]
    },
    cacheLifeProfiles: {
      default: [Object],
      seconds: [Object],
      minutes: [Object],
      hours: [Object],
      days: [Object],
      weeks: [Object],
      max: [Object]
    },
    isRevalidate: undefined,
    isBuildTimePrerendering: true,
    hasReadableErrorStacks: false,
    fetchCache: undefined,
    isOnDemandRevalidate: undefined,
    isDraftMode: undefined,
    requestEndedState: { ended: false },
    isPrefetchRequest: false,
    buildId: 'senL24mes0wCj4J8hXgfg',
    reactLoadableManifest: {
      '..\\..\\..\\node_modules\\jspdf\\dist\\jspdf.es.min.js -> canvg': [Object],
      '..\\..\\..\\node_modules\\jspdf\\dist\\jspdf.es.min.js -> dompurify': [Object],
      '..\\..\\..\\node_modules\\jspdf\\dist\\jspdf.es.min.js -> html2canvas': [Object],
      '..\\..\\..\\node_modules\\next\\dist\\client\\index.js -> ../pages/_app': [Object],
      '..\\..\\..\\node_modules\\next\\dist\\client\\index.js -> ../pages/_error': [Object],
      '..\\..\\..\\src\\book-components\\Chat\\Chat\\ChatMessageMap.tsx -> leaflet': [Object],
      '..\\..\\..\\src\\book-components\\Qr\\PromptbookQrCode.tsx -> qrcode': [Object],
      '..\\..\\..\\src\\commitments\\TEAM\\TEAM.ts -> ../../llm-providers/agent/RemoteAgent': [Object],
      'app\\agents\\[agentName]\\DeferredAgentProfileChat.tsx -> ./AgentProfileChat': [Object],
      'app\\swagger\\SwaggerPageClient.tsx -> swagger-ui-react': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ../AgentContextMenu/AgentContextMenu': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ../FolderContextMenu/FolderContextMenu': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ./AgentQrCodeModal': [Object],
      'components\\Homepage\\AgentsListDialogs.tsx -> ./FolderEditDialog': [Object],
      'components\\Homepage\\AgentsListViewContent.tsx -> ./AgentsGraph': [Object],
      'components\\Homepage\\AgentsListViewContent.tsx -> ./AgentsOffice': [Object],
      'components\\Homepage\\AgentsListViewContent.tsx -> ./AgentsPixelOffice': [Object]
    },
    assetPrefix: '',
    afterContext: eJ {
      workUnitStores: Set(0) {},
      waitUntil: [Function: bound ],
      onClose: [Function: bound onClose],
      onTaskError: [Function: onTaskError],
      callbackQueue: [o]
    },
    dynamicIOEnabled: false,
    dev: false,
    previouslyRevalidatedTags: [],
    refreshTagsByCacheKind: Map(2) { 'default' => [Object], 'remote' => [Object] },
    runInCleanSnapshot: [Function: bound] { asyncResource: [Getter/Setter] },
    fetchMetrics: [],
    dynamicUsageDescription: 'cookies',
    dynamicUsageStack: "Error: Dynamic server usage: Route /embed couldn't be rendered statically because it used `cookies`. See more info here: https://nextjs.org/docs/messages/dynamic-server-error\n" +
      '    at r (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next\\server\\chunks\\657.js:12:54555)\n' +
      '    at n (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next\\server\\chunks\\5208.js:1:9504)\n' +
      '    at N (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next\\server\\chunks\\4810.js:240:3768)\n' +
      '    at ek (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:32629)\n' +
      '    at e (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:36665)\n' +
      '    at eI (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:37127)\n' +
      '    at Array.toJSON (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:34252)\n' +
      '    at stringify (<anonymous>)\n' +
      '    at eH (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:46044)\n' +
      '    at ez (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:5:46422)'
  },
  [Symbol(kResourceStore)]: eg {
    type: 20,
    status: 14,
    flushScheduled: false,
    fatalError: null,
    destination: null,
    bundlerConfig: {
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\client-page.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\client-page.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\client-segment.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\client-segment.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\http-access-fallback\\error-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\http-access-fallback\\error-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\layout-router.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\layout-router.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\async-metadata.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\metadata\\async-metadata.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\metadata\\metadata-boundary.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\render-from-template-context.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\components\\render-from-template-context.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\lib\\metadata\\generate\\icon-mark.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\lib\\metadata\\generate\\icon-mark.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\global-error.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\public\\favicon.ico': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\LayoutWrapper\\LayoutWrapper.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\font\\google\\target.css?{"path":"src\\\\utils\\\\applicationFonts.ts","import":"Barlow_Condensed","arguments":[{"subsets":["latin"],"weight":["300","400","500","600","700"],"display":"swap","fallback":["Arial","Helvetica","sans-serif"],"variable":"--font-barlow-condensed"}],"variableName":"barlowCondensed"}': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\font\\google\\target.css?{"path":"src\\\\utils\\\\applicationFonts.ts","import":"Poppins","arguments":[{"subsets":["latin"],"weight":["400","500","600","700","800"],"display":"swap","fallback":["Arial","Helvetica","sans-serif"],"variable":"--font-poppins"}],"variableName":"poppins"}': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\globals.css': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\error.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\app-dir\\link.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\esm\\client\\app-dir\\link.js': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\AgentProfileWrapper.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\DeferredAgentProfileChat.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\AgentProfile\\AgentProfile.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\DeletedAgentBanner.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\api-tokens\\ApiTokensClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\ForbiddenPage\\ForbiddenPage.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\backup\\BackupClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\browser-test\\BrowserTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\chat-feedback\\ChatFeedbackClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\chat-history\\ChatHistoryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\custom-css\\CustomCssClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\error-simulation\\ErrorSimulationClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\files\\FilesGalleryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\image-generator-test\\ImageGeneratorTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\images\\ImagesGalleryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\limits\\LimitsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\custom-js\\CustomJsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\messages\\MessagesClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\messages\\send-email\\SendEmailClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\metadata\\MetadataClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\search-engine-test\\SearchEngineTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\servers\\ServersClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\task-manager\\TaskManagerClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\transcriptions\\TranscriptionsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\usage\\UsageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\users\\[userId]\\UserDetailClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\UsersList\\UsersList.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\admin\\voice-input-test\\VoiceInputTestClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\Homepage\\AgentsList.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\Homepage\\ExternalAgentsSectionClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\src\\_packages\\components.index.ts': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\DocsToolbar\\DocsToolbar.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\experiments\\story\\page.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\search\\SearchPageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\profile\\UserProfileClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\settings\\KeybindingsSettingsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\user-memory\\UserMemoryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\user-wallet\\UserWalletClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\utilities\\mocked-chats\\MockedChatsEditorClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\system\\utilities\\mocked-chats\\view\\MockedChatsViewerClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\src\\book-components\\Chat\\MarkdownContent\\MarkdownContent.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\components\\Homepage\\RecycleBinList.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\swagger\\SwaggerPageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\book+chat\\AgentBookAndChat.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\book\\BookEditorWrapper.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\chat\\loading.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\chat\\AgentChatHistoryClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\export-as-transpiled-code\\AgentCodePageClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\history\\RestoreVersionButton.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\_common\\components\\CodePreview\\CodePreview.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\ApiKeyIntegrationSections.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\CalendarIntegrationSection.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\PromptbookSdkTabs.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\integration\\WebsiteIntegrationTabs.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\system-message\\SystemMessageBookEditor.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\textarea\\AgentTextareaClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\agents\\[agentName]\\timeouts\\AgentTimeoutsClient.tsx': [Object],
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\embed\\page.tsx': [Object]
    },
    cache: Map(1) { [Function: E] => [WeakMap] },
    cacheController: AbortController { signal: AbortSignal { aborted: true } },
    nextChunkId: 30,
    pendingChunks: 0,
    hints: Set(6) {
      'L[style]/_next/static/css/de7c0ac3e7516811.css',
      'L[style]/_next/static/css/ed0f0e0535a22473.css',
      'L[style]/_next/static/css/2de9726a3c08d309.css',
      'L[style]/_next/static/css/07199a57b6717f23.css',
      'L[style]/_next/static/css/b1f7517bfd677245.css',
      'L[style]/_next/static/css/950d264b0bfdfd57.css'
    },
    abortableTasks: Set(0) {},
    pingedTasks: [],
    completedImportChunks: [],
    completedHintChunks: [],
    completedRegularChunks: [],
    completedErrorChunks: [],
    writtenSymbols: Map(2) {
      Symbol(react.fragment) => 1,
      Symbol(react.suspense) => 28
    },
    writtenClientReferences: Map(9) {
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\layout-router.js#' => 3,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\render-from-template-context.js#' => 4,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\client-page.js#ClientPageRoot' => 5,
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\embed\\page.tsx#default' => 6,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js#OutletBoundary' => 9,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\async-metadata.js#AsyncMetadataOutlet' => 11,
      'C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\app\\global-error.tsx#default' => 22,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js#ViewportBoundary' => 25,
      'C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\client\\components\\metadata\\metadata-boundary.js#MetadataBoundary' => 27
    },
    writtenServerReferences: Map(0) {},
    writtenObjects: WeakMap { <items unknown> },
    temporaryReferences: undefined,
    identifierPrefix: '',
    identifierCount: 1,
    taintCleanupQueue: [],
    onError: [Function (anonymous)],
    onPostpone: [Function: X],
    onAllReady: [Function: X],
    onFatalError: [Function: X]
  },
  [Symbol(kResourceStore)]: undefined,
  [Symbol(kResourceStore)]: {
    type: 'prerender-legacy',
    phase: 'render',
    rootParams: {},
    implicitTags: { tags: [Array], expirationsByCacheKind: [Map] },
    revalidate: 0,
    expire: 4294967294,
    stale: 4294967294,
    tags: [
      '_N_T_/layout',
      '_N_T_/embed/layout',
      '_N_T_/embed/page',
      '_N_T_/embed'
    ]
  }
}
 ✓ Generating static pages (109/109)
   Finalizing page optimization ...
   Collecting build traces ...

Route (app)                                                                     Size  First Load JS
┌ ƒ /                                                                          267 B         1.1 MB
├ ƒ /_not-found                                                                484 B         102 kB
├ ƒ /[agentName]                                                               156 B         211 kB
├ ƒ /[agentName]/[...rest]                                                     484 B         102 kB
├ ƒ /admin/about                                                             2.77 kB        1.03 MB
├ ƒ /admin/api-tokens                                                        3.51 kB         171 kB
├ ƒ /admin/backup                                                             3.7 kB         172 kB
├ ƒ /admin/browser-test                                                      2.94 kB         180 kB
├ ƒ /admin/chat-feedback                                                     8.67 kB         757 kB
├ ƒ /admin/chat-history                                                      8.54 kB         757 kB
├ ƒ /admin/custom-css                                                        6.83 kB         179 kB
├ ƒ /admin/custom-js                                                         8.29 kB         181 kB
├ ƒ /admin/error-simulation                                                  4.63 kB         173 kB
├ ƒ /admin/files                                                              4.1 kB         181 kB
├ ƒ /admin/image-generator-test                                              11.5 kB         217 kB
├ ƒ /admin/images                                                            4.95 kB         182 kB
├ ƒ /admin/limits                                                             4.5 kB         172 kB
├ ƒ /admin/messages                                                          3.85 kB         172 kB
├ ƒ /admin/messages/send-email                                               3.14 kB         177 kB
├ ƒ /admin/metadata                                                          16.5 kB         222 kB
├ ƒ /admin/models                                                            1.51 kB         169 kB
├ ƒ /admin/search-engine-test                                                3.05 kB         171 kB
├ ƒ /admin/servers                                                           16.6 kB         241 kB
├ ƒ /admin/task-manager                                                      7.54 kB         175 kB
├ ƒ /admin/tool-limits                                                        1.5 kB         169 kB
├ ƒ /admin/transcriptions                                                    5.71 kB         179 kB
├ ƒ /admin/usage                                                                7 kB         184 kB
├ ƒ /admin/users                                                             3.44 kB         171 kB
├ ƒ /admin/users/[userId]                                                    3.37 kB         171 kB
├ ƒ /admin/voice-input-test                                                  4.56 kB         172 kB
├ ƒ /agents                                                                    267 B         1.1 MB
├ ƒ /agents/[agentName]                                                        156 B         211 kB
├ ƒ /agents/[agentName]/api/book                                               484 B         102 kB
├ ƒ /agents/[agentName]/api/book/history                                       484 B         102 kB
├ ƒ /agents/[agentName]/api/book/missing-agent                                 484 B         102 kB
├ ƒ /agents/[agentName]/api/book/reference-diagnostics                         484 B         102 kB
├ ƒ /agents/[agentName]/api/calendar-connections                               484 B         102 kB
├ ƒ /agents/[agentName]/api/calendar-connections/[connectionId]/disconnect     484 B         102 kB
├ ƒ /agents/[agentName]/api/calendar-events                                    484 B         102 kB
├ ƒ /agents/[agentName]/api/chat                                               484 B         102 kB
├ ƒ /agents/[agentName]/api/feedback                                           484 B         102 kB
├ ƒ /agents/[agentName]/api/mcp                                                484 B         102 kB
├ ƒ /agents/[agentName]/api/meta-disclaimer                                    484 B         102 kB
├ ƒ /agents/[agentName]/api/model-requirements                                 484 B         102 kB
├ ƒ /agents/[agentName]/api/model-requirements/system-message                  484 B         102 kB
├ ƒ /agents/[agentName]/api/openai/chat/completions                            484 B         102 kB
├ ƒ /agents/[agentName]/api/openai/models                                      484 B         102 kB
├ ƒ /agents/[agentName]/api/openai/v1/chat/completions                         484 B         102 kB
├ ƒ /agents/[agentName]/api/openai/v1/models                                   484 B         102 kB
├ ƒ /agents/[agentName]/api/openrouter/chat/completions                        484 B         102 kB
├ ƒ /agents/[agentName]/api/profile                                            484 B         102 kB
├ ƒ /agents/[agentName]/api/share-target/[shareTargetId]/consume               484 B         102 kB
├ ƒ /agents/[agentName]/api/timeouts                                           484 B         102 kB
├ ƒ /agents/[agentName]/api/timeouts/[timeoutId]                               484 B         102 kB
├ ƒ /agents/[agentName]/api/timeouts/actions                                   484 B         102 kB
├ ƒ /agents/[agentName]/api/user-chats                                         484 B         102 kB
├ ƒ /agents/[agentName]/api/user-chats/[chatId]                                484 B         102 kB
├ ƒ /agents/[agentName]/api/user-chats/[chatId]/draft                          484 B         102 kB
├ ƒ /agents/[agentName]/api/user-chats/[chatId]/jobs/[jobId]/cancel            484 B         102 kB
├ ƒ /agents/[agentName]/api/user-chats/[chatId]/messages                       484 B         102 kB
├ ƒ /agents/[agentName]/api/user-chats/[chatId]/stream                         484 B         102 kB
├ ƒ /agents/[agentName]/api/user-chats/[chatId]/timeouts/[timeoutId]/cancel    484 B         102 kB
├ ƒ /agents/[agentName]/api/voice                                              484 B         102 kB
├ ƒ /agents/[agentName]/book                                                  4.5 kB         573 kB
├ ƒ /agents/[agentName]/book+chat                                            15.7 kB        1.07 MB
├ ƒ /agents/[agentName]/chat                                                 30.6 kB        1.08 MB
├ ƒ /agents/[agentName]/export-as-transpiled-code                            3.24 kB         110 kB
├ ƒ /agents/[agentName]/export-as-transpiled-code/api                          484 B         102 kB
├ ƒ /agents/[agentName]/history                                              2.04 kB         104 kB
├ ƒ /agents/[agentName]/iframe                                                 484 B         102 kB
├ ƒ /agents/[agentName]/images                                                 172 B         106 kB
├ ƒ /agents/[agentName]/images/default-avatar.png                              484 B         102 kB
├ ƒ /agents/[agentName]/images/icon-256.png                                    484 B         102 kB
├ ƒ /agents/[agentName]/images/screenshot-fullhd.png                           484 B         102 kB
├ ƒ /agents/[agentName]/images/screenshot-phone.png                            484 B         102 kB
├ ƒ /agents/[agentName]/integration                                          8.69 kB         188 kB
├ ƒ /agents/[agentName]/opengraph-image                                        484 B         102 kB
├ ƒ /agents/[agentName]/share-target                                           484 B         102 kB
├ ƒ /agents/[agentName]/system-message                                       1.35 kB         487 kB
├ ƒ /agents/[agentName]/textarea                                             8.94 kB         184 kB
├ ƒ /agents/[agentName]/timeouts                                             6.98 kB         112 kB
├ ƒ /agents/[agentName]/website-integration                                  3.04 kB         969 kB
├ ƒ /api/admin-email                                                           484 B         102 kB
├ ƒ /api/admin/backups/books                                                   484 B         102 kB
├ ƒ /api/admin/backups/server                                                  484 B         102 kB
├ ƒ /api/admin/chat-tasks                                                      484 B         102 kB
├ ƒ /api/admin/chat-tasks/[taskId]/cancel                                      484 B         102 kB
├ ƒ /api/admin/chat-tasks/[taskId]/retry                                       484 B         102 kB
├ ƒ /api/admin/error-simulation                                                484 B         102 kB
├ ƒ /api/admin/limits                                                          484 B         102 kB
├ ƒ /api/admin/servers                                                         484 B         102 kB
├ ƒ /api/admin/servers/[serverId]                                              484 B         102 kB
├ ƒ /api/admin/servers/[serverId]/migrate                                      484 B         102 kB
├ ƒ /api/admin/tool-limits                                                     484 B         102 kB
├ ƒ /api/agent-folders                                                         484 B         102 kB
├ ƒ /api/agent-folders/[folderId]                                              484 B         102 kB
├ ƒ /api/agent-folders/[folderId]/restore                                      484 B         102 kB
├ ƒ /api/agent-folders/[folderId]/visibility                                   484 B         102 kB
├ ƒ /api/agent-organization                                                    484 B         102 kB
├ ƒ /api/agents                                                                484 B         102 kB
├ ƒ /api/agents/[agentName]                                                    484 B         102 kB
├ ƒ /api/agents/[agentName]/clone                                              484 B         102 kB
├ ƒ /api/agents/[agentName]/restore                                            484 B         102 kB
├ ƒ /api/api-tokens                                                            484 B         102 kB
├ ƒ /api/auth/change-password                                                  484 B         102 kB
├ ƒ /api/auth/login                                                            484 B         102 kB
├ ƒ /api/auth/logout                                                           484 B         102 kB
├ ƒ /api/browser-artifacts/[artifactName]                                      484 B         102 kB
├ ƒ /api/browser-test/act                                                      484 B         102 kB
├ ƒ /api/browser-test/screenshot                                               484 B         102 kB
├ ƒ /api/browser-test/scroll-facebook                                          484 B         102 kB
├ ƒ /api/calendar-oauth/callback                                               484 B         102 kB
├ ƒ /api/calendar-oauth/connect                                                484 B         102 kB
├ ƒ /api/calendar-oauth/refresh                                                484 B         102 kB
├ ƒ /api/calendar-oauth/revoke                                                 484 B         102 kB
├ ƒ /api/calendar-oauth/status                                                 484 B         102 kB
├ ƒ /api/chat                                                                  484 B         102 kB
├ ƒ /api/chat-feedback                                                         484 B         102 kB
├ ƒ /api/chat-feedback/[id]                                                    484 B         102 kB
├ ƒ /api/chat-feedback/export                                                  484 B         102 kB
├ ƒ /api/chat-history                                                          484 B         102 kB
├ ƒ /api/chat-history/[id]                                                     484 B         102 kB
├ ƒ /api/chat-history/export                                                   484 B         102 kB
├ ƒ /api/chat-streaming                                                        484 B         102 kB
├ ƒ /api/custom-css                                                            484 B         102 kB
├ ƒ /api/custom-js                                                             484 B         102 kB
├ ƒ /api/docs/book-language.md                                                 484 B         102 kB
├ ƒ /api/docs/book.md                                                          484 B         102 kB
├ ƒ /api/elevenlabs/tts                                                        484 B         102 kB
├ ƒ /api/emails/incoming/sendgrid                                              484 B         102 kB
├ ƒ /api/embed.js                                                              484 B         102 kB
├ ƒ /api/error-reports/application                                             484 B         102 kB
├ ƒ /api/federated-agents                                                      484 B         102 kB
├ ƒ /api/github-app/callback                                                   484 B         102 kB
├ ƒ /api/github-app/connect                                                    484 B         102 kB
├ ƒ /api/github-app/status                                                     484 B         102 kB
├ ƒ /api/images/[filename]                                                     484 B         102 kB
├ ƒ /api/internal/user-chat-jobs/run                                           484 B         102 kB
├ ƒ /api/internal/user-chat-timeouts/run                                       484 B         102 kB
├ ƒ /api/long-running-task                                                     484 B         102 kB
├ ƒ /api/long-streaming                                                        484 B         102 kB
├ ƒ /api/messages                                                              484 B         102 kB
├ ƒ /api/metadata                                                              484 B         102 kB
├ ƒ /api/openai/v1/audio/transcriptions                                        484 B         102 kB
├ ƒ /api/openai/v1/chat/completions                                            484 B         102 kB
├ ƒ /api/openai/v1/models                                                      484 B         102 kB
├ ƒ /api/profile                                                               484 B         102 kB
├ ƒ /api/push-subscriptions                                                    484 B         102 kB
├ ƒ /api/scrape                                                                484 B         102 kB
├ ƒ /api/search                                                                484 B         102 kB
├ ƒ /api/send-email                                                            484 B         102 kB
├ ƒ /api/settings/keybindings                                                  484 B         102 kB
├ ƒ /api/settings/notifications                                                484 B         102 kB
├ ƒ /api/spawn-agent                                                           484 B         102 kB
├ ƒ /api/story/export                                                          484 B         102 kB
├ ƒ /api/system/mocked-chats                                                   484 B         102 kB
├ ƒ /api/team-agent-profile                                                    484 B         102 kB
├ ƒ /api/upload                                                                484 B         102 kB
├ ƒ /api/usage                                                                 484 B         102 kB
├ ƒ /api/user-memory                                                           484 B         102 kB
├ ƒ /api/user-memory/[memoryId]                                                484 B         102 kB
├ ƒ /api/user-wallet                                                           484 B         102 kB
├ ƒ /api/user-wallet/[walletId]                                                484 B         102 kB
├ ƒ /api/users                                                                 484 B         102 kB
├ ƒ /api/users/[username]                                                      484 B         102 kB
├ ƒ /api/v1/agents                                                             484 B         102 kB
├ ƒ /api/v1/agents/[agentId]                                                   484 B         102 kB
├ ƒ /api/v1/folders                                                            484 B         102 kB
├ ƒ /api/v1/folders/[folderId]                                                 484 B         102 kB
├ ƒ /api/v1/folders/[folderId]/agents/[agentId]                                484 B         102 kB
├ ƒ /api/v1/instance                                                           484 B         102 kB
├ ƒ /api/v1/me                                                                 484 B         102 kB
├ ƒ /dashboard                                                               2.68 kB         1.1 MB
├ ƒ /docs                                                                    3.03 kB         972 kB
├ ƒ /docs/[docId]                                                            3.02 kB         972 kB
├ ƒ /embed                                                                   7.24 kB         949 kB
├ ƒ /experiments/story                                                       5.43 kB         497 kB
├ ƒ /humans.txt                                                                484 B         102 kB
├ ƒ /manifest.webmanifest                                                      484 B         102 kB
├ ƒ /openapi.json                                                              484 B         102 kB
├ ƒ /pixel-agents-assets/[...assetPath]                                        484 B         102 kB
├ ƒ /recycle-bin                                                             4.71 kB         195 kB
├ ƒ /restricted                                                                172 B         106 kB
├ ƒ /robots.txt                                                                484 B         102 kB
├ ƒ /search                                                                  5.89 kB         111 kB
├ ƒ /security.txt                                                              484 B         102 kB
├ ƒ /sitemap.xml                                                               484 B         102 kB
├ ƒ /story/[[...story]]                                                        484 B         102 kB
├ ƒ /swagger                                                                 2.72 kB         168 kB
├ ƒ /system/profile                                                          2.73 kB         171 kB
├ ƒ /system/settings                                                         5.21 kB         167 kB
├ ƒ /system/user-memory                                                      3.91 kB         172 kB
├ ƒ /system/user-wallet                                                      7.79 kB         182 kB
├ ƒ /system/utilities                                                        1.51 kB         169 kB
├ ƒ /system/utilities/mocked-chats                                           17.6 kB         186 kB
├ ƒ /system/utilities/mocked-chats/view                                      5.47 kB         690 kB
├ ƒ /test/og-image                                                             484 B         102 kB
└ ƒ /test/og-image/opengraph-image                                             484 B         102 kB
+ First Load JS shared by all                                                 102 kB
  ├ chunks/1902-6c00c0ddb76957dc.js                                          44.4 kB
  ├ chunks/87c73c54-095cf9a90cf9ee03.js                                      54.1 kB
  └ other shared chunks (total)                                              3.48 kB

Route (pages)                                                                   Size  First Load JS
┌   /_app                                                                        0 B          93 kB
└ ○ /500 (1779 ms)                                                           4.77 kB        97.7 kB
+ First Load JS shared by all                                                 117 kB
  ├ chunks/framework-3ba54322c368b951.js                                     57.7 kB
  ├ chunks/main-66c18b52bbdfdff9.js                                          31.8 kB
  ├ css/b1f7517bfd677245.css                                                 23.6 kB
  └ other shared chunks (total)                                              3.55 kB

ƒ Middleware                                                                  162 kB

○  (Static)   prerendered as static content
ƒ  (Dynamic)  server-rendered on demand

   ▲ Next.js 15.4.11
   - Local:        http://localhost:4440
   - Network:      http://172.18.96.1:4440

 ✓ Starting...
🚀 Checking database migrations automatically for prefix "server_PavolHejny_"
 ✓ Ready in 3.4s
📂 Found 65 migration files
📋 Found 1 prefixes to migrate: server_PavolHejny_
🔌 Connected to database
🔒 Checking migration lock without waiting
🔒 Migration lock acquired

🏗️ Migrating prefix: "server_PavolHejny_"
✅ Automatic database migration check finished for prefix "server_PavolHejny_"
(node:55036) [DEP0040] DeprecationWarning: The `punycode` module is deprecated. Please use a userland alternative instead.
(Use `node --trace-deprecation ...` to show where the warning was created)
importAgent "https://core-test.ptbk.io/agents/adam"
Prerendered home page and saved to C:\Users\me\work\ai\promptbook\apps\agents-server\.next\prerendered\home.html
"🎉 All tests passed!"

=== test shell finished at 2026-04-18T08:05:01.175Z ===
Status: succeeded

