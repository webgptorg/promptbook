name: 🧪 Types

on:
    push:
        branches:
            - main
    pull_request:
        branches:
            - main

jobs:
    test-types:
        name: 🧪 Test types
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-node@v4
              with:
                  node-version: 22

            - name: Install dependencies
              run: npm ci

            - name: Test types
              run: tsc
