{
    "name": "playground",
    "scripts": {
        "dev": "(npx kill-port 4023 || true) && next dev -p 4023",
        "test-build": "npm run build",
        "build": "(npx kill-port 4023 || true) && next build",
        "start": "next start",
        "lint": "next lint",
        "postinstall": "cd ../../ && npm ci"
    }
}
