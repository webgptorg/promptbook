/**
 * Path of the admin page that tracks the presence of the bundled core agents and explains why each one exists.
 *
 * @private constant shared by every place offering to reinstate the core agents
 */
export const CORE_AGENTS_ADMIN_HREF = '/admin/core-agents';
