/**
 * Internal prompt parameter key used to pass the selected user chat id into the agent chat API.
 */
export const USER_CHAT_ID_PROMPT_PARAMETER = '__promptbook_user_chat_id';

/**
 * Polling interval used by user-chat pages to synchronize messages across windows/devices.
 */
export const USER_CHAT_SYNC_POLL_INTERVAL_MS = 5_000;
