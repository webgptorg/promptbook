/**
 * Wallet service id used for the persistent per-agent browser profile record.
 */
export const AGENT_BROWSER_PROFILE_WALLET_SERVICE = 'browser';

/**
 * Wallet key used for the persistent per-agent browser profile record.
 */
export const AGENT_BROWSER_PROFILE_WALLET_KEY = 'agent-browser-profile';
