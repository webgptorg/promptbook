/**
 * HTML entrypoint filenames recognized at the root of an agent project.
 *
 * The order matches the static project server's directory-index priority.
 */
export const AGENT_PROJECT_INDEX_HTML_FILE_NAMES = ['index.html', 'index.htm'] as const;
