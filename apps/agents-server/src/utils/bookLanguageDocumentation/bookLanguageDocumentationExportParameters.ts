/**
 * URL parameter opting into the low-level commitments chapter of the manual.
 *
 * The chapter targets advanced authors only, therefore it is opt-in.
 *
 * This module intentionally holds no server-only imports, so both the export
 * routes and the client-side docs toolbar can share one parameter name.
 */
export const BOOK_LANGUAGE_DOCUMENTATION_LOW_LEVEL_COMMITMENTS_PARAMETER = 'lowLevelCommitments';
