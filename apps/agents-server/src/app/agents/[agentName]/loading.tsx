import { AgentProfileLoadingSkeleton } from '../../../components/Skeleton/AgentProfileLoadingSkeleton';

/**
 * Renders the agent-profile skeleton while dynamic agent routes stream.
 */
export default function Loading() {
    return <AgentProfileLoadingSkeleton />;
}
