/**
 * Shared card styling for `/admin/update` cards that should size to their content inside stacked grids.
 *
 * @private constant of `/admin/update`
 */
export const UPDATE_PAGE_CARD_CLASS_NAME = '!h-auto min-w-0 hover:border-gray-200 hover:shadow-md';
