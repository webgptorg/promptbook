/**
 * Local storage key for the private mode preference.
 *
 * @private Shared helper for the Agents Server UI.
 */
export const PRIVATE_MODE_STORAGE_KEY = 'promptbook-private-mode-enabled';

/**
 * Cookie name that advertises private mode state to the server.
 *
 * @private Shared helper for Agents Server HTTP handlers.
 */
export const PRIVATE_MODE_COOKIE_NAME = 'promptbook-private-mode-enabled';
