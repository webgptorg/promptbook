{
    "name": "promptbook-coder-landing",
    "scripts": {
        "predev": "npx kill-port 4442 || exit 0",
        "dev": "next dev -p 4442",
        "test": "npm run lint && npm run test-build",
        "test-build": "next build",
        "prebuild": "npx kill-port 4442 || exit 0",
        "build": "next build",
        "start": "next start -p 4442",
        "lint": "eslint src --max-warnings=0",
        "postinstall": "cd ../../ && npm ci"
    }
}
